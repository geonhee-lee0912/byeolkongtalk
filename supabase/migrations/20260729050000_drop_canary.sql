-- 카나리아 제거 (2026-07-29)
--
-- 20260729020000 이 심고 040000 이 재생성한 검증용 객체를 걷어낸다.
-- 역할은 끝났다 — 기본 권한 회수가 "썼다"가 아니라 "먹었다"를 실물로 증명했다:
--
--   020000 직후 : 테이블 anon 401 ✅ / 함수 anon **200** ❌ (per-schema 로는 PUBLIC 을 못 뺀다)
--   040000 직후 : 테이블 anon 401 ✅ / 함수 anon **401** ✅ (전역 회수로 해결)
--
-- 카나리아가 없었으면 020000 에서 "설정 바뀌었네" 하고 넘어갔을 것이고, 함수는 열린 채로
-- 남았을 것이다. 다음에 권한 기본값을 건드릴 때도 같은 방식으로 검증할 것.

DROP FUNCTION IF EXISTS _canary_default_privs_fn();
DROP TABLE IF EXISTS _canary_default_privs;
