// 별마루 하루 이름 — 십신 10종 × 원인 마크. 순수(네트워크·LLM 0).
// 🔴 판정 엔진(day-score.ts)을 건드리지 않는다. 십신은 점수가 쓰는 elementRelation(5종)을
//    음양으로 쪼갠 것이라(생아=정인·편인, 아극=정재·편재 …) 새 계산이 아니고 등급도 그대로다.
// 🔴 기존 TEN_GOD_LABEL(lib/saju/pairing.ts)은 "사람"용(별자리 지도)이라 여기서 쓰지 않는다.
//    같은 십신이라도 "날"은 다른 어휘가 필요하다(예: 정인 = 날 감싸주는 사람 ↔ 날 감싸주는 결).
import type { TenGod } from "@/lib/saju/pairing";
import type { DayFactors } from "./day-score";

/** 하루 이름 — 히어로/상세 제목. 단정 금지(페르소나 화법): "좋다/나쁘다"가 아니라 결의 이름. */
export const DAY_NAME: Record<TenGod, string> = {
  정인: "날 감싸주는 결",
  편인: "생각이 깊어지는 결",
  비견: "내 결대로 가는 날",
  겁재: "티격태격 붙는 날",
  식신: "여유가 도는 날",
  상관: "끼가 튀는 날",
  정재: "차곡차곡 쌓는 날",
  편재: "판을 벌이기 좋은 날",
  정관: "자리가 잡히는 날",
  편관: "긴장이 도는 날",
};

/** 이름 바로 아래 한 줄. ⑥ 본문 뱅크(tone × relation)와 자리가 달라 겹치지 않는다.
 *  🔴 한 줄은 **톤-블라인드**다(등급을 모른 채 십신으로만 정해진다). 그래서 caution 비율이 높은
 *     십신(1회성 실측 3,600칸: 편관 83% · 정관 37% · 식신 32%, day-score.ts dayGrade 임계 70/45
 *     기준)의 줄을 낙관 일변도로 쓰면 안 된다 — 같은 화면의 등급 라벨("살짝 챙길 날")과 정면으로
 *     어긋난다. 양면을 담거나 출구를 남길 것.
 *     재측정: `node --import tsx scripts/byeolmaru-label-freq.ts` 의 "십신 × 등급 교차" 섹션이
 *     이 수치를 다시 잰다 — day-score.ts 가중치가 바뀌면 이 JSDoc 숫자보다 그 출력을 믿을 것.
 *  🔴 각 줄은 46~53자·2문장(다른 9개와 같은 결)을 벗어나지 않게 유지할 것 — 히어로 카드
 *     (`text-xs leading-relaxed`, 375px 폭)에서 유독 긴 줄만 3줄로 흘러 눈에 띈다. */
export const DAY_LINE: Record<TenGod, string> = {
  정인: "기대도 되는 날이야. 먼저 손 내밀어도 받아주는 결이라, 혼자 끌어안고 있던 걸 꺼내보기 좋아.",
  편인: "바깥보다 안쪽이 도는 날이야. 답을 서둘러 내기보다 재료를 모아두면 나중이 편해져.",
  비견: "누구 눈치 볼 일 없이 네 페이스가 서는 날이야. 미뤄둔 걸 네 방식대로 밀어도 좋아.",
  겁재: "비슷한 결끼리 부딪히는 날이야. 지는 것 같아도 한 발 물러서면 오히려 빨리 정리돼.",
  식신: "잘 먹고 잘 쉬는 게 오늘은 제일 쓸모 있는 날이야. 늘어지면 하나만 가볍게 붙잡아봐.",
  상관: "말과 표현이 앞서 나가는 날이야. 재밌게 풀릴 수도, 한 마디가 길게 남을 수도 있어.",
  정재: "크게 벌리기보다 있는 걸 단단히 하는 날이야. 숫자나 약속을 한 번 더 확인해두면 좋아.",
  편재: "손이 넓어지는 날이야. 새로 벌인 게 어디까지 갈지는 아직 모르니 가볍게 시작해봐.",
  정관: "규칙이나 절차가 앞서는 날이야. 정리를 끝내면 개운하고, 조이면 하나씩만 봐도 돼.",
  편관: "밀어붙이는 힘이 바깥에서 들어오는 날이야. 다 받아내지 않아도 돼, 하나만 골라봐.",
};

export type DayGlyph = "✧" | "◇" | "△" | "＋";

export interface DayMark {
  glyph: DayGlyph;
  label: string;
}

/**
 * 그날을 그렇게 만든 원인 마크. 달력 셀 우상단 + 상세 칩에 쓴다.
 * 우리 오늘 셀의 ✨끌림·🔗결속과 같은 어법 — 두 탭이 한 문법을 쓰게 한다.
 * 순서는 고정(천간합 → 육합 → 충 → 빈 곳)이라 같은 날이 늘 같은 순서로 보인다.
 */
export function dayMarks(f: DayFactors): DayMark[] {
  const out: DayMark[] = [];
  if (f.heavenlyCombo) out.push({ glyph: "✧", label: "천간합" });
  if (f.sixCombo) out.push({ glyph: "◇", label: "육합" });
  if (f.clash) out.push({ glyph: "△", label: "충" });
  // scarce(1개)는 마크를 붙이지 않는다 — "아예 없던 게 채워지는" 날만 눈에 띄게 한다.
  if (f.scarcity === "absent") out.push({ glyph: "＋", label: "빈 곳 채움" });
  return out;
}
