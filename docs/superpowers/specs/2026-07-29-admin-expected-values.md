# 어드민 집계 정답지 — RPC 전환 기대값 + 판독 스냅샷 (2026-07-29)

> **용도 2가지**: ①어드민 집계 RPC 전환(플랜 A/B)의 **검증 기대값** ②판정 사이클 스냅샷의 **판독 원본**.
> 재생성: `SUPABASE_PAT=<값> node scripts/run-prod-query.mjs scripts/admin-expected-values.sql`
> 설계 정본: `specs/2026-07-29-admin-aggregation-rpc-and-visitor-mix-design.md` · 실행 플랜: `plans/2026-07-29-admin-rpc-a-traffic-visitor-mix.md`

## 실행 조건 (이걸 안 맞추면 대조가 성립하지 않는다)

| 항목 | 값 |
|---|---|
| 실행 시각 | `2026-07-29T09:20:47Z` = **2026-07-29 18:20 KST** |
| 오늘 버킷 (오전 10시 롤오버) | **`2026-07-29`** = 7/29 10:00 ~ 7/30 10:00 KST → **약 8시간 경과한 진행 중 버킷** |
| 판정 사이클 | day 0 = 2026-07-26 → 오늘은 **d3**. d4 는 7/30 (같은 파일 재실행) |
| **어드민 제외** | **미적용.** 스펙 §6-5 고정점(628/400/320)이 미제외 기준이라 조건을 맞췄다 |
| 봇 | `is_bot = false` 로 제외. 단 `bot` 지표만 봇 포함(분모) |
| 조회창 | **창 필터 없음 = 전량.** `page_views` 첫 행이 2026-07-25(비콘 배포일)라 30일 창과 결과가 같다. 데이터가 30일을 넘기면 이 파일에 `where created_at >= now() - interval '30 days'` 를 넣어야 앱과 대조된다 |

⚠️ **앱 값이 정답지보다 조금 작은 것이 정상이다** (어드민 제외분). 차이가 어드민 활동량을 넘으면 드리프트다.

---

## 1. 일별 PV / UV (`trend`)

| 버킷 | PV | UV | PV/UV | 유저만 PV | 유저만 UV | 유저 PV/UV |
|---|---|---|---|---|---|---|
| 2026-07-25 | 488 | 65 | 7.5 | 455 | 64 | 7.1 |
| 2026-07-26 | 611 | 79 | 7.7 | 520 | 78 | 6.7 |
| 2026-07-27 | 809 | 57 | 14.2 | 742 | 55 | **13.5** |
| 2026-07-28 | 426 | 49 | 8.7 | 347 | 47 | 7.4 |
| 2026-07-29 * | 66 | 7 | 9.4 | 29 | 5 | 5.8 |

`*` 진행 중 버킷(8시간). **추세 판단에 쓰지 말 것.**
"유저만" 열은 `path not like '/admin%'` — 정답지 본체에는 없는 보조 조회다(07-27 이상치 원인 규명용).

## 2. 방문자 구성 (`visitor_mix`) — 🆕 이번 전환의 신규 지표

| 버킷 | UV | 신규 | 연속 | 복귀 | 재방문 | **재방문율** |
|---|---|---|---|---|---|---|
| 2026-07-25 | 65 | 65 | 0 | 0 | 0 | 0.0% |
| 2026-07-26 | 79 | 76 | 3 | 0 | 3 | 3.8% |
| 2026-07-27 | 57 | 52 | 3 | 2 | 5 | 8.8% |
| 2026-07-28 | 49 | 43 | 6 | 0 | 6 | **12.2%** |
| 2026-07-29 * | 7 | 3 | 4 | 0 | 4 | 57.1% * |

✅ **Step 3 검증 통과** — 5버킷 전부 `신규+연속+복귀 = UV`.
`*` 진행 중 버킷. 하루 초반에는 신규 유입이 아직 안 쌓여 재방문율이 구조적으로 과대하다.

## 3. 봇 비율 (`bot`)

| 전체 PV(봇 포함) | 봇 PV | 봇 비율 |
|---|---|---|
| 2,403 | 3 | **0.1%** |

봇 제외 PV 합 2,400 = 2,403 − 3 ✅ (`trend` 5버킷 합과 일치)

## 4. 라우트별 상위 20 (`routes`)

| # | path | PV | UV | PV/UV |
|---|---|---|---|---|
| 1 | `/start` | 326 | 176 | 1.9 |
| 2 | `/` | 272 | 110 | 2.5 |
| 3 | `/login` | 238 | 148 | 1.6 |
| 4 | `/tarot` | 218 | 111 | 2.0 |
| 5 | `/concern` | 216 | 116 | 1.9 |
| 6 | `/admin` | 213 | 3 | 71.0 |
| 7 | `/tarot/draw` | 181 | 103 | 1.8 |
| 8 | `/tarot/reading` | 132 | 102 | 1.3 |
| 9 | `/relationship` | 109 | 64 | 1.7 |
| 10 | `/shop` | 106 | 39 | 2.7 |
| 11 | `/tarot/result` | 96 | 68 | 1.4 |
| 12 | `/mypage` | 79 | 39 | 2.0 |
| 13 | `/fortune` | 44 | 26 | 1.7 |
| 14 | `/readings` | 31 | 17 | 1.8 |
| 15 | `/admin/traffic` | 25 | 3 | 8.3 |
| 16 | `/fortune/daily` | 13 | 8 | 1.6 |
| 17 | `/admin/users` | 13 | 3 | 4.3 |
| 18 | `/admin/errors` | 10 | 3 | 3.3 |
| 19 | `/admin/readings/:id` | 9 | 2 | 4.5 |
| 20 | `/fortune/result` | 8 | 6 | 1.3 |

정렬 = PV desc, UV desc (RPC `admin_traffic_routes` 와 동일 순서여야 한다).
어드민 라우트 5개 = PV 270. 앱 화면에서는 제외되어 이 자리에 다른 라우트가 올라온다.

## 5. 로그인 전 / 후 (`auth`)

| segment | UV | PV |
|---|---|---|
| guest | 226 | 665 |
| member | 121 | 1,735 |

⚠️ 두 UV 합(347)은 전체 UV 가 아니다 — **같은 `anon_id` 가 가입 순간 양쪽에 나타난다**(설계상 정상, 스펙 §5).

## 6. 상담 퍼널 고정점 (`consult_funnel`)

30일 창, `emotion_tag not like 'fortune:%'`(운세 제외), 어드민 미제외.

| | 2026-07-28 (스펙 §6-5 고정점) | **2026-07-29 (이 문서)** | Δ |
|---|---|---|---|
| 시작(readings) | 628 | **642** | +14 |
| 종료(`[END]`) | 400 | **406** | +6 |
| 결과 열람 | 320 | **326** | +6 |
| 완료율 | 63.7% | **63.2%** | −0.5%p |
| 열람률(완료 대비) | 80.0% | **80.3%** | +0.3%p |
| **미열람 절대수** | 80 | **80** | **0** |

🟢 하루 새 늘어난 완료 6건이 **전부 열람됐다**(미열람 절대수가 80 에서 안 움직였다).

---

## d3 판독 소견 (2026-07-29)

**1. 재방문율 — 🔴 최초 기록(3.8/8.8/12.2%)은 틀렸다. 정정한다.**

> **정정 (2026-07-29, 화면 대조 중 발견)**: 위 §2 표는 **어드민 미제외**다. 그런데 **운영자 계정이 매일 들어오므로 "연속"에 통째로 잡힌다** — 07-28 은 UV 49→46(−3)인데 그 −3 이 **전부 연속**이었다(6→3). 즉 최초 소견의 상승분 절반이 운영자 트래픽이었다.
> 정답지를 어드민 미제외로 뜬 건 퍼널 고정점(628/400/320)과 조건을 맞추기 위함인데, **방문자 구성만큼은 그 조건이 지표를 망친다**(운영자는 정의상 100% 연속 방문자다). → **재방문율 판독은 반드시 어드민 제외 기준으로.**

**어드민 제외 실측 (화면 값과 일치 확인):**

| 버킷 | UV | 신규 | 연속 | 복귀 | 재방문율 |
|---|---|---|---|---|---|
| 07-25 | 63 | 63 | 0 | 0 | 0.0% |
| 07-26 | 77 | 76 | 1 | 0 | **1.3%** |
| 07-27 | 54 | 51 | 1 | 2 | **5.6%** |
| 07-28 | 46 | 43 | 3 | 0 | **6.5%** |

우상향은 맞지만 **최초 보고의 절반**이고, 절대수로는 07-28 에 재방문 **3명**이다. "W5 재방문 0 의 반증"이라 부르기엔 약하다 — 방향만 있고 크기가 없다.

**2. 🔴 획득 소재별로 쪼개니 진짜 그림이 나왔다 — 광고 유입은 돌아오지 않는다.**

| 획득 소재 | 방문자 | 2일 이상 방문 | 재방문율 |
|---|---|---|---|
| `love` (광고) | 173 | **1** | 0.6% |
| `relationship` (광고) | 45 | **0** | 0.0% |
| `(직접/오가닉)` | 20 | 5 | 25.0% |

돌아올 기회가 있던 사람(첫 방문 ≤ 07-27)으로 좁혀도 같다 — 광고 174명 중 1명, 오가닉 16명 중 5명.
**전체 재방문율이 오르는 것처럼 보인 건 오가닉 소수가 반복 방문하기 때문이지, 광고 코호트가 살아난 게 아니다.**

⚠️ **판정 전 확인할 교란 2개** (지금 데이터로는 못 가른다):
- **인앱 브라우저** — 광고 유입은 Meta/Instagram 웹뷰로 들어온다. 웹뷰 스토리지가 분리되면 재방문이 새 `anon_id` 로 잡혀 **광고 코호트만 구조적으로 과소**된다. 광고 0.6% 는 하한값일 수 있다.
- **오가닉 20명의 정체** — 어드민 제외는 `user_id` 기준이라 **운영자의 비로그인·시크릿 브라우징은 안 걸러진다**(AGENTS.md 기재된 한계). 오가닉 25% 의 분자 5명에 자기 테스트가 섞였을 수 있다. n=20 이라 1~2명이 결과를 뒤집는다.

그래도 **분모가 큰 쪽(광고 218명 중 1명)** 은 교란에 덜 흔들린다. d7(8/2)·d14(8/9) 판정에서 이 축을 꼭 볼 것 — CAC ₩505 회수 13% 문제와 직결된다([[pnl-findings-2026-07-25]]). 재방문이 없으면 LTV ≈ 첫 세션 가치이고, 리텐션으로 광고비를 회수할 경로가 없다.

**2. 07-27 PV/UV 13.5 는 행동 변화가 아니라 소수 헤비 세션이다.**
어드민 제외 후에도 남아(어드민 PV 는 67뿐) 실제 유저 현상인데, 상위 5 `anon_id` 가 유저 PV 742 중 **222(30%)** 를 만들었다(최다 56 PV). 5명 모두 로그인 유저이고 제외 목록에 없다. → 평균 지표로 07-27 을 읽으면 안 된다.

**3. 상담 퍼널은 63% 완료 / 80% 열람에서 안정.**
`/tarot/reading` UV 102 → `/tarot/result` UV 68 (**−33%**) 의 라우트 낙차가 미열람 동선 문제와 같은 방향을 가리킨다. 단 분모가 다르므로(UV vs readings) 두 수치를 같은 것으로 읽지 말 것. 미열람 픽스는 d14(8/9) 슬롯 그대로 두면 된다 — **절대수가 안 늘고 있다.**

**4. 봇 0.1% = 계측 건강.** 단 UA 기반 판정이라 하한값이다.

**5. 유입 지표(`landing_variant`·`utm_content`)는 이 정답지에 없다.** `admin_traffic_entry` 는 first-touch 귀속이라 SQL 이 길고, 대조는 Task 4 Step 2 에서 함수 본문 그대로 돌려 맞춘다.

---

## 전환 후 대조 결과 (2026-07-29, prod 배포 직후)

`/admin` 화면 대신 **배포된 prod 함수 본문 ↔ 정답지 기준 SQL 을 한 쿼리(같은 스냅샷) 안에서 diff** 했다.
화면 판독으로 대조하지 않은 이유: `scripts/run-prod-query.mjs` 는 `supabase_read_only_user` 로 도는데
이번 마이그레이션이 RPC EXECUTE 를 `service_role` 로 좁혀 그 롤로는 함수를 **호출할 수 없다**(권한이
실제로 걸렸다는 증거이기도 하다). 그래서 두 갈래로 나눠 증명했다.

**① 배포된 함수 = 검증한 함수** — `md5(prosrc)` 가 prod·dev 7개 전부 동일:

| 함수 | src md5 |
|---|---|
| `admin_normalize_entry` | `84b0fe16…` |
| `admin_traffic_auth` | `679b5865…` |
| `admin_traffic_bot` | `b80628d3…` |
| `admin_traffic_entry` | `94802838…` |
| `admin_traffic_routes` | `0540b76d…` |
| `admin_traffic_trend` | `c02931a2…` |
| `admin_traffic_visitor_mix` | `e2b9dc09…` |

**② 그 함수 로직 = 정답지** — prod 실데이터, 한 스냅샷 diff:

| 대조 항목 | 불일치 |
|---|---|
| `trend` (일별 UV/PV) | **0** |
| `visitor_mix` (신규/연속/복귀) | **0** |
| `routes` (상위 20, 순서 포함) | **0** |
| `auth` (guest/member) | **0** |
| `bot` (봇 포함 분모) | **0** |
| 3분할 합 = UV | **위반 0** |

대조 시점 스냅샷 (오늘 버킷 `2026-07-29`, 진행 중):

| 버킷 | PV | UV | 신규 | 연속 | 복귀 | 재방문율 |
|---|---|---|---|---|---|---|
| 07-25 | 488 | 65 | 65 | 0 | 0 | 0.0% |
| 07-26 | 611 | 79 | 76 | 3 | 0 | 3.8% |
| 07-27 | 809 | 57 | 52 | 3 | 2 | 8.8% |
| 07-28 | 426 | 49 | 43 | 6 | 0 | 12.2% |
| 07-29 * | 87 | 9 | 5 | 4 | 0 | 44.4% * |

`*` 진행 중 버킷 — 추세 판단에 쓰지 말 것.

**앱 레이어**는 로컬 dev 서버 + dev DB 로 실렌더 검증했다(임시 어드민 쿠키, 검증 후 원복):
방문자 구성 섹션 렌더 · 전 행 `신규+연속+복귀=UV` · `/admin` UV 와 `/admin/traffic` 오늘 UV 동일 ·
유입표 3열(오늘 열 제거) · `(직접/오가닉)` 맨 아래 · 콘솔 에러 0 · 가로 오버플로 없음.
`.from("page_views")` 가 두 파일에서 소멸. 테스트 219/219, `tsc` 클린. 배포 후 3시간 `error_logs` 0건.

🙋 **남은 사람 손 1건**: prod `/admin/traffic` · `/admin` 을 실제로 열어 눈으로 확인
(에이전트는 카카오 OAuth 를 통과할 수 없다). 위 표의 07-28 행 `49 / 43 / 6 / 0 / 12.2%` 가
어드민 제외분만큼 조금 작게 보이면 정상이다.

---

## 🔴 이 작업 중 발견한 별건 사고 — RPC 가 공개 anon 키로 실행 가능했다

**`REVOKE 를 썼다 != 먹었다`.** prod 의 `charge_stars`·`spend_stars`·`purchase_relationship_pass` 가
`SECURITY DEFINER`(RLS 우회) + `anon=X` 상태였다. 기존 마이그레이션은 `REVOKE … FROM PUBLIC` 만
했는데, Supabase 의 `ALTER DEFAULT PRIVILEGES` 가 public 스키마 신규 함수에 **anon·authenticated
직접 grant** 를 붙인다 — 직접 grant 는 PUBLIC revoke 로 안 지워진다.

세 함수 모두 호출자 신원 검사가 없다(`p_user_id` 를 호출자가 넘긴다). `p_payment_id` 는 `payments`
대조 없는 순수 멱등키라 아무 문자열이 통한다. **anon 키를 쥔 사람이면 결제 없이 별을 충전할 수 있는
구성이었다.**

> ⚠️ **심각도 정정 (2026-07-29, 사후 조사)**: 최초 보고에서 "결제 없이 별 충전 가능"이라고만 썼는데,
> **실제 외부 악용 가능성은 그보다 낮았다.** 악용에는 prod anon 키가 필요한데:
> - prod 클라이언트 번들에 **없다**(홈 HTML + 청크 전수 확인). `getSupabase()` 가 죽은 코드라 트리셰이킹됐다
> - **레포가 PUBLIC 인데도** 커밋 이력에 JWT 가 **한 번도 없다**(`.env*` gitignore)
> - `getSupabase()` 가 클라이언트 컴포넌트에서 쓰인 이력도 **없다** → 과거 번들에도 실린 적 없음
> - prod 프로젝트 ref 는 노출된다(팝업 이미지 URL 이 `/api/popups` 로 로그인 유저에게 나감). 다만
>   ref 만으로는 anon JWT 를 만들 수 없다
>
> **그래도 고친 건 옳다.** anon 키는 *설계상 공개되는 값*이고 Supabase 보안 모델은 "키는 알려진다,
> 권한과 RLS 가 통제한다"를 전제한다. 키가 안 퍼진 건 통제가 아니라 **죽은 코드의 부산물**이었고,
> `getSupabase()` 를 누가 한 번만 호출하면 그날로 공개된다. 즉 **노출 직전 상태**였지 안전이 아니었다.

**픽스**: `20260729010000_revoke_rpc_execute_from_anon.sql` — 10개 함수 전부
`REVOKE … FROM PUBLIC, anon, authenticated`. prod 적용 확인(`proacl` = `{postgres=X, service_role=X}`),
anon HTTP 호출 401, service_role 200, 변경 흔적 0.

⚠️ **검증 함정**: PostgREST 는 인자 모양이 안 맞아도 **404** 를 준다(권한 거부와 구분 불가).
플랜 원문의 "404 면 안전" 판정법으로는 이 사고를 **못 잡는다** — 실제로 인자를 잘못 맞춰
3개가 404 로 나와 하마터면 안전으로 오판할 뻔했다. **함수별 정확한 인자로 호출할 것.**
회수 후에는 401 이 돌아오므로 그때는 구분이 명확하다.

**테이블은 무사**했다 — 22개 전부 RLS ON + 정책 0개 = deny-all(anon SELECT 실측 `[]`).
그래서 `SECURITY DEFINER` RPC 가 유일한 통로였고, 그게 열려 있었다.
