# 사주 중복구매 가드 Implementation Plan (Phase 1 / #1)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 같은 사주(프로필)로 이미 본 사주 상품을 다시 사면 재과금·중복생성 대신 보관함의 기존 리포트로 유도하되, 사용자가 명시적으로 원하면 재과금 재생성(force)을 허용한다.

**Architecture:** 서버 `/api/fortune/create` 가 권위 — 생성·차감 전에 "같은 user·emotion_tag·프로필" 기존 리딩을 조회해, `body.force!==true` 면 `{alreadyOwned:true, id}` 를 과금 없이 반환. 클라(단일 프로필 `/fortune/[type]`, 궁합 `CompatInput`)는 그 응답을 받으면 `AlreadyOwnedModal` 로 [보관함에서 보기]/[새로 뽑기(force)] 를 제시. 궁합은 두 프로필 id 를 `saju_data`(JSONB)에 심어 순서무관 매칭 — 마이그레이션 없음.

**Tech Stack:** Next.js 16 App Router, TypeScript, Supabase(service role), node:test + tsx(유닛).

**Scope:** daily(하루 1건)·monthly(월 1건/프로필)·tarot_*(뽑을 때마다 카드 상이)는 **대상 아님**(각자 성격이 다르며 daily/monthly 는 이미 자체 단건 로직 보유). 대상 = `saju_full`·`good_days`·신규 15종·`compat`·`compat_social`. 저장 프로필 없이 즉석 입력(usedProfileId=null)한 경우는 안정 키가 없어 가드 제외(60초 recent-dup 가드가 여전히 커버). 연간 상품의 연도 갱신 허용은 범위 밖(후속).

---

### Task 1: 기존-리딩 조회 헬퍼 (순수 로직 + DB 래퍼)

**Files:**
- Create: `lib/fortune/existing-lookup.ts`
- Test: `lib/fortune/existing-lookup.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`lib/fortune/existing-lookup.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { pickExistingReadingId } from "./existing-lookup.ts";

const row = (id: string, profile_id: string | null, saju_data: unknown = null) => ({
  id,
  profile_id,
  saju_data,
  created_at: "",
});

test("단일 프로필: profile_id 일치 시 최신 리딩 id, 없으면 null", () => {
  const rows = [row("r2", "pB"), row("r1", "pA")]; // 최신순 입력
  assert.equal(pickExistingReadingId(rows, { emotionTag: "x", profileId: "pA" }), "r1");
  assert.equal(pickExistingReadingId(rows, { emotionTag: "x", profileId: "pC" }), null);
});

test("궁합: 두 프로필 순서 무관 매칭", () => {
  const rows = [row("c1", "pA", { aId: "pA", bId: "pB" })];
  assert.equal(pickExistingReadingId(rows, { emotionTag: "x", compatPair: { aId: "pA", bId: "pB" } }), "c1");
  assert.equal(pickExistingReadingId(rows, { emotionTag: "x", compatPair: { aId: "pB", bId: "pA" } }), "c1");
  assert.equal(pickExistingReadingId(rows, { emotionTag: "x", compatPair: { aId: "pA", bId: "pC" } }), null);
});

test("궁합: saju_data 에 id 없으면(레거시 행) 매칭 안 됨", () => {
  const rows = [row("c1", "pA", { names: { a: "가", b: "나" } })];
  assert.equal(pickExistingReadingId(rows, { emotionTag: "x", compatPair: { aId: "pA", bId: "pB" } }), null);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test lib/fortune/existing-lookup.test.ts`
Expected: FAIL — `Cannot find module './existing-lookup.ts'` (또는 export 없음).

- [ ] **Step 3: 헬퍼 구현**

`lib/fortune/existing-lookup.ts`:

```ts
// 이미 본 동일 사주 상품(같은 user·emotion_tag·프로필) 리딩을 찾아 재과금·중복생성을 막는다.
// daily(하루)·monthly(월)·tarot(매 뽑기 상이)는 대상 아님 — 호출측에서 제외한다.
import { getServiceSupabase } from "@/lib/supabase";

export interface ExistingSig {
  emotionTag: string;
  /** 단일 프로필 상품(saju_full·good_days·신규 15종) */
  profileId?: string | null;
  /** 궁합(compat·compat_social) — 두 프로필, 순서 무관 */
  compatPair?: { aId: string; bId: string };
}

interface Row {
  id: string;
  profile_id: string | null;
  saju_data: unknown;
  created_at: string;
}

function sajuIds(saju_data: unknown): { aId?: string; bId?: string } {
  if (!saju_data || typeof saju_data !== "object") return {};
  const d = saju_data as Record<string, unknown>;
  return {
    aId: typeof d.aId === "string" ? d.aId : undefined,
    bId: typeof d.bId === "string" ? d.bId : undefined,
  };
}

/** rows(최신순)에서 시그니처와 일치하는 첫 리딩 id. 순수 함수 — 테스트 대상. */
export function pickExistingReadingId(rows: Row[], sig: ExistingSig): string | null {
  for (const row of rows) {
    if (sig.compatPair) {
      const { aId, bId } = sajuIds(row.saju_data);
      if (!aId || !bId) continue;
      const pair = new Set([aId, bId]);
      if (pair.size === 2 && pair.has(sig.compatPair.aId) && pair.has(sig.compatPair.bId)) {
        return row.id;
      }
    } else if (sig.profileId != null) {
      if (row.profile_id === sig.profileId) return row.id;
    }
  }
  return null;
}

/** 같은 emotion_tag 리딩을 최신순으로 받아 시그니처 일치분을 찾는다. 없으면 null. */
export async function findExistingFortuneReadingId(
  userId: string,
  sig: ExistingSig
): Promise<string | null> {
  const { data } = await getServiceSupabase()
    .from("readings")
    .select("id, profile_id, saju_data, created_at")
    .eq("user_id", userId)
    .eq("emotion_tag", sig.emotionTag)
    .order("created_at", { ascending: false })
    .limit(50);
  return pickExistingReadingId((data ?? []) as Row[], sig);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test lib/fortune/existing-lookup.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: 커밋**

```bash
git add lib/fortune/existing-lookup.ts lib/fortune/existing-lookup.test.ts
git commit -m "feat(fortune): 기존-리딩 조회 헬퍼(중복구매 가드용) + 유닛"
```

---

### Task 2: 서버 가드 배선 (`/api/fortune/create`)

**Files:**
- Modify: `app/api/fortune/create/route.ts`

- [ ] **Step 1: import 추가**

`route.ts` 상단 import 블록(다른 `@/lib/fortune/*` import 근처)에 추가:

```ts
import { findExistingFortuneReadingId } from "@/lib/fortune/existing-lookup";
```

- [ ] **Step 2: 요청 바디에 `force` 추가**

`route.ts` 의 `let body: {...}` 타입에 `force?: unknown;` 필드를 추가한다. 기존:

```ts
  let body: {
    type?: unknown;
    input?: unknown;
    profileId?: unknown;
    profileA?: unknown;
    profileB?: unknown;
    drawnCards?: unknown;
  };
```

를 아래로 교체:

```ts
  let body: {
    type?: unknown;
    input?: unknown;
    profileId?: unknown;
    profileA?: unknown;
    profileB?: unknown;
    drawnCards?: unknown;
    force?: unknown;
  };
```

- [ ] **Step 3: 궁합 프로필 B 를 이후에서 쓰도록 hoist + saju_data 에 id 저장**

`let usedProfileId: string | null = null;` 선언 바로 아래에 추가:

```ts
  let usedProfileB: string | null = null;
```

궁합 branch 의 저장 라인(현재 `sajuDataToStore = { a: saju, b: sajuB, names };` 와 `usedProfileId = profileA;`)을 아래로 교체:

```ts
    sajuDataToStore = { a: saju, b: sajuB, names, aId: profileA, bId: profileB };
    usedProfileId = profileA;
    usedProfileB = profileB;
```

- [ ] **Step 4: 이미-소유 가드 삽입**

`// 중복 생성 방어 — 더블클릭...` 주석으로 시작하는 `findRecentDuplicateReading` 블록 **바로 위**에 삽입:

```ts
  // 이미 본 동일 상품(같은 사주·프로필) → 재과금·재생성 없이 기존 리딩으로.
  // force:true(사용자가 '새로 뽑기' 선택) 면 우회. daily/monthly/tarot 는 대상 아님(성격 다름).
  if (body.force !== true && cfg.base !== "tarot" && cfg.type !== "daily" && cfg.type !== "monthly") {
    const sig =
      cfg.type === "compat" || cfg.type === "compat_social"
        ? usedProfileId && usedProfileB
          ? { emotionTag: cfg.emotionTag, compatPair: { aId: usedProfileId, bId: usedProfileB } }
          : null
        : usedProfileId
          ? { emotionTag: cfg.emotionTag, profileId: usedProfileId }
          : null;
    if (sig) {
      const existingId = await findExistingFortuneReadingId(userId, sig);
      if (existingId) {
        return NextResponse.json({ id: existingId, success: true, cost: 0, alreadyOwned: true });
      }
    }
  }
```

- [ ] **Step 5: 타입체크 + 빌드 확인**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 6: 커밋**

```bash
git add app/api/fortune/create/route.ts
git commit -m "feat(fortune): create 라우트 이미-소유 가드(force 우회) + 궁합 프로필 id 저장"
```

---

### Task 3: AlreadyOwnedModal 컴포넌트

**Files:**
- Create: `components/fortune/AlreadyOwnedModal.tsx`

- [ ] **Step 1: 컴포넌트 작성**

`components/fortune/AlreadyOwnedModal.tsx`:

```tsx
"use client";

// 이미 같은 사주로 본 상품을 다시 사려 할 때 — 보관함 유도 + 재생성(force) 선택.
import { useEffect, useState } from "react";
import { createPortal } from "react-dom";

export default function AlreadyOwnedModal({
  label,
  cost,
  onReview,
  onRegenerate,
  onClose,
}: {
  label: string;
  cost: number;
  onReview: () => void;
  onRegenerate: () => void;
  onClose: () => void;
}) {
  const [mounted, setMounted] = useState(false);
  useEffect(() => setMounted(true), []);
  if (!mounted) return null;

  return createPortal(
    <div
      className="fixed inset-0 z-[80] flex items-end sm:items-center justify-center bg-night/50 backdrop-blur-sm animate-fade-in"
      onClick={onClose}
    >
      <div
        className="w-full max-w-md mx-auto bg-cream rounded-t-3xl sm:rounded-3xl p-6 pb-[max(env(safe-area-inset-bottom),24px)] sm:pb-6"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="text-center mb-5">
          <p className="font-display text-[17px] text-eye-purple leading-tight">이미 이 사주로 봤어</p>
          <p className="text-[12.5px] text-text-light/85 mt-1.5 leading-relaxed">
            같은 사주로 <b className="text-eye-purple">{label}</b>은(는) 이미 봤어.
            <br />
            보관함에서 다시 볼 수 있어. 새로 뽑으면 별 {cost}개가 다시 들어.
          </p>
        </div>
        <div className="flex flex-col gap-2.5">
          <button
            onClick={onReview}
            className="w-full py-3.5 rounded-full bg-lilac-deep text-white font-bold text-[14px] active:scale-[0.98] transition"
          >
            보관함에서 보기
          </button>
          <button
            onClick={onRegenerate}
            className="w-full py-3 rounded-full border-2 border-lilac-deep/40 text-lilac-deep font-bold text-[13px] hover:bg-lilac-deep/5 transition"
          >
            그래도 새로 뽑기 · ⭐{cost}
          </button>
          <button onClick={onClose} className="w-full py-2.5 text-[13px] text-text-light">
            닫기
          </button>
        </div>
      </div>
    </div>,
    document.body
  );
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 3: 커밋**

```bash
git add components/fortune/AlreadyOwnedModal.tsx
git commit -m "feat(fortune): AlreadyOwnedModal(보관함 유도 + 재생성)"
```

---

### Task 4: 단일 프로필 페이지 배선 (`/fortune/[type]`)

**Files:**
- Modify: `app/fortune/[type]/page.tsx`

- [ ] **Step 1: import + 상태 추가**

`import FortuneRefundModal ...` 아래에 추가:

```tsx
import AlreadyOwnedModal from "@/components/fortune/AlreadyOwnedModal";
```

`const [refunded, setRefunded] = useState(false);` 아래에 추가:

```tsx
  const [alreadyOwned, setAlreadyOwned] = useState<{ id: string } | null>(null);
```

`const inFlightRef = useRef(false);` 아래에 추가(force 재시도 시 프로필 유지):

```tsx
  const lastProfileIdRef = useRef<string | null>(null);
```

- [ ] **Step 2: handleGenerate 를 force 지원 + alreadyOwned 처리로 교체**

기존 `const handleGenerate = async () => { ... };` 전체를 아래로 교체:

```tsx
  const handleGenerate = async (force = false) => {
    const profileId = pendingProfileId ?? lastProfileIdRef.current;
    if (!profileId || inFlightRef.current) return;
    lastProfileIdRef.current = profileId;
    inFlightRef.current = true;
    setPendingProfileId(null);
    setGenerating(true);
    setError(null);
    setNeedCharge(false);

    try {
      const res = await fetch("/api/fortune/create", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ type: cfg.type, profileId, force }),
      });
      if (!res.ok) {
        const data = await res.json().catch(() => ({}));
        inFlightRef.current = false;
        if (data?.code === "INSUFFICIENT_STARS") {
          setError("별이 모자라. 충전소에서 별을 채우고 다시 올래?");
          setNeedCharge(true);
        } else if (data?.refunded) {
          setRefunded(true);
        } else {
          setError(
            data?.error === "rate_limited"
              ? "조금만 천천히! 잠시 후 다시 시도해줄래?"
              : "운세를 못 펼쳤어. 잠시 후 다시 시도해줄래?"
          );
        }
        setGenerating(false);
        return;
      }
      const data = await res.json();
      // 이미 같은 사주로 본 상품 — 과금 없이 선택지 제시(force 아니었을 때만).
      if (data.alreadyOwned && !force) {
        inFlightRef.current = false;
        setGenerating(false);
        setAlreadyOwned({ id: data.id });
        return;
      }
      // 생성 시작 시점에 이미 별이 차감됨 — 헤더 잔액 즉시 갱신
      window.dispatchEvent(new Event("byeolkong:balance-updated"));
      router.push(`/fortune/result?id=${data.id}`);
    } catch {
      inFlightRef.current = false;
      setError("연결이 잠시 흔들렸어. 다시 시도해줄래?");
      setGenerating(false);
    }
  };
```

- [ ] **Step 3: StarConfirmModal onConfirm 을 화살표로 감싸기 (force 오전달 방지)**

`StarConfirmModal` 의 `onConfirm={handleGenerate}` 를 아래로 교체(클릭 이벤트가 force 인자로 새는 것 방지):

```tsx
          onConfirm={() => handleGenerate(false)}
```

- [ ] **Step 4: AlreadyOwnedModal 렌더 추가**

`{refunded && ( ... )}` 블록 **아래**, `</main>` **위**에 추가:

```tsx
      {alreadyOwned && (
        <AlreadyOwnedModal
          label={cfg.label}
          cost={cfg.cost}
          onReview={() => {
            router.push(`/fortune/result?id=${alreadyOwned.id}&from=history`);
          }}
          onRegenerate={() => {
            setAlreadyOwned(null);
            void handleGenerate(true);
          }}
          onClose={() => setAlreadyOwned(null)}
        />
      )}
```

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 6: 커밋**

```bash
git add app/fortune/[type]/page.tsx
git commit -m "feat(fortune): [type] 페이지 alreadyOwned 처리 + 새로뽑기(force)"
```

---

### Task 5: 궁합 배선 (`CompatInput`)

**Files:**
- Modify: `components/fortune/compat/CompatInput.tsx`

- [ ] **Step 1: import + 상태 + 재시도 ref 추가**

`import FortuneReportHeader ...` 아래:

```tsx
import AlreadyOwnedModal from "@/components/fortune/AlreadyOwnedModal";
```

`const [refunded, setRefunded] = useState(false);` 아래:

```tsx
  const [alreadyOwned, setAlreadyOwned] = useState<{ id: string } | null>(null);
```

`const inFlightRef = useRef(false);` 아래(force 재시도 시 페어 유지):

```tsx
  const lastPairRef = useRef<{ a: string; b: string } | null>(null);
```

- [ ] **Step 2: handleGenerate 교체(force + alreadyOwned)**

기존 `const handleGenerate = async () => { ... };` 전체를 아래로 교체:

```tsx
  const handleGenerate = async (force = false) => {
    const pair = pending ? { a: pending.a, b: pending.b } : lastPairRef.current;
    if (!pair || inFlightRef.current) return;
    lastPairRef.current = pair;
    inFlightRef.current = true;
    setPending(null);
    setGenerating(true);
    setError(null);
    setNeedCharge(false);

    try {
      const res = await fetch("/api/fortune/create", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ type, profileA: pair.a, profileB: pair.b, force }),
      });
      if (!res.ok) {
        const data = await res.json().catch(() => ({}));
        inFlightRef.current = false;
        if (data?.code === "INSUFFICIENT_STARS") {
          setError("별이 모자라. 충전소에서 별을 채우고 다시 올래?");
          setNeedCharge(true);
        } else if (data?.refunded) {
          setRefunded(true);
        } else {
          setError(
            data?.error === "rate_limited"
              ? "조금만 천천히! 잠시 후 다시 시도해줄래?"
              : "궁합을 못 펼쳤어. 잠시 후 다시 시도해줄래?"
          );
        }
        setGenerating(false);
        return;
      }
      const data = await res.json();
      if (data.alreadyOwned && !force) {
        inFlightRef.current = false;
        setGenerating(false);
        setAlreadyOwned({ id: data.id });
        return;
      }
      window.dispatchEvent(new Event("byeolkong:balance-updated"));
      router.push(`/fortune/result?id=${data.id}`);
    } catch {
      inFlightRef.current = false;
      setError("연결이 잠시 흔들렸어. 다시 시도해줄래?");
      setGenerating(false);
    }
  };
```

- [ ] **Step 3: StarConfirmModal onConfirm 화살표 감싸기**

`onConfirm={handleGenerate}` → 아래로 교체:

```tsx
          onConfirm={() => handleGenerate(false)}
```

- [ ] **Step 4: AlreadyOwnedModal 렌더 추가**

`{refunded && ( ... )}` 아래, `</main>` 위에 추가:

```tsx
      {alreadyOwned && (
        <AlreadyOwnedModal
          label={cfg.label}
          cost={cfg.cost}
          onReview={() => {
            router.push(`/fortune/result?id=${alreadyOwned.id}&from=history`);
          }}
          onRegenerate={() => {
            setAlreadyOwned(null);
            void handleGenerate(true);
          }}
          onClose={() => setAlreadyOwned(null)}
        />
      )}
```

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 6: 커밋**

```bash
git add components/fortune/compat/CompatInput.tsx
git commit -m "feat(fortune): 궁합 alreadyOwned 처리 + 새로뽑기(force)"
```

---

### Task 6: 통합 검증 (빌드 + 유닛 + 브라우저 E2E)

**Files:** (없음 — 검증 전용)

- [ ] **Step 1: 유닛 + 타입체크 + 빌드**

Run: `npx tsx --test lib/fortune/existing-lookup.test.ts && npx tsc --noEmit`
Expected: 유닛 PASS(3) · tsc exit 0.

- [ ] **Step 2: 브라우저 E2E — 단일 프로필 재구매 차단**

dev 서버(`preview_start byeolkong-dev`) + 소유자 쿠키 주입(`document.cookie="byeolkong_user_id=<dev user>"`) 후:
1. `/fortune/love_self` (또는 이미 리포트가 있는 프로필 상품) 진입 → 프로필 선택 → 확인 → 생성.
2. 같은 상품·프로필로 다시 진입 → 확인 → **AlreadyOwnedModal 노출**(과금 X: `/api/stars/balance` 로 잔액 불변 확인).
3. [보관함에서 보기] → `/fortune/result?id=...&from=history` 이동 확인.
4. 재진입 → [그래도 새로 뽑기] → 새 리딩 생성·과금 확인.

증거: `read_network_requests` 로 `/api/fortune/create` 응답에 `alreadyOwned:true, cost:0` 확인 + 잔액 로그.

- [ ] **Step 3: 회귀 — daily/monthly/tarot 불변 확인**

`/fortune/daily`(하루 1건 유지), `/fortune/monthly`(월 1건 + 기존 '다시보기' 유지), 타로 운세 1종(매 뽑기 생성)이 종전대로 동작하는지 확인.

- [ ] **Step 4: 최종 커밋(없으면 스킵) + dev 푸시 대기**

이 시점까지 태스크별 커밋 완료. 배포는 사용자 지시 시 `git push origin dev`.

---

## Self-Review 결과

- **스펙 커버리지(Part A)**: 서버 권위 가드(Task 2) · 재생성 force(Task 2·4·5) · 보관함 유도 UX(Task 3·4·5) · 궁합 두 프로필(Task 1·2) · daily/monthly/tarot 제외(Task 2 조건) · 연간 상품 v1 프로필당 1건(가드가 시간창 없음). ✅ 피커 "다시보기" 사전 노출은 이번 플랜에서 리액티브 모달로 대체(스펙의 monthly 일반화는 후속 개선으로 남김 — 기능적 요구는 모달이 충족).
- **플레이스홀더**: 없음(모든 스텝 실제 코드/명령).
- **타입 일관성**: `pickExistingReadingId`/`findExistingFortuneReadingId`/`ExistingSig`·`alreadyOwned` 응답 키·`handleGenerate(force)` 시그니처가 Task 전반에서 일치.

## 범위 밖 / 후속
- 피커에서 구매 전 "보관함에서 보기" **사전** 노출(monthly 패턴 전 상품 일반화) — UX 개선, 후속.
- 연간 상품 연도 갱신 허용.
- 레거시 궁합 리딩(saju_data 에 aId/bId 없음)은 dedup 미적용 — 신규분부터 적용(fix-forward).
