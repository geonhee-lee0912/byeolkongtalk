# 별마루 P5-2 — 달력 범위·무료선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루 달력을 "오늘부터 30일"에서 **"이번 달(1일~말일)"**로 바꾸고, 무료선을 **"지나간 날 + 오늘"**로 조여 구독이 "앞당겨 보는 권리"를 팔게 만든다. 동시에 출석 별 환급을 폐지하고 자동 체크인으로 전환한다.

**Architecture:** 판정 엔진(`day-score.ts`·`pair-day.ts`)은 **한 줄도 건드리지 않는다.** 바뀌는 건 ①일진을 어느 범위로 뽑느냐(`calcDailyLuckRange` 신설) ②서버가 그중 어디까지 내려주느냐(`splitByFreeLine`) ③그리드가 안 온 날을 어떻게 그리느냐(`CalendarGrid` 잠긴 칸)뿐이다. **안 온 날의 판정 결과는 서버가 아예 직렬화하지 않는다** — 클라 devtools 로도 안 새게(기존 "비구독 완전블러 무누출" 규율 계승). 곁들여 P5-1 이 이월한 4건(DAY_LINE 톤 어긋남 · narrative 하루 이름 주입 · OG 하루 이름 · 글리프 범례)을 닫는다.

**Tech Stack:** Next.js 16 App Router · TypeScript strict · `node --test` (tsx loader) · Supabase(마이그레이션 1장) · Tailwind v4 `@theme` 토큰

---

## 스코프 밖 (이 플랜이 건드리지 않는 것)

- **웰컴 20→15** — 스펙 §11/§14 의 "(별건)". 별마루 밖 전 상품에 파급돼 무료선 변경과 지표가 섞이므로 별도 배포. 이 플랜에서 `WELCOME_BONUS_STARS` 를 만지지 말 것.
- **허브 재조립 · 라이트 B 비주얼 · 페이월 맥락 분산** — P5-3/P5-4. 이 플랜은 **라벨 정정과 깨진 계산 복구까지만** 한다(30일 전제로 짠 코드가 이번 달로 바뀌며 조용히 틀리는 곳).
- **`narrative`/`pair-narrative` 라우트의 일진 범위** — 이 둘은 "오늘 서술 + 앞으로 좋은 날 추천"이라 **오늘부터 30일이 맞다.** `includeMonth:true` 를 그대로 둔다. 달력만 이번 달이다.
- **`calcTemporalLuck` 의 `includeMonth` 동작** — 기존 30일 그대로. fortune/create·readings 가 같이 쓴다.

## 사전 지식 (이 저장소에서 처음 일하는 사람용)

- 테스트는 `node:test` + `tsx` 로 돈다. 전체: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")` / 개별: `node --import tsx --test lib/byeolmaru/calendar.test.ts`
- 타입체크: `npx tsc --noEmit` · 빌드: `npm run build`
- **날짜는 전부 KST 자정 기준 `"YYYY-MM-DD"` 문자열**이다. 비교는 문자열 사전순(`c.date <= todayKst`)으로 해도 정확하다(zero-padding 고정폭). `new Date()` 를 서버에서 그대로 쓰면 Vercel 이 UTC 라 KST 00~09시에 하루 밀린다 — 반드시 `kstDate(new Date().toISOString())`.
- `lib/byeolmaru/*.ts` 중 `attendance.ts`·`subscription.ts`·`daily-report.ts`·`watch.ts` 만 서버 전용(`getServiceSupabase` 를 문다). `calendar.ts`·`pair-day.ts`·`day-label.ts`·`day-score.ts`·`static-lines.ts` 는 **순수라 클라 컴포넌트에서 import 해도 된다.**
- 커밋 메시지는 한글, 접두사는 `feat(byeolmaru):` / `fix(byeolmaru):` / `refactor(byeolmaru):` 관례.

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `lib/saju/calc.ts` (수정) | `calcDailyLuckRange(startKst,endKst)` 신설 + `includeMonth` 루프를 거기에 위임 | T1 |
| `lib/byeolmaru/calendar.ts` (수정) | `monthRange()` · `splitByFreeLine()` — 달력 범위와 무료선의 **단일 원천** | T1 |
| `app/api/byeolmaru/calendar/route.ts` (수정) | self·pair 두 경로를 이번 달 + 무료선으로. 안 온 날은 `lockedDates` 로만 | T2, T5, T6 |
| `components/byeolmaru/CalendarGrid.tsx` (수정) | 잠긴 칸 렌더 · offset 계측 재정의 · 글리프 범례 | T3 |
| `components/byeolmaru/SajuTodayView.tsx` (수정) | 잠긴 칸 전달 · "30일" 라벨 정정 · 공유에 하루 이름 | T4, T8 |
| `components/byeolmaru/ByeolmaruHub.tsx` (수정) | 7일 스트립 기준 복구 · 출석 핸들러 제거 | T4, T6 |
| `components/byeolmaru/WooriTodayView.tsx` (수정) | 무료도 그리드 렌더 · staticLine 을 선택 셀 기준으로 | T5 |
| `lib/byeolmaru/attendance.ts` (수정) | 환급 3종 삭제 · `AttendanceState` 축소 | T6 |
| `components/byeolmaru/AttendanceStrip.tsx` (수정) | 버튼·보상 문구 제거 → "이번 달 N칸 채움 · M일 연속" | T6 |
| `app/api/byeolmaru/checkin/route.ts` (삭제) | 자동 체크인으로 대체 | T6 |
| `supabase/migrations/20260913000000_byeolmaru_drop_reward.sql` (신규) | `reward_granted_at` 컬럼 제거 | T6 |
| `lib/byeolmaru/day-label.ts` (수정) | 정관·식신 DAY_LINE 톤 중립화 | T7 |
| `lib/byeolmaru/narrative-prompt.ts` (수정) | `buildNarrativeSystem` 에 하루 이름 주입 | T7 |
| `app/api/og/byeolmaru/saju/route.tsx` (수정) | `?tg=` 로 하루 이름 렌더(화이트리스트) | T8 |

---

### Task 1: 일진 범위 계산 + 월 경계·무료선 헬퍼

범위를 "이번 달"로 바꾸는 계산의 토대. **순수 함수만** 만들고 아무도 아직 안 쓴다.

**Files:**
- Modify: `lib/saju/calc.ts` (`calcTemporalLuck` 근처, 약 212-240행)
- Modify: `lib/byeolmaru/calendar.ts`
- Test: `lib/saju/calc.test.ts` · `lib/byeolmaru/calendar.test.ts`

- [ ] **Step 1: `calcDailyLuckRange` 의 실패 테스트를 쓴다**

`lib/saju/calc.test.ts` 맨 끝에 추가:

```ts
test("calcDailyLuckRange — 양끝 포함, 월 경계를 넘어간다", () => {
  const r = calcDailyLuckRange("2026-09-28", "2026-10-02");
  assert.equal(r.length, 5, "9/28~10/2 = 5일(양끝 포함)");
  assert.equal(r[0].date, "2026-09-28");
  assert.equal(r[4].date, "2026-10-02");
});

test("calcDailyLuckRange — 하루짜리 범위", () => {
  const r = calcDailyLuckRange("2026-09-13", "2026-09-13");
  assert.equal(r.length, 1);
  assert.equal(r[0].date, "2026-09-13");
});

test("calcDailyLuckRange — end < start 면 빈 배열(호출부 실수를 조용히 늘리지 않는다)", () => {
  assert.deepEqual(calcDailyLuckRange("2026-09-13", "2026-09-12"), []);
});

test("calcDailyLuckRange — 같은 날짜는 calcTemporalLuck 의 30일 일진과 간지가 일치한다", () => {
  // 두 경로가 같은 tyme4ts 계산을 쓴다는 계약. 어긋나면 달력과 서술이 다른 간지를 말한다.
  const t = calcTemporalLuck(baseDateForKst("2026-09-13"), 1994, { includeMonth: true });
  const r = calcDailyLuckRange("2026-09-13", "2026-10-12");
  assert.equal(r.length, 30);
  for (let i = 0; i < 30; i++) {
    assert.equal(r[i].date, t.dailyLuck?.[i].date, `${i}번째 날짜 불일치`);
    assert.equal(
      r[i].stem + r[i].branch,
      (t.dailyLuck?.[i].stem ?? "") + (t.dailyLuck?.[i].branch ?? ""),
      `${r[i].date} 간지 불일치`
    );
  }
});
```

파일 상단 import 에 `calcDailyLuckRange` 를 추가한다 (기존 import 줄에 이름만 끼워 넣는다).

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --import tsx --test lib/saju/calc.test.ts`
Expected: FAIL — `calcDailyLuckRange is not a function` (또는 tsx 의 import 에러)

- [ ] **Step 3: `calcDailyLuckRange` 를 구현하고 `calcTemporalLuck` 이 그걸 쓰게 한다**

`lib/saju/calc.ts` 의 `calcTemporalLuck` **바로 위**에 추가:

```ts
/**
 * [startKst, endKst] 양끝 포함 일진 목록. calcTemporalLuck 의 30일 루프와 **같은 규약**이다 —
 * 정오 고정(자정 경계 반올림 회피) · 로컬 getter = 달력 날짜(baseDateForKst 주석 참조).
 * 별마루 P5-2 에서 달력 범위가 "오늘부터 30일" → "이번 달"로 바뀌며 필요해졌다.
 * 🔴 end < start 면 빈 배열 — 호출부 실수로 무한 루프가 나지 않게 while 조건이 곧 가드다.
 */
export function calcDailyLuckRange(startKst: string, endKst: string): DailyLuck[] {
  const out: DailyLuck[] = [];
  const cur = baseDateForKst(startKst);
  const end = baseDateForKst(endKst);
  while (cur.getTime() <= end.getTime()) {
    const dayParts = toParts(
      SolarTime.fromYmdHms(cur.getFullYear(), cur.getMonth() + 1, cur.getDate(), 12, 0, 0)
        .getLunarHour()
        .getEightChar()
        .getDay()
    );
    out.push({ date: fmtDate(cur), stem: dayParts.stem, branch: dayParts.branch, element: dayParts.stemElement });
    cur.setDate(cur.getDate() + 1);
  }
  return out;
}
```

그리고 `calcTemporalLuck` 안의 `if (opts?.includeMonth) { ... }` 블록 **전체**를 아래로 교체한다(동작 무변경 — 오늘부터 30일 그대로):

```ts
  let dailyLuck: DailyLuck[] | undefined;
  if (opts?.includeMonth) {
    // 오늘부터 30일(양끝 포함이므로 +29). 범위 계산은 calcDailyLuckRange 단일 원천.
    const endD = new Date(baseDate);
    endD.setDate(endD.getDate() + 29);
    dailyLuck = calcDailyLuckRange(fmtDate(baseDate), fmtDate(endD));
  }
```

- [ ] **Step 4: 테스트 통과 확인 — 기존 30일 계약이 안 깨졌는지 포함**

Run: `node --import tsx --test lib/saju/calc.test.ts lib/byeolmaru/day-label.test.ts`
Expected: PASS (전부). 특히 기존 `assert.equal(temporal.dailyLuck?.length, 30, "includeMonth:true 는 항상 30일")` 과 day-label 의 "30일이면 십신 10종이 정확히 3일씩" 이 그대로 통과해야 한다.

- [ ] **Step 5: `monthRange` · `splitByFreeLine` 의 실패 테스트를 쓴다**

`lib/byeolmaru/calendar.test.ts` 맨 끝에 추가(상단 import 에 `monthRange, splitByFreeLine` 추가):

```ts
test("monthRange — 그 달 1일~말일", () => {
  assert.deepEqual(monthRange("2026-09-13"), { start: "2026-09-01", end: "2026-09-30" });
  assert.deepEqual(monthRange("2026-01-01"), { start: "2026-01-01", end: "2026-01-31" });
  assert.deepEqual(monthRange("2026-12-31"), { start: "2026-12-01", end: "2026-12-31" });
});

test("monthRange — 2월 평년 28일 · 윤년 29일", () => {
  assert.equal(monthRange("2026-02-10").end, "2026-02-28", "2026 은 평년");
  assert.equal(monthRange("2028-02-10").end, "2028-02-29", "2028 은 윤년");
});

test("splitByFreeLine — 비자격은 오늘까지만 열리고 나머지는 날짜만 남는다", () => {
  const cells = [{ date: "2026-09-11" }, { date: "2026-09-12" }, { date: "2026-09-13" }];
  const r = splitByFreeLine(cells, "2026-09-12", false);
  assert.deepEqual(r.open.map((c) => c.date), ["2026-09-11", "2026-09-12"], "지나간 날 + 오늘");
  assert.deepEqual(r.lockedDates, ["2026-09-13"], "안 온 날은 날짜만");
});

test("splitByFreeLine — 자격자는 전부 열리고 잠긴 날이 없다", () => {
  const cells = [{ date: "2026-09-11" }, { date: "2026-09-13" }];
  const r = splitByFreeLine(cells, "2026-09-12", true);
  assert.equal(r.open.length, 2);
  assert.deepEqual(r.lockedDates, []);
});

test("splitByFreeLine — 오늘은 언제나 열린 쪽(경계 off-by-one 고정)", () => {
  const r = splitByFreeLine([{ date: "2026-09-12" }], "2026-09-12", false);
  assert.deepEqual(r.open.map((c) => c.date), ["2026-09-12"]);
  assert.deepEqual(r.lockedDates, []);
});
```

- [ ] **Step 6: 테스트 실패 확인**

Run: `node --import tsx --test lib/byeolmaru/calendar.test.ts`
Expected: FAIL — `monthRange is not a function`

- [ ] **Step 7: `monthRange` · `splitByFreeLine` 구현**

`lib/byeolmaru/calendar.ts` 의 `buildCalendar` **바로 아래**에 추가:

```ts
/**
 * KST 오늘 → 그 달의 1일·말일. P5-2 달력 범위("이번 달")의 단일 원천.
 * Date.UTC(y, m, 0) 은 m 이 1-based 일 때 그 달의 말일을 준다(0일 = 전달 마지막 날).
 * UTC 로 계산하는 건 윤년 판정만 쓰고 TZ 영향을 안 받기 위함 — 반환은 순수 문자열 조립이다.
 */
export function monthRange(todayKst: string): { start: string; end: string } {
  const [y, m] = todayKst.split("-").map(Number);
  const last = new Date(Date.UTC(y, m, 0)).getUTCDate();
  const ym = todayKst.slice(0, 7);
  return { start: `${ym}-01`, end: `${ym}-${String(last).padStart(2, "0")}` };
}

/**
 * 무료선(P5-2 스펙 §6): **무료 = 지나간 날 + 오늘 · 구독 = 앞당겨 보기.**
 * 🔴 안 온 날은 "가려서 보여주는" 게 아니라 **판정 결과를 아예 안 실어 보낸다** — 날짜만 남는다.
 *    클라에서 가리면 devtools 로 다 보이고, 그건 무료선이 아니라 눈속임이다.
 * 나(DayCell)·우리(PairDayCell) 양쪽에 쓰므로 date 만 요구하는 제네릭이다.
 */
export function splitByFreeLine<T extends { date: string }>(
  cells: T[],
  todayKst: string,
  entitled: boolean
): { open: T[]; lockedDates: string[] } {
  if (entitled) return { open: cells, lockedDates: [] };
  const open: T[] = [];
  const lockedDates: string[] = [];
  for (const c of cells) {
    if (c.date <= todayKst) open.push(c);
    else lockedDates.push(c.date);
  }
  return { open, lockedDates };
}
```

- [ ] **Step 8: 테스트 통과 확인**

Run: `node --import tsx --test lib/byeolmaru/calendar.test.ts lib/saju/calc.test.ts`
Expected: PASS

- [ ] **Step 9: 타입체크 + 커밋**

Run: `npx tsc --noEmit`
Expected: 출력 없음(exit 0)

```bash
git add lib/saju/calc.ts lib/saju/calc.test.ts lib/byeolmaru/calendar.ts lib/byeolmaru/calendar.test.ts
git commit -m "feat(byeolmaru): 일진 범위 계산 + 이번 달 경계·무료선 헬퍼 (P5-2)"
```

---

### Task 2: calendar 라우트 self 경로를 이번 달 + 무료선으로

**Files:**
- Modify: `app/api/byeolmaru/calendar/route.ts` (약 44-56행, 118-145행)
- Test: `lib/byeolmaru/free-line.test.ts` (신규)

- [ ] **Step 1: 일진 계산을 이번 달 범위로 바꾼다**

`app/api/byeolmaru/calendar/route.ts` 에서 아래 블록을

```ts
    const todayKst = kstDate(new Date().toISOString());
    const temporal = calcTemporalLuck(baseDateForKst(todayKst), input.year, { includeMonth: true });
    // includeMonth:true 면 30개가 보장되지만 타입이 그걸 못 담는다 — 조용히 빈 캘린더를
    // 200 으로 내보내느니 500 으로 터뜨린다.
    if (!temporal.dailyLuck?.length) {
      return NextResponse.json({ error: "calc_failed" }, { status: 500 });
    }
```

아래로 교체한다:

```ts
    const todayKst = kstDate(new Date().toISOString());
    // P5-2 — 달력 범위는 "오늘부터 30일"이 아니라 **이번 달 1일~말일**이다(스펙 §6).
    // temporal 은 오늘 간지(todayGanji)만 쓰므로 includeMonth 를 켜지 않는다 — 30일 루프가 낭비다.
    const temporal = calcTemporalLuck(baseDateForKst(todayKst), input.year);
    const { start: monthStart, end: monthEnd } = monthRange(todayKst);
    const monthLuck = calcDailyLuckRange(monthStart, monthEnd);
    // 28~31개가 보장되지만 tyme4ts 범위 밖 입력이면 빈 배열이 올 수 있다 — 조용히 빈 캘린더를
    // 200 으로 내보내느니 500 으로 터뜨린다.
    if (!monthLuck.length) {
      return NextResponse.json({ error: "calc_failed" }, { status: 500 });
    }
```

import 를 고친다:

```ts
import { calcSaju, calcTemporalLuck, calcDailyLuckRange, baseDateForKst } from "@/lib/saju/calc";
import { buildCalendar, weekBuckets, monthRange, splitByFreeLine } from "@/lib/byeolmaru/calendar";
```

- [ ] **Step 2: self 응답을 무료선으로 자른다**

같은 파일에서 아래 블록을

```ts
    const cells = buildCalendar(saju, temporal.dailyLuck, todayKst);

    const ent = await getEntitlement(userId);
```

아래로 교체:

```ts
    const ent = await getEntitlement(userId);

    // P5-2 무료선 — 비자격자에겐 안 온 날의 판정을 **직렬화하지 않는다**(날짜만 lockedDates 로).
    const allCells = buildCalendar(saju, monthLuck, todayKst);
    const { open: cells, lockedDates } = splitByFreeLine(allCells, todayKst, ent.entitled);
```

그리고 self 응답 객체를 아래로 교체한다(`weeks` 가 `cells` 를 받으므로 비자격자의 주차 요약도 자동으로 오늘까지만 집계된다 — 미래 good/caution 개수 누출 차단):

```ts
    return NextResponse.json({
      today: todayKst,
      todayGanji: temporal.day.stem + temporal.day.branch,
      monthStart,
      monthEnd,
      cells,
      lockedDates,
      weeks: weekBuckets(cells),
      entitled: ent.entitled,
      trialUsed: ent.trialUsed,
      subscriptionExpiresAt: ent.subscriptionExpiresAt,
      attendance,
    });
```

⚠️ `attendance` 줄은 그대로 둔다 — T6 에서 그 윗줄(`grantDueReward`/`getAttendanceState`)을 바꾼다.

- [ ] **Step 3: 누출 방지 계약 테스트를 쓴다**

Create: `lib/byeolmaru/free-line.test.ts`

```ts
// P5-2 무료선 계약 — 라우트가 조립하는 모양을 순수 함수로 재현해 "안 온 날이 안 샌다"를 고정한다.
// (라우트 자체는 DB·세션을 물어 유닛에서 못 돌린다 — 조립 규칙만 여기서 지킨다.)
import { test } from "node:test";
import assert from "node:assert/strict";
import { calcSaju, calcDailyLuckRange } from "@/lib/saju/calc";
import { buildCalendar, monthRange, splitByFreeLine, weekBuckets } from "./calendar.ts";

const TODAY = "2026-09-13";
const saju = calcSaju({ year: 1994, month: 5, day: 17, hour: 14, gender: "other", isLunar: false });

function build(entitled: boolean) {
  const { start, end } = monthRange(TODAY);
  const all = buildCalendar(saju, calcDailyLuckRange(start, end), TODAY);
  const { open, lockedDates } = splitByFreeLine(all, TODAY, entitled);
  return { all, open, lockedDates, weeks: weekBuckets(open) };
}

test("이번 달 달력은 1일부터 말일까지 전부 계산된다", () => {
  const { all } = build(true);
  assert.equal(all.length, 30, "2026-09 는 30일");
  assert.equal(all[0].date, "2026-09-01");
  assert.equal(all[29].date, "2026-09-30");
  assert.equal(all.filter((c) => c.isToday).length, 1);
});

test("비자격자 응답엔 오늘 이후 셀이 하나도 없다(판정 누출 0)", () => {
  const { open, lockedDates } = build(false);
  assert.ok(open.every((c) => c.date <= TODAY), "열린 셀에 미래가 섞였다");
  assert.equal(open.length, 13, "9/1~9/13");
  assert.equal(lockedDates.length, 17, "9/14~9/30");
  assert.ok(lockedDates.every((d) => d > TODAY));
});

test("비자격자의 주차 요약도 오늘까지만 집계된다(good/caution 개수로 미래가 새지 않는다)", () => {
  const { weeks } = build(false);
  assert.ok(weeks.every((w) => w.endDate <= TODAY), "주차 버킷이 미래를 덮었다");
});

test("자격자는 이번 달 전부가 열리고 잠긴 날이 없다", () => {
  const { open, lockedDates } = build(true);
  assert.equal(open.length, 30);
  assert.deepEqual(lockedDates, []);
});
```

- [ ] **Step 4: 테스트 실행**

Run: `node --import tsx --test lib/byeolmaru/free-line.test.ts`
Expected: PASS (T1 의 헬퍼가 이미 있으므로 바로 통과한다 — 이 파일은 회귀 방지용 계약이다)

- [ ] **Step 5: 타입체크 + 커밋**

Run: `npx tsc --noEmit`
Expected: 출력 없음

```bash
git add app/api/byeolmaru/calendar/route.ts lib/byeolmaru/free-line.test.ts
git commit -m "feat(byeolmaru): 달력을 이번 달로 · 무료선은 지나간 날+오늘 (P5-2)"
```

---

### Task 3: CalendarGrid — 잠긴 칸 · offset 계측 재정의 · 글리프 범례

**Files:**
- Modify: `components/byeolmaru/CalendarGrid.tsx`

- [ ] **Step 1: Props 를 넓힌다**

`interface Props` 를 아래로 교체:

```tsx
interface Props {
  cells: GridCell[];
  /** 아직 안 온 날(P5-2 무료선). 판정이 없는 날짜 문자열만 온다 — 서버가 내용을 안 실어 보낸다. */
  lockedDates: string[];
  /** KST 오늘. 계측 offset 의 기준이자 "안 온 날" 문구의 기준. */
  todayDate: string;
  selectedDate: string;
  onSelect: (date: string) => void;
}
```

- [ ] **Step 2: 잠긴 칸을 섞어 그리도록 본문 앞부분을 바꾼다**

`export default function CalendarGrid(...)` 의 시그니처와 앞부분(`if (cells.length === 0) return null;` 부터 `const blanks = ...` 까지)을 아래로 교체:

```tsx
export default function CalendarGrid({ cells, lockedDates, todayDate, selectedDate, onSelect }: Props) {
  // 열린 칸 + 안 온 칸을 날짜순으로 합친다. cell 이 없는 슬롯 = 아직 안 온 날.
  const slots: { date: string; cell?: GridCell }[] = [
    ...cells.map((c) => ({ date: c.date, cell: c })),
    ...lockedDates.map((d) => ({ date: d })),
  ].sort((a, b) => a.date.localeCompare(b.date));
  if (slots.length === 0) return null;

  // 첫 슬롯(=이번 달 1일)의 요일만큼 앞을 비워 요일 열을 맞춘다.
  const firstWeekday = new Date(`${slots[0].date}T00:00:00`).getDay();
  const blanks = Array.from({ length: firstWeekday }, (_, i) => i);

  // 범례는 **이 달에 실제로 뜬 마크만** 보여준다 — 안 나온 글리프까지 나열하면 잡음이 된다.
  const legend = Array.from(new Map(cells.flatMap((c) => c.marks).map((m) => [m.glyph, m])).values());
```

- [ ] **Step 3: 렌더 루프를 슬롯 기준으로 바꾸고 offset 계측을 오늘 기준으로 재정의한다**

`{cells.map((c) => {` 로 시작하는 줄부터 `aria-pressed={selected}` 줄까지를 아래로 교체한다(그 아래 `// 오늘/선택 링을 겹치지 않게 —` 주석부터 `</button>` 까지는 **그대로 둔다**):

```tsx
        {slots.map(({ date, cell: c }) => {
          if (!c) {
            // 아직 안 온 날 — 자물쇠를 쓰지 않는다(스펙 §2). 버튼이 아니라 div 라 탭도 안 먹는다.
            return (
              <div
                key={date}
                aria-label={`${date} 아직 안 온 날`}
                className="flex aspect-square flex-col items-center justify-center rounded-xl border border-dashed border-lilac-mid/40 bg-white/30"
              >
                <span className="text-[13px] font-semibold leading-none text-text-light/60">
                  {Number(date.slice(8, 10))}
                </span>
                <span aria-hidden className="mt-1.5 h-1 w-1 rounded-full bg-lilac-mid/60" />
              </div>
            );
          }
          const selected = c.date === selectedDate;
          return (
            <button
              key={c.date}
              type="button"
              onClick={() => {
                // 🔴 offset 은 **오늘로부터의 일수 차이**다. 배열 인덱스를 쓰면 달력이 이번 달로
                //    바뀐 순간 "1일로부터의 거리"가 되어 스펙 §13 의 관문(offset≠0 비율 = 오늘
                //    말고 다른 날을 보는가)이 조용히 다른 지표가 된다. 과거는 음수다.
                const offset = Math.round(
                  (Date.parse(`${c.date}T00:00:00Z`) - Date.parse(`${todayDate}T00:00:00Z`)) / 86400000
                );
                trackUiEvent("byeolmaru_day_selected", { meta: { offset, tone: c.tone } });
                onSelect(c.date);
              }}
              aria-label={`${c.date} ${c.label}${c.marks.length ? ` · ${c.marks.map((m) => m.label).join(", ")}` : ""}`}
              aria-pressed={selected}
```

이어서 남아 있는 본문의 `c.marks?.length` 두 군데를 `c.marks.length` 로 바꾼다 — `marks` 는 필수 필드다(P5-1 이 "조용히 사라지는 구멍"을 막으려 필수화했다. 옵셔널 체이닝이 남아 있으면 그 의도가 흐려진다).

- [ ] **Step 4: 범례와 안내 줄을 그리드 아래에 붙인다**

격자를 감싼 `<div className="grid grid-cols-7 gap-1">` 가 닫힌 **직후**, 바깥 `<div className="rounded-2xl bg-cream-warm p-3">` 이 닫히기 **직전**에 추가:

```tsx
      {(legend.length > 0 || lockedDates.length > 0) && (
        <div className="mt-2 space-y-0.5 text-[10px] leading-relaxed text-text-light">
          {legend.length > 0 && (
            <p>
              {legend.map((m) => (
                <span key={m.glyph} className="mr-2">
                  <span aria-hidden className="font-bold text-eye-purple">{m.glyph}</span> {m.label}
                </span>
              ))}
            </p>
          )}
          {lockedDates.length > 0 && <p>점선 칸은 아직 안 온 날이야 — 그날이 오면 열려.</p>}
        </div>
      )}
```

- [ ] **Step 5: 타입체크로 미전달 호출부를 찾는다**

Run: `npx tsc --noEmit`
Expected: FAIL — `SajuTodayView.tsx` 와 `WooriTodayView.tsx` 에서 `lockedDates`/`todayDate` 누락 에러 2건. **이건 정상이다**(T4·T5 가 고친다). 에러가 정확히 그 두 파일뿐인지 확인할 것.

- [ ] **Step 6: 커밋(호출부는 다음 태스크에서 — 중간 상태 tsc 실패를 커밋 메시지에 남긴다)**

```bash
git add components/byeolmaru/CalendarGrid.tsx
git commit -m "feat(byeolmaru): 달력 그리드에 안 온 날 칸 · 오늘 기준 offset · 글리프 범례 (P5-2)

호출부(SajuTodayView·WooriTodayView) 전달은 다음 커밋 — 이 커밋 단독으로는 tsc 가 그 둘에서 실패한다."
```

---

### Task 4: SajuTodayView · ByeolmaruHub — 잠긴 칸 전달과 "30일" 라벨 정정

달력이 이번 달이 되면서 **조용히 틀리는 계산 두 곳**을 같이 고친다: ①허브의 `next7 = cells.slice(0,7)` 이 "오늘부터 7일"이 아니라 "1일부터 7일"이 된다 ②"앞으로 30일" 문구가 전부 거짓말이 된다.

**Files:**
- Modify: `components/byeolmaru/SajuTodayView.tsx`
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`

- [ ] **Step 1: SajuTodayView 의 응답 타입에 새 필드를 넣는다**

`interface CalendarResponse` 에 세 줄 추가:

```ts
  monthStart: string;
  monthEnd: string;
  lockedDates: string[];
```

- [ ] **Step 2: 그리드 호출과 선택 폴백을 고친다**

아래 블록을

```tsx
  const { data } = state;
  const cell = data.cells.find((c) => c.date === selected) ?? data.cells[0];
```

아래로 교체:

```tsx
  const { data } = state;
  // 폴백은 cells[0](= 이번 달 1일)이 아니라 **오늘**이다 — 달력이 이번 달로 바뀌며 1일이 되면
  // 첫 진입에서 엉뚱한 날짜의 상세가 열린다.
  const todayCell = data.cells.find((c) => c.isToday) ?? data.cells[data.cells.length - 1];
  const cell = data.cells.find((c) => c.date === selected) ?? todayCell;
```

- [ ] **Step 3: 그리드 섹션과 요약 문구를 이번 달 기준으로 바꾼다**

아래 블록을

```tsx
      <section aria-label="30일 캘린더">
        <CalendarGrid cells={selfGridCells} selectedDate={cell.date} onSelect={setSelected} />
      </section>
      {good > 0 ? (
        <p className="text-center text-[13px] text-text-light">앞으로 30일, <span className="font-bold text-eye-purple">잘 맞는 날 {good}일</span> ✨</p>
      ) : null}
```

아래로 교체:

```tsx
      <section aria-label="이번 달 캘린더">
        <CalendarGrid
          cells={selfGridCells}
          lockedDates={data.lockedDates}
          todayDate={data.today}
          selectedDate={cell.date}
          onSelect={setSelected}
        />
      </section>
      {/* 무료는 오늘까지만 집계돼 있으므로 "이번 달"이라고 하면 틀린 말이 된다 — 범위를 밝힌다.
          🔴 스펙 §15-1 완화: 챙길 날 수를 앞세우지 않는다(좋은 날 중심 서술). */}
      {good > 0 ? (
        <p className="text-center text-[13px] text-text-light">
          {data.entitled ? "이번 달" : "오늘까지"}, <span className="font-bold text-eye-purple">잘 맞는 날 {good}일</span> ✨
        </p>
      ) : null}
```

- [ ] **Step 4: 주차 요약 섹션 제목을 고친다**

```tsx
        <h2 className="mb-2 font-display text-base text-eye-purple">앞으로 30일 흐름</h2>
```

을 아래로 바꾼다:

```tsx
        <h2 className="mb-2 font-display text-base text-eye-purple">{data.entitled ? "이번 달 흐름" : "오늘까지의 흐름"}</h2>
```

- [ ] **Step 5: 허브의 7일 스트립 기준을 복구한다**

`ByeolmaruHub.tsx` 의 `interface CalendarResponse` 에 `lockedDates: string[];` 를 추가하고, 아래 줄을

```tsx
  const next7 = data.cells.slice(0, 7); // 오늘부터 롤링 7일(캘린더 '주' 아님 — calendar.ts:62 경고)
```

아래로 교체:

```tsx
  // 🔴 P5-2 로 cells 가 "이번 달 1일~"이 되면서 slice(0,7) 은 "1일부터 7일"을 뜻하게 됐다.
  //    자격자는 오늘부터 앞으로 7일(구독이 파는 게 '앞당겨 보기'라 앞을 보여준다),
  //    비자격자는 오늘이 마지막 칸이라 앞이 없으므로 **오늘로 끝나는 최근 7일**을 보여준다.
  const strip7 = data.entitled
    ? data.cells.filter((c) => c.date >= data.today).slice(0, 7)
    : data.cells.slice(-7);
```

그리고 `next7.map(...)` 을 `strip7.map(...)` 으로 바꾸고, 그 아래 문구를

```tsx
        <p className="text-xs text-lilac-deep">앞으로 7일 흐름 · 30일 전체 보기 →</p>
```

에서 아래로 바꾼다:

```tsx
        <p className="text-xs text-lilac-deep">{data.entitled ? "앞으로 7일 흐름" : "지난 7일 흐름"} · 이번 달 전체 보기 →</p>
```

- [ ] **Step 6: 타입체크 — 남은 에러가 WooriTodayView 하나뿐인지 확인**

Run: `npx tsc --noEmit`
Expected: `WooriTodayView.tsx` 의 CalendarGrid 호출 에러만 남는다(T5 가 고친다)

- [ ] **Step 7: 커밋**

```bash
git add components/byeolmaru/SajuTodayView.tsx components/byeolmaru/ByeolmaruHub.tsx
git commit -m "fix(byeolmaru): 이번 달 달력에 맞춰 7일 스트립 기준·30일 문구 정정 (P5-2)"
```

---

### Task 5: 우리 오늘 — 비자격 응답을 1칸에서 "이번 달 1일~오늘"로 확대

스펙 §8: 무료선이 두 탭에 걸쳐 한 문장이어야 한다. 지금 서버는 비자격 pair 에 `cells: [todayCell]` **1칸만** 준다.

**Files:**
- Modify: `app/api/byeolmaru/calendar/route.ts` (pair 경로, 약 58-116행)
- Modify: `components/byeolmaru/WooriTodayView.tsx`

- [ ] **Step 1: pair 경로를 이번 달 + 무료선으로 바꾼다**

`const partnerSaju = calcSaju(profileRowToSajuInput(pRow));` 부터 pair 경로의 **두 `return NextResponse.json(...)` 전체**를 아래로 교체:

```ts
      const partnerSaju = calcSaju(profileRowToSajuInput(pRow));
      const pairCells = buildPairCalendar(saju, partnerSaju, monthLuck, todayKst);
      const backdrop = pairBackdrop(saju, partnerSaju);
      const todayGanji = temporal.day.stem + temporal.day.branch;
      const ent = await getEntitlement(userId);

      // 관계 유형(썸/연애/짝사랑/헤어진) — watch 행의 성질. 무료 정적 한 줄의 프레이밍에만 쓰이므로
      // 비자격 분기에서만 읽는다(구독자는 LLM 서술이 pair-narrative 에서 따로 조회한다). 실패해도
      // 캘린더는 떠야 하니 null 폴백(문구만 영향 — score/tags 판정은 무관).
      let status: RelationshipStatus | null = null;
      if (!ent.entitled) {
        const { data: watchRow, error: watchErr } = await getServiceSupabase()
          .from("byeolmaru_watch")
          .select("status")
          .eq("user_id", userId)
          .eq("profile_id", subject)
          .maybeSingle();
        if (watchErr) {
          await logError(watchErr, ctxFromRequest(req, { route: "/api/byeolmaru/calendar", userId, extra: { stage: "watch_status" } }));
        }
        status = (watchRow?.status as RelationshipStatus | null) ?? null;
      }

      // P5-2 §8 — 무료선을 나 탭과 **같은 규칙**으로: 지나간 날 + 오늘은 열리고 안 온 날은 날짜만.
      // buildPairCalendar 는 룰 100% 라 칸이 1 → 13 으로 늘어도 원가는 0이다.
      const { open, lockedDates } = splitByFreeLine(pairCells, todayKst, ent.entitled);

      return NextResponse.json({
        subject,
        entitled: ent.entitled,
        today: todayKst,
        todayGanji,
        monthStart,
        monthEnd,
        partnerName: pRow.display_name,
        cells: open,
        lockedDates,
        backdrop,
        // 🔴 staticLine(단일 문장)을 더 이상 내리지 않는다 — 무료도 여러 날을 고를 수 있게 됐으므로
        //    한 줄은 **선택한 셀 기준**이어야 한다. getPairStaticLine 은 순수라 클라가 직접 푼다
        //    (PAIR_TONE_LABEL·DAY_NAME 을 클라가 직접 푸는 것과 같은 패턴 — 와이어에 중복을 안 둔다).
        status,
      });
```

import 에서 `getPairStaticLine` 을 뺀다:

```ts
import { buildPairCalendar, pairBackdrop } from "@/lib/byeolmaru/pair-day";
```

- [ ] **Step 2: WooriTodayView 의 상태 타입과 fetch 처리를 고친다**

`pairData` state 선언을 아래로 교체:

```tsx
  const [pairData, setPairData] = useState<{
    cells: PairDayCell[];
    lockedDates: string[];
    today: string;
    backdrop: PairBackdrop;
    partnerName: string;
    entitled: boolean;
    status: RelationshipStatus | null;
  } | null>(null);
```

import 두 줄을 추가/교체:

```tsx
import { PAIR_TONE_LABEL, getPairStaticLine } from "@/lib/byeolmaru/pair-day";
import type { RelationshipStatus } from "@/lib/relationship/types";
```

fetch 성공 처리를 아래로 교체:

```tsx
        if (Array.isArray(j.cells) && j.cells.length > 0) {
          setPairData({
            cells: j.cells,
            lockedDates: Array.isArray(j.lockedDates) ? j.lockedDates : [],
            today: j.today,
            backdrop: j.backdrop,
            partnerName: j.partnerName,
            entitled: !!j.entitled,
            status: j.status ?? null,
          });
          setPairSelected(j.today);
          return;
        }
```

- [ ] **Step 3: 셀 폴백을 오늘로 고친다**

`pairCell` 계산을 아래로 교체:

```tsx
  // 폴백은 오늘 — cells[0] 은 이번 달 1일이라 첫 진입에서 엉뚱한 날이 열린다.
  const pairCell = pairData
    ? pairData.cells.find((c) => c.date === pairSelected) ??
      pairData.cells.find((c) => c.isToday) ??
      pairData.cells[pairData.cells.length - 1]
    : null;
```

- [ ] **Step 4: 무료에도 그리드를 그리고 한 줄을 선택 셀 기준으로 계산한다**

렌더에서 아래 블록을

```tsx
          {pairData.entitled && (
            <section aria-label="우리 30일 캘린더">
              <CalendarGrid cells={pairGridCells} selectedDate={pairCell.date} onSelect={setPairSelected} />
            </section>
          )}
```

아래로 교체(자격 조건을 **없앤다** — 무료도 1일~오늘 + 점선 칸을 본다):

```tsx
          <section aria-label="이번 달 우리 캘린더">
            <CalendarGrid
              cells={pairGridCells}
              lockedDates={pairData.lockedDates}
              todayDate={pairData.today}
              selectedDate={pairCell.date}
              onSelect={setPairSelected}
            />
          </section>
```

그리고 `PairDayDetailCard` 호출의 `staticLine` prop 을 아래로 바꾼다:

```tsx
            staticLine={pairData.entitled ? null : getPairStaticLine(pairCell, pairData.status)}
```

- [ ] **Step 5: 타입체크·빌드·전체 테스트**

Run: `npx tsc --noEmit`
Expected: 출력 없음(exit 0) — 여기서 CalendarGrid 호출부 에러가 모두 해소된다

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")`
Expected: 전부 PASS (`# fail 0`)

- [ ] **Step 6: 커밋**

```bash
git add app/api/byeolmaru/calendar/route.ts components/byeolmaru/WooriTodayView.tsx
git commit -m "feat(byeolmaru): 우리 오늘 무료를 1칸에서 이번 달 1일~오늘로 확대 (P5-2)"
```

---

### Task 6: 출석 — 별 환급 폐지 + 자동 체크인

스펙 §10. 보상이 사라지면 "출석하기" 버튼도 의미가 없어진다 → **방문이 곧 출석**(사용자 결정).

**Files:**
- Modify: `lib/byeolmaru/attendance.ts` · `lib/byeolmaru/attendance.test.ts`
- Modify: `app/api/byeolmaru/calendar/route.ts`
- Delete: `app/api/byeolmaru/checkin/route.ts`
- Modify: `components/byeolmaru/AttendanceStrip.tsx` · `components/byeolmaru/ByeolmaruHub.tsx`
- Modify: `lib/analytics/ui-events.ts` · `lib/analytics/ui-events.test.ts`
- Create: `supabase/migrations/20260913000000_byeolmaru_drop_reward.sql`

- [ ] **Step 1: 테스트에서 환급 계약을 먼저 지운다**

`lib/byeolmaru/attendance.test.ts` 의 import 를 아래로 바꾸고

```ts
import { computeStreak, type AttendanceState } from "./attendance.ts";
```

마지막 테스트(`"보상 상수 — 구독료(20)보다 작다(매출 붕괴 방지)"`) **블록 전체**를 아래로 교체:

```ts
test("AttendanceState 는 보상 필드를 갖지 않는다(환급 폐지 — 보상은 그날의 운세다)", () => {
  // 타입 레벨 계약이라 런타임 단언이 아니라 tsc 가 지킨다. 아래 객체에 daysThisSub 나 threshold 를
  // 되살리면 초과 속성으로 컴파일이 막힌다 — 폐지가 조용히 되돌아오는 걸 막는 문지기.
  const s: AttendanceState = { checkedInToday: true, streak: 3 };
  assert.equal(s.streak, 3);
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx tsc --noEmit`
Expected: FAIL — `Property 'daysThisSub' is missing`(새 테스트가 축소된 타입을 기대하는데 아직 안 줄었다)

- [ ] **Step 3: attendance.ts 에서 환급을 들어낸다**

`import { chargeStars } from "@/lib/stars";` 와 `import { kstDate } from "@/lib/admin-time";` 두 줄을 **삭제**한다(둘 다 환급 전용이었다).

상수 2줄(`ATTENDANCE_THRESHOLD`·`ATTENDANCE_REWARD_STARS`)을 삭제하고 `AttendanceState` 를 아래로 교체:

```ts
/** 출석 상태 — P5-2 부터 보상이 없다(스펙 §10: 보상은 그날의 운세 = 달력 칸이 열리는 것).
 *  스트릭은 "매일 오는가"의 표시용으로만 남는다. */
export type AttendanceState = {
  checkedInToday: boolean;
  streak: number;
};
```

`activeSub` 함수 **전체**와 `grantDueReward` 함수 **전체**를 삭제한다.

`getAttendanceState` 를 아래로 교체:

```ts
export async function getAttendanceState(userId: string, todayKst: string): Promise<AttendanceState> {
  const supa = getServiceSupabase();
  const since = addDays(todayKst, -60);
  const { data: rows } = await supa
    .from("byeolmaru_checkins")
    .select("checkin_date")
    .eq("user_id", userId)
    .gte("checkin_date", since);
  const dates = (rows ?? []).map((r) => r.checkin_date as string);
  return { checkedInToday: dates.includes(todayKst), streak: computeStreak(dates, todayKst) };
}
```

파일 첫 줄 주석도 정정한다:

```ts
// lib/byeolmaru/attendance.ts — 별마루 출석: 순수 스트릭 + DB(방문 시 자동 체크인·상태 조회).
```

- [ ] **Step 4: 테스트·타입체크 통과 확인**

Run: `node --import tsx --test lib/byeolmaru/attendance.test.ts`
Expected: PASS

- [ ] **Step 5: calendar 라우트를 자동 체크인으로 바꾼다**

`app/api/byeolmaru/calendar/route.ts` 의 아래 블록을

```ts
    // 보상 정산(write)은 best-effort — 실패해도 캘린더는 떠야 한다(로그만 남기고 삼킨다).
    // grantDueReward 는 대개 no-op(만료·미정산 구독 없음).
    try { await grantDueReward(userId); } catch (e) { await logError(e, { route: "/api/byeolmaru/calendar", userId, extra: { stage: "reward" } }); }
    const attendance = await getAttendanceState(userId, todayKst);
```

아래로 교체:

```ts
    // P5-2 — 방문이 곧 출석이다(버튼 폐지). recordCheckin 은 복합 PK upsert 라 멱등이고, 기록 후
    // 최신 상태를 그대로 돌려준다. 쓰기 실패는 best-effort — 캘린더는 떠야 하므로 상태 조회로
    // 폴백한다(스트릭 반영만 늦을 뿐 화면은 정상).
    let attendance;
    try {
      attendance = await recordCheckin(userId, todayKst);
    } catch (e) {
      await logError(e, { route: "/api/byeolmaru/calendar", userId, extra: { stage: "checkin" } });
      attendance = await getAttendanceState(userId, todayKst);
    }
```

import 를 고친다:

```ts
import { getAttendanceState, recordCheckin } from "@/lib/byeolmaru/attendance";
```

- [ ] **Step 6: checkin 라우트와 클라 핸들러를 지운다**

```bash
git rm app/api/byeolmaru/checkin/route.ts
```

`ByeolmaruHub.tsx` 에서 `const [checkinLoading, setCheckinLoading] = useState(false);` 줄과 `handleCheckin` 함수 **전체**를 삭제하고, `AttendanceStrip` 호출을 아래로 교체:

```tsx
      <AttendanceStrip attendance={attendance} filledDays={data.cells.length} />
```

⚠️ `attendance` state 는 그대로 둔다(`refresh()` 가 `data.attendance` 로 채운다).
⚠️ 이 호출은 `const { data } = state;` **뒤**에 있어야 한다 — 현재 코드에서 이미 그 아래다.

- [ ] **Step 7: AttendanceStrip 을 "채움 + 연속"으로 바꾼다**

파일 **전체**를 아래로 교체:

```tsx
// components/byeolmaru/AttendanceStrip.tsx — 이번 달 채움 진행 + 연속 방문. P5-2 부터 별 환급이 없다
// (스펙 §10 — 보상은 그날의 운세다. 매일 오면 칸이 하나씩 열린다).
"use client";
import type { AttendanceState } from "@/lib/byeolmaru/attendance";

interface Props {
  attendance: AttendanceState | null;
  /** 이번 달에 열린 칸 수 = 서버가 내려준 cells 길이(무료는 오늘까지, 구독은 말일까지). */
  filledDays: number;
}

export default function AttendanceStrip({ attendance, filledDays }: Props) {
  if (!attendance) return null;
  return (
    <section className="rounded-2xl bg-cream-warm px-4 py-3">
      <p className="text-sm font-medium text-eye-purple">
        이번 달 {filledDays}칸 채움
        {attendance.streak > 1 ? ` · ${attendance.streak}일 연속` : ""}
      </p>
      <p className="mt-0.5 text-xs text-text-light">매일 오면 그날 칸이 하나씩 열려.</p>
    </section>
  );
}
```

- [ ] **Step 8: `byeolmaru_checkin` UI 이벤트를 지운다**

`lib/analytics/ui-events.ts` 의 `"byeolmaru_checkin",` 줄을 삭제하고, `lib/analytics/ui-events.test.ts` 의 `assert.equal(isUiEvent("byeolmaru_checkin"), true);` 줄을 아래로 교체:

```ts
  assert.equal(isUiEvent("byeolmaru_day_selected"), true);
```

- [ ] **Step 9: 마이그레이션으로 컬럼을 지운다**

Create: `supabase/migrations/20260913000000_byeolmaru_drop_reward.sql`

```sql
-- 20260913000000_byeolmaru_drop_reward.sql — 별마루 출석 별 환급 폐지(P5-2, 스펙 §10).
-- 보상은 이제 "그날의 운세"다 — 달력 칸이 매일 하나씩 열리는 것 자체가 보상이라 10별 환급은 중복이고,
-- 구조가 거꾸로였다(환급은 구독자 전용인데 구독자는 이미 다 열려 있고, 매일 올 이유가 필요한 쪽은
-- 비구독자였다). 지급 코드(grantDueReward)는 같은 커밋에서 삭제됐다.
-- 🔴 prod(main)에 별마루 자체가 아직 없어 정리할 지급 이력이 없다 — dev 에서만 존재하던 컬럼이다.
ALTER TABLE byeolmaru_subscriptions DROP COLUMN IF EXISTS reward_granted_at;
```

- [ ] **Step 10: 전체 테스트 + 타입체크 + 빌드**

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")`
Expected: `# fail 0`

Run: `npx tsc --noEmit`
Expected: 출력 없음

Run: `npm run build`
Expected: exit 0. 라우트 표에서 `/api/byeolmaru/checkin` 이 **사라졌는지** 확인한다.

- [ ] **Step 11: 커밋**

```bash
git add -A lib/byeolmaru/attendance.ts lib/byeolmaru/attendance.test.ts app/api/byeolmaru/calendar/route.ts components/byeolmaru/AttendanceStrip.tsx components/byeolmaru/ByeolmaruHub.tsx lib/analytics/ui-events.ts lib/analytics/ui-events.test.ts supabase/migrations/20260913000000_byeolmaru_drop_reward.sql app/api/byeolmaru/checkin
git commit -m "feat(byeolmaru): 출석 별 환급 폐지 · 방문 자동 체크인 (P5-2)"
```

---

### Task 7: P5-1 이월 ① DAY_LINE 톤 어긋남 ② narrative 하루 이름 주입

**Files:**
- Modify: `lib/byeolmaru/day-label.ts`
- Modify: `lib/byeolmaru/narrative-prompt.ts` · `lib/byeolmaru/narrative-prompt.test.ts`

**근거(실측 3,600칸 = 120명 × 30일, 2026-09-01 기준):** 십신별 `caution` 비율이 편관 83.3% · **정관 36.9%** · **식신 31.7%** · 상관 15.8% · 비견 12.8% 순이다. 편관의 한 줄("다 받아내지 않아도 돼")은 이미 caution 과 맞고, 상관("한 마디가 길게 남을 수도 있어")·편재도 양면을 담고 있다. **정관·식신 두 줄만 낙관 일변도**라 같은 화면의 등급 라벨("살짝 챙길 날")과 정면으로 어긋난다.

- [ ] **Step 1: 두 줄을 톤 중립으로 바꾼다**

`lib/byeolmaru/day-label.ts` 의 `DAY_LINE` 에서 두 항목을 교체:

```ts
  식신: "힘을 빼도 되는 날이야. 잘 챙겨 먹고 잘 쉬는 게 오늘은 제일 쓸모 있을 수 있어. 늘어지는 것 같으면 하나만 가볍게 붙잡아봐.",
```

```ts
  정관: "규칙이나 절차가 앞에 서는 날이야. 미뤄둔 정리를 끝내면 개운하고, 조이는 게 많게 느껴지면 하나씩만 봐도 돼.",
```

`DAY_LINE` 의 JSDoc 을 아래로 교체:

```ts
/** 이름 바로 아래 한 줄. ⑥ 본문 뱅크(tone × relation)와 자리가 달라 겹치지 않는다.
 *  🔴 한 줄은 **톤-블라인드**다(등급을 모른 채 십신으로만 정해진다). 그래서 caution 비율이 높은
 *     십신(실측 3,600칸: 편관 83% · 정관 37% · 식신 32%)의 줄을 낙관 일변도로 쓰면 안 된다 —
 *     같은 화면의 등급 라벨("살짝 챙길 날")과 정면으로 어긋난다. 양면을 담거나 출구를 남길 것. */
```

- [ ] **Step 2: 기존 테스트로 문구 규율을 확인**

Run: `node --import tsx --test lib/byeolmaru/day-label.test.ts`
Expected: PASS — 특히 "한 줄이 반말로 끝난다"(`[다요]` 종결 금지)와 길이 ≥20 을 두 새 문구가 통과해야 한다.

- [ ] **Step 3: narrative 프롬프트에 하루 이름을 넣는 테스트를 쓴다**

먼저 `lib/byeolmaru/narrative-prompt.test.ts` 의 **첫 40줄을 읽어** 기존 테스트가 쓰는 사주 상수 이름과 `buildCalendar` 호출 방식을 확인한다. 그 관례를 그대로 따라 아래 테스트를 파일 끝에 추가한다(아래는 상수명이 `SAJU` 인 경우):

```ts
test("buildNarrativeSystem — 화면에 뜬 하루 이름을 프롬프트가 알고, 재설명을 막는다", () => {
  const t = calcTemporalLuck(baseDateForKst("2026-09-05"), 1996, { includeMonth: true });
  const daily = t.dailyLuck;
  if (!daily) throw new Error("dailyLuck 필요");
  const cell = buildCalendar(SAJU, daily, "2026-09-05")[0];
  const sys = buildNarrativeSystem(SAJU, cell, "갑자");
  assert.ok(sys.includes(DAY_NAME[cell.tenGod]), "하루 이름이 프롬프트에 없다");
  assert.ok(sys.includes(cell.tenGod), "십신 키가 프롬프트에 없다");
  assert.ok(/다시 설명하지 말고/.test(sys), "재설명 금지 지시가 없다");
});
```

파일 상단 import 에 추가: `import { DAY_NAME } from "./day-label.ts";`

- [ ] **Step 4: 테스트 실패 확인**

Run: `node --import tsx --test lib/byeolmaru/narrative-prompt.test.ts`
Expected: FAIL — "하루 이름이 프롬프트에 없다"

- [ ] **Step 5: `buildNarrativeSystem` 에 한 줄을 넣는다**

`lib/byeolmaru/narrative-prompt.ts` 상단에 import 추가:

```ts
import { DAY_NAME } from "./day-label.ts";
```

`buildNarrativeSystem` 을 아래로 교체:

```ts
export function buildNarrativeSystem(saju: SajuResult, cell: DayCell, todayGanji: string): string {
  return [
    loadCore(),
    "",
    "# 별마루 오늘 개인화",
    `너는 '${cell.date}' 하루를 이 사람의 사주로 풀어준다. 오늘 일진은 ${todayGanji}.`,
    `이 사람: ${formatPillars(saju)}. 오늘 등급 ${cell.grade.label}. 축(연애 ${cell.axes.love}·돈 ${cell.axes.money}·일 ${cell.axes.work}).`,
    // P5-1 이월 — 화면 위쪽엔 이미 하루 이름과 한 줄 요약이 떠 있다. 프롬프트가 그걸 모르면 유료
    // 서술이 같은 관계를 처음부터 다시 설명해(무료 요약과 중복) 돈 낸 사람이 같은 말을 두 번 읽는다.
    `화면엔 이미 오늘 이름 '${DAY_NAME[cell.tenGod]}'(${cell.tenGod})과 한 줄 요약이 떠 있다. 그 이름의 뜻을 다시 설명하지 말고, 그게 이 사람의 사주에 구체적으로 어떻게 떨어지는지부터 말해.`,
    "규칙: 3~4문단, 반말, 단정적 예언 금지(흐름·가능성·선택). 첫 문장은 사주 일간과 오늘 일진의 관계로 시작.",
    "마지막은 따뜻한 한 줄. 별표/제목/마커 없이 줄글만.",
  ].join("\n");
}
```

- [ ] **Step 6: 테스트 통과 + 커밋**

Run: `node --import tsx --test lib/byeolmaru/narrative-prompt.test.ts lib/byeolmaru/day-label.test.ts`
Expected: PASS

```bash
git add lib/byeolmaru/day-label.ts lib/byeolmaru/narrative-prompt.ts lib/byeolmaru/narrative-prompt.test.ts
git commit -m "fix(byeolmaru): 정관·식신 한 줄 톤 중립화 + 유료 서술에 하루 이름 주입 (P5-1 이월)"
```

---

### Task 8: P5-1 이월 ③ OG 공유에 하루 이름 싣기

지금 OG 는 등급 3종만 안다 — 10종 중 3종만 바이럴된다.

**Files:**
- Modify: `app/api/og/byeolmaru/saju/route.tsx`
- Modify: `components/byeolmaru/SajuTodayView.tsx`

- [ ] **Step 1: OG 라우트가 `?tg=` 를 화이트리스트로 받게 한다**

`app/api/og/byeolmaru/saju/route.tsx` 의 import 에 추가:

```tsx
import { DAY_NAME } from "@/lib/byeolmaru/day-label";
import type { TenGod } from "@/lib/saju/pairing";
```

`const ganji = ganjiRaw ? ganjiRaw.slice(0, GANJI_MAX_LEN) : null;` **다음 줄**에 추가:

```tsx
  // 🔴 하루 이름을 URL 로 그대로 받지 않는다 — 임의 문자열이 별콩톡 브랜드 카드에 렌더되면 그건
  //    남의 문구를 우리 OG 로 찍어주는 것이다. 십신 키만 받아 **서버가 DAY_NAME 으로 푼다**.
  //    모르는 값은 400 이 아니라 조용히 무시한다(옛 링크·오타가 깨진 이미지가 되지 않게).
  const tgRaw = sp.get("tg");
  const dayName = tgRaw && tgRaw in DAY_NAME ? DAY_NAME[tgRaw as TenGod] : null;
```

- [ ] **Step 2: 하루 이름을 제목으로, 등급을 부제로 그린다**

가운데 블록(`<div style={{ flex: 1, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }}>` 부터 그 `</div>` 까지)을 아래로 교체:

```tsx
        <div style={{ flex: 1, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }}>
          {/* 하루 이름이 제목 — 등급 3종보다 10종이 공유될 때 더 다양하고 "내 얘기"로 읽힌다.
              tg 없는 옛 링크(P5-2 이전 공유분)는 예전처럼 등급만 크게 나온다(하위호환). */}
          <div style={{ display: "flex", fontSize: dayName ? 64 : 72, color: "#F2D78A", fontWeight: 700, textAlign: "center" }}>
            {dayName ?? label}
          </div>
          {dayName ? (
            <div style={{ display: "flex", fontSize: 30, color: "rgba(255,255,255,0.85)", marginTop: 16 }}>
              {label}
            </div>
          ) : null}
          {ganji ? (
            <div style={{ display: "flex", fontSize: 28, color: "rgba(255,255,255,0.8)", marginTop: dayName ? 12 : 22 }}>
              오늘 · {ganji}
            </div>
          ) : null}
        </div>
```

- [ ] **Step 3: 공유 버튼이 `tg` 를 붙이게 한다**

`components/byeolmaru/SajuTodayView.tsx` 상단에 `import { DAY_NAME } from "@/lib/byeolmaru/day-label";` 를 추가하고, `shareToKakao({...})` 호출을 아래로 교체:

```tsx
            const ok = shareToKakao({
              title: `오늘 사주 · ${DAY_NAME[cell.tenGod]}`,
              description: "오늘 네 하루 흐름, 별마루에서 무료로 매일 확인해봐.",
              imageUrl: `${window.location.origin}/api/og/byeolmaru/saju?grade=${cell.grade.tone}&ganji=${encodeURIComponent(cell.ganji)}&tg=${encodeURIComponent(cell.tenGod)}`,
              link: `${window.location.origin}/byeolmaru`,
              buttonTitle: "나도 보러가기",
            });
```

- [ ] **Step 4: OG 를 실제로 렌더해 눈으로 확인한다**

Run: dev 서버를 띄운다(Browser pane 의 preview_start 사용 — Bash 로 `npm run dev` 를 띄우지 말 것).

다음 4개를 연다:
1. `http://localhost:3000/api/og/byeolmaru/saju?grade=good&ganji=갑자&tg=정인` — "날 감싸주는 결"이 크게, "잘 맞는 날"이 아래 작게
2. `http://localhost:3000/api/og/byeolmaru/saju?grade=caution&ganji=경오&tg=편관` — "긴장이 도는 날" + "살짝 챙길 날"
3. `http://localhost:3000/api/og/byeolmaru/saju?grade=normal&ganji=임신` — **tg 없음**: 예전처럼 "무난한 날"이 크게(하위호환)
4. `http://localhost:3000/api/og/byeolmaru/saju?grade=good&tg=아무말` — `tg` 가 무시되고 등급만 렌더(400 아님, 임의 문자열이 화면에 없어야 한다)

Expected: 4개 모두 1200×630 이미지가 뜨고, 3·4번은 옛 레이아웃이다.

- [ ] **Step 5: 전체 검증 + 커밋**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0

```bash
git add app/api/og/byeolmaru/saju/route.tsx components/byeolmaru/SajuTodayView.tsx
git commit -m "feat(byeolmaru): 오늘 사주 OG 에 십신 하루 이름 (P5-1 이월)"
```

---

## 최종 검증 (모든 태스크 후)

- [ ] **전체 테스트**

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")`
Expected: `# fail 0`

- [ ] **타입체크 · 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0. 라우트 표에 `ƒ Proxy` 가 있고 `/api/byeolmaru/checkin` 이 없는지 확인.

- [ ] **브라우저 실렌더 — 무료 유저**

`/byeolmaru/saju` 에서 확인할 것:
1. 달력이 **이번 달 1일부터** 시작하고 1일의 요일에 맞춰 앞이 비어 있다
2. 오늘 **이후** 칸이 점선 + 옅은 점이고 **탭이 안 먹는다**
3. 그리드 아래에 글리프 범례와 "점선 칸은 아직 안 온 날이야" 가 뜬다
4. 요약이 "오늘까지, 잘 맞는 날 N일" 이다
5. **devtools Network 에서 `/api/byeolmaru/calendar` 응답을 열어 `cells` 에 오늘 이후 날짜가 하나도 없는지 확인** — 이게 무료선의 진짜 검증이다
6. 출석 스트립에 별 얘기가 없고 "이번 달 N칸 채움" 이다

- [ ] **브라우저 실렌더 — 구독 유저**

체험 시작 또는 구독 후:
1. 달력에 점선 칸이 **하나도 없고** 말일까지 전부 칠해져 있다
2. 허브 7점 스트립이 **오늘부터 앞으로** 7일이다
3. `/byeolmaru/woori` 에서 상대를 고르면 이번 달 전체가 뜬다

- [ ] **브라우저 실렌더 — 우리 오늘 무료**

비구독 상태로 `/byeolmaru/woori` → 상대 선택:
1. **캘린더가 뜬다**(전엔 구독자만 떴다) — 1일~오늘 + 점선
2. 과거 날을 고르면 그 날 기준 한 줄이 바뀐다(오늘 문구가 박혀 있지 않다)
3. 응답 `cells` 에 미래가 없다

- [ ] **계측 확인**

달력에서 오늘이 아닌 날을 눌러 `byeolmaru_day_selected` 의 `meta.offset` 이 **오늘로부터의 일수 차이**(과거면 음수)인지 확인한다. 배열 인덱스가 아니어야 한다.

---

## Self-Review 기록

**스펙 §14 "P5-2" 항목별 커버리지**

| 스펙 항목 | 태스크 |
|---|---|
| `buildCalendar` 를 이번 달로 | T1(범위 계산) + T2(라우트) |
| 안 온 날 / 지나간 날 상태 | T2(서버 분할) + T3(그리드 렌더) |
| 구독 = 앞당겨 보기 | T2 `splitByFreeLine(…, entitled)` |
| 비자격 pair 응답 1칸 → 이번 달 1일~오늘 (§8) | T5 |
| 출석 환급 폐지 및 스트립 대체 (§10) | T6 |
| (스펙 §13) offset 계측 정의 보존 | T3 Step 3 |
| (P5-1 이월) DAY_LINE·narrative·OG·범례 | T7, T8, T3 Step 4 |

**의도적으로 남긴 것**

- `weekBuckets` 의 "7일씩 롤링" 성격은 그대로다 — 이번 달 1일부터 7일씩 끊기므로 그리드 주(일~토)와 여전히 경계가 다르다. `calendar.ts:62` 의 기존 경고 주석이 계속 유효하다. 월 기준 주차로 재정의하는 건 P5-3 허브 재조립에서 표시 형태와 함께 정한다.
- `PairDayDetailCard` 의 "앞으로 30일 흐름까지 펼쳐주면…" 블러 문구는 P5-4(페이월 맥락 분산)에서 통째로 다시 쓴다. 여기서 문구만 고치면 두 번 고치게 된다.
- 비로그인 `GuestPeek` 은 달력을 안 그린다(P5-3 §12 의 "안 칠해진 이번 달"이 그 일을 한다).
