# 별마루 ⑤-b — good_days 진열/추천 폐지 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 또는 executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** 죽은 good_days(35별·구매0)를 **진열·추천 표면 4곳에서만** 걷어낸다. 타입·config·생성·SajuProduct·reco enum·계약테스트는 **전부 보존**(기존 리딩 재열람 + tsc + 테스트 무결성).

**Architecture:** 제거 작업(신규 코드 없음). 기존 tarot 타입 은퇴 선례(`active:false` + config 유지)를 그대로 따른다. 검증은 "새 테스트 작성"이 아니라 **기존 계약 테스트 전량 GREEN 유지 + tsc + build + 옛 리딩 재열람**으로 한다.

**Tech Stack:** Next.js 16, TypeScript strict, `node --import tsx --test`.

**설계:** `docs/superpowers/specs/2026-09-05-별마루-5-원카드-폐지-낙수-design.md` §3(제거 4곳 + 보존 목록)·§4(one_card 유지). 스코프 결정 = 2026-09-05 사용자("좋은날만 폐지, 원카드 유지").

**🔴 보존 불변식 (어떤 태스크도 건드리지 말 것):**
- `FORTUNE_CONFIG.good_days` 레코드 · `FortuneType` union · `FORTUNE_GRADIENTS`/`FORTUNE_HASHTAGS`/`FORTUNE_LENGTH_HINT`/`FORTUNE_CATEGORY` 의 good_days 키 (전부 `Record<FortuneType>` — 빼면 tsc 깨짐)
- `lib/saju/products.ts` 의 good_days (SAJU_PRODUCTS·SAJU_PRODUCT_INFO·isSajuProduct — 옛 상담 리딩 라벨·아이콘)
- `lib/fortune/{hero,prompt,generic-report,existing-lookup,response-format,model}.ts`·`lib/claude.ts` 의 good_days 생성 분기 (기존 리포트 재열람)
- `lib/reco-utils.ts` 의 `saju:good_days` (RecoProduct·RECO_PRODUCTS·RECO_DISPLAY) — 페르소나 유도가 사라져 휴면, 라우팅(`/saju` + 보존 SajuProduct)과 일관
- `components/upsell/cross-cards.ts` 의 `RELATED_SAJU.good_days` (pickValid active 가드가 자동 제외)
- 계약 테스트 전부 (`types.test`·`model.test`·`response-format.test`·`route-labels.test`·`reco-utils.test`·`cross-cards.test`·`cases.test`)
- one_card / `lib/tarot/spreads.ts` 전부 (폐지 취소)

---

### Task 1: `/fortune` 좋은 날 리포트 진열 폐지 (FORTUNE_LIST 제거 + active:false)

**Files:**
- Modify: `lib/fortune/types.ts` (FORTUNE_LIST 배열, good_days config)

- [ ] **Step 1: FORTUNE_LIST 에서 good_days 한 줄 삭제**

`FORTUNE_LIST` 배열(현 `FORTUNE_CONFIG.monthly,` 와 `FORTUNE_CONFIG.nature_self,` 사이)에서 아래 한 줄 제거:

```ts
  FORTUNE_CONFIG.good_days,
```

→ 카탈로그(`app/fortune/page.tsx` 의 `fortuneProductsByCategory`)·`timing` 카테고리·크로스셀 `sameBase`·`app/start` 목록에서 사라짐.

- [ ] **Step 2: good_days config 를 active:false + 은퇴 주석**

`FORTUNE_CONFIG.good_days` 레코드에서:

```ts
    emotionTag: `${FORTUNE_SENTINEL_PREFIX}good_days`,
    href: "/fortune/good_days",
    active: true,
  },
```

를 아래로 (기존 tarot 은퇴 타입 주석 관행과 동일):

```ts
    emotionTag: `${FORTUNE_SENTINEL_PREFIX}good_days`,
    href: "/fortune/good_days",
    // 폐지(2026-09-05, ⑤-b) — config 는 기존 리딩 재열람/Record<FortuneType> 무결성 위해 유지.
    // active:false → 직링크 /fortune/good_days 는 [type]/page.tsx valid 가드가 /fortune 로 리다이렉트.
    active: false,
  },
```

- [ ] **Step 3: tsc 확인**

Run: `npx tsc --noEmit`
Expected: 에러 0 (config 레코드를 유지하므로 Record<FortuneType> 맵 전부 온전).

- [ ] **Step 4: fortune 계약 테스트 GREEN 확인**

Run: `node --import tsx --test lib/fortune/types.test.ts lib/fortune/model.test.ts lib/fortune/response-format.test.ts lib/analytics/route-labels.test.ts`
Expected: 전부 PASS. (types.test 의 timing 테스트는 daily-부재·saju_full-존재만 검사하고 good_days 멤버십은 안 봄 → 영향 없음.)

- [ ] **Step 5: Commit**

```bash
git add lib/fortune/types.ts
git commit -m "feat(byeolmaru): good_days /fortune 진열 폐지 — FORTUNE_LIST 제거 + active:false (⑤-b Task1)"
```

---

### Task 2: 타로 페이지 good_days 크로스링크 제거

**Files:**
- Modify: `app/tarot/page.tsx` (크로스링크 블록)

- [ ] **Step 1: "언제 연락 올까" 태그의 good_days 링크 블록 삭제**

아래 블록을 통째로 제거(그 아래 `compat-social` 블록은 유지):

```tsx
        {normalizedTag === "언제 연락 올까, 타이밍이 궁금해" && (
          <Link
            href="/fortune/good_days"
            className="flex items-center justify-between p-3.5 rounded-2xl border border-dashed border-lilac-mid/60 bg-cream/50"
          >
            <span className="text-[12.5px] text-eye-purple">
              📅 정확한 날짜가 궁금하면 <b>사주 좋은 날 리포트</b>로
            </span>
            <span className="text-text-light text-[12px]">›</span>
          </Link>
        )}
```

- [ ] **Step 2: 남은 참조 없음 확인 + tsc**

Run: `npx tsc --noEmit`
Expected: 에러 0. (`normalizedTag` 는 다른 블록에서도 쓰이므로 미사용 경고 없음 — grep 로 재확인.)

Run: 이 파일에 `good_days` 잔존 0 확인 (Grep `good_days` in `app/tarot/page.tsx`).

- [ ] **Step 3: Commit**

```bash
git add app/tarot/page.tsx
git commit -m "feat(byeolmaru): 타로 페이지 good_days 크로스링크 제거 (⑤-b Task2)"
```

---

### Task 3: 타로톡 페르소나 good_days 추천 제거

**Files:**
- Modify: `data/persona/byeolkong_tarot.md` (good_days 유도 섹션)

- [ ] **Step 1: good_days 유도 섹션 전체 삭제**

`## 달력 수준의 시점을 거듭 파고들면 — 사주로 잇기 (대화당 1회)` 헤딩부터 그 아래 `**유저가 수락하면 ("사주 볼래" 류)**: ... + [END].` 문단까지 **섹션 전체(헤딩 + 5문단)** 를 삭제. (이 섹션은 전부 good_days 유도·수락 처리 전용. 삭제 후 날짜 질문은 공통 코어 §"언제·될까"가 그대로 처리한다 — 섹션 자신이 그 폴백을 명시.)

⚠️ `[RECO:saju:good_days]` 문자열이 이 파일에서 완전히 사라져야 한다.

- [ ] **Step 2: 잔존 확인**

Run: Grep `good_days` in `data/persona/byeolkong_tarot.md`
Expected: 매치 0.

- [ ] **Step 3: Commit**

```bash
git add data/persona/byeolkong_tarot.md
git commit -m "feat(byeolmaru): 타로톡 페르소나 good_days 추천 제거 (⑤-b Task3)"
```

---

### Task 4: 폐지 무결성 검증 게이트

**Files:** (없음 — 검증 전용)

- [ ] **Step 1: 남은 진열/추천 표면 0 감사**

Grep `good_days` 전역 → 남는 매치가 **전부 보존 목록**(config 레코드·생성·SajuProduct·reco enum·cross-cards RELATED·계약테스트·docs·scripts)에 속하는지 육안 확인. **진열/추천/링크 성격의 잔존이 0** 이어야 한다.

- [ ] **Step 2: 전체 테스트 GREEN**

Run: `node --import tsx --test lib/**/*.test.ts qa/**/*.test.ts`
(또는 프로젝트의 표준 테스트 실행 방식)
Expected: 전부 PASS. 특히 `reco-utils.test`·`cross-cards.test`·`cases.test` 포함.

- [ ] **Step 3: tsc + 프로덕션 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: exit 0.

- [ ] **Step 4: 브라우저 회귀 (dev 서버 재시작 후 — 페르소나 모듈 캐시)**

- `/fortune` 카탈로그: 모든 칩(특히 timing)에서 **"좋은 날 리포트" 카드가 안 보임** + "준비 중" 으로도 안 뜸.
- `/fortune/good_days` 직접 진입 → **`/fortune` 로 리다이렉트**(구매창 미표시).
- 기존 good_days **리포트** 재열람: `/fortune/result?id=<기존 good_days reading>` → 정상 렌더(생성 코드 보존).
- (있다면) 기존 good_days **상담** 리딩: `/readings` 에서 상품 라벨·아이콘 정상(SAJU_PRODUCTS 보존).

- [ ] **Step 5: 최종 커밋(없으면 skip) + dev push**

```bash
git push origin dev
```

Expected: dev Preview 배포 + Supabase 마이그레이션 없음(코드 전용).

---

## Self-Review 체크
- [ ] 제거는 §3 의 4곳뿐인가? (FORTUNE_LIST·active·tarot 링크·페르소나)
- [ ] 보존 불변식(위 목록)을 하나도 안 건드렸는가?
- [ ] 계약 테스트 전량 GREEN + tsc + build exit0?
- [ ] 기존 good_days 리딩 재열람이 살아있는가?
- [ ] one_card / spreads.ts 는 손대지 않았는가?
