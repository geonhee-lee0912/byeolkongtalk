# 우리 사이 — 스킬 연속성 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** "우리 사이" 스킬(4종)을 소환하면 별도 화면으로 나갔다가, 돌아오면 별콩이가 결과를 짚으며 대화가 자연스럽게 이어지게 한다.

**Architecture:** 기존 `[CHECKIN]` 복귀 장치를 스킬용으로 확장. ① 페르소나 규칙으로 소환 예고, ② 결과 화면에 스레드 복귀 CTA, ③ `logSkillToThread`가 `memo.pending_skill_recap` seed → 복귀 시 `ThreadChat`이 무과금 템플릿 인사 버블 1회 + 서버 소진. 스킬의 실제 이어짐은 기존 `skill_log` 파일블록 주입으로 모델이 담당.

**Tech Stack:** Next.js 16 (App Router, React 19), TypeScript, Supabase, Claude Sonnet 5. 유닛 테스트 = `node:test` + `tsx`.

**Spec:** [docs/superpowers/specs/2026-07-23-우리사이-스킬-연속성-design.md](../specs/2026-07-23-우리사이-스킬-연속성-design.md)

**공통 명령어**
- 유닛 테스트: `node --import tsx --test lib/relationship/<파일>.test.ts`
- 타입 체크: `npx tsc --noEmit`

---

## Task 0 (확인만, 코드 없음): 싸움 판정 복귀 동선

`app/relationship/verdict/[id]/page.tsx`는 이미 `[END]` 시 "대화로 돌아가기 →" Link(`/relationship`)를 노출한다(279–284행). **판정 스킬의 ② 복귀 동선은 이미 충족 — 변경 없음.** (원하면 카피만 통일하되 이 플랜에선 생략.)

---

## Task 1: `applySkillToMemo` 순수 함수 + `pending_skill_recap` 타입

**Files:**
- Modify: `lib/relationship/types.ts` (`RelationshipMemo`에 필드 추가, 54행 부근)
- Modify: `lib/relationship/memory.ts` (순수 함수 추가, 파일 끝)
- Test: `lib/relationship/memory.test.ts`

- [ ] **Step 1: 타입 추가** — `lib/relationship/types.ts`의 `RelationshipMemo`에 필드 추가:

```ts
export interface RelationshipMemo {
  prescriptions?: { text: string; created_at: string; resolved_at?: string }[];
  pending_checkin?: { text: string; created_at: string } | null;
  skill_log?: { skill: string; reading_id: string; summary: string; created_at: string }[];
  pending_skill_recap?: { skill: string; summary: string; created_at: string } | null;
}
```

(기존 다른 필드가 더 있으면 그대로 두고 `pending_skill_recap`만 추가.)

- [ ] **Step 2: 실패 테스트 작성** — `lib/relationship/memory.test.ts` 상단 import에 `applySkillToMemo` 추가하고 타입 import, 파일 끝에 테스트 추가:

```ts
import { splitThreadMessages, RECENT_MSGS, SUMMARY_TRIGGER, buildRelationshipFileBlock, applySkillToMemo } from "./memory.ts";
import type { RelationshipMemo } from "./types.ts";
```

```ts
test("applySkillToMemo — skill_log 적립 + pending_skill_recap 세팅 + summary 정규화", () => {
  const out = applySkillToMemo({}, "compat", "r1", "  케미   좋음  ", "2026-07-23T00:00:00Z");
  assert.equal(out.skill_log?.length, 1);
  assert.equal(out.skill_log?.[0].skill, "compat");
  assert.equal(out.skill_log?.[0].reading_id, "r1");
  assert.equal(out.pending_skill_recap?.skill, "compat");
  assert.equal(out.pending_skill_recap?.summary, "케미 좋음");
  assert.equal(out.pending_skill_recap?.created_at, "2026-07-23T00:00:00Z");
});

test("applySkillToMemo — skill_log 최근 20개만 유지", () => {
  let memo: RelationshipMemo = {};
  for (let i = 0; i < 25; i++) memo = applySkillToMemo(memo, "verdict", `r${i}`, `s${i}`, "2026-07-23T00:00:00Z");
  assert.equal(memo.skill_log?.length, 20);
  assert.equal(memo.skill_log?.[0].reading_id, "r5"); // 앞 5개 잘림
});
```

- [ ] **Step 3: 실패 확인**

Run: `node --import tsx --test lib/relationship/memory.test.ts`
Expected: FAIL (`applySkillToMemo is not a function` / export 없음)

- [ ] **Step 4: 구현** — `lib/relationship/memory.ts` 파일 끝에 추가 (`RelationshipMemo`는 1행에서 이미 import됨):

```ts
/** 스킬 완료 결과를 memo에 반영 — skill_log 적립(최근 20개) + pending_skill_recap(복귀 인사) 세팅. 순수 함수. */
export function applySkillToMemo(
  memo: RelationshipMemo,
  skillKey: string,
  readingId: string,
  summary: string,
  nowIso: string
): RelationshipMemo {
  const s = summary.replace(/\s+/g, " ").trim().slice(0, 120);
  return {
    ...memo,
    skill_log: [
      ...(memo.skill_log ?? []),
      { skill: skillKey, reading_id: readingId, summary: s, created_at: nowIso },
    ].slice(-20),
    pending_skill_recap: { skill: skillKey, summary: s, created_at: nowIso },
  };
}
```

- [ ] **Step 5: 통과 확인**

Run: `node --import tsx --test lib/relationship/memory.test.ts`
Expected: PASS (기존 테스트 + 신규 2개)

- [ ] **Step 6: 커밋**

```bash
git add lib/relationship/types.ts lib/relationship/memory.ts lib/relationship/memory.test.ts
git commit -m "feat(relationship): applySkillToMemo — skill_log + pending_skill_recap seed"
```

---

## Task 2: `logSkillToThread`가 `applySkillToMemo` 사용 (4종 recap seed)

**Files:**
- Modify: `lib/relationship/skill-log.ts` (전체 교체)

- [ ] **Step 1: 구현** — `lib/relationship/skill-log.ts` 전체를 아래로 교체:

```ts
// lib/relationship/skill-log.ts — 스킬 완료 결과를 관계 memo에 적립.
// skill_log(별콩이 기억) + pending_skill_recap(복귀 시 스레드 인사 버블) 을 함께 세팅.
// 스킬 reading([END]/리포트 완료) 시 호출 → 이후 스레드에서 별콩이가 "저번에 궁합 봤을 때~" 참조.
import { getServiceSupabase } from "@/lib/supabase";
import { applySkillToMemo } from "./memory";
import type { RelationshipMemo } from "./types";

export async function logSkillToThread(
  relationshipId: string,
  skillKey: string,
  readingId: string,
  summary: string
): Promise<void> {
  const supabase = getServiceSupabase();
  const { data: rel } = await supabase
    .from("relationships")
    .select("memo")
    .eq("id", relationshipId)
    .maybeSingle();
  if (!rel) return;
  const memo = applySkillToMemo(
    (rel.memo ?? {}) as RelationshipMemo,
    skillKey,
    readingId,
    summary,
    new Date().toISOString()
  );
  await supabase.from("relationships").update({ memo }).eq("id", relationshipId);
}
```

(호출부 3곳 — `app/api/fortune/create/route.ts`, `app/api/consultations/tarot/chat/route.ts`, `app/api/relationship/verdict/chat/route.ts` — 시그니처 동일하므로 수정 불필요.)

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add lib/relationship/skill-log.ts
git commit -m "refactor(relationship): logSkillToThread uses applySkillToMemo (seeds recap for all 4 skills)"
```

---

## Task 3: `buildSkillRecapText` 순수 함수

**Files:**
- Modify: `lib/relationship/skills.ts` (파일 끝)
- Test: `lib/relationship/skills.test.ts`

- [ ] **Step 1: 실패 테스트 작성** — `lib/relationship/skills.test.ts` import에 `buildSkillRecapText` 추가, 파일 끝에:

```ts
import { RELATIONSHIP_SKILLS, getSkill, listActiveSkills, buildSkillRecapText } from "./skills.ts";
```

```ts
test("buildSkillRecapText — 라벨/이모지/요약 반영", () => {
  const t = buildSkillRecapText("compat", "케미 좋음");
  assert.ok(t.includes("우리 궁합"));
  assert.ok(t.includes("💑"));
  assert.ok(t.includes("케미 좋음"));
});

test("buildSkillRecapText — 미지 스킬/빈 요약 폴백", () => {
  const t = buildSkillRecapText("unknown", "");
  assert.ok(t.includes("스킬"));
  assert.ok(t.length > 0);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/relationship/skills.test.ts`
Expected: FAIL (`buildSkillRecapText` export 없음)

- [ ] **Step 3: 구현** — `lib/relationship/skills.ts` 파일 끝에 추가:

```ts
/** 복귀 인사 버블 텍스트 — 스킬 결과를 보고 스레드로 돌아왔을 때 별콩이가 먼저 짚는 한마디. */
export function buildSkillRecapText(skillKey: string, summary: string): string {
  const skill = getSkill(skillKey);
  const label = skill?.label ?? "스킬";
  const emoji = skill?.emoji ?? "✨";
  const s = summary.trim();
  return `${emoji} ${label} 결과 봤어! ${s ? s + " — " : ""}이거 갖고 더 얘기하고 싶은 거 있으면 편하게 꺼내봐.`;
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/relationship/skills.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/relationship/skills.ts lib/relationship/skills.test.ts
git commit -m "feat(relationship): buildSkillRecapText — 복귀 인사 버블 텍스트"
```

---

## Task 4: 소진 라우트 `POST /api/relationship/recap-seen`

**Files:**
- Create: `app/api/relationship/recap-seen/route.ts`

- [ ] **Step 1: 구현** — 신규 파일 작성:

```ts
// app/api/relationship/recap-seen/route.ts — 복귀 인사 버블 소진(1회).
// ThreadChat이 버블을 렌더한 뒤 fire-and-forget으로 호출 → pending_skill_recap 제거.
import { NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import type { RelationshipMemo } from "@/lib/relationship/types";

export const dynamic = "force-dynamic";

export async function POST() {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required" }, { status: 401 });

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase
    .from("relationships")
    .select("id, memo")
    .eq("user_id", userId)
    .maybeSingle();
  if (!rel) return NextResponse.json({ ok: true });

  const memo = (rel.memo ?? {}) as RelationshipMemo;
  if (memo.pending_skill_recap) {
    memo.pending_skill_recap = null;
    await supabase.from("relationships").update({ memo }).eq("id", rel.id);
  }
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add app/api/relationship/recap-seen/route.ts
git commit -m "feat(relationship): POST /api/relationship/recap-seen — 복귀 인사 소진"
```

---

## Task 5: `GET /api/relationship` 응답에 `recap` 추가

**Files:**
- Modify: `app/api/relationship/route.ts` (import 8행, GET 응답 39–50행)

- [ ] **Step 1: import에 타입 추가** — 8행:

```ts
import { dailyTurnAllowance, type RelationshipStatus, type RelationshipMemo } from "@/lib/relationship/types";
```

- [ ] **Step 2: 응답에 recap 추가** — GET의 `return NextResponse.json({ ... })` 에 top-level 필드 추가:

```ts
  return NextResponse.json({
    relationship: {
      id: rel.id, label: rel.label, status: rel.status,
      selfProfileId: rel.self_profile_id, partnerProfileId: rel.partner_profile_id,
      threadReadingId: rel.thread_reading_id, memo: rel.memo,
    },
    pass: pass ? { kind: pass.kind, expiresAt: pass.expires_at } : null,
    daily: pass
      ? { used: todayTurns, allowance: dailyTurnAllowance(todayExtend), extendCount: todayExtend }
      : null,
    messages: msgRows ?? [],
    recap: (rel.memo as RelationshipMemo | null)?.pending_skill_recap ?? null,
  });
```

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add app/api/relationship/route.ts
git commit -m "feat(relationship): GET /api/relationship returns recap"
```

---

## Task 6: `ThreadChat` — 복귀 인사 버블 렌더 + 소진

**Files:**
- Modify: `components/relationship/ThreadChat.tsx` (import, props, mount effect)

- [ ] **Step 1: import 추가** — 13행 부근(기존 skills import 옆):

```ts
import { getSkill, buildSkillRecapText } from "@/lib/relationship/skills";
```

(기존 `import { getSkill } from "@/lib/relationship/skills";` 를 위 한 줄로 교체.)

- [ ] **Step 2: prop 추가** — `ThreadChatProps` 인터페이스에 추가(80행 부근):

```ts
  /** 스킬 결과를 보고 복귀했을 때 1회 노출할 인사 버블(무과금·비영속). 활성 스레드(S3)에서만 전달. */
  skillRecap?: { skill: string; summary: string } | null;
```

그리고 컴포넌트 시그니처 구조분해에 `skillRecap = null,` 추가(93행 부근).

- [ ] **Step 3: 마운트 이펙트 추가** — 컴포넌트 내부, 기존 스크롤 이펙트(118–122행) 아래에:

```ts
  // 복귀 인사 버블 — 스킬 결과 보고 돌아왔을 때 1회. 모델·턴·패스 소모 없음(클라 전용, DB 미저장).
  const recapShownRef = useRef(false);
  useEffect(() => {
    if (!skillRecap || recapShownRef.current) return;
    recapShownRef.current = true;
    setMessages((prev) => [
      ...prev,
      {
        role: "assistant",
        content: buildSkillRecapText(skillRecap.skill, skillRecap.summary),
        createdAt: new Date().toISOString(),
      },
    ]);
    void fetch("/api/relationship/recap-seen", { method: "POST" }).catch(() => {});
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);
```

(버블 content엔 `[SKILL:]` 마커가 없으므로 실행 칩은 안 뜨고 일반 별콩이 말풍선으로 렌더된다.)

- [ ] **Step 4: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add components/relationship/ThreadChat.tsx
git commit -m "feat(relationship): ThreadChat renders skill recap bubble + consume"
```

---

## Task 7: `/relationship` 페이지가 `skillRecap` 전달 (S3만)

**Files:**
- Modify: `app/relationship/page.tsx` (state, load, S3 렌더의 ThreadChat)

- [ ] **Step 1: recap state 추가** — 다른 useState 아래(59행 부근):

```ts
  const [recap, setRecap] = useState<{ skill: string; summary: string } | null>(null);
```

- [ ] **Step 2: load()에서 세팅** — `setBalance(...)` 근처(87행 부근)에 추가:

```ts
    setRecap((rel?.recap as { skill: string; summary: string } | null) ?? null);
```

- [ ] **Step 3: S3 렌더의 ThreadChat에 prop 전달** — 277–288행의 `<ThreadChat …/>` (S3 — `canSend={!capReached}` 쪽)에 추가:

```tsx
        <ThreadChat
          className="flex-1 min-h-0"
          relationshipId={relationship.id}
          initialMessages={messages}
          canSend={!capReached}
          capReached={capReached}
          selfProfileId={relationship.selfProfileId}
          partnerProfileId={relationship.partnerProfileId}
          skillRecap={recap}
          onDailyCapReached={() => void load()}
          onExtended={() => void load()}
          onPassRequired={() => void load()}
        />
```

(S2 읽기전용 렌더 224–231행에는 `skillRecap`을 **전달하지 않는다** — 패스 없는 상태에선 인사 버블 생략.)

- [ ] **Step 4: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add app/relationship/page.tsx
git commit -m "feat(relationship): pass skillRecap to ThreadChat (S3)"
```

---

## Task 8: `/api/readings/[id]`가 `relationshipId` 반환 (② CTA 게이트)

**Files:**
- Modify: `app/api/readings/[id]/route.ts` (GET select 24행 + 응답 52–65행)

- [ ] **Step 1: select에 컬럼 추가** — 24행 select 문자열 끝에 `, relationship_id` 추가:

```ts
    .select(
      "id, user_id, profile_id, question, saju_data, consultation_type, spread_type, spread_category, emotion_tag, drawn_cards, stars_spent, has_sensitive, next_reco, created_at, relationship_id"
    )
```

- [ ] **Step 2: 응답에 필드 추가** — reading 객체(52–64행)에 추가:

```ts
      nextReco: reading.next_reco ?? null,
      createdAt: reading.created_at,
      relationshipId: reading.relationship_id ?? null,
```

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add app/api/readings/[id]/route.ts
git commit -m "feat(readings): expose relationshipId on GET /api/readings/[id]"
```

---

## Task 9: 궁합 결과 화면(`/fortune/result`)에 "우리 사이로 돌아가기" CTA

**Files:**
- Modify: `app/fortune/result/page.tsx` (state, load, CTA)

- [ ] **Step 1: state 추가** — `FortuneResultInner` 내 useState 그룹(180행 부근)에:

```ts
  const [relationshipId, setRelationshipId] = useState<string | null>(null);
```

- [ ] **Step 2: load()에서 세팅** — 리포트 파싱 성공 경로(`if (r.reading.createdAt) setCreatedAt(...)` 근처, 247행 부근)에 추가:

```ts
      setRelationshipId(r.reading.relationshipId ?? null);
```

- [ ] **Step 3: CTA 추가** — `!isPublic` 브랜치(506–536행) 안, `<>` 최상단(카카오 공유 버튼 위)에:

```tsx
          <>
            {relationshipId && (
              <Link
                href="/relationship"
                className="w-full py-3.5 rounded-xl bg-lilac-deep text-white font-bold text-[15px] text-center hover:bg-lilac-deep/90 active:scale-[0.98] transition"
              >
                💬 우리 사이로 돌아가 이어 얘기하기
              </Link>
            )}
            <button
              onClick={handleKakaoShare}
              ...
```

(`Link`는 이미 import됨. relationshipId 있을 때만 = 궁합 스킬 reading에만 노출.)

- [ ] **Step 4: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add app/fortune/result/page.tsx
git commit -m "feat(fortune): 관계 스킬 궁합 결과에 우리 사이 복귀 CTA"
```

---

## Task 10: 타로 스킬 결과 화면(`/tarot/result`)에 복귀 CTA

**Files:**
- Modify: `app/tarot/result/page.tsx` (FetchData 타입 37–48행, CTA)

- [ ] **Step 1: 타입에 필드 추가** — `FetchData.reading`(37–48행)에:

```ts
    nextReco: NextReco | null;
    createdAt: string;
    relationshipId: string | null;
  };
```

- [ ] **Step 2: CTA 추가** — 상단 헤더(155–162행) 아래, "고민 분류" 블록 위에:

```tsx
      {reading.relationshipId && (
        <div className="w-full max-w-md mx-auto px-5 mb-4">
          <Link
            href="/relationship"
            className="w-full py-3 rounded-xl bg-lilac-deep text-white font-bold text-[14px] text-center flex items-center justify-center gap-1.5 active:scale-[0.98] transition"
          >
            💬 우리 사이로 돌아가 이어 얘기하기
          </Link>
        </div>
      )}
```

(`reading`은 129행에서 구조분해됨. `Link` 이미 import됨.)

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add app/tarot/result/page.tsx
git commit -m "feat(tarot): 관계 스킬 타로 결과에 우리 사이 복귀 CTA"
```

---

## Task 11: 페르소나 — 소환 예고 규칙 (①)

**Files:**
- Modify: `data/persona/byeolkong_relationship.md` (`## 스킬 제안 ([SKILL:key])` 섹션, 73행 부근)

- [ ] **Step 1: 규칙 추가** — 마커 관련 불릿(73행 "마커 직전에 자연스러운 한 문장을 붙여…") 바로 아래에 새 불릿 추가:

```markdown
- **연속성 예고 (필수)**: 마커 직전 문장에 "갔다 오면 그 결과 갖고 여기서 바로 이어서 보자"는 **약속**을 반드시 담아 — 결과를 보고 스레드로 돌아오면 별콩이가 그걸 짚으며 대화를 잇는다는 걸 미리 알려. 단, 위 '탈출구·유도 문구 금지'와 충돌하지 않게: "안 봐도 돼 / 부담 없이"가 아니라 **이어짐을 확신시키는 톤**이어야 해. 예) "그 얘긴 우리 궁합으로 더 자세히 볼 수 있어. 보고 오면 그거 갖고 여기서 이어서 얘기하자."
```

- [ ] **Step 2: 커밋**

```bash
git add data/persona/byeolkong_relationship.md
git commit -m "feat(persona): 관계 스킬 소환 시 연속성 예고 규칙"
```

- [ ] **Step 3: (검증) QA 하네스 스팟** — dev 서버 재시작 후(페르소나 모듈 캐시), 우리 사이 대화에서 스킬 제안이 나오는 턴 응답에 연속성 예고 문구가 포함되는지 확인. (자동 assert 아님 — 출력 육안 확인.)

---

## 통합 검증 (전체 완료 후)

- [ ] **A. 유닛 테스트 전체**

Run: `node --import tsx --test lib/relationship/memory.test.ts lib/relationship/skills.test.ts`
Expected: 전부 PASS

- [ ] **B. 타입 체크 + 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 에러 없음

- [ ] **C. 브라우저 E2E (dev)** — 패스 활성 상태에서:
  1. 우리 사이 대화 → 별콩이가 스킬 제안 시 "보고 오면 이어서" 예고 문구 확인 (①)
  2. 궁합 칩 소환 → `/fortune/result` 에 "💬 우리 사이로 돌아가 이어 얘기하기" CTA 확인 (②)
  3. CTA로 복귀 → 별콩이 인사 버블("💑 우리 궁합 결과 봤어! …") 1회 노출 확인 (③)
  4. 새로고침 → 인사 버블 재노출 안 됨(소진 확인)
  5. 이어서 유저 메시지 전송 → 별콩이가 궁합 결과를 자연스럽게 반영해 이어가는지 (skill_log 주입)
  6. 카드뽑기 스킬(체크인) 도 `/tarot/result` CTA + 복귀 인사 동일 동작 확인

---

## 자체 점검 결과 (플랜 작성자)

- **스펙 커버리지**: ① Task 11, ② Task 8·9·10(+Task 0 판정 기존), ③ Task 1·2·4·5·6·7. 전부 매핑됨.
- **타입 일관성**: `pending_skill_recap: {skill, summary, created_at}` — types(T1)·applySkillToMemo(T1)·recap-seen(T4)·GET recap(T5) 동일. `skillRecap: {skill, summary}` prop — ThreadChat(T6)·page(T7) 동일. `relationshipId` — readings API(T8)·fortune(T9)·tarot(T10) 동일.
- **비목표 준수**: 인라인 렌더/칩 서브라벨/서버 강제문장/복귀 모델호출 없음.
