# 별자리 지도 시각 재정비 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별자리 지도의 주인 별을 원으로, 삼합 색을 청록으로 통일(범례 일치)하고, 삼합 2단계 칩·선 미감(또렷+골드 글로우)을 적용한다.

**Architecture:** 순수 모듈(`display`·`layout`·`scale`)은 TDD로 계약을 바꾸고, 컴포넌트(`ConstellationCanvas`·`ConstellationView`·`[shareId]/page`)는 그 출력을 SVG/JSX로 옮긴다. 관계선 색은 `BOND_COLOR` 단일 원천으로 모은다. DB/API/마이그레이션 변경 없음.

**Tech Stack:** Next 16(App Router)·React 19·TypeScript·SVG·node:test(`node --import tsx --test`).

**테스트 실행:** 유닛은 `node --import tsx --test lib/byeoljari/<file>.test.ts` (package.json에 test 스크립트 없음). 타입체크 `npx tsc --noEmit`. E2E는 dev 서버(`npm run dev`) + 브라우저 프리뷰(로엔 맵 `7uBv0cLKet`, 375px).

**스펙:** `docs/superpowers/specs/2026-08-23-별자리-지도-시각-재정비-design.md`

---

## 파일 구조

| 파일 | 역할 | 태스크 |
|---|---|---|
| `lib/byeoljari/display.ts` | 색·라벨 단일 원천 — `BOND_COLOR` 신설 | 1 |
| `lib/byeoljari/layout.ts` | `resolveGlyph` 반환 타입 재정의(주인=원) | 2 |
| `components/byeoljari/ConstellationCanvas.tsx` | 글리프 렌더 교체 + 선색 `BOND_COLOR` + 삼합 청록 | 3, 6, 7 |
| `app/fortune/byeoljari/[shareId]/page.tsx` | 초대 버튼 `viewerIsOwner` 반영 | 4 |
| `components/byeoljari/ConstellationView.tsx` | 범례 색 상수화 + 삼합 2단계 칩 | 5, 6 |
| `lib/byeoljari/scale.ts` | 선 두께 상향 | 7 |

**Canvas 태스크(3→6→7)는 같은 파일이라 순차.** 나머지(1·2·4·5)는 독립.

---

## Task 1: BOND_COLOR 색 단일 원천

**Files:**
- Modify: `lib/byeoljari/display.ts` (STAR_ELEMENT_COLORS 블록 아래)
- Test: `lib/byeoljari/display.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

`lib/byeoljari/display.test.ts` 상단 import에 `BOND_COLOR` 추가:

```ts
import {
  STAR_ELEMENT_COLORS,
  starColor,
  BOND_COLOR,
  elementRelationLabel,
  relationTypeLabel,
  subjectParticle,
  directionParticle,
} from "./display.ts";
```

파일 끝에 테스트 추가:

```ts
test("BOND_COLOR — 끌림 골드·결속 보라·같은 결 청록", () => {
  assert.equal(BOND_COLOR.heavenly, "#F2D78A");
  assert.equal(BOND_COLOR.six, "#A98BEE");
  assert.equal(BOND_COLOR.triad, "#5DCAA5");
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/byeoljari/display.test.ts`
Expected: FAIL — `BOND_COLOR` is not exported / undefined.

- [ ] **Step 3: 구현**

`lib/byeoljari/display.ts`의 `ELEMENT_FALLBACK` 상수와 `starColor` 함수 사이(또는 `starColor` 아래)에 추가:

```ts
// 관계선 색(단일 원천) — 범례·지도 선·칩이 전부 참조(드리프트 방지). STAR_ELEMENT_COLORS(별색)와 별개.
export const BOND_COLOR = {
  heavenly: "#F2D78A", // 끌림(천간합) — 골드
  six: "#A98BEE",      // 결속(육합) — 보라
  triad: "#5DCAA5",    // 같은 결(삼합) — 청록
} as const;
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/byeoljari/display.test.ts`
Expected: PASS (전체 통과).

- [ ] **Step 5: 커밋**

```bash
git add lib/byeoljari/display.ts lib/byeoljari/display.test.ts
git commit -m "feat(byeoljari): 관계선 색 단일 원천 BOND_COLOR"
```

---

## Task 2: resolveGlyph — 주인 별 원

**Files:**
- Modify: `lib/byeoljari/layout.ts:97-105`
- Test: `lib/byeoljari/layout.test.ts:73-79`

- [ ] **Step 1: 테스트 교체**

`lib/byeoljari/layout.test.ts`의 기존 `resolveGlyph` 테스트(73-79행)를 아래로 교체:

```ts
test("resolveGlyph — 주인=원(host-circle, meId 무관), 나(비host)=강조별, 그 외=별", () => {
  assert.equal(resolveGlyph(node("h", true), null), "host-circle"); // 호스트, meId 없음 → 원
  assert.equal(resolveGlyph(node("h", true), "h"), "host-circle"); // 호스트==나 → 원(host 우선)
  assert.equal(resolveGlyph(node("g"), "g"), "me-star"); // 게스트==나 → 강조 별(best-effort)
  assert.equal(resolveGlyph(node("g"), "other"), "star"); // 게스트, 나 아님
  assert.equal(resolveGlyph(node("g"), null), "star"); // 게스트, meId 없음
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/byeoljari/layout.test.ts`
Expected: FAIL — `resolveGlyph(host, null)` 이 `"host-star"` 반환(기대 `"host-circle"`).

- [ ] **Step 3: 구현**

`lib/byeoljari/layout.ts`의 `resolveGlyph`(97-105행)를 교체:

```ts
/** 노드 표현: 주인(host)=원(맵 주인, meId·로그인·기기 무관) / 남의 맵에 참여한 나=강조 별 / 그 외=별. */
export function resolveGlyph(
  node: GraphNode,
  meId: string | null
): "host-circle" | "me-star" | "star" {
  if (node.isHost) return "host-circle"; // 주인 = 원 (항상)
  if (meId && node.id === meId) return "me-star"; // 남의 맵에서 나 = 흰 테두리 강조 별(best-effort)
  return "star";
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/byeoljari/layout.test.ts`
Expected: PASS.

- [ ] **Step 5: 커밋** (Task 3에서 Canvas가 새 반환값을 소비하므로 tsc는 Task 3 후 통과. 여기선 유닛만 확인 후 커밋)

```bash
git add lib/byeoljari/layout.ts lib/byeoljari/layout.test.ts
git commit -m "feat(byeoljari): resolveGlyph 주인=원(host-circle)·나=강조별(me-star)"
```

---

## Task 3: Canvas 글리프 렌더 교체 + 선색 BOND_COLOR + 삼합 청록

**Files:**
- Modify: `components/byeoljari/ConstellationCanvas.tsx`

Task 2의 새 반환값을 소비. 이 태스크 완료 시 tsc가 다시 통과해야 한다.

- [ ] **Step 1: import에 BOND_COLOR 추가**

5행 교체:

```ts
import { starColor, BOND_COLOR } from "@/lib/byeoljari/display";
```

- [ ] **Step 2: triadRing 색을 청록 고정으로**

62-64행:

```ts
  graph.triads.forEach((t) =>
    t.memberIds.forEach((id) => triadRing.set(id, BOND_COLOR.triad))
  );
```

- [ ] **Step 3: 관계선 stroke를 BOND_COLOR로**

126행의 `stroke=...` 를 교체:

```tsx
                stroke={e.heavenlyCombo ? BOND_COLOR.heavenly : e.sixCombo ? BOND_COLOR.six : focusMode ? "#B8A8D8" : starColor(colorNode?.element ?? "")}
```

- [ ] **Step 4: 삼합 폴리곤 stroke를 청록으로**

153행의 `stroke={starColor(t.element)}` 를 교체:

```tsx
                stroke={BOND_COLOR.triad}
```

- [ ] **Step 5: 노드 글리프 렌더 블록 교체**

160-234행(주석 `{/* 노드 = 별 또는 원 ... */}` 부터 `})}` 까지)을 교체:

```tsx
        {/* 노드 = 주인 원(host-circle) / 나 강조 별(me-star) / 별. 삼합 멤버는 별 뒤 은은한 광(glow). */}
        {graph.nodes.map((n) => {
          const p = pos(n.id);
          if (!p) return null;
          const glyph = resolveGlyph(n, meId);
          const isHostCircle = glyph === "host-circle";
          const isMeStar = glyph === "me-star";
          const color = starColor(n.element);
          const dimByFilter =
            !focusMode && activeFilter != null && !nodeActiveForBond(n.id, activeFilter, graph.edges, triadMemberIds);
          const dimByPair = highlightPairIds != null && !highlightPairIds.includes(n.id);
          const nodeOpacity = dimByFilter || dimByPair ? DIM : 1;
          const ringColor = triadRing.get(n.id);
          const baseR = isHostCircle ? sizes.hostOuter : sizes.starOuter;
          return (
            <g
              key={n.id}
              onClick={() => onSelect(n.id)}
              style={{ cursor: "pointer", transition: "opacity 300ms" }}
              opacity={nodeOpacity}
            >
              {/* 넉넉한 투명 히트영역 */}
              <circle cx={p.x} cy={p.y} r={sizes.hitR} fill="transparent" />
              {/* 삼합(같은 국) — 별 뒤 부드러운 청록 광. 그룹 연결선은 위 폴리곤. */}
              {ringColor && (
                <circle
                  cx={p.x}
                  cy={p.y}
                  r={baseR + 1}
                  fill={ringColor}
                  opacity={0.45}
                  filter="url(#triadGlow)"
                />
              )}
              {isHostCircle ? (
                // 주인 = 원(오행색 + 골드 테두리)
                <circle
                  cx={p.x}
                  cy={p.y}
                  r={sizes.hostOuter}
                  fill={color}
                  stroke={BOND_COLOR.heavenly}
                  strokeOpacity={0.85}
                  strokeWidth={0.7}
                />
              ) : (
                // 나(me-star)=흰 테두리 강조 별 / 그 외=별
                <polygon
                  points={starPoints(p.x, p.y, sizes.starOuter, sizes.starInner)}
                  fill={color}
                  stroke={isMeStar ? "#FFFFFF" : "none"}
                  strokeOpacity={isMeStar ? 0.9 : 0}
                  strokeWidth={isMeStar ? 0.7 : 0}
                  strokeLinejoin="round"
                />
              )}
              {sizes.showLabels && n.name && (
                <text
                  x={p.x}
                  y={p.y + baseR + 4}
                  textAnchor="middle"
                  fontSize={isHostCircle ? sizes.hostLabelFont : sizes.labelFont}
                  fill="#EDE6D6"
                  fillOpacity={0.9}
                >
                  {n.name}
                </text>
              )}
            </g>
          );
        })}
```

- [ ] **Step 6: 타입체크 + 회귀 유닛**

Run: `npx tsc --noEmit`
Expected: 에러 0 (Canvas가 새 글리프 값을 다 처리 — `me-circle`/`host-star` 잔존 참조 없음).

Run: `node --import tsx --test lib/byeoljari/layout.test.ts lib/byeoljari/scale.test.ts lib/byeoljari/bond-filter.test.ts`
Expected: PASS.

- [ ] **Step 7: 커밋**

```bash
git add components/byeoljari/ConstellationCanvas.tsx
git commit -m "feat(byeoljari): 주인 별 원 렌더 + 선색 BOND_COLOR·삼합 청록 통일"
```

---

## Task 4: 초대 버튼 viewerIsOwner 반영

**Files:**
- Modify: `app/fortune/byeoljari/[shareId]/page.tsx:82-106`

- [ ] **Step 1: 버튼 조건 교체**

82행 `{!meId && !showJoin && (` 를 교체:

```tsx
          {!meId && !state.graph.viewerIsOwner && !showJoin && (
```

91행 `{meId && (` 를 교체:

```tsx
          {(meId || state.graph.viewerIsOwner) && (
```

(이 블록은 `state.status === "ready"` 안이라 `state.graph` 접근 가능. `viewerIsOwner`=서버 세션 기준 주인이면 로그인 주인은 항상 "친구 초대"가 뜨고, 비로그인·다른 기기 주인만 join 폴백 — 근본 한계, 글리프 host=원은 이와 무관하게 항상 정확.)

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 0.

- [ ] **Step 3: 커밋**

```bash
git add "app/fortune/byeoljari/[shareId]/page.tsx"
git commit -m "fix(byeoljari): 자기 맵 초대 버튼을 viewerIsOwner 기준으로(내 별 놓기 오노출 해소)"
```

---

## Task 5: 범례 색 BOND_COLOR 상수화

**Files:**
- Modify: `components/byeoljari/ConstellationView.tsx` (import + 범례 359-371행)

- [ ] **Step 1: import에 BOND_COLOR 추가**

8-12행 import 블록의 `STAR_ELEMENT_COLORS,` 다음 줄들에 `BOND_COLOR` 를 포함:

```ts
import {
  STAR_ELEMENT_COLORS,
  BOND_COLOR,
  starColor,
  relationTypeLabel,
  directionParticle,
} from "@/lib/byeoljari/display";
```

- [ ] **Step 2: 범례 3선 색을 상수 참조로**

360-371행의 범례 선 3개에서 하드코딩 색을 교체:

- 끌림 line `stroke="#F2D78A"` → `stroke={BOND_COLOR.heavenly}`
- 결속 line `stroke="#A98BEE"` → `stroke={BOND_COLOR.six}`
- 같은 결 line `stroke="#5DCAA5"` → `stroke={BOND_COLOR.triad}`

(값은 동일하지만 단일 원천으로 묶어 Task 3의 지도 삼합 청록과 계약 일치.)

- [ ] **Step 3: 타입체크**

Run: `npx tsc --noEmit`
Expected: 에러 0.

- [ ] **Step 4: 커밋**

```bash
git add components/byeoljari/ConstellationView.tsx
git commit -m "refactor(byeoljari): 범례 선색 BOND_COLOR 참조로 통일"
```

---

## Task 6: 삼합 2단계 칩 (그룹 격리)

**Files:**
- Modify: `components/byeoljari/ConstellationCanvas.tsx` (Props + triadMemberIds + 폴리곤)
- Modify: `components/byeoljari/ConstellationView.tsx` (상태 + 하위 칩 + Canvas prop)

`bond-filter.ts`는 **변경하지 않는다** — 그룹 격리는 Canvas가 `triadMemberIds`를 "전체 삼합" 대신 "선택 그룹만"으로 좁혀 기존 `nodeActiveForBond`를 그대로 재사용한다(스펙의 시그니처 확장보다 단순).

- [ ] **Step 1: Canvas Props에 activeTriadGroup 추가**

`components/byeoljari/ConstellationCanvas.tsx` `interface Props`(22-35행)에 추가:

```ts
  activeTriadGroup?: number | null; // "같은 결" 하위 칩 선택 — 그 삼합 그룹만 격리 표시
```

구조분해(37-50행)에도 추가(`activeFilter,` 다음 줄):

```ts
  activeTriadGroup = null,
```

- [ ] **Step 2: triadMemberIds를 선택 그룹으로 좁히기**

56-58행의 `triadMemberIds` 정의를 교체:

```tsx
  const nodeById = new Map(graph.nodes.map((n) => [n.id, n]));
  const pos = (id: string) => layout.get(id);
  const triadMemberIds = new Set(
    activeTriadGroup != null && graph.triads[activeTriadGroup]
      ? graph.triads[activeTriadGroup].memberIds
      : graph.triads.flatMap((t) => t.memberIds)
  );
```

- [ ] **Step 3: 삼합 폴리곤을 선택 그룹만 렌더**

139-158행 삼합 폴리곤 `.map` 콜백 안, `if (pts.length < 2) return null;` 아래에 그룹 격리 가드 추가(map 콜백 첫 줄):

```tsx
          graph.triads.map((t, ti) => {
            if (activeTriadGroup != null && ti !== activeTriadGroup) return null; // 하위 칩 선택 시 그 그룹만
            const pts = t.memberIds
```

- [ ] **Step 4: ConstellationView 상태 + Canvas 배선**

`components/byeoljari/ConstellationView.tsx`:

49행 `const [activeFilter, setActiveFilter] = useState<BondFilter | null>(null);` 아래에 추가:

```ts
  const [activeTriadGroup, setActiveTriadGroup] = useState<number | null>(null);
```

110행 `effectiveFilter` 정의 아래에 추가:

```ts
  // 그룹 격리는 "같은 결" 필터가 실제 활성일 때만 유효.
  const effectiveTriadGroup = effectiveFilter === "triad" ? activeTriadGroup : null;
```

- [ ] **Step 5: 상위 칩 클릭 시 그룹 리셋**

243-246행 상위 칩 `onClick` 을 교체(필터 바꾸면 하위 선택 초기화):

```tsx
                onClick={() => {
                  setActiveFilter(t);
                  setActiveTriadGroup(null);
                  if (focusId) resetToOverview(); // 포커스 중 필터 누르면 전체 지도로 나가며 적용
                }}
```

- [ ] **Step 6: 하위 칩(①②③) 렌더**

상위 칩 `</div>`(256행, `)}` 직전의 닫는 div) 다음에 하위 칩 블록 추가:

```tsx
      {effectiveFilter === "triad" && graph.triads.length >= 2 && (
        <div className="mb-3 flex flex-wrap justify-center gap-2">
          {graph.triads.map((_, i) => {
            const on = effectiveTriadGroup === i;
            return (
              <button
                key={i}
                type="button"
                aria-pressed={on}
                onClick={() => setActiveTriadGroup(on ? null : i)}
                className={`rounded-full px-3 py-1 text-xs ${
                  on ? "text-white" : "text-eye-purple"
                }`}
                style={on ? { backgroundColor: BOND_COLOR.triad } : { backgroundColor: "#CFEDE4" }}
              >
                {i + 1}
              </button>
            );
          })}
        </div>
      )}
```

- [ ] **Step 7: Canvas에 activeTriadGroup 전달**

267-282행 `<ConstellationCanvas ... />` 의 `activeFilter={effectiveFilter}` 다음 줄에 추가:

```tsx
          activeTriadGroup={effectiveTriadGroup}
```

- [ ] **Step 8: 타입체크 + E2E**

Run: `npx tsc --noEmit`
Expected: 에러 0.

E2E: 로엔 맵(`7uBv0cLKet`)은 삼합 1그룹이라 하위 칩 미노출 확인. 다그룹 확인은 dev에 삼합 2+ 시드가 있으면 그것으로(없으면 육안 로직 검토). "같은 결" → 하위 칩 ①② 등장 → ① 클릭 시 그 그룹 폴리곤·멤버만 강조·나머지 dim.

- [ ] **Step 9: 커밋**

```bash
git add components/byeoljari/ConstellationCanvas.tsx components/byeoljari/ConstellationView.tsx
git commit -m "feat(byeoljari): 삼합 2단계 칩 — 같은 결→①②③ 그룹 격리"
```

---

## Task 7: 선 미감 — 또렷 + 골드 글로우

**Files:**
- Modify: `lib/byeoljari/scale.ts:18-25`
- Modify: `components/byeoljari/ConstellationCanvas.tsx` (defs + 선 opacity + 끌림 글로우)

- [ ] **Step 1: scale.ts 선 두께 상향**

18-25행 `FEW`/`MANY`의 `lineWidth`·`goldLineWidth` 를 상향:

```ts
const FEW: Omit<SizeSpec, "showLabels"> = {
  hostOuter: 6.8, hostInner: 3.7, starOuter: 4.6, starInner: 2.5, meR: 4.6, hitR: 6.8,
  lineWidth: 0.5, goldLineWidth: 1.05, labelFont: 3, hostLabelFont: 3.6,
};
const MANY: Omit<SizeSpec, "showLabels"> = {
  hostOuter: 5.2, hostInner: 2.85, starOuter: 3, starInner: 1.65, meR: 3.2, hitR: 5.2,
  lineWidth: 0.36, goldLineWidth: 0.72, labelFont: 2.3, hostLabelFont: 2.9,
};
```

`scale.test.ts`는 현재 lineWidth/goldLineWidth 절대값을 단정하지 않으므로(보간·경계만 검증) 회귀 없음 — Step 3에서 확인.

- [ ] **Step 2: 특별 선 opacity 상향**

`components/byeoljari/ConstellationCanvas.tsx` 127행 `strokeOpacity=...` 를 교체(끌림/결속 0.62→0.85, 삼합 폴리곤은 Step 4에서):

```tsx
                strokeOpacity={(special ? 0.85 : focusMode ? 0.4 : 0.22) * dim}
```

- [ ] **Step 3: 골드 글로우 필터 추가**

`<defs>`(68-73행) 안 `triadGlow` 필터 다음에 추가:

```tsx
        {/* 끌림(천간합) 골드 글로우 — 반짝 강조 */}
        <filter id="goldGlow" x="-70%" y="-70%" width="240%" height="240%">
          <feGaussianBlur stdDeviation="1.1" />
        </filter>
```

- [ ] **Step 4: 끌림 선에 글로우 겹선 + 삼합 폴리곤 opacity**

관계선 `<g key=...>` 블록(112-133행)에서, `focusMode &&` 히트라인 다음, 본선 `<line>` **앞**에 끌림 글로우 겹선 추가:

```tsx
              {e.heavenlyCombo && !focusMode && (
                <line
                  x1={pa.x} y1={pa.y} x2={pb.x} y2={pb.y}
                  stroke={BOND_COLOR.heavenly}
                  strokeOpacity={0.4 * dim}
                  strokeWidth={sizes.goldLineWidth * 2.4}
                  strokeLinecap="round"
                  filter="url(#goldGlow)"
                />
              )}
```

삼합 폴리곤(154행) `strokeOpacity={0.5}` 를 교체:

```tsx
                strokeOpacity={0.82}
```

- [ ] **Step 5: 타입체크 + 회귀 유닛 + E2E**

Run: `npx tsc --noEmit`
Expected: 에러 0.

Run: `node --import tsx --test lib/byeoljari/scale.test.ts`
Expected: PASS.

E2E(로엔 맵 375px): 끌림 선이 골드 글로우로 또렷, 삼합 청록 선명, 전체 선 존재감↑.

- [ ] **Step 6: 커밋**

```bash
git add lib/byeoljari/scale.ts components/byeoljari/ConstellationCanvas.tsx
git commit -m "feat(byeoljari): 선 미감 — 두께·opacity 상향 + 끌림 골드 글로우"
```

---

## Task 8: 통합 검증 + dev push

**Files:** 없음(검증·배포만)

- [ ] **Step 1: 전체 유닛 + 타입 + 빌드**

```bash
node --import tsx --test lib/byeoljari/*.test.ts
npx tsc --noEmit
npm run build
```
Expected: 유닛 전량 PASS · tsc 0 · build exit 0.

- [ ] **Step 2: 라이브 E2E (dev + 브라우저, 375px, 로엔 `7uBv0cLKet`)**

확인 항목:
- 주인(로엔) = **원**(비로그인 공유뷰에서도).
- 삼합 = **청록** 선·glow, 범례 "같은 결"과 색 일치.
- 끌림 = 골드 글로우로 또렷 / 결속 = 보라 점선 / 전체 선 존재감↑.
- 삼합 1그룹이라 하위 칩 미노출. "같은 결" 누르면 삼합만 강조.
- 콘솔 에러 0.

- [ ] **Step 3: origin/dev push**

```bash
git push origin dev
```

---

## Self-Review 체크 (작성자 기록)

- **스펙 커버리지:** ①=Task 2·3·4 / ②=Task 3·5 / ③=Task 3 / ④=Task 6 / ⑤=Task 7 / 색 단일원천=Task 1. 전 항목 태스크 존재.
- **타입 일관성:** `resolveGlyph` 반환 `"host-circle"|"me-star"|"star"` — Task 2 정의, Task 3에서 `isHostCircle`/`isMeStar`로 소비(일치). `activeTriadGroup` — Task 6 Canvas Props·View 상태·prop 배선 이름 일치. `BOND_COLOR.{heavenly,six,triad}` — Task 1 정의, Task 3·5·6에서 참조.
- **placeholder:** 없음(모든 코드 스텝에 실코드).
- **스펙과의 차이(의도적):** 스펙 §④는 `bond-filter` 시그니처 확장을 적었으나, 플랜은 `triadMemberIds`를 좁혀 기존 함수 재사용(더 단순·`bond-filter.test.ts` 회귀 0). 스펙의 "구현 재량" 범위.
