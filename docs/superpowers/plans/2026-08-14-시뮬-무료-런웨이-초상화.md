# 시뮬 무료 런웨이 + 초상화 축적 payoff — 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시뮬 첫 3판/관계를 무료로 + 주1회 리텐션 무료 판 + 디브리핑에 "쌓이는 초상화"를 노출해, 유료벽 전에 축적을 시작하게 하고 런웨이 소진 지점에서 15별 자기 전환시킨다.

**Architecture:** funding 판별(`runway`/`hook`/`paid`)을 순수 함수 `resolveFunding`로 분리하고 세션 생성 라우트가 게이트한다(무료면 `spendStars` skip). funding 태그는 `SimMeta`(saju_data JSONB)에 실어 마이그레이션 0. 초상화는 기존 디브리핑 LLM 콜에 `[PORTRAIT:]` 마커를 얹어 관찰을 추출→dedup→`user_profiles.personality`에 append(추가 콜 없음)하고, 디브리핑 화면에 카드로 노출 + 유저 직접 성향 입력창(기존 feedback 경로 재사용) 추가.

**Tech Stack:** Next.js 16 App Router, TypeScript strict, Supabase(service role), node:test(`node --import tsx --test`), luna(`CHAT_MODEL`) 시뮬 대화.

**전제 스펙:** `docs/superpowers/specs/2026-08-14-시뮬-무료-런웨이-초상화-design.md`

**공통 명령:**
- 유닛 테스트: `node --import tsx --test <파일>`
- 타입체크: `npx tsc --noEmit`
- 시뮬 QA: `npm run qa:sim` (⚠️ 페르소나/프롬프트 수정 후 dev 서버 재시작 필요)

---

## Phase 1 — 무료 예산 게이트 (런웨이 + 훅)

### Task 1: 상수 + `resolveFunding` 순수 함수

**Files:**
- Modify: `lib/relationship/types.ts` (SIM_COST 부근, 53행 근처)
- Modify: `lib/relationship/sim.ts` (SimMeta 타입 + 신규 함수)
- Test: `lib/relationship/sim.test.ts` (없으면 생성)

- [ ] **Step 1: 상수 추가** — `lib/relationship/types.ts` 의 `SIM_SUGGEST_COST` 정의 아래에 추가:

```ts
/** 무료 런웨이 — 관계당 첫 N판은 무료(SIM_COST 면제). 축적 시작을 유료벽 앞에 둔다(스펙 §3). 서버 권위. */
export const SIM_FREE_RUNWAY = 3;
/** 리텐션 훅 — 유저당 이 일수(롤링)마다 무료 판 1회. 재방문 케이던스(스펙 §3). 서버 권위. */
export const SIM_HOOK_INTERVAL_DAYS = 7;
```

- [ ] **Step 2: 실패하는 테스트 작성** — `lib/relationship/sim.test.ts` 에 추가(없으면 파일 생성, 아래 import 헤더 포함):

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { resolveFunding } from "./sim.ts";

test("런웨이 소진 전엔 무조건 runway", () => {
  const now = new Date("2026-08-14T00:00:00Z");
  assert.equal(resolveFunding({ runwayUsed: 0, hookLastAt: null, now }), "runway");
  assert.equal(resolveFunding({ runwayUsed: 2, hookLastAt: null, now }), "runway");
});

test("런웨이 소진 후 훅 이력 없으면 hook", () => {
  const now = new Date("2026-08-14T00:00:00Z");
  assert.equal(resolveFunding({ runwayUsed: 3, hookLastAt: null, now }), "hook");
});

test("훅은 7일 롤링 — 6일 전이면 paid, 7일 전이면 hook", () => {
  const now = new Date("2026-08-14T00:00:00Z");
  const sixDays = new Date("2026-08-08T00:00:00Z").toISOString();  // 6일 전
  const sevenDays = new Date("2026-08-07T00:00:00Z").toISOString(); // 7일 전
  assert.equal(resolveFunding({ runwayUsed: 3, hookLastAt: sixDays, now }), "paid");
  assert.equal(resolveFunding({ runwayUsed: 5, hookLastAt: sevenDays, now }), "hook");
});
```

- [ ] **Step 3: 실패 확인** — Run: `node --import tsx --test lib/relationship/sim.test.ts`
  Expected: FAIL — `resolveFunding is not a function` (또는 export 없음).

- [ ] **Step 4: 구현** — `lib/relationship/sim.ts` 상단 import 를 `SIM_TURN_CAP` 에서 확장하고 함수 추가:

```ts
// 파일 상단 import 수정:
import { SIM_TURN_CAP, SIM_FREE_RUNWAY, SIM_HOOK_INTERVAL_DAYS } from "./types";
```

`SimMeta` 인터페이스에 `funding` 필드 추가:

```ts
export interface SimMeta {
  situationId: string;
  userContext: string | null;
  phase: "stage" | "debriefed";
  insight?: string;
  sendMessage?: string;
  /** 판 자금원 — 무료 런웨이/훅/유료. 게이팅·측정 소스(스펙 §3·§6). 없으면 레거시=paid 취급. */
  funding?: "runway" | "hook" | "paid";
}
```

파일 하단에 순수 함수 추가:

```ts
/** 판 자금원 결정(서버 게이트). runwayUsed=이 관계의 기존 runway 판 수, hookLastAt=이 유저의 최근 hook 판 시각.
 *  런웨이 소진 전=runway / 소진 후 7일 롤링 지났으면 hook / 아니면 paid. 순수 함수(라우트가 쿼리 주입). */
export function resolveFunding(args: {
  runwayUsed: number;
  hookLastAt: string | null;
  now: Date;
}): "runway" | "hook" | "paid" {
  if (args.runwayUsed < SIM_FREE_RUNWAY) return "runway";
  if (!args.hookLastAt) return "hook";
  const elapsedDays = (args.now.getTime() - new Date(args.hookLastAt).getTime()) / 86_400_000;
  return elapsedDays >= SIM_HOOK_INTERVAL_DAYS ? "hook" : "paid";
}
```

- [ ] **Step 5: 통과 확인** — Run: `node --import tsx --test lib/relationship/sim.test.ts`
  Expected: PASS (3 tests).

- [ ] **Step 6: 커밋**

```bash
git add lib/relationship/types.ts lib/relationship/sim.ts lib/relationship/sim.test.ts
git commit -m "feat(sim): 무료 런웨이/훅 상수 + resolveFunding 순수 함수"
```

---

### Task 2: 세션 생성 라우트 funding 게이트

**Files:**
- Modify: `app/api/relationship/sim/route.ts:61-88` (spend + reading insert)

- [ ] **Step 1: import 확장** — 파일 상단 import 수정:

```ts
import { SIM_COST, SIM_FREE_RUNWAY, RELATIONSHIP_STATUS_LABELS, type RelationshipStatus } from "@/lib/relationship/types";
import { buildSimFrame, resolveFunding, type SimMeta } from "@/lib/relationship/sim";
import { spendStars, chargeStars, getStarBalance } from "@/lib/stars";
```

- [ ] **Step 2: funding 판별 + 조건부 차감** — 현재 61~67행(무조건 `spendStars` 블록)을 아래로 교체. `rel` 로드 직후(58행 no_profile 게이트 다음)에 위치:

```ts
  // ── 자금원 판별(서버 권위): 런웨이(관계당 3) → 훅(유저당 7일 롤링 1) → 유료. ──
  const { count: runwayUsed } = await supabase
    .from("readings")
    .select("id", { count: "exact", head: true })
    .eq("user_id", userId)
    .eq("relationship_id", rel.id)
    .eq("consultation_type", "relationship_sim")
    .eq("saju_data->>funding", "runway");
  let hookLastAt: string | null = null;
  if ((runwayUsed ?? 0) >= SIM_FREE_RUNWAY) {
    const { data: lastHook } = await supabase
      .from("readings")
      .select("created_at")
      .eq("user_id", userId)
      .eq("consultation_type", "relationship_sim")
      .eq("saju_data->>funding", "hook")
      .order("created_at", { ascending: false })
      .limit(1)
      .maybeSingle();
    hookLastAt = lastHook?.created_at ?? null;
  }
  const funding = resolveFunding({ runwayUsed: runwayUsed ?? 0, hookLastAt, now: new Date() });

  // 유료만 차감(서버 최종 권위). 무료 판은 skip + 현재 잔액 조회. 실패 → 402 → 클라 /shop.
  let balance: number;
  if (funding === "paid") {
    const spend = await spendStars(userId, SIM_COST, { source: "relationship_sim" });
    if (!spend.success)
      return NextResponse.json(
        { error: "Insufficient stars", code: "INSUFFICIENT_STARS", reason: spend.reason, balance: spend.balance, required: SIM_COST },
        { status: 402 }
      );
    balance = spend.balance;
  } else {
    balance = await getStarBalance(userId);
  }
  const cost = funding === "paid" ? SIM_COST : 0;
```

- [ ] **Step 3: 환불 조건 + 메타/차감액 반영** — **69~76행(refund 헬퍼 + `const meta` 선언 통째)** 을 아래로 교체(원본 76행 meta 를 포함해야 중복 선언 안 생김). refund 는 유료만, meta 는 funding 포함:

```ts
  // ── 유료 판만 환불 대상 ── 이후 실패 시 차감했으면 되돌린다.
  const refund = async () => {
    if (funding !== "paid") return;
    const r = await chargeStars(userId, SIM_COST, `refund_${randomUUID()}`, "relationship_sim_refund").catch(() => null);
    if (!r?.success)
      await logError(new Error("sim_refund_failed"), ctxFromRequest(request, { route: "/api/relationship/sim", userId, extra: { relationshipId: rel.id } }));
  };

  const meta: SimMeta = { situationId: situation.id, userContext, phase: "stage", funding };
```

그리고 reading insert 의 `stars_spent: SIM_COST` 를 `stars_spent: cost` 로, 응답의 `cost: SIM_COST` 를 `cost,` 로, `balance: spend.balance` 를 `balance,` 로 교체(위에서 `balance`·`cost` 를 이미 계산).

- [ ] **Step 4: 타입체크** — Run: `npx tsc --noEmit`
  Expected: exit 0.

- [ ] **Step 5: 커밋**

```bash
git add app/api/relationship/sim/route.ts
git commit -m "feat(sim): 세션 생성 funding 게이트 — 무료 런웨이/훅 판 차감 skip"
```

---

### Task 3: 쿼트 엔드포인트 (정직한 가격 표기용)

**Files:**
- Create: `app/api/relationship/sim/quote/route.ts`

- [ ] **Step 1: 쿼트 라우트 작성** — 다음 판의 funding·비용·런웨이 잔여를 반환(읽기 전용, 차감 없음). Task 2 의 판별 로직과 동일하되 side-effect 0:

```ts
// app/api/relationship/sim/quote/route.ts — 다음 시뮬 판의 자금원·비용 미리보기(진입 UI 정직한 가격). 차감 없음.
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { SIM_COST, SIM_FREE_RUNWAY } from "@/lib/relationship/types";
import { resolveFunding } from "@/lib/relationship/sim";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export async function GET(request: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required", code: "LOGIN_REQUIRED" }, { status: 401 });

  const relationshipId = new URL(request.url).searchParams.get("relationshipId");
  if (!relationshipId) return NextResponse.json({ error: "invalid_input" }, { status: 400 });

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase
    .from("relationships").select("id, user_id").eq("id", relationshipId).maybeSingle();
  if (!rel || rel.user_id !== userId) return NextResponse.json({ error: "not_found" }, { status: 404 });

  const { count: runwayUsed } = await supabase
    .from("readings").select("id", { count: "exact", head: true })
    .eq("user_id", userId).eq("relationship_id", rel.id)
    .eq("consultation_type", "relationship_sim").eq("saju_data->>funding", "runway");
  let hookLastAt: string | null = null;
  if ((runwayUsed ?? 0) >= SIM_FREE_RUNWAY) {
    const { data: lastHook } = await supabase
      .from("readings").select("created_at")
      .eq("user_id", userId).eq("consultation_type", "relationship_sim").eq("saju_data->>funding", "hook")
      .order("created_at", { ascending: false }).limit(1).maybeSingle();
    hookLastAt = lastHook?.created_at ?? null;
  }
  const funding = resolveFunding({ runwayUsed: runwayUsed ?? 0, hookLastAt, now: new Date() });
  return NextResponse.json({
    funding,
    cost: funding === "paid" ? SIM_COST : 0,
    runwayRemaining: Math.max(0, SIM_FREE_RUNWAY - (runwayUsed ?? 0)),
  });
}
```

- [ ] **Step 2: 타입체크** — Run: `npx tsc --noEmit`
  Expected: exit 0.

- [ ] **Step 3: 커밋**

```bash
git add app/api/relationship/sim/quote/route.ts
git commit -m "feat(sim): 다음 판 자금원 쿼트 엔드포인트"
```

---

## Phase 2 — 초상화 축적 (패시브 관찰)

### Task 4: `[PORTRAIT:]` 마커 추출 + dedup 순수 함수

**Files:**
- Modify: `lib/relationship/sim.ts`
- Test: `lib/relationship/sim.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성** — `lib/relationship/sim.test.ts` 에 추가:

```ts
import { extractPortraitObservations, dedupPortraitNotes, stripSimMarkers } from "./sim.ts";

test("초상화 마커 최대 2개 추출 + 공백 정리", () => {
  const raw = "통찰...\n[PORTRAIT:표현이 서툴지만 관심 있으면 질문이 많아진다]\n[PORTRAIT:갈등 상황에선 먼저 거리를 둔다]\n[SEND:요즘 어때?]";
  assert.deepEqual(extractPortraitObservations(raw), [
    "표현이 서툴지만 관심 있으면 질문이 많아진다",
    "갈등 상황에선 먼저 거리를 둔다",
  ]);
});

test("초상화 마커 3개 이상이면 앞 2개만", () => {
  const raw = "[PORTRAIT:a][PORTRAIT:b][PORTRAIT:c]";
  assert.deepEqual(extractPortraitObservations(raw), ["a", "b"]);
});

test("stripSimMarkers 는 SEND·PORTRAIT 둘 다 제거", () => {
  const raw = "본문\n[PORTRAIT:x]\n[SEND:y]";
  assert.equal(stripSimMarkers(raw), "본문");
});

test("dedup — 기존과 근접 중복인 후보는 버린다", () => {
  const existing = "· 표현이 서툴지만 관심 있으면 질문이 많아진다";
  assert.deepEqual(
    dedupPortraitNotes(existing, ["표현이 서툴지만 관심 있으면 질문이 많아진다", "새 관찰"]),
    ["새 관찰"]
  );
});
```

- [ ] **Step 2: 실패 확인** — Run: `node --import tsx --test lib/relationship/sim.test.ts`
  Expected: FAIL — 함수 미정의.

- [ ] **Step 3: 구현** — `lib/relationship/sim.ts` 에 추가하고 기존 `stripSimMarkers` 를 PORTRAIT 도 제거하도록 수정:

```ts
const SIM_PORTRAIT_RE = /\[PORTRAIT:([^\]]+)\]/g;

/** 디브리핑 응답에서 [PORTRAIT:관찰] 마커를 최대 2개 추출(초상화 누적용). 없으면 빈 배열. */
export function extractPortraitObservations(raw: string): string[] {
  return [...raw.matchAll(SIM_PORTRAIT_RE)].map((m) => m[1].trim()).filter(Boolean).slice(0, 2);
}

/** 후보 관찰 중 기존 초상화(personality)와 근접 중복인 것을 버린다. 정규화(공백·불릿·문장부호 제거) 후 포함관계로 판정. */
export function dedupPortraitNotes(existing: string | null, candidates: string[]): string[] {
  const norm = (s: string) => s.replace(/[·\s.,!?~]/g, "").toLowerCase();
  const base = norm(existing ?? "");
  const out: string[] = [];
  for (const c of candidates) {
    const nc = norm(c);
    if (!nc) continue;
    if (base.includes(nc) || out.some((o) => norm(o).includes(nc) || nc.includes(norm(o)))) continue;
    out.push(c);
  }
  return out;
}
```

기존 `stripSimMarkers` 를 교체:

```ts
/** 화면 표시용 — [SEND:...]·[PORTRAIT:...] 마커 제거 + 여백 정리. */
export function stripSimMarkers(text: string): string {
  return text
    .replace(/\[SEND:[^\]]*\]/g, "")
    .replace(/\[PORTRAIT:[^\]]*\]/g, "")
    .replace(/\n{3,}/g, "\n\n")
    .trim();
}
```

- [ ] **Step 4: 통과 확인** — Run: `node --import tsx --test lib/relationship/sim.test.ts`
  Expected: PASS (기존 + 신규 전부).

- [ ] **Step 5: 커밋**

```bash
git add lib/relationship/sim.ts lib/relationship/sim.test.ts
git commit -m "feat(sim): [PORTRAIT:] 관찰 추출 + dedup + 마커 스트립 확장"
```

---

### Task 5: 디브리핑 프롬프트에 `[PORTRAIT:]` 지시 추가

**Files:**
- Modify: `lib/claude.ts:833` (SIM_DEBRIEF_GUIDE)
- Modify: `data/persona/byeolkong_sim.md` (§디브리핑)

- [ ] **Step 1: SIM_DEBRIEF_GUIDE 확장** — `lib/claude.ts:833` 의 `SIM_DEBRIEF_GUIDE` 상수 끝(`[END] 쓰지 마.` 앞)에 문장 추가. 전체를 아래로 교체:

```ts
const SIM_DEBRIEF_GUIDE = `\n\n## 이번 호출 — 디브리핑 (정리하기)\n인형을 내려놓고 유저 곁으로 돌아왔어. byeolkong_sim.md §디브리핑대로 — 본문은 (1) 통찰 2~3개(무대에서 오간 말 근거, 예측 금지) → (2) 따뜻한 마무리, 두 부분만. 그리고 실제 상대에게 보낼 한 문장은 응답 맨 끝에 [SEND:...] 마커로 한 줄 단독으로만 붙여(화면엔 별도 카드로 표시). ⚠️본문(통찰·마무리)에는 보낼 말을 쓰지 마 — 카드와 중복된다.\n또한 이번 판에서 새로 알게 된 그 사람/유저에 대한 **지속되는 성향 관찰**을 1~2개, [SEND:] 앞에 [PORTRAIT:관찰 한 줄] 마커로 각각 한 줄씩 붙여(화면엔 '초상화'로 누적). ⚠️일회성 사건이 아니라 다음에도 유효한 성향으로, 인형 대사를 되읊지 말고 **유저가 어떻게 반응했나·어떤 상황을 힘들어하나** 같은 실제 드러난 결을 우선해. 새로 알게 된 게 없으면 생략해도 돼. [END] 쓰지 마.`;
```

- [ ] **Step 2: byeolkong_sim.md §디브리핑 보강** — `data/persona/byeolkong_sim.md` 의 3번 항목(`💌 보낼 말 (마커 전용)`) 다음에 4번 항목 추가:

```markdown
4. **🖼️ 초상화 관찰 (마커 전용)** — 이번 판에서 새로 알게 된 **지속되는 성향**을 1~2개, 응답 맨 끝에 `[PORTRAIT:관찰 한 줄]` 마커로 각각 한 줄 단독으로 붙여. 화면엔 판마다 쌓이는 '초상화'로 누적돼(유저가 걔를/자기를 알아가는 표면). ⚠️ 일회성 사건("오늘 화냈다") 말고 **다음에도 유효한 결**("갈등이 생기면 먼저 거리를 두는 편")로. 인형 대사를 되읊지 말고, **유저 쪽 반응·반복되는 상황**에서 드러난 걸 우선해. 순환(유저가 준 설명 되풀이) 금지. 새로 알게 된 게 없으면 생략.
```

- [ ] **Step 3: 확인** — Run: `npx tsc --noEmit` (Expected: exit 0). 프롬프트 텍스트라 유닛 무관 — QA 는 Task 9.

- [ ] **Step 4: 커밋**

```bash
git add lib/claude.ts data/persona/byeolkong_sim.md
git commit -m "feat(sim): 디브리핑에 [PORTRAIT:] 성향 관찰 마커 지시(코어 무편집)"
```

---

### Task 6: 디브리핑 라우트 — 초상화 추출·append·반환

**Files:**
- Modify: `app/api/relationship/sim/chat/route.ts:128-163` (debrief 블록)

- [ ] **Step 1: import 확장** — `lib/relationship/sim` import 에 추가:

```ts
import {
  simForceDebrief, extractSendLine, stripSimMarkers,
  buildSimContextBlock, formatPartnerForDoll, extractSuggestions,
  extractPortraitObservations, dedupPortraitNotes, appendPersonalityNote, type SimMeta,
} from "@/lib/relationship/sim";
```

- [ ] **Step 2: debrief 블록에 초상화 처리 삽입** — 144~147행(`sendMessage`·`display`·`nextMeta` 계산부)와 저장부 사이에 초상화 추출·append 를 넣고, 응답에 `portrait` 를 싣는다. debrief 블록의 `const sendMessage = extractSendLine(raw);` 부터 `return NextResponse.json(...)` 까지를 아래로 교체:

```ts
    const sendMessage = extractSendLine(raw);
    const display = stripSimMarkers(raw);
    const nowIso = new Date().toISOString();
    const nextMeta: SimMeta = { ...meta, phase: "debriefed", sendMessage: sendMessage ?? undefined };

    // 초상화 누적 — 이번 판 관찰을 상대 personality 에 append(패시브, 유저 노력 무의존). 기존값 대비 dedup.
    let portrait = personality ?? "";
    const observations = dedupPortraitNotes(personality, extractPortraitObservations(raw));
    if (observations.length && rel.partner_profile_id) {
      let next = personality ?? "";
      for (const obs of observations) next = appendPersonalityNote(next, obs);
      const { error: pErr } = await supabase
        .from("user_profiles").update({ personality: next })
        .eq("id", rel.partner_profile_id).eq("user_id", userId);
      if (pErr)
        await logError(pErr, ctxFromRequest(request, { ...logCtx, extra: { ...logCtx.extra, stage: "portrait_append" } }));
      else portrait = next;
    }

    // 디브리핑 메시지 저장(skill_key='sim_debrief') + 판 메타 갱신 — best-effort(유실 > 저장 실패, 스펙 §6).
    try {
      const { error: mErr } = await supabase.from("messages").insert([
        { reading_id: reading.id, role: "assistant", content: display, skill_key: "sim_debrief", created_at: nowIso },
      ]);
      const { error: uErr } = await supabase.from("readings").update({ saju_data: nextMeta }).eq("id", reading.id);
      if (mErr || uErr)
        await logError(mErr ?? uErr ?? new Error("debrief_save_failed"),
          ctxFromRequest(request, { ...logCtx, extra: { ...logCtx.extra, stage: "debrief_save" } }));
    } catch (err) {
      await logError(err, ctxFromRequest(request, { ...logCtx, extra: { ...logCtx.extra, stage: "debrief_save" } }));
    }

    return NextResponse.json({ debrief: display, sendMessage, portrait, situationId: situation.id, success: true });
```

(참고: `loadSim` 이 이미 `personality` 와 `rel.partner_profile_id` 를 반환하므로 추가 조회 불필요. debrief 는 `phase==="debriefed"` 시 88행에서 409 → **판당 정확히 1회만** append.)

- [ ] **Step 3: 타입체크** — Run: `npx tsc --noEmit`
  Expected: exit 0.

- [ ] **Step 4: 커밋**

```bash
git add app/api/relationship/sim/chat/route.ts
git commit -m "feat(sim): 디브리핑서 초상화 관찰 추출→dedup→personality append + portrait 반환"
```

---

## Phase 3 — 성향 입력 + 초상화 카드 UI

### Task 7: 성향 입력 — feedback 라우트 `trait` 종류 추가

**Files:**
- Modify: `app/api/relationship/sim/feedback/route.ts`

- [ ] **Step 1: kind 확장** — 15행 `Body` 와 32~35행 검증을 `trait` 허용하도록 수정:

```ts
interface Body { simReadingId: string; kind: "up" | "down" | "trait"; note?: string }
```

```ts
  const kind = body.kind;
  const note = typeof body.note === "string" ? body.note.trim().slice(0, MAX_NOTE_LEN) : "";
  if (!body.simReadingId || (kind !== "up" && kind !== "down" && kind !== "trait") || !note)
    return NextResponse.json({ error: "invalid_input" }, { status: 400 });
```

(나머지 로직 동일 — `appendPersonalityNote` → `user_profiles.personality` update → `{ success, personality }` 반환. trait 는 유저가 디브리핑에서 직접 적은 성향 한 줄.)

- [ ] **Step 2: 타입체크** — Run: `npx tsc --noEmit`
  Expected: exit 0.

- [ ] **Step 3: 커밋**

```bash
git add app/api/relationship/sim/feedback/route.ts
git commit -m "feat(sim): feedback 라우트 trait 종류 — 디브리핑 성향 직접 입력"
```

---

### Task 8: 재열람용 초상화 반환 (GET /sim)

**Files:**
- Modify: `app/api/relationship/sim/route.ts` GET (142~186행)

- [ ] **Step 1: 상대 personality 조회 + 반환** — GET 의 `rel` 조회(142~147행)에서 `partner_profile_id` 도 select 하고, 이후 personality 를 조회해 응답에 `portrait` 추가. `rel` select 를 `.select("id, label, status, partner_profile_id")` 로 바꾸고, 최종 `return NextResponse.json({...})` 직전에 삽입:

```ts
  let portrait = "";
  if (rel.partner_profile_id) {
    const { data: p } = await supabase
      .from("user_profiles").select("personality")
      .eq("id", rel.partner_profile_id).eq("user_id", userId).maybeSingle();
    portrait = p?.personality ?? "";
  }
```

그리고 응답 객체에 `portrait,` 추가.

- [ ] **Step 2: 타입체크** — Run: `npx tsc --noEmit`
  Expected: exit 0.

- [ ] **Step 3: 커밋**

```bash
git add app/api/relationship/sim/route.ts
git commit -m "feat(sim): GET /sim 재열람에 초상화(portrait) 반환"
```

---

### Task 9: SimDebrief — 초상화 카드 + 성향 입력창

**Files:**
- Modify: `components/relationship/sim/SimDebrief.tsx`
- Modify: `app/relationship/sim/page.tsx` (initialPortrait 전달)

- [ ] **Step 1: page 에서 portrait 전달** — `app/relationship/sim/page.tsx` 의 재진입 로드(85~88행)에서 `loadedDebrief` 에 portrait 를 함께 싣고, SimDebrief 렌더(203~208행)에 prop 추가.

`loadedDebrief` state 타입·세팅 수정:

```ts
  const [loadedDebrief, setLoadedDebrief] = useState<{ debrief: string; sendMessage: string | null; portrait: string } | null>(null);
```

```ts
        if (data.phase === "debriefed") {
          setReadOnly(true);
          setLoadedDebrief({ debrief: data.debrief ?? "", sendMessage: data.sendMessage ?? null, portrait: data.portrait ?? "" });
        }
```

SimDebrief 렌더에 prop 추가:

```tsx
        {phase === "debrief" && session && (
          <SimDebrief
            simReadingId={session.simReadingId}
            initialDebrief={loadedDebrief?.debrief}
            initialSendMessage={loadedDebrief?.sendMessage}
            initialPortrait={loadedDebrief?.portrait}
          />
        )}
```

- [ ] **Step 2: SimDebrief — props·state 추가** — `SimDebrief` 컴포넌트 props 와 state 확장:

```tsx
export default function SimDebrief({
  simReadingId,
  initialDebrief,
  initialSendMessage,
  initialPortrait,
}: {
  simReadingId: string;
  initialDebrief?: string;
  initialSendMessage?: string | null;
  initialPortrait?: string;
}) {
```

state 추가(기존 state 선언부 근처):

```tsx
  const [portrait, setPortrait] = useState(initialPortrait ?? "");
  const [traitInput, setTraitInput] = useState("");
  const [traitSaving, setTraitSaving] = useState(false);
  const [traitDone, setTraitDone] = useState(false);
  const readOnly = initialDebrief != null; // 재열람 판은 입력 숨김(완료 판 personality 변경 방지, p3a 규칙)
```

- [ ] **Step 3: fresh 디브리핑 응답에서 portrait 수신** — `useEffect` 의 fresh 생성 분기(63~66행)에서 portrait 도 세팅:

```tsx
        const d = await res.json();
        setDebrief(d.debrief ?? "");
        setSendMessage(d.sendMessage ?? null);
        setPortrait(d.portrait ?? "");
        setState("done");
```

- [ ] **Step 4: 초상화 카드 + 입력창 렌더** — `sendMessage` 카드 블록(102~108행) 다음, "별콩이랑 더 얘기하기" 버튼 앞에 삽입:

```tsx
        {portrait.trim() && (
          <div className="mt-4 rounded-2xl border border-lilac-mid/30 bg-night/40 p-4">
            <div className="text-lilac text-[12px] font-bold mb-2">🖼️ 별콩이가 알아가는 초상화</div>
            <ul className="space-y-1.5">
              {portrait.split("\n").map((line, i) => {
                const t = line.replace(/^·\s*/, "").trim();
                return t ? <li key={i} className="text-cream-warm/90 text-[14px] leading-relaxed">· {t}</li> : null;
              })}
            </ul>
          </div>
        )}
        {!readOnly && (
          <div className="mt-3 rounded-2xl border border-lilac-mid/20 bg-night/30 p-4">
            <div className="text-lilac text-[12px] mb-2">걔는 사실 이런 사람이야 — 적을수록 인형이 더 걔처럼 돼</div>
            {traitDone ? (
              <p className="text-gold-soft text-[13px]">초상화에 더했어, 고마워 ✓</p>
            ) : (
              <>
                <textarea
                  value={traitInput}
                  onChange={(e) => setTraitInput(e.target.value)}
                  maxLength={300}
                  rows={2}
                  placeholder="예: 표현은 서툴러도 좋으면 바로 티가 나"
                  className="w-full rounded-xl bg-night/60 border border-lilac-mid/25 p-2.5 text-[14px] text-cream-warm placeholder:text-lilac/50 resize-none"
                />
                <button
                  disabled={traitSaving || !traitInput.trim()}
                  onClick={saveTrait}
                  className="mt-2 w-full rounded-xl py-2.5 bg-lilac-deep text-white font-bold text-sm disabled:opacity-40"
                >
                  {traitSaving ? "저장 중…" : "초상화에 더하기"}
                </button>
              </>
            )}
          </div>
        )}
```

- [ ] **Step 5: saveTrait 핸들러** — `copySend` 함수 근처에 추가:

```tsx
  async function saveTrait() {
    const note = traitInput.trim();
    if (!note) return;
    setTraitSaving(true);
    try {
      const res = await fetch("/api/relationship/sim/feedback", {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ simReadingId, kind: "trait", note }),
      });
      if (res.ok) {
        const d = await res.json();
        setPortrait(d.personality ?? portrait);
        setTraitInput("");
        setTraitDone(true);
      }
    } catch { /* 조용한 실패 방지: 실패 시 버튼 상태만 원복 */ }
    setTraitSaving(false);
  }
```

- [ ] **Step 6: 타입체크 + 커밋**

Run: `npx tsc --noEmit` (Expected: exit 0)

```bash
git add components/relationship/sim/SimDebrief.tsx app/relationship/sim/page.tsx
git commit -m "feat(sim): 디브리핑 초상화 카드 + 성향 직접 입력창(재열람은 입력 숨김)"
```

---

## Phase 4 — 정직한 가격 표기

### Task 10: 진입 시 쿼트 반영 (무료/유료 정직 표기)

**Files:**
- Modify: `app/relationship/sim/page.tsx`

- [ ] **Step 1: 쿼트 state + 조회** — 초기 로드(97~120행)에서 balance 조회와 함께 쿼트도 병렬 조회하고 state 에 저장. state 선언 추가:

```tsx
  const [quote, setQuote] = useState<{ funding: "runway" | "hook" | "paid"; cost: number; runwayRemaining: number } | null>(null);
```

`Promise.all` 을 3개로 확장(rel 확인 전이라 relationshipId 사용):

```tsx
      const [list, bal, q] = await Promise.all([
        fetch("/api/relationship", { cache: "no-store" }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
        fetch("/api/stars/balance", { cache: "no-store" }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
        fetch(`/api/relationship/sim/quote?relationshipId=${relationshipId}`, { cache: "no-store" }).then((r) => (r.ok ? r.json() : null)).catch(() => null),
      ]);
```

`found` 확인·noProfile 게이트 통과 후:

```tsx
      setQuote(q && typeof q.cost === "number" ? q : { funding: "paid", cost: SIM_COST, runwayRemaining: 0 });
```

- [ ] **Step 2: 결제 확인 모달 분기** — 212~225행의 `StarConfirmModal` 을 무료/유료 분기로 교체. 무료면 별 표기 없는 간단 확인, 유료면 기존 모달(쿼트 cost):

```tsx
      {pending && quote && (
        quote.funding === "paid" ? (
          <StarConfirmModal
            cost={quote.cost}
            balance={balance}
            loading={creating}
            accent="#9F8AD0"
            title="연애 시뮬레이션 한 판"
            subtitle="연습 + 별콩이 코칭 + 디브리핑까지 포함"
            confirmLabel="시작하기"
            onConfirm={createSession}
            onCharge={() => router.push("/shop?reason=sim")}
            onClose={() => setPending(null)}
          />
        ) : (
          <div className="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-black/50 p-4" onClick={() => setPending(null)}>
            <div className="w-full max-w-sm rounded-2xl bg-cream-warm p-5 text-eye-purple" onClick={(e) => e.stopPropagation()}>
              <div className="text-center">
                <div className="text-lg font-display mb-1">🎁 이번 판은 무료야</div>
                <p className="text-[13px] text-text-light mb-4">
                  {quote.funding === "runway"
                    ? `무료로 시작하는 첫 판이야 (남은 무료 ${quote.runwayRemaining}판). 편하게 걔 인형을 만나봐.`
                    : "이번 주 돌아온 김에 주는 무료 판이야. 초상화를 더 채워보자."}
                </p>
                <button disabled={creating} onClick={createSession} className="w-full rounded-xl py-3 bg-lilac-deep text-white font-bold disabled:opacity-40">
                  {creating ? "여는 중…" : "무료로 시작하기"}
                </button>
              </div>
            </div>
          </div>
        )
      )}
```

- [ ] **Step 3: createSession 후 쿼트 갱신** — 한 판 소비 후 다음 판 자금원이 바뀌므로, `createSession` 성공부(176행 `setPhase("stage")` 앞)에 쿼트 무효화(재진입 시 새로 조회되나, 같은 세션서 연속 판 방지용으로 null 처리):

```tsx
    setQuote(null);
    setPhase("stage");
```

- [ ] **Step 4: 타입체크 + 커밋**

Run: `npx tsc --noEmit` (Expected: exit 0)

```bash
git add app/relationship/sim/page.tsx
git commit -m "feat(sim): 진입 시 쿼트로 무료/유료 정직 표기(런웨이·훅 무료 확인)"
```

---

## Phase 5 — 코호트 + 검증

### Task 11: PROMPT_VERSION 범프

**Files:**
- Modify: `lib/prompt-version.ts`

- [ ] **Step 1: 버전 슬러그 갱신** — `PROMPT_VERSION` 상수를 기존 형식(`YYYY-MM-DD-slug`)에 맞춰 `2026-08-14-sim-portrait` 로 변경(디브리핑 프롬프트 변경 코호트 분리).

- [ ] **Step 2: 타입체크 + 유닛 전체** — Run:
```bash
npx tsc --noEmit
node --import tsx --test $(find lib -name '*.test.ts')
```
Expected: tsc exit 0 · 유닛 전부 PASS(sim.test.ts 신규 포함).

- [ ] **Step 3: 커밋**

```bash
git add lib/prompt-version.ts
git commit -m "chore(sim): PROMPT_VERSION 범프 (시뮬 초상화 코호트)"
```

---

### Task 12: QA + 수기 검증

**Files:** 없음(검증만)

- [ ] **Step 1: dev 서버 재시작** — 페르소나/프롬프트 수정 반영(모듈 캐시). QA 하네스는 `.env.local` 로 충분.

- [ ] **Step 2: 시뮬 QA** — Run: `npm run qa:sim`
  확인: ①디브리핑이 `[PORTRAIT:]` 1~2개 방출 ②화면 표시에 마커 누출 0(stripSimMarkers) ③관찰이 순환(유저 설명 되풀이) 아닌 유저-신호/드러난 결 기반 ④3블록·[SEND] 기존 동작 불변.

- [ ] **Step 3: 수기 E2E(로컬 dev)** — 시뮬 진입 → 쿼트가 "무료(런웨이 3판)" 표기 → 무료 판 생성 시 별 미차감(잔액 불변) → 대화 → 디브리핑에 초상화 카드 + 성향 입력창 → 입력 시 카드 갱신 → 재열람 시 카드 표시·입력창 숨김. 그리고 런웨이 소진 후(4번째 판) 쿼트가 "15별"로 바뀌고 차감되는지.
  (⚠️ 무료 판/유료 판을 만들면 별·readings 가 실제로 쌓이니, 검증용 관계는 QA/테스트 계정에서. prod 아님.)

- [ ] **Step 4: 배포 노트** — prod(main)은 **roadmap ④ dev 누적분 한 판**에 포함. 마이그레이션 없음(funding=JSONB). 배포 후 관찰: 무료→유료 전환율 · 초상화가 연애상담 품질 오염 안 하는지(스펙 §2 리뷰 포인트).

---

## Self-Review (플랜 작성자 체크 — 완료)

- **스펙 커버리지:** §2 초상화(3통로 Task 4·5·6·7·9)·§3 무료 예산(Task 1·2·3)·§4 진열/전환(Task 10, 답변추천 불변=무변경)·§5 통합 지점 전부 태스크 매핑·§6 측정(funding 태그=Task 2)·§7 QA/PROMPT_VERSION(Task 11·12). 풀 P3b·프로액티브 넛지는 비스코프(미포함, 정상).
- **타입 일관성:** `resolveFunding`·`extractPortraitObservations`·`dedupPortraitNotes`·`SimMeta.funding`·`portrait` 필드명 태스크 간 일치. `getStarBalance`(lib/stars 실재)·`appendPersonalityNote`(재사용) 확인.
- **placeholder:** 없음. TODO 주석은 기존 코드의 것(에러 토스트)이라 본 변경과 무관.
- **가정:** 무료 판도 rate-limit·환불 로직 불변(무료는 refund no-op). debrief 멱등(409)으로 초상화 append 판당 1회.

## Execution Handoff

플랜은 `docs/superpowers/plans/2026-08-14-시뮬-무료-런웨이-초상화.md` 에 저장됨.
