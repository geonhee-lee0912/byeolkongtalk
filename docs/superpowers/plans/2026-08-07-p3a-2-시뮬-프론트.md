# P3a 시뮬레이션 프론트 (화면) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** P3a-1 에서 완성한 시뮬 엔진(API)을 유저가 실제로 쓰는 **화면 3개**(상황 선택 → 밤 무대 3화자 대화 → 디브리핑)로 잇는다 — 파일 허브의 "연애 시뮬레이션" 카드에서 진입해 한 판을 돌리고 스레드로 연결.

**Architecture:** 세션형 화면 관례(tarot/reading = 플랫 라우트 + phase state)를 따르되, 밤 무대는 스펙 §2 "스레드와 확 다른 밤 톤 = 화자 혼동 물리 차단"이라 **몰입 라우트**로 분리한다: `app/relationship/sim/page.tsx` 단일 페이지 + 내부 `phase: "select"|"stage"|"debrief"` state + `?rel=<relationshipId>` 쿼리, `AppShell` 의 `HIDE_SHELL_PREFIXES` 에 `/relationship/sim` 추가(Header/BottomTab 숨김 — 코드베이스 최초 사례, 근거는 몰입). SSE 소비는 이 레포 관례대로 컴포넌트 내 `fetch → res.body.getReader()` 루프(공용 훅 없음). 백엔드 계약은 P3a-1 그대로 소비 — 신규 API 0.

**Tech Stack:** Next.js 16 App Router(client component), React 19, TypeScript strict, Tailwind v4(`@theme` 토큰), 기존 SSE(plain text chunk) 소비, `lib/relationship/{situations,sim,types}.ts`(순수 상수·헬퍼 클라 직접 import), 재사용 컴포넌트(`StarConfirmModal`·`SafetyBanner`·별 파티클 패턴).

> **검증 방식:** 이 레포는 라우트/UI 유닛 테스트가 없다(기존 관례). 프론트 검증은 **`npx tsc --noEmit` + `npx next build`(라우트 표) + 브라우저 E2E**(dev 서버 preview: 진입→상황→무대→디브리핑 클릭 흐름 + 콘솔/네트워크 확인). 순수 로직(스트리밍 마커 스트립 등)만 `node:test` 유닛. 페르소나/톤은 P3a-1 QA(`qa:sim`)가 이미 검증했으므로 프론트는 "계약을 올바르게 소비/표시하는가"만 본다.

---

## File Structure

**신규(Create)**

| 경로 | 책임 (한 줄) |
|---|---|
| `app/relationship/sim/page.tsx` | 시뮬 phase 오케스트레이터 — `?rel` 로드 + 소유권/로그인 가드 + `select→stage→debrief` 전환 + 각 단계 컴포넌트 마운트 |
| `components/relationship/sim/SituationSelect.tsx` | ① 상황 선택(관계별 카드 그리드, `getSituations`) + ①-b 라이트 컨텍스트 입력(선택·건너뛰기) → 세션 생성 트리거 |
| `components/relationship/sim/NightStage.tsx` | ② 밤 무대 셸 — 밤 톤 배경·별 파티클·접히는 인형·대화 리스트·입력창(💭 도움/정리하기) + say/note SSE 소비 + `X-Sim-*` 헤더/409 처리 |
| `components/relationship/sim/DollPortrait.tsx` | 접히는 인형 초상화(진입 ~80px 글로우+태그 → 대화 후 sticky ~30px), `DollAvatar` 색 프리셋 재사용 |
| `components/relationship/sim/ByeolkongNote.tsx` | 별콩이 노트 카드(금색, 버블 아님) — 자동/온디맨드 노트 렌더 |
| `components/relationship/sim/SimBubble.tsx` | 인형 대사 버블(별콩이 각인 없는 버전) + 스트리밍 커서 |
| `components/relationship/sim/SimDebrief.tsx` | ③ 디브리핑 — `debrief` JSON 로드(스트림 아님) + 통찰/💌보낼말/마무리 렌더 + [보낼말 저장·복사]/[별콩이랑 더 얘기하기] CTA |
| `lib/relationship/sim-stream.ts` | 클라 SSE 소비 헬퍼(순수·테스트 가능): reader 루프 러너 + 스트리밍 중 `[SEND:` 꼬리 마커 숨김(`hideTrailingSendMarker`) |
| `lib/relationship/sim-stream.test.ts` | `hideTrailingSendMarker` 순수 유닛 테스트 |

**수정(Modify)**

| 경로 | 변경 |
|---|---|
| `components/relationship/ProductList.tsx` | 🎭 시뮬 placeholder 카드(44~69행)를 활성 `<button onClick={onOpenSim}>` 로 + `onOpenSim` prop 추가 |
| `app/relationship/page.tsx` | `ProductList` 에 `onOpenSim={() => router.push('/relationship/sim?rel=' + selected)}` 연결(허브에서 선택된 상대) |
| `components/layout/AppShell.tsx` | `HIDE_SHELL_PREFIXES` 에 `"/relationship/sim"` 추가(밤 무대 몰입 = 탭/헤더 숨김) |

---

### Task 1: 클라 스트리밍 헬퍼 `sim-stream.ts` (순수 · TDD)

밤 무대가 `[SEND:...]` 마커를 스트리밍 중 화면에 흘리지 않도록, 완성 텍스트용 `stripSimMarkers`(P3a-1, 이미 존재) 옆에 **스트리밍 중 꼬리 마커 숨김** 순수 함수를 둔다. 이건 유닛으로 검증 가능한 유일한 프론트 로직이라 TDD.

**Files:**
- Create: `lib/relationship/sim-stream.ts`
- Test: `lib/relationship/sim-stream.test.ts`

- [ ] **Step 1: 실패 테스트 작성** — `lib/relationship/sim-stream.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { hideTrailingSendMarker } from "./sim-stream.ts";

test("완성된 [SEND:...] 마커는 화면에서 제거(디브리핑은 JSON 이지만 방어적)", () => {
  assert.equal(hideTrailingSendMarker("오늘 잘했어.\n[SEND:연락해봐]"), "오늘 잘했어.");
});

test("스트리밍 중 꼬리가 부분 마커면 그 지점부터 숨김(깜빡임 방지)", () => {
  assert.equal(hideTrailingSendMarker("오늘 잘했어.\n[SE"), "오늘 잘했어.");
  assert.equal(hideTrailingSendMarker("오늘 잘했어.\n[SEND:연락"), "오늘 잘했어.");
});

test("마커 없으면 원문 그대로(뒤 공백만 정리)", () => {
  assert.equal(hideTrailingSendMarker("그냥 대화"), "그냥 대화");
  assert.equal(hideTrailingSendMarker("대괄호 [진짜 대화] 는 건드리지 않음"), "대괄호 [진짜 대화] 는 건드리지 않음");
});
```

- [ ] **Step 2: 테스트 실행 → FAIL 확인** — `node --import tsx --test lib/relationship/sim-stream.test.ts`
  기대: `Cannot find module './sim-stream.ts'` — 비정상 종료.

- [ ] **Step 3: 최소 구현** — `lib/relationship/sim-stream.ts`:

```ts
// lib/relationship/sim-stream.ts — 밤 무대 클라 스트리밍 소비 보조(순수). 완성 텍스트 스트립은 lib/relationship/sim.ts 의 stripSimMarkers.
// 여기선 "스트리밍 중" 화면 표시용으로 [SEND: 꼬리(완성/미완성)를 숨겨 깜빡임을 막는다.
// `[SEND:` 로 시작하는 온전한/부분 마커가 문자열 끝부분에 나타나면 그 지점부터 잘라낸다.
const SEND_TAIL_RE = /\[(?:S(?:E(?:N(?:D(?::[^\]]*)?)?)?)?)?$/;

/** 스트리밍 중 표시 텍스트에서 끝에 걸친 [SEND:...] (완성/미완성)를 숨긴다. 중간의 완성 마커도 제거. */
export function hideTrailingSendMarker(text: string): string {
  // 완성 마커 먼저 제거(디브리핑 외 방어적), 그다음 끝에 걸친 부분 마커 컷.
  return text
    .replace(/\[SEND:[^\]]*\]/g, "")
    .replace(SEND_TAIL_RE, "")
    .replace(/\n{3,}/g, "\n\n")
    .trimEnd();
}
```

- [ ] **Step 4: 테스트 실행 → PASS 확인** — `node --import tsx --test lib/relationship/sim-stream.test.ts`
  기대: `ℹ pass 3 / ℹ fail 0`.

- [ ] **Step 5: 커밋**
```
git add lib/relationship/sim-stream.ts lib/relationship/sim-stream.test.ts
git commit -m "$(cat <<'EOF'
feat(sim-fe): 밤 무대 스트리밍 [SEND] 꼬리 숨김 순수 헬퍼

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 진입 배선 — ProductList 활성화 + 라우트 스캐폴드 + 셸 숨김

파일 허브의 비활성 시뮬 카드를 활성화하고, 몰입 라우트 뼈대를 세운다. 이 태스크 끝에서 "카드 클릭 → 밤 톤 빈 페이지(로그인/소유권 가드 통과)"가 브라우저에서 확인돼야 한다.

**Files:**
- Modify: `components/relationship/ProductList.tsx`
- Modify: `app/relationship/page.tsx`
- Modify: `components/layout/AppShell.tsx`
- Create: `app/relationship/sim/page.tsx`

- [ ] **Step 1: `ProductList` 시뮬 카드 활성화** — `components/relationship/ProductList.tsx` 의 `ProductListProps` 에 `onOpenSim: () => void` 를 추가하고(기존 `onOpenThread` 옆), 44~69행 비활성 placeholder `<div ... opacity-70>` 를 활성 `<button>` 으로 교체. 활성 상담 카드(💬)의 마크업/클래스를 그대로 따르되 이모지/문구만 시뮬용으로:

```tsx
<button
  type="button"
  onClick={onOpenSim}
  className="w-full rounded-2xl p-4 border flex items-center gap-3.5 bg-cream-warm border-lilac-mid/20 hover:border-lilac-mid/40 transition-colors text-left"
>
  <span className="text-2xl shrink-0">🎭</span>
  <span className="flex-1 min-w-0">
    <span className="block text-[15px] font-bold text-eye-purple">연애 시뮬레이션</span>
    <span className="block text-[13px] text-text-light mt-0.5">난감한 상황을 인형과 미리 연습</span>
  </span>
  <span className="text-lilac-mid shrink-0">›</span>
</button>
```
(정확한 클래스는 같은 파일 활성 상담 카드에서 복사해 이모지·라벨만 교체 — "곧 열려"/`opacity-70`/`#a49db4` 회색 제거.)

- [ ] **Step 2: 허브에서 연결** — `app/relationship/page.tsx` 의 `<ProductList onOpenThread={...} />`(780~785행 근처)에 `onOpenSim` 추가. `selected`(선택된 relationshipId, `"me"` 아님)를 쿼리로:

```tsx
<ProductList
  onOpenThread={() => { if (!selectionLoading) setView("thread"); }}
  onOpenSim={() => { if (!selectionLoading && selected !== "me") router.push(`/relationship/sim?rel=${selected}`); }}
/>
```
(`router` 는 이 파일에 이미 `useRouter()` 로 존재 — `load()` 의 `router.replace` 사용처 확인. 없으면 import 추가.)

- [ ] **Step 3: 셸 숨김** — `components/layout/AppShell.tsx` 의 `HIDE_SHELL_PREFIXES` 배열에 `"/relationship/sim"` 추가:

```ts
const HIDE_SHELL_PREFIXES: string[] = ["/login", "/admin", "/start", "/relationship/sim"];
```
⚠️ 순서 주의: `/relationship`(스레드/허브)은 셸을 **유지**해야 하므로 `/relationship/sim` 만 매치돼야 한다. prefix 매칭이 `startsWith` 이므로 `/relationship/sim` 는 `/relationship` 과 구분됨(더 긴 경로). 기존 매칭 로직이 `some(p => pathname.startsWith(p))` 인지 확인하고, `/relationship` 자체가 목록에 없음을 재확인(있으면 스레드 셸까지 사라짐).

- [ ] **Step 4: 라우트 스캐폴드** — `app/relationship/sim/page.tsx`(client component). 이 태스크에선 가드 + 밤 톤 컨테이너 + phase state 골격만(각 단계 컴포넌트는 T3~T7 에서 채움):

```tsx
"use client";
import { useEffect, useRef, useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";

type Phase = "select" | "stage" | "debrief";

export default function SimPage() {
  const router = useRouter();
  const params = useSearchParams();
  const relationshipId = params.get("rel");
  const [phase, setPhase] = useState<Phase>("select");
  const [loading, setLoading] = useState(true);
  const [rel, setRel] = useState<{ id: string; label: string; status: string } | null>(null);
  const startedRef = useRef(false);

  useEffect(() => {
    if (startedRef.current) return;
    startedRef.current = true;
    (async () => {
      const me = await fetch("/api/auth/me", { cache: "no-store" }).then((r) => (r.ok ? r.json() : null)).catch(() => null);
      if (!me?.isAuthenticated) { router.replace(`/login?next=/relationship/sim?rel=${relationshipId ?? ""}`); return; }
      if (!relationshipId) { router.replace("/relationship"); return; }
      // 소유권/상대 로드: 허브 목록에서 이 관계를 확인(없으면 허브로).
      const list = await fetch("/api/relationship", { cache: "no-store" }).then((r) => (r.ok ? r.json() : null)).catch(() => null);
      const found = (list?.relationships ?? []).find((r: { id: string }) => r.id === relationshipId) ?? null;
      if (!found) { router.replace("/relationship"); return; }
      setRel({ id: found.id, label: found.label, status: found.status });
      setLoading(false);
    })();
  }, [relationshipId, router]);

  if (loading || !rel) {
    return (
      <main className="min-h-dvh bg-gradient-to-br from-night to-night-deep flex items-center justify-center">
        <span className="inline-block w-5 h-5 rounded-full border-2 border-gold/30 border-t-gold animate-spin" />
      </main>
    );
  }

  return (
    <main className="min-h-dvh bg-gradient-to-br from-night to-night-deep text-cream-warm">
      {/* T3: phase==="select" → <SituationSelect/> */}
      {/* T4~T6: phase==="stage" → <NightStage/> */}
      {/* T7: phase==="debrief" → <SimDebrief/> */}
      <div className="p-4 text-center text-sm text-lilac">시뮬 셸 (phase: {phase}) — 상대: {rel.label}</div>
    </main>
  );
}
```
⚠️ `GET /api/relationship` 의 실제 응답 shape(`{relationships:[{id,label,status,...}]}`)를 먼저 확인(허브 `load()` 가 쓰는 그대로). 다르면 맞출 것.

- [ ] **Step 5: 타입 체크 + 빌드 라우트 확인** — `npx tsc --noEmit && npx next build`
  기대: tsc 0. 라우트 표에 `○ /relationship/sim` (또는 `ƒ`) 등장.

- [ ] **Step 6: 브라우저 검증** — dev 서버(preview) 기동 후:
  1. 로그인 세션 주입(문서: `browser-e2e-session-injection` — `document.cookie` 로 `byeolkong_user_id`) 또는 실제 로그인.
  2. `/relationship` → 상대 선택 → 🎭 연애 시뮬레이션 카드 클릭 → `/relationship/sim?rel=<id>` 로 이동, **Header/BottomTab 없이** 밤 톤 배경 + "시뮬 셸(phase: select)" 확인.
  3. `read_console_messages` 로 에러 0 확인. `rel` 없는 직접 진입 → `/relationship` 리다이렉트 확인.

- [ ] **Step 7: 커밋**
```
git add components/relationship/ProductList.tsx app/relationship/page.tsx components/layout/AppShell.tsx app/relationship/sim/page.tsx
git commit -m "$(cat <<'EOF'
feat(sim-fe): 시뮬 진입 카드 활성화 + 몰입 라우트 스캐폴드(셸 숨김)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: ① 상황 선택 + ①-b 라이트 컨텍스트 (`SituationSelect`)

관계별 상황 카드 그리드(라이트 톤 본문 + 밤 톤 상단 바, 스펙 §3) → 카드 선택 시 ①-b 맞춤 질문(`contextPrompt`) 표시(선택·건너뛰기) → 세션 생성은 T4 결제 확인으로 넘김(여기선 `onPick(situationId, userContext)` 콜백까지).

**Files:**
- Create: `components/relationship/sim/SituationSelect.tsx`
- Modify: `app/relationship/sim/page.tsx`(마운트 + `onPick` 배선)

- [ ] **Step 1: 컴포넌트 작성** — `components/relationship/sim/SituationSelect.tsx`:

```tsx
"use client";
import { useState } from "react";
import { getSituations, type SimSituation } from "@/lib/relationship/situations";
import type { RelationshipStatus } from "@/lib/relationship/types";

interface Props {
  status: RelationshipStatus;
  partnerLabel: string;
  onPick: (situationId: string, userContext: string) => void;
  onClose: () => void; // 허브로 나가기
}

export default function SituationSelect({ status, partnerLabel, onPick, onClose }: Props) {
  const situations = getSituations(status); // 관계별 + any
  const [picked, setPicked] = useState<SimSituation | null>(null);
  const [ctx, setCtx] = useState("");

  // 스텝 1: 카드 그리드
  if (!picked) {
    return (
      <section className="animate-fade-in">
        <header className="sticky top-0 bg-gradient-to-br from-night to-night-deep px-4 py-3 flex items-center gap-2">
          <button onClick={onClose} className="text-lilac text-sm" aria-label="닫기">‹ 나가기</button>
          <h1 className="flex-1 text-center text-cream-warm font-display text-lg">어떤 상황을 연습해볼까?</h1>
          <span className="w-12" />
        </header>
        <div className="bg-cream rounded-t-3xl min-h-[70dvh] p-4 grid grid-cols-1 gap-3">
          <p className="text-center text-text-light text-sm mb-1">{partnerLabel} 와의 상황을 골라봐</p>
          {situations.map((s) => (
            <button key={s.id} onClick={() => setPicked(s)}
              className="w-full text-left rounded-2xl p-4 bg-cream-warm border border-lilac-mid/20 hover:border-lilac-deep/40 transition-colors flex items-start gap-3">
              <span className="text-2xl shrink-0">{s.emoji}</span>
              <span className="min-w-0">
                <span className="block font-bold text-eye-purple">{s.label}</span>
                <span className="block text-[13px] text-text-light mt-0.5">{s.desc}</span>
              </span>
            </button>
          ))}
          {/* ✍️ 직접 쓰기 = 백엔드 미지원(카드 8개만) → under-spec #2, 이번 범위 제외 */}
        </div>
      </section>
    );
  }

  // 스텝 2: ①-b 라이트 컨텍스트(선택·건너뛰기)
  return (
    <section className="animate-fade-in">
      <header className="sticky top-0 bg-gradient-to-br from-night to-night-deep px-4 py-3 flex items-center gap-2">
        <button onClick={() => setPicked(null)} className="text-lilac text-sm" aria-label="뒤로">‹ 뒤로</button>
        <h1 className="flex-1 text-center text-cream-warm font-display text-lg">{picked.emoji} {picked.label}</h1>
        <span className="w-12" />
      </header>
      <div className="bg-cream rounded-t-3xl min-h-[70dvh] p-4">
        <p className="text-eye-purple font-medium mb-2">{picked.contextPrompt}</p>
        <textarea value={ctx} onChange={(e) => setCtx(e.target.value.slice(0, 500))}
          placeholder="한 줄만 적어도 좋아 (건너뛰기 OK)" rows={4}
          className="w-full rounded-xl border border-lilac-mid/30 p-3 text-eye-purple resize-none focus:border-lilac-deep outline-none" />
        <div className="mt-4 flex flex-col gap-2">
          <button onClick={() => onPick(picked.id, ctx.trim())}
            className="w-full rounded-xl py-3 bg-lilac-deep text-white font-bold">시작하기</button>
          <button onClick={() => onPick(picked.id, "")}
            className="w-full rounded-xl py-2.5 text-text-light text-sm">건너뛰고 바로 시작</button>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: page.tsx 배선** — `app/relationship/sim/page.tsx` 의 `phase==="select"` 자리에 마운트. `onPick` 은 T4 에서 결제/세션 생성으로 연결(이 태스크에선 임시로 `console.log` 후 `setPhase("stage")` 스텁 — T4 에서 교체):

```tsx
{phase === "select" && (
  <SituationSelect
    status={rel.status as RelationshipStatus}
    partnerLabel={rel.label}
    onClose={() => router.replace("/relationship")}
    onPick={(situationId, userContext) => { /* T4: 결제 확인 → 세션 생성 */ setPending({ situationId, userContext }); }}
  />
)}
```
(`pending` state `{situationId,userContext}|null` 추가 — T4 결제 모달의 입력.)

- [ ] **Step 3: 타입 체크** — `npx tsc --noEmit` → 0.

- [ ] **Step 4: 브라우저 검증** — `/relationship/sim?rel=<id>`:
  - 관계 status 에 맞는 카드만 뜨는가(예: dating 상대 → 서운함/화해 2장 + any). 카드 이모지·라벨·desc 정상?
  - 카드 클릭 → contextPrompt 질문 + textarea + [시작하기]/[건너뛰기] 표시. 뒤로가기 동작.
  - 상단 밤 톤 바 + 본문 크림 톤(스펙 §3) 스크린샷.

- [ ] **Step 5: 커밋**
```
git add components/relationship/sim/SituationSelect.tsx app/relationship/sim/page.tsx
git commit -m "$(cat <<'EOF'
feat(sim-fe): 상황 선택 + 라이트 컨텍스트(①·①-b) 화면

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 결제 확인 + 세션 생성 (StarConfirmModal + `POST /api/relationship/sim`)

무료 첫 판 없음 → `SIM_COST`(15별) 차감 전 `StarConfirmModal` 확인(스펙 §7). 확인 시 세션 생성 API 호출 → `simReadingId`/`frame`/`contextPrompt` 받아 `phase="stage"` 로.

**Files:**
- Modify: `app/relationship/sim/page.tsx`

- [ ] **Step 1: 결제·생성 배선** — `pending` 이 set 되면 `StarConfirmModal` 을 띄우고, 확인 시 `POST /api/relationship/sim`:

```tsx
import StarConfirmModal from "@/components/common/StarConfirmModal";
import { SIM_COST } from "@/lib/relationship/types";
// ...
const [pending, setPending] = useState<{ situationId: string; userContext: string } | null>(null);
const [creating, setCreating] = useState(false);
const [session, setSession] = useState<{ simReadingId: string; frame: string; statusLabel: string } | null>(null);
const [balance, setBalance] = useState<number | null>(null); // /api/auth/me 또는 세션 응답에서

async function createSession() {
  if (!pending || !rel) return;
  setCreating(true);
  const res = await fetch("/api/relationship/sim", {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ relationshipId: rel.id, situationId: pending.situationId, userContext: pending.userContext }),
  });
  if (res.status === 402) { setCreating(false); router.push("/shop?reason=sim"); return; }
  if (!res.ok) { setCreating(false); /* 에러 토스트 */ return; }
  const data = await res.json();
  setSession({ simReadingId: data.simReadingId, frame: data.frame, statusLabel: data.statusLabel });
  setBalance(data.balance);
  setPending(null); setCreating(false); setPhase("stage");
}
// JSX:
{pending && (
  <StarConfirmModal
    cost={SIM_COST}
    balance={balance ?? 0}
    loading={creating}
    accent="lilac"
    title="연애 시뮬레이션 한 판"
    subtitle="연습 + 별콩이 코칭 + 디브리핑까지 포함"
    confirmLabel="시작하기"
    onConfirm={createSession}
    onCharge={() => router.push("/shop?reason=sim")}
    onClose={() => setPending(null)}
  />
)}
```
⚠️ `StarConfirmModal` 의 실제 props(`cost·balance·loading·accent·title·subtitle·confirmLabel·onConfirm·onCharge·onClose`)를 파일에서 재확인하고 정확히 맞출 것. `balance` 초기값은 `/api/auth/me` 로드시 함께 받아두거나(가드 `useEffect`에서), 없으면 0 으로 두고 확인 시 402 로 처리.

- [ ] **Step 2: 타입 체크** — `npx tsc --noEmit` → 0.

- [ ] **Step 3: 브라우저 검증** — 상황 선택 → [시작하기] → 결제 확인 모달(15별, 잔액 표시) → 확인 → 밤 무대(phase stage)로. 잔액 부족 계정으로 402 → `/shop` 이동 확인. `read_network_requests` 로 `POST /api/relationship/sim` 200 + `simReadingId` 응답 확인.

- [ ] **Step 4: 커밋**
```
git add app/relationship/sim/page.tsx
git commit -m "$(cat <<'EOF'
feat(sim-fe): 판 결제 확인 + 세션 생성 배선(SIM_COST/402)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: ② 밤 무대 셸 — 배경·접히는 인형·프레임 고지 (`NightStage`/`DollPortrait`)

밤 무대의 시각 셸: night 그라데이션 + 금색 별 파티클 + 접히는 인형(진입 대형 → 대화 후 sticky 소형, 스펙 §2) + 프레임 고지(세션 생성 응답 `frame`) + 하단 입력창 자리. 대화 로직은 T6.

**Files:**
- Create: `components/relationship/sim/DollPortrait.tsx`
- Create: `components/relationship/sim/NightStage.tsx`
- Create: `components/relationship/sim/SimBubble.tsx`
- Modify: `app/relationship/sim/page.tsx`(stage 마운트)

- [ ] **Step 1: 별 파티클 + 접히는 인형** — `components/relationship/sim/DollPortrait.tsx`. 별 파티클은 `components/tarot/CardSpreadView.tsx:92-126` `BackgroundStars` 패턴을 금색으로 복제. 인형은 `components/relationship/DollAvatar.tsx` 의 status 색 프리셋 재사용:

```tsx
"use client";
import DollAvatar from "@/components/relationship/DollAvatar";

const STARS = [{ top: "8%", left: "10%", s: 3, d: 0 }, { top: "14%", left: "82%", s: 2, d: 0.4 }, { top: "22%", left: "40%", s: 2, d: 0.8 }, { top: "6%", left: "60%", s: 3, d: 1.2 }, { top: "30%", left: "18%", s: 2, d: 0.6 }, { top: "26%", left: "70%", s: 3, d: 1.5 }];

export function NightStars() {
  return (
    <div className="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden>
      {STARS.map((p, i) => (
        <span key={i} className="absolute rounded-full bg-gold-soft animate-star-twinkle"
          style={{ top: p.top, left: p.left, width: p.s, height: p.s, boxShadow: `0 0 ${p.s * 2}px rgba(232,194,106,0.6)`, animationDelay: `${p.d}s` }} />
      ))}
    </div>
  );
}

export default function DollPortrait({ status, label, collapsed }: { status: string; label: string; collapsed: boolean }) {
  return (
    <div className={collapsed ? "flex items-center gap-2" : "flex flex-col items-center gap-2 py-6 animate-fade-in"}>
      <div className={collapsed ? "" : "animate-float"}>
        <DollAvatar kind="partner" status={status} name={label} size={collapsed ? 30 : 80} />
      </div>
      {!collapsed && (
        <>
          <span className="text-cream-warm font-bold">{label}</span>
          <span className="text-[12px] text-gold-soft">🌙 마음이 그린 인형</span>
        </>
      )}
      {collapsed && <span className="text-sm text-cream-warm/90">{label}</span>}
    </div>
  );
}
```
⚠️ `DollAvatar` 의 실제 props(`kind`·`status`·`name`·`size`·`imageUrl?`)를 재확인. `size` 가 px number 인지 확인(조사: `size?`).

- [ ] **Step 2: 인형 대사 버블** — `components/relationship/sim/SimBubble.tsx`. `components/tarot/ChatBubble.tsx` 를 참고하되 별콩이 각인 없이 인형용(왼쪽 정렬, 밤 톤 위 읽히는 색):

```tsx
export default function SimBubble({ content, streaming }: { content: string; streaming?: boolean }) {
  return (
    <div className="max-w-[82%] self-start rounded-2xl rounded-tl-sm bg-lilac-soft/90 text-eye-purple px-3.5 py-2.5 text-[15px] leading-relaxed whitespace-pre-wrap">
      {content}{streaming && <span className="inline-block w-1.5 h-4 ml-0.5 align-middle bg-eye-purple/60 animate-pulse-soft" />}
    </div>
  );
}
```

- [ ] **Step 3: 밤 무대 셸** — `components/relationship/sim/NightStage.tsx`. 이 태스크엔 셸+프레임고지+접히는 인형+빈 대화영역+입력창 자리(전송 로직 T6 에서). props 로 세션 정보 받음:

```tsx
"use client";
import { useState } from "react";
import DollPortrait, { NightStars } from "./DollPortrait";
import ByeolkongNote from "./ByeolkongNote";
import SimBubble from "./SimBubble";

export interface NightStageProps {
  simReadingId: string;
  status: string;
  label: string;
  frame: string; // 프레임 고지(별콩이 노트, 결정적)
  onDebrief: () => void; // 정리하기 → T7 디브리핑 phase
}

export default function NightStage(props: NightStageProps) {
  const [started, setStarted] = useState(false); // 첫 유저 발화 후 인형 접힘
  // T6: messages/liveText/헤더 상태 + say/note 소비
  return (
    <div className="relative flex flex-col" style={{ height: "100dvh" }}>
      <NightStars />
      <div className="sticky top-0 z-10 bg-gradient-to-b from-night to-transparent px-4 pt-3 pb-2">
        <DollPortrait status={props.status} label={props.label} collapsed={started} />
      </div>
      <div className="relative z-10 flex-1 overflow-y-auto px-4 py-3 flex flex-col gap-3">
        <ByeolkongNote text={props.frame} kind="frame" />
        {/* T6: 인형 버블 + 별콩이 노트 + liveText */}
      </div>
      <div className="relative z-10 border-t border-lilac-mid/20 bg-night-deep/80 px-3 py-2.5">
        {/* T6: 💭 도움 / 입력창 / 정리하기 */}
        <button onClick={props.onDebrief} className="text-gold-soft text-sm">정리하기</button>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: page.tsx stage 마운트** —

```tsx
{phase === "stage" && session && (
  <NightStage
    simReadingId={session.simReadingId} status={rel.status} label={rel.label} frame={session.frame}
    onDebrief={() => setPhase("debrief")}
  />
)}
```

- [ ] **Step 5: 타입 체크 + 브라우저** — `npx tsc --noEmit` → 0. 브라우저: 세션 생성 후 밤 무대 진입 → 대형 인형(글로우+"🌙 마음이 그린 인형") + 프레임 고지 노트 카드 + 금색 별 반짝임 스크린샷. (대화·접힘은 T6 후 재검증.)

- [ ] **Step 6: 커밋**
```
git add components/relationship/sim/DollPortrait.tsx components/relationship/sim/SimBubble.tsx components/relationship/sim/NightStage.tsx app/relationship/sim/page.tsx
git commit -m "$(cat <<'EOF'
feat(sim-fe): 밤 무대 셸(별 파티클·접히는 인형·프레임 고지)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: ② 밤 무대 대화 — say/note SSE 소비 + 헤더/409 + 별콩이 노트 (`NightStage` 완성 · `ByeolkongNote`)

3화자 대화의 심장. `say`(인형) 스트림 소비 + `X-Sim-Turn`/`X-Sim-Autonote`/`X-Sim-Force-Debrief`/`X-Sim-Sensitive` 헤더 처리 + 409 턴캡 + `note`(💭 온디맨드 / autonote 자동) + `SafetyBanner`(crisis).

**Files:**
- Create: `components/relationship/sim/ByeolkongNote.tsx`
- Modify: `components/relationship/sim/NightStage.tsx`

- [ ] **Step 1: 별콩이 노트 카드** — `components/relationship/sim/ByeolkongNote.tsx`(금색 노트, 버블 아님. 스펙 §2·§5):

```tsx
export default function ByeolkongNote({ text, kind = "note", streaming }: { text: string; kind?: "frame" | "note"; streaming?: boolean }) {
  return (
    <div className="self-stretch rounded-2xl border border-gold/40 bg-gold/10 px-4 py-3 animate-fade-in">
      <div className="flex items-center gap-1.5 mb-1 text-gold-soft text-[12px] font-bold">
        <span>🌙</span><span>{kind === "frame" ? "별콩이" : "별콩이 노트"}</span>
      </div>
      <p className="text-cream-warm/95 text-[14px] leading-relaxed whitespace-pre-wrap">{text}{streaming && <span className="inline-block w-1.5 h-4 ml-0.5 align-middle bg-gold-soft/70 animate-pulse-soft" />}</p>
    </div>
  );
}
```

- [ ] **Step 2: 대화 상태 + say 소비** — `NightStage.tsx` 에 메시지 상태와 전송 로직 추가. 메시지 모델: `{ id; who: "doll"|"note"; text: string }[]`. say 소비는 `ThreadChat.send()`(components/relationship/ThreadChat.tsx:175-252) reader 루프를 미러:

```tsx
import { useRef, useState } from "react";
import { hideTrailingSendMarker } from "@/lib/relationship/sim-stream";
import SafetyBanner from "@/components/safety/SafetyBanner";
import type { SensitiveCategory } from "@/lib/sensitive"; // 실제 타입 경로 확인

type Msg = { id: number; who: "doll" | "note"; text: string };
// state: messages Msg[], live {who,text}|null, input string, sending bool,
//        forceDebrief bool(정리하기 강조), crisis {category,severity}|null

async function sendSay(text: string) {
  setSending(true); setInput("");
  setMessages((m) => [...m, { id: Date.now(), who: "doll", text: `__user__:${text}` }]); // 유저 발화도 리스트에(렌더 시 우측)
  const res = await fetch("/api/relationship/sim/chat", {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ simReadingId, message: text, action: "say" }),
  });
  if (res.status === 409) { setSending(false); setForceDebrief(true); return; } // 턴캡 → 정리하기 유도
  if (!res.ok || !res.body) { setSending(false); /* 에러 */ return; }
  const sensitive = res.headers.get("X-Sim-Sensitive") === "1";
  if (sensitive) setCrisis({ category: res.headers.get("X-Sensitive-Category") ?? "other", severity: Number(res.headers.get("X-Sensitive-Severity") ?? "1") });
  const autonote = res.headers.get("X-Sim-Autonote") === "1";
  const forceDeb = res.headers.get("X-Sim-Force-Debrief") === "1";
  const reader = res.body.getReader(); const dec = new TextDecoder(); let acc = "";
  setLive({ who: sensitive ? "note" : "doll", text: "" });
  while (true) { const { done, value } = await reader.read(); if (done) break; acc += dec.decode(value, { stream: true }); setLive({ who: sensitive ? "note" : "doll", text: acc }); }
  setMessages((m) => [...m, { id: Date.now() + 1, who: sensitive ? "note" : "doll", text: acc }]);
  setLive(null); setStarted(true); setSending(false);
  if (forceDeb) setForceDebrief(true);
  if (autonote && !sensitive) await fetchNote(); // 자동 노트
}

async function fetchNote() {
  const res = await fetch("/api/relationship/sim/chat", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ simReadingId, action: "note" }) });
  if (!res.ok || !res.body) return;
  const reader = res.body.getReader(); const dec = new TextDecoder(); let acc = "";
  setLive({ who: "note", text: "" });
  while (true) { const { done, value } = await reader.read(); if (done) break; acc += dec.decode(value, { stream: true }); setLive({ who: "note", text: acc }); }
  setMessages((m) => [...m, { id: Date.now() + 2, who: "note", text: acc }]); setLive(null);
}
```
- 렌더: `messages.map` → `who==="note"` 면 `<ByeolkongNote>`, `doll` 이면 `text.startsWith("__user__:")` 로 유저/인형 구분(유저=우측 정렬 버블, 인형=`<SimBubble>`). `live` 는 `hideTrailingSendMarker(live.text)` 로 표시(say 는 [SEND] 없지만 방어적, note 도 동일).
- 하단 바: `💭 도움`(→`fetchNote()`, sending 중 비활성) + textarea(IME 조합 가드 `composingRef`, Enter 전송, 8000자, `ThreadChat` 693~748행 템플릿 복사) + `정리하기`(→`onDebrief`, `forceDebrief` 면 금색 강조/펄스).
- `crisis` set 시 상단에 `<SafetyBanner category={crisis.category} severity={crisis.severity} onClose={()=>setCrisis(null)} />`.

- [ ] **Step 3: 타입 체크** — `npx tsc --noEmit` → 0. (`SensitiveCategory` 타입 경로·`SafetyBanner` props 실제 확인.)

- [ ] **Step 4: 브라우저 E2E(핵심)** — dev 서버 + 실제 판 하나:
  1. 유저 발화 입력 → 인형 대사 스트리밍(좌측 버블) + 첫 발화 후 인형 초상화 sticky 축소(collapsed) 확인.
  2. 3턴째쯤 자동 노트(🌙 금색 카드) 등장(`X-Sim-Autonote`). `💭 도움` 클릭 → 온디맨드 노트.
  3. `read_network_requests` 로 say/note 200 + 헤더 확인. `read_console_messages` 에러 0.
  4. (선택) 위기 문구 입력 → `SafetyBanner` + 별콩이 노트 톤 응답(인형 대사 아님) 확인.

- [ ] **Step 5: 커밋**
```
git add components/relationship/sim/ByeolkongNote.tsx components/relationship/sim/NightStage.tsx
git commit -m "$(cat <<'EOF'
feat(sim-fe): 밤 무대 3화자 대화(say/note SSE·턴캡·자동노트·crisis)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: ③ 디브리핑 (`SimDebrief`) + 스레드 연결

`정리하기`/턴캡 → `action:"debrief"`(JSON, 스트림 아님) → 통찰/💌보낼말/마무리 3블록 + [보낼말 저장·복사]/[별콩이랑 더 얘기하기→스레드] CTA(스펙 §6).

**Files:**
- Create: `components/relationship/sim/SimDebrief.tsx`
- Modify: `app/relationship/sim/page.tsx`(debrief 마운트)

- [ ] **Step 1: 디브리핑 컴포넌트** — `components/relationship/sim/SimDebrief.tsx`. debrief 는 완료까지 로딩(스피너) 후 JSON:

```tsx
"use client";
import { useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { NightStars } from "./DollPortrait";

export default function SimDebrief({ simReadingId }: { simReadingId: string }) {
  const router = useRouter();
  const [state, setState] = useState<"loading" | "done" | "error">("loading");
  const [debrief, setDebrief] = useState(""); const [sendMessage, setSendMessage] = useState<string | null>(null);
  const [copied, setCopied] = useState(false);
  const ran = useRef(false);

  useEffect(() => {
    if (ran.current) return; ran.current = true;
    (async () => {
      const res = await fetch("/api/relationship/sim/chat", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ simReadingId, action: "debrief" }) });
      if (!res.ok) { setState("error"); return; }
      const d = await res.json();
      setDebrief(d.debrief ?? ""); setSendMessage(d.sendMessage ?? null); setState("done");
    })();
  }, [simReadingId]);

  if (state === "loading") return (
    <div className="min-h-dvh flex flex-col items-center justify-center gap-3 text-cream-warm">
      <NightStars /><span className="inline-block w-6 h-6 rounded-full border-2 border-gold/30 border-t-gold animate-spin" />
      <p className="text-lilac text-sm">별콩이가 오늘 무대를 정리하고 있어…</p>
    </div>
  );
  if (state === "error") return (
    <div className="min-h-dvh flex flex-col items-center justify-center gap-3 text-cream-warm px-6 text-center">
      <p>정리 중 문제가 생겼어. 잠시 후 다시 시도해줘.</p>
      <button onClick={() => router.replace("/relationship")} className="text-gold-soft">파일로 돌아가기</button>
    </div>
  );

  async function copySend() { if (!sendMessage) return; try { await navigator.clipboard.writeText(sendMessage); setCopied(true); setTimeout(() => setCopied(false), 1500); } catch { /* noop */ } }

  return (
    <div className="min-h-dvh bg-gradient-to-b from-night-deep to-cream text-eye-purple animate-fade-in">
      <div className="px-5 py-8 max-w-lg mx-auto">
        <h1 className="font-display text-xl text-cream-warm text-center mb-1">오늘 무대, 정리해볼게</h1>
        <div className="mt-6 rounded-2xl bg-cream-warm border border-lilac-mid/20 p-5 whitespace-pre-wrap leading-relaxed text-[15px]">{debrief}</div>
        {sendMessage && (
          <div className="mt-4 rounded-2xl border border-gold/50 bg-gold/10 p-4">
            <div className="text-gold-soft text-[12px] font-bold mb-1.5">💌 이 사람에게 보낼 말</div>
            <p className="text-eye-purple text-[15px] leading-relaxed">{sendMessage}</p>
            <button onClick={copySend} className="mt-3 w-full rounded-xl py-2.5 bg-lilac-deep text-white font-bold text-sm">{copied ? "복사됐어 ✓" : "보낼 말 복사하기"}</button>
          </div>
        )}
        <button onClick={() => router.push("/relationship")} className="mt-6 w-full rounded-xl py-3 border border-lilac-mid/40 text-eye-purple font-medium">별콩이랑 더 얘기하기 →</button>
      </div>
    </div>
  );
}
```
⚠️ 스펙 §6 "3블록"은 별콩이가 `debrief` 텍스트 안에 통찰+보낼말+마무리로 생성한다(P3a-1 페르소나). `sendMessage` 는 `[SEND:]` 에서 추출된 별도 필드라 위처럼 강조 카드로 뽑아 보여준다. 본문 `debrief` 는 `stripSimMarkers` 로 이미 마커 제거된 상태(서버 T10).
⚠️ [별콩이랑 더 얘기하기] = 스레드로. 현재 스레드는 `/relationship` 의 `view="thread"` state라 URL 로 직접 못 연다 → MVP 는 `/relationship` 로 보내고(허브), 스레드 자동 오픈은 under-spec #3(허브가 쿼리로 thread 뷰 열게 하는 소폭 수정 필요).

- [ ] **Step 2: page.tsx debrief 마운트** —
```tsx
{phase === "debrief" && session && <SimDebrief simReadingId={session.simReadingId} />}
```

- [ ] **Step 3: 타입 체크 + 빌드** — `npx tsc --noEmit && npx next build` → tsc 0, 라우트 표에 `/relationship/sim`.

- [ ] **Step 4: 브라우저 E2E** — 무대에서 `정리하기` → 로딩 스피너("정리하고 있어") → 3블록 디브리핑 + 💌 보낼 말 카드 + 복사(클립보드) 동작 + [별콩이랑 더 얘기하기] → `/relationship`. `read_network_requests` 로 `debrief` 200 JSON(`debrief`·`sendMessage`) 확인.

- [ ] **Step 5: 커밋**
```
git add components/relationship/sim/SimDebrief.tsx app/relationship/sim/page.tsx
git commit -m "$(cat <<'EOF'
feat(sim-fe): 디브리핑 화면(3블록·보낼 말 복사·스레드 연결)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: 전체 흐름 E2E + 마감

한 판 전체(진입 → 상황 → 결제 → 무대 → 디브리핑 → 스레드)를 실제 dev 서버에서 통과시키고 빌드/타입을 최종 확인.

**Files:** (검증 전용 — 필요 시 소폭 수정)

- [ ] **Step 1: 전체 E2E** — dev 서버 + 실제(또는 세션 주입) 로그인:
  1. `/relationship` → 상대 선택 → 🎭 시뮬 카드 → 상황 카드 → 컨텍스트(입력/건너뛰기 둘 다) → 결제 확인 → 밤 무대.
  2. 3~5턴 대화(인형 스트리밍·접힘·자동노트·💭 도움) → `정리하기` → 디브리핑(3블록·💌·복사) → [더 얘기하기] → `/relationship`.
  3. `read_console_messages`(에러 0)·`read_network_requests`(sim/sim-chat 2xx, 402/409 분기 정상) 확인. 데스크톱+모바일(`resize_window mobile`) 각각 스크린샷.
- [ ] **Step 2: 회귀** — `npx tsc --noEmit && npx next build`(에러 0, 라우트 표) + `node --import tsx --test lib/relationship/sim-stream.test.ts`(pass 3/0). 기존 `/relationship` 스레드/허브가 셸 숨김 변경(T2)에 영향 없는지 브라우저로 재확인(`/relationship` 은 Header/BottomTab 유지).
- [ ] **Step 3: 커밋(있으면)** + 마감 보고.

---

## Self-Review

### 스펙 커버리지 맵 (§ → Task)

| 스펙 § | 내용 | Task |
|---|---|---|
| §1 흐름(허브→상황→①-b→무대→디브리핑→스레드) | 진입 배선 + phase 머신 + 3화면 + CTA | T2·T3·T5·T6·T7 |
| §2 밤 무대(밤 톤·접히는 인형·노트 카드·자유대화·💭/정리하기·턴한도) | `NightStage`·`DollPortrait`·`ByeolkongNote`·say/note·409 | T5·T6 |
| §3 상황 선택 + 라이트 컨텍스트(카드·contextPrompt·건너뛰기) | `SituationSelect` | T3 |
| §5 3화자(인형/별콩이 분리·온디맨드+자동노트·안전) | say/note 분리 소비·autonote·`SafetyBanner` | T6 |
| §6 디브리핑(통찰+💌보낼말+CTA) | `SimDebrief`·복사·스레드 | T7 |
| §7 과금(판 고정가 X<20·lib/stars) | `StarConfirmModal`+`SIM_COST`+402 | T4 |
| §8 저장(reading·파일 허브 노출) | 서버가 이미 저장(P3a-1). 파일 허브 자동 노출은 관계 필터(기존) | — (백엔드) |
| §2 몰입(스레드와 물리 차단) | 몰입 라우트 + `HIDE_SHELL_PREFIXES` | T2 |

### 플레이스홀더 스캔
- 로직(SSE 소비·헤더/409 분기·상태 전이·클립보드)은 실제 코드로 명시. 스타일/JSX 는 재사용 토큰·컴포넌트(§File Structure)와 스펙 §2·§6 규격 참조 + 브라우저 검증 루프로 확정(프론트 특성상 픽셀 세부는 검증에서 조정). "적절히 처리" 류 없음.
- ⚠️ 각 Task 의 "실제 props/응답 shape 재확인" 지시는 **의도된 것**(조사에서 확인했지만 구현자가 파일에서 재확인해 드리프트 방지). 플레이스홀더 아님.

### 컨트롤러가 풀어야 할 under-specified 지점 (구현 전 확인 권장)
1. **몰입 라우트 방식** — 본 플랜은 `app/relationship/sim/page.tsx` 신설 + `HIDE_SHELL_PREFIXES` 로 Header/BottomTab 숨김(스펙 §2 몰입·물리 차단 근거, 코드베이스 최초). 대안: `/relationship` 내부 view state 추가(셸 못 숨김). **결정 필요.**
   → ✅ **오너 결정(2026-08-07): 전용 라우트 + 탭·헤더 숨김 채택**(플랜 기본, FE-T2).
2. **✍️ 직접 쓰기(자유 상황)** — 스펙 §3 "카드가 주, 자유가 보조"인데 **백엔드(`getSituation`)는 카드 8개만 지원**(자유 상황 id 없음). 본 플랜은 **카드 8개만(MVP)**, 자유쓰기는 이월. 자유쓰기를 넣으려면 백엔드에 generic 상황(예: `custom` + 유저 텍스트를 dollStance 대체)이 필요 = P3a-1 수정. **범위 확인.**
   → ✅ **오너 결정(2026-08-07): 자유쓰기 지금 지원.** `situations.ts` 에 `CUSTOM_SITUATION`(id `custom`, relationship `any`, safety high)을 **SITUATIONS 배열 밖**에 두고 getter 3종(`getSituations`·`getSituation`·`BY_ID`)만 포함 → 라우트·빌더 **무수정**, generic dollStance + `userContext`(유저 서술) 주입으로 일반 상황처럼 흐름. 기존 `situations.test.ts` 8개 그대로 통과(길이 8 불변). FE-T3 에 백엔드 스텝 + custom 카드 처리(서술 사실상 필수) 반영.
3. **[별콩이랑 더 얘기하기] 스레드 자동 오픈** — 스레드는 `/relationship` 의 `view="thread"` state라 URL 로 직접 못 연다. 본 플랜 MVP 는 `/relationship`(허브)로 이동. 자동 스레드 오픈은 `app/relationship/page.tsx` 가 `?view=thread&rel=` 쿼리를 읽게 하는 소폭 수정 필요 — **이월 or T2 에 포함할지 확인.**
4. **보관함 노출** — `app/readings/page.tsx` 는 고민톡/사주 2탭뿐, `relationship_sim` 분기 없음. 1차 노출은 "그 사람 파일"(관계 필터, 기존)이라 MVP 무관하나, 보관함 탭 노출을 원하면 별도 작업. **확인만.**
5. **잔액(balance) 초기 로드** — 결제 모달의 `balance` 표시를 위해 `/api/auth/me` 또는 전용 조회가 필요. 본 플랜은 가드 로드시 함께 받거나 0→402 처리. 정확한 별 잔액 API 경로 확인 필요.

### 타입 정합
- `getSituations(status: RelationshipStatus)` / `SimSituation` / `SIM_COST`(=15) — P3a-1 export 그대로 클라 import.
- 세션 생성 응답 `{simReadingId, frame, statusLabel, contextPrompt, cost, balance, success}` → `session` state. chat say/note = plain text 스트림(reader), debrief = JSON(`{debrief, sendMessage}`).
- 헤더 키는 fetch 응답에서 소문자 정규화 주의(`res.headers.get("X-Sim-Autonote")` 는 대소문자 무관 동작 — get 은 case-insensitive).
- `hideTrailingSendMarker`(T1) — `NightStage` liveText 표시에 사용.

### 프론트 검증 한계 명시
- 페르소나/톤 품질은 P3a-1 `qa:sim` 이 이미 검증(8판 통과). 이 플랜은 "계약을 올바르게 소비/표시"만 검증(브라우저 E2E + tsc + build). 유닛은 순수 로직(`sim-stream`)만.
