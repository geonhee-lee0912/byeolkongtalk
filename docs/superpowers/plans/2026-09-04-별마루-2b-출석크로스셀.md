# 별마루 ②-b 출석·보상 + 크로스셀 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루에 ①출석 체크인 버튼(전 유저 습관, 스트릭 표시)+구독 연동 보상(30일 중 20일 출석→10별 lazy grant)과 ②하단 자사 크로스셀 추천 카드를 붙인다.

**Architecture:** 출석 = 신규 `byeolmaru_checkins`(user_id, checkin_date PK) 일 1행. 보상은 구독 만료 시 **다음 별마루 방문에서 lazy 정산**(만료·미정산 구독의 창 내 출석 ≥20 → `chargeStars(10, 멱등키)` + `reward_granted_at` 마킹). 크로스셀 = 오늘 축 기반 룰로 자사 상품 1개 추천(별콩이 톤 카드). 전부 룰/기존 재화 인프라 — 신규 LLM·변동비 0.

**Tech Stack:** Next.js 16, TypeScript, Supabase RPC(`charge_stars` 멱등 by payment_id TEXT), `node:test`, 기존 재사용(`lib/stars.ts` `chargeStars`, ②-a `entitlement.ts`, `ByeolmaruView`, `PremiumBlock`/`PartnerSlot` 카드 패턴).

**부모 스펙:** `docs/superpowers/specs/2026-09-04-별마루-2-구독게이트-design.md` §6(출석·보상)·§7(크로스셀)·§8(데이터)·§9(계측). **선행:** ②-a 완료(`7a38b28`) — `byeolmaru_subscriptions`(+`reward_granted_at` 컬럼 이미 존재)·`entitlement.ts`·계측 4종.

---

## 파일 구조

**생성:**
- `supabase/migrations/20260904200000_byeolmaru_checkins.sql` — 출석 테이블
- `lib/byeolmaru/attendance.ts` — 순수 `computeStreak` + 상수 + DB(`recordCheckin`/`getAttendanceState`/`grantDueReward`)
- `lib/byeolmaru/attendance.test.ts`
- `lib/byeolmaru/crosssell.ts` — 순수 추천 룰
- `lib/byeolmaru/crosssell.test.ts`
- `app/api/byeolmaru/checkin/route.ts` — POST 출석
- `components/byeolmaru/AttendanceStrip.tsx` — 출석 버튼/스트릭
- `components/byeolmaru/CrossSellCard.tsx` — 추천 카드

**수정:**
- `app/api/byeolmaru/calendar/route.ts` — 응답에 `attendance` 추가 + `grantDueReward(userId)` 호출
- `components/byeolmaru/ByeolmaruView.tsx` — `CalendarResponse.attendance` + `AttendanceStrip`(header 아래) + `CrossSellCard`(30일 흐름↔PartnerSlot) + checkin 핸들러
- `lib/analytics/ui-events.ts` — `byeolmaru_checkin`·`byeolmaru_crosssell_clicked` 추가
- `lib/analytics/ui-events.test.ts`

**핵심 인터페이스(태스크 간 계약):**
```ts
// lib/byeolmaru/attendance.ts
export const ATTENDANCE_THRESHOLD = 20;    // 구독 30일 중 이만큼 출석하면 보상
export const ATTENDANCE_REWARD_STARS = 10; // 구독료(20)의 절반. 보상 ≥ 구독료면 매출 붕괴(스펙 §6)
export function computeStreak(dates: string[], today: string): number; // 순수. "YYYY-MM-DD"
export type AttendanceState = { checkedInToday: boolean; streak: number; daysThisSub: number | null; threshold: number };
export function getAttendanceState(userId: string, todayKst: string): Promise<AttendanceState>;
export function recordCheckin(userId: string, todayKst: string): Promise<AttendanceState>;
export function grantDueReward(userId: string): Promise<void>;

// lib/byeolmaru/crosssell.ts
export type CrossSell = { product: "saju_report" | "tarot"; title: string; desc: string; href: string };
export function pickCrossSell(cell: DayCell): CrossSell; // 순수. 오늘 축 기반
```
`todayKst` 는 `kstDate(new Date().toISOString())`(calendar 라우트와 동일). `DayCell` 은 `lib/byeolmaru/calendar.ts`.

---

## Phase A — 데이터 & 로직

### Task 1: 마이그레이션 — 출석 테이블

**Files:** Create `supabase/migrations/20260904200000_byeolmaru_checkins.sql`

- [ ] **Step 1: 작성** (`reward_granted_at` 컬럼은 ②-a `20260904100000` 에 이미 있음 — 여기선 안 만든다. `ui_events`(`20260731070000`) 의 RLS/GRANT 관례. 복합 PK 라 시퀀스 grant 불필요.)

```sql
-- 20260904200000_byeolmaru_checkins.sql — 별마루 출석(하루 1행). 습관 스트릭(전 유저) + 구독 보상 카운트.
-- 스펙: docs/superpowers/specs/2026-09-04-별마루-2-구독게이트-design.md §6·§8
CREATE TABLE IF NOT EXISTS byeolmaru_checkins (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  checkin_date DATE NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, checkin_date)
);
CREATE INDEX IF NOT EXISTS idx_byeolmaru_checkins_user ON byeolmaru_checkins(user_id, checkin_date DESC);

ALTER TABLE byeolmaru_checkins ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE byeolmaru_checkins FROM PUBLIC, anon, authenticated;
GRANT ALL ON TABLE byeolmaru_checkins TO service_role;
```

- [ ] **Step 2: 커밋** (SQL 파일만, tsc 무관, push·로컬적용 안 함 — dev push 시 자동)
```bash
git add supabase/migrations/20260904200000_byeolmaru_checkins.sql
git commit -m "feat(byeolmaru): 출석 체크인 테이블 (②-b Task1)"
```

---

### Task 2: 출석 로직 모듈 + 테스트

**Files:** Create `lib/byeolmaru/attendance.ts`, `lib/byeolmaru/attendance.test.ts`

- [ ] **Step 1: 실패 테스트** (`computeStreak` 순수 로직만 — DB 함수는 Task 3 라우트에서 통합 검증)

```ts
// lib/byeolmaru/attendance.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { computeStreak, ATTENDANCE_THRESHOLD, ATTENDANCE_REWARD_STARS } from "./attendance.ts";

test("오늘 포함 연속 3일이면 streak=3", () => {
  assert.equal(computeStreak(["2026-09-02", "2026-09-03", "2026-09-04"], "2026-09-04"), 3);
});
test("오늘 미출석이면 어제까지의 연속 run 을 센다(오늘 채우면 이어질 값)", () => {
  assert.equal(computeStreak(["2026-09-02", "2026-09-03"], "2026-09-04"), 2);
});
test("어제도 오늘도 없으면 0", () => {
  assert.equal(computeStreak(["2026-09-01"], "2026-09-04"), 0);
});
test("중간에 끊기면 최근 run 만", () => {
  assert.equal(computeStreak(["2026-08-30", "2026-09-03", "2026-09-04"], "2026-09-04"), 2);
});
test("빈 기록이면 0", () => {
  assert.equal(computeStreak([], "2026-09-04"), 0);
});
test("보상 상수 — 구독료(20)보다 작다(매출 붕괴 방지)", () => {
  assert.equal(ATTENDANCE_THRESHOLD, 20);
  assert.ok(ATTENDANCE_REWARD_STARS < 20, "보상은 구독료 미만");
});
```

- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/byeolmaru/attendance.test.ts` → FAIL

- [ ] **Step 3: 구현**

```ts
// lib/byeolmaru/attendance.ts — 별마루 출석: 순수 스트릭 + DB(체크인·상태·보상 lazy grant).
import { getServiceSupabase } from "@/lib/supabase";
import { chargeStars } from "@/lib/stars";

export const ATTENDANCE_THRESHOLD = 20;    // 구독 30일 중 20일 출석 → 보상
export const ATTENDANCE_REWARD_STARS = 10; // 구독료(20)의 절반. 보상 ≥ 구독료면 구독이 무료가 됨(스펙 §6)

export type AttendanceState = {
  checkedInToday: boolean;
  streak: number;
  daysThisSub: number | null; // 구독자만 — 활성 구독 창 내 출석일수. 비구독자 null.
  threshold: number;
};

/** "YYYY-MM-DD" 문자열 하루 단위 계산 — UTC 자정 기준(로컬 TZ 무관). */
function addDays(iso: string, delta: number): string {
  const d = new Date(iso + "T00:00:00Z");
  d.setUTCDate(d.getUTCDate() + delta);
  return d.toISOString().slice(0, 10);
}

/** 오늘(또는 오늘 미출석 시 어제)에서 뒤로 이어지는 연속 출석일 수. 순수. */
export function computeStreak(dates: string[], today: string): number {
  const set = new Set(dates);
  let cursor = set.has(today) ? today : addDays(today, -1);
  let streak = 0;
  while (set.has(cursor)) {
    streak++;
    cursor = addDays(cursor, -1);
  }
  return streak;
}

/** 활성 구독(expires_at > now, 최신) 1건. 없으면 null. */
async function activeSub(userId: string): Promise<{ started_at: string; expires_at: string } | null> {
  const supa = getServiceSupabase();
  const { data } = await supa
    .from("byeolmaru_subscriptions")
    .select("started_at, expires_at")
    .eq("user_id", userId)
    .gt("expires_at", new Date().toISOString())
    .order("expires_at", { ascending: false })
    .limit(1);
  return data?.[0] ?? null;
}

export async function getAttendanceState(userId: string, todayKst: string): Promise<AttendanceState> {
  const supa = getServiceSupabase();
  // 최근 60일 체크인(스트릭 계산엔 충분 — 60일 연속 이상은 표시상 의미 없음).
  const since = addDays(todayKst, -60);
  const { data: rows } = await supa
    .from("byeolmaru_checkins")
    .select("checkin_date")
    .eq("user_id", userId)
    .gte("checkin_date", since);
  const dates = (rows ?? []).map((r) => r.checkin_date as string);
  const streak = computeStreak(dates, todayKst);
  const checkedInToday = dates.includes(todayKst);

  const sub = await activeSub(userId);
  let daysThisSub: number | null = null;
  if (sub) {
    const startDate = sub.started_at.slice(0, 10);
    const { count } = await supa
      .from("byeolmaru_checkins")
      .select("checkin_date", { count: "exact", head: true })
      .eq("user_id", userId)
      .gte("checkin_date", startDate)
      .lte("checkin_date", todayKst);
    daysThisSub = count ?? 0;
  }
  return { checkedInToday, streak, daysThisSub, threshold: ATTENDANCE_THRESHOLD };
}

/** 오늘 출석 기록(멱등 — 복합 PK 로 하루 1행). 기록 후 최신 상태 반환. */
export async function recordCheckin(userId: string, todayKst: string): Promise<AttendanceState> {
  const supa = getServiceSupabase();
  // upsert: 이미 있으면 무시(ignoreDuplicates). PK(user_id, checkin_date) 가 하루 1행 보장.
  await supa
    .from("byeolmaru_checkins")
    .upsert({ user_id: userId, checkin_date: todayKst }, { onConflict: "user_id,checkin_date", ignoreDuplicates: true });
  return getAttendanceState(userId, todayKst);
}

/**
 * 만료·미정산 구독의 출석 보상을 다음 방문에서 lazy 정산한다.
 * 창 내 출석 ≥ THRESHOLD 면 10별 지급(멱등키=구독 id). 지급 여부와 무관하게 reward_granted_at 을
 * 찍어 "정산됨"으로 표시(이후 재검사 안 함). 동시 호출은 chargeStars 멱등 + CAS 로 안전.
 */
export async function grantDueReward(userId: string): Promise<void> {
  const supa = getServiceSupabase();
  const nowIso = new Date().toISOString();
  const { data } = await supa
    .from("byeolmaru_subscriptions")
    .select("id, started_at, expires_at")
    .eq("user_id", userId)
    .lte("expires_at", nowIso)
    .is("reward_granted_at", null)
    .order("expires_at", { ascending: false })
    .limit(1);
  const sub = data?.[0];
  if (!sub) return;

  const startDate = (sub.started_at as string).slice(0, 10);
  const endDate = (sub.expires_at as string).slice(0, 10);
  const { count } = await supa
    .from("byeolmaru_checkins")
    .select("checkin_date", { count: "exact", head: true })
    .eq("user_id", userId)
    .gte("checkin_date", startDate)
    .lte("checkin_date", endDate);

  if ((count ?? 0) >= ATTENDANCE_THRESHOLD) {
    // 멱등: payment_id=구독 id 로 charge_stars 가 중복 지급을 막는다(TEXT, FK 없음).
    await chargeStars(userId, ATTENDANCE_REWARD_STARS, `byeolmaru_reward_${sub.id}`, "byeolmaru_attendance_reward");
  }
  // 정산 마킹(CAS) — 지급/미지급 공통. 이미 누가 찍었으면 no-op.
  await supa
    .from("byeolmaru_subscriptions")
    .update({ reward_granted_at: nowIso })
    .eq("id", sub.id)
    .is("reward_granted_at", null);
}
```

- [ ] **Step 4: 통과** — `node --import tsx --test lib/byeolmaru/attendance.test.ts` → PASS(6) · `npx tsc --noEmit` → 0
- [ ] **Step 5: 커밋**
```bash
git add lib/byeolmaru/attendance.ts lib/byeolmaru/attendance.test.ts
git commit -m "feat(byeolmaru): 출석 로직 — 스트릭·상태·보상 lazy grant (②-b Task2)"
```

---

## Phase B — API

### Task 3: 체크인 라우트 + 캘린더에 attendance/보상 배선

**Files:** Create `app/api/byeolmaru/checkin/route.ts`; Modify `app/api/byeolmaru/calendar/route.ts`

- [ ] **Step 1: checkin 라우트**
```ts
// app/api/byeolmaru/checkin/route.ts — 오늘 출석(하루 1회, 멱등). 로그인 유저면 누구나(습관은 전 유저).
import { NextResponse } from "next/server";
import { getSession } from "@/lib/session";
import { kstDate } from "@/lib/admin-time";
import { recordCheckin } from "@/lib/byeolmaru/attendance";

export const runtime = "nodejs";

export async function POST() {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "login_required" }, { status: 401 });
  const attendance = await recordCheckin(userId, kstDate(new Date().toISOString()));
  return NextResponse.json({ ok: true, attendance });
}
```

- [ ] **Step 2: calendar 라우트에 attendance + grantDueReward 추가**

`app/api/byeolmaru/calendar/route.ts` 상단 import 에 추가:
```ts
import { getAttendanceState, grantDueReward } from "@/lib/byeolmaru/attendance";
```
`const ent = await getEntitlement(userId);` **다음**에 삽입(이미 `todayKst` 가 위에서 계산돼 있음):
```ts
    // 보상 정산(write)은 best-effort — 실패해도 캘린더는 떠야 한다(로그만 남기고 삼킨다).
    // grantDueReward 는 대개 no-op(만료·미정산 구독 없음). logError 는 calendar 라우트가 이미 import.
    try { await grantDueReward(userId); } catch (e) { await logError(e, { route: "/api/byeolmaru/calendar", userId, extra: { stage: "reward" } }); }
    const attendance = await getAttendanceState(userId, todayKst);
```
그리고 return 객체에 `attendance` 필드 추가:
```ts
    return NextResponse.json({
      today: todayKst,
      todayGanji: temporal.day.stem + temporal.day.branch,
      cells,
      weeks: weekBuckets(cells),
      entitled: ent.entitled,
      trialUsed: ent.trialUsed,
      subscriptionExpiresAt: ent.subscriptionExpiresAt,
      attendance,   // 추가
    });
```
(기존 try/catch·guards·`is_primary:true` 불변. `grantDueReward` 는 이미 try 안이라 throw 시 500+logError 로 흡수됨.)

- [ ] **Step 3: tsc** — `npx tsc --noEmit` → 0. (라이브는 Task 4 후 브라우저.)
- [ ] **Step 4: 커밋**
```bash
git add app/api/byeolmaru/checkin/route.ts app/api/byeolmaru/calendar/route.ts
git commit -m "feat(byeolmaru): 체크인 라우트 + 캘린더 attendance·보상 정산 (②-b Task3)"
```

---

## Phase C — 프론트(출석)

### Task 4: AttendanceStrip + ByeolmaruView 배선

**Files:** Create `components/byeolmaru/AttendanceStrip.tsx`; Modify `components/byeolmaru/ByeolmaruView.tsx`

- [ ] **Step 1: AttendanceStrip 컴포넌트** (순수 프레젠테이션 — 상태·핸들러는 ByeolmaruView. 카드 톤은 PartnerSlot 과 통일.)
```tsx
// components/byeolmaru/AttendanceStrip.tsx — 출석 체크인 + 스트릭(전 유저 습관). 보상 진행은 구독자만.
"use client";
import type { AttendanceState } from "@/lib/byeolmaru/attendance";

interface Props {
  attendance: AttendanceState | null;
  loading: boolean;
  onCheckin: () => void;
}

export default function AttendanceStrip({ attendance, loading, onCheckin }: Props) {
  if (!attendance) return null;
  const { checkedInToday, streak, daysThisSub, threshold } = attendance;
  return (
    <section className="flex items-center justify-between rounded-2xl bg-cream-warm px-4 py-3">
      <div>
        <p className="text-sm font-medium text-eye-purple">
          {checkedInToday ? `오늘 출석 완료 · ${streak}일째` : "오늘 아직 출석 안 했어"}
        </p>
        {daysThisSub !== null && (
          <p className="mt-0.5 text-xs text-text-light">
            이번 구독 {daysThisSub}/{threshold}일 · {threshold}일 채우면 10별
          </p>
        )}
      </div>
      {!checkedInToday && (
        <button
          onClick={onCheckin}
          disabled={loading}
          className="rounded-xl bg-gold px-4 py-2 text-sm font-medium text-eye-purple disabled:opacity-60"
        >
          출석하기
        </button>
      )}
    </section>
  );
}
```

- [ ] **Step 2: ByeolmaruView 배선**

Import 추가: `import AttendanceStrip from "./AttendanceStrip";` `import type { AttendanceState } from "@/lib/byeolmaru/attendance";`
`CalendarResponse` 에 `attendance: AttendanceState;` 추가.
상태 추가: `const [checkinLoading, setCheckinLoading] = useState(false);` 그리고 attendance 는 `data.attendance` 로 렌더하되, 체크인 후 갱신을 위해 로컬 상태로도 들고 간다:
```ts
const [attendance, setAttendance] = useState<AttendanceState | null>(null);
```
`refresh()` 의 `setState({ kind: "ready", data })` 직후에 `setAttendance(data.attendance);` 추가(캘린더가 attendance 를 실어 오므로 새로고침마다 동기화).
핸들러:
```ts
async function handleCheckin() {
  trackUiEvent("byeolmaru_checkin", { meta: { streak: attendance?.streak ?? 0 } });
  setCheckinLoading(true);
  try {
    const r = await fetch("/api/byeolmaru/checkin", { method: "POST" });
    const j = await r.json();
    if (j.attendance) setAttendance(j.attendance);
  } finally {
    setCheckinLoading(false);
  }
}
```
렌더: `<header>...</header>` **다음**(캘린더 `<section aria-label="30일 캘린더">` 앞)에 삽입:
```tsx
<AttendanceStrip attendance={attendance} loading={checkinLoading} onCheckin={handleCheckin} />
```
> ⚠️ `byeolmaru_checkin` 은 Task 7 에서 allowlist 에 추가됨 — 그 전엔 tsc 가 그 문자열에서 에러. Task 7 을 이 태스크와 함께(또는 먼저) 처리하면 tsc 그린. 구현 순서상 Task 7 을 앞당겨도 됨(컨트롤러 판단). 캐스팅으로 우회 금지.

- [ ] **Step 3: tsc + 브라우저** — `npx tsc --noEmit` → 0(단 Task7 allowlist 선반영 시). dev 서버 + 세션 주입(프로필 유저)으로 `/byeolmaru`: 출석 스트립 렌더 → "출석하기" 클릭 → "오늘 출석 완료 · 1일째" 전환 확인.
- [ ] **Step 4: 커밋**
```bash
git add components/byeolmaru/AttendanceStrip.tsx components/byeolmaru/ByeolmaruView.tsx
git commit -m "feat(byeolmaru): 출석 스트립 UI + 체크인 배선 (②-b Task4)"
```

---

## Phase D — 크로스셀

### Task 5: 크로스셀 추천 룰 + 테스트

**Files:** Create `lib/byeolmaru/crosssell.ts`, `lib/byeolmaru/crosssell.test.ts`

- [ ] **Step 1: 실패 테스트**
```ts
// lib/byeolmaru/crosssell.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { pickCrossSell } from "./crosssell.ts";
import type { DayCell } from "./calendar.ts";

function cell(love: number, money: number, work: number): DayCell {
  return { date: "2026-09-04", ganji: "辛巳", element: "금",
    grade: { label: "무난한 날", tone: "normal" }, axes: { love, money, work },
    score: 60, isToday: true } as unknown as DayCell;
}

test("연애 축이 최고면 타로 추천", () => {
  const r = pickCrossSell(cell(80, 40, 30));
  assert.equal(r.product, "tarot");
  assert.equal(r.href, "/");
});
test("연애가 최고가 아니면 사주 리포트", () => {
  const r = pickCrossSell(cell(20, 70, 40));
  assert.equal(r.product, "saju_report");
  assert.equal(r.href, "/fortune");
});
test("항상 title·desc 가 채워진다", () => {
  const r = pickCrossSell(cell(50, 50, 50));
  assert.ok(r.title.length > 0 && r.desc.length > 0);
});
```

- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/byeolmaru/crosssell.test.ts` → FAIL

- [ ] **Step 3: 구현** (스펙 §7·§12 — 초기엔 사주 리포트·타로 2종. "미경험 상품 제외"·시즌(신년운세)은 후속. 축 기반 룰.)
```ts
// lib/byeolmaru/crosssell.ts — 별마루 하단 자사 크로스셀 추천(룰, 오늘 축 기반). 3rd-party 광고 아님.
import type { DayCell } from "./calendar.ts";

export type CrossSell = { product: "saju_report" | "tarot"; title: string; desc: string; href: string };

const TAROT: CrossSell = {
  product: "tarot", href: "/",
  title: "오늘 이 흐름, 타로로 더 깊게",
  desc: "마음이 복잡한 하루엔 카드 한 장이 방향을 잡아줘.",
};
const SAJU: CrossSell = {
  product: "saju_report", href: "/fortune",
  title: "네 사주로 더 길게 보기",
  desc: "오늘 하루 너머, 이번 달·올해 흐름까지 풀어줄게.",
};

/** 연애 축이 최고인 날 → 타로(연애 결), 그 외 → 사주 리포트. 동점은 사주로. */
export function pickCrossSell(cell: DayCell): CrossSell {
  const { love, money, work } = cell.axes;
  return love > money && love > work ? TAROT : SAJU;
}
```

- [ ] **Step 4: 통과** — 테스트 PASS(3) · `npx tsc --noEmit` → 0
- [ ] **Step 5: 커밋**
```bash
git add lib/byeolmaru/crosssell.ts lib/byeolmaru/crosssell.test.ts
git commit -m "feat(byeolmaru): 크로스셀 추천 룰 (②-b Task5)"
```

---

### Task 6: CrossSellCard + 배치

**Files:** Create `components/byeolmaru/CrossSellCard.tsx`; Modify `components/byeolmaru/ByeolmaruView.tsx`

- [ ] **Step 1: CrossSellCard** (PartnerSlot 톤. 클릭 계측은 Task 7 allowlist.)
```tsx
// components/byeolmaru/CrossSellCard.tsx — 별콩이 추천(자사 크로스셀). 하단 브라우즈 존.
"use client";
import Link from "next/link";
import type { CrossSell } from "@/lib/byeolmaru/crosssell";
import { trackUiEvent } from "@/lib/analytics/ui-events";

export default function CrossSellCard({ item }: { item: CrossSell }) {
  return (
    <section className="rounded-2xl bg-cream-warm p-4">
      <div className="mb-1 flex items-center gap-1 text-xs text-lilac-deep">
        <span aria-hidden>✨</span> 별콩이 추천
      </div>
      <h2 className="mb-1 font-display text-base text-eye-purple">{item.title}</h2>
      <p className="mb-3 text-sm text-text-light">{item.desc}</p>
      <Link
        href={item.href}
        onClick={() => trackUiEvent("byeolmaru_crosssell_clicked", { meta: { product: item.product } })}
        className="inline-block rounded-xl bg-lilac-deep px-4 py-2 text-sm text-cream"
      >
        보러 가기
      </Link>
    </section>
  );
}
```

- [ ] **Step 2: ByeolmaruView 배치**

Import: `import CrossSellCard from "./CrossSellCard";` `import { pickCrossSell } from "@/lib/byeolmaru/crosssell";`
`ready` 렌더의 `const cell = ...` 아래에 `const crossSell = pickCrossSell(cell);` (cell 은 이미 계산돼 있음).
렌더: "앞으로 30일 흐름" `</section>` **다음**, `<PartnerSlot />` **앞**에 삽입:
```tsx
<CrossSellCard item={crossSell} />
```
> ⚠️ `byeolmaru_crosssell_clicked` 도 Task 7 allowlist 대상 — 동일 주의.

- [ ] **Step 3: tsc + 브라우저** — tsc 0(Task7 선반영 시). `/byeolmaru` 하단에 추천 카드가 30일 흐름과 우리 사이 슬롯 사이에 렌더되는지 확인.
- [ ] **Step 4: 커밋**
```bash
git add components/byeolmaru/CrossSellCard.tsx components/byeolmaru/ByeolmaruView.tsx
git commit -m "feat(byeolmaru): 크로스셀 추천 카드 + 배치 (②-b Task6)"
```

---

## Phase E — 계측

### Task 7: 계측 이벤트 2종 allowlist

**Files:** Modify `lib/analytics/ui-events.ts`, `lib/analytics/ui-events.test.ts`
> 🔴 구현 순서: 이 태스크를 **Task 4 이전에** 처리하면 Task 4·6 의 tsc 가 바로 그린이 된다(`trackUiEvent` 인자는 `UiEvent` 유니온이라 미등록 문자열은 tsc 에러). 컨트롤러가 Task 4 앞에 끼워 실행 권장.

- [ ] **Step 1: 실패 테스트** — `lib/analytics/ui-events.test.ts` 에 추가:
```ts
test("UI_EVENTS — 별마루 ②-b 이벤트가 등록돼 있다", () => {
  assert.equal(isUiEvent("byeolmaru_checkin"), true);
  assert.equal(isUiEvent("byeolmaru_crosssell_clicked"), true);
});
```
- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/analytics/ui-events.test.ts` → FAIL
- [ ] **Step 3: allowlist 추가** — `UI_EVENTS` 배열 끝(`byeolmaru_subscribe_completed` 다음)에:
```ts
  /** 별마루 — 출석 체크인. meta:{streak} */
  "byeolmaru_checkin",
  /** 별마루 — 크로스셀 추천 카드 클릭. meta:{product} */
  "byeolmaru_crosssell_clicked",
```
- [ ] **Step 4: 통과** — 해당 테스트 PASS · `npx tsc --noEmit` → 0 · 전체 `node --import tsx --test lib/byeolmaru lib/analytics/ui-events.test.ts`(글로브 대신 파일 지정) 회귀 0
- [ ] **Step 5: 커밋**
```bash
git add lib/analytics/ui-events.ts lib/analytics/ui-events.test.ts
git commit -m "feat(byeolmaru): ②-b 계측 이벤트 2종 allowlist (②-b Task7)"
```

---

## 완료 기준 (라이브 검증)

dev push 후(마이그 `20260904200000` 자동 적용 확인):
1. 프로필 유저 `/byeolmaru` → 출석 스트립 "오늘 아직 출석 안 했어" + "출석하기" → 클릭 → "오늘 출석 완료 · 1일째", `byeolmaru_checkin` insert. 재클릭 불가(버튼 사라짐).
2. 하단에 별콩이 추천 카드(오늘 축 기준 타로/사주) 렌더 · 클릭 시 `byeolmaru_crosssell_clicked`.
3. 구독자면 스트립에 "이번 구독 N/20일" 표기.
4. 보상 lazy grant: 만료·20일+출석 구독을 테스트 세팅 → 다음 방문 시 `star_transactions`(source=`byeolmaru_attendance_reward`) 1행 + 잔액 +10 + `reward_granted_at` 세팅. 재방문 시 중복 지급 없음(멱등).
5. ACL: `byeolmaru_checkins` anon 접근 불가(카나리아).
6. 회귀: ②-a 게이트/체험/구독 정상 · tsc 0 · 전체 유닛 GREEN.

---

## 미결 / 후속(비블로커)
- 크로스셀 "이미 구매한 상품 제외" + 시즌(신년운세) 로테이션 — 스펙 §7·§12, v1 은 축 기반 2종만.
- 스트릭 표시가 `computeStreak`(연속)인데 보상은 누적(daysThisSub) — 의도된 이중 지표(습관 표시 vs 보상 카운트). 혼동되면 카피 조정.
