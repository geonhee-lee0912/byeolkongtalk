# 별마루 P5-4 — 페이월·구독 시트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루의 페이월을 **한 곳에 모인 잠금**에서 **각 콘텐츠 옆의 맥락형 미끼 3자리**로 바꾼다 — 자물쇠를 없애고, "길다·정확하다"를 말이 아니라 증거(분량 칩·구성·이어받는 첫 줄)로 보여주고, 거절하면 그날은 접는다.

**Architecture:** `PremiumBlock` 이 지금 **자격자의 서술 표시**와 **비자격자의 미끼**를 겸하는데, P5-4 는 **비자격자 쪽만** `slot` 기반으로 재작성한다(자격자 경로는 무회귀). 자리별 카피는 순수 모듈 `lib/byeolmaru/bait.ts` 에 모으고, "첫 줄"은 **LLM 이 아니라 룰**로 만든다 — `daily-report` 라우트가 비자격자에게 403 이라 실제 리포트를 가져올 수 없고, 스펙이 "위 무료 요약을 **이어받아** 쓴다"고 명시하기 때문이다(원가 0).

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript strict · Tailwind v4 `@theme` · `node --test`(tsx) · localStorage(당일 접힘)

---

## 🔴 이 플랜을 새 세션에서 시작할 때 (인수인계)

**작성 시점: 2026-09-13.** P5-1·P5-2·P5-3 이 dev 로컬에 끝나 있고 **origin/dev 에 미푸시**다.

### 먼저 읽을 것
- 메모리 `byeolmaru-p5-spec` — P5-1~3 완료 상태와 **교훈 8개**. 특히 "범위를 바꾸면 그걸 전제한 계산·문구가 조용히 틀린다"
- 스펙 `docs/superpowers/specs/2026-09-12-별마루-p5-화면개편-design.md` **§9(페이월)·§13(계측)·§11(재화)** — Grep 으로 해당 절만(긴 문서다)
- 직전 플랜 `docs/superpowers/plans/2026-09-13-별마루-p5-3-허브-재조립.md` — 지금 화면이 어떤 모양인지

### 현재 화면 (P5-3 결과)
```
/byeolmaru 허브
  header · 달력 판(출석 스트립 · 오늘 히어로 골드 타일 · 인연 칩 · 격자 · ★미끼 자리★)
  · 무료 목록 3종 · 낙수
/byeolmaru/saju   격자 + 상세(무료 taste) + ★미끼 또는 리포트★ + 주차 요약
/byeolmaru/tarot  오늘의 카드(뽑기·정적 taste·★미끼 또는 LLM 서술★·낙수)
/byeolmaru/woori  인연 칩 + 격자 + 상세(무료 한 줄 또는 LLM 서술)
```

### 🔴 이 리포의 함정 (실제로 물린 것들)
- **다른 Claude 세션이 같은 브랜치에 동시에 커밋한다.** `git add` 로 스테이징하면 그 세션 커밋에 섞여 들어간 사고가 실제로 났다 → **pathspec 커밋만**(`git commit <경로> -m "..."`), 커밋 직후 `git show --stat HEAD` 확인
- **테스트 명령에 `-not -path "./.claude/*"` 를 반드시 붙인다** — 다른 세션들의 격리 워크트리가 무관한 실패를 대량으로 만든다
- **브라우저 쿠키가 오리진 단위로 탭 간 공유된다** — 세션 주입 검증 중 **사용자 탭 로그인을 덮어쓴 사고**가 났다. 주입 전 기존 값을 캡처하고 끝나면 복원할 것
- **`npm run dev` 를 Bash 로 돌리지 마라** — dev 서버가 이미 떠 있고 다른 세션의 활성 탭이 있을 수 있다. 재시작 금지
- dev DB 테스트 데이터: QA봇(`11111111-1111-4111-8111-111111111111`)에 생년월일 프로필 + 상대 "테스트상대"가 등록돼 있다(비구독 무료 상태 — 미끼 검증에 그대로 쓸 수 있다)

### 결정된 것 (다시 묻지 말 것)
- **§8 "우리 셀에 ✨끌림 🔗결속 태그"는 P5-5 로** (사용자 확인 2026-09-13)
- **구 게스트 5카드 계측 소실은 그대로 둔다** (새 설계에선 측정 의미가 없다)
- **웰컴 20→15 집행 완료** (`3b4a789`)

### P5-4 가 반드시 해소해야 할 것 (최종 리뷰가 남긴 것)
- 🔴 **지금 비자격자 허브 미끼가 `teaser={null}` 이라 "이어서 별콩이가 네 월·시 기둥까지…" 라는 앞 문장 없는 조각**으로 시작한다. T2·T4 가 이걸 없앤다
- 🔴 **인연 탭 미끼 자리가 없다** — 우리 모드에서도 나-탭 블록이 그대로 뜬다. T4 가 `isPair` 로 가른다
- 같은 `PremiumBlock` 이 허브와 `/byeolmaru/saju` 두 곳에 뜬다 — **자리별로 다른 내용**이 되면 중복이 해소된다

---

## 스코프

**건드리는 것:** `components/byeolmaru/PremiumBlock.tsx` · `useByeolmaruSubscribe.tsx` · `ByeolmaruHub.tsx` · `SajuTodayView.tsx` · `DailyCardBlock.tsx` · `lib/byeolmaru/bait.ts`(신규) · `lib/analytics/ui-events.ts`

**🚫 건드리지 않는 것:**
- 판정 엔진 · 무료선(`splitByFreeLine`) · 달력 범위 — P5-2 결과
- 달력 판 비주얼 · 히어로 · 무료 목록 · 인연 칩 — P5-3 결과
- `app/fortune/*` · `components/fortune/*` · 홈 · 마이 (스펙 스코프)
- **우리 오늘 분량**(무료 350자/유료 1,200자)과 `pair-narrative` 캐시 — **P5-5**
- `/byeolmaru/woori` 화면 자체 — 거기 미끼는 이미 `PairDayDetailCard` 가 갖고 있다. P5-4 는 **허브의 인연 탭 자리**만 만든다

## 사전 지식

- 테스트 `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")` · 타입체크 `npx tsc --noEmit` · 빌드 `npm run build`
- 디자인 토큰은 `app/globals.css` `@theme` 이 정본. **새 토큰을 만들지 않는다**
- 🔴 **새 배경색 위에 텍스트를 얹으면 WCAG 대비를 계산하라.** P5-3 에서 세 번 걸렸다(2.78 / 3.26 / **1.39**:1). 작은 텍스트 4.5:1, 반투명은 뒤 배경과 합성해 계산
- 별콩이 화법: 반말(해체) · 따뜻함 · **단정적 예언 금지** · 불안 자극·운명론적 협박 금지
- 주석은 "왜"를 남기는 밀도 높은 한글. 커밋 메시지도 한글

## 현재 페이월 코드

`PremiumBlock` 비자격 분기(**이걸 바꾼다**):
```tsx
<section className="rounded-2xl bg-[#F3EEFB] p-4">
  <div className="mb-1 flex items-center gap-1 text-xs text-lilac-deep"><span aria-hidden>🔒</span> 별콩이의 개인화</div>
  {teaser && <p ...>{teaser}</p>}
  <p className="... [mask-image:linear-gradient(#000,transparent)] opacity-60">
    이어서 별콩이가 네 월·시 기둥까지 겹쳐서 오늘 누구와…
  </p>
  {!trialUsed ? <button ...>3일 무료 체험 시작</button>
             : <button ...>구독하고 매일 보기 · 20별 / 30일</button>}
</section>
```

호출부 3곳: `ByeolmaruHub.tsx`(`teaser={null}`) · `SajuTodayView.tsx`(`teaser={null}`) · `DailyCardBlock.tsx`(자체 블러 블록, `PremiumBlock` 미사용)

구독 시트는 `useByeolmaruSubscribe` 가 `StarConfirmModal` 을 `title="별마루 구독"` · `subtitle="30일 동안 매일 개인화를 열어둬"` 로 띄운다.

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `lib/byeolmaru/bait.ts` (신규) | 자리 3종의 카피 뱅크 + **이어받는 첫 줄** 룰 함수. 순수 | T1 |
| `lib/byeolmaru/bait.test.ts` (신규) | 카피 규율(화법·자리 누락·첫 줄이 맥락을 이어받는가) | T1 |
| `components/byeolmaru/PremiumBlock.tsx` (수정) | 비자격 분기를 slot 기반으로. 자물쇠 제거·배지·CTA·가격 2단·계측 | T2 |
| `components/byeolmaru/useBaitDismiss.ts` (신규) | 당일 접힘 — slot별 localStorage | T3 |
| `components/byeolmaru/ByeolmaruHub.tsx` (수정) | 나/인연 자리 분기 배선 | T4 |
| `components/byeolmaru/SajuTodayView.tsx` (수정) | `saju_report` 자리 + 실제 무료 요약 이어받기 | T5 |
| `components/byeolmaru/DailyCardBlock.tsx` (수정) | 자체 블러 블록을 `PremiumBlock`(`tarot_rich`)로 교체 | T6 |
| `components/byeolmaru/useByeolmaruSubscribe.tsx` (수정) | 구독 시트 — 가치 3종 + 가격 크게 + "체험 중엔 별이 안 나가" | T7 |
| `lib/analytics/ui-events.ts` (수정) | `meta.slot` 계약 + `byeolmaru_gate_dismissed` 신설 | T2·T3 |

---

### Task 1: 미끼 카피 뱅크

스펙 §9 의 "증거로 보여준다"를 데이터로 만든다. **LLM 을 쓰지 않는다** — `daily-report` 는 비자격자에게 403 이고, 스펙이 "첫 줄은 위 무료 요약을 **이어받아** 쓴다"고 했다.

**Files:** Create `lib/byeolmaru/bait.ts` · `lib/byeolmaru/bait.test.ts`

- [ ] **Step 1: 실패 테스트를 쓴다**

Create: `lib/byeolmaru/bait.test.ts`

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { BAIT, BAIT_SLOTS, baitLead, type BaitSlot } from "./bait.ts";

test("BAIT — 자리 3종이 전부 있고 필드가 채워져 있다", () => {
  assert.equal(BAIT_SLOTS.length, 3);
  for (const slot of BAIT_SLOTS) {
    const b = BAIT[slot];
    assert.ok(b.title.length > 0, `${slot} title 없음`);
    assert.ok(b.chips.length >= 1, `${slot} chips 없음`);
    assert.ok(b.tail.length > 0, `${slot} tail 없음`);
  }
});

test("BAIT — 자물쇠·잠금 어휘를 쓰지 않는다(스펙 §9: 잠긴 게 아니라 아직 안 온 것)", () => {
  for (const slot of BAIT_SLOTS) {
    const all = [BAIT[slot].title, BAIT[slot].tail, ...BAIT[slot].chips].join(" ");
    assert.ok(!/🔒|잠금|잠긴|잠겨/.test(all), `${slot} 에 잠금 어휘가 있다: ${all}`);
  }
});

test("BAIT — 별콩이 화법: 존댓말·문어체 종결을 쓰지 않는다", () => {
  for (const slot of BAIT_SLOTS) {
    for (const line of [BAIT[slot].title, BAIT[slot].tail]) {
      assert.ok(!/[다요]\.?$/.test(line.trim()), `${slot} 이 반말 종결이 아니다: ${line}`);
    }
  }
});

test("baitLead — saju_report 는 화면에 뜬 등급 라벨을 이어받는다", () => {
  const lead = baitLead("saju_report", { gradeLabel: "잘 맞는 날" });
  assert.ok(lead.includes("잘 맞는 날"), `등급을 안 이어받았다: ${lead}`);
});

test("baitLead — 등급이 없으면 등급 없이도 말이 되는 문장을 준다", () => {
  const lead = baitLead("saju_report", {});
  assert.ok(lead.length > 0);
  assert.ok(!lead.includes("undefined"), `폴백에 undefined 가 샜다: ${lead}`);
});

test("baitLead — 세 자리 모두 문장을 준다", () => {
  for (const slot of BAIT_SLOTS) {
    const lead = baitLead(slot, { gradeLabel: "무난한 날", partnerName: "지민" });
    assert.ok(lead.length > 0, `${slot} lead 없음`);
    assert.ok(!lead.includes("undefined"), `${slot} 에 undefined: ${lead}`);
  }
});

test("baitLead — woori_30d 는 상대 이름을 쓰되 없으면 대명사로 흘린다", () => {
  assert.ok(baitLead("woori_30d", { partnerName: "지민" }).includes("지민"));
  const noName = baitLead("woori_30d", {});
  assert.ok(!noName.includes("undefined") && noName.length > 0);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `node --import tsx --test lib/byeolmaru/bait.test.ts`
Expected: FAIL — 모듈이 없다

- [ ] **Step 3: 카피 뱅크를 만든다**

Create: `lib/byeolmaru/bait.ts`

```ts
// lib/byeolmaru/bait.ts — 페이월 미끼 카피(스펙 §9). 순수(LLM·네트워크 0).
// 🔴 "첫 줄"을 LLM 으로 만들지 않는다. /api/byeolmaru/daily-report 는 비자격자에게 403 이라
//    실제 리포트를 가져올 수 없고(원가 경계), 스펙이 "첫 줄은 위 무료 요약을 **이어받아** 쓴다"고
//    명시했다. 그래서 화면에 이미 떠 있는 것(등급 라벨·상대 이름)을 받아 룰로 잇는다 — 원가 0.
// 🔴 자물쇠를 쓰지 않는다. 유료라는 사실은 "구독" 배지가 남기고, 문장은 초대여야 한다(스펙 §9 톤).

export const BAIT_SLOTS = ["saju_report", "woori_30d", "tarot_rich"] as const;
export type BaitSlot = (typeof BAIT_SLOTS)[number];

export interface BaitCopy {
  /** 자물쇠 대신 오는 초대 한 줄. */
  title: string;
  /** "길다·정확하다"를 말이 아니라 증거로 — 분량과 구성(2탭 FORTUNE_LENGTH_HINT 관행 재사용). */
  chips: string[];
  /** 첫 줄 뒤에 이어지다 페이드로 흐려지는 꼬리. 끝을 맺지 않는다(— 로 끊는다). */
  tail: string;
}

export const BAIT: Record<BaitSlot, BaitCopy> = {
  saju_report: {
    title: "별콩이가 더 깊이 읽어줄게",
    chips: ["약 2,100자", "재물 · 직장 · 애정 · 건강 · 학업"],
    tail: "네 월·시 기둥까지 겹쳐서 오늘 어디에 힘을 실으면 좋을지—",
  },
  woori_30d: {
    title: "둘 사이를 매일 짚어줄게",
    chips: ["이번 달 전체", "끌림 · 결속 · 삐걱"],
    tail: "언제 마음이 가까워지고 언제 한 박자 쉬면 좋은지, 날짜로—",
  },
  tarot_rich: {
    title: "이 카드를 네 사주에 얹어줄게",
    chips: ["약 800자", "카드 × 네 사주"],
    tail: "같은 카드도 사람마다 다르게 내려앉는데, 너한테는—",
  },
};

/** 첫 줄이 받을 맥락 — 화면에 **이미 떠 있는 것**만 넣는다(새로 계산하지 않는다). */
export interface BaitContext {
  /** 오늘 등급 라벨("잘 맞는 날"…). 허브 히어로·오늘 사주 상세에 이미 보인다. */
  gradeLabel?: string;
  /** 인연 탭에서 고른 상대 이름. */
  partnerName?: string;
}

/**
 * 무료 요약을 이어받는 첫 줄(스펙 §9). 무료→유료가 **한 흐름**으로 읽히게 한다 —
 * 광고 문구가 아니라 방금 읽은 것의 다음 문장처럼.
 * 맥락이 없으면 그것 없이도 말이 되는 문장으로 떨어진다(undefined 가 새면 안 된다).
 */
export function baitLead(slot: BaitSlot, ctx: BaitContext): string {
  if (slot === "saju_report") {
    return ctx.gradeLabel
      ? `위에서 본 '${ctx.gradeLabel}'이 왜 그런지부터 풀어줄게.`
      : "오늘이 왜 이런 결인지부터 풀어줄게.";
  }
  if (slot === "woori_30d") {
    return ctx.partnerName
      ? `${ctx.partnerName}와 너, 오늘 왜 이런 결인지부터 짚어줄게.`
      : "둘이 오늘 왜 이런 결인지부터 짚어줄게.";
  }
  return "이 카드가 지금 네 흐름에 어떻게 걸리는지부터 말해줄게.";
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --import tsx --test lib/byeolmaru/bait.test.ts`
Expected: PASS (7개)

⚠️ `"별콩이가 더 깊이 읽어줄게"` 는 `줄게` 로 끝나 `[다요]` 규칙을 통과한다. 새 문구를 쓸 때도 그 규칙을 지킬 것.

- [ ] **Step 5: 타입체크 + 커밋**

Run: `npx tsc --noEmit`
Expected: 출력 없음

```bash
git add lib/byeolmaru/bait.ts lib/byeolmaru/bait.test.ts
git commit lib/byeolmaru/bait.ts lib/byeolmaru/bait.test.ts -m "feat(byeolmaru): 페이월 미끼 카피 뱅크 + 이어받는 첫 줄 (P5-4)"
git show --stat HEAD
```

---

### Task 2: `PremiumBlock` 을 slot 기반으로

스펙 §9 의 톤·CTA·가격 2단을 여기서 전부 바꾼다. **자격자 분기는 손대지 않는다**(무회귀).

**Files:** Modify `components/byeolmaru/PremiumBlock.tsx` · `lib/analytics/ui-events.ts`

- [ ] **Step 1: 계측 계약을 넓힌다**

`lib/analytics/ui-events.ts` 의 `byeolmaru_gate_shown` JSDoc 을 읽고, `meta.slot` 을 담도록 갱신하라(주변 형식을 그대로 따를 것). 예:

```ts
  /** 별마루 — 페이월 미끼 노출(마운트 1회). meta:{slot:"saju_report"|"woori_30d"|"tarot_rich"} */
  "byeolmaru_gate_shown",
```

그리고 `byeolmaru_trial_started`·`byeolmaru_subscribe_clicked` 의 JSDoc 에도 `slot?` 이 실릴 수 있음을 적어라(T4~T6 이 넘긴다).

- [ ] **Step 2: 비자격 분기를 교체한다**

`PremiumBlock` 의 `Props` 에 `slot` 과 `ctx` 를 더하고 `teaser` 를 **비자격 경로에서만** 대체한다:

```tsx
interface Props {
  entitled: boolean;
  trialUsed: boolean;
  narrative: string | null;   // 자격 && 생성 성공
  teaser: string | null;      // 자격 + 빈 서술 폴백의 첫 줄(자격자 경로 전용)
  loading: boolean;
  onStartTrial: () => void;
  onSubscribe: () => void;
  /** 🔴 P5-4 — 미끼는 자리마다 다른 물건이다(스펙 §9 "한 곳에 모으지 않는다"). */
  slot: BaitSlot;
  /** 첫 줄이 이어받을 맥락 — 화면에 이미 떠 있는 것만. */
  baitCtx?: BaitContext;
}
```

import 를 추가한다:

```tsx
import { BAIT, baitLead, type BaitSlot, type BaitContext } from "@/lib/byeolmaru/bait";
import { BYEOLMARU_SUBSCRIPTION } from "@/lib/byeolmaru/constants";
```

`useEffect` 의 계측에 slot 을 싣는다:

```tsx
  useEffect(() => {
    if (!entitled) trackUiEvent("byeolmaru_gate_shown", { meta: { slot } });
  }, [entitled, slot]);
```

그리고 **비자격 `return` 을 통째로** 아래로 교체한다:

```tsx
  const copy = BAIT[slot];
  const lead = baitLead(slot, baitCtx ?? {});
  return (
    <section className="rounded-2xl border border-lilac-mid/25 bg-white p-4 shadow-[0_2px_10px_rgba(159,138,208,0.08)]">
      {/* 🔴 자물쇠를 쓰지 않는다(스펙 §9) — 잠긴 게 아니라 "더 깊이 읽어주겠다"는 초대다.
          유료라는 사실은 사라지지 않고 작은 '구독' 배지가 명확히 남긴다. */}
      <div className="mb-1.5 flex items-center gap-1.5">
        <span className="font-display text-base text-eye-purple">{copy.title}</span>
        <span className="rounded-full bg-lilac-soft/70 px-1.5 py-0.5 text-[10px] font-bold text-lilac-deep">구독</span>
      </div>
      {/* 증거 — "길다·정확하다"를 말이 아니라 분량·구성으로 보여준다(2탭 분량 힌트 관행). */}
      <div className="mb-2 flex flex-wrap gap-1">
        {copy.chips.map((c) => (
          <span key={c} className="rounded-full bg-cream-warm px-2 py-0.5 text-[11px] font-medium text-text-light">
            {c}
          </span>
        ))}
      </div>
      {/* 첫 줄은 위 무료 요약을 이어받는다 → 무료→유료가 한 흐름으로 읽힌다. */}
      <p className="text-sm leading-relaxed text-eye-purple">{lead}</p>
      <p className="mt-0.5 text-sm leading-relaxed text-eye-purple opacity-60 [mask-image:linear-gradient(#000,transparent)]">
        {copy.tail}
      </p>
      {!trialUsed ? (
        <>
          {/* CTA 는 잠금이 아니라 **열쇠**다(스펙 §9). */}
          <button onClick={onStartTrial} disabled={loading}
            className="mt-3 w-full rounded-xl bg-gold py-2.5 text-sm font-bold text-night disabled:opacity-60">
            3일 무료로 열어보기
          </button>
          {/* 가격 노출 2단 — 여기선 작게, 구독 시트에서 크게(스펙 §9). */}
          <p className="mt-1.5 text-center text-[11px] text-text-light">
            체험 끝나면 {BYEOLMARU_SUBSCRIPTION.cost}별 / {BYEOLMARU_SUBSCRIPTION.days}일
          </p>
        </>
      ) : (
        <button onClick={onSubscribe} disabled={loading}
          className="mt-3 w-full rounded-xl bg-gold py-2.5 text-sm font-bold text-night disabled:opacity-60">
          구독하고 매일 보기 · {BYEOLMARU_SUBSCRIPTION.cost}별 / {BYEOLMARU_SUBSCRIPTION.days}일
        </button>
      )}
    </section>
  );
```

🔴 **CTA 텍스트를 `text-eye-purple`(#5A3E8C) 에서 `text-night`(#1F1735) 로 바꿨다** — 골드 `#E8C26A` 위에서 `eye-purple` 은 4.94:1 로 통과하지만, P5-3 에서 골드 배경 대비가 세 번 걸렸으므로 여유를 둔다. **직접 계산해 확인하고, 다르면 보고하라.**

- [ ] **Step 3: 타입체크로 호출부를 찾는다**

Run: `npx tsc --noEmit`
Expected: FAIL — `ByeolmaruHub.tsx` 와 `SajuTodayView.tsx` 에서 `slot` 누락 에러 2건. **정상이다**(T4·T5 가 고친다). 에러가 정확히 그 둘뿐인지 확인할 것

- [ ] **Step 4: 커밋**

```bash
git commit components/byeolmaru/PremiumBlock.tsx lib/analytics/ui-events.ts -m "feat(byeolmaru): 페이월 톤 교체 — 자물쇠 제거·증거 칩·열쇠 CTA·가격 2단 (P5-4)

호출부 배선은 다음 커밋 — 이 커밋 단독으로는 tsc 가 Hub·SajuTodayView 에서 실패한다."
git show --stat HEAD
```

---

### Task 3: 당일 접힘

스펙 §9 "거절하면 그날은 접힌다". **자리마다 다른 물건이므로 slot 별로** 접는다.

**Files:** Create `components/byeolmaru/useBaitDismiss.ts` · Modify `components/byeolmaru/PremiumBlock.tsx` · `lib/analytics/ui-events.ts`

- [ ] **Step 1: 훅을 만든다**

```tsx
"use client";

// components/byeolmaru/useBaitDismiss.ts — 미끼 당일 접힘(스펙 §9 "거절하면 그날은 접힌다").
// 🔴 slot 별로 접는다 — 자리마다 다른 물건이라 하나를 닫았다고 나머지까지 닫으면 과하다.
// 저장소는 localStorage: 이건 "이 사람 이 기기에서 오늘 한 번 거절했다"는 per-viewer 편의라
// 서버에 둘 값이 아니다. 쿠키·storage prefix 는 이 저장소 관례대로 byeolkong_.
// ⚠️ 사파리 프라이빗 등에서 접근 자체가 throw 할 수 있어 읽기·쓰기를 전부 try/catch 로 감싼다.
import { useEffect, useState } from "react";
import type { BaitSlot } from "@/lib/byeolmaru/bait";

const KEY = "byeolkong_bait_dismissed";

/** KST 오늘 "YYYY-MM-DD" — 서버를 안 부른다(접힘은 개인화가 아니라 편의라 기기 시각으로 충분). */
function todayKst(): string {
  const kst = new Date(Date.now() + 9 * 3600_000);
  return kst.toISOString().slice(0, 10);
}

function read(): Record<string, string> {
  try {
    const raw = localStorage.getItem(KEY);
    const parsed = raw ? JSON.parse(raw) : null;
    return parsed && typeof parsed === "object" ? (parsed as Record<string, string>) : {};
  } catch {
    return {};
  }
}

export function useBaitDismiss(slot: BaitSlot): { dismissed: boolean; dismiss: () => void } {
  // 🔴 초기값을 false 로 두고 effect 에서 읽는다 — localStorage 는 서버에 없어서, 초기 렌더에
  //    읽으면 하이드레이션 불일치가 난다.
  const [dismissed, setDismissed] = useState(false);

  useEffect(() => {
    setDismissed(read()[slot] === todayKst());
  }, [slot]);

  function dismiss() {
    setDismissed(true);
    try {
      localStorage.setItem(KEY, JSON.stringify({ ...read(), [slot]: todayKst() }));
    } catch {
      /* 저장에 실패해도 이번 세션 동안은 접힌 상태가 유지된다 — 그걸로 충분하다 */
    }
  }

  return { dismissed, dismiss };
}
```

- [ ] **Step 2: 계측 이벤트를 등록한다**

`lib/analytics/ui-events.ts` 의 `UI_EVENTS` 에 추가(주변 JSDoc 형식을 따를 것):

```ts
  /** 별마루 — 페이월 미끼 닫기(당일 접힘). meta:{slot:"saju_report"|"woori_30d"|"tarot_rich"} */
  "byeolmaru_gate_dismissed",
```

`lib/analytics/ui-events.test.ts` 에 단언을 추가:

```ts
  assert.equal(isUiEvent("byeolmaru_gate_dismissed"), true);
```

- [ ] **Step 3: `PremiumBlock` 에 닫기를 붙인다**

비자격 분기 맨 위(`const copy = BAIT[slot];` 아래)에 훅을 쓰고, 접혔으면 아무것도 안 그린다:

```tsx
  const { dismissed, dismiss } = useBaitDismiss(slot);
  if (dismissed) return null;
```

⚠️ **훅은 조건부로 호출할 수 없다.** `if (entitled) return (...)` 가 위에 있으므로 `useBaitDismiss` 를 그 아래에서 부르면 자격 상태가 바뀔 때 훅 순서가 달라진다 — **컴포넌트 최상단에서 호출**하고 값만 비자격 분기에서 쓰라. 구현자는 이 규칙을 지켜 배치를 정하고 **어떻게 했는지 보고**하라.

그리고 `<section>` 안 우상단에 닫기 버튼을 둔다(제목 행을 `justify-between` 으로):

```tsx
        <button
          onClick={() => { trackUiEvent("byeolmaru_gate_dismissed", { meta: { slot } }); dismiss(); }}
          aria-label="오늘은 그만 보기"
          className="shrink-0 rounded-full px-2 py-1 text-[11px] text-text-light/70"
        >
          오늘은 그만
        </button>
```

import: `import { useBaitDismiss } from "./useBaitDismiss";`

- [ ] **Step 4: 검증**

Run: `node --import tsx --test lib/analytics/ui-events.test.ts`
Expected: PASS

Run: `npx tsc --noEmit`
Expected: T2 와 같은 2건(Hub·SajuTodayView)만

- [ ] **Step 5: 커밋**

```bash
git add components/byeolmaru/useBaitDismiss.ts
git commit components/byeolmaru/useBaitDismiss.ts components/byeolmaru/PremiumBlock.tsx lib/analytics/ui-events.ts lib/analytics/ui-events.test.ts -m "feat(byeolmaru): 미끼 당일 접힘(slot별) + 닫기 계측 (P5-4)"
git show --stat HEAD
```

---

### Task 4: 허브 배선 — 나 탭 / 인연 탭 분기

스펙 §9 는 미끼를 **자리별로 다른 물건**으로 둔다. 허브엔 자리가 둘이다(나 탭 달력 아래 / 인연 탭).
🔴 지금은 `isPair` 여도 나-탭 블록이 그대로 뜬다 — 최종 리뷰가 지적한 것이다.

**Files:** Modify `components/byeolmaru/ByeolmaruHub.tsx`

- [ ] **Step 1: 미끼 호출부를 교체한다**

현재:
```tsx
        {!data.entitled && (
          <PremiumBlock entitled={false} trialUsed={data.trialUsed} narrative={null} teaser={null}
            loading={false} onStartTrial={startTrial} onSubscribe={openSubscribe} />
        )}
```

아래로 교체:
```tsx
        {/* 🔴 미끼는 자리마다 다른 물건이다(스펙 §9). 나 탭은 오늘 사주 리포트를,
            인연 탭은 우리 오늘을 판다 — 같은 블록을 두 자리에 쓰면 광고로 읽힌다. */}
        {!data.entitled && (
          <PremiumBlock
            entitled={false}
            trialUsed={data.trialUsed}
            narrative={null}
            teaser={null}
            loading={false}
            onStartTrial={startTrial}
            onSubscribe={openSubscribe}
            slot={isPair ? "woori_30d" : "saju_report"}
            baitCtx={
              isPair
                ? { partnerName: partners.find((p) => p.id === subject)?.name }
                : { gradeLabel: todayCell.grade.label }
            }
          />
        )}
```

⚠️ `isPair`·`partners`·`subject`·`todayCell` 이 그 스코프에 이미 있는지 확인하라(P5-3 이 만들었다). 없으면 보고하라.

- [ ] **Step 2: 검증**

Run: `npx tsc --noEmit`
Expected: `SajuTodayView.tsx` 1건만 남는다(T5 가 고친다)

- [ ] **Step 3: 커밋**

```bash
git commit components/byeolmaru/ByeolmaruHub.tsx -m "feat(byeolmaru): 허브 미끼를 나/인연 자리로 분기 (P5-4)"
git show --stat HEAD
```

---

### Task 5: 오늘 사주 배선 — 실제 무료 요약을 이어받는다

허브와 달리 `/byeolmaru/saju` 에는 **무료 요약(`getSajuTaste`)이 실제로 화면에 있다.** 미끼의 첫 줄이 그걸 바로 이어받아야 스펙 §9 의 "한 흐름"이 성립한다.

**Files:** Modify `components/byeolmaru/SajuTodayView.tsx`

- [ ] **Step 1: 미끼 호출부를 교체한다**

`PremiumBlock` 호출을 찾아 `slot`·`baitCtx` 를 더한다:

```tsx
        <PremiumBlock
          entitled={data.entitled}
          trialUsed={data.trialUsed}
          narrative={null}
          teaser={null}
          loading={false}
          onStartTrial={startTrial}
          onSubscribe={openSubscribe}
          slot="saju_report"
          // 🔴 바로 위 DayDetailCard 가 이 셀의 무료 요약을 보여주고 있다 — 미끼의 첫 줄이
          //    그 등급을 그대로 이어받아야 "광고"가 아니라 "방금 읽은 것의 다음 문장"이 된다.
          baitCtx={{ gradeLabel: cell.grade.label }}
        />
```

⚠️ **`cell` 이 선택된 셀이다**(오늘이 아닐 수 있다). 그게 맞다 — 사용자가 보고 있는 날의 등급을 이어받는 게 자연스럽다. `todayCell` 을 쓰지 마라.

🔴 **실행 중 정정(2026-09-13):** 이 지시는 뒤집혔다. `daily-report` 라우트에 **날짜 파라미터가 없어**
항상 오늘만 만들고(자격자 분기도 `dateLabel="오늘"`), 과거 날 등급을 이어받으면 못 지킬 약속이 된다.
최종 구현은 `baitCtx={{ gradeLabel: cell.isToday ? cell.grade.label : undefined }}` — 오늘일 때만
인용하고 아니면 `baitLead` 폴백("오늘은 왜 이런 결인지부터 풀어줄게.")으로 떨어진다.

- [ ] **Step 2: 검증**

Run: `npx tsc --noEmit`
Expected: 출력 없음(모든 호출부가 배선됐다)

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*") 2>&1 | tail -4`
Expected: `# fail 0`

- [ ] **Step 3: 커밋**

```bash
git commit components/byeolmaru/SajuTodayView.tsx -m "feat(byeolmaru): 오늘 사주 미끼가 그 날 무료 요약을 이어받는다 (P5-4)"
git show --stat HEAD
```

---

### Task 6: 오늘 타로 배선

`DailyCardBlock` 은 `PremiumBlock` 을 안 쓰고 **자체 블러 블록**을 갖고 있다. 스펙 §9 는 이 자리를 `tarot_rich` 미끼로 본다.

**Files:** Modify `components/byeolmaru/DailyCardBlock.tsx`

- [ ] **Step 1: 먼저 현재 비자격 블록을 읽는다**

`components/byeolmaru/DailyCardBlock.tsx` 에서 `entitled === false` 분기(블러 티저 + 체험/구독 버튼)를 찾아 **정확한 범위**를 확인하라. 그 안에 낙수 링크("이 카드, 타로로 더 깊게 →")가 섞여 있으면 **그건 미끼가 아니라 크로스셀이니 남긴다.**

- [ ] **Step 2: 그 블록을 `PremiumBlock` 으로 교체한다**

```tsx
          <PremiumBlock
            entitled={false}
            trialUsed={trialUsed}
            narrative={null}
            teaser={null}
            loading={false}
            onStartTrial={onStartTrial}
            onSubscribe={onSubscribe}
            slot="tarot_rich"
          />
```

import: `import PremiumBlock from "./PremiumBlock";`

⚠️ **`baitCtx` 를 안 넘긴다** — 타로 미끼의 첫 줄은 등급·상대와 무관하다(`baitLead` 가 맥락 없이도 문장을 준다).
⚠️ 프로필 없는 구독자 안내(`narrativeBlocked === "no_profile"`)는 **자격자 경로**라 건드리지 마라.
⚠️ 카드 뽑기·정적 taste·공유·낙수를 건드리지 마라.

- [ ] **Step 3: 검증**

Run: `npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*") 2>&1 | tail -4`
Expected: 출력 없음 · `# fail 0`

- [ ] **Step 4: 커밋**

```bash
git commit components/byeolmaru/DailyCardBlock.tsx -m "feat(byeolmaru): 오늘 타로 미끼를 공용 페이월 블록으로 (P5-4)"
git show --stat HEAD
```

---

### Task 7: 구독 시트 — 가치 3종 + 가격 크게

스펙 §9: "구독 시트 — **전체 가치 3종 + 가격을 여기서 크게, 딱 한 번**" · 시트 하단 "체험 중엔 별이 안 나가".

지금은 `StarConfirmModal` 에 `subtitle="30일 동안 매일 개인화를 열어둬"` 한 줄뿐이다.

**Files:** Modify `components/byeolmaru/useByeolmaruSubscribe.tsx`

- [ ] **Step 1: `StarConfirmModal` 이 무엇을 받을 수 있는지 확인한다**

`components/common/StarConfirmModal.tsx` 의 props 는 `cost`·`balance`·`loading`·`accent`·`title?`·`subtitle?`·`confirmLabel?`·`targetName?`·`spreadLabel?`·`onConfirm`·`onCharge`·`onClose` 다. **자유 ReactNode 슬롯이 없다.**

→ 판단이 필요하다. 둘 중 하나를 고르고 **근거를 보고**하라:
- **(A)** `subtitle` 에 가치 3종을 줄바꿈으로 욱여넣는다 — 변경 0, 다만 시각 위계가 약하다
- **(B)** `StarConfirmModal` 에 옵셔널 `children`(또는 `extra?: React.ReactNode`)을 더한다 — 이 모달은 **타로·관계·별마루가 공유**하므로 **기본값이 없으면 기존 호출부가 안 깨지게** 옵셔널이어야 한다

⚠️ (B)를 고르면 `StarConfirmModal` 의 **기존 호출부를 전부 grep** 해 무회귀를 확인하라.

- [ ] **Step 2: 가치 3종과 안심 문구를 넣는다**

무엇을 파는지는 `lib/byeolmaru/bait.ts` 의 `BAIT` 가 이미 안다 — **거기서 끌어와 중복 저작을 피한다**:

```tsx
import { BAIT, BAIT_SLOTS } from "@/lib/byeolmaru/bait";
```

시트에 들어갈 내용(마크업은 위 (A)/(B) 선택에 따라 배치):
- 가치 3종 = `BAIT_SLOTS.map((s) => BAIT[s].title)` 에 각 `chips[0]`(분량/범위)을 곁들인다
- 가격은 **크게** — `BYEOLMARU_SUBSCRIPTION.cost`별 / `.days`일
- 하단에 작게 **"체험 중엔 별이 안 나가"**

⚠️ 별콩이 화법을 지킬 것. ⚠️ `handleConfirm`·402 처리·계측은 **건드리지 마라.**

- [ ] **Step 3: 검증**

Run: `npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*") 2>&1 | tail -4`
Expected: 출력 없음 · `# fail 0`

브라우저: 허브에서 "3일 무료로 열어보기"가 아니라 **구독 버튼**이 뜨는 상태(체험 소진)로 시트를 열어 가치 3종·가격·안심 문구를 눈으로 확인하라. 현재 QA봇은 **체험 소진 + 비구독**이라 그대로 재현된다.
🔴 **"구독하기"를 실제로 누르지 마라** — 별이 차감된다.

- [ ] **Step 4: 커밋**

```bash
git commit components/byeolmaru/useByeolmaruSubscribe.tsx -m "feat(byeolmaru): 구독 시트에 가치 3종·가격·체험 안심 문구 (P5-4)"
git show --stat HEAD
```

(`StarConfirmModal.tsx` 를 고쳤으면 경로 추가)

---

### Task 8: CTA 계측에 slot 싣기 + 최종 검증

어느 자리가 파는지 봐야 스펙 §13 의 "어느 미끼가 파는가"가 성립한다. 지금 `byeolmaru_trial_started`·`byeolmaru_subscribe_clicked` 는 `useByeolmaruSubscribe` 안에서 **자리를 모른 채** 찍힌다.

**Files:** Modify `components/byeolmaru/useByeolmaruSubscribe.tsx` · `PremiumBlock.tsx`

- [ ] **Step 1: 훅이 slot 을 받게 한다**

`useByeolmaruSubscribe(onChanged)` 의 `startTrial`·`openSubscribe` 가 **호출 시점에** slot 을 받도록 바꾼다:

```tsx
  async function startTrial(slot?: string) {
    trackUiEvent("byeolmaru_trial_started", slot ? { meta: { slot } } : undefined);
    await fetch("/api/byeolmaru/trial", { method: "POST" });
    onChanged();
  }

  function openSubscribe(slot?: string) {
    trackUiEvent("byeolmaru_subscribe_clicked", slot ? { meta: { slot } } : undefined);
    …기존 그대로…
  }
```

⚠️ **옵셔널로 둬서 기존 호출부가 안 깨지게** 하라 — `PairDayDetailCard` 등이 인자 없이 부른다. `trackUiEvent` 의 두 번째 인자가 옵셔널인지 확인할 것.

- [ ] **Step 2: `PremiumBlock` 이 자기 slot 을 넘긴다**

```tsx
        <button onClick={() => onStartTrial(slot)} …>
```

→ 그러려면 `Props` 의 `onStartTrial`·`onSubscribe` 시그니처가 `(slot?: string) => void` 여야 한다. **기존 호출부가 `() => void` 를 넘겨도 타입이 맞는지** 확인하라(TS 는 인자 적은 함수를 허용한다).

- [ ] **Step 3: 전체 검증**

Run: `npx tsc --noEmit`
Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*") 2>&1 | tail -4`
Run: `npm run build`
Expected: 출력 없음 · `# fail 0` · exit 0 + `ƒ Proxy (Middleware)`

- [ ] **Step 4: 커밋**

```bash
git commit components/byeolmaru/useByeolmaruSubscribe.tsx components/byeolmaru/PremiumBlock.tsx -m "feat(byeolmaru): 체험·구독 CTA 계측에 미끼 자리 싣기 (P5-4)"
git show --stat HEAD
```

---

## 최종 검증

- [ ] **자동 게이트**

Run: `npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*") 2>&1 | tail -4 && npm run build`
Expected: 전부 통과 · `ƒ Proxy (Middleware)` 확인

- [ ] **브라우저 — 비자격자(QA봇: 비구독·체험 소진·프로필 있음·상대 1명)**

| 자리 | 확인할 것 |
|---|---|
| 허브 나 탭 | "별콩이가 더 깊이 읽어줄게" + `구독` 배지 · 칩 `약 2,100자`/`재물 · 직장 …` · 첫 줄이 **오늘 등급을 이어받는가** · 페이드 꼬리 · CTA · 가격 2단 |
| 허브 인연 탭 | 상대 칩을 누르면 미끼가 **우리 오늘로 바뀌는가**(제목·칩·첫 줄에 상대 이름) |
| `/byeolmaru/saju` | 과거 날을 골랐을 때 첫 줄이 **그 날 등급**을 이어받는가 |
| `/byeolmaru/tarot` | 카드 밑 미끼가 `tarot_rich` 문구인가 |
| 구독 시트 | 가치 3종 + 가격 크게 + "체험 중엔 별이 안 나가" (🔴 **구독하기를 누르지 마라**) |

- [ ] **당일 접힘**

허브에서 "오늘은 그만"을 누른다 → 미끼가 사라진다 → **새로고침해도 안 뜬다** → `/byeolmaru/saju` 의 미끼는 **여전히 뜬다**(slot 별 접힘) → devtools 에서 `localStorage.byeolkong_bait_dismissed` 가 `{"saju_report":"YYYY-MM-DD"}` 모양인지 확인.
⚠️ 확인 후 그 키를 지워 원상 복구하라.

- [ ] **자격자 무회귀**

체험/구독 중인 상태에서 `/byeolmaru/saju` 에 **리포트가 그대로** 뜨고 미끼가 **안 뜨는지**. QA봇으로 재현하려면 체험을 써야 하므로, **누르지 말고** 코드로 확인하고 그 사실을 보고하라.

- [ ] **계측**

미끼가 뜰 때 `byeolmaru_gate_shown` 의 `meta.slot` 이, 닫을 때 `byeolmaru_gate_dismissed` 가, CTA 클릭 시 `byeolmaru_trial_started`/`byeolmaru_subscribe_clicked` 의 `meta.slot` 이 `/api/event` 페이로드에 실리는지 확인(`sendBeacon` 을 패치해 캡처).

- [ ] **375px 모바일**

칩이 두 줄로 흐를 때 레이아웃이 깨지지 않는지 · 닫기 버튼이 제목과 겹치지 않는지.

- [ ] **대비**

CTA 텍스트(골드 배경) · 칩 텍스트(`bg-cream-warm`) · 배지(`bg-lilac-soft/70`) · 페이드 꼬리(`opacity-60`) 의 대비를 계산해 AA 를 확인하라. **P5-3 에서 이 종류로 세 번 걸렸다.**

---

## Self-Review 기록

**스펙 §9 항목별 커버리지**

| 스펙 요구 | 태스크 |
|---|---|
| 나 탭 달력 아래 = 오늘 사주 리포트 미끼(분량 칩·5섹션·첫 줄+페이드) | T1(카피)·T2(렌더)·T4(허브 배선) |
| 인연 탭 = 우리 오늘 | T1·T4 |
| 오늘 타로 화면 = 깊은 해석 티저 | T6 |
| 구독 시트 = 가치 3종 + 가격 크게 | T7 |
| 자물쇠 제거 + "별콩이가 더 깊이 읽어줄게" + `구독` 배지 | T2 |
| CTA "3일 무료로 열어보기" | T2 |
| 가격 2단(CTA 밑 작게 / 시트에서 크게) | T2·T7 |
| 비로그인·생일 미입력 숨김 | **이미 충족** — P5-3 이 그 두 상태를 `EmptyMonthShell` 로 갈라 `ready` 분기에 도달하지 않는다. 새 코드 불필요(플랜 T4 주석이 이 사실을 남긴다) |
| 거절하면 그날 접힘 | T3 |
| §13 `meta.slot` 으로 어느 미끼가 파는지 | T2(노출)·T3(닫기)·T8(CTA) |

**의도적으로 남긴 것**
- **우리 오늘 분량 확장과 `pair-narrative` 캐시는 P5-5.** 이 플랜은 허브 인연 탭에 **미끼 자리**만 만든다
- **§8 "우리 셀 ✨끌림 🔗결속 태그"도 P5-5**(사용자 확인 2026-09-13)
- `PairDayDetailCard` 의 비자격 블록은 그대로 둔다 — `/byeolmaru/woori` 화면은 이 플랜의 스코프가 아니고, P5-5 가 그 화면의 분량을 다룰 때 함께 본다
- **`byeolmaru_gate_shown` 이 허브에서 새로 찍히기 시작한 것**(P5-3 부터)은 slot 축이 생기며 해소된다 — prod 베이스라인이 없어 파괴되는 과거 데이터는 없다
