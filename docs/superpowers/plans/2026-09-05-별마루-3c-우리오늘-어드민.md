# 별마루 ③-c 우리 오늘 어드민 metrics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 3-b에서 배선한 우리 오늘 계측을 읽는 어드민 뷰를 `/admin/free/byeolmaru`에 얹는다 — 담기 퍼널(선택→담기→구매) · 우리 CTA→구독 전환 · 상대 수 분포.

**Architecture:** ④ `admin_byeolmaru_*` RPC 패턴을 그대로 미러(집계는 RPC, 앱은 원본 행 안 끌어옴). 신규 RPC 2개(요약·분포) + 기존 API 라우트·페이지 확장. 날짜 그룹 없는 **누적** 집계라 KST 날짜 트랩 무관.

**Tech Stack:** Supabase RPC(SQL SECURITY DEFINER) · Next.js 서버 컴포넌트 · `requireAdmin`.

**Spec:** `2026-09-05-별마루-3-우리오늘-design.md` §9. 미러 원본 = `supabase/migrations/20260904000000_admin_byeolmaru_aggregates.sql`, `app/api/admin/byeolmaru/route.ts`, `app/admin/free/byeolmaru/page.tsx`.

**🔴 AGENTS.md 어드민 함정 — 이 플랜에 적용:**
- 마이그레이션의 **모든 함수 본문**이 유효해야 파일 전체가 적용된다(일부 오류 → 파일 통째 실패 → 코드만 배포돼 화면 깨짐). SQL 은 ④ 를 정독해 정확히 미러.
- `_` 는 LIKE 와일드카드 → **정확 이벤트 매칭**(`event = ANY(ARRAY[...])`)만 쓴다(LIKE 금지).
- NULL 3값 논리 → ui_events exclude 는 `(user_id IS NULL OR user_id <> ALL(p_exclude))`. `byeolmaru_watch` 는 user_id NOT NULL(FK)이라 `user_id <> ALL(p_exclude)` 단독 안전.
- 날짜식 트랩 → 이 RPC 들은 **누적(날짜 그룹 없음)**이라 해당 없음.
- 신규 SECURITY DEFINER 는 `REVOKE … FROM PUBLIC, anon, authenticated` **셋 다** + `GRANT … TO service_role`.
- RETURNS TABLE OUT 파라미터명과 본문 컬럼 별칭이 겹치지 않게(④ 경고).

---

## File Structure

- Create `supabase/migrations/20260905010000_admin_byeolmaru_watch.sql` — RPC 2개.
- Modify `app/api/admin/byeolmaru/route.ts` — 신규 RPC 2개 호출 + 응답에 추가.
- Modify `app/admin/free/byeolmaru/page.tsx` — "우리 오늘" 섹션(퍼널 Stat + 전환 + 상대 수 분포 표).

Verify: `npx tsc --noEmit` · `npm run build`. Commit: conventional + blank line + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## Task 1: 마이그레이션 — 우리 오늘 집계 RPC 2개

**Files:** Create `supabase/migrations/20260905010000_admin_byeolmaru_watch.sql`

- [ ] **Step 1: 작성** — `20260904000000_admin_byeolmaru_aggregates.sql` 을 먼저 정독해 스타일(STABLE·SECURITY DEFINER·search_path·REVOKE/GRANT·`::BIGINT` 캐스트)을 정확히 맞춘 뒤:

```sql
-- 20260905010000_admin_byeolmaru_watch.sql — 우리 오늘 담기 퍼널 + 상대 수 분포.
-- ④ admin_byeolmaru_* 미러. 누적(날짜 그룹 없음) → AT TIME ZONE 트랩 무관.
-- exclude: ui_events 는 NULL-safe, byeolmaru_watch 는 user_id NOT NULL(FK)이라 단독.
-- 이벤트는 정확 매칭(ANY(ARRAY[...]))만 — LIKE '_' 와일드카드 트랩 회피.
-- 스펙: docs/superpowers/specs/2026-09-05-별마루-3-우리오늘-design.md §9

-- ── 1. 담기 퍼널 요약 (스칼라 1행) — 5 이벤트 events/actors + 우리 CTA→구독 전환 ──
CREATE OR REPLACE FUNCTION admin_byeolmaru_watch_summary(p_exclude UUID[])
RETURNS TABLE (
  partner_selected_events BIGINT, partner_selected_actors BIGINT,
  watch_add_events BIGINT, watch_add_actors BIGINT,
  watch_limit_events BIGINT, watch_limit_actors BIGINT,
  watch_purchase_events BIGINT, watch_purchase_actors BIGINT,
  woori_cta_events BIGINT, woori_cta_actors BIGINT,
  woori_converted_actors BIGINT
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH ev AS (
    SELECT e.event AS ev_name, coalesce(e.user_id::text, e.anon_id) AS actor
    FROM ui_events e
    WHERE e.event = ANY(ARRAY[
      'byeolmaru_partner_selected','byeolmaru_watch_add','byeolmaru_watch_limit',
      'byeolmaru_watch_purchase','byeolmaru_subscribe_from_woori','byeolmaru_subscribe_completed'
    ])
      AND (e.user_id IS NULL OR e.user_id <> ALL(p_exclude))
  )
  SELECT
    count(*) FILTER (WHERE ev_name='byeolmaru_partner_selected')::BIGINT,
    count(DISTINCT actor) FILTER (WHERE ev_name='byeolmaru_partner_selected')::BIGINT,
    count(*) FILTER (WHERE ev_name='byeolmaru_watch_add')::BIGINT,
    count(DISTINCT actor) FILTER (WHERE ev_name='byeolmaru_watch_add')::BIGINT,
    count(*) FILTER (WHERE ev_name='byeolmaru_watch_limit')::BIGINT,
    count(DISTINCT actor) FILTER (WHERE ev_name='byeolmaru_watch_limit')::BIGINT,
    count(*) FILTER (WHERE ev_name='byeolmaru_watch_purchase')::BIGINT,
    count(DISTINCT actor) FILTER (WHERE ev_name='byeolmaru_watch_purchase')::BIGINT,
    count(*) FILTER (WHERE ev_name='byeolmaru_subscribe_from_woori')::BIGINT,
    count(DISTINCT actor) FILTER (WHERE ev_name='byeolmaru_subscribe_from_woori')::BIGINT,
    (SELECT count(DISTINCT actor) FROM ev
       WHERE ev_name='byeolmaru_subscribe_from_woori'
         AND actor IN (SELECT actor FROM ev WHERE ev_name='byeolmaru_subscribe_completed'))::BIGINT
  FROM ev;
$$;

-- ── 2. 상대 수 분포 (watch_count당 유저 수) ──
CREATE OR REPLACE FUNCTION admin_byeolmaru_watch_distribution(p_exclude UUID[])
RETURNS TABLE (watch_count INT, user_count BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT wc::INT, count(*)::BIGINT
  FROM (
    SELECT w.user_id, count(*) AS wc
    FROM byeolmaru_watch w
    WHERE w.user_id <> ALL(p_exclude)
    GROUP BY w.user_id
  ) t
  GROUP BY wc
  ORDER BY wc;
$$;

-- ── 권한 (AGENTS.md — PUBLIC·anon·authenticated 셋 다 회수 + service_role 부여) ──
REVOKE EXECUTE ON FUNCTION admin_byeolmaru_watch_summary(UUID[]) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION admin_byeolmaru_watch_distribution(UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_byeolmaru_watch_summary(UUID[]) TO service_role;
GRANT EXECUTE ON FUNCTION admin_byeolmaru_watch_distribution(UUID[]) TO service_role;
```

- [ ] **Step 2: 본문 유효성 자기검증** — 두 함수의 SELECT 본문을 (RETURNS TABLE 래핑 없이) 눈으로 파싱: `FILTER`·CTE·서브쿼리 문법, 캐스트, 컬럼 수(요약 11개 = RETURNS TABLE 11개 일치)를 확인. `byeolmaru_watch`·`ui_events` 컬럼명 실재 확인(`user_id`, `anon_id`, `event`).

- [ ] **Step 3: 커밋** — `git add supabase/migrations/20260905010000_admin_byeolmaru_watch.sql` msg `feat(byeolmaru): 우리오늘 어드민 집계 RPC(담기 퍼널·상대 수 분포)`.

> 적용은 dev push 시 자동(Task 4). 신규 함수는 기본 닫힘(AGENTS.md 2026-07-29 기본권한) + 명시 REVOKE 이중.

---

## Task 2: API 라우트 확장

**Files:** Modify `app/api/admin/byeolmaru/route.ts`

- [ ] **Step 1: 신규 RPC 2개 호출 추가** — 기존 `Promise.all([...])` 에 두 호출을 더하고 응답에 실어 보낸다(블록별 error 플래그 관행 유지):

```ts
  const [summaryRes, trendRes, retentionRes, watchSummaryRes, watchDistRes] = await Promise.all([
    supa.rpc("admin_byeolmaru_summary", { p_exclude }),
    supa.rpc("admin_byeolmaru_trend", { p_since, p_exclude }),
    supa.rpc("admin_byeolmaru_retention", { p_exclude }),
    supa.rpc("admin_byeolmaru_watch_summary", { p_exclude }),
    supa.rpc("admin_byeolmaru_watch_distribution", { p_exclude }),
  ]);
```
그리고 반환 객체에 추가:
```ts
    watchSummary: watchSummaryRes.data?.[0] ?? null,
    watchSummaryError: !!watchSummaryRes.error,
    watchDist: watchDistRes.data ?? [],
    watchDistError: !!watchDistRes.error,
```

- [ ] **Step 2: tsc + 커밋** — `npx tsc --noEmit` → 0. `git add app/api/admin/byeolmaru/route.ts` msg `feat(byeolmaru): 어드민 API 우리오늘 집계 2종 추가`.

---

## Task 3: 페이지 "우리 오늘" 섹션

**Files:** Modify `app/admin/free/byeolmaru/page.tsx`

- [ ] **Step 1: 타입 + 로드 확장** — `load()` 의 fetch 파싱에 `watchSummary`/`watchDist`(+ error 플래그) 추가. 타입:
```ts
type WatchSummaryRow = {
  partner_selected_events: number; partner_selected_actors: number;
  watch_add_events: number; watch_add_actors: number;
  watch_limit_events: number; watch_limit_actors: number;
  watch_purchase_events: number; watch_purchase_actors: number;
  woori_cta_events: number; woori_cta_actors: number;
  woori_converted_actors: number;
};
type WatchDistRow = { watch_count: number; user_count: number };
```
`load()` 반환에 `watchSummaryError`, `watchDistError`, 정규화한 `ws`(Number(...) 방어), `watchDist` 배열 포함(기존 `sum` 정규화 패턴과 동일).

- [ ] **Step 2: 섹션 렌더** — "D1~D7 재방문" 섹션 다음(또는 인터랙션 섹션 뒤)에 추가. Stat 카드 + 전환 + 분포 표. `Stat`·`LoadFailed` 재사용:
```tsx
<section>
  <h2 className="text-sm text-white/60 mb-3">우리 오늘 <span className="text-white/35">(담기 퍼널 · 구독 전용)</span></h2>
  <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
    <Stat label="상대 선택" value={s.watchSummaryError ? "—" : s.ws.partnerSelectedEvents} sub={s.watchSummaryError ? undefined : `${s.ws.partnerSelectedActors}명`} />
    <Stat label="담기" value={s.watchSummaryError ? "—" : s.ws.watchAddEvents} sub={s.watchSummaryError ? undefined : `${s.ws.watchAddActors}명`} />
    <Stat label="5별벽 도달" value={s.watchSummaryError ? "—" : s.ws.watchLimitEvents} sub={s.watchSummaryError ? undefined : `${s.ws.watchLimitActors}명`} />
    <Stat label="추가상대 결제" value={s.watchSummaryError ? "—" : s.ws.watchPurchaseEvents} sub={s.watchSummaryError ? undefined : `${s.ws.watchPurchaseActors}명`} />
  </div>
  <div className="grid grid-cols-2 md:grid-cols-4 gap-3 mt-3">
    <Stat label="우리 락 CTA" value={s.watchSummaryError ? "—" : s.ws.wooriCtaEvents} sub={s.watchSummaryError ? undefined : `${s.ws.wooriCtaActors}명`} />
    <Stat label="→ 구독 전환" value={s.watchSummaryError ? "—" : s.ws.wooriConvertedActors} sub={s.watchSummaryError ? undefined : `CTA ${s.ws.wooriCtaActors}명 중`} />
  </div>
  {s.watchSummaryError && <LoadFailed block="admin_byeolmaru_watch_summary" className="mt-2" />}

  <h3 className="text-[13px] text-white/50 mt-4 mb-2">상대 수 분포</h3>
  {s.watchDistError ? (
    <LoadFailed block="admin_byeolmaru_watch_distribution" />
  ) : s.watchDist.length === 0 ? (
    <div className="text-[12px] text-white/40">아직 담은 유저 없음</div>
  ) : (
    <table className="w-full text-[12px] max-w-xs">
      <thead><tr className="text-white/40 text-left"><th className="py-1 pr-3">상대 수</th><th className="py-1 px-2 text-right">유저 수</th></tr></thead>
      <tbody>
        {s.watchDist.map((d) => (
          <tr key={d.watch_count} className="border-t border-white/5 text-white/70">
            <td className="py-1 pr-3">{d.watch_count}명</td><td className="py-1 px-2 text-right">{d.user_count}</td>
          </tr>
        ))}
      </tbody>
    </table>
  )}
</section>
```
(`s.ws.*` 는 Step 1에서 camelCase 정규화한 이름에 맞출 것.)

- [ ] **Step 3: tsc + build + 커밋** — `npx tsc --noEmit` → 0 · `npm run build` → exit 0 (`ƒ Proxy` 유지). `git add app/admin/free/byeolmaru/page.tsx` msg `feat(byeolmaru): 어드민 우리오늘 섹션(퍼널·전환·상대 수 분포)`.

---

## Task 4: dev 적용 + 검증

- [ ] **Step 1: push + 마이그 적용 확인** — `git push origin dev`. Supabase dev 워크플로 SUCCESS 확인 후, RPC 존재 + ACL 클린 확인(AGENTS.md 한 줄 감사): `run-prod-query.mjs` 는 prod 라 dev 확인은 어드민 페이지 실렌더로 대체(아래). **마이그가 실패하면 파일 전체 실패 → 어드민 페이지가 두 RPC 를 404 로 호출** — 그래서 Step 2 가 곧 적용 검증이다.
- [ ] **Step 2: 어드민 실렌더 검증** — [[admin-local-verify-trick]](임시 ADMIN_USER_IDS + HMAC 쿠키) 로 `/admin/free/byeolmaru` 진입: "우리 오늘" 섹션이 LoadFailed 없이 뜨고(값 0이어도 정상 — 아직 트래픽 전), 상대 수 분포가 "아직 담은 유저 없음" 또는 실데이터. 두 신규 블록 모두 error=false. 검증 후 ADMIN_USER_IDS 원복.
- [ ] **Step 3: 숫자 sanity** — 신규 집계라 대조할 옛 JS 가 없다 → raw `ui_events`(dev)의 이벤트별 count 와 RPC 요약을 눈대중 대조(예: `byeolmaru_partner_selected` 원시 count == 요약 partner_selected_events). fixture E2E 잔재가 있으면 그만큼 잡힘(무해).

---

## Self-Review (작성자 체크)
**1. Spec 커버리지:** §9 담기 퍼널(선택→담기→결제) → watch_summary ✅ / CTA→구독 전환 → woori_cta/converted ✅ / 상대 수 분포 → watch_distribution ✅. 우리 D1-D7 재방문은 ④ byeolmaru retention(같은 /byeolmaru 페이지)로 근사 — 우리-전용 코호트는 v1 후속(명시).
**2. Placeholder 스캔:** SQL·API 전체 코드. 페이지는 ④ 패턴(Stat/LoadFailed/정규화) 재사용 지시 + 섹션 JSX 전체.
**3. 어드민 함정:** LIKE 회피(ANY) · NULL-safe exclude · REVOKE 3종 · 누적(날짜트랩 무관) · OUT/본문 별칭 분리(wc vs watch_count) · 컬럼 수 11 일치. 전부 적용.
**4. 타입 일관성:** RETURNS TABLE 컬럼(Task1) ↔ API 반환(Task2) ↔ 페이지 타입(Task3) 이름 일치(snake_case RPC → camelCase 정규화).

---

## Execution Handoff
3-c 완료 후 별마루 순서 = ⑤(good_days/one_card 폐지+낙수CTA) → ⑥(정적콘텐츠 1회생성·검수) → ⑦(일지 캐릭터 12종) → 배포 한 판 → 노출 스위치 ON. prod 는 사용자 명시 go.
