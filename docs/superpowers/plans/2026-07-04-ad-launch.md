# 광고 런칭 전체 실행 계획 (오퍼 재화 → 소재 제작 → 트래킹 → 메타 등록)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스펙(`docs/superpowers/specs/2026-07-04-ad-strategy-design.md`)의 인스타 소액 광고(일 ₩10k)를 실제 런칭 가능한 상태로 만든다 — 오퍼(웰컴 30별 + 첫 충전 보너스), 소재 3종(힉스필드 협업 제작), 유입 트래킹(UTM+Pixel+CAPI), 메타 등록까지.

**Architecture:** 코드 2단계(Phase 1 오퍼 재화 → Phase 3 트래킹)는 기존 `charge_stars` RPC(TEXT payment_id 멱등 키)를 재사용해 마이그레이션 없이 구현. 소재(Phase 2)는 힉스필드 MCP(이미지 생성) + Figma(조판·export) 협업 워크플로. 메타 등록(Phase 4)은 운영 체크리스트.

**Tech Stack:** Next.js 16 · Supabase RPC(`charge_stars`) · Meta Pixel + Conversions API · Higgsfield MCP(호스팅 `https://mcp.higgsfield.ai/mcp`, OAuth) · Figma MCP

**검증 방식 주의:** 이 레포엔 유닛테스트 러너가 없다(package.json에 jest/vitest 없음). 프로젝트 컨벤션(AGENTS.md "코드 변경 → 빌드 → dev push → 새로고침 검증")대로 각 태스크의 검증은 `npx tsc --noEmit` + dev 배포 후 브라우저/SQL 확인으로 한다.

---

## Phase 0 — 힉스필드 MCP 연결 (사용자 1회 작업)

### Task 0: Higgsfield MCP 등록

**Files:** 없음 (로컬 MCP 설정)

- [ ] **Step 1: 터미널에서 MCP 추가** (Claude Code가 설치된 인터랙티브 터미널에서)

```bash
claude mcp add --transport http --scope user higgsfield https://mcp.higgsfield.ai/mcp
```

- [ ] **Step 2: OAuth 로그인** — 첫 사용 시 브라우저가 열리면 힉스필드 계정으로 로그인(무료 월 150크레딧, 이미지 1장 ≈ 2~10크레딧).

- [ ] **Step 3: 연결 확인** — 새 Claude 세션에서 "힉스필드 툴 보여줘"라고 요청 → `mcp__higgsfield__*` 툴이 잡히면 성공. 안 잡히면 `claude mcp list`로 등록 여부 확인.

---

## Phase 1 — 코드: 오퍼 재화 (웰컴 30별 + 첫 충전 보너스)

### Task 1: 웰컴 별 30개 지급 (카카오 신규 가입)

**Files:**
- Modify: `app/api/auth/kakao/route.ts` (신규 유저 `star_balances` INSERT 직후, 약 113~116행)
- Create 없음, Migration 없음 — 기존 `charge_stars` RPC 재사용 (`p_payment_id`는 TEXT + 유니크 partial index → `welcome:{userId}` 키로 멱등 보장)

- [ ] **Step 1: 상수 추가** — `lib/saju/constants.ts`가 아닌 공용 상수 파일 `lib/constants.ts` 끝에 추가:

```typescript
/** 카카오 신규 가입 웰컴 별 — 사주 상담(20)·타로 원/투/쓰리(10/15/25)·이번달(20) 1회 커버 */
export const WELCOME_BONUS_STARS = 30;
/** 첫 충전 보너스 비율 — 첫 결제 패키지 별의 +50% (반올림) */
export const FIRST_CHARGE_BONUS_RATE = 0.5;
```

- [ ] **Step 2: 카카오 콜백에서 지급** — `app/api/auth/kakao/route.ts`의 신규 유저 `star_balances` INSERT(`balance: 0`) 바로 뒤에 추가. 파일 상단 import에 `chargeStars`·`WELCOME_BONUS_STARS` 추가:

```typescript
import { chargeStars } from "@/lib/stars";
import { WELCOME_BONUS_STARS } from "@/lib/constants";
```

```typescript
      // 웰컴 별 지급 — charge_stars RPC 멱등 키(welcome:{userId})로 유저당 1회 보장
      const welcome = await chargeStars(
        user.id,
        WELCOME_BONUS_STARS,
        `welcome:${user.id}`,
        "welcome_bonus"
      );
      if (!welcome.success) {
        await logError(new Error("welcome bonus grant failed"), {
          route: "/api/auth/kakao",
          userId: user.id,
          extra: { severity: "WELCOME_BONUS_FAILED" },
        });
      }
```

주의: 해당 블록의 유저 id 변수명은 파일 내 실제 변수(예: `user.id` 또는 `userId`)에 맞춘다. `logError`는 이 라우트에 이미 import돼 있는지 확인하고 없으면 `@/lib/logger`에서 추가.

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 0

- [ ] **Step 4: 커밋**

```bash
git add lib/constants.ts app/api/auth/kakao/route.ts
git commit -m "feat(offer): 카카오 신규 가입 웰컴 별 30개 지급 (charge_stars 멱등)"
```

### Task 2: 첫 충전 보너스 별 (+50%)

**Files:**
- Modify: `app/api/payment/confirm/route.ts` (`chargeStars` 성공 분기 뒤, 167~181행 부근)

- [ ] **Step 1: 첫 충전 판정 + 보너스 지급** — `const charge = await chargeStars(userId, pkg.stars, payment.id);` 성공 처리 뒤, 최종 `return NextResponse.json({ success: true, ... })` 앞에 추가. 상단 import에 `FIRST_CHARGE_BONUS_RATE` 추가:

```typescript
    // ━━ 첫 충전 보너스: 이 유저의 completed 결제가 이번 건뿐이면 +50% 별 지급.
    // 멱등 키 first_bonus:{userId} → 동시 결제 레이스에도 평생 1회만 지급된다.
    let bonusStars = 0;
    const { count: paidCount } = await supabase
      .from("payments")
      .select("id", { count: "exact", head: true })
      .eq("user_id", userId)
      .eq("status", "completed");
    if (charge.success && paidCount === 1) {
      bonusStars = Math.round(pkg.stars * FIRST_CHARGE_BONUS_RATE);
      const bonus = await chargeStars(
        userId,
        bonusStars,
        `first_bonus:${userId}`,
        "first_charge_bonus"
      );
      if (!bonus.success) bonusStars = 0;
      else if (bonus.idempotent) bonusStars = 0; // 이미 받은 적 있음(레이스/재시도)
    }
```

- [ ] **Step 2: 응답에 보너스 노출** — 최종 성공 응답을 다음으로 교체:

```typescript
    return NextResponse.json({
      success: true,
      stars: pkg.stars,
      bonusStars,
      balance: bonusStars > 0 ? charge.balance + bonusStars : charge.balance,
      paymentKey: paymentResult.paymentKey,
    });
```

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 0

- [ ] **Step 4: 커밋**

```bash
git add app/api/payment/confirm/route.ts lib/constants.ts
git commit -m "feat(offer): 첫 충전 보너스 별 +50% (first_bonus 멱등 키)"
```

### Task 3: Phase 1 dev 검증 + 배포

- [ ] **Step 1: dev push**

```bash
git push origin dev
```

- [ ] **Step 2: 웰컴 별 검증** — dev Supabase SQL Editor에서 테스트 카카오 계정 탈퇴 → dev.byeolkongtalk.com 재가입 → 확인:

```sql
SELECT amount, source, payment_id FROM star_transactions
 WHERE payment_id LIKE 'welcome:%' ORDER BY created_at DESC LIMIT 5;
```
Expected: `amount=30, source=welcome_bonus` row 1건. 헤더 ⭐ 칩 = 30.

- [ ] **Step 3: 첫 충전 보너스 검증** — dev(토스 테스트 키)에서 10별(1,000원) 테스트 결제 → 확인:

```sql
SELECT amount, source FROM star_transactions
 WHERE payment_id LIKE 'first_bonus:%' ORDER BY created_at DESC LIMIT 5;
```
Expected: `amount=5, source=first_charge_bonus` 1건. 같은 계정 2번째 결제에선 추가 row 없음(멱등).

- [ ] **Step 4: prod 배포** — 이상 없으면:

```bash
git checkout main && git merge --ff-only dev && git push origin main && git checkout dev
```

---

## Phase 2 — 크리에이티브: 소재 3종 제작 (나 + 힉스필드 + Figma 협업)

> 코드가 아닌 **가이드형 협업 세션**. 새 세션에서 "광고 소재 제작 시작하자"라고 하면 아래를 순서대로 같이 진행한다. 전제: Phase 0 완료.

### Task 4: 비주얼 생성 (힉스필드, 소재당 2~3안)

- [ ] **Step 1: 캐릭터 레퍼런스 준비** — `public/byeolkong-main.png`를 스타일 레퍼런스로 사용(캐릭터 일관성). 힉스필드 캐릭터/이미지 참조 기능이 있으면 첨부, 없으면 프롬프트만.

- [ ] **Step 2: 소재별 생성 프롬프트** (Soul V2 또는 Flux 2, 세로 3:4~9:16, **이미지 안에 글자 넣지 않음** — 한글 텍스트는 Figma에서 오버레이):

소재 1 고민상담 훅:
```
A cute small star-guardian fairy character with cream-white round body, lavender cloud patterns, golden star pendant, gently listening to someone at night. Warm cozy dawn atmosphere, cream to soft-lilac gradient background, floating golden star particles, soft plush 3D-render style, empty space at top third for text overlay, no text, vertical composition
```

소재 2 무료 오늘의 운세 훅:
```
Same cute star-guardian fairy character holding a small glowing golden star like a gift, cheerful morning mood, cream and pale-lilac sky, sparkling star particles, soft plush 3D-render style, generous empty space at top for headline, no text, vertical composition
```

소재 3 직접 뽑는 타로 훅:
```
Same cute star-guardian fairy character presenting a fanned deck of tarot cards face-down, one card glowing golden being pulled out by an unseen hand, mystical night-purple backdrop with golden star particles, inviting interactive feeling, soft plush 3D-render style, space at top for text, no text, vertical composition
```

- [ ] **Step 3: 생성 실행** — 내가 힉스필드 MCP 툴을 호출해 소재당 2~3안 생성(약 6~9장, ~20~60크레딧). 사용자가 베스트 1안씩 선택.

- [ ] **Step 4: 재생성 루프** — 캐릭터 톤이 별콩이와 어긋나면(눈 색·구름 무늬·펜던트) 프롬프트 보정 후 재생성. 최대 2라운드로 제한(크레딧 관리).

### Task 5: 조판 + export (Figma MCP)

- [ ] **Step 1: Figma 파일 생성** — 내가 Figma MCP로 새 파일에 프레임 6개 생성: 소재 3종 × (피드 1080×1350, 스토리 1080×1920).

- [ ] **Step 2: 레이아웃 구성** — 각 프레임: 선택된 비주얼 배치 + 상단 카피(Cafe24 Ssurround 스타일 볼드) + "무료" 배지(gold `#E8C26A`) + 하단 CTA 버튼(lilac-deep `#9F8AD0`). 카피(스펙 §4 그대로):
  - 소재 1: `요즘 마음 복잡하지?` / 서브 `별콩이한테 고민 상담, 첫 회 무료` / CTA `무료로 물어보기`
  - 소재 2: `오늘 하루, 어떤 흐름일까?` / 서브 `오늘의 운세 무료로 보고 가` / CTA `무료로 보기`
  - 소재 3: `카드는 네가 직접 뽑아` / 서브 `별콩이가 풀어줄게 · 첫 회 무료` / CTA `무료로 뽑아보기`
  - 규칙: 단정 예언 문구 금지("~된다" X), CTA 1개, "무료" 항상 노출.

- [ ] **Step 3: PNG export** — 6장 export → 사용자 다운로드. 파일명: `ad_counsel_feed.png`, `ad_counsel_story.png`, `ad_daily_feed.png`, `ad_daily_story.png`, `ad_tarot_feed.png`, `ad_tarot_story.png`.

- [ ] **Step 4: 최종 검수** — 모바일에서 6장 확인: 글자 크기(폰 화면에서 읽히는지), "무료" 배지 시인성, 세이프존(스토리 상하 250px 텍스트 금지 — 프로필/CTA UI에 가림).

---

## Phase 3 — 코드: 유입 트래킹 (UTM + Meta Pixel + CAPI)

### Task 6: 환경변수 + Meta Pixel 클라이언트

**Files:**
- Create: `components/analytics/MetaPixel.tsx`
- Modify: `app/layout.tsx` (body 안, 기존 `AuthBootstrap`/`KakaoSdkLoader` 옆)
- Modify: `.env.local.template` (키 2개 추가)

- [ ] **Step 1: env 키 추가** — `.env.local.template`에:

```bash
# Meta 광고 트래킹 (Phase 4에서 발급)
NEXT_PUBLIC_META_PIXEL_ID=
META_CAPI_ACCESS_TOKEN=
```

- [ ] **Step 2: Pixel 컴포넌트 생성** — `components/analytics/MetaPixel.tsx`:

```tsx
"use client";

import Script from "next/script";

/** Meta Pixel — PageView 자동 추적. 표준 전환 이벤트(가입/구매)는 서버 CAPI에서 전송(중복 방지 분담). */
export default function MetaPixel() {
  const pixelId = process.env.NEXT_PUBLIC_META_PIXEL_ID;
  if (!pixelId) return null;
  return (
    <Script id="meta-pixel" strategy="afterInteractive">
      {`!function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?
n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;
n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;
t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,
document,'script','https://connect.facebook.net/en_US/fbevents.js');
fbq('init', '${pixelId}');
fbq('track', 'PageView');`}
    </Script>
  );
}
```

- [ ] **Step 3: 루트 레이아웃에 마운트** — `app/layout.tsx`의 `<body>` 안 기존 부트스트랩 컴포넌트들 옆에 `<MetaPixel />` 추가 + import.

- [ ] **Step 4: 타입 체크 + 커밋**

```bash
npx tsc --noEmit
git add components/analytics/MetaPixel.tsx app/layout.tsx .env.local.template
git commit -m "feat(ads): Meta Pixel 클라이언트 (PageView)"
```

### Task 7: CAPI 서버 유틸

**Files:**
- Create: `lib/meta-capi.ts`

- [ ] **Step 1: CAPI 유틸 작성** — `lib/meta-capi.ts`:

```typescript
// Meta Conversions API — 서버 발화 전환 이벤트 (iOS/쿠키 차단 대응).
// Pixel(클라)은 PageView만, 전환(가입/체험완료/구매)은 여기서만 보내 중복을 원천 차단.
import { createHash } from "crypto";
import { logError } from "./logger";

const PIXEL_ID = process.env.NEXT_PUBLIC_META_PIXEL_ID;
const ACCESS_TOKEN = process.env.META_CAPI_ACCESS_TOKEN;

type CapiEventName =
  | "CompleteRegistration" // 카카오 가입
  | "StartTrial" // 무료 리딩(체험) 완료
  | "Purchase"; // 별 충전 결제

export async function sendCapiEvent(params: {
  eventName: CapiEventName;
  userId: string; // external_id 로 해시 전송
  eventId: string; // 멱등/중복제거 키 (예: reg:{userId}, purchase:{paymentId})
  value?: number; // Purchase 원화 금액
  fbp?: string | null; // _fbp 쿠키
  fbc?: string | null; // _fbc 쿠키
  clientIp?: string | null;
  userAgent?: string | null;
  sourceUrl?: string;
}): Promise<void> {
  if (!PIXEL_ID || !ACCESS_TOKEN) return; // 미설정 시 no-op (dev 안전)
  try {
    const body = {
      data: [
        {
          event_name: params.eventName,
          event_time: Math.floor(Date.now() / 1000),
          event_id: params.eventId,
          action_source: "website",
          event_source_url: params.sourceUrl ?? "https://byeolkongtalk.com",
          user_data: {
            external_id: [
              createHash("sha256").update(params.userId).digest("hex"),
            ],
            ...(params.fbp ? { fbp: params.fbp } : {}),
            ...(params.fbc ? { fbc: params.fbc } : {}),
            ...(params.clientIp ? { client_ip_address: params.clientIp } : {}),
            ...(params.userAgent ? { client_user_agent: params.userAgent } : {}),
          },
          ...(params.eventName === "Purchase"
            ? { custom_data: { currency: "KRW", value: params.value ?? 0 } }
            : {}),
        },
      ],
    };
    const res = await fetch(
      `https://graph.facebook.com/v21.0/${PIXEL_ID}/events?access_token=${ACCESS_TOKEN}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(body),
      }
    );
    if (!res.ok) {
      const text = await res.text();
      await logError(new Error(`CAPI ${params.eventName} failed: ${text}`), {
        route: "lib/meta-capi",
        userId: params.userId,
      });
    }
  } catch (e) {
    await logError(e, { route: "lib/meta-capi", userId: params.userId });
  }
}

/** 요청에서 CAPI 매칭용 신호 추출 (fbp/fbc 쿠키 + IP + UA) */
export function capiSignalsFromRequest(request: Request): {
  fbp: string | null;
  fbc: string | null;
  clientIp: string | null;
  userAgent: string | null;
} {
  const cookie = request.headers.get("cookie") ?? "";
  const pick = (name: string) =>
    cookie.match(new RegExp(`(?:^|;\\s*)${name}=([^;]+)`))?.[1] ?? null;
  return {
    fbp: pick("_fbp"),
    fbc: pick("_fbc"),
    clientIp:
      request.headers.get("x-forwarded-for")?.split(",")[0]?.trim() ?? null,
    userAgent: request.headers.get("user-agent"),
  };
}
```

- [ ] **Step 2: 타입 체크 + 커밋**

```bash
npx tsc --noEmit
git add lib/meta-capi.ts
git commit -m "feat(ads): Meta Conversions API 서버 유틸"
```

### Task 8: 전환 이벤트 발화 3곳

**Files:**
- Modify: `app/api/auth/kakao/route.ts` (신규 가입 분기 — Task 1 웰컴 별 코드 옆)
- Modify: `app/api/payment/confirm/route.ts` (`chargeStars` 성공 뒤)
- Modify: `app/api/consultations/saju/chat/route.ts` + `app/api/consultations/tarot/chat/route.ts` (스트림 완료 후 DB 기록 지점) — 첫 리딩이면 StartTrial. 판단: 해당 유저 `readings` 중 메시지 기록이 남은 완료 리딩이 이번이 처음인지 count 쿼리. one-shot 운세는 `app/api/fortune/create/route.ts` 리포트 저장 성공 지점에 동일 패턴.

- [ ] **Step 1: 가입 이벤트** — 카카오 콜백 신규 유저 분기(웰컴 별 지급 코드 옆)에:

```typescript
      const signals = capiSignalsFromRequest(request);
      void sendCapiEvent({
        eventName: "CompleteRegistration",
        userId: user.id,
        eventId: `reg:${user.id}`,
        ...signals,
      });
```

import: `import { sendCapiEvent, capiSignalsFromRequest } from "@/lib/meta-capi";`

- [ ] **Step 2: 구매 이벤트** — `app/api/payment/confirm/route.ts` 첫 충전 보너스 블록(Task 2) 뒤에:

```typescript
    const signals = capiSignalsFromRequest(request);
    void sendCapiEvent({
      eventName: "Purchase",
      userId,
      eventId: `purchase:${payment.id}`,
      value: amount,
      ...signals,
    });
```

- [ ] **Step 3: 체험완료 이벤트** — 각 chat 라우트의 "스트림 완료 후 messages INSERT" 지점과 fortune create의 리포트 저장 성공 지점에, 첫 완료 여부 확인 후:

```typescript
    // 이 유저의 첫 리딩 완료면 StartTrial (CAPI 상향 최적화용 시그널)
    const { count: doneCount } = await supabase
      .from("readings")
      .select("id", { count: "exact", head: true })
      .eq("user_id", userId);
    if ((doneCount ?? 0) <= 1) {
      const signals = capiSignalsFromRequest(request);
      void sendCapiEvent({
        eventName: "StartTrial",
        userId,
        eventId: `trial:${userId}`,
        ...signals,
      });
    }
```

주의: `eventId`가 `trial:{userId}` 고정이라 메타 측에서 48시간 내 중복은 자동 dedup — count 판정이 러프해도 안전.

- [ ] **Step 4: 타입 체크 + 커밋**

```bash
npx tsc --noEmit
git add app/api/auth/kakao/route.ts app/api/payment/confirm/route.ts app/api/consultations app/api/fortune
git commit -m "feat(ads): CAPI 전환 이벤트 — 가입/체험완료/구매"
```

### Task 9: 광고 딥링크 확정 + Phase 3 검증·배포

- [ ] **Step 1: 딥링크 3개 확정** (신규 페이지 없이 기존 라우트 + UTM. `utm_content`가 소재 식별자):

```
소재1 상담:  https://byeolkongtalk.com/?utm_source=meta&utm_medium=paid&utm_campaign=launch_2026q3&utm_content=counsel
소재2 운세:  https://byeolkongtalk.com/fortune/daily?utm_source=meta&utm_medium=paid&utm_campaign=launch_2026q3&utm_content=daily
소재3 타로:  https://byeolkongtalk.com/tarot?utm_source=meta&utm_medium=paid&utm_campaign=launch_2026q3&utm_content=tarot
```

메타 광고가 자동으로 `fbclid`를 붙이고 Pixel이 `_fbc` 쿠키로 저장 → CAPI 매칭에 사용되므로 별도 UTM 저장 코드는 이번 범위에서 생략(YAGNI — 소재별 성과는 메타 관리자 화면의 광고별 분해로 충분).

주의(스펙 §1 대비 단순화): 소재1을 `/concern` 직행으로 붙이는 게 이상적이나 `/concern`은 `sessionStorage.byeolkong:emotion` 선행을 전제한다. 런칭은 `/` 홈으로 시작하고, 2주 판독에서 "클릭 OK·가입 약함"이 나오면 그때 `/concern` 직행(emotion 없이 진입 허용) 개선을 랜딩 개선 카드로 올린다.

- [ ] **Step 2: env 등록** — Vercel(dev/prod 스코프)에 `NEXT_PUBLIC_META_PIXEL_ID`, `META_CAPI_ACCESS_TOKEN` 추가(값은 Phase 4에서 발급) 후 redeploy. 미설정 상태에선 코드가 no-op이라 배포 순서 자유.

- [ ] **Step 3: dev push + 검증**

```bash
git push origin dev
```

dev에서: 새 시크릿창 가입 → Meta Events Manager(테스트 이벤트 탭)에서 `CompleteRegistration` 수신 확인 → 무료 리딩 완료 → `StartTrial` 확인 → 테스트 결제 → `Purchase`(value=1000) 확인.

- [ ] **Step 4: prod 배포**

```bash
git checkout main && git merge --ff-only dev && git push origin main && git checkout dev
```

---

## Phase 4 — 메타 등록 & 런칭 (운영 가이드, 코드 없음)

### Task 10: 계정·픽셀 세팅

- [ ] **Step 1:** [business.facebook.com](https://business.facebook.com) → 비즈니스 포트폴리오 생성(개인 페이스북 계정 필요).
- [ ] **Step 2:** 자산: **페이스북 페이지**(별콩톡) + **인스타그램 계정** 연결(인스타는 소재 게시 주체).
- [ ] **Step 3:** 이벤트 관리자 → **픽셀(데이터 세트) 생성** → Pixel ID 복사 → Vercel `NEXT_PUBLIC_META_PIXEL_ID`에 등록.
- [ ] **Step 4:** 같은 화면 → 설정 → **Conversions API 액세스 토큰 생성** → `META_CAPI_ACCESS_TOKEN` 등록 → redeploy → Task 9 Step 3 검증 통과 확인.
- [ ] **Step 5:** 결제 수단 등록(광고 계정 설정) + 광고 계정 시간대·통화(KRW) 확인.

### Task 11: 캠페인 생성 (스펙 §5·§6 그대로)

- [ ] **Step 1:** 광고 관리자 → 캠페인 생성 → 목표 **판매/전환** → 전환 위치 웹사이트 → 성과 목표 **전환 최대화**, 전환 이벤트 = **CompleteRegistration** (초기. 가입 주간 수십 건 도달 시 StartTrial로 상향).
- [ ] **Step 2:** 애드셋 **1개**: 위치 대한민국 · 연령 20~39 · 성별 전체 · **Advantage+ 타겟팅(상세 타겟팅 비움)** · 노출 위치 Advantage+(자동) · 일 예산 **₩10,000**.
- [ ] **Step 3:** 광고 3개 등록(소재 1/2/3): 각각 피드용 4:5 + 스토리용 9:16 업로드, 기본 문구 = 소재 카피, 랜딩 URL = Task 9의 딥링크(소재별 매칭), CTA 버튼 = "더 알아보기".
- [ ] **Step 4:** 게시 → 심사 통과 확인(보통 24h 내. 점술 카테고리는 과장 문구에 민감 — 카피에 "무조건", "100%" 류 금지. 우리 카피는 해당 없음).

### Task 12: 운영 규칙 (첫 4주)

- [ ] **Week 1~2: 손대지 않기** — 학습기. 소재 끄기/예산 변경 금지. 볼 것: 노출이 나가는지, Events Manager에 이벤트가 흐르는지.
- [ ] **Week 2 끝: 1차 판독** — 광고별 CTR·CPC·가입 CPA 기록(우리 실측 벤치마크 갱신). CTR 바닥(<0.5%) 소재만 교체 검토.
- [ ] **Week 3~4: 판정** — 스펙 §7 결정 규칙 적용:
  - 가입 CPA 양호 + 체험→결제 신호 → 예산 증액 + 최적화 이벤트 StartTrial 상향 + 바이럴(B) 기획
  - 클릭 OK·가입 약함 → 랜딩/오퍼 개선
  - CTR 낮음 → Phase 2 재가동(소재 교체)
  - CAC vs LTV 판정 → 스케일 or 유료 축소·오가닉 피벗

---

## 실행 순서 요약

```
Phase 0 (너, 5분) ─┐
Phase 1 (코드) ────┼→ Phase 2 (소재, 나+너) ─┐
Phase 3 (코드) ────┘                          ├→ Phase 4 (메타 등록·런칭)
                                              ┘
```

Phase 1·3은 연속 코드 작업으로 한 번에 진행 가능. Phase 2는 Phase 0 완료 후 아무 때나. Phase 4는 전부 모인 뒤.
