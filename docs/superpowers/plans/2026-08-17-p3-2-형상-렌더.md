# 별자리(byeoljari) P3-2 형상 렌더 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별자리 관계망에 오행 신수(형상)를 얹는다 — 상단 엠블럼 헤더, 은은한 배경 레이어, 진화 힌트, 성장 리빌 축하.

**Architecture:** P2 GET 응답의 `nodes[]`(각 `element`)와 `nodes.length`(인원)만으로 순수 함수 `resolveShape`가 지배 오행·진화 단계·에셋 경로·다음 단계 힌트를 계산한다. 렌더는 기존 `ConstellationView`(헤더·힌트·리빌)와 `ConstellationCanvas`(은은 배경 `<image>`) 두 컴포넌트를 수정해서 얹는다. 에셋 15종 투명 PNG는 이미 제작·커밋 완료(`public/byeoljari/creatures/`).

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript strict · SVG 인라인 렌더 · Tailwind v4 · 순수함수 TDD(`node:test`).

---

## 배경 / 전제 (구현 전 필독)

- **이 작업은 dev 브랜치에서 직접 진행**(P1~P3-3 전부 dev 직접 작업했음. worktree 미사용). 현재 워킹트리 clean, `dev`=`origin/dev` 동기화.
- **에셋 이미 완료**: `public/byeoljari/creatures/{wood,fire,earth,metal,water}-{1,2,3}.png` 15종. 오행 매핑 목=wood·화=fire·토=earth·금=metal·수=water, 단계 1/2/3. 전부 배경 투명·정면·둥근 chibi.
- **정본 스펙**: `docs/superpowers/specs/2026-08-16-p3-2-형상-렌더-design.md`. 사용자 확정 결정: ①엠블럼=상단 헤더 ②은은 배경 opacity ~10% ③성장 연출=둘 다(힌트+리빌) ④단계=1~4명:1단계 / 5~14명:2단계 / 15명+:3단계(임계 5·15).
- **기존 코드 계약(수정 금지 대상)**:
  - `lib/byeoljari/types.ts` — `GraphNode { id, name: string|null, isHost, relationType, element: FiveElement, compatVisible }` · `StarGraph { ok, shareId, claimed, nodes, edges, triads }`.
  - `lib/saju/elements.ts` — `export type FiveElement = "목"|"화"|"토"|"금"|"수"`.
  - `lib/byeoljari/display.ts` — 방어적 접근자 패턴(enum 밖 값 폴백). `subjectParticle`(이/가) 조사 함수가 여기 있음 — 신규 조사 함수도 여기 형제로 추가.
  - `lib/byeoljari/scale.ts` `SizeSpec` + `scaleForCount` — 인원 적응 크기. 건드리지 않음.
- **테스트 규칙**: `node:test` + `node:assert/strict`. import 경로에 **`.ts` 확장자 명시**(`from "./shape.ts"`). 실행 `node --import tsx --test <files>`. package.json에 test 스크립트 없음(직접 호출).
- **방어적 렌더 규칙**([[recurring-crash-class-config-by-dynamic-key]]): 동적 키로 config 조회 시 반드시 폴백. `resolveShape`/조사 함수는 범위 밖 입력에 크래시 금지.
- **한글 답변·주석**([[always-korean-replies]]).

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lib/byeoljari/shape.ts` | 지배 오행·진화 단계·에셋 경로·힌트·리빌 판정 순수 로직 | **신규** |
| `lib/byeoljari/shape.test.ts` | 위 순수함수 단위 테스트 | **신규** |
| `lib/byeoljari/display.ts` | 방향 조사 `directionParticle`(으로/로) 추가 | 수정 |
| `lib/byeoljari/display.test.ts` | `directionParticle` 테스트 추가 | 수정 |
| `components/byeoljari/ConstellationCanvas.tsx` | `shape` prop + 은은 배경 `<image>`(줌 무관 고정, water-3 축소) | 수정 |
| `components/byeoljari/ConstellationView.tsx` | 헤더(엠블럼+이름)·진행 힌트·리빌 오버레이·`shape` 계산·Canvas 전달 | 수정 |
| `app/fortune/byeoljari/[shareId]/page.tsx` | 중복 `<h1>우리 별자리</h1>` 제거(헤더로 통일) | 수정 |

**태스크 순서:** 순수 로직(T1·T2) → 렌더 배경(T3) → 헤더·힌트(T4) → 리빌(T5) → 통합 검증(T6). 각 태스크는 독립 커밋.

---

### Task 1: 형상 순수 로직 `lib/byeoljari/shape.ts` (TDD)

**Files:**
- Create: `lib/byeoljari/shape.ts`
- Test: `lib/byeoljari/shape.test.ts`

지배 오행(`dominantElement`), 진화 단계(`evolutionStage`), 형상 정보(`resolveShape`), 리빌 판정(`shouldReveal`)을 한 파일에 모은다. 전부 순수·방어적.

- [ ] **Step 1: 실패하는 테스트 작성**

Create `lib/byeoljari/shape.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { dominantElement, evolutionStage, resolveShape, shouldReveal } from "./shape.ts";
import type { GraphNode } from "./types.ts";
import type { FiveElement } from "../saju/elements.ts";

function node(element: FiveElement, isHost = false): GraphNode {
  return {
    id: element + (isHost ? "-h" : "-" + Math.random().toString(36).slice(2)),
    name: null,
    isHost,
    relationType: "friend",
    element,
    compatVisible: false,
  };
}

test("dominantElement — 최빈 오행", () => {
  assert.equal(dominantElement([node("목"), node("목"), node("화")]), "목");
});

test("dominantElement — 동률이면 호스트 오행 우선", () => {
  // 목2 화2, 호스트=화 → 화
  assert.equal(dominantElement([node("목"), node("목"), node("화", true), node("화")]), "화");
});

test("dominantElement — 동률이고 호스트가 소수 오행이면 고정순서(목화토금수)", () => {
  // 목1 화1, 호스트=토(1) → 동률 목/화 중 목(먼저); 호스트 토는 최빈 아님
  assert.equal(dominantElement([node("목"), node("화"), node("토", true)]), "목");
});

test("dominantElement — 빈 입력 null", () => {
  assert.equal(dominantElement([]), null);
});

test("evolutionStage — 경계 1/5/15", () => {
  assert.equal(evolutionStage(1), 1);
  assert.equal(evolutionStage(4), 1);
  assert.equal(evolutionStage(5), 2);
  assert.equal(evolutionStage(14), 2);
  assert.equal(evolutionStage(15), 3);
  assert.equal(evolutionStage(30), 3);
});

test("resolveShape — stage1 목: 청개구리 + 다음 이무기 힌트", () => {
  const s = resolveShape([node("목", true)]); // 1명 stage1
  assert.equal(s?.element, "목");
  assert.equal(s?.stage, 1);
  assert.equal(s?.assetSrc, "/byeoljari/creatures/wood-1.png");
  assert.equal(s?.name, "청개구리");
  assert.equal(s?.nextName, "이무기");
  assert.equal(s?.membersToNext, 4); // 5-1
});

test("resolveShape — stage2 경계(5명): 다음까지 10명", () => {
  const nodes = Array.from({ length: 5 }, (_, i) => node("화", i === 0)); // 5명 화 stage2
  const s = resolveShape(nodes);
  assert.equal(s?.stage, 2);
  assert.equal(s?.name, "불새");
  assert.equal(s?.nextName, "봉황");
  assert.equal(s?.membersToNext, 10); // 15-5
});

test("resolveShape — stage3(15명)는 next 없음", () => {
  const nodes = Array.from({ length: 15 }, (_, i) => node("토", i === 0));
  const s = resolveShape(nodes);
  assert.equal(s?.stage, 3);
  assert.equal(s?.name, "기린");
  assert.equal(s?.nextName, null);
  assert.equal(s?.membersToNext, null);
  assert.equal(s?.assetSrc, "/byeoljari/creatures/earth-3.png");
});

test("resolveShape — 빈 입력 null", () => {
  assert.equal(resolveShape([]), null);
});

test("shouldReveal — 첫 생성 stage1은 리빌 안 함", () => {
  assert.equal(shouldReveal(null, 1), false);
});

test("shouldReveal — 저장 없고 이미 stage2(링크로 큰 별자리 첫 방문) 리빌", () => {
  assert.equal(shouldReveal(null, 2), true);
});

test("shouldReveal — 성장(1→2) 리빌", () => {
  assert.equal(shouldReveal("1", 2), true);
});

test("shouldReveal — 재방문(동일 stage) 리빌 안 함", () => {
  assert.equal(shouldReveal("2", 2), false);
});

test("shouldReveal — 파싱 실패는 baseline 1", () => {
  assert.equal(shouldReveal("abc", 1), false);
  assert.equal(shouldReveal("abc", 2), true);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --import tsx --test lib/byeoljari/shape.test.ts`
Expected: FAIL — `Cannot find module './shape.ts'` (또는 export 없음).

- [ ] **Step 3: 최소 구현**

Create `lib/byeoljari/shape.ts`:

```ts
// 별자리 형상(오행 신수) 순수 로직. 지배 오행 → 진화 단계 → 에셋/이름/힌트/리빌 판정.
// 전부 순수·방어적(범위 밖 값 폴백). 에셋: public/byeoljari/creatures/{code}-{stage}.png.
import type { FiveElement } from "@/lib/saju/elements";
import type { GraphNode } from "./types";

// 오행 → 에셋 파일 코드
export const ELEMENT_CODE: Record<FiveElement, string> = {
  목: "wood",
  화: "fire",
  토: "earth",
  금: "metal",
  수: "water",
};

// 15 신수 이름 [1단계, 2단계, 3단계]
export const SHAPE_NAME: Record<FiveElement, [string, string, string]> = {
  목: ["청개구리", "이무기", "청룡"],
  화: ["반딧불이", "불새", "봉황"],
  토: ["옥토끼", "사슴", "기린"],
  금: ["흰담비", "눈표범", "백호"],
  수: ["물고기", "가오리", "거북이"],
};

const ELEMENT_ORDER: FiveElement[] = ["목", "화", "토", "금", "수"];

/** 최빈 오행. 동률이면 호스트 오행 우선, 그다음 고정순서(목화토금수). 빈 입력 null. */
export function dominantElement(nodes: GraphNode[]): FiveElement | null {
  if (nodes.length === 0) return null;
  const count = new Map<string, number>();
  for (const n of nodes) count.set(n.element, (count.get(n.element) ?? 0) + 1);
  const host = nodes.find((n) => n.isHost);
  let best: FiveElement = ELEMENT_ORDER[0];
  let bestC = -1;
  for (const e of ELEMENT_ORDER) {
    const c = count.get(e) ?? 0;
    if (c > bestC) {
      best = e;
      bestC = c;
    }
  }
  // 동률 중 호스트 오행이 있으면 그것 우선
  if (host && (count.get(host.element) ?? 0) === bestC) return host.element;
  return best;
}

/** 인원 → 진화 단계. 1~4:1 · 5~14:2 · 15+:3. */
export function evolutionStage(count: number): 1 | 2 | 3 {
  if (count < 5) return 1;
  if (count < 15) return 2;
  return 3;
}

export interface ShapeInfo {
  element: FiveElement;
  stage: 1 | 2 | 3;
  assetSrc: string; // /byeoljari/creatures/wood-3.png
  name: string; // 현재 형상 이름
  nextName: string | null; // 다음 단계 형상 이름(있으면)
  membersToNext: number | null; // 다음 단계까지 남은 인원(있으면)
}

/** 노드 집합 → 형상 정보(엠블럼·배경·힌트·리빌이 소비). 빈 입력 null. */
export function resolveShape(nodes: GraphNode[]): ShapeInfo | null {
  const element = dominantElement(nodes);
  if (!element) return null;
  const stage = evolutionStage(nodes.length);
  const names = SHAPE_NAME[element];
  const THRESH = [5, 15]; // stage1→2, stage2→3
  let nextName: string | null = null;
  let membersToNext: number | null = null;
  if (stage !== 3) {
    // stage!==3 (not <3): TS는 관계연산자로 number-literal union을 narrow 못함 → names[stage] 타입에러
    nextName = names[stage]; // 0-index: stage(1)→names[1]=2단계 이름
    membersToNext = THRESH[stage - 1] - nodes.length;
  }
  return {
    element,
    stage,
    assetSrc: `/byeoljari/creatures/${ELEMENT_CODE[element]}-${stage}.png`,
    name: names[stage - 1],
    nextName,
    membersToNext,
  };
}

/**
 * 성장 리빌 오버레이를 띄울지. storedRaw=localStorage seenStage 원본(없으면 null).
 * 기준선 = 저장값(유효한 1 이상) 아니면 1 → 첫 형상(stage1)은 리빌 안 함.
 * 현재 stage가 기준선 초과면 리빌.
 */
export function shouldReveal(storedRaw: string | null, stage: 1 | 2 | 3): boolean {
  const n = Number(storedRaw);
  const baseline = Number.isFinite(n) && n >= 1 ? n : 1;
  return stage > baseline;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --import tsx --test lib/byeoljari/shape.test.ts`
Expected: PASS — 12 tests pass.

- [ ] **Step 5: 커밋**

```bash
git add lib/byeoljari/shape.ts lib/byeoljari/shape.test.ts
git commit -m "feat(byeoljari): P3-2 형상 순수 로직(지배오행·진화단계·에셋·리빌 판정) TDD"
```

---

### Task 2: 방향 조사 `directionParticle` (display.ts, TDD)

진행 힌트("{nextName}(으)로 진화") · 리빌("{name}(으)로 진화했어")에 필요. 받침 유무로 "으로/로" 선택.

**Files:**
- Modify: `lib/byeoljari/display.ts` (파일 끝에 함수 추가)
- Test: `lib/byeoljari/display.test.ts` (테스트 추가)

- [ ] **Step 1: 실패하는 테스트 추가**

`lib/byeoljari/display.test.ts` 파일 상단 import에 `directionParticle`을 추가하고(기존 import 구문에 이름만 추가), 파일 끝에 아래 테스트 블록 추가:

```ts
test("directionParticle — 받침 없으면 로", () => {
  assert.equal(directionParticle("이무기"), "로");
  assert.equal(directionParticle("백호"), "로");
  assert.equal(directionParticle("가오리"), "로");
});

test("directionParticle — 받침 있으면 으로", () => {
  assert.equal(directionParticle("청룡"), "으로");
  assert.equal(directionParticle("기린"), "으로");
  assert.equal(directionParticle("사슴"), "으로");
});

test("directionParticle — ㄹ받침은 로(예외)", () => {
  assert.equal(directionParticle("서울"), "로");
});

test("directionParticle — 한글 완성형 밖은 로", () => {
  assert.equal(directionParticle("abc"), "로");
});
```

> ⚠️ import 구문 예시(기존 파일 형태에 맞춰 이름만 추가): `import { subjectParticle, directionParticle } from "./display.ts";` — 실제 기존 import 목록을 보고 `directionParticle`만 끼워넣을 것. 이미 다른 이름들이 import 돼 있으면 그 목록에 추가.

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --import tsx --test lib/byeoljari/display.test.ts`
Expected: FAIL — `directionParticle is not a function` / export 없음.

- [ ] **Step 3: 최소 구현**

`lib/byeoljari/display.ts` 파일 끝(기존 `subjectParticle` 아래)에 추가:

```ts
// 방향 조사 (으)로. 받침 없거나 ㄹ받침 → "로", 그 외 받침 → "으로". 한글 완성형만; 그 외 "로".
export function directionParticle(word: string): "으로" | "로" {
  const last = word.trim().slice(-1);
  const code = last.charCodeAt(0);
  if (Number.isNaN(code) || code < 0xac00 || code > 0xd7a3) return "로"; // 한글 완성형 밖
  const jong = (code - 0xac00) % 28; // 종성 인덱스: 0=받침없음, 8=ㄹ
  return jong === 0 || jong === 8 ? "로" : "으로";
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --import tsx --test lib/byeoljari/display.test.ts`
Expected: PASS — 기존 테스트 + 신규 4 tests pass.

- [ ] **Step 5: 커밋**

```bash
git add lib/byeoljari/display.ts lib/byeoljari/display.test.ts
git commit -m "feat(byeoljari): P3-2 방향 조사 directionParticle(으로/로, ㄹ예외) TDD"
```

---

### Task 3: 은은 배경 레이어 `ConstellationCanvas.tsx`

밤하늘 `<rect>` 위·관계선/노드 `<g>` 아래에 형상 이미지를 opacity ~0.1로 깐다. **줌 transform `<g>` 밖**에 둬서 노드/선만 줌되고 배경은 고정.

**Files:**
- Modify: `components/byeoljari/ConstellationCanvas.tsx`

- [ ] **Step 1: `shape` prop 추가**

파일 상단 import에 `ShapeInfo` 타입 추가:

```ts
import type { ShapeInfo } from "@/lib/byeoljari/shape";
```

`Props` interface에 optional 필드 추가(기존 필드 유지, 아래 한 줄 삽입):

```ts
  shape?: ShapeInfo | null; // 은은 배경 형상(없으면 배경 생략)
```

컴포넌트 함수 시그니처의 구조분해에 `shape`를 추가:

```ts
export default function ConstellationCanvas({
  graph,
  layout,
  meId,
  transform,
  sizes,
  activeFilter,
  shape,
  onSelect,
}: Props) {
```

- [ ] **Step 2: 배경 크기 계산 + `<image>` 렌더**

컴포넌트 함수 본문 최상단(`const nodeById = ...` 위)에 배경 크기 계산 추가:

```ts
  // 은은 배경: 중앙 60×60. water-3(거북이)만 다른 신수보다 커서 ~0.9배(54)로 축소.
  const bgSmall = shape ? shape.element === "수" && shape.stage === 3 : false;
  const bgSize = bgSmall ? 54 : 60;
  const bgOff = (100 - bgSize) / 2;
```

JSX에서 `<rect ... />` **바로 아래**, 줌 `<g>` **위**에 삽입:

```tsx
      <rect x="0" y="0" width="100" height="100" fill="#1F1735" />
      {/* 은은 배경 형상 — 밤하늘 위·노드/선 아래, 줌 transform 밖(고정 ambient). */}
      {shape && (
        <image
          href={shape.assetSrc}
          x={bgOff}
          y={bgOff}
          width={bgSize}
          height={bgSize}
          opacity={0.1}
          preserveAspectRatio="xMidYMid meet"
        />
      )}
      <g
        style={{ transition: "transform 420ms cubic-bezier(0.22,0.68,0.28,1)" }}
        transform={`translate(${transform.tx} ${transform.ty}) scale(${transform.s})`}
      >
```

- [ ] **Step 3: tsc 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음(exit 0). `shape` prop이 optional이라 미전달 호출부(View는 T4에서 전달)도 아직 통과.

- [ ] **Step 4: 커밋**

```bash
git add components/byeoljari/ConstellationCanvas.tsx
git commit -m "feat(byeoljari): P3-2 은은 배경 형상 레이어(줌 무관 고정·opacity 0.1·water-3 축소)"
```

---

### Task 4: 상단 헤더 + 진행 힌트 + shape 전달 `ConstellationView.tsx`

`resolveShape`를 한 번 계산해서 헤더(엠블럼+이름)·진행 힌트를 렌더하고 Canvas에 `shape`를 전달. 페이지의 중복 `<h1>` 제거.

**Files:**
- Modify: `components/byeoljari/ConstellationView.tsx`
- Modify: `app/fortune/byeoljari/[shareId]/page.tsx`

- [ ] **Step 1: import + shape 계산**

`ConstellationView.tsx` 상단 import에 추가:

```ts
import { resolveShape } from "@/lib/byeoljari/shape";
import { directionParticle } from "@/lib/byeoljari/display";
```

> ⚠️ `directionParticle`은 기존 `@/lib/byeoljari/display` import 줄에 이름만 추가해도 됨(기존: `STAR_ELEMENT_COLORS, RELATION_TYPE_LABEL, relationTypeLabel`). 별도 줄로 둬도 무방.

컴포넌트 본문 `sizes` useMemo 아래에 shape 계산 추가:

```ts
  const shape = useMemo(() => resolveShape(graph.nodes), [graph.nodes]);
```

- [ ] **Step 2: 헤더 렌더**

`return (` 직후 최상단 `<div>` 바로 안, 필터 칩(`{showFilter && ...}`) **위**에 헤더 삽입:

```tsx
  return (
    <div>
      {/* 형상 엠블럼 헤더 — "우리 별자리 · {신수이름}". shape null(노드0)이면 텍스트만. */}
      <h1 className="mb-3 flex items-center justify-center gap-2 font-display text-lg text-eye-purple">
        {shape && (
          <img
            src={shape.assetSrc}
            alt=""
            className="h-14 w-14 object-contain"
            style={
              shape.element === "수" && shape.stage === 3
                ? { transform: "scale(0.9)" }
                : undefined
            }
          />
        )}
        우리 별자리{shape ? ` · ${shape.name}` : ""}
      </h1>
      {showFilter && (
```

- [ ] **Step 3: Canvas에 shape 전달**

`<ConstellationCanvas ... />` 호출부 props에 `shape={shape}` 추가(기존 props 유지):

```tsx
        <ConstellationCanvas
          graph={graph}
          layout={layout}
          meId={meId}
          transform={transform}
          sizes={sizes}
          activeFilter={effectiveFilter}
          shape={shape}
          onSelect={handleSelect}
        />
```

- [ ] **Step 4: 진행 힌트 렌더**

LEGEND `<div className="mt-3 ...">` 블록 **아래**(그 닫는 `</div>` 다음, 최상단 `</div>` 앞)에 힌트 삽입:

```tsx
      {shape?.membersToNext != null && shape.nextName && (
        <p className="mt-3 text-center text-sm text-eye-purple">
          ✨ {shape.membersToNext}명 더 오면 {shape.nextName}
          {directionParticle(shape.nextName)} 진화!
        </p>
      )}
      {shape?.stage === 3 && (
        <p className="mt-3 text-center text-sm text-eye-purple">✨ {shape.name} 완성!</p>
      )}
```

- [ ] **Step 5: 페이지 중복 h1 제거**

`app/fortune/byeoljari/[shareId]/page.tsx`에서 아래 줄 **삭제**(헤더가 View로 이동했으므로 중복):

```tsx
      <h1 className="mb-4 text-center font-display text-2xl text-eye-purple">우리 별자리</h1>
```

> 삭제 후에도 `loading`/`error` 상태 블록은 그대로 둘 것(그 상태엔 View가 없어 제목이 없지만, 로딩/에러 문구 자체가 화면을 채움 — 스펙 비스코프).

- [ ] **Step 6: tsc + 빌드 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

Run: `npm run build`
Expected: exit 0, `/fortune/byeoljari/[shareId]` 라우트 정상 빌드.

- [ ] **Step 7: 커밋**

```bash
git add components/byeoljari/ConstellationView.tsx app/fortune/byeoljari/[shareId]/page.tsx
git commit -m "feat(byeoljari): P3-2 형상 엠블럼 헤더+진화 힌트(shape 전달·페이지 h1 통일)"
```

---

### Task 5: 성장 리빌 오버레이 `ConstellationView.tsx`

localStorage `byeoljari:seenStage:{shareId}`로 마지막 본 단계를 추적. 현재 단계가 더 크면 축하 오버레이 1회. 내가 채워 넘든 남이 넘든 다음 방문에 리빌.

**Files:**
- Modify: `components/byeoljari/ConstellationView.tsx`

- [ ] **Step 1: import + state + effect**

상단 `import { useMemo, useState } from "react";`를 아래로 교체(useEffect 추가):

```ts
import { useEffect, useMemo, useState } from "react";
```

`shape` import 줄에 `shouldReveal` 추가:

```ts
import { resolveShape, shouldReveal } from "@/lib/byeoljari/shape";
```

컴포넌트 본문 state 선언부(`const [activeFilter, ...]` 아래)에 추가:

```ts
  const [reveal, setReveal] = useState(false);
```

`shape` useMemo 아래에 effect 추가:

```ts
  useEffect(() => {
    if (!shape) return;
    const key = `byeoljari:seenStage:${graph.shareId}`;
    const stored = localStorage.getItem(key);
    if (shouldReveal(stored, shape.stage)) setReveal(true);
    localStorage.setItem(key, String(shape.stage)); // 리빌 여부와 무관하게 기준선 갱신
  }, [graph.shareId, shape?.stage]); // eslint-disable-line react-hooks/exhaustive-deps
```

> `shape` 객체 전체가 아니라 `shape?.stage`만 deps로 두는 이유: 같은 그래프면 stage 불변 → effect 재실행 방지. `shape` 참조는 useMemo라 nodes 바뀔 때만 갱신되지만, 안전하게 stage로 좁힘.

- [ ] **Step 2: 오버레이 JSX**

최상단 `<div>`의 닫는 `</div>` **직전**(진행 힌트 아래)에 오버레이 삽입:

```tsx
      {reveal && shape && (
        <div
          className="fixed inset-0 z-50 flex flex-col items-center justify-center bg-night/80 animate-fade-in"
          onClick={() => setReveal(false)}
          role="dialog"
          aria-label="형상 진화"
        >
          <img src={shape.assetSrc} alt="" className="h-40 w-40 object-contain" />
          <p className="mt-4 font-display text-xl text-gold">
            {shape.name}
            {directionParticle(shape.name)} 진화했어!
          </p>
          <button
            type="button"
            className="mt-6 rounded-xl bg-lilac-deep px-6 py-2 text-white"
            onClick={() => setReveal(false)}
          >
            닫기
          </button>
        </div>
      )}
```

- [ ] **Step 3: tsc + 빌드 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

Run: `npm run build`
Expected: exit 0.

- [ ] **Step 4: 커밋**

```bash
git add components/byeoljari/ConstellationView.tsx
git commit -m "feat(byeoljari): P3-2 성장 리빌 오버레이(seenStage localStorage·1회 축하)"
```

---

### Task 6: 통합 검증 + 브라우저 E2E + 최종 상태

**Files:** 없음(검증 전용). 발견 이슈 있으면 해당 파일 수정 후 재검증.

- [ ] **Step 1: 전체 유닛 회귀**

Run: `node --import tsx --test lib/byeoljari/*.test.ts`
Expected: PASS — 기존(share-id·validate·display·scale·layout·pairing) + 신규(shape) 전부 green. shape 12 + display 신규 4 포함.

- [ ] **Step 2: tsc + 프로덕션 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0.

- [ ] **Step 3: dev 서버 기동**

`.claude/launch.json`에 dev 서버 설정이 없으면 아래로 생성(있으면 그대로 사용):

```json
{
  "version": "0.0.1",
  "configurations": [
    { "name": "byeoljari-dev", "runtimeExecutable": "npm", "runtimeArgs": ["run", "dev"], "port": 3000 }
  ]
}
```

preview_start `{name: "byeoljari-dev"}` → 기동 확인.

- [ ] **Step 4: stage1(소규모) E2E**

navigate → `http://localhost:3000/fortune/byeoljari/HGRGPjuruS` (dev 2인 맵. 없으면 만들기 페이지로 새 맵 생성해 shareId 확보).
- read_page/screenshot로 확인:
  - **헤더**: "우리 별자리 · {신수이름}"(엠블럼 이미지 표시 — 목이면 청개구리 wood-1).
  - **은은 배경**: 밤하늘 중앙에 형상이 아주 연하게(opacity 0.1). 노드/선이 그 위에.
  - **진행 힌트**: "✨ N명 더 오면 {2단계}(으)로 진화!" — 조사 정확한지(예 "이무기로", "불새로").
- read_console_messages → 에러 0 확인.

- [ ] **Step 5: 줌 시 배경 고정 확인**

노드 하나 클릭(computer left_click) → 줌(1:1 패널) 진입.
- 배경 형상이 **줌과 함께 커지지 않고 고정**인지 확인(노드/선만 확대). 뒤로 → 복귀.

- [ ] **Step 6: stage3(대규모) + 완성 힌트**

navigate → `http://localhost:3000/fortune/byeoljari/hKQDZjRoOd` (dev 15인 맵 = stage3).
- 헤더에 3단계 신수(예 기린/봉황/청룡 등 지배 오행 따라) + 큰 엠블럼.
- 힌트 "✨ {신수} 완성!"(다음 단계 힌트 없음).
- water 지배로 거북이(water-3)면 배경/엠블럼이 살짝 작은지 확인(있으면).

- [ ] **Step 7: 리빌 오버레이 확인**

브라우저 콘솔(javascript_tool)로 seenStage를 낮춰 리빌 트리거:
```js
localStorage.setItem("byeoljari:seenStage:hKQDZjRoOd", "1"); location.reload();
```
- 리로드 후 **축하 오버레이**(큰 신수 + "{신수}(으)로 진화했어!" + 닫기 버튼 + fade-in) 표시 확인.
- 닫기 클릭 → 사라짐. 다시 리로드 → **재출현 안 함**(seenStage=3 저장됨) 확인.
- 검증 후 정리: `localStorage.removeItem("byeoljari:seenStage:hKQDZjRoOd")` (테스트 잔여 제거).

- [ ] **Step 8: 최종 상태 보고**

- 스크린샷(stage1 헤더+배경 / stage3 / 리빌 오버레이) 확보.
- 실패/이상 없으면 완료. 있으면 원인 파일 수정 → Step 1부터 재검증.
- 별도 커밋 불필요(검증 전용). 이슈 수정 시에만 커밋.

---

## Self-Review (플랜 작성자 체크 — 실행 전 확인 완료)

**1. 스펙 커버리지:**
- §1-1 엠블럼 상단 헤더 → T4 Step 2 ✓
- §1-2 은은 배경 opacity ~10% → T3 ✓
- §1-3 성장 연출 둘 다(힌트+리빌) → T4 Step 4(힌트) + T5(리빌) ✓
- §1-4 단계 1~4/5~14/15+ (임계 5·15) → T1 `evolutionStage` + 테스트 경계 ✓
- §2 shape.ts 순수 로직(dominantElement·evolutionStage·resolveShape) → T1 ✓ (+shouldReveal 추가)
- §3-1 헤더 "우리 별자리 · {name}" → T4 ✓
- §3-2 배경 `<image>` 줌 밖 고정·water-3 축소 → T3 ✓
- §3-3 진행 힌트 "{membersToNext}명 더…" / stage3 "완성" → T4 Step 4 ✓
- §3-4 리빌 localStorage seenStage·1회·fade-in → T5 ✓
- §4 파일(shape.ts신규·View·Canvas 수정, 에셋 완료) → File Structure ✓ (+페이지 h1 정리는 헤더 통일에 필요해 추가)

**2. 플레이스홀더 스캔:** 모든 코드 스텝에 실제 코드 있음. "적절히 처리" 류 없음 ✓

**3. 타입 정합성:** `ShapeInfo`(T1 정의) → Canvas prop(T3)·View 계산(T4)·리빌(T5)에서 동일 사용. `resolveShape`/`shouldReveal`/`directionParticle` 시그니처 T1·T2 정의와 T3~T5 호출 일치 ✓. `dominantElement`가 `FiveElement` 반환 → `ELEMENT_CODE`/`SHAPE_NAME` 키와 일치 ✓.

**관련:** 스펙 `specs/2026-08-16-p3-2-형상-렌더-design.md` · [[byeoljari-p3-2-asset-progress]] · [[star-inyeon-map-progress]] · [[recurring-crash-class-config-by-dynamic-key]].
