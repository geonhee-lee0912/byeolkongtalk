# 별마루 ⑤-a 오늘의 카드 (블록4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 별마루에 **오늘의 카드**(하루 1장 무료 원카드)를 신설한다 — 유저가 타로톡과 동일하게 뽑고(정/역 포함), 그날 고정. 무료=카드+키워드+정적 / 구독=LLM 개인화(비자격 블러) + 인라인 낙수.

**Architecture:** 카드 뱅크(`lib/tarot/cards.ts` 78장)와 드로우 UX(`components/tarot/CardDrawRitual`)를 **재사용**. 뽑은 카드는 신규 `byeolmaru_daily_card`(user·date·card·reversed)에 하루 1행으로 굳힌다(축적). 구독 LLM 해석은 ②-a `narrative`·3-b `pair-narrative` 패턴 미러(nano `generateOnce`·비자격 미호출=원가0).

**Tech Stack:** Next.js 16 · Supabase · `lib/tarot`(cards/CardDrawRitual) · `lib/claude` generateOnce · node:test.

**Spec:** `2026-09-05-별마루-5-원카드-폐지-낙수-design.md` §2. (5-b = good_days·one_card 폐지는 이 플랜 완료 후 = 무료 대체 실재 뒤.)

---

## File Structure
- Create `supabase/migrations/20260905020000_byeolmaru_daily_card.sql` — 테이블.
- Create `lib/byeolmaru/daily-card.ts` (+ `.test.ts`) — 조회/기록 래퍼.
- Create `app/api/byeolmaru/daily-card/route.ts` — GET(오늘 카드) · POST(뽑은 카드 저장).
- Modify `lib/byeolmaru/narrative-prompt.ts` — `buildCardNarrativeSystem` + `CARD_NARRATIVE_KICKOFF` (+ 테스트).
- Create `app/api/byeolmaru/card-narrative/route.ts` — 구독 LLM 해석.
- Create `components/byeolmaru/DailyCardBlock.tsx` — 뽑기(CardDrawRitual 재사용) + 무료 정적 + 구독/블러 + 인라인 낙수.
- Modify `components/byeolmaru/ByeolmaruView.tsx` — 블록4 배치.

Verify: `node --import tsx --test <file>` · `npx tsc --noEmit` · `npm run build`(`ƒ Proxy`). Commit: conventional + blank line + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. `@/` imports omit `.ts`; relative carry `.ts`.

---

## Task 1: 마이그레이션 `byeolmaru_daily_card`
**Files:** Create `supabase/migrations/20260905020000_byeolmaru_daily_card.sql`
- [ ] **Step 1** — `20260904200000_byeolmaru_checkins.sql` 패턴(RLS+REVOKE 3종+GRANT service_role) 확인 후:
```sql
-- 20260905020000_byeolmaru_daily_card.sql — 별마루 오늘의 카드(하루 1행). 유저가 뽑은 원카드 고정.
-- 카드 데이터는 lib/tarot/cards.ts(78장) 정본, 이 테이블은 "그날 뽑은 카드" 링크뿐.
-- 스펙: docs/superpowers/specs/2026-09-05-별마루-5-원카드-폐지-낙수-design.md §2
CREATE TABLE IF NOT EXISTS byeolmaru_daily_card (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  card_date DATE NOT NULL,
  card_id INT NOT NULL,
  reversed BOOLEAN NOT NULL DEFAULT false,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, card_date)
);
CREATE INDEX IF NOT EXISTS idx_byeolmaru_daily_card_user ON byeolmaru_daily_card(user_id, card_date DESC);
ALTER TABLE byeolmaru_daily_card ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE byeolmaru_daily_card FROM PUBLIC, anon, authenticated;
GRANT ALL ON TABLE byeolmaru_daily_card TO service_role;
```
- [ ] **Step 2** — 커밋 `feat(byeolmaru): byeolmaru_daily_card 테이블 (오늘의 카드)`.

---

## Task 2: `daily-card.ts` 래퍼 (+테스트)
**Files:** Create `lib/byeolmaru/daily-card.ts`, `lib/byeolmaru/daily-card.test.ts`

- [ ] **Step 1: 실패 테스트** — 순수 검증은 카드 id 유효성 헬퍼만(DB 함수는 API 라이브로). `lib/tarot/cards.ts` `getCardCount()`(78) 재사용:
```ts
// lib/byeolmaru/daily-card.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { isValidCardId } from "./daily-card.ts";

test("isValidCardId: 0~77 만 허용", () => {
  assert.equal(isValidCardId(0), true);
  assert.equal(isValidCardId(77), true);
  assert.equal(isValidCardId(78), false);
  assert.equal(isValidCardId(-1), false);
  assert.equal(isValidCardId(1.5), false);
});
```
- [ ] **Step 2: 구현** — `getCardCount()` 로 상한을 뱅크에서 가져와 하드코딩 회피:
```ts
// lib/byeolmaru/daily-card.ts — 오늘의 카드 조회/기록 (DB 래퍼).
import { getServiceSupabase } from "@/lib/supabase";
import { getCardCount } from "@/lib/tarot/cards";

export interface DailyCard { cardId: number; reversed: boolean; }

export function isValidCardId(id: unknown): id is number {
  return typeof id === "number" && Number.isInteger(id) && id >= 0 && id < getCardCount();
}

export async function getTodayCard(userId: string, todayKst: string): Promise<DailyCard | null> {
  const { data } = await getServiceSupabase()
    .from("byeolmaru_daily_card")
    .select("card_id, reversed")
    .eq("user_id", userId).eq("card_date", todayKst).maybeSingle();
  return data ? { cardId: data.card_id, reversed: data.reversed } : null;
}

/** 하루 1장 멱등 기록 — 이미 오늘 카드가 있으면 그걸 반환(덮어쓰기 금지 = 재뽑기 방지). */
export async function recordDraw(userId: string, todayKst: string, cardId: number, reversed: boolean): Promise<DailyCard> {
  const existing = await getTodayCard(userId, todayKst);
  if (existing) return existing;
  const { error } = await getServiceSupabase()
    .from("byeolmaru_daily_card")
    .insert({ user_id: userId, card_date: todayKst, card_id: cardId, reversed });
  // 동시 뽑기(PK 충돌 23505)면 방금 들어간 상대의 카드를 반환 — 재조회로 수렴(덮어쓰기 없음).
  if (error) { const again = await getTodayCard(userId, todayKst); if (again) return again; throw error; }
  return { cardId, reversed };
}
```
- [ ] **Step 3** — 테스트 PASS·tsc 0·커밋 `feat(byeolmaru): daily-card 래퍼(하루 1장 멱등)`.

---

## Task 3: `daily-card` API (GET/POST)
**Files:** Create `app/api/byeolmaru/daily-card/route.ts`
- [ ] **Step 1** — `app/api/byeolmaru/calendar/route.ts` 의 세션·에러·kstDate 패턴 미러. GET=오늘 카드(없으면 null), POST=뽑은 카드 저장(멱등). `todayKst = kstDate(new Date().toISOString())`.
```ts
import { NextRequest, NextResponse } from "next/server";
import { getSession } from "@/lib/session";
import { kstDate } from "@/lib/admin-time";
import { getTodayCard, recordDraw, isValidCardId } from "@/lib/byeolmaru/daily-card";
import { logError, ctxFromRequest } from "@/lib/logger";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export async function GET(req: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required", code: "LOGIN_REQUIRED" }, { status: 401 });
  try {
    const card = await getTodayCard(userId, kstDate(new Date().toISOString()));
    return NextResponse.json({ card });
  } catch (err) {
    await logError(err, ctxFromRequest(req, { route: "/api/byeolmaru/daily-card", userId }));
    return NextResponse.json({ error: "internal" }, { status: 500 });
  }
}

export async function POST(req: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required", code: "LOGIN_REQUIRED" }, { status: 401 });
  let body: { cardId?: unknown; reversed?: unknown };
  try { body = await req.json(); } catch { return NextResponse.json({ error: "invalid_json" }, { status: 400 }); }
  if (!isValidCardId(body.cardId)) return NextResponse.json({ error: "invalid_card" }, { status: 400 });
  const reversed = body.reversed === true;
  try {
    const card = await recordDraw(userId, kstDate(new Date().toISOString()), body.cardId, reversed);
    return NextResponse.json({ card });
  } catch (err) {
    await logError(err, ctxFromRequest(req, { route: "/api/byeolmaru/daily-card", userId }));
    return NextResponse.json({ error: "internal" }, { status: 500 });
  }
}
```
- [ ] **Step 2** — tsc·build·커밋 `feat(byeolmaru): daily-card API(GET 오늘카드 / POST 뽑기 저장)`.

---

## Task 4: `buildCardNarrativeSystem` (+테스트)
**Files:** Modify `lib/byeolmaru/narrative-prompt.ts` (+테스트 추가)
- [ ] **Step 1: 실패 테스트** — `getCard`(cards.ts)로 실 카드. 프롬프트가 카드명·정/역·규칙을 담는지:
```ts
import { getCard } from "@/lib/tarot/cards";
import { buildCardNarrativeSystem, CARD_NARRATIVE_KICKOFF } from "./narrative-prompt.ts";
test("buildCardNarrativeSystem: 카드명·정역·규칙", () => {
  const a = calcSaju({ year: 1996, month: 4, day: 11, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
  const card = getCard(0)!; // The Fool
  const sys = buildCardNarrativeSystem(a, card, false, "임오");
  assert.ok(sys.includes(card.name_kr));
  assert.ok(/정위|역위/.test(sys));
  assert.ok(/반말/.test(sys) && /단정/.test(sys));
  assert.ok(CARD_NARRATIVE_KICKOFF.length > 0);
});
```
- [ ] **Step 2: 구현** — 기존 `loadCore`/`formatPillars` 재사용. import `import type { TarotCard } from "@/lib/tarot/cards";`:
```ts
export function buildCardNarrativeSystem(saju: SajuResult, card: TarotCard, reversed: boolean, todayGanji: string): string {
  const orient = reversed ? "역위" : "정위";
  const kw = (reversed ? card.reversed : card.upright).join(", ");
  return [
    loadCore(), "",
    "# 별마루 오늘의 카드",
    `너는 이 사람이 오늘 뽑은 타로 한 장을 그 사람의 사주로 풀어준다. 오늘 일진은 ${todayGanji}.`,
    `카드: ${card.name_kr} (${orient}). 키워드: ${kw}.`,
    `이 사람: ${formatPillars(saju)}.`,
    "규칙: 2~3문단, 반말, 단정적 예언 금지(흐름·가능성·선택). 카드 의미를 오늘 이 사람의 흐름에 얹어. 마지막은 따뜻한 한 줄. 별표/제목/마커 없이 줄글만.",
  ].join("\n");
}
export const CARD_NARRATIVE_KICKOFF = "오늘 내 카드 풀어줘.";
```
- [ ] **Step 3** — 테스트 PASS·tsc·커밋 `feat(byeolmaru): 오늘의 카드 서술 프롬프트`.

---

## Task 5: `card-narrative` 라우트 (구독 LLM)
**Files:** Create `app/api/byeolmaru/card-narrative/route.ts`
- [ ] **Step 1** — `pair-narrative/route.ts` 미러: 세션 → 자격 게이트(비자격 403, LLM 미호출) → 오늘 카드 조회(없으면 `{entitled:true, narrative:null}`) → self is_primary 사주 → `getCard(card_id)` → `buildCardNarrativeSystem` → `generateOnce`(BYEOLMARU_NARRATIVE_MODEL·NARRATIVE_MAX_TOKENS) → `{entitled:true, narrative}`(빈/실패는 null). 카드 없으면 해석할 게 없으니 조기 반환.
  - self 사주 조회 = calendar/narrative 라우트와 동일(is_primary·birth_date 가드). `todayKst`·`temporal(day ganji)`는 narrative 라우트처럼 계산.
- [ ] **Step 2** — tsc·build(`/api/byeolmaru/card-narrative` 등장·`ƒ Proxy`)·커밋 `feat(byeolmaru): 오늘의 카드 서술 라우트(구독 전용)`.

---

## Task 6: `DailyCardBlock` 컴포넌트
**Files:** Create `components/byeolmaru/DailyCardBlock.tsx`
`components/tarot/CardDrawRitual` 와 `WatchAddModal`(모달 패턴)·`PremiumBlock`(블러 게이트)·`crosssell` 카피를 먼저 읽는다.
- [ ] **Step 1: 구현**
  - Props: `{ entitled: boolean }` (self profile 존재는 상위 ByeolmaruView 가 이미 보장).
  - 마운트: `GET /api/byeolmaru/daily-card` → 오늘 카드.
  - **뽑기 전(card null)**: "오늘의 카드" + "뽑기" 버튼 → `CardDrawRitual`(cardCount=1, 적절한 accent/label, onComplete) 를 **모달/오버레이**로 띄움(CardDrawRitual props 는 파일 읽어 정확히; 결제 모달 없이 무료). onComplete(drawn) → `drawn[0]` 의 cardId·reversed 로 `POST /api/byeolmaru/daily-card` → 카드 상태 세팅.
    - ⚠️ `DrawnCard` 실제 필드(`lib/tarot/spreads.ts`)를 읽어 cardId/reversed 추출 경로를 맞출 것.
  - **뽑은 후**: `getCardImagePath(cardId)` 이미지 + `getCard(cardId).name_kr` + 정/역 라벨 + 키워드(정/역별 `upright`/`reversed`) + 짧은 정적 문장.
    - 구독자: `GET /api/byeolmaru/card-narrative` → 서술(로딩→표시, ②-a 패턴). 비구독: 서술 자리에 **블러 + "구독하고 이 카드 개인화 해석 보기" CTA**(PremiumBlock/락 패턴 재사용).
    - **인라인 낙수**(무료/비구독 대상): 카드 아래 "이 카드, 타로로 더 깊게" → `/`(타로톡). 구독자는 이미 해석 있으니 생략 가능.
- [ ] **Step 2** — tsc·커밋 `feat(byeolmaru): DailyCardBlock(오늘의 카드 뽑기·무료정적·구독서술·낙수)`.

---

## Task 7: `ByeolmaruView` 배치
**Files:** Modify `components/byeolmaru/ByeolmaruView.tsx`
- [ ] **Step 1** — `subject === "me"` 뷰에 `<DailyCardBlock entitled={data.entitled} />` 배치(스펙 §3 블록 순서: 캘린더/상세 → **오늘의 카드** → 개인화 서술 → 주간흐름 → 크로스셀 근처. 읽기 좋은 위치 선택). 상대(우리 오늘) 뷰에는 넣지 않음(오늘의 카드는 "나" 전용). self 경로 무회귀 유지.
- [ ] **Step 2** — tsc·build·**dev 라이브 확인**은 Task 8. 커밋 `feat(byeolmaru): 오늘의 카드 블록 배치`.

---

## Task 8: 회귀 + dev 라이브 E2E
- [ ] **Step 1** — `node --import tsx --test lib/byeolmaru/*.test.ts` GREEN · tsc 0 · `npm run build` exit 0(`ƒ Proxy`·새 라우트 2개).
- [ ] **Step 2** — `git push origin dev`(마이그 자동 적용). 폴링으로 `byeolmaru_daily_card` 적용 확인(3-a·3-c 방식, `.then()` 패턴 — top-level await 금지).
- [ ] **Step 3** — fixture 유저(self)로 dev 라이브: /byeolmaru → "오늘의 카드 뽑기" → CardDrawRitual 1장 → 저장 → 카드+키워드 렌더. 재로드 시 같은 카드(고정). 구독(체험)이면 서술 생성 확인, 비구독이면 블러. self 캘린더 무회귀. 정리(fixture --clean).

---

## Self-Review
**1. Spec 커버리지:** §2 오늘의카드(하루1장·CardDrawRitual·무료정적·구독LLM·비자격 블러·인라인낙수) → Task 1~7 ✅. 카드 뱅크 재사용 ✅. good_days/one_card 폐지 = **5-b**(명시).
**2. Placeholder:** SQL·래퍼·프롬프트·API·daily-card route 전체 코드. card-narrative·DailyCardBlock 은 미러 원본(pair-narrative·CardDrawRitual·PremiumBlock) 지시 + 핵심 흐름(억지 코드보다 실컴포넌트 재사용이 옳음). DrawnCard 필드는 "읽어서 맞춰라" 경고.
**3. 타입 일관성:** `DailyCard{cardId,reversed}`(Task2) ↔ API(Task3) ↔ 컴포넌트(Task6). `buildCardNarrativeSystem(saju,card,reversed,ganji)`(Task4) ↔ card-narrative(Task5) 인자 일치. `isValidCardId` 뱅크 상한.

## Execution Handoff
5-b(good_days·one_card 폐지) = 이 플랜 완료 후. 무료 대체(오늘의 카드)가 실재하므로 "동시 폐지" 조건 충족. grep 감사 중심(신규 노출만 차단·타입/데이터 보존).
