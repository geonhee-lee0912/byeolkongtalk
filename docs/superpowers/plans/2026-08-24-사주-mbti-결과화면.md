# 사주 MBTI — 플로우 + 결과화면 구현 플랜 (하위 D, E 흡수)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement task-by-task. Steps use checkbox (`- [ ]`). UI 태스크는 브라우저 검증 단계 포함.

**Goal:** 무료 사주 MBTI 의 입력→계산→리빌-라스트 결과→공유 UI 전체. 순수 클라이언트·무영속. A·B·C 라이브러리의 첫 UI 소비자.

**Architecture:** 테스트 가능한 순수부(토큰·오각 기하·조립)를 TDD로 잡고, UI 컴포넌트는 상세 스펙대로 빌드 후 브라우저 DOM 단정으로 검증. 단일 클라 스테이지 머신 + sessionStorage, 비-PII 결과 토큰으로 공유.

**Tech Stack:** Next.js 16 App Router, React 19, TS strict, Tailwind v4(@theme 토큰). node:test + tsx(순수부). 브라우저 검증(dev 서버).

**Spec:** `docs/superpowers/specs/2026-08-24-사주-mbti-결과화면-design.md`

---

## File Structure

- `lib/saju-mbti/share-tokens.ts` + `.test.ts` — 결과 토큰 인코딩/디코딩/검증(순수).
- `lib/saju-mbti/pentagon.ts` + `.test.ts` — 오각 기하(순수).
- `lib/saju-mbti/assembly.test.ts` — calcSaju→palja→self→match 조립 통합 테스트.
- `components/saju-mbti/ElementPentagon.tsx` — 오각 SVG(기하 소비).
- `components/saju-mbti/QuizStage.tsx` · `BirthStage.tsx` · `ResultView.tsx` · `SharedTeaser.tsx` · `SajuMbtiFlow.tsx`(오케스트레이터).
- `app/fortune/saju-mbti/page.tsx` — 서버 래퍼 + `generateMetadata`.
- `app/api/og/saju-mbti/route.tsx` — stateless OG.
- `app/fortune/page.tsx` — 무료 카드 추가(수정).

**재사용:** `@/lib/saju/calc`(`calcSaju`), `@/lib/saju-mbti/{mapping,self-type,match,content,codes,questions}`, `@/components/saju/SajuBoard`, `@/lib/saju/elements`(`ELEMENT_COLORS`,`FiveElement`), `@/lib/kakao-share`, `components/saju/SajuInputForm`(생년월일 입력 패턴 참고).

**테스트:** `node --import tsx --test lib/saju-mbti/*.test.ts`

---

## 공통 참조 — 조립 · 디자인 토큰 (매 태스크 준수)

**조립**(스펙 §5):
```ts
const saju = calcSaju({ ...birth, gender: "other" });      // @/lib/saju/calc
const palja = paljaType(saju);                              // @/lib/saju-mbti/mapping
const self  = selfType(answers);                            // @/lib/saju-mbti/self-type
const match = matchRate(self.axes, palja.axes);             // ★ .axes 양쪽 — @/lib/saju-mbti/match
const content = TYPE_CONTENT[palja.code];                   // 없으면 렌더 중단
```
- `palja`: `{ axes, code, element:FiveElement, elementDist:Record<FiveElement,number>, tenGods, jangan }`.
- `self`: `{ axes:Record<AxisKey,AxisResult>, code }`. `match`: `{ matchCount:0..4, band, perAxis:[{axis,selfPole,paljaPole,agree}] }`.
- `content`(TYPE_CONTENT[code]): `{ hanja, character, memeSubtitle, oneLiner, personality, light, shadow, love, compat:{fits,clashes}, shareText }`.
- ⚠️ `SelfType.pct`(원점수비율) vs `PaljaType.pct`(백분위) 척도 다름 — %막대 나란히 그릴 때 크기비교 금지. 극/일치만 신뢰.

**디자인 토큰**(Tailwind v4 @theme, `app/globals.css` 정본 — 유틸 클래스로): `cream`/`cream-warm`(배경·카드) · `lilac-soft`/`lilac`/`lilac-mid`/`lilac-deep` · `gold`/`gold-soft` · `eye-purple`(본문)/`text-light`(보조) · `night`/`night-deep`(다크·낙관). 카드 `rounded-2xl`, 애니 `animate-fade-in`. Server Component 기본, 인터랙션만 Client.

**보이스**: 카피는 밈부제/공유=위트, 안내문=별콩이 톤(C 스펙 §3).

---

## Task 1: 결과 토큰 (share-tokens.ts, 순수 TDD)

**Files:** Create `lib/saju-mbti/share-tokens.ts`, `lib/saju-mbti/share-tokens.test.ts`

- [ ] **Step 1: 실패 테스트** — create `share-tokens.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { encodeResult, decodeResult } from "./share-tokens.ts";

const R = { paljaCode: "음유인생", selfCode: "양강재생", band: "절충" as const, element: "화" as const };

test("share-tokens — 왕복 인코딩 복원", () => {
  assert.deepEqual(decodeResult(encodeResult(R)), R);
});

test("share-tokens — 형식은 인덱스 4토막", () => {
  assert.match(encodeResult(R), /^\d+\.\d+\.\d+\.\d+$/);
});

test("share-tokens — 무효 토큰은 null", () => {
  for (const t of [null, undefined, "", "1.2.3", "16.0.0.0", "0.0.3.0", "0.0.0.5", "a.b.c.d", "-1.0.0.0"]) {
    assert.equal(decodeResult(t as never), null, `${t} 는 null 이어야`);
  }
});
```

- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/saju-mbti/share-tokens.test.ts` → FAIL.

- [ ] **Step 3: 구현** — create `share-tokens.ts`:

```ts
import { ALL_CODES } from "./codes.ts";

export const BANDS = ["천명", "절충", "거스름"] as const;
export const ELS = ["목", "화", "토", "금", "수"] as const;
export type Band = (typeof BANDS)[number];
export type El = (typeof ELS)[number];

export interface ResultTokens {
  paljaCode: string;
  selfCode: string;
  band: Band;
  element: El;
}

// 인덱스 인코딩 — 비-PII·짧음·한글 URL 회피. 입력은 실제 계산값(항상 유효) 가정.
export function encodeResult(r: ResultTokens): string {
  return [
    ALL_CODES.indexOf(r.paljaCode),
    ALL_CODES.indexOf(r.selfCode),
    BANDS.indexOf(r.band),
    ELS.indexOf(r.element),
  ].join(".");
}

export function decodeResult(token: string | null | undefined): ResultTokens | null {
  if (!token) return null;
  const parts = token.split(".");
  if (parts.length !== 4) return null;
  const [pi, si, bi, ei] = parts.map((p) => (/^\d+$/.test(p) ? Number(p) : -1));
  if (pi < 0 || pi > 15 || si < 0 || si > 15 || bi < 0 || bi > 2 || ei < 0 || ei > 4) return null;
  return { paljaCode: ALL_CODES[pi], selfCode: ALL_CODES[si], band: BANDS[bi], element: ELS[ei] };
}
```

- [ ] **Step 4: 통과 확인** → PASS (3 tests).
- [ ] **Step 5: 커밋** — `feat(saju-mbti): 결과 토큰 인코딩/검증(비-PII)`

---

## Task 2: 오각 기하 + 컴포넌트 (pentagon)

**Files:** Create `lib/saju-mbti/pentagon.ts`, `lib/saju-mbti/pentagon.test.ts`, `components/saju-mbti/ElementPentagon.tsx`

- [ ] **Step 1: 실패 테스트** — create `pentagon.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { pentagonGeometry } from "./pentagon.ts";

test("pentagon — 5꼭지·목화토금수 순·최상단 시작", () => {
  const g = pentagonGeometry({ 목: 2, 화: 1, 토: 2, 금: 1, 수: 2 }, 200);
  assert.equal(g.axes.length, 5);
  assert.deepEqual(g.axes.map((a) => a.element), ["목", "화", "토", "금", "수"]);
  assert.ok(Math.abs(g.axes[0].x - 100) < 0.5, "첫 꼭지 상단(x≈cx)");
  assert.ok(g.axes[0].y < 100, "첫 꼭지 상단(y<cy)");
});

test("pentagon — 최대 오행이 외곽 반지름", () => {
  const g = pentagonGeometry({ 목: 8, 화: 0, 토: 0, 금: 0, 수: 0 }, 200);
  const d = Math.hypot(g.axes[0].x - g.cx, g.axes[0].y - g.cy);
  assert.ok(Math.abs(d - g.r) < 0.5, "최대값 꼭지가 r");
});

test("pentagon — 전부 0 이면 전 꼭지 중심(방어)", () => {
  const g = pentagonGeometry({ 목: 0, 화: 0, 토: 0, 금: 0, 수: 0 }, 200);
  for (const a of g.axes) assert.ok(Math.hypot(a.x - g.cx, a.y - g.cy) < 0.5);
});
```

- [ ] **Step 2: 실패 확인** → FAIL.

- [ ] **Step 3: 구현 기하** — create `pentagon.ts`:

```ts
import type { FiveElement } from "@/lib/saju/elements";

const ORDER: FiveElement[] = ["목", "화", "토", "금", "수"];

export interface PentAxis {
  element: FiveElement;
  value: number;
  x: number; y: number;         // 데이터 점
  labelX: number; labelY: number;
}
export interface PentGeometry {
  axes: PentAxis[];
  polygon: string;              // 데이터 5점
  ring: string;                 // 가이드 외곽
  cx: number; cy: number; r: number;
}

export function pentagonGeometry(dist: Record<FiveElement, number>, size: number): PentGeometry {
  const cx = size / 2, cy = size / 2, r = size * 0.38, labelR = size * 0.47;
  const max = Math.max(1, ...ORDER.map((e) => dist[e] ?? 0));
  const ang = (i: number) => -Math.PI / 2 + (i * 2 * Math.PI) / 5;
  const axes: PentAxis[] = ORDER.map((element, i) => {
    const value = dist[element] ?? 0;
    const rr = (value / max) * r;
    return {
      element, value,
      x: cx + rr * Math.cos(ang(i)), y: cy + rr * Math.sin(ang(i)),
      labelX: cx + labelR * Math.cos(ang(i)), labelY: cy + labelR * Math.sin(ang(i)),
    };
  });
  const ring = ORDER.map((_, i) => `${(cx + r * Math.cos(ang(i))).toFixed(2)},${(cy + r * Math.sin(ang(i))).toFixed(2)}`).join(" ");
  return { axes, cx, cy, r, ring, polygon: axes.map((a) => `${a.x.toFixed(2)},${a.y.toFixed(2)}`).join(" ") };
}
```

- [ ] **Step 4: 통과 확인** → PASS (3 tests).

- [ ] **Step 5: 컴포넌트** — create `components/saju-mbti/ElementPentagon.tsx` (Client 불필요 — 순수 렌더, Server OK):
  - Props: `{ dist: Record<FiveElement, number>; size?: number }`(기본 200).
  - `pentagonGeometry(dist, size)` 호출. `<svg viewBox="0 0 size size" role="img">` + `<title>오행 분포</title>`.
  - 렌더: 가이드 `<polygon points={ring}>` (fill none, stroke `lilac` 얇게) + 데이터 `<polygon points={polygon}>` (fill `lilac-deep` 20% 투명, stroke `lilac-deep`) + 각 꼭지 `<circle>`(색 `ELEMENT_COLORS[el].bar`) + `<text x=labelX y=labelY>`(오행 글자, `ELEMENT_COLORS[el].text`) + 값 작게.
  - 색은 `ELEMENT_COLORS`(`@/lib/saju/elements`)에서. 반응형: `w-full max-w-[220px]`.

- [ ] **Step 6: 커밋** — `feat(saju-mbti): 오행 오각 기하+컴포넌트`

---

## Task 3: 조립 통합 테스트 (assembly.test.ts)

**Files:** Create `lib/saju-mbti/assembly.test.ts`

- [ ] **Step 1: 테스트 작성**(조립 정합·`.axes` 계약 잠금):

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { calcSaju } from "@/lib/saju/calc";
import { paljaType } from "./mapping.ts";
import { selfType } from "./self-type.ts";
import { matchRate } from "./match.ts";
import { TYPE_CONTENT } from "./content.ts";
import { MATCH_NARRATIVE } from "./content.ts";
import { QUESTIONS } from "./questions.ts";

test("assembly — 골든 사주 + 첫선택지 답안 → 유효 결과 전개", () => {
  const saju = calcSaju({ year: 1992, month: 9, day: 12, hour: 13, minute: 47, gender: "other" });
  const palja = paljaType(saju);
  assert.equal(palja.code, "음강인단"); // A 골든

  const answers = Object.fromEntries(QUESTIONS.map((q) => [q.id, q.options[0].id]));
  const self = selfType(answers);
  assert.equal(self.code, "양강재생"); // B 계약

  const match = matchRate(self.axes, palja.axes); // ★ .axes
  assert.ok(match.matchCount >= 0 && match.matchCount <= 4);
  assert.equal(match.perAxis.length, 4);
  assert.ok(["천명", "절충", "거스름"].includes(match.band));

  assert.ok(TYPE_CONTENT[palja.code], "팔자 content 존재");
  assert.ok(TYPE_CONTENT[self.code], "자아 content 존재");
  assert.ok(MATCH_NARRATIVE[match.band].title, "밴드 서사 존재");
});
```

- [ ] **Step 2: 실행** → PASS(전부 dev 로컬 기존 라이브러리라 즉시 green이면 조립 정합 확인). 만약 `palja.code`/`self.code` 기대와 다르면 A/B 회귀 신호 — 멈추고 조사.
- [ ] **Step 3: 커밋** — `test(saju-mbti): 조립 통합(사주→팔자·문항→자아·일치율)`

---

## Task 4: QuizStage (12문항)

**Files:** Create `components/saju-mbti/QuizStage.tsx` (`"use client"`)

- [ ] **Step 1: 빌드** — 스펙 §4-2:
  - Props: `{ onDone: (answers: Record<string, string>) => void }`.
  - `QUESTIONS`(`@/lib/saju-mbti/questions`) 순차. state: 현재 인덱스 + 누적 답.
  - 각 문항: `q.prompt` + 4선택지. **선택지 셔플은 문항별 1회**(`useMemo(()=>shuffle(q.options),[q.id])` — 마운트/문항전환 시 고정, 재렌더에 안 흔들림). 점수는 옵션 `id` 기준이라 위치 무관(B).
  - 선택 시 다음 문항, 마지막이면 `onDone(answers)`. 진행바(`현재/12`). 뒤로 가기(선택).
  - 스타일: cream 배경, 선택지 카드 `rounded-2xl` hover `lilac-soft`, 진행바 `gold`.
- [ ] **Step 2: 브라우저 검증** — dev 서버 `/fortune/saju-mbti`(Task 8 후) 또는 임시 하네스에서: read_page 로 12문항 순차·진행바·완주 시 onDone 확인. (Task 8 통합 후 전체 플로우로 재검증하므로 여기선 컴포넌트 단위 확인만.)
- [ ] **Step 3: 커밋** — `feat(saju-mbti): QuizStage 12문항 스텝퍼(선택지 셔플)`

---

## Task 5: BirthStage (생년월일)

**Files:** Create `components/saju-mbti/BirthStage.tsx` (`"use client"`)

- [ ] **Step 1: 빌드** — 스펙 §2·§4-3:
  - Props: `{ onDone: (birth: { year:number; month:number; day:number; hour:number|null; minute:number|null; isLunar:boolean; isLeapMonth:boolean }) => void }`. **성별 없음**.
  - 입력: 생년(연도 셀렉트/입력)·월·일 · 태어난 시(0–23시 셀렉트 + "시간 모름" 체크→hour:null,minute:null) · 양력/음력 토글(음력 시 윤달 체크 노출). `components/saju/SajuInputForm` 패턴 참고(단 gender 제외).
  - 유효성: 연·월·일 범위·실존 날짜. 통과 시 "결과 보기 →" 활성.
  - 스타일: cream, 입력 `rounded-xl border-lilac`, CTA `gold`.
- [ ] **Step 2: 브라우저 검증** — 날짜/시간/음양력 입력·시간모름·유효성. (Task 8 통합 재검증.)
- [ ] **Step 3: 커밋** — `feat(saju-mbti): BirthStage 생년월일 입력(성별 제외·시간모름)`

---

## Task 6: ResultView (리빌-라스트 풀 결과)

**Files:** Create `components/saju-mbti/ResultView.tsx`

- [ ] **Step 1: 빌드** — 스펙 §5, C 스펙 §6, 목업(리빌-라스트) 준수:
  - Props: `{ saju: SajuResult; palja: PaljaType; self: SelfType; match: MatchRate; onRestart: ()=>void; onShare: ()=>void }`. 상단서 `const content = TYPE_CONTENT[palja.code]; if (!content) return null;`(방어).
  - 블록 순서:
    1. **팔자 히어로**(night 카드): "타고난 너 · 사주 팔자" 라벨 + 낙관(`content.hanja`, night+gold 도장) + `content.character`(display, gold-soft) + `palja.element` 표기 + `content.oneLiner`(cream) + 하단 "↓ 맨 아래에서" 티저.
    2. `content.memeSubtitle`(lilac-soft 칩).
    3. **본문**(cream 카드들): `content.personality` / `content.light`·`content.shadow`(2칸, 그림자 라벨 눅게) / `content.love` / **궁합**: `content.compat.fits`·`clashes` 각 박스 = `TYPE_CONTENT[c.code].character` + 코드 + `c.reason`(gold/lilac 톤). / 오행 `ELEMENT_MODULE[palja.element].texture`(amber 톤 카드, "사주 전용").
    4. **사주 원판**: `<SajuBoard saju={saju}/>` + `<ElementPentagon dist={palja.elementDist}/>`.
    5. **리빌**(night 카드): "그런데, 네가 문항에서 답한 너는—" + 자아 `TYPE_CONTENT[self.code].character`+코드 → **4축 비교**(`match.perAxis` 4행: 축라벨·selfPole↔paljaPole·agree면 "일치"/아니면 "갈림" 강조) → 일치율 `match.matchCount`/4 + `MATCH_NARRATIVE[match.band].title`(gold) + `.body` → 원칙② 톤.
    6. 공유(`onShare`)·다시하기(`onRestart`).
  - ⚠️ 궁합·리빌서 `TYPE_CONTENT[c.code]`/`MATCH_NARRATIVE[band]` 접근 전 존재 확인. %막대 쓰면 self/palja 척도 혼용 금지(극/일치만).
  - 타입: `SajuResult`(`@/lib/saju/calc`), `PaljaType`(`@/lib/saju-mbti/mapping`), `SelfType`(`self-type`), `MatchRate`(`match`).
- [ ] **Step 2: 브라우저 검증** — 임시 고정 props(골든 사주+첫선택지 답)로 렌더 → read_page 로 5블록·낙관·오각·4축·밴드 존재 DOM 단정.
- [ ] **Step 3: 커밋** — `feat(saju-mbti): ResultView 리빌-라스트 결과`

---

## Task 7: SharedTeaser (축약 티저)

**Files:** Create `components/saju-mbti/SharedTeaser.tsx`

- [ ] **Step 1: 빌드** — 스펙 §7:
  - Props: `{ tokens: ResultTokens; onStart: ()=>void }`. `const content = TYPE_CONTENT[tokens.paljaCode]; if (!content) { onStart(); return null; }`.
  - 렌더: 팔자 캐릭터+낙관(`content.hanja`)+`content.oneLiner`+밴드 한 줄(`MATCH_NARRATIVE[tokens.band].title`) — "○○님의 결과" 톤. + **대형 CTA "나도 해보기"**(`onStart`). 상대 명식·오각·자아 없음.
  - night 히어로 톤 + gold CTA.
- [ ] **Step 2: 브라우저 검증** — Task 8 통합서 `?r=<token>` 진입 재검증.
- [ ] **Step 3: 커밋** — `feat(saju-mbti): SharedTeaser 공유 축약 티저`

---

## Task 8: SajuMbtiFlow 오케스트레이터 + 라우트 + 진입 카드

**Files:** Create `components/saju-mbti/SajuMbtiFlow.tsx`(`"use client"`), `app/fortune/saju-mbti/page.tsx`; Modify `app/fortune/page.tsx`

- [ ] **Step 1: 빌드 오케스트레이터** — 스펙 §4:
  - Props: `{ sharedToken?: string }`(서버서 searchParams `r` 전달).
  - state: `stage: "intro"|"quiz"|"birth"|"result"|"shared"`. 초기: `sharedToken` 이 `decodeResult` 유효 && 세션 없음 → `"shared"`, 아니면 `"intro"`.
  - `sessionStorage['saju-mbti:session']` 복원: `{birthInput, answers}` 있으면 계산해 `result` 로.
  - quiz `onDone(answers)` → birth. birth `onDone(birthInput)` → 계산(조립 §5) → `sessionStorage` 저장 + `history.replaceState(null,"", "?r="+encodeResult({paljaCode:palja.code,selfCode:self.code,band:match.band,element:palja.element}))` + `result`.
  - `result`: `<ResultView .../>`(onRestart→세션삭제·`intro`·URL 정리, onShare→§7 카카오/복사). `shared`: `<SharedTeaser tokens={decoded} onStart={()=>setStage("intro")}/>`.
  - onShare 구현: `shareToKakao`(`@/lib/kakao-share`) + `navigator.clipboard`(토스트). link/imageUrl 은 `window.location.origin` + `?r=`/OG.
- [ ] **Step 2: 라우트** — create `app/fortune/saju-mbti/page.tsx`(Server):
  - `export async function generateMetadata({ searchParams }): Promise<Metadata>` — `r` 디코드 유효 시 `openGraph.images:[`/api/og/saju-mbti?r=${r}`]` + title/description(팔자 캐릭터), 아니면 기본. `contentMetadata()`(`@/lib/seo/metadata`) 경유(공용 필드 병합 규칙, AGENTS.md). `robots` noindex(공유 결과라 색인 불필요, byeoljari 선례).
  - 본문: `<SajuMbtiFlow sharedToken={searchParams.r} />`.
- [ ] **Step 3: 진입 카드** — modify `app/fortune/page.tsx`: byeoljari 무료 카드 옆/아래 사주 MBTI 카드 추가(`<Link href="/fortune/saju-mbti">` + 무료 badge). 제품 config 아님(byeoljari와 동일 취급).
- [ ] **Step 4: 셸 확인** — `/fortune/saju-mbti` 는 `HIDE_SHELL_PREFIXES` 에 **추가 안 함**(셸 유지). `/fortune` 탭 자동 하이라이트 확인.
- [ ] **Step 5: 전체 플로우 브라우저 검증**(핵심) — dev 서버:
  - `/fortune` → 사주 MBTI 카드 노출·링크.
  - `/fortune/saju-mbti` → intro → quiz 12답(read_page 로 문항 진행) → birth 입력 → result 렌더(5블록 DOM 단정).
  - URL 에 `?r=` 토큰 생김 확인. 새 시크릿(세션X) `?r=<token>` → SharedTeaser. 무효 `?r=zzz` → intro.
  - ⚠️ 인앱 screenshot 타임아웃 → read_page/DOM 단정으로 증거(메모리 `browser-e2e-session-injection`).
- [ ] **Step 6: 커밋** — `feat(saju-mbti): 플로우 오케스트레이터+라우트+진입 카드`

---

## Task 9: stateless OG 이미지

**Files:** Create `app/api/og/saju-mbti/route.tsx`

- [ ] **Step 1: 빌드** — `app/api/og/saju/[readingId]/route.tsx` 패턴 참고:
  - `runtime = "nodejs"`. `GET(req)`: `const token = new URL(req.url).searchParams.get("r"); const d = decodeResult(token); if (!d) return new Response("bad", {status:400});`
  - `const content = TYPE_CONTENT[d.paljaCode];` 없으면 400. IP rate-limit(기존 헬퍼 있으면 재사용, 30/분).
  - `new ImageResponse(<div>...(night 배경·낙관 content.hanja·캐릭터 content.character gold 큼·content.oneLiner·`${match n}/4 · ${MATCH_NARRATIVE[d.band].title}`·"별콩톡 사주 MBTI")...</div>, {width:1200,height:630,fonts})`. 폰트 모듈-레벨 캐시(기존 패턴).
  - ⚠️ 밴드 라벨의 "N/4" 는 토큰에 matchCount 없음 → 밴드 title 만 표기(또는 토큰에 matchCount 추가 검토는 범위 밖, 밴드로 충분).
- [ ] **Step 2: 검증** — dev 서버 `/api/og/saju-mbti?r=<유효토큰>` → 200 + 이미지(read_network_requests/직접 fetch). 무효 `?r=zzz` → 400.
- [ ] **Step 3: 커밋** — `feat(saju-mbti): stateless OG 이미지(토큰 기반)`

---

## Task 10: 최종 게이트

- [ ] **Step 1: 순수 테스트** — `node --import tsx --test lib/saju-mbti/*.test.ts` → 전부 PASS.
- [ ] **Step 2: 타입** — `npx tsc --noEmit` → exit 0.
- [ ] **Step 3: 빌드** — `next build` → 성공 + 라우트 표에 `/fortune/saju-mbti` · `/api/og/saju-mbti`.
- [ ] **Step 4: 전체 E2E**(dev, 재확인) — Task 8 Step 5 전 플로우 + Task 9 OG + shared 티저 + 무효 토큰. read_page/DOM 단정으로 증거.
- [ ] **Step 5: 커밋**(있으면) — `chore(saju-mbti): D 최종 게이트`

---

## Self-Review

**1. Spec coverage:**
- §3 라우팅·파일 → Task 4~9(전 파일). ✓
- §4 플로우(스테이지·sessionStorage·토큰 URL) → Task 8. ✓
- §5 결과 5블록·조립·pct 주의 → Task 6(+Task 3 조립 잠금). ✓
- §6 오각(순수 기하 분리+컴포넌트) → Task 2. ✓
- §7 공유(토큰·카카오·복사·OG·티저) → Task 7·8·9. ✓
- §8 토큰 검증·무가드 인덱싱 금지 → Task 1(+소비처 방어 Task 6·7·8·9). ✓
- §9 검증(유닛·통합·E2E·OG·build) → Task 1·2·3·8·9·10. ✓

**2. Placeholder scan:** 순수부(토큰·오각·조립) 풀코드. UI 태스크는 **완전한 빌드 스펙**(props·데이터 출처·블록 순서·스타일 토큰·검증) — 콘텐츠/UI 플랜 특성상 JSX 는 실행서 저술, 스펙은 위약 아님. 브라우저 DOM 단정이 렌더 게이트.

**3. Type consistency:** `ResultTokens`/`Band`/`El`(Task1) → Task7·8·9 소비. `PentGeometry`(Task2) → ElementPentagon. 조립 타입(`PaljaType`·`SelfType`·`MatchRate`·`SajuResult`) A·B·C·calc 정의 재사용. `matchRate(self.axes, palja.axes)` 계약을 Task3 가 잠금, Task6·8 동일 사용.

## Execution Handoff

(플랜 저장 후 실행 방식 선택 — UI 다수라 subagent 병렬 가능하나 브라우저 검증·플로우 통합은 순차. 배치 검토 포함.)
