# 연애 상담 탭 리네임 + 리디자인 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** "우리 사이"를 "연애 상담"으로 리네임하고(+사주톡→별콩 운세), 활성 스레드 화면 상단을 1행으로 줄여 대화 영역을 최대화하며, 패스를 시간권으로 표시하고, 미등록 소개(S1) 카피를 다듬는다.

**Architecture:** 표기/레이아웃/표시단위만 변경. DB 스키마·라우트·RPC(`purchase_relationship_pass`, `/api/relationship/extend`)·페르소나·안전망은 불변. 순수 로직(남은시간 포맷, 패스 라벨)은 `lib/relationship`에 두고 `node:test`로 단위 검증하고, UI는 `tsc --noEmit` + 브라우저로 검증한다.

**Tech Stack:** Next.js 16 (App Router, "use client"), React 19, Tailwind v4, TypeScript strict. 테스트: Node 내장 러너(`node --import tsx --test`), 기존 `lib/relationship/*.test.ts` 패턴.

**스펙:** `docs/superpowers/specs/2026-07-18-연애상담-리네임-리디자인-design.md`

---

## Testing approach (읽고 시작)

- **순수 로직**(Task 2·3): 기존 패턴대로 `node:test` + `node:assert/strict`, 파일명 `*.test.ts`, `import "./x.ts"`. 실행: `node --import tsx --test <파일>`.
- **UI**(Task 4~11): 이 저장소에 컴포넌트 테스트 프레임워크가 없다(vitest/jest 없음). 따라서 UI는 **`npx tsc --noEmit`(타입/JSX) + 개발서버 브라우저 확인**으로 검증한다. 억지 단위테스트를 만들지 않는다.
- 브라우저 확인은 `preview_start`(name `byeolkong-dev`) 또는 `npm run dev` 후 `/relationship` 에서 상태별(S1 미등록 / S2 패스없음 / S3 활성 / S4 캡도달) 확인.
- 커밋은 작업 단위로 자주. 현재 브랜치 `dev`. 푸시는 사용자 지시 시에만.

## File Structure (변경 지도)

- `components/layout/BottomTab.tsx` — 탭 라벨 2곳 (사주톡→별콩 운세, 우리 사이→연애 상담)
- `lib/relationship/types.ts` — `PASS_PLANS[].label` 시간권 병기
- `lib/relationship/types.test.ts` — 라벨 단언 갱신
- `lib/relationship/passDisplay.ts` *(신규)* — `formatPassRemaining` 순수 함수
- `lib/relationship/passDisplay.test.ts` *(신규)* — 위 함수 단위 테스트
- `app/relationship/page.tsx` — 헤더 1행화·패스 pill·상태줄/스킬행 제거·S1 카피
- `components/relationship/PassSheet.tsx` — 패스 상태 + 오늘 연장 + 구매 위임
- `components/relationship/PassPanel.tsx` / `PassConfirmModal.tsx` — 안내 문구 "대략" 정렬
- `components/relationship/SkillSheet.tsx` *(신규)* — ⚡ 스킬 바텀시트(프레젠테이션)
- `components/relationship/ThreadChat.tsx` — ⚡ 스킬 버튼+시트, 캡 도달 칩 재스타일
- `components/relationship/SkillChipRow.tsx` — 고아화 → 삭제
- `app/relationship/verdict/[id]/page.tsx`, `app/refund/page.tsx` — 문구 리네임

---

## Task 1: 탭 라벨 리네임 (BottomTab)

**Files:**
- Modify: `components/layout/BottomTab.tsx` (label `"사주톡"` 라인 ~38, `"우리 사이"` 라인 ~48)

- [ ] **Step 1: 라벨 2곳 변경**

`fortune` 탭 라벨:
```tsx
    label: "별콩 운세",
```
(기존 `label: "사주톡",` 대체 — `key: "fortune"` 객체 안)

`history` 탭 라벨:
```tsx
    label: "연애 상담",
```
(기존 `label: "우리 사이",` 대체 — `key: "history"` 객체 안. `key`/`href`/`matchPrefixes`/아이콘은 그대로.)

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음 (EXIT 0)

- [ ] **Step 3: 잔여 확인**

Run: `git grep -n "사주톡\|우리 사이" -- components/ app/`
Expected: `BottomTab.tsx` 매치 없음. (`app/relationship`·`verdict`·`refund`의 "우리 사이"는 이후 Task 에서 처리 — 여기선 BottomTab 만 확인)

- [ ] **Step 4: 커밋**

```bash
git add components/layout/BottomTab.tsx
git commit -m "feat(rel): 탭 라벨 사주톡→별콩 운세, 우리 사이→연애 상담"
```

---

## Task 2: 패스 시간권 라벨 (types.ts) — TDD

**Files:**
- Modify: `lib/relationship/types.ts:23-27` (`PASS_PLANS`)
- Test: `lib/relationship/types.test.ts`

- [ ] **Step 1: 실패 테스트 추가**

`lib/relationship/types.test.ts` 에 아래 테스트를 추가(기존 import 재사용; 없으면 상단에 `import { PASS_PLANS } from "./types.ts";`):
```ts
test("PASS_PLANS 라벨은 시간권(일 병기) 표기", () => {
  const byKind = Object.fromEntries(PASS_PLANS.map((p) => [p.kind, p.label]));
  assert.equal(byKind.day1, "24시간(1일)");
  assert.equal(byKind.day3, "72시간(3일)");
  assert.equal(byKind.day7, "168시간(7일)");
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/relationship/types.test.ts`
Expected: FAIL (현재 라벨은 "1일권" 등)

- [ ] **Step 3: 라벨 변경**

`lib/relationship/types.ts` `PASS_PLANS` 를 아래로 교체(‎`kind`/`days`/`cost` 불변, `label`만):
```ts
export const PASS_PLANS: PassPlan[] = [
  { kind: "day1", days: 1, cost: 20, label: "24시간(1일)" },
  { kind: "day3", days: 3, cost: 40, label: "72시간(3일)" },
  { kind: "day7", days: 7, cost: 60, label: "168시간(7일)" },
];
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/relationship/types.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/relationship/types.ts lib/relationship/types.test.ts
git commit -m "feat(rel): 패스 라벨 시간권(일 병기) 표기"
```

---

## Task 3: 남은시간 포맷 순수 함수 (passDisplay.ts) — TDD

`formatPassRemaining(expiresMs, planDays, nowMs)`:
- 1시간 이상: `{floor시}/{총시}시간` (총시 = planDays*24). 남은 > 총(스택 재구매): `{floor시}시간 남음`.
- 마지막 1시간(남은 < 60분): 10분 버킷 `{bucket}분/{총시}시간` (bucket = `min(50, ceil(분/10)*10)` → 51~59분=50, 10분 미만=10).
- 0 이하: `만료`.

**Files:**
- Create: `lib/relationship/passDisplay.ts`
- Test: `lib/relationship/passDisplay.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`lib/relationship/passDisplay.test.ts`:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { formatPassRemaining } from "./passDisplay.ts";

const H = 3600_000;
const M = 60_000;

test("1시간 이상은 시간 단위 (남은/총)", () => {
  assert.equal(formatPassRemaining(48 * H, 3, 0), "48/72시간");
  assert.equal(formatPassRemaining(72 * H, 3, 0), "72/72시간");
  assert.equal(formatPassRemaining(5 * H, 1, 0), "5/24시간");
  assert.equal(formatPassRemaining(100 * H, 7, 0), "100/168시간");
});

test("소수 시간은 내림", () => {
  assert.equal(formatPassRemaining(48 * H + 59 * M, 3, 0), "48/72시간");
});

test("스택 재구매로 총시간 초과면 분수 없이 남음", () => {
  assert.equal(formatPassRemaining(82 * H, 3, 0), "82시간 남음");
});

test("마지막 1시간은 10분 버킷 (분/총)", () => {
  assert.equal(formatPassRemaining(40 * M, 3, 0), "40분/72시간");
  assert.equal(formatPassRemaining(55 * M, 3, 0), "50분/72시간");
  assert.equal(formatPassRemaining(9 * M, 3, 0), "10분/72시간");
  assert.equal(formatPassRemaining(60 * M, 3, 0), "1/72시간"); // 정확히 60분은 시간 branch
});

test("만료", () => {
  assert.equal(formatPassRemaining(0, 3, 1), "만료");
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/relationship/passDisplay.test.ts`
Expected: FAIL ("formatPassRemaining" 없음 / 모듈 없음)

- [ ] **Step 3: 구현**

`lib/relationship/passDisplay.ts`:
```ts
// lib/relationship/passDisplay.ts — 패스 남은시간 표시 포맷 (순수 함수, 단위 테스트 대상)

/**
 * 헤더 pill 의 남은시간 문자열.
 * @param expiresMs 만료 시각 (epoch ms)
 * @param planDays  보유 패스권의 일수 (총시간 = planDays*24)
 * @param nowMs     현재 시각 (epoch ms)
 */
export function formatPassRemaining(
  expiresMs: number,
  planDays: number,
  nowMs: number
): string {
  const remainMs = expiresMs - nowMs;
  if (remainMs <= 0) return "만료";
  const totalHours = planDays * 24;
  const remainMin = remainMs / 60_000;
  if (remainMin < 60) {
    const bucket = Math.min(50, Math.ceil(remainMin / 10) * 10);
    return `${bucket}분/${totalHours}시간`;
  }
  const remainHours = Math.floor(remainMin / 60);
  if (remainHours > totalHours) return `${remainHours}시간 남음`;
  return `${remainHours}/${totalHours}시간`;
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/relationship/passDisplay.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/relationship/passDisplay.ts lib/relationship/passDisplay.test.ts
git commit -m "feat(rel): 패스 남은시간 포맷 formatPassRemaining + 테스트"
```

---

## Task 4: 활성 화면(S3/S4) 헤더 1행화 (page.tsx)

헤더카드를 1행(타이틀·호칭·상태·✏️ + 우측 패스 pill)으로 바꾸고, `SkillChipRow` 행과 상태줄(오늘 N번·D일·연장구매)을 제거한다. `formatPassDDay` → `formatPassRemaining`.

**Files:**
- Modify: `app/relationship/page.tsx`

- [ ] **Step 1: import 정리**

상단 import 블록에서 `SkillChipRow` import 제거하고, `formatPassRemaining` + `PASS_PLAN_BY_KIND` 추가. 최종형:
```tsx
import RegisterOnboarding from "@/components/relationship/RegisterOnboarding";
import PassPanel from "@/components/relationship/PassPanel";
import PassSheet from "@/components/relationship/PassSheet";
import RelationshipEditModal from "@/components/relationship/RelationshipEditModal";
import ThreadChat, { type ThreadChatMsg } from "@/components/relationship/ThreadChat";
import { formatPassRemaining } from "@/lib/relationship/passDisplay";
import {
  DAILY_TURN_CAP,
  EXTEND_COST,
  EXTEND_TURNS,
  PASS_PLANS,
  PASS_PLAN_BY_KIND,
  RELATIONSHIP_STATUS_LABELS,
  type RelationshipStatus,
} from "@/lib/relationship/types";
```
(‎`SkillChipRow` import 줄 삭제.)

- [ ] **Step 2: `formatPassDDay` 함수 삭제**

`app/relationship/page.tsx` 의 `formatPassDDay`(파일 상단, `/** 패스 만료까지 남은 날짜 표기 ... */` + 함수 본문 전체)를 삭제한다. (더 이상 사용 안 함.)

- [ ] **Step 3: `headerCard` 를 1행 헤더로 교체**

`if (relationship) {` 블록 안, `const headerCard = ( ... );` 정의를 아래로 교체:
```tsx
    const planDays = pass ? PASS_PLAN_BY_KIND[pass.kind]?.days ?? 0 : 0;
    const passStatus =
      pass && planDays
        ? formatPassRemaining(new Date(pass.expiresAt).getTime(), planDays, Date.now())
        : null;

    const headerCard = (
      <div className="bg-white rounded-2xl px-4 py-2.5 border border-lilac-soft shadow-sm flex items-center gap-2.5">
        <div className="min-w-0">
          <p className="text-[10.5px] text-text-light leading-none">연애 상담</p>
          <div className="flex items-center gap-1 mt-1 text-[14px] text-eye-purple">
            <span className="font-bold truncate">{relationship.label}</span>
            <span aria-hidden style={{ color: "#C4488A" }}>❤</span>
            <span className="shrink-0 text-text-light">
              {RELATIONSHIP_STATUS_LABELS[relationship.status]}
            </span>
          </div>
        </div>
        <button
          type="button"
          onClick={() => setShowEditModal(true)}
          aria-label="관계 정보 수정"
          className="shrink-0 w-7 h-7 rounded-full flex items-center justify-center text-lilac-deep hover:bg-lilac-soft/40 active:scale-95 transition"
        >
          <span aria-hidden className="text-[13px]">✏️</span>
        </button>
        <span className="flex-1" />
        {passStatus && (
          <button
            type="button"
            onClick={() => setShowPassSheet(true)}
            className="shrink-0 whitespace-nowrap text-[11px] text-text-light active:scale-95 transition"
          >
            <span className="font-bold text-eye-purple">{passStatus}</span>
            <span className="mx-1.5 text-lilac-mid/60">|</span>
            <span className="font-bold text-lilac-deep">패스권 구매</span>
            <span className="ml-0.5 text-lilac-mid">›</span>
          </button>
        )}
      </div>
    );
```

- [ ] **Step 4: S3/S4 상단에서 SkillChipRow 행 + 상태줄 제거**

S3/S4 return 블록의 `<div className="shrink-0 w-full max-w-md mx-auto px-5 pt-4 pb-3 border-b border-lilac-soft">` 내부를 아래로 교체 (SkillChipRow 행과 `{daily && pass && (...)}` 상태줄 삭제, `headerCard` + `partnerBanner` 만 유지):
```tsx
        <div className="shrink-0 w-full max-w-md mx-auto px-5 pt-4 pb-3 border-b border-lilac-soft">
          {headerCard}
          {partnerBanner}
        </div>
```

- [ ] **Step 5: PassSheet 에 pass/daily/onExtended 전달 (Task 5에서 소비)**

같은 파일 S3/S4 return 의 `{showPassSheet && (<PassSheet .../>)}` 를 아래로 교체:
```tsx
        {showPassSheet && (
          <PassSheet
            relationshipId={relationship.id}
            pass={pass}
            daily={daily}
            onClose={() => setShowPassSheet(false)}
            onExtended={() => void load()}
            onPurchased={() => {
              setShowPassSheet(false);
              void load();
            }}
          />
        )}
```

- [ ] **Step 6: 타입 체크**

Run: `npx tsc --noEmit`
Expected: `PassSheet` 의 새 prop(`pass`/`daily`/`onExtended`)이 아직 없어 에러가 날 수 있음 → **Task 5 완료 후 통과**. 이 시점엔 `page.tsx` 자체의 문법/미사용 import 에러가 없는지만 확인(‎`SkillChipRow`/`formatPassDDay` 잔재 없음).

- [ ] **Step 7: 커밋**

```bash
git add app/relationship/page.tsx
git commit -m "feat(rel): 활성 화면 헤더 1행화 + 패스 pill, 상태줄/스킬행 제거"
```

---

## Task 5: PassSheet — 패스 상태 + 오늘 연장 + 구매

헤더에서 뺀 정보(남은시간 상세, 오늘 남은 대화)를 시트로 이동하고, 오늘 +5턴 연장 버튼을 추가한다. 구매 목록은 기존 `PassPanel` 위임 유지.

**Files:**
- Modify: `components/relationship/PassSheet.tsx`

- [ ] **Step 1: props 확장 + 상태/연장 UI 추가**

`PassSheet` 전체를 아래로 교체 (기존 chrome 유지 + body에 상태·연장 추가):
```tsx
"use client";

import { useEffect, useState } from "react";
import { createPortal } from "react-dom";
import { useRouter } from "next/navigation";
import PassPanel from "./PassPanel";
import { formatPassRemaining } from "@/lib/relationship/passDisplay";
import {
  EXTEND_COST,
  EXTEND_TURNS,
  PASS_PLAN_BY_KIND,
} from "@/lib/relationship/types";

interface PassInfo {
  kind: string;
  expiresAt: string;
}
interface DailyInfo {
  used: number;
  allowance: number;
}

interface PassSheetProps {
  relationshipId: string;
  pass: PassInfo | null;
  daily: DailyInfo | null;
  onClose: () => void;
  onPurchased: () => void;
  onExtended: () => void;
}

export default function PassSheet({
  relationshipId,
  pass,
  daily,
  onClose,
  onPurchased,
  onExtended,
}: PassSheetProps) {
  const router = useRouter();
  const [extending, setExtending] = useState(false);
  const [extendError, setExtendError] = useState<string | null>(null);

  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", onKey);
    document.body.style.overflow = "hidden";
    return () => {
      document.removeEventListener("keydown", onKey);
      document.body.style.overflow = "";
    };
  }, [onClose]);

  const planDays = pass ? PASS_PLAN_BY_KIND[pass.kind as keyof typeof PASS_PLAN_BY_KIND]?.days ?? 0 : 0;
  const remaining =
    pass && planDays
      ? formatPassRemaining(new Date(pass.expiresAt).getTime(), planDays, Date.now())
      : null;
  const todayLeft = daily ? Math.max(0, daily.allowance - daily.used) : null;

  const handleExtend = async () => {
    if (extending) return;
    setExtending(true);
    setExtendError(null);
    try {
      const res = await fetch("/api/relationship/extend", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ relationshipId }),
      });
      const data = await res.json().catch(() => ({}));
      if (res.status === 402) {
        if (data?.error === "INSUFFICIENT_STARS") router.push("/shop");
        else setExtendError("패스가 만료됐어 — 다시 등록해줄래?");
        return;
      }
      if (!res.ok) {
        setExtendError("연장이 안 됐어. 잠시 후 다시 시도해줄래?");
        return;
      }
      onExtended();
    } catch {
      setExtendError("연결이 흔들렸어. 잠시 후 다시 시도해줄래?");
    } finally {
      setExtending(false);
    }
  };

  return createPortal(
    <div
      className="fixed inset-0 z-[90] flex items-end justify-center bg-night/70 backdrop-blur-sm animate-fade-in"
      onClick={onClose}
      role="dialog"
      aria-modal="true"
      aria-label="패스 연장·구매"
    >
      <div
        className="w-full max-w-md mx-auto bg-cream rounded-t-3xl border border-lilac-mid/30 shadow-[0_-4px_24px_rgba(31,23,53,0.18)] max-h-[85vh] overflow-y-auto pb-[max(env(safe-area-inset-bottom),16px)]"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="flex justify-center pt-3 pb-1">
          <div className="w-10 h-1 bg-lilac-mid/40 rounded-full" />
        </div>
        <div className="flex items-center justify-between px-5 pt-2 pb-3">
          <h2 className="font-display text-[16px] font-bold text-eye-purple">패스 · 오늘 대화</h2>
          <button
            onClick={onClose}
            aria-label="닫기"
            className="w-8 h-8 rounded-full flex items-center justify-center text-text-light/70 hover:bg-lilac-soft/50"
          >
            ✕
          </button>
        </div>

        <div className="px-5">
          {pass && (
            <div className="rounded-2xl border border-lilac-mid/25 bg-white/70 px-4 py-3 mb-3 text-[12.5px]">
              <div className="flex items-center justify-between">
                <span className="text-text-light">지금 패스</span>
                <span className="font-bold text-eye-purple">{remaining}</span>
              </div>
              {todayLeft !== null && (
                <div className="flex items-center justify-between mt-1.5">
                  <span className="text-text-light">오늘 남은 대화</span>
                  <span className="font-bold text-eye-purple">{todayLeft} / 대략 {DAILY_TURN_CAP}번</span>
                </div>
              )}
              <button
                type="button"
                onClick={() => void handleExtend()}
                disabled={extending}
                className="mt-3 w-full flex items-center justify-between rounded-xl bg-lilac-soft/50 px-3.5 py-2.5 active:scale-[0.99] transition disabled:opacity-60"
              >
                <span className="text-[12.5px] font-bold text-eye-purple">
                  {extending ? "연장하는 중…" : `오늘 ${EXTEND_TURNS}번 더 (제한 없이)`}
                </span>
                <span className="text-[12.5px] font-bold text-lilac-deep">⭐{EXTEND_COST}</span>
              </button>
              {extendError && (
                <p className="mt-1.5 text-[11px] text-red-500">{extendError}</p>
              )}
            </div>
          )}

          <PassPanel relationshipId={relationshipId} onPurchased={onPurchased} />
        </div>
      </div>
    </div>,
    document.body
  );
}
```
> 주의: `DAILY_TURN_CAP` import 필요 — 위 import 목록에 이미 `EXTEND_COST`/`EXTEND_TURNS`/`PASS_PLAN_BY_KIND` 만 있으니 `DAILY_TURN_CAP` 을 추가한다.

- [ ] **Step 2: import에 DAILY_TURN_CAP 포함 확인**

`components/relationship/PassSheet.tsx` 상단 types import 를 다음으로 맞춘다:
```tsx
import {
  DAILY_TURN_CAP,
  EXTEND_COST,
  EXTEND_TURNS,
  PASS_PLAN_BY_KIND,
} from "@/lib/relationship/types";
```

- [ ] **Step 3: 타입 체크 (Task 4 + 5 합류)**

Run: `npx tsc --noEmit`
Expected: EXIT 0 (page.tsx ↔ PassSheet prop 정합)

- [ ] **Step 4: 브라우저 확인**

개발서버 후 로그인 → 활성 패스 있는 계정으로 `/relationship` → 헤더 우측 `NN/총시간 | 패스권 구매` 노출 확인 → 탭 → 시트에 "지금 패스 / 오늘 남은 대화 / 오늘 N번 더 ⭐5 / 구매목록(시간권)" 확인.
Expected: 콘솔 에러 없음, 시트 정상.

- [ ] **Step 5: 커밋**

```bash
git add components/relationship/PassSheet.tsx
git commit -m "feat(rel): 패스 시트에 남은시간/오늘대화/연장 + 시간권 구매"
```

---

## Task 6: ⚡ 스킬 시트 (SkillSheet 신규 + ThreadChat 진입점)

상시 스킬 칩 행 대신, 입력창 왼쪽 ⚡ 버튼 → 바텀시트로 스킬 4종 노출·실행.

**Files:**
- Create: `components/relationship/SkillSheet.tsx`
- Modify: `components/relationship/ThreadChat.tsx`

- [ ] **Step 1: SkillSheet 컴포넌트 생성 (프레젠테이션)**

`components/relationship/SkillSheet.tsx`:
```tsx
"use client";

import { useEffect } from "react";
import { createPortal } from "react-dom";
import type { RelationshipSkill } from "@/lib/relationship/skills";

interface SkillSheetProps {
  skills: RelationshipSkill[];
  busyKey: string | null;
  onLaunch: (key: string) => void;
  onClose: () => void;
}

export default function SkillSheet({ skills, busyKey, onLaunch, onClose }: SkillSheetProps) {
  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [onClose]);

  return createPortal(
    <div
      className="fixed inset-0 z-[90] flex items-end justify-center bg-night/70 backdrop-blur-sm animate-fade-in"
      onClick={onClose}
      role="dialog"
      aria-modal="true"
      aria-label="스킬"
    >
      <div
        className="w-full max-w-md mx-auto bg-cream rounded-t-3xl border border-lilac-mid/30 shadow-[0_-4px_24px_rgba(31,23,53,0.18)] pb-[max(env(safe-area-inset-bottom),16px)]"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="flex justify-center pt-3 pb-1">
          <div className="w-10 h-1 bg-lilac-mid/40 rounded-full" />
        </div>
        <div className="flex items-center justify-between px-5 pt-2 pb-3">
          <h2 className="font-display text-[16px] font-bold text-eye-purple">이런 것도 꺼내 쓸 수 있어</h2>
          <button
            onClick={onClose}
            aria-label="닫기"
            className="w-8 h-8 rounded-full flex items-center justify-center text-text-light/70 hover:bg-lilac-soft/50"
          >
            ✕
          </button>
        </div>
        <div className="px-5 pb-2 flex flex-col gap-2">
          {skills.map((s) => (
            <button
              key={s.key}
              type="button"
              onClick={() => onLaunch(s.key)}
              disabled={busyKey === s.key}
              className="flex items-center gap-3 rounded-2xl border border-lilac-mid/25 bg-white px-4 py-3 text-left active:scale-[0.99] transition disabled:opacity-60"
            >
              <span className="text-[20px]" aria-hidden>{s.emoji}</span>
              <div className="flex-1 min-w-0">
                <p className="text-[13.5px] font-bold text-eye-purple">
                  {busyKey === s.key ? "여는 중…" : s.label}
                </p>
                <p className="text-[11.5px] text-text-light leading-snug">{s.tagline}</p>
              </div>
              <span className="text-[12px] font-bold text-lilac-deep shrink-0">⭐{s.starCost}</span>
            </button>
          ))}
        </div>
      </div>
    </div>,
    document.body
  );
}
```

- [ ] **Step 2: ThreadChat 에 ⚡ 버튼 + 시트 배선**

`components/relationship/ThreadChat.tsx`:

(a) import 추가 (상단, 기존 imports 뒤):
```tsx
import SkillSheet from "./SkillSheet";
import { listActiveSkills } from "@/lib/relationship/skills";
```
(b) 컴포넌트 본문 상단 state 추가 (다른 `useState` 근처):
```tsx
  const [showSkills, setShowSkills] = useState(false);
```
(c) 입력 `<form onSubmit={handleSubmit} className="flex items-end gap-2">` 의 **첫 자식**으로 `<textarea>` 앞에 ⚡ 버튼 삽입:
```tsx
              <button
                type="button"
                onClick={() => setShowSkills(true)}
                aria-label="스킬 열기"
                className="shrink-0 h-[44px] w-[44px] rounded-xl bg-lilac-soft/50 text-lilac-deep flex items-center justify-center active:scale-95 transition"
              >
                <svg className="w-5 h-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden>
                  <path d="M11,15H6L13,1V9H18L11,23V15Z" />
                </svg>
              </button>
```
(d) 컴포넌트 return 의 최상위 fragment 안(토스트 포털 근처)에 시트 렌더 추가:
```tsx
      {showSkills && (
        <SkillSheet
          skills={listActiveSkills()}
          busyKey={busyKey}
          onLaunch={(k) => {
            setShowSkills(false);
            launch(k);
          }}
          onClose={() => setShowSkills(false)}
        />
      )}
```
> `launch`/`busyKey` 는 이미 `useSkillLaunch` 로 컴포넌트에 존재(‎`[SKILL:]` 칩에서 사용 중) — 재사용.

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: EXIT 0

- [ ] **Step 4: 브라우저 확인**

`/relationship` 활성 화면 입력창 왼쪽 ⚡ → 탭 → 스킬 시트(관계 체크인 ⭐45 · 걔 속마음 ⭐40 · 우리 궁합 ⭐40 · 싸움 잘잘못 판정 ⭐30) → 항목 탭 시 기존 런치 흐름(tarot_draw→/tarot/draw, compat/dialogue→해당 라우트) 동작.
Expected: 콘솔 에러 없음.

- [ ] **Step 5: 커밋**

```bash
git add components/relationship/SkillSheet.tsx components/relationship/ThreadChat.tsx
git commit -m "feat(rel): 입력창 ⚡ 스킬 버튼 → 스킬 바텀시트"
```

---

## Task 7: 캡 도달 UI를 칩으로 재스타일 (ThreadChat)

기존 골드 연장 버튼을 "💬 오늘 대화 추가 구매 · ⭐N로 N번 더" 칩 형태로. 로직(`handleExtend`)은 유지.

**Files:**
- Modify: `components/relationship/ThreadChat.tsx` (capReached 블록, 현재 `{capReachedLocal ? (...)}`)

- [ ] **Step 1: capReached 블록 JSX 교체**

`{capReachedLocal ? (` 다음의 `<div className="flex flex-col items-center gap-2 py-1"> ... </div>` 를 아래로 교체:
```tsx
            <div className="flex flex-col items-center gap-1.5 py-1">
              <button
                type="button"
                onClick={() => void handleExtend()}
                disabled={extending}
                className="inline-flex items-center gap-1.5 rounded-full bg-lilac-deep text-white font-bold text-[12.5px] px-4 py-2.5 active:scale-[0.97] transition disabled:opacity-60"
              >
                <span aria-hidden>💬</span>
                {extending
                  ? "연장하는 중…"
                  : `오늘 대화 추가 구매 · ⭐${EXTEND_COST}로 ${EXTEND_TURNS}번 더`}
              </button>
              <p className="text-[11px] text-text-light text-center">
                오늘은 여기까지 · 내일 자정에 다시 채워져
              </p>
              {extendError && <p className="text-[11px] text-red-500">{extendError}</p>}
            </div>
```

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: EXIT 0

- [ ] **Step 3: 브라우저 확인 (가능하면)**

캡 도달(하루 20턴)은 재현이 무거우니, 최소 타입/렌더 확인. 가능하면 QA 시드 계정에서 캡 도달 상태를 만들어 칩 노출/⭐5 연장 동작 확인. 어려우면 코드 리뷰로 대체하고 커밋.
Expected: 칩 스타일 노출, 탭 시 기존 extend 흐름.

- [ ] **Step 4: 커밋**

```bash
git add components/relationship/ThreadChat.tsx
git commit -m "feat(rel): 캡 도달 연장 UI를 💬 칩으로 재스타일"
```

---

## Task 8: 고아화된 SkillChipRow 삭제

Task 4에서 페이지 헤더의 유일 사용처를 제거했으므로 파일이 고아가 됨.

**Files:**
- Delete: `components/relationship/SkillChipRow.tsx`

- [ ] **Step 1: 잔여 참조 확인**

Run: `git grep -n "SkillChipRow"`
Expected: 매치 없음 (Task 4에서 import/사용 제거 완료). 매치가 있으면 먼저 해당 참조 제거.

- [ ] **Step 2: 삭제**

```bash
git rm components/relationship/SkillChipRow.tsx
```

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: EXIT 0

- [ ] **Step 4: 커밋**

```bash
git commit -m "chore(rel): 미사용 SkillChipRow 제거 (⚡ 시트로 대체)"
```

---

## Task 9: 미등록 소개(S1) 카피 정리 (page.tsx)

구조 6블록 유지, 카피만 변경.

**Files:**
- Modify: `app/relationship/page.tsx` (미등록 return 블록, `{showOnboarding ? ... : (` 안의 `<div className="px-5">`)

- [ ] **Step 1: 뱃지 + 히어로 subtitle**

히어로 뱃지 텍스트 `우리 사이` → `연애 상담` (뱃지 `<span>` 안의 텍스트). 그리고 히어로 설명 `<p>` 를 아래로 교체:
```tsx
              <p className="mt-3 text-[13px] text-text-light leading-relaxed">
                한 번 보고 끝나는 상담이 아니야. 상대를 등록하면 별콩이랑 언제든 이어서
                대화할 수 있어, 지난 얘기를 다 기억하니까.
              </p>
```

- [ ] **Step 2: 핵심 카드 (헤더 + 카드 본문) 교체**

`{/* 무엇을 할 수 있나 ... */}` 아래 `<p>이런 걸 할 수 있어</p>` 를 `별콩이는 이런 친구야` 로 바꾸고, 핵심 카드(💬 "별콩이와 연애 상담 대화") 블록의 제목/본문을 교체:
```tsx
              <p className="text-[13px] font-bold text-eye-purple mb-3 px-1">
                별콩이는 이런 친구야
              </p>
              <div className="rounded-2xl p-4 border border-lilac-mid/40 bg-gradient-to-br from-lilac-soft/60 to-cream-warm mb-3">
                <div className="flex items-start gap-2.5">
                  <span className="text-[24px] leading-none" aria-hidden>💜</span>
                  <div>
                    <p className="text-[14px] font-bold text-eye-purple leading-snug">
                      너만의 연애 상담 친구
                    </p>
                    <p className="text-[11.5px] text-text-light mt-1 leading-relaxed">
                      설레는 것도, 서운한 것도, 어떻게 해야 할지 모를 때도 그냥 편하게
                      털어놔. 별콩이가 네 편에서 같이 고민해줄게.
                    </p>
                  </div>
                </div>
              </div>
```
(‎스킬 4종 그리드 `SKILL_PREVIEWS` 블록은 그대로 둔다.)

- [ ] **Step 3: 이용권 — 설명형 한 블록 + 인라인 가격**

`{/* 패스 가격 */}` 블록(‎`<p>이용권 안내</p>` + `PASS_PLANS.map(...)` 버튼형 3줄)을 아래로 교체:
```tsx
            <div className="mb-4">
              <p className="text-[13px] font-bold text-eye-purple mb-2 px-1">이용권</p>
              <div className="rounded-2xl border border-lilac-mid/30 bg-white/70 p-4">
                <p className="text-[12.5px] text-text-light leading-relaxed mb-2.5">
                  패스를 켜 두면 그 기간 동안 매일 별콩이랑 연애 상담을 이어갈 수 있어.
                </p>
                <p className="text-[13px] font-bold text-eye-purple text-center">
                  {PASS_PLANS.map((p) => `${p.label} ⭐${p.cost}`).join("  ·  ")}
                </p>
              </div>
            </div>
```

- [ ] **Step 4: 하루 한도 문구 "대략" 완화**

하루 한도 박스(`📌 하루에 얼마나 대화할 수 있어?`)의 첫 `<p>` 를 아래로 교체:
```tsx
              <p className="mt-2 text-[12.5px] text-eye-purple/90 leading-relaxed">
                패스가 있는 동안 하루 <b>대략 {DAILY_TURN_CAP}번</b>(4천~8천 자쯤) 주고받을
                수 있어. 웬만한 고민 하나는 그날 깊이 풀 양이야.
              </p>
```

- [ ] **Step 5: 타입 체크 + 브라우저**

Run: `npx tsc --noEmit` → EXIT 0
브라우저: 미등록(신규/패스없고 relationship 없는) 계정으로 `/relationship` → 뱃지 "연애 상담", 히어로/핵심카드/이용권/한도 카피 반영 확인.

- [ ] **Step 6: 커밋**

```bash
git add app/relationship/page.tsx
git commit -m "feat(rel): S1 소개 카피 정리(친구 톤·이용권 설명형·대략 20번)"
```

---

## Task 10: 안내 문구 "대략" 정렬 (PassPanel / PassConfirmModal)

S1과 톤 일치 — "하루 최대 20번" → "하루 대략 20번".

**Files:**
- Modify: `components/relationship/PassPanel.tsx` (info 박스 `하루 최대 {DAILY_TURN_CAP}번`)
- Modify: `components/relationship/PassConfirmModal.tsx` (동일 info 박스 있으면)

- [ ] **Step 1: 문구 교체**

두 파일에서 `하루 최대 <b>{DAILY_TURN_CAP}번</b>` → `하루 대략 <b>{DAILY_TURN_CAP}번</b>` 으로 교체. (PassConfirmModal 에 동일 문구가 없으면 PassPanel 만.)

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: EXIT 0

- [ ] **Step 3: 커밋**

```bash
git add components/relationship/PassPanel.tsx components/relationship/PassConfirmModal.tsx
git commit -m "chore(rel): 패스 안내 문구 '대략 20번'으로 정렬"
```

---

## Task 11: verdict / refund 문구 리네임

**Files:**
- Modify: `app/relationship/verdict/[id]/page.tsx:~201`
- Modify: `app/refund/page.tsx:~113`

- [ ] **Step 1: verdict 문구**

`app/relationship/verdict/[id]/page.tsx` 의 `우리 사이로 돌아가기` → `연애 상담으로 돌아가기`.

- [ ] **Step 2: refund 문구**

`app/refund/page.tsx` 의 `기간 단위로 제공되는 이용권(예: 우리 사이 패스)에는` → `기간 단위로 제공되는 이용권(예: 연애 상담 패스)에는`.

- [ ] **Step 3: 타입 체크 + 잔여 확인**

Run: `npx tsc --noEmit` → EXIT 0
Run: `git grep -n "우리 사이" -- app/ components/`
Expected: UI 라벨 매치 없음. (남는 건 `app/tarot/draw` 의 내부 concern 라벨/주석 등 비라벨 — 이번 범위 밖, 그대로 둠.)

- [ ] **Step 4: 커밋**

```bash
git add app/relationship/verdict/[id]/page.tsx app/refund/page.tsx
git commit -m "feat(rel): verdict/refund 문구 우리 사이→연애 상담"
```

---

## Task 12: 전체 검증

**Files:** 없음 (검증)

- [ ] **Step 1: 타입 + 로직 테스트**

```bash
npx tsc --noEmit
node --import tsx --test lib/relationship/types.test.ts lib/relationship/passDisplay.test.ts
```
Expected: tsc EXIT 0, 테스트 전부 PASS

- [ ] **Step 2: 브라우저 상태별 스윕**

개발서버 → `/relationship` 4상태 확인:
- S1 미등록: 카피/뱃지
- S2 패스없음: 헤더(‎pill 없음) + PassPanel
- S3 활성: 헤더 1행 + `NN/총시간 | 패스권 구매` + ⚡ 스킬 시트 + 대화 영역 확대 체감
- S4 캡도달: 💬 칩
그리고 하단탭 라벨 `고민톡 · 별콩 운세 · 연애 상담 · 별콩 상점 · 내 정보` 정합/폭.
Expected: 콘솔 에러 없음.

- [ ] **Step 3: (선택) API 회귀**

가능하면 `npm run qa`(dev 서버 필요)로 관계 스레드/스킬 회귀. 스킵 시 사유 기록.

- [ ] **Step 4: 마무리 커밋 (수정분 있으면)**

```bash
git add -A
git commit -m "test(rel): 연애 상담 리디자인 검증 통과"
```

---

## Self-Review 결과 (작성자 체크)

- **스펙 커버리지**: §2 명명→T1, §3 헤더/스킬→T4·T6, §4 시간권→T2·T3·T4·T5, §5 캡 칩→T7, §6 S1→T9, 리네임(verdict/refund)→T11, 고아 정리→T8, 안내톤→T10. 전 섹션 대응됨.
- **스펙과의 의도적 차이 1건**: 스펙 §4.3은 "패스 시트"에 오늘 연장(⭐5)을 포함한다고 적었고, 본 계획은 그대로 시트에도 연장 버튼을 넣되(T5) **일일 +5턴의 주 진입점은 캡 도달 칩(T7)** 임을 명확히 한다. "패스 연장(=시간권 재구매)"은 구매목록(PassPanel), "오늘 대화 +5턴"은 시트 버튼+캡 칩 — 두 개념 분리. (스펙과 모순 아님, 구현상 명확화.)
- **타입 정합**: `formatPassRemaining(expiresMs, planDays, nowMs)` 시그니처가 T3 정의·T4·T5 호출에서 일치. `PassSheet` props(pass/daily/onExtended/onPurchased)가 T4 호출·T5 정의에서 일치. `SkillSheet` props(skills/busyKey/onLaunch/onClose)가 T6 정의·호출에서 일치.
- **플레이스홀더 없음**: 모든 코드 단계에 실제 코드 포함.
