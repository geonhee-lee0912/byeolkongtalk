# 사주 MBTI — 축 매핑 로직 구현 플랜 (하위 프로젝트 A)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 생년월일시 → 팔자 유형(4축 2분법 + 오행 주기질)을 산출하는 결정론 순수 모듈 `lib/saju-mbti/` 를 만든다.

**Architecture:** `calcSaju`(tyme4ts) 결과 위에서 4축 raw 점수를 산출(음양·강유·재인·생단, 재료 분리) → 합성 샘플 분위 테이블로 백분위(%)·극 매핑. 십신·오행관계는 `lib/saju/pairing.ts` 재사용. calc/DB 무변경.

**Tech Stack:** TypeScript(strict), node:test + tsx, tyme4ts(간접), 기존 `lib/saju/{calc,pairing,elements}.ts`.

**Spec:** `docs/superpowers/specs/2026-08-23-사주-mbti-축-매핑-design.md`

---

## File Structure

- `lib/saju-mbti/constants.ts` — 정적 테이블(위치가중치·지장간본기·정순·오행음양)·타입·분위테이블(보정 산출물)
- `lib/saju-mbti/mapping.ts` — 헬퍼(activeChars·stemOf·elementOf) + 4축 raw + 오행주기질 + 백분위 + `paljaType()`
- `lib/saju-mbti/mapping.test.ts` — 단위·경계·통합 테스트
- `lib/saju-mbti/test-utils.ts` — 테스트용 `mkSaju` 빌더(제어된 SajuResult)
- `scripts/saju-mbti-calibrate.mjs` — 합성 샘플 → 분위 테이블 + 분포 점검 출력

**재사용(수정 안 함):** `lib/saju/calc.ts`(`SajuResult`), `lib/saju/pairing.ts`(`elementRelation`·`tenGod`·`STEM_ELEMENT`), `lib/saju/elements.ts`(`FiveElement`).

**테스트 실행:** `node --import tsx --test lib/saju-mbti/mapping.test.ts`

---

## Task 1: 정적 상수 테이블 (constants.ts)

**Files:**
- Create: `lib/saju-mbti/constants.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Create `lib/saju-mbti/mapping.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { JANGAN_BONGI, STEM_ORDER, BRANCH_ORDER, ELEMENT_YINYANG, POSITION_WEIGHT } from "./constants.ts";
import { STEM_ELEMENT } from "@/lib/saju/pairing";

test("constants — 지장간 본기 12지지 전부·유효 천간", () => {
  assert.equal(Object.keys(JANGAN_BONGI).length, 12);
  for (const [branch, stem] of Object.entries(JANGAN_BONGI)) {
    assert.ok(BRANCH_ORDER.includes(branch), `${branch} 미등록 지지`);
    assert.ok(STEM_ORDER.includes(stem), `${stem} 미등록 천간`);
    assert.ok(STEM_ELEMENT[stem], `${stem} 오행 없음`);
  }
  assert.equal(JANGAN_BONGI["자"], "계");
  assert.equal(JANGAN_BONGI["인"], "갑");
});

test("constants — 오행 음양(목화 양·금수 음·토 중립)", () => {
  assert.equal(ELEMENT_YINYANG["목"], 1);
  assert.equal(ELEMENT_YINYANG["화"], 1);
  assert.equal(ELEMENT_YINYANG["토"], 0);
  assert.equal(ELEMENT_YINYANG["금"], -1);
  assert.equal(ELEMENT_YINYANG["수"], -1);
});

test("constants — 위치 가중치(월지 3.0·일간 0)", () => {
  assert.equal(POSITION_WEIGHT.monthBranch, 3.0);
  assert.equal(POSITION_WEIGHT.dayStem, 0); // 일간 기준점(합산 제외)
  assert.equal(POSITION_WEIGHT.dayBranch, 1.5);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `Cannot find module './constants.ts'`

- [ ] **Step 3: Write minimal implementation**

Create `lib/saju-mbti/constants.ts`:

```ts
import type { FiveElement } from "@/lib/saju/elements";

export type Pole = "양" | "음" | "강" | "유" | "재" | "인" | "생" | "단";
export type AxisKey = "yinYang" | "strength" | "wealth" | "nurture";

// 위치 가중치 (일간은 기준점 → 축 합산에서 제외 = 0)
export const POSITION_WEIGHT = {
  yearStem: 1.0,
  yearBranch: 1.0,
  monthStem: 1.2,
  monthBranch: 3.0,
  dayStem: 0,
  dayBranch: 1.5,
  hourStem: 1.0,
  hourBranch: 1.0,
} as const;

// 오행 주기질 산출 시 일간 가중(본인 핵심 → 일지와 동일 1.5). 축 합산엔 미적용.
export const DAY_STEM_ELEMENT_WEIGHT = 1.5;

// 생/단 이웃쌍 관계에서 일간 참여 가중(일주 비중)
export const DAY_STEM_PAIR_WEIGHT = 1.5;

// 지장간 본기 (지지 → 대표 천간). 정통 정기(正氣) 기준.
export const JANGAN_BONGI: Record<string, string> = {
  자: "계", 축: "기", 인: "갑", 묘: "을", 진: "무", 사: "병",
  오: "정", 미: "기", 신: "경", 유: "신", 술: "무", 해: "임",
};

// 천간·지지 정순 (표기상 음양 = index 짝수 → 양)
export const STEM_ORDER = ["갑", "을", "병", "정", "무", "기", "경", "신", "임", "계"];
export const BRANCH_ORDER = ["자", "축", "인", "묘", "진", "사", "오", "미", "신", "유", "술", "해"];

// 오행 기질 음양 (목화 양 / 금수 음 / 토 중립)
export const ELEMENT_YINYANG: Record<FiveElement, number> = {
  목: 1, 화: 1, 토: 0, 금: -1, 수: -1,
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/constants.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 축 매핑 정적 상수 테이블"
```

---

## Task 2: 테스트 빌더 + activeChars/stemOf/elementOf 헬퍼

**Files:**
- Create: `lib/saju-mbti/test-utils.ts`, `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Create `lib/saju-mbti/test-utils.ts` (제어된 SajuResult 빌더 — pairRelation 테스트 패턴 차용):

```ts
import type { SajuResult } from "@/lib/saju/calc";
import { STEM_ELEMENT } from "@/lib/saju/pairing";

/** 8글자를 직접 지정해 SajuResult 를 만든다(매핑이 읽는 필드만 채움). */
export function mkSaju(
  pillars: {
    year: [string, string];
    month: [string, string];
    day: [string, string];
    hour: [string, string];
  },
  opts: { hourKnown?: boolean } = {}
): SajuResult {
  const cell = ([s, b]: [string, string]) => ({ stem: s, branch: b, hanja: "" });
  const dayStem = pillars.day[0];
  return {
    pillars: {
      year: cell(pillars.year),
      month: cell(pillars.month),
      day: cell(pillars.day),
      hour: cell(pillars.hour),
    },
    dayStem,
    dayElement: STEM_ELEMENT[dayStem],
    elementCount: { 목: 0, 화: 0, 토: 0, 금: 0, 수: 0 },
    yinYangCount: { yang: 0, yin: 0 },
    koreanString: "",
    hanjaString: "",
    input: { gender: "other", hourKnown: opts.hourKnown ?? true, inputCalendar: "solar", isLeapMonth: false },
  };
}
```

Append to `mapping.test.ts`:

```ts
import { activeChars, stemOf, elementOf } from "./mapping.ts";
import { mkSaju } from "./test-utils.ts";

test("activeChars — 일간 제외 7글자(시간 앎) / 5글자(시간 모름)", () => {
  const s = mkSaju({ year: ["갑", "자"], month: ["병", "인"], day: ["무", "오"], hour: ["경", "신"] });
  const known = activeChars(s);
  assert.equal(known.length, 7); // 일간(무) 제외
  assert.ok(!known.some((c) => c.char === "무" && c.isStem)); // 일간 없음
  const unknown = activeChars(mkSaju(
    { year: ["갑", "자"], month: ["병", "인"], day: ["무", "오"], hour: ["경", "신"] },
    { hourKnown: false }
  ));
  assert.equal(unknown.length, 5); // 시간·시지도 제외
});

test("stemOf — 천간 그대로 / 지지는 본기", () => {
  assert.equal(stemOf({ char: "갑", isStem: true, weight: 1 }), "갑");
  assert.equal(stemOf({ char: "인", isStem: false, weight: 1 }), "갑"); // 인 본기 갑
});

test("elementOf — 본기 오행", () => {
  assert.equal(elementOf({ char: "자", isStem: false, weight: 1 }), "수"); // 자→계→수
  assert.equal(elementOf({ char: "병", isStem: true, weight: 1 }), "화");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `Cannot find module './mapping.ts'`

- [ ] **Step 3: Write minimal implementation**

Create `lib/saju-mbti/mapping.ts`:

```ts
import type { SajuResult } from "@/lib/saju/calc";
import type { FiveElement } from "@/lib/saju/elements";
import { STEM_ELEMENT } from "@/lib/saju/pairing";
import { JANGAN_BONGI, POSITION_WEIGHT } from "./constants.ts";

export interface CharCell {
  char: string; // 표면 글자(한글)
  isStem: boolean;
  weight: number;
}

/** 일간 제외, 시간 모름이면 시주 제외한 글자·가중치 목록. */
export function activeChars(saju: SajuResult): CharCell[] {
  const p = saju.pillars;
  const cells: CharCell[] = [
    { char: p.year.stem, isStem: true, weight: POSITION_WEIGHT.yearStem },
    { char: p.year.branch, isStem: false, weight: POSITION_WEIGHT.yearBranch },
    { char: p.month.stem, isStem: true, weight: POSITION_WEIGHT.monthStem },
    { char: p.month.branch, isStem: false, weight: POSITION_WEIGHT.monthBranch },
    // 일간(p.day.stem)은 기준점 → 제외
    { char: p.day.branch, isStem: false, weight: POSITION_WEIGHT.dayBranch },
  ];
  if (saju.input.hourKnown) {
    cells.push({ char: p.hour.stem, isStem: true, weight: POSITION_WEIGHT.hourStem });
    cells.push({ char: p.hour.branch, isStem: false, weight: POSITION_WEIGHT.hourBranch });
  }
  return cells;
}

/** 글자가 환원되는 천간(천간 그대로 / 지지는 본기). */
export function stemOf(cell: CharCell): string {
  return cell.isStem ? cell.char : JANGAN_BONGI[cell.char];
}

/** 글자의 오행(본기 환원 기준). */
export function elementOf(cell: CharCell): FiveElement {
  return STEM_ELEMENT[stemOf(cell)];
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/test-utils.ts lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): activeChars/stemOf/elementOf 헬퍼 + 테스트 빌더"
```

---

## Task 3: 음/양 축 raw (표기상 70% + 오행기질 30%)

**Files:**
- Modify: `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { yinYangRaw } from "./mapping.ts";

test("yinYangRaw — 전부 양(갑·인 계열)이면 양(+)", () => {
  // 갑(양목) 자(양·본기 계=수 음30%) 등 — 표기상 양 우세면 raw>0
  const s = mkSaju({ year: ["갑", "인"], month: ["갑", "인"], day: ["무", "오"], hour: ["갑", "인"] });
  assert.ok(yinYangRaw(s) > 0);
});

test("yinYangRaw — 대칭 상쇄 확인(양간+음간 동일가중이면 표기상 0 근처)", () => {
  // 년간 갑(양)·년지 축(음) 표기상 상쇄, 오행기질만 남음
  const s = mkSaju({ year: ["갑", "축"], month: ["갑", "축"], day: ["무", "오"], hour: ["갑", "축"] });
  const raw = yinYangRaw(s);
  // 갑(목=화양계열 +1) 3개, 축(본기 기=토 0) 3개, 오행기질 기여만 → 양(+)
  assert.ok(raw > 0);
});

test("yinYangRaw — 시간 모름이면 시주 미반영(값이 달라짐)", () => {
  const p = { year: ["갑", "자"], month: ["병", "인"], day: ["무", "오"], hour: ["임", "해"] } as const;
  assert.notEqual(yinYangRaw(mkSaju(p)), yinYangRaw(mkSaju(p, { hourKnown: false })));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `yinYangRaw is not exported` / `is not a function`

- [ ] **Step 3: Write minimal implementation**

Append to `mapping.ts`:

```ts
import { STEM_ORDER, BRANCH_ORDER, ELEMENT_YINYANG } from "./constants.ts";

/** 음/양 raw. + = 양. 표기상(표면 index 짝수=양) 0.7 + 오행기질(목화+1·금수-1·토0, 본기) 0.3, 위치가중. */
export function yinYangRaw(saju: SajuResult): number {
  let sum = 0;
  for (const cell of activeChars(saju)) {
    const order = cell.isStem ? STEM_ORDER : BRANCH_ORDER;
    const surfaceParity = order.indexOf(cell.char) % 2 === 0 ? 1 : -1;
    const elementYy = ELEMENT_YINYANG[elementOf(cell)];
    sum += (0.7 * surfaceParity + 0.3 * elementYy) * cell.weight;
  }
  return sum;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 음양 축 raw (표기상 7 + 오행기질 3)"
```

---

## Task 4: 강/유 축 raw (신강/신약)

**Files:**
- Modify: `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { strengthRaw } from "./mapping.ts";

test("strengthRaw — 월지·일지가 일간을 도우면 득령+득지(≥60)", () => {
  // 일간 갑목. 월지 인(본기 갑=목, 비겁), 일지 자(본기 계=수, 인성=수생목)
  const s = mkSaju({ year: ["경", "신"], month: ["병", "인"], day: ["갑", "자"], hour: ["경", "신"] });
  assert.ok(strengthRaw(s) >= 60); // 득령40 + 득지20
});

test("strengthRaw — 온통 극·설기면 신약(낮음)", () => {
  // 일간 갑목. 월지 신(본기 경=금, 극아), 일지 오(본기 정=화, 아생 설기), 주변 금·화
  const s = mkSaju({ year: ["경", "신"], month: ["경", "신"], day: ["갑", "오"], hour: ["병", "오"] });
  assert.ok(strengthRaw(s) < 40);
});

test("strengthRaw — 0~100 범위", () => {
  const s = mkSaju({ year: ["갑", "인"], month: ["갑", "인"], day: ["갑", "인"], hour: ["갑", "인"] });
  const r = strengthRaw(s);
  assert.ok(r >= 0 && r <= 100);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `strengthRaw is not a function`

- [ ] **Step 3: Write minimal implementation**

Append to `mapping.ts`:

```ts
import { elementRelation } from "@/lib/saju/pairing";

/** 일간을 돕는가(비겁=비화 / 인성=생아). */
function supportsDay(dayElement: FiveElement, other: FiveElement): boolean {
  const r = elementRelation(dayElement, other); // 일간 입장
  return r === "비화" || r === "생아";
}

/** 강/유 raw = 득령(월지 40) + 득지(일지 20) + 득세(나머지 비겁·인성 세력 최대 40). 0~100, 클수록 강. */
export function strengthRaw(saju: SajuResult): number {
  const dayEl = saju.dayElement;
  const p = saju.pillars;
  const monthBranchEl = STEM_ELEMENT[JANGAN_BONGI[p.month.branch]];
  const dayBranchEl = STEM_ELEMENT[JANGAN_BONGI[p.day.branch]];

  const deukRyeong = supportsDay(dayEl, monthBranchEl) ? 40 : 0;
  const deukJi = supportsDay(dayEl, dayBranchEl) ? 20 : 0;

  // 득세: 나머지 글자(활성 글자 중 월지·일지 제외) 가운데 비겁·인성 세력 비율 × 40
  const rest = activeChars(saju).filter(
    (c) => !(c.char === p.month.branch && !c.isStem) && !(c.char === p.day.branch && !c.isStem)
  );
  let support = 0;
  let total = 0;
  for (const c of rest) {
    total += c.weight;
    if (supportsDay(dayEl, elementOf(c))) support += c.weight;
  }
  const deukSe = total === 0 ? 0 : (support / total) * 40;

  return deukRyeong + deukJi + deukSe;
}
```

> 주의: `rest` 필터는 월지/일지를 지지로만 매칭한다(같은 글자가 천간에 있어도 그건 남긴다). 동일 지지가 년/시지에도 있으면 그건 득세에 정상 포함된다.

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 강유 축 raw (신강신약 득령·득지·득세)"
```

---

## Task 5: 재/인 축 raw (십신 클러스터)

**Files:**
- Modify: `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { wealthRaw } from "./mapping.ts";

test("wealthRaw — 재성·관성 우세면 재(+)", () => {
  // 일간 갑목. 무·기(재성 토), 경·신(관성 금) 위주 → 재(+)
  const s = mkSaju({ year: ["무", "술"], month: ["경", "신"], day: ["갑", "진"], hour: ["기", "축"] });
  assert.ok(wealthRaw(s) > 0);
});

test("wealthRaw — 인성·식상 우세면 인(-)", () => {
  // 일간 갑목. 임·계(인성 수), 병·정(식상 화) 위주 → 인(-)
  const s = mkSaju({ year: ["임", "자"], month: ["병", "오"], day: ["갑", "해"], hour: ["정", "사"] });
  assert.ok(wealthRaw(s) < 0);
});

test("wealthRaw — 비겁은 제외(재도 인도 아님)", () => {
  // 일간 갑목. 전부 갑·을·인·묘(비겁 목) → R=0,I=0
  const s = mkSaju({ year: ["갑", "인"], month: ["을", "묘"], day: ["갑", "인"], hour: ["을", "묘"] });
  assert.equal(wealthRaw(s), 0);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `wealthRaw is not a function`

- [ ] **Step 3: Write minimal implementation**

Append to `mapping.ts`:

```ts
import { tenGod } from "@/lib/saju/pairing";

const WEALTH_CAMP = new Set(["편재", "정재", "편관", "정관"]); // 재성 + 관성
const SEAL_CAMP = new Set(["편인", "정인", "식신", "상관"]); // 인성 + 식상
// 비견·겁재(비겁)는 어느 진영도 아님 → 제외

/** 재/인 raw = 재진영 가중합 − 인진영 가중합. + = 재. */
export function wealthRaw(saju: SajuResult): number {
  let wealth = 0;
  let seal = 0;
  for (const cell of activeChars(saju)) {
    const god = tenGod(saju.dayStem, stemOf(cell));
    if (WEALTH_CAMP.has(god)) wealth += cell.weight;
    else if (SEAL_CAMP.has(god)) seal += cell.weight;
  }
  return wealth - seal;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 재인 축 raw (십신 클러스터·비겁 제외)"
```

---

## Task 6: 생/단 축 raw (관계 카운트법)

**Files:**
- Modify: `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { nurtureRaw } from "./mapping.ts";

test("nurtureRaw — 상생 분위기면 생(+)", () => {
  // 목→화→토 흐름(상생 이웃 다수). 갑(목) 오(본기 정=화): 목생화 상생 등
  const s = mkSaju({ year: ["갑", "오"], month: ["병", "진"], day: ["무", "신"], hour: ["경", "자"] });
  assert.ok(nurtureRaw(s) > 0);
});

test("nurtureRaw — 상극 분위기면 단(-)", () => {
  // 목-토-수-화-금 상극 이웃 다수. 갑(목) 진(본기 무=토): 목극토 상극
  const s = mkSaju({ year: ["갑", "진"], month: ["무", "자"], day: ["임", "오"], hour: ["병", "신"] });
  assert.ok(nurtureRaw(s) < 0);
});

test("nurtureRaw — 시간 모름이면 시주 쌍 제외(값 달라짐)", () => {
  const p = { year: ["갑", "오"], month: ["병", "진"], day: ["무", "신"], hour: ["경", "자"] } as const;
  assert.notEqual(nurtureRaw(mkSaju(p)), nurtureRaw(mkSaju(p, { hourKnown: false })));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `nurtureRaw is not a function`

- [ ] **Step 3: Write minimal implementation**

Append to `mapping.ts`:

```ts
import { DAY_STEM_PAIR_WEIGHT } from "./constants.ts";

/** 생/단 raw = 상생쌍 가중합 − 상극쌍 가중합. + = 생. 이웃쌍 = 간-지(각 기둥) + 인접천간 + 인접지지. */
export function nurtureRaw(saju: SajuResult): number {
  const p = saju.pillars;
  const hk = saju.input.hourKnown;

  // 각 글자를 {element(본기), weight} 로. 일간은 pair 전용 가중(1.5).
  type Node = { el: FiveElement; w: number };
  const stem = (ch: string, w: number): Node => ({ el: STEM_ELEMENT[ch], w });
  const branch = (ch: string, w: number): Node => ({ el: STEM_ELEMENT[JANGAN_BONGI[ch]], w });

  const yS = stem(p.year.stem, POSITION_WEIGHT.yearStem);
  const yB = branch(p.year.branch, POSITION_WEIGHT.yearBranch);
  const mS = stem(p.month.stem, POSITION_WEIGHT.monthStem);
  const mB = branch(p.month.branch, POSITION_WEIGHT.monthBranch);
  const dS = stem(p.day.stem, DAY_STEM_PAIR_WEIGHT); // 일간 pair 가중
  const dB = branch(p.day.branch, POSITION_WEIGHT.dayBranch);
  const hS = stem(p.hour.stem, POSITION_WEIGHT.hourStem);
  const hB = branch(p.hour.branch, POSITION_WEIGHT.hourBranch);

  const pairs: [Node, Node][] = [
    [yS, yB], [mS, mB], [dS, dB], // 각 기둥 간-지 (년·월·일)
    [yS, mS], [mS, dS], // 인접 천간 (년-월, 월-일)
    [yB, mB], [mB, dB], // 인접 지지 (년-월, 월-일)
  ];
  if (hk) {
    pairs.push([hS, hB]); // 시주 간-지
    pairs.push([dS, hS]); // 일-시 천간
    pairs.push([dB, hB]); // 일-시 지지
  }

  let generate = 0;
  let restrain = 0;
  for (const [a, b] of pairs) {
    const rel = elementRelation(a.el, b.el); // a 입장
    const w = (a.w + b.w) / 2;
    if (rel === "생아" || rel === "아생") generate += w;
    else if (rel === "극아" || rel === "아극") restrain += w;
    // 비화 = 중립
  }
  return generate - restrain;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 생단 축 raw (오행 상생상극 이웃쌍)"
```

---

## Task 7: 오행 주기질 (분포 + 최다)

**Files:**
- Modify: `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { elementDistribution, dominantElement } from "./mapping.ts";

test("elementDistribution — 일간 포함·본기 기준 가중 합", () => {
  const s = mkSaju({ year: ["갑", "인"], month: ["갑", "인"], day: ["갑", "인"], hour: ["갑", "인"] });
  const dist = elementDistribution(s);
  // 전부 목(갑·인본기 갑) → 목만 양수, 나머지 0
  assert.ok(dist["목"] > 0);
  assert.equal(dist["화"], 0);
});

test("dominantElement — 최다 오행, 동점 시 월지 우선", () => {
  const s = mkSaju({ year: ["갑", "인"], month: ["경", "신"], day: ["무", "오"], hour: ["임", "자"] });
  // 월지 신(금) 가중 3.0 → 금 세력 큼
  assert.equal(dominantElement(s), "금");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `elementDistribution is not a function`

- [ ] **Step 3: Write minimal implementation**

Append to `mapping.ts`:

```ts
import { DAY_STEM_ELEMENT_WEIGHT } from "./constants.ts";

/** 오행 분포(본기 기준·위치가중). 주기질은 일간 포함(가중 1.5). */
export function elementDistribution(saju: SajuResult): Record<FiveElement, number> {
  const dist: Record<FiveElement, number> = { 목: 0, 화: 0, 토: 0, 금: 0, 수: 0 };
  for (const cell of activeChars(saju)) dist[elementOf(cell)] += cell.weight;
  // 일간(activeChars 에서 제외됨) 을 주기질 전용 가중으로 추가
  dist[saju.dayElement] += DAY_STEM_ELEMENT_WEIGHT;
  return dist;
}

const ELEMENT_KEYS: FiveElement[] = ["목", "화", "토", "금", "수"];

/** 최다 오행. 동점이면 월지(본기) 오행 우선, 그래도 동점이면 정순(목화토금수). */
export function dominantElement(saju: SajuResult): FiveElement {
  const dist = elementDistribution(saju);
  const monthEl = STEM_ELEMENT[JANGAN_BONGI[saju.pillars.month.branch]];
  let best = ELEMENT_KEYS[0];
  for (const el of ELEMENT_KEYS) {
    if (dist[el] > dist[best]) best = el;
    else if (dist[el] === dist[best] && el === monthEl && best !== monthEl) best = el;
  }
  return best;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 오행 주기질 분포·최다"
```

---

## Task 8: 백분위 매핑 + 합성 샘플 보정

**Files:**
- Create: `scripts/saju-mbti-calibrate.mjs`
- Modify: `lib/saju-mbti/constants.ts` (QUANTILE_TABLE 추가), `lib/saju-mbti/mapping.ts` (axisPercentile)
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { axisPercentile } from "./mapping.ts";

test("axisPercentile — 분위 배열 선형보간(0~100·단조)", () => {
  const q = Array.from({ length: 21 }, (_, i) => i * 10); // P0..P100 = 0,10,...,200
  assert.equal(axisPercentile(-5, q), 0);   // 최소 이하
  assert.equal(axisPercentile(205, q), 100); // 최대 이상
  assert.equal(axisPercentile(100, q), 50);  // 중앙
  assert.ok(axisPercentile(150, q) > axisPercentile(50, q)); // 단조
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `axisPercentile is not a function`

- [ ] **Step 3: Write minimal implementation (axisPercentile)**

Append to `mapping.ts`:

```ts
/** raw → 백분위(0~100). q: 21개 오름차순 분위점(P0,P5,...,P100). 선형보간. */
export function axisPercentile(raw: number, q: number[]): number {
  if (raw <= q[0]) return 0;
  if (raw >= q[q.length - 1]) return 100;
  for (let i = 1; i < q.length; i++) {
    if (raw <= q[i]) {
      const lo = q[i - 1];
      const hi = q[i];
      const frac = hi === lo ? 0 : (raw - lo) / (hi - lo);
      return (i - 1 + frac) * 5; // 스텝당 5퍼센타일
    }
  }
  return 100;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS (axisPercentile test)

- [ ] **Step 5: Write the calibration script**

Create `scripts/saju-mbti-calibrate.mjs`:

```js
// 합성 샘플(1970-2010 매일 정오) → 축별 raw 분포 → 21분위 테이블 + 분포 점검.
// run: node --import tsx scripts/saju-mbti-calibrate.mjs
import { calcSaju } from "../lib/saju/calc.ts";
import { yinYangRaw, strengthRaw, wealthRaw, nurtureRaw } from "../lib/saju-mbti/mapping.ts";

const raws = { yinYang: [], strength: [], wealth: [], nurture: [] };
for (let y = 1970; y <= 2010; y++) {
  for (let m = 1; m <= 12; m++) {
    const dim = new Date(y, m, 0).getDate();
    for (let d = 1; d <= dim; d++) {
      const s = calcSaju({ year: y, month: m, day: d, hour: 12, minute: 0, gender: "other" });
      raws.yinYang.push(yinYangRaw(s));
      raws.strength.push(strengthRaw(s));
      raws.wealth.push(wealthRaw(s));
      raws.nurture.push(nurtureRaw(s));
    }
  }
}

function quantiles(arr) {
  const a = [...arr].sort((x, z) => x - z);
  const out = [];
  for (let p = 0; p <= 100; p += 5) {
    const idx = Math.min(a.length - 1, Math.round((p / 100) * (a.length - 1)));
    out.push(Number(a[idx].toFixed(4)));
  }
  return out;
}

const table = {};
for (const k of Object.keys(raws)) table[k] = quantiles(raws[k]);
console.log("샘플 수:", raws.yinYang.length);
console.log("QUANTILE_TABLE =", JSON.stringify(table, null, 2));

// 분포 점검: 각 축 중앙(P50) 기준 앞/뒤 극 비율
for (const k of Object.keys(raws)) {
  const med = table[k][10]; // P50
  const front = raws[k].filter((v) => v >= med).length;
  console.log(`${k}: 앞극 ${(100 * front / raws[k].length).toFixed(1)}%`);
}
```

- [ ] **Step 6: Run calibration and capture the table**

Run: `node --import tsx scripts/saju-mbti-calibrate.mjs`
Expected: prints `샘플 수: ~14975`, a `QUANTILE_TABLE = {...}` object, and 앞극 ≈ 50% per axis.
**Copy the printed `QUANTILE_TABLE` object literal** for the next step.

- [ ] **Step 7: Add QUANTILE_TABLE to constants.ts**

Append to `lib/saju-mbti/constants.ts` (replace the `{...}` with the **exact object printed** in Step 6):

```ts
// 합성 샘플 보정 산출물 (scripts/saju-mbti-calibrate.mjs, 1970-2010 매일 정오 ~15k건).
// 각 축: raw 오름차순 21분위점 [P0, P5, ..., P100]. 재생성 시 스크립트 재실행 후 교체.
export const QUANTILE_TABLE: Record<AxisKey, number[]> = {
  yinYang: [/* Step 6 출력 붙여넣기 */],
  strength: [/* Step 6 출력 붙여넣기 */],
  wealth: [/* Step 6 출력 붙여넣기 */],
  nurture: [/* Step 6 출력 붙여넣기 */],
};
```

- [ ] **Step 8: Add a table-shape test**

Append to `mapping.test.ts`:

```ts
import { QUANTILE_TABLE } from "./constants.ts";

test("QUANTILE_TABLE — 4축·21점·오름차순", () => {
  for (const k of ["yinYang", "strength", "wealth", "nurture"]) {
    const q = QUANTILE_TABLE[k];
    assert.equal(q.length, 21, `${k} 분위점 21개 아님`);
    for (let i = 1; i < q.length; i++) assert.ok(q[i] >= q[i - 1], `${k} 비단조`);
  }
});
```

- [ ] **Step 9: Run tests**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS (including QUANTILE_TABLE shape)

- [ ] **Step 10: Commit**

```bash
git add scripts/saju-mbti-calibrate.mjs lib/saju-mbti/constants.ts lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): 합성 샘플 보정 + 백분위 매핑"
```

---

## Task 9: paljaType 통합 + 16코드

**Files:**
- Modify: `lib/saju-mbti/mapping.ts`
- Test: `lib/saju-mbti/mapping.test.ts`

- [ ] **Step 1: Write the failing test**

Append to `mapping.test.ts`:

```ts
import { paljaType } from "./mapping.ts";

test("paljaType — 코드 4글자·축 순서(음양·강유·재인·생단)", () => {
  const s = mkSaju({ year: ["임", "신"], month: ["기", "유"], day: ["신", "묘"], hour: ["을", "미"] });
  const t = paljaType(s);
  assert.equal(t.code.length, 4);
  assert.ok(["양", "음"].includes(t.axes.yinYang.pole));
  assert.ok(["강", "유"].includes(t.axes.strength.pole));
  assert.ok(["재", "인"].includes(t.axes.wealth.pole));
  assert.ok(["생", "단"].includes(t.axes.nurture.pole));
  assert.equal(
    t.code,
    t.axes.yinYang.pole + t.axes.strength.pole + t.axes.wealth.pole + t.axes.nurture.pole
  );
});

test("paljaType — pct 0~100·pole 은 pct≥50 기준", () => {
  const s = mkSaju({ year: ["임", "신"], month: ["기", "유"], day: ["신", "묘"], hour: ["을", "미"] });
  const t = paljaType(s);
  for (const key of ["yinYang", "strength", "wealth", "nurture"]) {
    const a = t.axes[key];
    assert.ok(a.pct >= 0 && a.pct <= 100);
  }
  assert.equal(t.axes.yinYang.pole === "양", t.axes.yinYang.pct >= 50);
});

test("paljaType — element·elementDist·raw 양념 포함", () => {
  const s = mkSaju({ year: ["임", "신"], month: ["기", "유"], day: ["신", "묘"], hour: ["을", "미"] });
  const t = paljaType(s);
  assert.ok(["목", "화", "토", "금", "수"].includes(t.element));
  assert.equal(Object.values(t.elementDist).reduce((a, b) => a + b, 0) > 0, true);
  assert.ok(Array.isArray(t.tenGods));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: FAIL — `paljaType is not a function`

- [ ] **Step 3: Write minimal implementation**

Append to `mapping.ts`:

```ts
import type { AxisKey, Pole } from "./constants.ts";
import { QUANTILE_TABLE } from "./constants.ts";

export interface AxisResult {
  raw: number;
  pct: number; // 0~100 (앞극 방향 백분위)
  pole: Pole;
}

export interface PaljaType {
  axes: {
    yinYang: AxisResult;
    strength: AxisResult;
    wealth: AxisResult;
    nurture: AxisResult;
  };
  code: string; // 예: "음유인생"
  element: FiveElement; // 오행 주기질
  elementDist: Record<FiveElement, number>;
  tenGods: string[]; // 활성 글자별 십신 raw
  jangan: string[]; // 지지 본기 raw
}

const POLES: Record<AxisKey, [Pole, Pole]> = {
  yinYang: ["양", "음"],
  strength: ["강", "유"],
  wealth: ["재", "인"],
  nurture: ["생", "단"],
};

function axisResult(key: AxisKey, raw: number): AxisResult {
  const pct = axisPercentile(raw, QUANTILE_TABLE[key]);
  const [front, back] = POLES[key];
  return { raw, pct, pole: pct >= 50 ? front : back };
}

export function paljaType(saju: SajuResult): PaljaType {
  const axes = {
    yinYang: axisResult("yinYang", yinYangRaw(saju)),
    strength: axisResult("strength", strengthRaw(saju)),
    wealth: axisResult("wealth", wealthRaw(saju)),
    nurture: axisResult("nurture", nurtureRaw(saju)),
  };
  const code = axes.yinYang.pole + axes.strength.pole + axes.wealth.pole + axes.nurture.pole;
  return {
    axes,
    code,
    element: dominantElement(saju),
    elementDist: elementDistribution(saju),
    tenGods: activeChars(saju).map((c) => tenGod(saju.dayStem, stemOf(c))),
    jangan: activeChars(saju).filter((c) => !c.isStem).map((c) => JANGAN_BONGI[c.char]),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add lib/saju-mbti/mapping.ts lib/saju-mbti/mapping.test.ts
git commit -m "feat(saju-mbti): paljaType 통합 + 16 한글코드"
```

---

## Task 10: 분포 검증 (쏠림 점검)

**Files:**
- Modify: `scripts/saju-mbti-calibrate.mjs` (16유형 분포 출력 추가)

- [ ] **Step 1: Extend the calibration script with type distribution**

Append to `scripts/saju-mbti-calibrate.mjs` (분위 테이블이 constants.ts 에 반영된 뒤 실행):

```js
import { paljaType } from "../lib/saju-mbti/mapping.ts";

const typeCount = {};
for (let y = 1970; y <= 2010; y++) {
  for (let m = 1; m <= 12; m++) {
    const dim = new Date(y, m, 0).getDate();
    for (let d = 1; d <= dim; d++) {
      const s = calcSaju({ year: y, month: m, day: d, hour: 12, minute: 0, gender: "other" });
      const code = paljaType(s).code;
      typeCount[code] = (typeCount[code] || 0) + 1;
    }
  }
}
const total = Object.values(typeCount).reduce((a, b) => a + b, 0);
const sorted = Object.entries(typeCount).sort((a, b) => b[1] - a[1]);
console.log("\n16유형 분포:");
for (const [code, n] of sorted) console.log(`  ${code}: ${(100 * n / total).toFixed(1)}%`);
console.log("유형 수:", sorted.length, "/ 최대:", (100 * sorted[0][1] / total).toFixed(1) + "%");
```

- [ ] **Step 2: Run and inspect distribution**

Run: `node --import tsx scripts/saju-mbti-calibrate.mjs`
Expected: 16 types appear; 최대 유형 비율이 과도하지 않음(목표 <15%). 극단 쏠림(>15% 또는 <2%) 유형이 있으면 §Self-Review 판단 기록에 남기고 재료·가중치 재검토(별도 이슈).

- [ ] **Step 3: Commit**

```bash
git add scripts/saju-mbti-calibrate.mjs
git commit -m "feat(saju-mbti): 보정 스크립트에 16유형 분포 점검 추가"
```

- [ ] **Step 4: Full test + typecheck gate**

Run: `node --import tsx --test lib/saju-mbti/mapping.test.ts && npx tsc --noEmit`
Expected: 전체 PASS + tsc exit 0.

---

## Self-Review

**1. Spec coverage:**
- §2 팔자추출·위치가중·본기환원·시간모름 → Task 1(상수)·Task 2(activeChars 시간모름). ✓
- §3.1 음양(7:3) → Task 3. ✓ / §3.2 강유 → Task 4. ✓ / §3.3 재인(비겁제외) → Task 5. ✓ / §3.4 생단(관계카운트) → Task 6. ✓
- §3.5 오행주기질 → Task 7. ✓ (일간 포함 가중 1.5 = 스펙의 "8글자" 해석 명시)
- §4 하이브리드 보정(합성샘플·분위·백분위) → Task 8. ✓
- §5 출력형태(축별 %·극·16코드·주기질·raw양념) → Task 9. ✓
- §7 검증(분포 쏠림) → Task 10. ✓
- §5 일치율 = A 범위 밖(팔자 % 제공까지만) — Task 9 가 pct 제공, 비교는 후속. ✓ (범위 일치)

**2. Placeholder scan:** QUANTILE_TABLE 값은 Task 8 Step 6-7 에서 스크립트 출력으로 채움(생성 절차·코드 완비, 위약 아님). 그 외 위약 없음.

**3. Type consistency:** `CharCell`·`AxisKey`·`Pole`·`PaljaType`·`AxisResult` 정의와 사용 일치. `stemOf`/`elementOf`/`activeChars` 시그니처 Task 2 정의 그대로 이후 재사용. `elementRelation(dayEl, other)` 인자 순서(일간 입장) 전 축 동일. `tenGod(dayStem, otherStem)` 동일. ✓

## Execution Handoff

(플랜 저장 후 실행 방식 선택 안내)
