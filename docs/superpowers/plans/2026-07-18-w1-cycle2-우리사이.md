# W1 사이클 2 — "우리 사이" v1 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 지속 대화형 연애 상담 종목 "우리 사이" v1 — 상대 등록 + 영원한 스레드(파일/임계치 요약 기억) + 기간권 패스(소프트캡·연장) + 확장형 스킬 4종 + 인앱 체크인.

**Architecture:** 하이브리드 — 신규 `relationships`/`relationship_passes` 테이블(관계 파일·패스) + 스레드 본체는 `readings`(`consultation_type='relationship'`) + `messages` 재사용. 채팅은 기존 `streamChat`/SSE/민감감지 배관을 재사용하되 수렴(`[END]`) 대신 일일 소프트캡+패스 게이트. 스킬은 단일 진실 원천 레지스트리(`RELATIONSHIP_SKILLS`)로 라우트·UI·가격이 자동 추종.

**Tech Stack:** Next.js 16 App Router, TypeScript strict, Tailwind v4, Supabase(PostgreSQL RPC), Claude(`claude-sonnet-5` 본문 / haiku 요약). 검증: `node --import tsx --test`(순수 로직) + `npm run build`(타입체크) + dev 브라우저 E2E. QA 하네스·prod 일괄 배포는 사이클 3.

**Spec:** `docs/superpowers/specs/2026-07-18-w1-cycle2-우리사이-design.md`

⚠️ **배포 정책: 이 플랜의 모든 커밋은 dev 전용. prod(main) 머지는 사이클 1~3 전체 QA 후 1회 일괄 — 이 플랜 범위 밖.**

---

## 사전 지식 (실행자 필독)

- **reading 생성 패턴** ([app/api/consultations/tarot/route.ts](../../../app/api/consultations/tarot/route.ts)): 잔액 사전확인 → readings INSERT → `spendStars(userId, cost, {readingId, source})` → 실패 시 readings DELETE 롤백. 서버가 가격을 config에서 읽음(위조 차단).
- **별 차감** ([lib/stars.ts](../../../lib/stars.ts)): `spendStars` / `chargeStars`는 RPC 래퍼. RPC는 `SELECT FOR UPDATE`로 직렬화. 새 RPC도 이 패턴.
- **KST 경계** ([lib/admin-time.ts](../../../lib/admin-time.ts)): `startOfTodayKstIso()` = KST 자정을 UTC ISO로. 일일 턴/연장 카운트에 재사용.
- **페르소나 합성** ([lib/claude.ts:36-59](../../../lib/claude.ts)): `getCore()`(byeolkong_core.md) + 도메인 파일. `build*SystemMessage`는 `{staticPart(캐시), dynamicPart}` 반환. `streamChat(system, messages, maxTokens)` 재사용.
- **보관함 리스트** ([app/api/readings/route.ts:63-151](../../../app/api/readings/route.ts) GET): 본인 readings 50개. **스레드 본체는 여기서 제외해야 함**(consultation_type='relationship').
- **채팅 SSE** ([app/api/consultations/tarot/chat/route.ts](../../../app/api/consultations/tarot/chat/route.ts)): messages에서 서버측 턴/글자 카운팅, rate limit, 민감 감지, INSERT. 관계 스레드 라우트가 이 골격을 복제하되 wrap 제거.
- **draw sessionStorage 계약**: `/tarot`(스프레드) → `/tarot/draw`(카드) → `/tarot/reading`. 스킬 tarot_draw는 이 흐름 재사용.
- **테스트**: `node --import tsx --test <file>.test.ts` (`node:test` + `node:assert/strict`, import는 `.ts` 확장자). 예: [lib/continuation.test.ts](../../../lib/continuation.test.ts).
- **페르소나/데이터 파일 수정 후 dev 서버 재시작 필수** (모듈 캐시).

---

# Stage 2a — 기반 (마이그레이션 · 등록 · 탭)

### Task 1: 마이그레이션 — relationships + passes + readings 확장

**Files:**
- Create: `supabase/migrations/20260718000000_relationship_core.sql`

- [ ] **Step 1: 마이그레이션 SQL 작성**

```sql
-- 20260718000000_relationship_core.sql — W1 사이클 2 "우리 사이" 코어
-- relationships(관계 파일, 유저당 1개) + relationship_passes(기간권) + readings 확장(스레드/스킬 귀속)

-- 1) 관계 파일
CREATE TABLE IF NOT EXISTS relationships (
  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id             UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  label               VARCHAR(50) NOT NULL,                     -- 호칭
  status              VARCHAR(20) NOT NULL
                        CHECK (status IN ('crush','dating','breakup','onesided')), -- 썸/연애중/이별/짝사랑
  self_profile_id     UUID REFERENCES user_profiles(id) ON DELETE SET NULL,
  partner_profile_id  UUID REFERENCES user_profiles(id) ON DELETE SET NULL,
  thread_reading_id   UUID REFERENCES readings(id) ON DELETE SET NULL,
  rolling_summary     TEXT,
  summarized_msg_count INT NOT NULL DEFAULT 0,                  -- rolling_summary가 커버한 older 메시지 수
  memo                JSONB NOT NULL DEFAULT '{}'::jsonb,       -- prescriptions/pending_checkin/skill_log
  last_visited_at     TIMESTAMPTZ,
  created_at          TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_relationships_user_one ON relationships(user_id); -- v1 단일
ALTER TABLE relationships ENABLE ROW LEVEL SECURITY;

-- 2) 기간권 패스
CREATE TABLE IF NOT EXISTS relationship_passes (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  relationship_id UUID NOT NULL REFERENCES relationships(id) ON DELETE CASCADE,
  kind            VARCHAR(10) NOT NULL CHECK (kind IN ('day1','day3','day7')),
  stars_spent     INT NOT NULL,
  started_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
  expires_at      TIMESTAMPTZ NOT NULL,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_passes_active
  ON relationship_passes(relationship_id, expires_at DESC);
ALTER TABLE relationship_passes ENABLE ROW LEVEL SECURITY;

-- 3) readings 확장 — 스레드 본체 + 스킬 자식 귀속
ALTER TABLE readings
  DROP CONSTRAINT IF EXISTS readings_consultation_type_check;
ALTER TABLE readings
  ADD CONSTRAINT readings_consultation_type_check
  CHECK (consultation_type IN ('saju','tarot','relationship'));
ALTER TABLE readings
  ADD COLUMN IF NOT EXISTS relationship_id UUID REFERENCES relationships(id) ON DELETE CASCADE;
ALTER TABLE readings
  ADD COLUMN IF NOT EXISTS skill_key VARCHAR(20);  -- 스킬 reading 식별(스레드=NULL). 확장 훅
CREATE INDEX IF NOT EXISTS idx_readings_relationship
  ON readings(relationship_id, created_at) WHERE relationship_id IS NOT NULL;
```

주의: `readings_consultation_type_check` 제약명이 다르면(20260607 마이그레이션에서 `ADD COLUMN ... CHECK`로 익명 생성됐을 수 있음) `DROP CONSTRAINT IF EXISTS`가 무해히 통과하고 새 제약이 추가됨. 실제 제약명은 dev 적용 후 Supabase에서 `\d readings`로 확인 — 익명명이 남아 충돌하면 그 이름으로 DROP 한 줄 추가.

- [ ] **Step 2: dev push 전 로컬 SQL 문법 확인**

Run: `node --import tsx -e "import{readFileSync}from'fs';const s=readFileSync('supabase/migrations/20260718000000_relationship_core.sql','utf8');if(!/CREATE TABLE IF NOT EXISTS relationships/.test(s)||!/relationship_passes/.test(s)||!/relationship_id UUID REFERENCES relationships/.test(s))throw new Error('migration incomplete');console.log('migration file OK')"`
Expected: `migration file OK`

- [ ] **Step 3: 커밋** (dev push는 Task 5 함께 — 스키마만 먼저 push해도 무해하나 탭/등록과 묶어 검증)

```bash
git add supabase/migrations/20260718000000_relationship_core.sql
git commit -m "feat(w1-c2): 우리 사이 마이그레이션 — relationships/passes/readings 확장"
```

---

### Task 2: 공용 타입·상수 (`lib/relationship/types.ts`)

**Files:**
- Create: `lib/relationship/types.ts`
- Test: `lib/relationship/types.test.ts`

- [ ] **Step 1: 타입·상수 작성**

```typescript
// lib/relationship/types.ts — "우리 사이" 공용 타입/상수 (단일 진실 원천)

export type RelationshipStatus = "crush" | "dating" | "breakup" | "onesided";

export const RELATIONSHIP_STATUS_LABELS: Record<RelationshipStatus, string> = {
  crush: "썸 타는 중",
  dating: "연애 중",
  breakup: "헤어진 사이",
  onesided: "짝사랑",
};

export type PassKind = "day1" | "day3" | "day7";

export interface PassPlan {
  kind: PassKind;
  days: number;
  cost: number;
  label: string;
  recommended?: boolean;
}

/** 패스 상품 — 서버 권위(클라가 보낸 cost 신뢰 X). 스펙 §6: 1일20/3일40/7일60. */
export const PASS_PLANS: PassPlan[] = [
  { kind: "day1", days: 1, cost: 20, label: "1일권" },
  { kind: "day3", days: 3, cost: 40, label: "3일권" },
  { kind: "day7", days: 7, cost: 60, label: "7일권", recommended: true },
];

export const PASS_PLAN_BY_KIND: Record<PassKind, PassPlan> = Object.fromEntries(
  PASS_PLANS.map((p) => [p.kind, p])
) as Record<PassKind, PassPlan>;

/** 일일 자유대화 소프트캡 + 연장(결정: 5별 → +5턴, 반복 가능) */
export const DAILY_TURN_CAP = 20;
export const EXTEND_COST = 5;
export const EXTEND_TURNS = 5;

/** 오늘 연장 횟수 → 허용 턴 수 */
export function dailyTurnAllowance(todayExtendCount: number): number {
  return DAILY_TURN_CAP + EXTEND_TURNS * Math.max(0, todayExtendCount);
}

export interface RelationshipMemo {
  prescriptions?: { text: string; created_at: string; resolved_at?: string }[];
  pending_checkin?: { text: string; created_at: string } | null;
  skill_log?: { skill: string; reading_id: string; summary: string; created_at: string }[];
}
```

- [ ] **Step 2: 실패 테스트 작성**

```typescript
// lib/relationship/types.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  PASS_PLAN_BY_KIND, PASS_PLANS, dailyTurnAllowance, DAILY_TURN_CAP,
} from "./types.ts";

test("패스 상품 = 1일20/3일40/7일60, 7일권 추천", () => {
  assert.equal(PASS_PLAN_BY_KIND.day1.cost, 20);
  assert.equal(PASS_PLAN_BY_KIND.day3.cost, 40);
  assert.equal(PASS_PLAN_BY_KIND.day7.cost, 60);
  assert.equal(PASS_PLAN_BY_KIND.day7.days, 7);
  assert.equal(PASS_PLANS.filter((p) => p.recommended).length, 1);
});

test("일일 허용 = 20 + 5*연장횟수", () => {
  assert.equal(dailyTurnAllowance(0), DAILY_TURN_CAP);
  assert.equal(dailyTurnAllowance(1), 25);
  assert.equal(dailyTurnAllowance(3), 35);
  assert.equal(dailyTurnAllowance(-1), 20); // 방어
});
```

- [ ] **Step 3: 테스트 실행 → 통과**

Run: `node --import tsx --test lib/relationship/types.test.ts`
Expected: 2 tests pass.

- [ ] **Step 4: 커밋**

```bash
git add lib/relationship/types.ts lib/relationship/types.test.ts
git commit -m "feat(w1-c2): 우리 사이 공용 타입·패스 상품·소프트캡 상수"
```

---

### Task 3: 관계 조회/등록 API (`/api/relationship`)

**Files:**
- Create: `app/api/relationship/route.ts`
- 참조: [app/api/readings/route.ts](../../../app/api/readings/route.ts) (profile INSERT + validateProfile 패턴)

- [ ] **Step 1: GET(내 관계+패스상태) + POST(등록) 작성**

```typescript
// app/api/relationship/route.ts — 내 관계 조회 + 상대 등록
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { logError } from "@/lib/logger";
import { validateProfile } from "@/lib/saju/profile-input";
import { getActivePass, getTodayThreadTurns, getTodayExtendCount } from "@/lib/relationship/passes";
import { dailyTurnAllowance, type RelationshipStatus } from "@/lib/relationship/types";

export const dynamic = "force-dynamic";
const VALID_STATUS: RelationshipStatus[] = ["crush", "dating", "breakup", "onesided"];

export async function GET() {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ relationship: null }, { status: 200 });

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase
    .from("relationships")
    .select("id, label, status, self_profile_id, partner_profile_id, thread_reading_id, memo, last_visited_at")
    .eq("user_id", userId)
    .maybeSingle();

  if (!rel) return NextResponse.json({ relationship: null });

  const pass = await getActivePass(rel.id);
  const todayTurns = pass ? await getTodayThreadTurns(rel.thread_reading_id) : 0;
  const todayExtend = pass ? await getTodayExtendCount(userId) : 0;

  return NextResponse.json({
    relationship: {
      id: rel.id, label: rel.label, status: rel.status,
      selfProfileId: rel.self_profile_id, partnerProfileId: rel.partner_profile_id,
      threadReadingId: rel.thread_reading_id, memo: rel.memo,
    },
    pass: pass ? { kind: pass.kind, expiresAt: pass.expires_at } : null,
    daily: pass
      ? { used: todayTurns, allowance: dailyTurnAllowance(todayExtend), extendCount: todayExtend }
      : null,
  });
}

interface RegisterBody {
  label: string;
  status: RelationshipStatus;
  selfProfileId?: string;        // 기존 primary 재사용
  partnerProfile?: unknown;       // inline 등록 (relation_type='partner')
}

export async function POST(request: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required", code: "LOGIN_REQUIRED" }, { status: 401 });

  let body: RegisterBody;
  try { body = (await request.json()) as RegisterBody; }
  catch { return NextResponse.json({ error: "invalid_json" }, { status: 400 }); }

  if (typeof body.label !== "string" || body.label.trim().length < 1 || body.label.length > 50)
    return NextResponse.json({ error: "invalid_label" }, { status: 400 });
  if (!VALID_STATUS.includes(body.status))
    return NextResponse.json({ error: "invalid_status" }, { status: 400 });

  const supabase = getServiceSupabase();

  // v1 단일 관계 — 이미 있으면 그대로 반환(멱등)
  const { data: existing } = await supabase
    .from("relationships").select("id").eq("user_id", userId).maybeSingle();
  if (existing) return NextResponse.json({ id: existing.id, existed: true });

  // self 프로필: 전달됐으면 소유권 확인, 없으면 null(나중에 등록 가능)
  let selfProfileId: string | null = null;
  if (typeof body.selfProfileId === "string" && body.selfProfileId) {
    const { data: owned } = await supabase.from("user_profiles")
      .select("id").eq("id", body.selfProfileId).eq("user_id", userId).maybeSingle();
    if (owned) selfProfileId = owned.id;
  }

  // partner 프로필: inline이면 검증 후 생성(relation_type='partner')
  let partnerProfileId: string | null = null;
  if (body.partnerProfile) {
    const v = validateProfile(body.partnerProfile);
    if ("error" in v) return NextResponse.json({ error: v.error }, { status: 400 });
    const { data: pRow, error: pErr } = await supabase.from("user_profiles").insert({
      user_id: userId, display_name: v.displayName, relation_type: "partner",
      birth_date: v.birthDate, birth_time: v.birthTime, is_lunar_input: v.isLunarInput,
      is_leap_month: v.isLeapMonth, gender: v.gender, is_primary: false,
    }).select("id").single();
    if (pErr || !pRow) return NextResponse.json({ error: "partner_profile_failed" }, { status: 500 });
    partnerProfileId = pRow.id;
  }

  // 관계 + 스레드 본체 reading 생성 (스레드는 무료 — 패스가 대화를 게이트)
  const { data: rel, error: rErr } = await supabase.from("relationships").insert({
    user_id: userId, label: body.label.trim(), status: body.status,
    self_profile_id: selfProfileId, partner_profile_id: partnerProfileId,
  }).select("id").single();
  if (rErr || !rel) {
    await logError(rErr ?? new Error("relationship insert null"), { route: "/api/relationship", userId });
    return NextResponse.json({ error: "relationship_failed" }, { status: 500 });
  }

  const { data: thread, error: tErr } = await supabase.from("readings").insert({
    user_id: userId, consultation_type: "relationship", relationship_id: rel.id,
    profile_id: null, saju_data: null, stars_spent: 0, has_sensitive: false,
  }).select("id").single();
  if (tErr || !thread) {
    await supabase.from("relationships").delete().eq("id", rel.id);
    return NextResponse.json({ error: "thread_failed" }, { status: 500 });
  }
  await supabase.from("relationships").update({ thread_reading_id: thread.id }).eq("id", rel.id);

  return NextResponse.json({ id: rel.id, threadReadingId: thread.id, success: true });
}
```

주의: `getActivePass`/`getTodayThreadTurns`/`getTodayExtendCount`는 Task 6에서 만든다. **이 태스크에서 함께 커밋하지 말고**, Task 6 완료 후 빌드가 통과할 때 함께 커밋한다. (또는 Task 6을 먼저 진행) — 실행자는 Task 6을 Task 3보다 먼저 해도 무방.

- [ ] **Step 2: 빌드**

Run: `npm run build`
Expected: `lib/relationship/passes`의 3함수가 없으면 타입 에러 — Task 6 후 통과. 통과 시 다음.

- [ ] **Step 3: 커밋** (Task 6과 묶어서)

```bash
git add app/api/relationship/route.ts
git commit -m "feat(w1-c2): 관계 조회/등록 API"
```

---

### Task 4: 우리 사이 탭 교체 + 보관함 내정보 이동

**Files:**
- Modify: `components/layout/BottomTab.tsx:46-56` (history 탭 정의)
- Modify: `app/api/readings/route.ts:70-77` (GET 리스트에서 스레드 제외)
- Modify: `app/mypage/page.tsx` (보관함 링크 추가)

- [ ] **Step 1: BottomTab의 history 탭 → relationship**

[BottomTab.tsx](../../../components/layout/BottomTab.tsx)의 `key: "history"` 탭 객체를 아래로 교체 (아이콘은 mdi heart-outline / heart로 교체):

```typescript
  {
    key: "history",
    label: "우리 사이",
    href: "/relationship",
    matchPrefixes: ["/relationship"],
    // mdi heart-outline / heart
    iconLine:
      "M12.1,18.55L12,18.65L11.89,18.55C7.14,14.24 4,11.39 4,8.5C4,6.5 5.5,5 7.5,5C9.04,5 10.54,6 11.07,7.36H12.93C13.46,6 14.96,5 16.5,5C18.5,5 20,6.5 20,8.5C20,11.39 16.86,14.24 12.1,18.55M16.5,3C14.76,3 13.09,3.81 12,5.08C10.91,3.81 9.24,3 7.5,3C4.42,3 2,5.41 2,8.5C2,12.27 5.4,15.36 10.55,20.03L12,21.35L13.45,20.03C18.6,15.36 22,12.27 22,8.5C22,5.41 19.58,3 16.5,3Z",
    iconFill:
      "M12,21.35L10.55,20.03C5.4,15.36 2,12.27 2,8.5C2,5.41 4.42,3 7.5,3C9.24,3 10.91,3.81 12,5.08C13.09,3.81 14.76,3 16.5,3C19.58,3 22,5.41 22,8.5C22,12.27 18.6,15.36 13.45,20.03L12,21.35Z",
  },
```

`from=history` 강제 active 로직([BottomTab.tsx:92,111](../../../components/layout/BottomTab.tsx))은 보관함이 mypage로 이동하므로 의미가 바뀐다 — 지금은 그대로 두되(무해), 보관함 다시보기 진입은 Task 후속에서 mypage 경유로 자연 해소. (동작 회귀 없음: `from=history`가 없으면 기존대로 pathname 기반.)

- [ ] **Step 2: 보관함 GET에서 스레드 제외**

[app/api/readings/route.ts](../../../app/api/readings/route.ts) GET의 쿼리에 필터 추가:

```typescript
  const { data, error } = await supabase
    .from("readings")
    .select(
      "id, question, saju_data, consultation_type, spread_type, saju_product, drawn_cards, emotion_tag, stars_spent, has_sensitive, created_at, profile:user_profiles(display_name, relation_type)"
    )
    .eq("user_id", userId)
    .neq("consultation_type", "relationship")   // ← 스레드 본체·verdict 스킬 제외 (우리 사이 탭에서 노출)
    .order("created_at", { ascending: false })
    .limit(50);
```

- [ ] **Step 3: mypage에 보관함 진입점 추가**

`app/mypage/page.tsx`를 읽고, 기존 메뉴/링크 그룹에 "내 고민톡 보관함" 항목을 추가한다 (기존 링크 컴포넌트/스타일 재사용). href="/readings". 문구: "내 고민톡 보관함 — 지난 상담 다시 보기".

- [ ] **Step 4: 빌드 + dev 확인**

Run: `npm run build`
Expected: 통과. `/relationship` 페이지는 아직 없으니 탭 클릭 시 404 — Task 5에서 생성.

- [ ] **Step 5: 커밋**

```bash
git add components/layout/BottomTab.tsx app/api/readings/route.ts app/mypage/page.tsx
git commit -m "feat(w1-c2): 우리 사이 탭 교체 + 보관함 내정보 이동 + 리스트에서 스레드 제외"
```

---

### Task 5: `/relationship` 페이지 — S1 콜드스타트 + 등록 온보딩

**Files:**
- Create: `app/relationship/page.tsx` (클라이언트 컴포넌트, 상태 분기 셸)
- Create: `components/relationship/RegisterOnboarding.tsx`
- 참조: [components/saju/ProfileForm.tsx](../../../components/saju/ProfileForm.tsx) (생년월일시 입력 재사용)

- [ ] **Step 1: 페이지 셸 (상태 분기)**

`app/relationship/page.tsx`: 마운트 시 `GET /api/relationship` → 분기.
- 미로그인(401/relationship null + 세션없음): 로그인 유도 or `/login?next=/relationship`.
- `relationship === null`: **S1 콜드스타트** — "우리 사이" 소개(지속 대화형 연애 상담 + 스킬 미리보기 + 패스 가격 `PASS_PLANS`) + "상대 등록하고 시작" 버튼 → `RegisterOnboarding` 노출.
- `relationship !== null`: (S2~S4) — 이 태스크에선 **"준비 중" placeholder** + 관계 헤더(label/status)만. 실제 스레드/패스 UI는 Stage 2b Task 11.

기존 홈/페이지의 히어로·카드 스타일(cream/lilac 토큰, rounded-2xl) 재사용. Header/BottomTab은 AppShell이 자동 부착하므로 `pb-20` 여백만 신경.

- [ ] **Step 2: 등록 온보딩 컴포넌트**

`components/relationship/RegisterOnboarding.tsx`: 3스텝 (호칭 입력 → 관계 상태 4택(`RELATIONSHIP_STATUS_LABELS`) → 양쪽 생년월일시). 내 생일 = 기존 primary 프로필 있으면 재사용 체크박스, 없으면 입력. 상대 생일 = ProfileForm 재사용(relation_type는 서버가 partner 고정). 생일은 "궁합 볼 때 필요 — 나중에 추가 가능"으로 **건너뛰기 허용**. 제출 → `POST /api/relationship` → 성공 시 `router.refresh()` 또는 상태 갱신.

- [ ] **Step 3: dev 확인**

Run: dev 서버 → `/relationship` (로그인 상태) → 콜드스타트 → 등록 → relationships/스레드 생성 확인(Supabase에서 row + thread_reading_id 세팅).
Expected: 등록 후 페이지가 "준비 중"(등록됨 상태)로 전환.

- [ ] **Step 4: 커밋 + dev push (Stage 2a 마감)**

```bash
git add app/relationship components/relationship
git commit -m "feat(w1-c2): 우리 사이 콜드스타트 + 상대 등록 온보딩"
git push origin dev
```

Expected: dev에서 마이그레이션 적용(Supabase Workflow logs SUCCESS) + 탭/등록 동작. **main 머지 금지.**

---

# Stage 2b — 지속 스레드 + 패스

### Task 6: 패스 헬퍼 + 카운팅 (`lib/relationship/passes.ts`)

**Files:**
- Create: `lib/relationship/passes.ts`
- Test: `lib/relationship/passes.test.ts`
- Create: `supabase/migrations/20260718010000_relationship_pass_rpc.sql`

- [ ] **Step 1: 패스 구매 RPC**

```sql
-- 20260718010000_relationship_pass_rpc.sql — 패스 구매(원자 차감 + 만료 이어붙임)
CREATE OR REPLACE FUNCTION purchase_relationship_pass(
  p_user_id UUID, p_relationship_id UUID, p_kind TEXT, p_cost INT, p_days INT
) RETURNS JSON LANGUAGE plpgsql SECURITY DEFINER SET search_path = public AS $$
DECLARE
  v_balance INT; v_new_balance INT; v_current_expiry TIMESTAMPTZ; v_new_expiry TIMESTAMPTZ; v_pass_id UUID;
BEGIN
  IF p_cost IS NULL OR p_cost <= 0 OR p_days IS NULL OR p_days <= 0 THEN
    RETURN json_build_object('success', false, 'reason', 'invalid');
  END IF;

  SELECT balance INTO v_balance FROM star_balances WHERE user_id = p_user_id FOR UPDATE;
  IF NOT FOUND THEN
    INSERT INTO star_balances (user_id, balance, total_earned, total_spent) VALUES (p_user_id, 0, 0, 0);
    v_balance := 0;
  END IF;
  IF v_balance < p_cost THEN
    RETURN json_build_object('success', false, 'reason', 'insufficient', 'balance_after', v_balance);
  END IF;

  -- 활성 패스 있으면 그 만료에 이어붙임(결정: 시간 손실 방지)
  SELECT MAX(expires_at) INTO v_current_expiry
    FROM relationship_passes WHERE relationship_id = p_relationship_id AND expires_at > now();
  v_new_expiry := COALESCE(GREATEST(v_current_expiry, now()), now()) + (p_days || ' days')::interval;

  v_new_balance := v_balance - p_cost;
  UPDATE star_balances SET balance = v_new_balance, total_spent = total_spent + p_cost, updated_at = now()
    WHERE user_id = p_user_id;
  INSERT INTO star_transactions (user_id, type, amount, balance_after, source)
    VALUES (p_user_id, 'spend', p_cost, v_new_balance, 'relationship_pass');
  INSERT INTO relationship_passes (user_id, relationship_id, kind, stars_spent, expires_at)
    VALUES (p_user_id, p_relationship_id, p_kind, p_cost, v_new_expiry) RETURNING id INTO v_pass_id;

  RETURN json_build_object('success', true, 'balance_after', v_new_balance,
    'pass_id', v_pass_id, 'expires_at', v_new_expiry);
END; $$;
REVOKE EXECUTE ON FUNCTION purchase_relationship_pass FROM PUBLIC;
GRANT EXECUTE ON FUNCTION purchase_relationship_pass TO service_role;
```

- [ ] **Step 2: 헬퍼 + 순수 함수 작성**

```typescript
// lib/relationship/passes.ts
import { getServiceSupabase } from "@/lib/supabase";
import { startOfTodayKstIso } from "@/lib/admin-time";
import { PASS_PLAN_BY_KIND, type PassKind } from "./types";

export interface ActivePass { id: string; kind: string; expires_at: string; }

/** relationship의 현재 활성 패스(가장 늦은 만료). 없으면 null. */
export async function getActivePass(relationshipId: string): Promise<ActivePass | null> {
  const supabase = getServiceSupabase();
  const { data } = await supabase
    .from("relationship_passes")
    .select("id, kind, expires_at")
    .eq("relationship_id", relationshipId)
    .gt("expires_at", new Date().toISOString())
    .order("expires_at", { ascending: false })
    .limit(1)
    .maybeSingle();
  return data ?? null;
}

/** 오늘(KST) 스레드에 쌓인 user 턴 수. */
export async function getTodayThreadTurns(threadReadingId: string | null): Promise<number> {
  if (!threadReadingId) return 0;
  const supabase = getServiceSupabase();
  const { count } = await supabase
    .from("messages")
    .select("id", { count: "exact", head: true })
    .eq("reading_id", threadReadingId)
    .eq("role", "user")
    .gte("created_at", startOfTodayKstIso());
  return count ?? 0;
}

/** 오늘(KST) 연장 구매 횟수(source='rel_extend'). */
export async function getTodayExtendCount(userId: string): Promise<number> {
  const supabase = getServiceSupabase();
  const { count } = await supabase
    .from("star_transactions")
    .select("id", { count: "exact", head: true })
    .eq("user_id", userId)
    .eq("source", "rel_extend")
    .gte("created_at", startOfTodayKstIso());
  return count ?? 0;
}

/** 패스 구매 — RPC 래퍼. cost/days는 서버 config에서(위조 차단). */
export async function purchasePass(userId: string, relationshipId: string, kind: PassKind) {
  const plan = PASS_PLAN_BY_KIND[kind];
  const supabase = getServiceSupabase();
  const { data, error } = await supabase.rpc("purchase_relationship_pass", {
    p_user_id: userId, p_relationship_id: relationshipId,
    p_kind: kind, p_cost: plan.cost, p_days: plan.days,
  });
  if (error) return { success: false as const, reason: "rpc_error" };
  return {
    success: !!data.success as boolean,
    reason: data.reason as string | undefined,
    balance: (data.balance_after ?? 0) as number,
    expiresAt: data.expires_at as string | undefined,
  };
}
```

- [ ] **Step 3: 실패 테스트 (순수 함수 = dailyTurnAllowance는 Task 2, 여기선 구매 검증 로직 없음)**

이 파일은 대부분 DB 의존이라 순수 단위 테스트 대상이 적다. 만료 이어붙임 로직은 RPC(SQL)에 있어 dev 통합으로 검증(Step 5). 순수 검증 가능한 부분만:

```typescript
// lib/relationship/passes.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { PASS_PLAN_BY_KIND } from "./types.ts";

test("패스 kind → cost/days 매핑 (RPC 인자 정본)", () => {
  assert.deepEqual(
    { c: PASS_PLAN_BY_KIND.day7.cost, d: PASS_PLAN_BY_KIND.day7.days },
    { c: 60, d: 7 }
  );
});
```

- [ ] **Step 4: 빌드 + 테스트**

Run: `npm run build && node --import tsx --test lib/relationship/passes.test.ts`
Expected: 빌드 통과(Task 3의 import 해소), 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add lib/relationship/passes.ts lib/relationship/passes.test.ts app/api/relationship/route.ts supabase/migrations/20260718010000_relationship_pass_rpc.sql
git commit -m "feat(w1-c2): 패스 구매 RPC + 활성패스/일일카운트 헬퍼 + 관계 API 연결"
```

---

### Task 7: 스레드 기억 조립 (`lib/relationship/memory.ts`)

**Files:**
- Create: `lib/relationship/memory.ts`
- Test: `lib/relationship/memory.test.ts`

- [ ] **Step 1: 순수 분할 함수 + 파일 블록 작성**

```typescript
// lib/relationship/memory.ts — 스레드 기억: 최근 N턴 원문 + 임계치 요약 + 구조화 파일
import { RELATIONSHIP_STATUS_LABELS, type RelationshipStatus, type RelationshipMemo } from "./types";

/** API messages로 보낼 최근 메시지 수(user+assistant 합). 실측 튜닝. */
export const RECENT_MSGS = 24;      // 약 12턴
/** 미요약 older가 이만큼 쌓이면 요약 트리거. */
export const SUMMARY_TRIGGER = 32;  // 약 16턴

export interface ThreadMsg { role: "user" | "assistant"; content: string; }

export interface ThreadSplit {
  apiMessages: ThreadMsg[];        // 최근 원문 (Claude 입력)
  toSummarize: ThreadMsg[];        // 이번에 요약할 older 델타 ([] = 요약 안 함)
  newSummarizedCount: number;      // 요약 후 저장할 summarized_msg_count
}

/**
 * 전체 스레드 메시지(오름차순) + 이미 요약된 개수 → 최근창/요약델타 분할.
 * older = 최근창 밖. 미요약 older(older.slice(summarizedCount))가 SUMMARY_TRIGGER 이상이면 요약.
 */
export function splitThreadMessages(all: ThreadMsg[], summarizedCount: number): ThreadSplit {
  const recentStart = Math.max(0, all.length - RECENT_MSGS);
  const apiMessages = all.slice(recentStart);
  const older = all.slice(0, recentStart);
  const alreadic = Math.min(summarizedCount, older.length);
  const unsummarized = older.slice(alreadic);
  if (unsummarized.length >= SUMMARY_TRIGGER) {
    return { apiMessages, toSummarize: unsummarized, newSummarizedCount: older.length };
  }
  return { apiMessages, toSummarize: [], newSummarizedCount: summarizedCount };
}

export interface RelationshipFile {
  label: string;
  status: RelationshipStatus | string;
  hasSelfBirth: boolean;
  hasPartnerBirth: boolean;
  memo: RelationshipMemo;
}

/** system dynamicPart에 넣을 구조화 파일 블록 (결정적 — 요약 드리프트로 안 잃음). */
export function buildRelationshipFileBlock(f: RelationshipFile, rollingSummary: string | null): string {
  const statusLabel = RELATIONSHIP_STATUS_LABELS[f.status as RelationshipStatus] ?? f.status;
  const lines: string[] = [
    `## 관계 파일`,
    `[호칭: ${f.label}] [관계: ${statusLabel}]`,
    `[사주 등록: 나 ${f.hasSelfBirth ? "O" : "X"} / ${f.label} ${f.hasPartnerBirth ? "O" : "X"}]`,
  ];
  const rx = f.memo.prescriptions?.filter((p) => !p.resolved_at) ?? [];
  if (rx.length) lines.push(`[진행 중 처방: ${rx.map((p) => p.text).join(" / ")}]`);
  if (f.memo.skill_log?.length) {
    const recent = f.memo.skill_log.slice(-3).map((s) => `${s.skill}(${s.summary})`).join(" / ");
    lines.push(`[지난 스킬: ${recent}]`);
  }
  if (rollingSummary?.trim()) {
    lines.push(``, `## 지난 대화 요약`, rollingSummary.trim());
  }
  return "\n\n" + lines.join("\n");
}
```

- [ ] **Step 2: 실패 테스트**

```typescript
// lib/relationship/memory.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { splitThreadMessages, RECENT_MSGS, SUMMARY_TRIGGER, buildRelationshipFileBlock } from "./memory.ts";

const mk = (n: number) => Array.from({ length: n }, (_, i) => ({
  role: (i % 2 === 0 ? "user" : "assistant") as "user" | "assistant", content: `m${i}`,
}));

test("짧은 스레드 — 전부 최근창, 요약 없음", () => {
  const r = splitThreadMessages(mk(10), 0);
  assert.equal(r.apiMessages.length, 10);
  assert.deepEqual(r.toSummarize, []);
  assert.equal(r.newSummarizedCount, 0);
});

test("최근창 유지 + 트리거 미달이면 요약 안 함", () => {
  const total = RECENT_MSGS + SUMMARY_TRIGGER - 1;
  const r = splitThreadMessages(mk(total), 0);
  assert.equal(r.apiMessages.length, RECENT_MSGS);
  assert.deepEqual(r.toSummarize, []);
});

test("미요약 older가 트리거 도달 → 델타 요약 + 카운트 전진", () => {
  const total = RECENT_MSGS + SUMMARY_TRIGGER;
  const r = splitThreadMessages(mk(total), 0);
  assert.equal(r.apiMessages.length, RECENT_MSGS);
  assert.equal(r.toSummarize.length, SUMMARY_TRIGGER);
  assert.equal(r.newSummarizedCount, SUMMARY_TRIGGER);
});

test("이미 요약된 older는 재요약 안 함", () => {
  const total = RECENT_MSGS + SUMMARY_TRIGGER + 5;
  const r = splitThreadMessages(mk(total), SUMMARY_TRIGGER); // 앞 TRIGGER개 이미 요약
  assert.equal(r.toSummarize.length, 0); // 미요약 older=5 < TRIGGER
});

test("파일 블록 — 호칭/관계/처방 포함", () => {
  const b = buildRelationshipFileBlock(
    { label: "그이", status: "dating", hasSelfBirth: true, hasPartnerBirth: false,
      memo: { prescriptions: [{ text: "먼저 연락해보기", created_at: "x" }] } },
    "지난주 다툼 얘기를 나눴다."
  );
  assert.match(b, /그이/); assert.match(b, /연애 중/);
  assert.match(b, /먼저 연락해보기/); assert.match(b, /지난 대화 요약/);
});
```

- [ ] **Step 3: 테스트 실행 → 통과**

Run: `node --import tsx --test lib/relationship/memory.test.ts`
Expected: 5 tests pass.

- [ ] **Step 4: 커밋**

```bash
git add lib/relationship/memory.ts lib/relationship/memory.test.ts
git commit -m "feat(w1-c2): 스레드 기억 조립(최근창/임계요약 분할 + 관계 파일 블록)"
```

---

### Task 8: 요약 생성 + 관계 페르소나

**Files:**
- Create: `data/persona/byeolkong_relationship.md`
- Modify: `lib/claude.ts` (getRelationshipPersona + buildRelationshipSystemMessage + summarizeOlder)

- [ ] **Step 1: 관계 에이전트 페르소나 파일**

`data/persona/byeolkong_relationship.md` 작성 — 코어(byeolkong_core.md)에 합성될 도메인 파일. 핵심:
- 정체성: 이 관계를 계속 지켜보는 연애 상담 에이전트. 매번 새로 시작 X — 지난 맥락(관계 파일·요약)을 기억한 채 이어감.
- 첫 진입/복귀 안부: 관계 파일 기반으로 "요즘 {호칭}이랑은 어때?" 식 열기.
- 처방 → 체크인: 실행 가능한 조언을 줄 때 자연스럽게, 그리고 그게 다음 방문 안부의 씨앗임을 인지.
- 스킬 제안: 맥락상 적절할 때만 `[SKILL:key]` 마커(탈출구/유도 문구 금지 — C3 교훈). 남발 금지.
- 화법 원칙(코어 계승): 단정 예언 금지, 흐름·가능성·선택, 불안 자극 금지, 따뜻한 마무리.
- **[END] 없음**: 스레드는 종결 마커를 쓰지 않는다(하루 마무리는 서버가 소프트캡으로 유도).

- [ ] **Step 2: lib/claude.ts에 관계 도메인 추가**

[lib/claude.ts](../../../lib/claude.ts)의 타로 도메인 섹션 아래에 추가:

```typescript
// ===== 관계(우리 사이) 도메인 =====
let _cachedRelPersona: string | null = null;
function getRelationshipPersona(): string {
  if (_cachedRelPersona === null) {
    _cachedRelPersona =
      getCore() + "\n\n---\n\n" +
      readFileSync(join(process.cwd(), "data", "persona", "byeolkong_relationship.md"), "utf-8");
  }
  return _cachedRelPersona;
}

export interface RelationshipTurnContext {
  fileBlock: string;              // buildRelationshipFileBlock 결과
  nickname?: string | null;
  isFirstEver: boolean;          // 스레드 최초 진입(메시지 0)
  checkinPrompt?: string | null; // pending 체크인 → 먼저 안부
  dailyClose: boolean;           // 오늘 소프트캡 도달 → 하루 마무리 톤
}

export function buildRelationshipSystemMessage(ctx: RelationshipTurnContext): {
  staticPart: string; dynamicPart: string;
} {
  const staticPart = getRelationshipPersona();
  const nicknameLine = ctx.nickname?.trim() ? `\n[호칭(유저): ${ctx.nickname.trim()}]` : "";

  const firstGuide = ctx.isFirstEver
    ? `\n\n## 첫 진입 가이드\n관계 파일을 보고 {호칭}과의 지금 상황을 가볍게 짚으며 따뜻하게 열어. 처음 만난 낯선 상담이 아니라, 앞으로 이 관계를 계속 함께 볼 친구로. 무겁지 않게, 유저가 편하게 털어놓게.`
    : "";
  const checkinGuide = ctx.checkinPrompt
    ? `\n\n## 복귀 안부 (먼저 물어보기)\n지난번에 이런 처방/약속이 있었어: "${ctx.checkinPrompt}". 이번 응답은 그것부터 자연스럽게 안부로 물어("저번에 ~ 해보기로 했잖아, 어떻게 됐어?"). 확인 후 대화를 이어가.`
    : "";
  const closeGuide = ctx.dailyClose
    ? `\n\n## 오늘 마무리 톤 (하루 소프트캡 도달)\n오늘 나눈 대화가 충분히 쌓였어. 이번 응답은 오늘 얘기를 따뜻하게 매듭짓고 "내일 또 이어서 얘기하자"로 부드럽게 닫아. 단, [END] 마커는 절대 쓰지 마 — 스레드는 계속돼(내일 다시 열려). 새 주제를 크게 벌이지 말고 오늘 흐름을 정리.`
    : "";

  const dynamicPart = `---
## 이번 세션 정보${nicknameLine}
${ctx.fileBlock}
---${firstGuide}${checkinGuide}${closeGuide}`;

  return { staticPart, dynamicPart };
}

/** older 메시지 델타 요약 (haiku, 저비용). 이전 요약과 합쳐 갱신된 요약 반환. */
export async function summarizeOlder(
  prevSummary: string | null,
  older: { role: "user" | "assistant"; content: string }[]
): Promise<string> {
  const convo = older.map((m) => `${m.role === "user" ? "유저" : "별콩이"}: ${m.content}`).join("\n");
  const sys = `너는 연애 상담 대화의 기록 요약가야. 아래 [이전 요약]과 [새 대화]를 합쳐, 이 관계에서 오간 핵심(상황 변화·감정·별콩이 조언/처방·유저 반응)을 한국어 불릿 6~10개로 압축해. 사소한 잡담은 버리고, 나중에 대화를 이어갈 때 필요한 사실만. 200~500자.`;
  const user = `[이전 요약]\n${prevSummary ?? "(없음)"}\n\n[새 대화]\n${convo}`;
  const resp = await anthropic.messages.create({
    model: "claude-haiku-4-5-20251001",
    max_tokens: 700,
    system: sys,
    messages: [{ role: "user", content: user }],
  });
  const text = resp.content.find((b) => b.type === "text");
  return text && text.type === "text" ? text.text.trim() : (prevSummary ?? "");
}
```

- [ ] **Step 3: 빌드**

Run: `npm run build`
Expected: 통과.

- [ ] **Step 4: 커밋**

```bash
git add data/persona/byeolkong_relationship.md lib/claude.ts
git commit -m "feat(w1-c2): 관계 에이전트 페르소나 + system 빌더 + haiku older 요약"
```

---

### Task 9: 스레드 채팅 라우트 (`/api/relationship/chat`)

**Files:**
- Create: `app/api/relationship/chat/route.ts`
- 참조: [app/api/consultations/tarot/chat/route.ts](../../../app/api/consultations/tarot/chat/route.ts) (SSE·카운팅·민감·INSERT 골격)

- [ ] **Step 1: 라우트 작성 (게이트 → 캡 → 기억 → SSE)**

타로 chat 라우트를 복제하되 wrap/[END] 제거, 아래 로직으로 교체. 핵심 차이:
1. reading을 `relationship_id`로 스레드 확인, `getActivePass` 없으면 402.
2. 오늘 턴 ≥ `dailyTurnAllowance(오늘연장)`이면 `dailyClose=true`(하루 마무리 톤) — 그래도 이번 턴은 응답(마무리). 초과 저장은 되지만 다음 진입 시 클라가 연장 칩 노출.
3. 최근창(`splitThreadMessages`) → apiMessages만 Claude에 전송, `toSummarize` 있으면 스트림 후 `summarizeOlder` fire-and-forget → `rolling_summary`/`summarized_msg_count` 갱신.
4. `[CHECKIN:내용]` 마커 파싱 → `memo.pending_checkin` 저장(Task 17에서 소비). `last_visited_at` 갱신.

```typescript
// app/api/relationship/chat/route.ts (핵심 발췌 — 타로 chat 라우트 구조 준용)
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { buildRelationshipSystemMessage, streamChat, summarizeOlder } from "@/lib/claude";
import { checkRateLimit, getClientIp, maybeSweepExpired } from "@/lib/ratelimit";
import { logError, ctxFromRequest } from "@/lib/logger";
import { detectSensitiveSync, detectSensitiveAsync, recordSensitiveAlert } from "@/lib/sensitive";
import { getActivePass, getTodayThreadTurns, getTodayExtendCount } from "@/lib/relationship/passes";
import { dailyTurnAllowance, type RelationshipMemo, type RelationshipStatus } from "@/lib/relationship/types";
import { splitThreadMessages, buildRelationshipFileBlock, type ThreadMsg } from "@/lib/relationship/memory";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

const MAX_MESSAGE_LEN = 8000;
const MARKER_REGEX = /\[CHECKIN:[^\]]*\]|\[SKILL:[a-z_]+\]/g;
const CHECKIN_RE = /\[CHECKIN:([^\]]+)\]/;

interface Body { relationshipId: string; message: string; }

export async function POST(request: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required" }, { status: 401 });

  maybeSweepExpired();
  const ip = getClientIp(request);
  if (!checkRateLimit({ namespace: "rel_chat_session", key: userId, max: 20, windowMs: 60_000 }).ok ||
      !checkRateLimit({ namespace: "rel_chat_ip", key: ip, max: 60, windowMs: 60_000 }).ok) {
    return NextResponse.json({ error: "rate_limited" }, { status: 429, headers: { "Retry-After": "60" } });
  }

  let body: Body;
  try { body = (await request.json()) as Body; } catch { return NextResponse.json({ error: "invalid_json" }, { status: 400 }); }
  if (!body.relationshipId || typeof body.message !== "string" || body.message.length < 1 || body.message.length > MAX_MESSAGE_LEN)
    return NextResponse.json({ error: "invalid_input" }, { status: 400 });

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase
    .from("relationships")
    .select("id, user_id, label, status, self_profile_id, partner_profile_id, thread_reading_id, rolling_summary, summarized_msg_count, memo")
    .eq("id", body.relationshipId).maybeSingle();
  if (!rel || rel.user_id !== userId || !rel.thread_reading_id)
    return NextResponse.json({ error: "not_found" }, { status: 404 });

  // 패스 게이트
  const pass = await getActivePass(rel.id);
  if (!pass) return NextResponse.json({ error: "pass_required" }, { status: 402 });

  // 일일 소프트캡 — 도달 시 이번 턴은 하루 마무리 톤
  const [todayTurns, todayExtend] = await Promise.all([
    getTodayThreadTurns(rel.thread_reading_id), getTodayExtendCount(userId),
  ]);
  const dailyClose = todayTurns >= dailyTurnAllowance(todayExtend);

  // 누적 메시지(오름차순) → 최근창/요약델타
  const { data: pastRows } = await supabase
    .from("messages").select("role, content").eq("reading_id", rel.thread_reading_id)
    .order("created_at", { ascending: true });
  const past = (pastRows ?? []) as ThreadMsg[];
  const isFirstEver = past.length === 0;
  const split = splitThreadMessages([...past, { role: "user", content: body.message }], rel.summarized_msg_count ?? 0);

  // 사주 등록 여부(파일 블록용)
  const fileBlock = buildRelationshipFileBlock({
    label: rel.label, status: rel.status as RelationshipStatus,
    hasSelfBirth: !!rel.self_profile_id, hasPartnerBirth: !!rel.partner_profile_id,
    memo: (rel.memo ?? {}) as RelationshipMemo,
  }, rel.rolling_summary);

  const { data: userRow } = await supabase.from("users").select("nickname").eq("id", userId).maybeSingle();

  const systemMessage = buildRelationshipSystemMessage({
    fileBlock, nickname: (userRow?.nickname as string | null) ?? null,
    isFirstEver, checkinPrompt: null /* Task 17에서 pending_checkin 주입 */, dailyClose,
  });

  const sensitiveSync = detectSensitiveSync(body.message);
  const headers: Record<string, string> = {
    "Content-Type": "text/plain; charset=utf-8", "Cache-Control": "no-cache, no-transform",
    "X-Accel-Buffering": "no", "X-Daily-Cap": dailyClose ? "reached" : "ok",
  };
  if (sensitiveSync) { headers["X-Sensitive-Category"] = sensitiveSync.category; headers["X-Sensitive-Severity"] = String(sensitiveSync.severity); }

  const encoder = new TextEncoder();
  let assistantText = "";
  const stream = new ReadableStream({
    async start(controller) {
      try {
        for await (const chunk of streamChat(systemMessage, split.apiMessages, 1400)) {
          assistantText += chunk; controller.enqueue(encoder.encode(chunk));
        }
        if (!assistantText.trim()) throw new Error("empty_assistant_stream");

        const ts = Date.now();
        await supabase.from("messages").insert([
          { reading_id: rel.thread_reading_id, role: "user", content: body.message, created_at: new Date(ts).toISOString() },
          { reading_id: rel.thread_reading_id, role: "assistant", content: assistantText, created_at: new Date(ts + 1).toISOString() },
        ]);

        // last_visited_at + [CHECKIN:] 파싱
        const checkin = assistantText.match(CHECKIN_RE);
        const memo = (rel.memo ?? {}) as RelationshipMemo;
        if (checkin) memo.pending_checkin = { text: checkin[1].trim(), created_at: new Date().toISOString() };
        await supabase.from("relationships").update({ last_visited_at: new Date().toISOString(), memo }).eq("id", rel.id);

        // 임계 요약 (fire-and-forget)
        if (split.toSummarize.length > 0) {
          void summarizeOlder(rel.rolling_summary, split.toSummarize).then((sum) =>
            supabase.from("relationships").update({ rolling_summary: sum, summarized_msg_count: split.newSummarizedCount }).eq("id", rel.id)
          ).catch((e) => console.warn("[rel] summarize 실패:", e));
        }

        // 민감 감지
        if (sensitiveSync) {
          void recordSensitiveAlert({ match: sensitiveSync, userId, readingId: rel.thread_reading_id, messageText: body.message });
          await supabase.from("readings").update({ has_sensitive: true }).eq("id", rel.thread_reading_id);
          if (sensitiveSync.certainty !== "high")
            void detectSensitiveAsync(body.message).then((m) => { if (m && m.method !== "regex") void recordSensitiveAlert({ match: m, userId, readingId: rel.thread_reading_id!, messageText: body.message }); });
        }
        controller.close();
      } catch (err) {
        await logError(err, ctxFromRequest(request, { route: "/api/relationship/chat", userId, extra: { relationshipId: rel.id } }));
        controller.error(err);
      }
    },
  });
  return new Response(stream, { headers });
}
```

주의: 출력에서 `[SKILL:]`/`[CHECKIN:]` 마커는 클라가 렌더 시 제거(Task 11 UI). `MARKER_REGEX`는 향후 글자수 계산에 쓸 수 있게 정의만 유지.

- [ ] **Step 2: 빌드**

Run: `npm run build`
Expected: 통과.

- [ ] **Step 3: 커밋**

```bash
git add app/api/relationship/chat/route.ts
git commit -m "feat(w1-c2): 지속 스레드 채팅 라우트 — 패스 게이트·소프트캡·기억·요약"
```

---

### Task 10: 패스 구매 + 연장 라우트

**Files:**
- Create: `app/api/relationship/pass/route.ts` (구매)
- Create: `app/api/relationship/extend/route.ts` (5별 연장)

- [ ] **Step 1: 패스 구매 라우트**

```typescript
// app/api/relationship/pass/route.ts
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { purchasePass } from "@/lib/relationship/passes";
import { PASS_PLAN_BY_KIND, type PassKind } from "@/lib/relationship/types";

export const dynamic = "force-dynamic";

export async function POST(request: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required", code: "LOGIN_REQUIRED" }, { status: 401 });
  const body = await request.json().catch(() => null) as { relationshipId?: string; kind?: PassKind } | null;
  if (!body?.relationshipId || !body.kind || !(body.kind in PASS_PLAN_BY_KIND))
    return NextResponse.json({ error: "invalid_input" }, { status: 400 });

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase.from("relationships").select("id, user_id").eq("id", body.relationshipId).maybeSingle();
  if (!rel || rel.user_id !== userId) return NextResponse.json({ error: "not_found" }, { status: 404 });

  const res = await purchasePass(userId, rel.id, body.kind);
  if (!res.success) {
    const code = res.reason === "insufficient" ? "INSUFFICIENT_STARS" : "PURCHASE_FAILED";
    return NextResponse.json({ error: code, reason: res.reason, balance: res.balance }, { status: res.reason === "insufficient" ? 402 : 500 });
  }
  return NextResponse.json({ success: true, balance: res.balance, expiresAt: res.expiresAt });
}
```

- [ ] **Step 2: 5별 연장 라우트**

```typescript
// app/api/relationship/extend/route.ts — 오늘 소프트캡 +5턴 (5별)
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { spendStars } from "@/lib/stars";
import { getActivePass } from "@/lib/relationship/passes";
import { EXTEND_COST } from "@/lib/relationship/types";

export const dynamic = "force-dynamic";

export async function POST(request: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required" }, { status: 401 });
  const body = await request.json().catch(() => null) as { relationshipId?: string } | null;
  if (!body?.relationshipId) return NextResponse.json({ error: "invalid_input" }, { status: 400 });

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase.from("relationships").select("id, user_id, thread_reading_id").eq("id", body.relationshipId).maybeSingle();
  if (!rel || rel.user_id !== userId) return NextResponse.json({ error: "not_found" }, { status: 404 });
  // 연장은 활성 패스가 있을 때만 의미 있음
  if (!(await getActivePass(rel.id))) return NextResponse.json({ error: "pass_required" }, { status: 402 });

  const spend = await spendStars(userId, EXTEND_COST, { readingId: rel.thread_reading_id, source: "rel_extend" });
  if (!spend.success)
    return NextResponse.json({ error: "INSUFFICIENT_STARS", reason: spend.reason, balance: spend.balance }, { status: 402 });
  return NextResponse.json({ success: true, balance: spend.balance });
}
```

주의: 연장 이중탭 방지는 클라 버튼 disable(Task 11) + 원자 spend. (payment_id식 멱등 불가 — 스펙 §8, nonce는 백로그.)

- [ ] **Step 3: 빌드 + 커밋**

```bash
npm run build
git add app/api/relationship/pass app/api/relationship/extend
git commit -m "feat(w1-c2): 패스 구매 + 5별 연장 라우트"
```

---

### Task 11: `/relationship` 스레드 UI — S2/S3/S4

**Files:**
- Modify: `app/relationship/page.tsx` (등록됨 분기 = placeholder → 실제)
- Create: `components/relationship/ThreadChat.tsx`
- Create: `components/relationship/PassPanel.tsx`
- 참조: [app/tarot/reading/page.tsx](../../../app/tarot/reading/page.tsx) (SSE fetch + 버블 렌더), [components/consultations/](../../../components) ChatBubble

- [ ] **Step 1: PassPanel (구매 UI + 한도 명시)**

`PassPanel.tsx`: `PASS_PLANS` 순회 카드(가격·일수·7일권 추천 뱃지) + **한도 명시** 문구("하루 최대 20번의 대화, 초과 시 5별로 이어가기") + 잔액 표기. 클릭 → `POST /api/relationship/pass` → 성공 시 부모 상태 갱신. 잔액 부족(402) → `/shop` 유도.

- [ ] **Step 2: ThreadChat (SSE 대화)**

`ThreadChat.tsx`: 타로 reading 채팅 UI를 준용. 입력 → `POST /api/relationship/chat`(body `{relationshipId, message}`) SSE 스트림 → 버블. **응답 렌더 시 `[SKILL:...]`·`[CHECKIN:...]` 마커 제거**(정규식 `/\[(?:SKILL:[a-z_]+|CHECKIN:[^\]]+)\]/g`). 헤더 `X-Daily-Cap: reached`면 입력창 대신 **연장 칩**("오늘 여기까지 · 5별로 +5턴 이어가기" → `POST /api/relationship/extend`, 성공 시 입력 재활성) + "내일 이어서" 안내.

- [ ] **Step 3: page.tsx 상태 배선**

`GET /api/relationship` 응답으로:
- `pass === null` + 스레드 메시지 0: **S2a** — 관계 헤더 + 소개 + `PassPanel`(입력 잠금).
- `pass === null` + 메시지 있음: **S2b** — `ThreadChat`(읽기 전용, 마지막까지 렌더) + "패스 만료 — 다시 이어가자" + `PassPanel`.
- `pass !== null` + `daily.used < daily.allowance`: **S3** — 상단 패스 D-day + 오늘 남은 턴 + `ThreadChat`(활성). 첫 진입(메시지 0)이면 별콩이 자동 첫 인사: 빈 스레드면 클라가 개시용으로 짧은 시스템성 유저 메시지 없이 — 대신 **최초 1회 "별콩이랑 얘기 시작하기" 버튼** → 첫 `POST /api/relationship/chat`에 유저의 첫 발화 전송. (자동 발화 대신 유저 주도 시작 — 서버 isFirstEver 가이드가 따뜻하게 받음.)
- `pass !== null` + `daily.used >= daily.allowance`: **S4** — `ThreadChat` + 연장 칩(입력 잠금).
- `partnerProfileId == null`: 상단 비침습 배너 "상대 생년월일이 없어 — 궁합 보려면 다시 등록"(Task 15 재등록으로 연결, 지금은 문구만).

- [ ] **Step 4: dev E2E**

Run: dev → 등록된 계정 → `/relationship` → 패스 구매(1일권, 웰컴별) → 대화 몇 턴(SSE) → Supabase에서 messages 누적 + relationships.last_visited_at 갱신 확인. 소프트캡 테스트는 `DAILY_TURN_CAP`을 임시로 2로 낮춰 도달→연장 칩→+5 재개 확인 후 원복.
Expected: S2a→S3→(캡)→S4→연장→S3 흐름 통과.

- [ ] **Step 5: 커밋 + dev push (Stage 2b 마감)**

```bash
git add app/relationship components/relationship
git commit -m "feat(w1-c2): 우리 사이 스레드 UI — 패스 패널·SSE 대화·연장 칩 (S2/S3/S4)"
git push origin dev
```

---

# Stage 2c — 스킬 4종

### Task 12: 스킬 레지스트리 (`lib/relationship/skills.ts`)

**Files:**
- Create: `lib/relationship/skills.ts`
- Test: `lib/relationship/skills.test.ts`

- [ ] **Step 1: 레지스트리 작성**

```typescript
// lib/relationship/skills.ts — "우리 사이" 스킬 단일 진실 원천 (확장 가능)
import type { SpreadType } from "@/lib/tarot/spreads";

export type SkillKind = "tarot_draw" | "compat" | "dialogue";

export interface RelationshipSkill {
  key: string;
  label: string;
  tagline: string;
  starCost: number;
  kind: SkillKind;
  spread?: SpreadType;            // kind="tarot_draw"
  requiresPartnerBirth?: boolean; // compat
  active: boolean;
  order: number;
}

export const RELATIONSHIP_SKILLS: RelationshipSkill[] = [
  { key: "checkin", label: "관계 체크인", tagline: "두 사람의 상태와 서로의 필요를 나란히", starCost: 45, kind: "tarot_draw", spread: "checkin_6", active: true, order: 1 },
  { key: "deep_feelings", label: "걔 속마음", tagline: "겉모습 뒤의 진짜 속마음까지", starCost: 40, kind: "tarot_draw", spread: "deep_feelings_5", active: true, order: 2 },
  { key: "compat", label: "우리 궁합", tagline: "두 사람 사주로 보는 궁합", starCost: 40, kind: "compat", requiresPartnerBirth: true, active: true, order: 3 },
  { key: "verdict", label: "싸움 잘잘못 판정", tagline: "양쪽 입장을 듣고 비율로 판정 + 화해 처방", starCost: 30, kind: "dialogue", active: true, order: 4 },
];

const BY_KEY: Record<string, RelationshipSkill> = Object.fromEntries(
  RELATIONSHIP_SKILLS.map((s) => [s.key, s])
);

export function getSkill(key: string): RelationshipSkill | null {
  return BY_KEY[key] ?? null;
}

/** 진열용(active만, order순). */
export function listActiveSkills(): RelationshipSkill[] {
  return RELATIONSHIP_SKILLS.filter((s) => s.active).sort((a, b) => a.order - b.order);
}
```

- [ ] **Step 2: 실패 테스트**

```typescript
// lib/relationship/skills.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { RELATIONSHIP_SKILLS, getSkill, listActiveSkills } from "./skills.ts";

test("v1 스킬 4종 + 가격/kind", () => {
  assert.equal(getSkill("checkin")?.starCost, 45);
  assert.equal(getSkill("deep_feelings")?.kind, "tarot_draw");
  assert.equal(getSkill("compat")?.requiresPartnerBirth, true);
  assert.equal(getSkill("verdict")?.kind, "dialogue");
  assert.equal(getSkill("nope"), null);
});

test("tarot_draw 스킬은 spread 필수", () => {
  for (const s of RELATIONSHIP_SKILLS.filter((x) => x.kind === "tarot_draw"))
    assert.ok(s.spread, `${s.key} needs spread`);
});

test("listActiveSkills = active만 order순", () => {
  const ks = listActiveSkills().map((s) => s.key);
  assert.deepEqual(ks, ["checkin", "deep_feelings", "compat", "verdict"]);
});
```

- [ ] **Step 3: 테스트 → 통과 → 커밋**

```bash
node --import tsx --test lib/relationship/skills.test.ts
git add lib/relationship/skills.ts lib/relationship/skills.test.ts
git commit -m "feat(w1-c2): 스킬 확장 레지스트리 RELATIONSHIP_SKILLS"
```

---

### Task 13: 스킬 결과 → 스레드 반영 헬퍼

**Files:**
- Create: `lib/relationship/skill-log.ts`

- [ ] **Step 1: 헬퍼 작성**

```typescript
// lib/relationship/skill-log.ts — 스킬 완료 결과를 관계 memo.skill_log에 적립 (별콩이 기억용)
import { getServiceSupabase } from "@/lib/supabase";
import type { RelationshipMemo } from "./types";

/** 스킬 reading 완료 시 호출 — 결과 요약을 관계 파일에 남긴다. */
export async function logSkillToThread(
  relationshipId: string, skillKey: string, readingId: string, summary: string
): Promise<void> {
  const supabase = getServiceSupabase();
  const { data: rel } = await supabase.from("relationships").select("memo").eq("id", relationshipId).maybeSingle();
  if (!rel) return;
  const memo = (rel.memo ?? {}) as RelationshipMemo;
  memo.skill_log = [...(memo.skill_log ?? []), {
    skill: skillKey, reading_id: readingId,
    summary: summary.replace(/\s+/g, " ").trim().slice(0, 120),
    created_at: new Date().toISOString(),
  }].slice(-20); // 최근 20개만 유지
  await supabase.from("relationships").update({ memo }).eq("id", relationshipId);
}
```

- [ ] **Step 2: 빌드 + 커밋**

```bash
npm run build
git add lib/relationship/skill-log.ts
git commit -m "feat(w1-c2): 스킬 결과 → 관계 memo.skill_log 반영 헬퍼"
```

---

### Task 14: 스킬 실행 — tarot_draw + compat 디스패치

**Files:**
- Modify: `app/api/consultations/tarot/route.ts` (relationshipId/skillKey 수용 → 태깅 + 패스 게이트)
- Modify: `app/api/consultations/tarot/chat/route.ts` ([END] 후처리에 relationship_id면 logSkillToThread)
- Modify: `app/api/fortune/create/route.ts` (compat에 relationshipId 수용 → 태깅 + 완료 시 logSkillToThread)
- Modify: `app/tarot/draw/page.tsx` (스킬 draw 컨텍스트 sessionStorage 수용)

- [ ] **Step 1: 타로 생성 라우트에 스킬 태깅**

[app/api/consultations/tarot/route.ts](../../../app/api/consultations/tarot/route.ts) POST body에 `relationshipId?`, `skillKey?` 추가. 있으면:
- 관계 소유권 확인 + `getActivePass` 필수(없으면 402 `pass_required`).
- `skillKey`를 `RELATIONSHIP_SKILLS`로 검증하고 `spread`가 body.spreadType과 일치하는지 확인(위조 차단).
- readings INSERT에 `relationship_id`, `skill_key` 추가. (나머지 흐름 동일.)

```typescript
// insert 객체에 추가
      relationship_id: relationshipId ?? null,
      skill_key: skillKey ?? null,
```

- [ ] **Step 2: 타로 chat [END] 후처리 — 스킬이면 스레드 반영**

[app/api/consultations/tarot/chat/route.ts](../../../app/api/consultations/tarot/chat/route.ts)의 reading select에 `relationship_id, skill_key` 추가. `[END]` 도달 블록에서, `reading.relationship_id && reading.skill_key`면:

```typescript
if (assistantText.includes("[END]") && reading.relationship_id && reading.skill_key) {
  const { logSkillToThread } = await import("@/lib/relationship/skill-log");
  const { extractClosingLine } = await import("@/lib/saju/closing");
  const summary = extractClosingLine([...(pastMessages ?? []), { role: "assistant", content: assistantText }] as any) ?? "";
  void logSkillToThread(reading.relationship_id, reading.skill_key, reading.id, summary);
}
```

- [ ] **Step 3: compat 생성에 스킬 태깅**

[app/api/fortune/create/route.ts](../../../app/api/fortune/create/route.ts)를 읽고, compat 타입 생성 경로에 `relationshipId?` 수용 → readings INSERT에 `relationship_id`, `skill_key='compat'` 추가 + 활성 패스 확인. 리포트 생성 완료(응답 텍스트 확보) 직후 `logSkillToThread(relationshipId, 'compat', readingId, 리포트 요약필드)` 호출.

- [ ] **Step 4: draw 페이지 스킬 컨텍스트**

[app/tarot/draw/page.tsx](../../../app/tarot/draw/page.tsx): sessionStorage `byeolkong:rel_skill = {relationshipId, skillKey, spread}` 있으면, 스프레드/감정/고민을 스킬 값으로 세팅(감정=관계 파생, 고민="우리 사이: {스킬 label}")하고 draw 완료 후 `POST /api/consultations/tarot`에 `relationshipId`/`skillKey` 포함해 생성 → `/tarot/reading`으로. (기존 draw 흐름 재사용, 진입 컨텍스트만 스킬.)

- [ ] **Step 5: 빌드 + dev 확인 + 커밋**

Run: `npm run build` → dev에서 스킬 없이 기존 타로/궁합 흐름 회귀 없는지 먼저 확인.
```bash
git add app/api/consultations/tarot app/api/fortune/create app/tarot/draw
git commit -m "feat(w1-c2): 스킬 tarot_draw/compat 디스패치 — 관계 태깅·패스 게이트·스레드 반영"
```

---

### Task 15: 스킬 실행 — verdict(dialogue) + 스킬 UI (마커 칩 + 접힌 메뉴)

**Files:**
- Create: `app/api/relationship/verdict/route.ts` (verdict 세션 생성 + 대화)
- Create: `components/relationship/SkillMenu.tsx` (접힌 상시 메뉴)
- Modify: `components/relationship/ThreadChat.tsx` ([SKILL:key] 마커 → 칩)
- Create: `data/persona/byeolkong_verdict.md` (판정 프롬프트) 또는 relationship 페르소나 내 분기

- [ ] **Step 1: verdict 세션 모델**

verdict = 관계 하위 대화(consultation_type='relationship', `skill_key='verdict'`, relationship_id, **스레드와 별개 reading**). 짧은 수렴형(양쪽 입장 청취 → 비율 판정 → 화해 처방 → [END]). 생성 라우트:

```typescript
// app/api/relationship/verdict/route.ts (POST: 세션 시작 — 30별 차감 + reading 생성)
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { spendStars, getStarBalance } from "@/lib/stars";
import { getActivePass } from "@/lib/relationship/passes";
import { getSkill } from "@/lib/relationship/skills";

export const dynamic = "force-dynamic";

export async function POST(request: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required" }, { status: 401 });
  const body = await request.json().catch(() => null) as { relationshipId?: string } | null;
  if (!body?.relationshipId) return NextResponse.json({ error: "invalid_input" }, { status: 400 });
  const skill = getSkill("verdict")!;

  const supabase = getServiceSupabase();
  const { data: rel } = await supabase.from("relationships").select("id, user_id").eq("id", body.relationshipId).maybeSingle();
  if (!rel || rel.user_id !== userId) return NextResponse.json({ error: "not_found" }, { status: 404 });
  if (!(await getActivePass(rel.id))) return NextResponse.json({ error: "pass_required" }, { status: 402 });
  if ((await getStarBalance(userId)) < skill.starCost)
    return NextResponse.json({ error: "INSUFFICIENT_STARS", required: skill.starCost }, { status: 402 });

  const { data: reading } = await supabase.from("readings").insert({
    user_id: userId, consultation_type: "relationship", relationship_id: rel.id,
    skill_key: "verdict", profile_id: null, saju_data: null, stars_spent: skill.starCost, has_sensitive: false,
  }).select("id").single();
  if (!reading) return NextResponse.json({ error: "reading_failed" }, { status: 500 });

  const spend = await spendStars(userId, skill.starCost, { readingId: reading.id, source: "rel_skill_verdict" });
  if (!spend.success) { await supabase.from("readings").delete().eq("id", reading.id); return NextResponse.json({ error: "INSUFFICIENT_STARS" }, { status: 402 }); }
  return NextResponse.json({ id: reading.id, success: true, balance: spend.balance });
}
```

verdict 대화는 별도 chat 라우트 없이 **relationship/chat 라우트를 재사용하지 않는다**(스레드 전용). 대신 verdict 전용 간단 chat: `app/api/relationship/verdict/[id]/chat` 또는 verdict 라우트에 `?turn` — **간결화**: verdict는 관계 chat 라우트에 `skillReadingId` 파라미터를 받아 처리하도록 확장하되, 이 reading은 수렴형(간단 임계: 4턴 후 [END] 유도)으로 별도 시스템 프롬프트(`byeolkong_verdict.md` 또는 relationship 페르소나 내 verdict 가이드) 사용. 결과 [END] 시 `logSkillToThread(rel.id, 'verdict', reading.id, closing)`.

> 실행자 노트: verdict chat은 신규 소형 라우트(`app/api/relationship/verdict/chat/route.ts`)로 두는 게 스레드 라우트를 오염 안 시켜 더 깔끔하다. 타로 chat 골격 복제 + WRAP 대신 4턴 하드캡 + verdict 프롬프트. 결과는 스킬 reading의 messages에 저장.

- [ ] **Step 2: SkillMenu (접힌 상시 메뉴)**

`SkillMenu.tsx`: `listActiveSkills()` 순회 — 접힌 아코디언/시트로 스킬 카드(label·tagline·가격). 클릭 시 kind별:
- tarot_draw: `sessionStorage['byeolkong:rel_skill']` 세팅 → `/tarot/draw`.
- compat: `requiresPartnerBirth && !partnerProfileId`면 잠금(재등록 유도). 아니면 `POST /api/fortune/create`(compat + relationshipId) → 리포트로.
- dialogue(verdict): `POST /api/relationship/verdict` → verdict chat 화면.

- [ ] **Step 3: 마커 칩 (ThreadChat)**

`ThreadChat.tsx`: assistant 응답에 `[SKILL:key]`가 있으면 그 버블 하단에 해당 스킬 칩(가격 포함) 렌더 → 클릭 시 SkillMenu와 동일 디스패치. (마커 자체는 텍스트에서 제거해 표시.)

- [ ] **Step 4: 빌드 + dev E2E + 커밋**

Run: `npm run build` → dev: 접힌 메뉴에서 4스킬 각각 실행(체크인 draw→풀이, 속마음 draw, 궁합 리포트, 판정 대화) → 완료 후 스레드 대화에서 별콩이가 "저번에 궁합 봤을 때~" 식으로 기억하는지(memo.skill_log 주입) 확인.
```bash
git add app/api/relationship/verdict components/relationship data/persona/byeolkong_verdict.md
git commit -m "feat(w1-c2): verdict 판정 스킬 + 스킬 메뉴/마커 칩 (4종 라이브)"
```

---

### Task 16: 프로필 삭제 우아한 강등 (마이페이지)

**Files:**
- Modify: 마이페이지 프로필 삭제 경로 (`grep -rn "user_profiles" app/mypage app/api` 로 삭제 API 위치 확인)
- Modify: `app/relationship/page.tsx` (partner 누락 배너 → 재등록)

- [ ] **Step 1: 삭제 경고**

프로필 삭제 API/화면에서, 삭제 대상이 `relationships.self_profile_id` 또는 `partner_profile_id`로 참조되면 경고 반환/표시("우리 사이에서 사용 중 — 삭제하면 궁합을 다시 보려면 생일을 다시 등록해야 해요"). 확인 시 삭제 진행(FK SET NULL이 관계는 보존).

Run: `grep -rn "user_profiles" app/mypage app/api/profiles app/api/saju 2>/dev/null | grep -i delete` 로 정확한 삭제 지점 확인.

- [ ] **Step 2: 재등록 경로**

`app/relationship/page.tsx`의 partner 누락 배너를 상대 생일 재등록 폼(RegisterOnboarding의 partner 스텝 재사용, `POST /api/relationship`에 partnerProfile만 patch하는 경로 or PATCH 추가)으로 연결. **간결화**: `PATCH /api/relationship`에 `partnerProfile` 받아 새 partner 프로필 생성 + `partner_profile_id` 갱신.

- [ ] **Step 3: 빌드 + dev 확인 + 커밋 + dev push (Stage 2c 마감)**

Run: dev → 마이페이지에서 상대 프로필 삭제 → 경고 → 삭제 → `/relationship`에서 궁합 잠금 + 배너 → 재등록 → 궁합 해제 확인.
```bash
git add app/mypage app/api/relationship app/relationship
git commit -m "feat(w1-c2): 프로필 삭제 우아한 강등 + 상대 생일 재등록"
git push origin dev
```

---

# Stage 2d — 인앱 체크인

### Task 17: pending 체크인 주입 (복귀 안부)

**Files:**
- Modify: `app/api/relationship/chat/route.ts` (checkinPrompt 주입 + 소진)

- [ ] **Step 1: 복귀 안부 주입**

Task 9에서 `checkinPrompt: null`로 둔 것을 실제 로직으로. 스레드 진입 첫 턴 판정 + pending 존재 + 갭 조건:

```typescript
// rel select에 last_visited_at 추가. buildRelationshipSystemMessage 호출 전:
const memoNow = (rel.memo ?? {}) as RelationshipMemo;
const CHECKIN_GAP_MS = 6 * 60 * 60 * 1000;
const lastVisit = rel.last_visited_at ? new Date(rel.last_visited_at).getTime() : 0;
const checkinPrompt =
  memoNow.pending_checkin && Date.now() - lastVisit > CHECKIN_GAP_MS
    ? memoNow.pending_checkin.text : null;
// buildRelationshipSystemMessage({ ..., checkinPrompt })
```

- [ ] **Step 2: 소진 (안부 후 pending 제거)**

응답 저장 블록에서, `checkinPrompt`가 주입됐으면(이번 턴이 복귀 안부) 처리 후 `pending_checkin`을 null로 이동하고 `prescriptions`에 resolved 기록:

```typescript
if (checkinPrompt) {
  memo.prescriptions = [...(memo.prescriptions ?? []), { text: checkinPrompt, created_at: memoNow.pending_checkin!.created_at, resolved_at: new Date().toISOString() }];
  memo.pending_checkin = null;
}
// (위 [CHECKIN:] 신규 파싱과 병합해 하나의 memo update로)
```

주의: 같은 턴에 신규 `[CHECKIN:]`가 또 나오면 그건 새 pending으로 세팅(순서: 소진 먼저, 신규 세팅 나중).

- [ ] **Step 2: dev 확인**

Run: dev → 스레드에서 별콩이가 처방 주는 상황 유도(응답에 `[CHECKIN:]` 나오게) → relationships.memo.pending_checkin 세팅 확인 → `last_visited_at`을 6시간+ 과거로 수동 조정(Supabase) → 재진입 첫 턴에 별콩이가 먼저 안부 → pending 소진 확인.
Expected: 복귀 안부 → 소진 루프 동작.

- [ ] **Step 3: 커밋**

```bash
git add app/api/relationship/chat/route.ts
git commit -m "feat(w1-c2): 인앱 체크인 — 복귀 안부 주입 + pending 소진"
```

---

### Task 18: 탈퇴 정리 확인 + AGENTS.md 갱신

**Files:**
- 확인: `app/api/auth/withdraw/route.ts` (CASCADE로 자동 정리되는지)
- Modify: `AGENTS.md`

- [ ] **Step 1: 탈퇴 CASCADE 확인**

`app/api/auth/withdraw/route.ts`를 읽고, users 삭제 시 `relationships`(user_id CASCADE) → `relationship_passes`(CASCADE) + `readings`(relationship_id CASCADE는 relationships 삭제 시, user_id CASCADE는 users 삭제 시 — 스레드/verdict reading 정리) 체인 확인. 추가 코드 불필요해야 정상. 부족하면 withdraw에 relationships 삭제 단계 보강.

Run: dev 테스트 계정으로 관계+패스+스레드 생성 후 탈퇴 → 모든 row 정리 확인(Supabase).

- [ ] **Step 2: AGENTS.md 갱신**

AGENTS.md의 탭 구조(내 고민톡→우리 사이), 상품(패스 3종·스킬 4종), "우리 사이" 종목 설명, 사이클 2 완료 항목을 반영. 진행 상황 섹션에 "완료 (2026-07-18 세션 — W1 사이클 2)" 추가.

- [ ] **Step 3: 커밋**

```bash
git add AGENTS.md app/api/auth/withdraw
git commit -m "docs: AGENTS.md W1 사이클 2 우리 사이 반영 + 탈퇴 정리 확인"
```

---

### Task 19: Stage 2d 마감 dev 통합 확인 + push

- [ ] **Step 1: dev 전체 스모크 (성공 기준 §9)**

dev 브라우저로 순서 확인:
- [ ] S1 콜드스타트 → 등록 → S2a(입력잠금+구매패널)
- [ ] 1일권 구매(웰컴별) → S3 대화 SSE
- [ ] 재구매 시 만료 이어붙음 (Supabase expires_at 확인)
- [ ] 소프트캡 도달(임시 캡 하향) → S4 마무리 톤 + 연장 → +5 재개
- [ ] 스레드 깊어질 때 요약 갱신(rolling_summary 채워짐) + 응답 지연 없음
- [ ] 스킬 4종 실행 + 결과 스레드 기억(skill_log)
- [ ] 처방 → 재방문 복귀 안부 → 소진
- [ ] 파트너 프로필 삭제 → 궁합 잠금 + 재등록
- [ ] 탭 "우리 사이", 보관함은 내 정보, 스레드 본체 보관함 미노출
- [ ] 탈퇴 CASCADE 정리

- [ ] **Step 2: push**

```bash
git push origin dev
```

Expected: dev 도메인에서 사용자 최종 확인. **main 머지 금지 — 사이클 3(QA+prod 일괄)에서.**

---

## Self-Review 결과

- **스펙 커버리지**: §1 데이터(Task 1) · §2 상태 매트릭스 S0~S4(Task 5·11) · §2.1 온보딩(Task 5) · §3 스레드 라우트/게이트/캡/기억(Task 7·8·9) · §4.1 레지스트리(Task 12) · §4.2 4종(Task 14·15) · §4.3 프로필 삭제(Task 16) · §5 인앱 체크인(Task 9 파싱 + Task 17 주입/소진) · §6 탭/보관함 이동(Task 4) · §7 단계(2a~2d) · §9 성공기준(Task 19) 전부 대응.
- **타입 일관성**: `PassKind`/`PASS_PLAN_BY_KIND`(Task 2) → passes(Task 6)·라우트(Task 10)에서 동일 사용. `RelationshipMemo`(Task 2) → memory(Task 7)·chat(Task 9)·skill-log(Task 13)·checkin(Task 17) 동일. `splitThreadMessages`/`RECENT_MSGS`(Task 7) → chat(Task 9) 동일. `RELATIONSHIP_SKILLS`/`getSkill`(Task 12) → 디스패치(Task 14·15) 동일. `logSkillToThread(relationshipId, skillKey, readingId, summary)`(Task 13) → Task 14·15 동일 시그니처.
- **의존 순서 주의**: Task 3(관계 API)는 Task 6(passes 헬퍼) import에 의존 → 실행자는 Task 6을 먼저 하거나 함께 커밋(Task 3·6 Step에 명시). Task 9(chat)는 Task 7·8 필요. Task 17은 Task 9 확장.
- **하위호환**: 보관함 GET 스레드 제외(neq relationship) — 기존 사주/타로 reading 렌더 무영향. 타로 생성/chat 라우트는 relationshipId 없으면 기존 동작 그대로(옵셔널 태깅).
- **미결(플랜 내 명시)**: `readings_consultation_type_check` 실제 제약명(Task 1 dev 확인), compat 생성 라우트의 정확한 태깅 지점(Task 14 Step 3 파일 읽고 확정), verdict chat 소형 라우트 분리(Task 15 실행자 노트).
- **YAGNI**: 30일 패스·복수 관계·완전 롤링 요약·카카오 푸시·생일 스냅샷 비정규화는 백로그(스펙 §10) — 플랜 제외.
