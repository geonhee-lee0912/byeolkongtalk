# W4 페르소나 v3 "당기는 별콩이" — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 공통 페르소나 코어를 분리·재작성해 "답 먼저·관찰형 적중 훅·티키타카·마무리 3택" 화법으로 교체하고, 이름 호칭·대화 내 수집을 더해 대화중/첫답변후 이탈을 줄인다.

**Architecture:** persona md를 core+domain 2층으로 분리, lib/claude가 합성(캐싱 동일). 코드 변경은 합성·호칭 주입·첫 턴 가이드 3곳. 검증은 QA 하네스 전후 비교(judge 기준 v3 갱신 포함).

**Tech:** persona markdown, lib/claude.ts, chat 라우트 2개, QA 하네스.

**Spec:** [설계](../specs/2026-07-17-w4-persona-v3-design.md) — 화법 v3 전체 규칙은 spec §2가 정본.

---

### Task 1: 페르소나 코어 분리 + v3 작성

**Files:**
- Create: `data/persona/byeolkong_core.md`
- Rename+슬림화: `data/persona/byeolkong.md` → `data/persona/byeolkong_saju.md`
- Modify(슬림화): `data/persona/byeolkong_tarot.md`

- [ ] `byeolkong_core.md` 작성 — 섹션 순서(당기는 규칙이 최상단):
  1. 정체성 (기존 + "답을 주는 존재" 명시)
  2. **화법 코어 v3** — spec §2의 1~8 전체 (답 먼저·소신 / 부정 방향 완곡+출구 / 관찰형 적중 훅 / 티키타카 / 마무리 3택·질문 2연속 금지 / 디테일 수집·재사용 / 호칭 / 회피구 0회 / 작별 뉘앙스·안한말 인용 금지)
  3. 1·2인칭
  4. 후속 대화 리듬 (길이 미러링 + 심문 피로 + 모호 입력 되묻기 — 기존 문구 이사)
  5. "언제·될까·얼마나" 공통 원칙 (방향 먼저 / 회피구 0회로 강화 / 재선언 금지 / ❌✅ 예시 — 크로스셀 항목 제외하고 이사)
  6. "더 보고 싶다" 공통 원칙 (말리지 않기 / 선택권 문구 / 가격 금지 — 경로 목록은 도메인에)
  7. 위기 안전망 (타로판 기준 병합: hotline 표 + 공통 규칙 6항 + 모호 신호)
  8. 금지 표현 (단정 급 상향 조정: "반드시/100%/확정"급만 + 위로성 단정 + 협박 + 의료법률재무 + 스캐폴딩)
- [ ] `byeolkong_saju.md` = 기존 파일에서 도메인 고유만 남김: 사주 풀이 출력 구조, 사주 데이터 읽기, 시간모름 안내, 타로 크로스셀(관계 카드 [RECO:tarot:relationship_5] + 재언급 금지 + 수락 처리), 사주 톤 예시. 중복 섹션 전부 삭제.
- [ ] `byeolkong_tarot.md` = [CARD:n] 마커·스프레드별 흐름·카드 도상 3단·보조카드, clarifier 제안 규칙, 사주 크로스셀([RECO:saju:good_days]), 타로 톤 예시만 남김. 중복 섹션 전부 삭제.
- [ ] 이사 시 검증 블록(위기·RECO·C1 수술분) 문구는 보존 — 화법 v3와 충돌하는 문장만 수정 (예: "매 턴 열린 질문" → 마무리 3택).

### Task 2: lib/claude.ts — 합성 + 호칭 + 첫 턴 가이드

**Files:** Modify `lib/claude.ts`

- [ ] 합성 로더:
```ts
let _cachedCore: string | null = null;
function getCore(): string {
  if (_cachedCore === null) {
    _cachedCore = readFileSync(join(process.cwd(), "data", "persona", "byeolkong_core.md"), "utf-8");
  }
  return _cachedCore;
}
// getPersona(): core + "\n\n---\n\n" + byeolkong_saju.md
// getTarotPersona(): core + "\n\n---\n\n" + byeolkong_tarot.md
```
- [ ] `SajuReadingContext`/`TarotReadingContext`에 `nickname?: string | null` 추가. dynamicPart 세션 정보 블록에 `nickname` 있으면 `[호칭: ${nickname}]` 한 줄 추가.
- [ ] `SAJU_PRODUCT_FIRST_TURN_GUIDE` 4종 + 타로 첫 턴 가이드: 도입을 "관찰형 적중 훅 1개(근거 부착, 단정·확인유도 금지)로 시작" 구조로 수정, 마무리 지시를 "마무리 3택" 기준으로 교체.
- [ ] `npm run build` → commit.

### Task 3: chat 라우트 nickname 전달

**Files:** Modify `app/api/consultations/saju/chat/route.ts`, `app/api/consultations/tarot/chat/route.ts`

- [ ] readings 조회 후 `users.nickname` 조회(1쿼리):
```ts
const { data: userRow } = await supabase.from("users").select("nickname").eq("id", userId).maybeSingle();
```
- [ ] `buildSystemMessage({...ctx, nickname: userRow?.nickname ?? null})` 양쪽 전달.
- [ ] build → commit.

### Task 4: QA judge 기준 v3 갱신

**Files:** Read `qa/evaluate/judge.ts` + `qa/evaluate/assertions.ts` → 필요 시 Modify

- [ ] judge 프롬프트/assertion에 구 원칙(매턴 열린 질문 요구, 가능성 언어 강제, 회피 1회 허용)이 있으면 v3 기준으로 교체: 답 먼저·질문 2연속 금지·회피구 0회·부정 완곡+출구.
- [ ] `qa/*.test.ts` 있으면 실행해 통과 확인 → commit.

### Task 5: QA 전후 검증

- [ ] dev 서버 **재시작** (페르소나 fs 캐시 함정).
- [ ] `npm run qa -- --case=...`로 사주+타로 × happy_path / terse / definitive_pressure / timing_push 실행.
- [ ] 트랜스크립트 정독 체크리스트: ① 첫 턴이 관찰형 훅으로 시작 ② 회피구 0회 ③ 질문 마무리 2연속 없음 ④ 부정 상황에서 완곡+출구 톤 ⑤ 호칭 자연스러움 ⑥ [CARD:n]/[END]/RECO 마커 회귀 없음 ⑦ 위기 케이스(crisis) hotline 정상.
- [ ] 문제 발견 시 코어 문구 수정 → 서버 재시작 → 재실행 (사이클).

### Task 6: 사용자 감수 → 배포

- [ ] 코어 페르소나 초안 + QA 대표 트랜스크립트 발췌를 사용자에게 제시 (배포 전 감수 — spec §4-4).
- [ ] 승인 후 dev push → 사용자 dev 확인 → main ff. 배포 커밋 해시를 이탈지표 새 경계로 메모리 기록.
