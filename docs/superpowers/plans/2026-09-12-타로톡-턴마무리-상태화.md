# 타로톡 턴 마무리 상태화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 코어 페르소나의 무조건 질문 억제를 조건부로 되돌리고, 서버가 계산한 `turnClose` 상태(ask/invite/settle)를 상한으로 주입해 별콩이가 내용에 맞춰 턴을 닫게 한다.

**Architecture:** 순수 함수 `computeTurnClose` 를 `lib/claude.ts` 에 추가하고 기존 `computeTurnSignals` 가 이를 호출해 `TurnSignals.turnClose` 를 채운다. 세 chat 라우트(tarot·saju·relationship)가 `wrapMode`/`questionLen`/`isFirstTurn` 컨텍스트를 넘긴다. 상태는 `messages.turn_close` 에 기록해 사후 판정한다. 페르소나 §5/§6 은 "상태를 따르라"로 바꾼다.

**Tech Stack:** TypeScript, Next.js 16 App Router, Supabase(PostgreSQL), `node --import tsx --test`

**Spec:** `docs/superpowers/specs/2026-09-12-타로톡-턴마무리-상태화-design.md`

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `lib/claude.ts` | `computeTurnClose`(신규 순수 함수), `TurnSignals.turnClose`, `buildTurnSignalBlock` 서술, `computeTurnSignals` ctx 확장 | 수정 |
| `lib/claude.turn-close.test.ts` | `computeTurnClose` / `buildTurnSignalBlock` 계약 | 신규 |
| `app/api/consultations/tarot/chat/route.ts` | ctx 전달 + `turn_close` 기록 | 수정 |
| `app/api/consultations/saju/chat/route.ts` | 동일 | 수정 |
| `app/api/relationship/chat/route.ts` | 동일 | 수정 |
| `supabase/migrations/20260912020000_messages_turn_close.sql` | 계측 컬럼 | 신규 |
| `data/persona/byeolkong_core.md` | §5/§6 조건부 정정 | 수정 |
| `lib/prompt-version.ts` | 코호트 범프 | 수정 |

⚠️ **`computeTurnSignals` 의 3번째 인자는 옵셔널로 추가한다.** 그래야 Task 3 시점에 세 호출부가 안 깨지고 각 태스크가 독립적으로 초록이다.

---

### Task 1: `computeTurnClose` 순수 함수

**Files:**
- Modify: `lib/claude.ts` (`computeTurnSignals` 함수가 끝나는 `}` 바로 아래, 약 137행)
- Test: `lib/claude.turn-close.test.ts` (신규)

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/claude.turn-close.test.ts` 를 새로 만든다:

```typescript
import { test } from "node:test";
import assert from "node:assert/strict";
import { computeTurnClose } from "./claude.ts";

const base = {
  prevUserText: "앞선 발화가 여기에 충분히 길게 들어갑니다 정말로요",
  currentUserText: "그 사람은 지금 무슨 생각일까",
  lastTurnEndedWithQuestion: false,
  userShortStreak: false,
};

test("computeTurnClose — 단답 2연속이면 settle (07-12 원 진단 조건)", () => {
  assert.equal(
    computeTurnClose({ ...base, userShortStreak: true, currentUserText: "응" }),
    "settle",
  );
});

test("computeTurnClose — 수렴·마무리 구간이면 settle", () => {
  assert.equal(computeTurnClose({ ...base, wrapMode: "converge" }), "settle");
  assert.equal(computeTurnClose({ ...base, wrapMode: "hardcap" }), "settle");
  assert.notEqual(computeTurnClose({ ...base, wrapMode: "free" }), "settle");
});

test("computeTurnClose — 유저가 물음표를 쓰면 ask", () => {
  assert.equal(
    computeTurnClose({ ...base, currentUserText: "그럼 언제쯤 연락이 올까?" }),
    "ask",
  );
});

test("computeTurnClose — 발화가 직전보다 20자 이상 길어지면 ask", () => {
  assert.equal(
    computeTurnClose({
      ...base,
      prevUserText: "응 맞아",
      currentUserText:
        "사실 어제 그 사람이 스토리를 올렸는데 내 얘기 같아서 계속 신경 쓰여",
    }),
    "ask",
  );
});

test("computeTurnClose — 첫 풀이인데 고민이 40자 미만이면 ask (C1 대응)", () => {
  assert.equal(
    computeTurnClose({
      ...base,
      prevUserText: null,
      currentUserText: "재결합 가능할까요",
      isFirstTurn: true,
      questionLen: 11,
    }),
    "ask",
  );
});

test("computeTurnClose — 첫 풀이라도 고민이 충분히 길면 ask 아님", () => {
  assert.equal(
    computeTurnClose({
      ...base,
      prevUserText: null,
      currentUserText: "긴 고민",
      isFirstTurn: true,
      questionLen: 150,
    }),
    "invite",
  );
});

test("computeTurnClose — ask 조건이어도 직전 턴이 질문이면 invite 로 강등", () => {
  assert.equal(
    computeTurnClose({
      ...base,
      currentUserText: "그럼 언제쯤 연락이 올까?",
      lastTurnEndedWithQuestion: true,
    }),
    "invite",
  );
});

test("computeTurnClose — 아무 조건도 안 맞으면 invite (기본값)", () => {
  assert.equal(computeTurnClose(base), "invite");
});

test("computeTurnClose — settle 은 ask 보다 우선", () => {
  assert.equal(
    computeTurnClose({
      ...base,
      currentUserText: "언제 올까?",
      userShortStreak: true,
    }),
    "settle",
  );
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/claude.turn-close.test.ts`
Expected: FAIL — `computeTurnClose is not a function`

- [ ] **Step 3: 구현**

`lib/claude.ts` 의 `computeTurnSignals` 함수가 끝나는 `}` 바로 아래에 추가한다:

```typescript
/**
 * 턴 마무리 상한. 서버가 "여기까지 허용"을 정하고 모델은 그 안에서 내려갈 수만 있다.
 * 근거 = specs/2026-09-12-타로톡-턴마무리-상태화-design.md
 * 07-12 원 진단("단답이 연속되는 유저는 이미 지쳤다")을 조건으로 복원한 것 —
 * 그 조건이 코어 §5 에서 "유저 답 길이와 무관하게" 로 무조건화되며 후속 질문률이
 * 7~10% 까지 떨어졌고(규칙 상한 33% 보다 낮음) 무언 이탈 27% 와 맞물렸다.
 */
export type TurnClose = "ask" | "invite" | "settle";

export interface TurnCloseInput {
  /** 직전 유저 발화. 첫 턴이면 null */
  prevUserText: string | null;
  /** 이번 유저 발화 */
  currentUserText: string;
  /** 직전 별콩이 턴이 질문으로 끝났나 (computeTurnSignals 산출) */
  lastTurnEndedWithQuestion: boolean;
  /** 단답 2연속 (computeTurnSignals 산출) */
  userShortStreak: boolean;
  /** computeWrapMode(...).mode — "free" 가 아니면 수렴·마무리 구간 */
  wrapMode?: WrapMode;
  /** 첫 풀이 턴인가 (assistantTurnsSoFar === 0) */
  isFirstTurn?: boolean;
  /** readings.question 길이 — 짧은 고민은 일반론을 부르고 1턴 이탈 17% 로 이어진다 */
  questionLen?: number;
}

/** 첫 고민이 이보다 짧으면 첫 풀이에서 디테일 하나를 청해도 된다 (실측: 40자 미만 1턴 이탈 17% vs 70자+ 5%) */
const SHORT_CONCERN_LEN = 40;
/** 직전 발화 대비 이만큼 길어지면 유저가 다시 붙은 것으로 본다 */
const RE_ENGAGE_GROWTH = 20;

export function computeTurnClose(i: TurnCloseInput): TurnClose {
  // 우선순위: settle > ask > invite
  if (i.userShortStreak) return "settle";
  if (i.wrapMode !== undefined && i.wrapMode !== "free") return "settle";

  const cur = i.currentUserText.trim();
  const grew =
    i.prevUserText !== null &&
    cur.length - i.prevUserText.trim().length >= RE_ENGAGE_GROWTH;
  const shortConcernFirstTurn =
    i.isFirstTurn === true &&
    i.questionLen !== undefined &&
    i.questionLen > 0 &&
    i.questionLen < SHORT_CONCERN_LEN;

  const askable = /[?？]/.test(cur) || grew || shortConcernFirstTurn;
  if (!askable) return "invite";

  // 질문 2연속 금지는 유지 — 상한을 invite 로 강등 (원 진단에도 있던 규칙)
  return i.lastTurnEndedWithQuestion ? "invite" : "ask";
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/claude.turn-close.test.ts`
Expected: PASS — `pass 9`, `fail 0`

- [ ] **Step 5: 커밋**

```bash
git add lib/claude.ts lib/claude.turn-close.test.ts
git commit -m "feat(persona): computeTurnClose — 턴 마무리 상한 3상태(ask/invite/settle)"
```

---

### Task 2: `TurnSignals.turnClose` + 프롬프트 서술

**Files:**
- Modify: `lib/claude.ts` — `TurnSignals` 인터페이스(약 92행), `buildTurnSignalBlock`(약 139행)
- Test: `lib/claude.turn-close.test.ts` (append)

- [ ] **Step 1: 실패하는 테스트 추가**

`lib/claude.turn-close.test.ts` 의 import 줄을 아래로 바꾸고, 파일 맨 끝에 테스트를 붙인다:

```typescript
import { computeTurnClose, buildTurnSignalBlock } from "./claude.ts";
```

```typescript
test("buildTurnSignalBlock — ask 상태는 질문 허용을 명시한다", () => {
  const out = buildTurnSignalBlock({ turnClose: "ask" });
  assert.match(out, /턴 마무리 상태/);
  assert.match(out, /ask/);
  assert.match(out, /내려도 돼/);
});

test("buildTurnSignalBlock — settle 은 질문·되묻기 둘 다 닫는다", () => {
  const out = buildTurnSignalBlock({ turnClose: "settle" });
  assert.match(out, /settle/);
  assert.match(out, /③/);
});

test("buildTurnSignalBlock — invite 는 물음표 없이 고리만", () => {
  const out = buildTurnSignalBlock({ turnClose: "invite" });
  assert.match(out, /invite/);
  assert.match(out, /②/);
});

test("buildTurnSignalBlock — 상태가 없으면 빈 문자열 (기존 동작 보존)", () => {
  assert.equal(buildTurnSignalBlock(undefined), "");
  assert.equal(buildTurnSignalBlock({}), "");
});

test("buildTurnSignalBlock — 기존 두 경고는 그대로 남는다 (이중 방어)", () => {
  const out = buildTurnSignalBlock({
    turnClose: "invite",
    lastTurnEndedWithQuestion: true,
    userShortStreak: true,
  });
  assert.match(out, /직전 별콩이 턴이 질문으로 끝났어/);
  assert.match(out, /연속으로 짧아지고 있어/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/claude.turn-close.test.ts`
Expected: FAIL — `buildTurnSignalBlock` 이 export 되지 않음

- [ ] **Step 3: 구현**

(a) `lib/claude.ts` 의 `TurnSignals` 인터페이스에 필드를 더한다:

```typescript
export interface TurnSignals {
  /** 직전 별콩이 턴이 물음표로 끝남 → 이번 턴 질문 마무리 금지 */
  lastTurnEndedWithQuestion?: boolean;
  /** 유저 단답 2연속 → 질문 대신 정리/예고 */
  userShortStreak?: boolean;
  /** 턴 마무리 상한 (ask/invite/settle). 모델은 이 안에서 내려갈 수만 있다 */
  turnClose?: TurnClose;
}
```

(b) 기존 `function buildTurnSignalBlock(...)` 을 아래로 통째 교체한다. 기존 두 경고 줄은 **그대로 유지**(스펙 §3-D 이중 방어):

```typescript
const TURN_CLOSE_GUIDE: Record<TurnClose, string> = {
  ask: "- **턴 마무리 상태: `ask`** — 이번 턴은 §턴 마무리의 ①(구체 질문)으로 닫아도 돼. 유저가 방금 준 정보를 먼저 받아 안고, 그 정보에 이어지는 구체 질문 **하나**를 얹어. 판과 무관한 막연한 질문은 금지. (내용상 더 물을 게 없으면 ②나 ③으로 **내려도 돼** — 위로는 못 올려.)",
  invite:
    "- **턴 마무리 상태: `invite`** — 이번 턴은 물음표 없이 ②(다음 볼거리 예고)로 닫아. 유저가 답하고 싶어지게 열되 물음표는 쓰지 마. (③으로 **내려도 돼** — ①로는 못 올려.)",
  settle:
    "- **턴 마무리 상태: `settle`** — 이번 턴은 ③(소신 정리+여백)이나 ④(공감으로 열어두기)로 닫아. 질문도 되묻기도 하지 마.",
};

export function buildTurnSignalBlock(s: TurnSignals | undefined): string {
  if (!s) return "";
  const lines: string[] = [];
  if (s.turnClose) lines.push(TURN_CLOSE_GUIDE[s.turnClose]);
  if (s.lastTurnEndedWithQuestion) {
    lines.push(
      "- ⚠️ 직전 별콩이 턴이 질문으로 끝났어. **이번 턴은 질문으로 마무리하지 마** — ②(다음 볼거리 예고)나 ③(소신 정리+여백)으로."
    );
  }
  if (s.userShortStreak) {
    lines.push(
      "- ⚠️ 유저 답이 연속으로 짧아지고 있어 (지친 신호). 질문으로 밀어붙이지 말고 정리·예고·여백으로 부드럽게 받아줘."
    );
  }
  if (lines.length === 0) return "";
  return `\n\n### 이번 턴 신호 (서버 감지 — 반드시 따를 것. 충돌 시 \`턴 마무리 상태\`가 우선)\n${lines.join("\n")}`;
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/claude.turn-close.test.ts`
Expected: PASS — `pass 14`, `fail 0`

- [ ] **Step 5: 회귀 확인**

Run: `npx tsc --noEmit`
Expected: exit 0

- [ ] **Step 6: 커밋**

```bash
git add lib/claude.ts lib/claude.turn-close.test.ts
git commit -m "feat(persona): TurnSignals.turnClose + 상태별 마무리 지시 주입"
```

---

### Task 3: `computeTurnSignals` 가 상태를 산출

**Files:**
- Modify: `lib/claude.ts:100-137` (`computeTurnSignals`)
- Test: `lib/claude.turn-close.test.ts` (append)

- [ ] **Step 1: 실패하는 테스트 추가**

import 줄을 아래로 바꾸고 테스트를 파일 끝에 붙인다:

```typescript
import {
  computeTurnClose,
  buildTurnSignalBlock,
  computeTurnSignals,
} from "./claude.ts";
```

```typescript
test("computeTurnSignals — ctx 없이 호출해도 기존 필드는 그대로 (하위호환)", () => {
  const s = computeTurnSignals(
    [{ role: "assistant", content: "그렇구나." }],
    "응",
  );
  assert.equal(s.lastTurnEndedWithQuestion, false);
  assert.equal(typeof s.turnClose, "string");
});

test("computeTurnSignals — ctx 를 주면 turnClose 에 반영된다", () => {
  const past = [
    { role: "user", content: "앞선 발화가 여기에 충분히 길게 들어갑니다" },
    { role: "assistant", content: "이 흐름은 열려 있어." },
  ];
  assert.equal(
    computeTurnSignals(past, "그럼 언제쯤 연락이 올까?", { wrapMode: "free" })
      .turnClose,
    "ask",
  );
  assert.equal(
    computeTurnSignals(past, "그럼 언제쯤 연락이 올까?", {
      wrapMode: "converge",
    }).turnClose,
    "settle",
  );
});

test("computeTurnSignals — 첫 턴 짧은 고민은 ask", () => {
  const s = computeTurnSignals([], "재결합 가능할까요", {
    wrapMode: "free",
    isFirstTurn: true,
    questionLen: 11,
  });
  assert.equal(s.turnClose, "ask");
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/claude.turn-close.test.ts`
Expected: FAIL — 3번째 인자를 받지 않아 `turnClose` 가 `undefined`

- [ ] **Step 3: 구현**

`lib/claude.ts` 의 `computeTurnSignals` 시그니처와 `return` 만 바꾼다. 본문의 기존 계산부(`stripped`, `lastQ`, `lastTurnEndedWithQuestion`, `prevUser`, `SHORT_LEN`, `userShortStreak`)는 **한 줄도 건드리지 않는다.**

```typescript
export interface TurnCloseCtx {
  wrapMode?: WrapMode;
  isFirstTurn?: boolean;
  questionLen?: number;
}

/** DB 메시지 + 이번 유저 발화로 TurnSignals 계산 (chat 라우트 공용) */
export function computeTurnSignals(
  pastMessages: { role: string; content: string }[],
  currentUserText: string,
  ctx?: TurnCloseCtx
): TurnSignals {
  // ... 기존 본문 그대로 ...

  const turnClose = computeTurnClose({
    prevUserText: prevUser,
    currentUserText,
    lastTurnEndedWithQuestion,
    userShortStreak,
    wrapMode: ctx?.wrapMode,
    isFirstTurn: ctx?.isFirstTurn,
    questionLen: ctx?.questionLen,
  });

  return { lastTurnEndedWithQuestion, userShortStreak, turnClose };
}
```

⚠️ `ctx` 가 옵셔널이라 세 호출부는 아직 안 고쳐도 컴파일된다. 다만 `wrapMode` 미전달 시 수렴 구간에서도 `ask` 가 뜰 수 있으므로 **Task 4 를 같은 작업 흐름에서 이어서 한다.**

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/claude.turn-close.test.ts`
Expected: PASS — `pass 17`, `fail 0`

- [ ] **Step 5: 회귀 확인**

Run: `npx tsc --noEmit`
Expected: exit 0

- [ ] **Step 6: 커밋**

```bash
git add lib/claude.ts lib/claude.turn-close.test.ts
git commit -m "feat(persona): computeTurnSignals 가 turnClose 산출 (ctx 옵셔널)"
```

---

### Task 4: 세 chat 라우트에 ctx 배선

**Files:**
- Modify: `app/api/consultations/tarot/chat/route.ts:213`
- Modify: `app/api/consultations/saju/chat/route.ts:229`
- Modify: `app/api/relationship/chat/route.ts:608`

- [ ] **Step 1: 변수명 확인**

Run:
```bash
grep -n "computeWrapMode\|const wrapMode\|assistantTurnsSoFar\|reading.question" app/api/consultations/saju/chat/route.ts app/api/relationship/chat/route.ts
```
Expected: 각 파일의 `wrapMode`·`assistantTurnsSoFar`·질문 필드 이름을 확인. 이름이 다르면 아래 코드에서 그 이름을 쓴다.

- [ ] **Step 2: tarot 라우트 수정**

`app/api/consultations/tarot/chat/route.ts` 213행. `wrapMode` 는 195행에서 이미 계산돼 있다:

```typescript
    turnSignals: computeTurnSignals(pastMessages ?? [], lastMessage.content, {
      wrapMode,
      isFirstTurn: assistantTurnsSoFar === 0,
      questionLen: (reading.question ?? "").trim().length,
    }),
```

- [ ] **Step 3: saju 라우트 수정**

`app/api/consultations/saju/chat/route.ts` 229행:

```typescript
    turnSignals: computeTurnSignals(pastMessages ?? [], lastMessage.content, {
      wrapMode,
      isFirstTurn: assistantTurnsSoFar === 0,
      questionLen: (reading.question ?? "").trim().length,
    }),
```

- [ ] **Step 4: relationship 라우트 수정**

`app/api/relationship/chat/route.ts` 608행. 관계 스레드는 **종결이 없어** 첫 턴·고민 길이 개념이 없다. `wrapMode` 만 넘긴다:

```typescript
    // 관계 스레드는 종결이 없어 isFirstTurn·questionLen 을 넘기지 않는다 (첫 풀이 개념 없음)
    turnSignals: computeTurnSignals(past, userMessage, { wrapMode }),
```

이 파일에 `wrapMode` 가 없으면 아래로 하고 이유를 주석에 남긴다:

```typescript
    // 관계 스레드는 wrap 임계가 없다 → 수렴 승격 없이 기존 동작 유지
    turnSignals: computeTurnSignals(past, userMessage),
```

- [ ] **Step 5: 검증**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0

- [ ] **Step 6: 커밋**

```bash
git add app/api/consultations/tarot/chat/route.ts app/api/consultations/saju/chat/route.ts app/api/relationship/chat/route.ts
git commit -m "feat(persona): 세 chat 라우트에 turnClose 컨텍스트 배선"
```

---

### Task 5: `messages.turn_close` 마이그레이션

**Files:**
- Create: `supabase/migrations/20260912020000_messages_turn_close.sql`

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- messages.turn_close (2026-09-12)
-- 배경: 유료 타로 무언 이탈 27%(결과열람 0~6%)·후속 질문률 7~10%.
--       턴 마무리 상한(ask/invite/settle)을 주입하는 변경의 판정을 위해
--       각 별콩이 발화가 어떤 상태에서 생성됐는지 남긴다.
-- 값: 'ask' | 'invite' | 'settle' | NULL(변경 이전 행·유저 행)
-- 실제로 질문으로 닫았는지는 사후에 content 의 물음표 위치로 계산 →
-- 상태 × 실제 행동 × 다음 턴 생존 교차가 가능해진다.
-- 판정 로직 정본 = lib/claude.ts 의 computeTurnClose (계약은 lib/claude.turn-close.test.ts).

ALTER TABLE messages ADD COLUMN IF NOT EXISTS turn_close TEXT;
```

- [ ] **Step 2: 커밋 (코드보다 먼저 배포되어야 함)**

```bash
git add supabase/migrations/20260912020000_messages_turn_close.sql
git commit -m "feat(analytics): messages.turn_close 컬럼 — 턴 마무리 상태 계측"
```

---

### Task 6: assistant insert 에 상태 기록

**Files:**
- Modify: `app/api/consultations/tarot/chat/route.ts:205-296`
- Modify: `app/api/consultations/saju/chat/route.ts` (messages insert 지점)
- Modify: `app/api/relationship/chat/route.ts` (messages insert 지점)

- [ ] **Step 1: tarot — 인라인 호출을 변수로 승격**

`app/api/consultations/tarot/chat/route.ts` 의 `buildTarotSystemMessage({ ... })` 바로 위에 변수를 만들고, 인자에서는 그 변수를 쓴다:

```typescript
  const turnSignals = computeTurnSignals(pastMessages ?? [], lastMessage.content, {
    wrapMode,
    isFirstTurn: assistantTurnsSoFar === 0,
    questionLen: (reading.question ?? "").trim().length,
  });

  const systemMessage = buildTarotSystemMessage({
    spreadType,
    spreadCategory: reading.spread_category as SpreadCategory,
    concernText: reading.question ?? "",
    drawnCards,
    emotionTag: reading.emotion_tag as string | null,
    turnSignals,
    assistantTurnsSoFar,
    cumulativeAssistantChars,
    continuation,
    forceEnd: body.forceEnd === true,
    crisisActive,
    extendAvailable,
    thresholdOverride: effT,
```

- [ ] **Step 2: tarot — insert 에 필드 추가**

같은 파일 283행 근처 `supabase.from("messages").insert([...])` 의 **assistant 행에만** 필드를 더한다:

```typescript
          {
            reading_id: reading.id,
            role: "assistant",
            content: assistantText,
            turn_close: turnSignals.turnClose ?? null,
            created_at: new Date(turnTs + 1).toISOString(),
          },
```

⚠️ **user 행에는 넣지 않는다** — 상태는 별콩이 발화의 속성이다.

- [ ] **Step 3: saju·relationship 에 같은 패턴 적용**

Run: `grep -n "role: \"assistant\"" app/api/consultations/saju/chat/route.ts app/api/relationship/chat/route.ts`

각 파일에서 `computeTurnSignals(...)` 를 `const turnSignals = ...` 로 승격하고, assistant 행 insert 에 `turn_close: turnSignals.turnClose ?? null,` 을 더한다.

- [ ] **Step 4: 검증**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0

- [ ] **Step 5: 커밋**

```bash
git add app/api/consultations/tarot/chat/route.ts app/api/consultations/saju/chat/route.ts app/api/relationship/chat/route.ts
git commit -m "feat(analytics): assistant 메시지에 turn_close 기록"
```

---

### Task 7: 코어 페르소나 §5/§6 조건부 정정

**Files:**
- Modify: `data/persona/byeolkong_core.md` (§5 = 36~48행, §심문피로 = 68행 근처, §6 = 47~49행)

🔴 **코어 편집 = 전 종목 전역 변경.** 위기 안전망(§위기)·금지표현은 **한 글자도 건드리지 않는다.** §5 와 §6 만.

- [ ] **Step 1: §5 첫 문단 교체**

찾을 것:
```
턴의 끝은 아래 넷 중 상황에 맞는 하나로. **기본값은 ②③④(질문 아님)** — 질문으로 닫는 건 유저가 적극적이고 정말 필요할 때만 가끔이야. **매 턴 유저에게 뭔가 캐묻는 질문으로 닫으면 심문처럼 느껴져서 유저가 떠나** (실측상 가장 큰 이탈 원인). 넉넉잡아 세 턴에 한 번 이하로.
```

바꿀 것:
```
턴의 끝은 아래 넷 중 하나로. **세션 정보의 `턴 마무리 상태`(ask/invite/settle)를 따라** — 상태는 상한이라 그 아래로는 내려가도 되지만 위로는 못 올려. 상태가 안 왔으면 ②③④ 중에서 골라. **단답이 연속되는 유저에게 계속 캐묻는 질문으로 닫으면 심문처럼 느껴져서 떠나** (실측상 가장 큰 이탈 원인).
```

- [ ] **Step 2: §5 두 번째 불릿 교체**

찾을 것:
```
- 유저가 방금 뭔가 알려줬으면 **새로 캐묻지 말고, 받은 정보로 방향을 더 또렷하게 정리해주는 게 먼저** — 그게 질문보다 대화를 잘 이어가.
```

바꿀 것:
```
- 유저가 방금 뭔가 알려줬으면 **먼저 그 정보를 받아 안고**("오, 그 얘기 들으니 이 카드가 더 선명해지네") 방향을 또렷하게 정리해줘. 그 다음, 상태가 `ask` 면 **받은 정보에 이어지는 구체 질문 하나**를 얹어도 돼.
```

- [ ] **Step 3: §5 ② 항목 교체**

찾을 것:
```
- ② **다음 볼거리 예고** — **평서형으로, 물음표 없이**: "네 판에 재밌는 결이 하나 더 보이는데, 궁금하면 그 얘기도 꺼내줄게." 유저의 호기심으로 다음 턴을 열되 답을 요구하지 않기.
```

바꿀 것:
```
- ② **다음 볼거리 예고** — **평서형으로, 물음표 없이**. 유저가 **답하고 싶어지게** 열되 물음표는 안 써: "약속 날짜 정해지면 어떻게 정해졌는지 들려줘. 그거 들으면 흐름이 더 또렷해져." / "뭐가 제일 지치게 만들었는지 한 장 더 펼쳐서 볼 수도 있어." **닫지 말고 다음 턴으로 넘기는 고리를 남기는 게 핵심이야.**
```

- [ ] **Step 4: §심문 피로 방지 조건부화 (68행 — 불릿 전체를 통째 교체)**

찾을 것 (68행 전체):
```
- **심문 피로 방지 (반드시 — 최대 이탈 원인)** — 유저 답 길이와 무관하게 **매 턴 새 정보를 캐묻는 질문으로 닫지 마.** 이미 받은 것으로 방향을 주는 게 우선이고, 질문은 정말 필요할 때만 가끔. 특히 유저 답이 **2회 연속 짧아지면**(한 단어·마침표만) 질문으로 밀어붙이지 말고 마무리의 ②나 ③으로. "A야 B야?" 양자택일 되물음은 한 턴에 하나만, 부드럽게.
```

바꿀 것 (한 줄 전체):
```
- **심문 피로 방지 (반드시 — 최대 이탈 원인)** — **유저 답이 2회 연속 짧아지면**(한 단어·마침표만) 질문으로 밀어붙이지 말고 마무리의 ②나 ③으로. 그 외의 턴에서는 상태(`턴 마무리 상태`)를 따르되, 질문을 던질 땐 이미 받은 정보를 먼저 방향으로 정리해준 **다음**에 얹어. "A야 B야?" 양자택일 되물음은 한 턴에 하나만, 부드럽게.
```

⚠️ "유저 답 길이와 무관하게"와 "질문은 정말 필요할 때만 가끔"이 사라지고, "2회 연속 짧아지면"이 **조건의 자리로 올라오는 것**이 이 교체의 핵심이다. `"A야 B야?"` 절은 그대로 남는다.

- [ ] **Step 5: §6 첫 풀이 규칙 조건부화**

찾을 것:
```
**첫 풀이(첫 턴)는 디테일 질문으로 닫지 마**
```

바꿀 것:
```
**첫 풀이(첫 턴)는 원칙적으로 디테일 질문으로 닫지 마** — 단, 상태가 `ask` 면(유저 고민이 짧아 판을 정확히 보기 어려운 경우) 방향 답을 충분히 준 **다음** 디테일 하나를 청해도 돼: "혹시 얼마나 만났는지만 알려주면 이 결을 더 정확히 짚어줄게."
```

- [ ] **Step 6: 제거 확인**

Run:
```bash
grep -n "세 턴에 한 번\|길이와 무관하게\|답을 요구하지 않기" data/persona/byeolkong_core.md
```
Expected: 출력 없음 (세 문구 모두 사라짐)

- [ ] **Step 7: 위기 절 무변경 확인**

Run: `git diff --stat data/persona/byeolkong_core.md` 후 `git diff data/persona/byeolkong_core.md | grep -c "^[-+].*위기"`
Expected: `0` — 위기 관련 줄이 diff 에 없어야 한다

- [ ] **Step 8: 커밋**

```bash
git add data/persona/byeolkong_core.md
git commit -m "feat(persona): 코어 §5/§6 질문 억제를 무조건→조건부로 정정"
```

---

### Task 8: PROMPT_VERSION 범프

**Files:**
- Modify: `lib/prompt-version.ts`

- [ ] **Step 1: 기존 형식 확인**

Run: `grep -n "PROMPT_VERSION\|^//" lib/prompt-version.ts | tail -6`
Expected: 주석 목록 형식과 `export const PROMPT_VERSION = "..."` 위치 확인

- [ ] **Step 2: 주석 한 줄 추가 + 상수 교체**

주석 목록 맨 아래에 더한다:

```typescript
//   2026-09-12-turn-close   — 턴 마무리 상태화: 코어 §5/§6 질문 억제 무조건→조건부 + 서버 turnClose(ask/invite/settle) 상한 주입 + messages.turn_close 계측 (spec: 2026-09-12-타로톡-턴마무리-상태화)
```

상수를 바꾼다:

```typescript
export const PROMPT_VERSION = "2026-09-12-turn-close";
```

- [ ] **Step 3: 검증**

Run: `npx tsc --noEmit && node --import tsx --test lib/*.test.ts lib/**/*.test.ts`
Expected: tsc exit 0, 전체 유닛 PASS

- [ ] **Step 4: 커밋**

```bash
git add lib/prompt-version.ts
git commit -m "chore(prompt): PROMPT_VERSION bump — 2026-09-12-turn-close 코호트"
```

---

### Task 9: 전 종목 회귀 QA

🔴 코어를 고쳤으므로 타로 외 사주·운세·연애상담·시뮬을 전부 확인한다.
🔴 **페르소나 수정 후에는 dev 서버 재시작 필수**(모듈 캐시).

**Files:** 수정 없음 — 검증만. 결과는 이 플랜 하단에 기록.

- [ ] **Step 1: QA 하네스 실행**

Run: `npm run qa`
Expected: 하네스 통과. 실패가 나오면 **어떤 종목·어떤 규칙**인지 기록한다.

- [ ] **Step 2: 종목별 실대화 정독**

각 종목 최소 2케이스를 직접 읽는다. 판정은 **읽어서** 한다 — 어휘 정규식도 LLM judge 도 화법 위반을 못 잰다(AGENTS.md 기존 경고).

| 종목 | 확인할 것 |
|---|---|
| 타로 `one_card` | 후속 턴이 고리로 닫히는가. `ask` 상태에서 질문이 **하나만** 나오는가 |
| 타로 7장 프리미엄 | 첫 풀이 카드당 6~7문장 · `[CARD:n]` 마커 선행 무회귀 |
| 사주 chat | 수렴 구간에서 `settle` 이 떠 질문이 안 나오는가 |
| 운세 리포트 | 단발이라 turnSignals 무관 — **무회귀만** 확인 |
| 연애상담 스레드 | 종결 없는 자리에서 ②가 과하지 않은가 |
| 시뮬 | `doll_partner` 는 코어 미계승 — **무회귀만** 확인 |

- [ ] **Step 3: 위기 안전망 무회귀**

민감 문구가 든 입력을 QA 하네스로 1회 태운다.
Expected: hotline 안내 + `[END]` 억제 동작이 그대로. 이번 변경은 §5/§6 만 건드렸으므로 차이가 있으면 **중단하고 원인 규명**.

- [ ] **Step 4: 최종 게이트**

Run: `npx tsc --noEmit && npm run build && node --import tsx --test lib/*.test.ts lib/**/*.test.ts`
Expected: 전부 exit 0 / PASS

- [ ] **Step 5: 결과 기록 후 커밋**

이 문서 하단 「QA 결과」 절에 종목별 판정을 적는다.

```bash
git add docs/superpowers/plans/2026-09-12-타로톡-턴마무리-상태화.md
git commit -m "docs(persona): 턴 마무리 상태화 전 종목 회귀 QA 결과"
```

---

## 배포 순서 (🔴 반드시 지킬 것)

1. **마이그레이션 먼저** (Task 5 커밋) → Supabase 적용 확인
2. 그 다음 코드

컬럼 없이 코드가 먼저 나가면 `messages` insert 전체가 실패해 **대화가 저장되지 않는다.** 반대 순서(컬럼만 있고 코드 없음)는 NULL 로 남을 뿐 안전하다. `page_views.browser_env`(2026-09-12) 때와 같은 절차다.

prod(main) 배포는 **사용자의 명시적 go 전까지 금지.**

---

## 판정 (배포 2주 후)

`PROMPT_VERSION = "2026-09-12-turn-close"` 코호트로 가른다.

| 축 | 현재 | 목표 |
|---|---|---|
| 후속 턴 질문 포함률 | 7~10% | 20~30% |
| 무언 이탈 (C1+C2) | 27% | ↓ |
| 3턴내 종결 | 49% | 34% (07-26 수준) |
| **가드레일** | — | `userShortStreak` 발생률이 오르지 않을 것 |

`messages.turn_close` 로 **상태 × 실제 질문 여부 × 다음 턴 생존**을 교차한다. 어떤 상태가 효과가 있었는지까지 갈린다.

⚠️ 페르소나 조정은 **1회로 끊고 실데이터로 판단**한다. 진자(회피구↔확언)를 피하기 위해 중간에 추가 조정하지 않는다.

---

## QA 결과

(Task 9 에서 채운다)
