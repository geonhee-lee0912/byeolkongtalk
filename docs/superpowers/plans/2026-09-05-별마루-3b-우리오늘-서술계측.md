# 별마루 ③-b 우리 오늘 (LLM 서술 + 계측) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3-a(룰기반 우리 오늘) 위에 ①구독 전용 **별콩 LLM 서술**(둘 사이 오늘)을 얹고 ②우리 오늘 **계측 5종**을 배선한다.

**Architecture:** 서술은 ②-a `narrative` 라우트 패턴을 그대로 미러 — 새 `pair-narrative` 라우트가 self+상대 사주로 오늘 pair 셀·backdrop을 만들어 nano `generateOnce`로 생성(비자격 미호출=원가 0), `PairDayDetailCard`의 "너희 결" 아래 슬롯에 표시. 계측은 기존 `ui-events` allowlist + `trackUiEvent` 헬퍼에 5종을 더해 토글·모달·락티저 5지점에 배선. **어드민 우리 metrics는 3-c로 분리**(별도 리포팅 서브시스템·AGENTS.md 어드민 rigor).

**Tech Stack:** Next.js 16 · TypeScript strict · `lib/claude` `generateOnce`(gpt-5-nano) · `node:test`.

**Spec:** `docs/superpowers/specs/2026-09-05-별마루-3-우리오늘-design.md` §8(서술)·§9(계측). 3-a 플랜 = `2026-09-05-별마루-3a-우리오늘-룰기반.md`.

**3-a 확정 시그니처(재사용)**: `lib/byeolmaru/pair-day.ts` — `PairDayCell{date,ganji,score,tone,tags{spark,bond,friction,lead},isToday}`, `PairBackdrop{labelAtoB,labelBtoA,spark,bond,harmony}`, `buildPairCalendar(a,b,dailyLuck,todayKst)`, `pairBackdrop(a,b)`, `pairDayTone(score)`. `PAIR_TONE_LABEL` 는 현재 `components/byeolmaru/PairDayDetailCard.tsx` 에 있음(Task 2에서 lib로 이동).

---

## File Structure

- Modify `lib/byeolmaru/pair-day.ts` — `PAIR_TONE_LABEL` 를 이리로 옮겨 export(lib↔lib 재사용, 컴포넌트 import 제거).
- Modify `components/byeolmaru/PairDayDetailCard.tsx` — `PAIR_TONE_LABEL` 를 pair-day에서 import + `narrative` prop 추가·렌더.
- Modify `lib/byeolmaru/narrative-prompt.ts` — `buildPairNarrativeSystem` + `PAIR_NARRATIVE_KICKOFF` 추가(기존 `loadCore`/`formatPillars` 재사용).
- Create `lib/byeolmaru/narrative-prompt.test.ts` 확장 or 신규 — pair 프롬프트 형태 검증(기존 파일에 테스트가 있으면 추가).
- Create `app/api/byeolmaru/pair-narrative/route.ts` — GET `?subject` → 자격자만 nano 서술.
- Modify `components/byeolmaru/ByeolmaruView.tsx` — 상대 선택 시 pair-narrative 페치 → PairDayDetailCard 전달 + 계측(partner_selected·subscribe_from_woori) 배선.
- Modify `components/byeolmaru/WatchAddModal.tsx` — 계측(watch_add·watch_limit·watch_purchase) 배선.
- Modify `lib/analytics/ui-events.ts` — allowlist +5.

Verify: `node --import tsx --test <file>` · `npx tsc --noEmit` · `npm run build`(`ƒ Proxy` 유지). Commit: conventional + blank line + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. `@/` imports omit `.ts`; relative imports carry `.ts`.

---

## Task 1: 계측 allowlist +5

**Files:** Modify `lib/analytics/ui-events.ts`

- [ ] **Step 1: 5개 이벤트 추가**

`UI_EVENTS` 배열의 `byeolmaru_crosssell_clicked` 다음에 추가(마이그 불필요 — event 는 자유 문자열, allowlist 만 늘림):

```ts
  /** 별마루 우리오늘 — 토글에서 상대 선택. meta:{} */
  "byeolmaru_partner_selected",
  /** 별마루 우리오늘 — 상대 담기 성공. meta:{via:"pick"|"register"} */
  "byeolmaru_watch_add",
  /** 별마루 우리오늘 — 무료 슬롯 초과로 5별 확인창 도달. meta:{cost} */
  "byeolmaru_watch_limit",
  /** 별마루 우리오늘 — 추가 상대 5별 결제 완료. meta:{stars} */
  "byeolmaru_watch_purchase",
  /** 별마루 우리오늘 — 락 티저 CTA로 체험/구독 개시. meta:{action:"trial"|"subscribe"} */
  "byeolmaru_subscribe_from_woori",
```

- [ ] **Step 2: tsc + 커밋**

Run `npx tsc --noEmit` → 0. `git add lib/analytics/ui-events.ts` · msg `feat(byeolmaru): 우리오늘 계측 이벤트 5종 allowlist`.

---

## Task 2: `PAIR_TONE_LABEL` 를 lib로 이동

**Files:** Modify `lib/byeolmaru/pair-day.ts`, `components/byeolmaru/PairDayDetailCard.tsx`

lib(프롬프트 빌더)와 컴포넌트가 같은 라벨을 쓰므로 lib에 두고 양쪽이 import 한다(lib→component import 금지 규칙 회피).

- [ ] **Step 1: pair-day.ts 에 export 추가**

`pairDayTone` 아래에:
```ts
/** tone → 표시 라벨. dayGrade() 라벨과 동일 문구(무회귀). */
export const PAIR_TONE_LABEL: Record<PairTone, string> = {
  good: "잘 맞는 날",
  normal: "무난한 날",
  caution: "살짝 챙길 날",
};
```

- [ ] **Step 2: PairDayDetailCard 에서 정의 제거 → import**

`PairDayDetailCard.tsx` 상단의 `export const PAIR_TONE_LABEL = {...}` 정의를 지우고, import 에 추가:
```ts
import type { PairDayCell, PairBackdrop } from "@/lib/byeolmaru/pair-day";
import { PAIR_TONE_LABEL } from "@/lib/byeolmaru/pair-day";
```
⚠️ `ByeolmaruView.tsx` 가 `PAIR_TONE_LABEL` 를 `PairDayDetailCard` 에서 import 하고 있으면(3-a Task10에서 pair GridCell 매핑에 씀) 그 import 도 `@/lib/byeolmaru/pair-day` 로 바꾼다 — grep `PAIR_TONE_LABEL` 로 전 참조를 확인하고 전부 lib 출처로 통일.

- [ ] **Step 3: tsc + build + 커밋**

`npx tsc --noEmit` → 0 · `npm run build` → exit 0. `git add lib/byeolmaru/pair-day.ts components/byeolmaru/PairDayDetailCard.tsx components/byeolmaru/ByeolmaruView.tsx` · msg `refactor(byeolmaru): PAIR_TONE_LABEL 를 pair-day(lib)로 이동`.

---

## Task 3: pair 서술 프롬프트 빌더

**Files:** Modify `lib/byeolmaru/narrative-prompt.ts` (+ 테스트)

- [ ] **Step 1: 실패 테스트(형태 검증)**

`lib/byeolmaru/narrative-prompt.test.ts` 가 있으면 케이스 추가, 없으면 신규:
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { calcSaju } from "@/lib/saju/calc";
import { pairBackdrop, buildPairCalendar } from "./pair-day.ts";
import { buildPairNarrativeSystem, PAIR_NARRATIVE_KICKOFF } from "./narrative-prompt.ts";

test("buildPairNarrativeSystem: 두 사람·너희 결·오늘 신호를 담고 마커 규칙을 준다", () => {
  const a = calcSaju({ year: 1996, month: 4, day: 11, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
  const b = calcSaju({ year: 1994, month: 11, day: 3, hour: 21, gender: "male", isLunar: false, isLeapMonth: false });
  const bd = pairBackdrop(a, b);
  const cell = buildPairCalendar(a, b, [{ date: "2026-09-05", stem: "임", branch: "오", element: "수" }] as never, "2026-09-05")[0];
  const sys = buildPairNarrativeSystem(a, b, bd, cell, "임오", "지우");
  assert.ok(sys.includes("우리 오늘"));
  assert.ok(sys.includes("지우"));
  assert.ok(sys.includes(bd.labelAtoB));
  assert.ok(/반말/.test(sys) && /단정/.test(sys));
  assert.equal(PAIR_NARRATIVE_KICKOFF.length > 0, true);
});
```
⚠️ `DailyLuck` 실제 타입 필드를 `lib/saju/calc.ts`에서 확인해 `{date,stem,branch,element}` 캐스트를 맞출 것(3-a `buildPairCalendar` 시그니처와 정합). Run → FAIL(빌더 없음).

- [ ] **Step 2: `narrative-prompt.ts` 에 추가**

기존 private `loadCore()`/`formatPillars()` 를 그대로 재사용. import 추가: `import type { PairDayCell, PairBackdrop } from "./pair-day.ts"; import { PAIR_TONE_LABEL } from "./pair-day.ts";`

```ts
export function buildPairNarrativeSystem(
  self: SajuResult, partner: SajuResult, backdrop: PairBackdrop,
  cell: PairDayCell, todayGanji: string, partnerName: string
): string {
  const sig: string[] = [];
  if (cell.tags.spark) sig.push("끌림↑");
  if (cell.tags.bond) sig.push("결속");
  if (cell.tags.friction) sig.push("삐걱");
  if (cell.tags.lead === "me") sig.push("내가 리드");
  else if (cell.tags.lead === "partner") sig.push(`${partnerName}가 리드`);
  return [
    loadCore(),
    "",
    "# 별마루 우리 오늘",
    `너는 '${cell.date}' 하루, 이 사람과 상대('${partnerName}') 사이의 흐름을 사주로 풀어준다. 오늘 일진은 ${todayGanji}.`,
    `나: ${formatPillars(self)}.`,
    `${partnerName}: ${formatPillars(partner)}.`,
    `너희 결(고정): ${backdrop.labelAtoB} ↔ ${backdrop.labelBtoA}${backdrop.spark ? " · 끌림 있음" : ""}${backdrop.bond ? " · 결속 있음" : ""} · 연월조화 ${backdrop.harmony}.`,
    `오늘 둘 사이 결 ${PAIR_TONE_LABEL[cell.tone]}${sig.length ? ` · 신호 ${sig.join("·")}` : ""}.`,
    "규칙: 3~4문단, 반말, 단정적 예언 금지(흐름·가능성·선택). 첫 문장은 오늘 일진이 둘 사이를 어떻게 건드리는지로 시작. 한쪽을 탓하지 말고 둘의 흐름으로 말할 것. 마지막은 따뜻한 한 줄. 별표/제목/마커 없이 줄글만.",
  ].join("\n");
}

export const PAIR_NARRATIVE_KICKOFF = "오늘 우리 사이 흐름 풀어줘.";
```

- [ ] **Step 3: 통과 + 커밋**

Run test → PASS. `npx tsc --noEmit` → 0. `git add lib/byeolmaru/narrative-prompt.ts lib/byeolmaru/narrative-prompt.test.ts` · msg `feat(byeolmaru): 우리오늘 서술 프롬프트(buildPairNarrativeSystem)`.

---

## Task 4: pair 서술 라우트

**Files:** Create `app/api/byeolmaru/pair-narrative/route.ts`

②-a `app/api/byeolmaru/narrative/route.ts` 를 미러 — 단 self+상대 두 프로필을 읽고 pair 셀·backdrop 으로 시스템을 만든다. 캐시 없음(②-a와 동일, nano 재생성).

- [ ] **Step 1: 구현**

```ts
// 별마루 우리 오늘 서술 — 자격자만 nano 생성(비자격 미호출=원가0). ②-a narrative 미러.
import { NextRequest, NextResponse } from "next/server";
import { getSession } from "@/lib/session";
import { getServiceSupabase } from "@/lib/supabase";
import { getEntitlement } from "@/lib/byeolmaru/entitlement";
import { calcSaju, calcTemporalLuck, baseDateForKst } from "@/lib/saju/calc";
import { profileRowToSajuInput } from "@/lib/saju/profile-input";
import { buildPairCalendar, pairBackdrop } from "@/lib/byeolmaru/pair-day";
import { kstDate } from "@/lib/admin-time";
import { buildPairNarrativeSystem, PAIR_NARRATIVE_KICKOFF, BYEOLMARU_NARRATIVE_MODEL, NARRATIVE_MAX_TOKENS } from "@/lib/byeolmaru/narrative-prompt";
import { generateOnce } from "@/lib/claude";
import { logError, ctxFromRequest } from "@/lib/logger";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export async function GET(req: NextRequest) {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ error: "Login required", code: "LOGIN_REQUIRED" }, { status: 401 });
  const subject = new URL(req.url).searchParams.get("subject");
  if (!subject) return NextResponse.json({ error: "subject_required" }, { status: 400 });
  const logCtx = { route: "/api/byeolmaru/pair-narrative", userId };
  try {
    const ent = await getEntitlement(userId);
    if (!ent.entitled) return NextResponse.json({ entitled: false }, { status: 403 });

    const supa = getServiceSupabase();
    const { data: selfRow, error: selfErr } = await supa.from("user_profiles")
      .select("birth_date, birth_time, is_lunar_input, is_leap_month, gender")
      .eq("user_id", userId).eq("is_primary", true).maybeSingle();
    if (selfErr) { await logError(selfErr, ctxFromRequest(req, logCtx)); return NextResponse.json({ error: "internal" }, { status: 500 }); }
    if (!selfRow?.birth_date) return NextResponse.json({ error: "profile_not_found" }, { status: 404 });

    const { data: pRow, error: pErr } = await supa.from("user_profiles")
      .select("birth_date, birth_time, is_lunar_input, is_leap_month, gender, is_primary, display_name")
      .eq("id", subject).eq("user_id", userId).maybeSingle();
    if (pErr) { await logError(pErr, ctxFromRequest(req, logCtx)); return NextResponse.json({ error: "internal" }, { status: 500 }); }
    if (!pRow || pRow.is_primary || !pRow.birth_date) return NextResponse.json({ error: "invalid_profile" }, { status: 400 });

    const selfInput = profileRowToSajuInput(selfRow);
    const selfSaju = calcSaju(selfInput);
    const partnerSaju = calcSaju(profileRowToSajuInput(pRow));
    const todayKst = kstDate(new Date().toISOString());
    const temporal = calcTemporalLuck(baseDateForKst(todayKst), selfInput.year, { includeMonth: true });
    if (!temporal.dailyLuck?.length) return NextResponse.json({ error: "calc_failed" }, { status: 500 });
    const cell = buildPairCalendar(selfSaju, partnerSaju, temporal.dailyLuck, todayKst)[0];
    const todayGanji = temporal.day.stem + temporal.day.branch;

    try {
      const system = buildPairNarrativeSystem(selfSaju, partnerSaju, pairBackdrop(selfSaju, partnerSaju), cell, todayGanji, pRow.display_name ?? "그 사람");
      const narrative = await generateOnce(system, [{ role: "user", content: PAIR_NARRATIVE_KICKOFF }], NARRATIVE_MAX_TOKENS, logCtx, BYEOLMARU_NARRATIVE_MODEL, undefined);
      if (!narrative) { await logError(new Error("empty pair narrative"), { ...logCtx, extra: { stage: "generate_empty" } }); return NextResponse.json({ entitled: true, narrative: null }); }
      return NextResponse.json({ entitled: true, narrative });
    } catch (err) {
      await logError(err, { ...logCtx, extra: { stage: "generate" } });
      return NextResponse.json({ entitled: true, narrative: null });
    }
  } catch (err) {
    await logError(err, ctxFromRequest(req, logCtx));
    return NextResponse.json({ error: "internal" }, { status: 500 });
  }
}
```
⚠️ `BYEOLMARU_NARRATIVE_MODEL`/`NARRATIVE_MAX_TOKENS` 는 `narrative-prompt.ts` 에서 이미 export 됨(재확인). `generateOnce` 인자 순서는 ②-a route 와 동일하게(그 파일을 열어 대조).

- [ ] **Step 2: tsc + build + 커밋**

`npx tsc --noEmit` → 0 · `npm run build` → exit 0 + 라우트 표에 `/api/byeolmaru/pair-narrative` + `ƒ Proxy`. `git add app/api/byeolmaru/pair-narrative/route.ts` · msg `feat(byeolmaru): 우리오늘 서술 라우트(pair-narrative, nano·자격자 전용)`.

---

## Task 5: PairDayDetailCard 서술 슬롯

**Files:** Modify `components/byeolmaru/PairDayDetailCard.tsx`

- [ ] **Step 1: narrative prop 추가 + 렌더**

props 에 `narrative?: string | null`, `narrativeLoading?: boolean` 추가. "너희 결" `<div>` **아래**(Task10에서 남긴 슬롯)에 서술을 렌더:
```tsx
{narrativeLoading ? (
  <p className="mt-3 text-sm text-text-light">별콩이가 둘 사이 오늘을 읽고 있어…</p>
) : narrative ? (
  <div className="mt-3 space-y-2 text-sm leading-relaxed text-eye-purple whitespace-pre-line">{narrative}</div>
) : null}
```
(빈/실패 서술이면 아무것도 안 보임 — 룰 기반 tone/태그/너희결은 그대로 남아 화면이 비지 않는다.)

- [ ] **Step 2: tsc + 커밋**

`npx tsc --noEmit` → 0. `git add components/byeolmaru/PairDayDetailCard.tsx` · msg `feat(byeolmaru): PairDayDetailCard 서술 슬롯`.

---

## Task 6: ByeolmaruView — 서술 페치 + 계측(partner_selected·subscribe_from_woori)

**Files:** Modify `components/byeolmaru/ByeolmaruView.tsx`

- [ ] **Step 1: pair 서술 페치**

`pairData`(3-a에서 상대 선택 시 채워짐, entitled·非locked) 옆에 `pairNarrative`/`pairNarrativeLoading` 상태 추가. 상대 서술 페치 effect(`[subject, entitledNow]` — pair 캘린더 effect와 같은 트리거, cancelled 가드 동일 패턴):
- `subject==="me"` 또는 locked → skip(narrative null).
- else → `setPairNarrativeLoading(true)` → `GET /api/byeolmaru/pair-narrative?subject=<subject>` → `{narrative}` 세팅(403/실패는 null, 조용히). `PairDayDetailCard` 에 `narrative`/`narrativeLoading` 전달.
> pair 캘린더 페치와 **별도**로 둔다(캘린더는 룰이라 즉시, 서술은 nano라 뒤늦게 채워짐 — ②-a가 calendar와 narrative를 분리한 것과 동일 이유).

- [ ] **Step 2: 계측 배선**

- `onSelect` 핸들러(SubjectToggle): 선택 id 가 `"me"` 가 아니면 `trackUiEvent("byeolmaru_partner_selected")`.
- `LockedWooriTeaser` 의 체험/구독 CTA: 기존 `handleStartTrial`/`handleSubscribeClick` 호출에 더해 `trackUiEvent("byeolmaru_subscribe_from_woori", { meta: { action: "trial" } })` / `{ action: "subscribe" }` 를 각각 발사(락 티저 경유임을 구분).

- [ ] **Step 3: tsc + build + 라이브 확인 + 커밋**

`npx tsc --noEmit` → 0 · `npm run build` → exit 0. `git add components/byeolmaru/ByeolmaruView.tsx` · msg `feat(byeolmaru): 우리오늘 서술 페치 + 계측(partner_selected·subscribe_from_woori)`.

---

## Task 7: WatchAddModal — 계측(watch_add·watch_limit·watch_purchase)

**Files:** Modify `components/byeolmaru/WatchAddModal.tsx`

- [ ] **Step 1: 3지점 배선**

- `startAddFlow`: `nextCost > 0` 로 `StarConfirmModal` 을 띄우는 분기에서 `trackUiEvent("byeolmaru_watch_limit", { meta: { cost: nextCost } })`.
- `submitWatch` 성공(`res.ok`, `onAdded` 직전): `trackUiEvent("byeolmaru_watch_add", { meta: { via: registered ? "register" : "pick" } })`; 추가로 `res.charged > 0` 면(응답 `{ok, charged}`) `trackUiEvent("byeolmaru_watch_purchase", { meta: { stars: charged } })`. (`submitWatch` 가 현재 `res.charged` 를 읽는지 확인 — 안 읽으면 파싱 추가.)

- [ ] **Step 2: tsc + 커밋**

`npx tsc --noEmit` → 0. `git add components/byeolmaru/WatchAddModal.tsx` · msg `feat(byeolmaru): WatchAddModal 계측(watch_add·limit·purchase)`.

---

## Task 8: 회귀 게이트 + dev 라이브 검증

- [ ] **Step 1: 전체 게이트**

`node --import tsx --test lib/byeolmaru/*.test.ts` → 전부 GREEN(pair-day·watch·narrative-prompt 포함). `npx tsc --noEmit` → 0. `npm run build` → exit 0 + `ƒ Proxy` + `/api/byeolmaru/pair-narrative` 등장.

- [ ] **Step 2: dev push + 라이브 E2E**

`git push origin dev`(마이그 없음 — allowlist 는 자유 문자열, 코드만). fixture 유저(self+상대)로: 상대 선택 → 우리 캘린더 + **서술이 몇 초 뒤 슬롯에 채워짐** 확인(구독/체험 자격). 비자격 → 서술 미표시(pair-narrative 403). 계측은 `ui_events` 에 5종이 실제 insert 되는지 dev 확인(브라우저 network 또는 `/admin/errors` 무에러).

---

## Self-Review (작성자 체크)

**1. Spec 커버리지:** §8 서술(자격자 nano·비자격 미호출·너희결 아래 슬롯·별콩 톤) → Task 3·4·5·6 ✅ / §9 계측 5종 → Task 1·6·7 ✅. §9 어드민 → **3-c로 분리(명시)**.
**2. Placeholder 스캔:** Task 6은 3-a effect 패턴(cancelled 가드·[subject,entitledNow] 트리거)을 "동일 적용"으로 위임 — 3-a ByeolmaruView 실코드가 원본이라 억지 복붙보다 지시가 옳음(pair 캘린더 effect 바로 옆에 붙음). 나머지는 전체 코드.
**3. 타입 일관성:** `buildPairNarrativeSystem(self,partner,backdrop,cell,todayGanji,partnerName)`(Task3) ↔ Task4 호출 인자 일치. `PAIR_TONE_LABEL` 단일 출처(pair-day, Task2) ↔ Task3·5 사용. `narrative`/`narrativeLoading` prop(Task5) ↔ Task6 전달. 계측 이벤트명(Task1) ↔ Task6·7 발사 일치.

---

## Execution Handoff

3-c(어드민 우리 metrics)는 이 플랜 완료 후 별도 작성 — `admin_byeolmaru_*` RPC 패턴(`20260904000000`) 미러 + 담기 퍼널·상대 수 분포. AGENTS.md 어드민 함정 4종 + JS↔SQL 대조 검증 적용.
