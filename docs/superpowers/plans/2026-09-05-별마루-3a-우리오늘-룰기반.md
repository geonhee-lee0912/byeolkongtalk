# 별마루 ③-a 우리 오늘 (룰 기반) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루에 "우리 오늘"을 룰 100%로 완결한다 — 상대를 골라 담고(2무료+5별), 상단 토글로 나↔상대를 전환하며, 그 사람과 나의 30일 캘린더를 본다(비구독=완전 블러).

**Architecture:** 인물 데이터는 기존 `user_profiles`(단일 창고), "지켜보는 상대"만 신규 얇은 링크 테이블 `byeolmaru_watch`에. 판정은 순수 함수 `pair-day.ts`(C 하이브리드 3층: 각자 오늘 blend + 관계 촉발/충돌 + 정적 궁합 baseline) — 기존 `pairRelation`·`dayFactors` primitive 재사용. 캘린더 라우트에 `subject` 파라미터를 더해 나(self)/상대(pair) 두 경로를 한 벌로 처리. 게이트는 ②-a `entitlement` 재사용.

**Tech Stack:** Next.js 16(App Router) · TypeScript strict · Supabase(PostgreSQL) · `lib/stars`(별 차감) · `node:test`(유닛).

**Spec:** `docs/superpowers/specs/2026-09-05-별마루-3-우리오늘-design.md` (③-b = LLM 서술 + 계측은 이 플랜 완료 후 별도 작성).

---

## File Structure

- Create `supabase/migrations/20260905000000_byeolmaru_watch.sql` — 신규 링크 테이블.
- Create `lib/byeolmaru/pair-day.ts` — 우리 판정 C(순수) + `buildPairCalendar`.
- Create `lib/byeolmaru/pair-day.test.ts`.
- Modify `lib/byeolmaru/constants.ts` — watch 무료 수 + 추가 가격 상수.
- Create `lib/byeolmaru/watch.ts` — watch 현황/추가/삭제 (DB 래퍼, `lib/relationship/slots.ts` 미러).
- Create `lib/byeolmaru/watch.test.ts` — 순수 allowance 로직.
- Create `app/api/byeolmaru/watch/route.ts` — GET(담은 목록 + 추천) · POST(담기) · DELETE(빼기).
- Modify `app/api/byeolmaru/calendar/route.ts` — `?subject` 파라미터(self|profileId) + 우리 경로 + 게이트.
- Create `components/byeolmaru/SubjectToggle.tsx` — 나/상대들/＋ 토글.
- Create `components/byeolmaru/WatchAddModal.tsx` — 이미 아는 사람 pick / 새 등록.
- Create `components/byeolmaru/PairDayDetailCard.tsx` — 우리 셀 상세(tone/score/태그/"너희 결").
- Modify `components/byeolmaru/ByeolmaruView.tsx` — subject 상태 + 토글 + 우리 경로 + 게이트 블러 + `PartnerSlot` 제거.

각 파일은 하나의 책임만 진다. 판정(pair-day) · 재화/저장(watch) · 조립(calendar route) · 표시(components)로 분리.

---

## Task 1: 마이그레이션 — `byeolmaru_watch` 테이블

**Files:**
- Create: `supabase/migrations/20260905000000_byeolmaru_watch.sql`

- [ ] **Step 1: 마이그레이션 작성**

`20260904200000_byeolmaru_checkins.sql` 패턴을 따른다(RLS ON + REVOKE PUBLIC,anon,authenticated + GRANT service_role — AGENTS.md "새 SECURITY DEFINER/테이블은 닫힌 채 태어난다"). `profile_id`는 `user_profiles(id)` 참조, 둘 다 `ON DELETE CASCADE`(탈퇴/프로필 삭제 시 자동 정리 — AGENTS.md FK 규칙).

```sql
-- 20260905000000_byeolmaru_watch.sql — 별마루 "우리 오늘" 지켜보는 상대(얇은 링크).
-- 인물 데이터는 user_profiles(단일 원천), 이 테이블은 "이 상대를 우리 오늘에서 본다"는 링크뿐.
-- 스펙: docs/superpowers/specs/2026-09-05-별마루-3-우리오늘-design.md §5
CREATE TABLE IF NOT EXISTS byeolmaru_watch (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  profile_id UUID NOT NULL REFERENCES user_profiles(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, profile_id)
);
CREATE INDEX IF NOT EXISTS idx_byeolmaru_watch_user ON byeolmaru_watch(user_id, created_at ASC);

ALTER TABLE byeolmaru_watch ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE byeolmaru_watch FROM PUBLIC, anon, authenticated;
GRANT ALL ON TABLE byeolmaru_watch TO service_role;
```

- [ ] **Step 2: 커밋**

```bash
git add supabase/migrations/20260905000000_byeolmaru_watch.sql
git commit -m "feat(byeolmaru): byeolmaru_watch 테이블 (우리 오늘 지켜보는 상대 링크)"
```

> dev push 후 Supabase dev 자동 적용. 적용 확인은 Task 11에서 카나리아 없이 — 이 테이블은 service_role 전용이라 anon 접근 자체가 기본 차단(AGENTS.md 기본권한). 적용 여부만 `run-prod-query.mjs`로 `to_regclass('public.byeolmaru_watch')` 확인.

---

## Task 2: 상수 — watch 무료 수 + 추가 가격

**Files:**
- Modify: `lib/byeolmaru/constants.ts`

- [ ] **Step 1: 상수 추가**

`BYEOLMARU_SUBSCRIPTION` 아래에 추가. 클라이언트 세이프 유지(서버 전용 import 금지 — 이 파일 규칙).

```ts
/** 우리 오늘 "지켜보는 상대" 슬롯. 2명 무료 + 3번째부터 WATCH_EXTRA_COST 별.
 * 구독(20별)의 작은 애드온 — 답변추천(SIM_SUGGEST_COST=5) 티어. 서버 권위(클라 cost 신뢰 X).
 * 옛 relationship SLOT_COST(50)와 별개(그건 /relationship 잔존용). */
export const WATCH_FREE_SLOTS = 2;
export const WATCH_EXTRA_COST = 5;
```

- [ ] **Step 2: tsc 확인**

Run: `npx tsc --noEmit`
Expected: 에러 0(상수 추가만).

- [ ] **Step 3: 커밋**

```bash
git add lib/byeolmaru/constants.ts
git commit -m "feat(byeolmaru): watch 슬롯 상수(2무료+5별)"
```

---

## Task 3: 판정 C — `pair-day.ts` (순수)

**Files:**
- Create: `lib/byeolmaru/pair-day.ts`
- Test: `lib/byeolmaru/pair-day.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`day-score.test.ts`·`calendar.test.ts`의 스타일(node:test + assert, `calcSaju`로 실 사주 생성)을 따른다. 두 사람의 실제 사주로 셀을 만들고 형태·범위·태그를 검증한다.

```ts
// lib/byeolmaru/pair-day.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { calcSaju, calcTemporalLuck, baseDateForKst } from "@/lib/saju/calc";
import { buildPairCalendar, pairBackdrop, pairDayTone } from "./pair-day";

const A = calcSaju({ year: 1994, month: 5, day: 12, hour: 9, gender: "female", isLunarInput: false, isLeapMonth: false });
const B = calcSaju({ year: 1992, month: 11, day: 3, hour: null, gender: "male", isLunarInput: false, isLeapMonth: false });

test("buildPairCalendar: dailyLuck 길이만큼 셀, score 0~100, tone 정합", () => {
  const t = calcTemporalLuck(baseDateForKst("2026-09-05"), 1994, { includeMonth: true });
  const cells = buildPairCalendar(A, B, t.dailyLuck, "2026-09-05");
  assert.equal(cells.length, t.dailyLuck.length);
  for (const c of cells) {
    assert.ok(c.score >= 0 && c.score <= 100, `score 범위: ${c.score}`);
    assert.equal(c.tone, pairDayTone(c.score));
    assert.equal(typeof c.tags.spark, "boolean");
    assert.ok(c.tags.lead === null || c.tags.lead === "me" || c.tags.lead === "partner");
  }
  assert.equal(cells.filter((c) => c.isToday).length, 1); // 오늘 정확히 1칸
});

test("pairDayTone: 임계값 70/45", () => {
  assert.equal(pairDayTone(70), "good");
  assert.equal(pairDayTone(69), "normal");
  assert.equal(pairDayTone(45), "normal");
  assert.equal(pairDayTone(44), "caution");
});

test("pairBackdrop: 라벨·연월조화 노출", () => {
  const bd = pairBackdrop(A, B);
  assert.equal(typeof bd.labelAtoB, "string");
  assert.equal(typeof bd.labelBtoA, "string");
  assert.ok(bd.harmony >= 0 && bd.harmony <= 4);
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx tsx --test lib/byeolmaru/pair-day.test.ts` (또는 리포의 기존 테스트 러너 — `package.json` 스크립트 확인)
Expected: FAIL — `pair-day.ts` 없음.

- [ ] **Step 3: `pair-day.ts` 구현**

```ts
// lib/byeolmaru/pair-day.ts — 우리 오늘 판정(C 하이브리드). 순수·룰100%·LLM0.
// 나(A) × 상대(B) × 그날 일진 → 우리 셀(tone/score/tags) + "너희 결" 고정 배경.
// 🔴 가중치는 이 파일 상수가 정본·전부 튜닝 대상(day-score.ts 와 같은 규율).
import type { DailyLuck, SajuResult } from "@/lib/saju/calc";
import { pairRelation, heavenlyCombo, earthlySixCombo, earthlySixClash } from "@/lib/saju/pairing";
import { dayFactors, dayScore } from "./day-score";
import { toDaySelf } from "./calendar";

export type PairTone = "good" | "normal" | "caution";

export interface PairDayTags {
  spark: boolean;    // 끌림↑ — 오늘 천간이 A/B 일간과 천간합
  bond: boolean;     // 결속 — 오늘 지지가 A/B 일지와 육합
  friction: boolean; // 삐걱 — 오늘 지지가 A/B 일지와 육충
  lead: "me" | "partner" | null; // 누가 리드(개인 점수 차)
}

export interface PairDayCell {
  date: string;
  ganji: string;
  score: number; // 0~100
  tone: PairTone;
  tags: PairDayTags;
  isToday: boolean;
}

export interface PairBackdrop {
  labelAtoB: string; // 나→상대 십신 별콩 라벨
  labelBtoA: string; // 상대→나
  spark: boolean;    // pair 천간합(일간) = 끌림
  bond: boolean;     // pair 육합(일지) = 결속
  harmony: number;   // 연월조화 0~4
}

// ── 가중치(튜닝 대상) ─────────────────────
const LEAD_THRESHOLD = 8;   // 개인 점수 차 이 이상이면 리드 표시
const SPARK_W = 12;
const BOND_W = 9;
const FRICTION_W = -14;
const BASELINE_SPARK_W = 4;    // pair 천간합
const BASELINE_BOND_W = 4;     // pair 육합
const BASELINE_HARMONY_W = 2;  // 연월조화 1당

function clamp(n: number): number {
  return Math.max(0, Math.min(100, Math.round(n)));
}

export function pairBackdrop(a: SajuResult, b: SajuResult): PairBackdrop {
  const pr = pairRelation(a, b);
  return {
    labelAtoB: pr.labelAtoB,
    labelBtoA: pr.labelBtoA,
    spark: pr.heavenlyCombo,
    bond: pr.sixCombo,
    harmony: pr.extraPillarHarmony,
  };
}

export function pairDayTone(score: number): PairTone {
  if (score >= 70) return "good";
  if (score >= 45) return "normal";
  return "caution";
}

function pairDayScoreAndTags(a: SajuResult, b: SajuResult, d: DailyLuck, backdrop: PairBackdrop) {
  const dl = { stem: d.stem, branch: d.branch, element: d.element };
  const scoreA = dayScore(dayFactors(toDaySelf(a), dl));
  const scoreB = dayScore(dayFactors(toDaySelf(b), dl));

  // 1층: 각자 오늘 base(blend)
  const base = (scoreA + scoreB) / 2;

  // 2층: 관계 촉발/충돌 — 오늘 D 가 A/B 일주를 건드림
  const spark = heavenlyCombo(d.stem, a.dayStem) || heavenlyCombo(d.stem, b.dayStem);
  const bond = earthlySixCombo(d.branch, a.pillars.day.branch) || earthlySixCombo(d.branch, b.pillars.day.branch);
  const friction = earthlySixClash(d.branch, a.pillars.day.branch) || earthlySixClash(d.branch, b.pillars.day.branch);

  // 3층: 정적 궁합 baseline lift
  const baseline =
    (backdrop.spark ? BASELINE_SPARK_W : 0) +
    (backdrop.bond ? BASELINE_BOND_W : 0) +
    backdrop.harmony * BASELINE_HARMONY_W;

  const score = clamp(base + (spark ? SPARK_W : 0) + (bond ? BOND_W : 0) + (friction ? FRICTION_W : 0) + baseline);
  const lead: PairDayTags["lead"] =
    Math.abs(scoreA - scoreB) >= LEAD_THRESHOLD ? (scoreA > scoreB ? "me" : "partner") : null;

  return { score, tags: { spark, bond, friction, lead } as PairDayTags };
}

/** 나(a)×상대(b)의 30일 우리 캘린더. 오늘 판정은 인자 todayKst 로만(서버 TZ 무관 — 라우트가 계산). */
export function buildPairCalendar(a: SajuResult, b: SajuResult, dailyLuck: DailyLuck[], todayKst: string): PairDayCell[] {
  const backdrop = pairBackdrop(a, b);
  return dailyLuck.map((d) => {
    const { score, tags } = pairDayScoreAndTags(a, b, d, backdrop);
    return { date: d.date, ganji: d.stem + d.branch, score, tone: pairDayTone(score), tags, isToday: d.date === todayKst };
  });
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx tsx --test lib/byeolmaru/pair-day.test.ts`
Expected: PASS(3 테스트).

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/pair-day.ts lib/byeolmaru/pair-day.test.ts
git commit -m "feat(byeolmaru): 우리 오늘 판정 C 하이브리드(pair-day, 룰100%)"
```

---

## Task 4: watch 래퍼 — `watch.ts` (`slots.ts` 미러)

**Files:**
- Create: `lib/byeolmaru/watch.ts`
- Test: `lib/byeolmaru/watch.test.ts`

`lib/relationship/slots.ts`를 미러한다: 무료 허용 = `WATCH_FREE_SLOTS + 구매한 슬롯 수`(구매는 `star_transactions.source='byeolmaru_watch'` 카운트). remove/재add 게이밍 방지(라이브 행 수가 아니라 **구매 수** 기준 — slots.ts 와 동일).

- [ ] **Step 1: 순수 allowance 테스트**

```ts
// lib/byeolmaru/watch.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { watchAllowance } from "./watch";
import { WATCH_FREE_SLOTS } from "./constants";

test("watchAllowance: 무료 기본 + 구매분 가산", () => {
  assert.equal(watchAllowance(0), WATCH_FREE_SLOTS);       // 2
  assert.equal(watchAllowance(1), WATCH_FREE_SLOTS + 1);   // 3
  assert.equal(watchAllowance(3), WATCH_FREE_SLOTS + 3);   // 5
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx tsx --test lib/byeolmaru/watch.test.ts`
Expected: FAIL — `watch.ts` 없음.

- [ ] **Step 3: `watch.ts` 구현**

`slots.ts`의 구조 그대로, 대상 테이블만 `byeolmaru_watch`, source만 `byeolmaru_watch`로. 별 차감은 `lib/stars`의 `spendStars`(RPC 직접 호출 금지 — AGENTS.md).

```ts
// lib/byeolmaru/watch.ts — 우리 오늘 "지켜보는 상대" 조회/추가/삭제 (DB 래퍼).
// lib/relationship/slots.ts 미러: 허용 = WATCH_FREE_SLOTS + 구매 슬롯. 별 차감은 spendStars.
import { getServiceSupabase } from "@/lib/supabase";
import { spendStars } from "@/lib/stars";
import { WATCH_FREE_SLOTS, WATCH_EXTRA_COST } from "./constants";

export function watchAllowance(purchasedSlots: number): number {
  return WATCH_FREE_SLOTS + Math.max(0, purchasedSlots);
}

export interface WatchState {
  allowed: number;
  used: number;
  canAddFree: boolean;
  nextCost: number; // 다음 1명 담을 때 드는 별(0 = 무료)
}

export async function getWatchState(userId: string): Promise<WatchState> {
  const supabase = getServiceSupabase();
  const [{ count: purchased }, { count: used }] = await Promise.all([
    supabase.from("star_transactions").select("id", { count: "exact", head: true })
      .eq("user_id", userId).eq("source", "byeolmaru_watch"),
    supabase.from("byeolmaru_watch").select("profile_id", { count: "exact", head: true })
      .eq("user_id", userId),
  ]);
  const allowed = watchAllowance(purchased ?? 0);
  const usedN = used ?? 0;
  const canAddFree = usedN < allowed;
  return { allowed, used: usedN, canAddFree, nextCost: canAddFree ? 0 : WATCH_EXTRA_COST };
}

/** 상대를 담는다. 무료 여유 없으면 WATCH_EXTRA_COST 별 차감 후 담는다.
 * profileId 는 반드시 이 유저 소유의 user_profiles(비-self) 여야 한다(라우트가 검증). */
export async function addWatch(
  userId: string, profileId: string
): Promise<{ success: boolean; charged: number; reason?: string; balance?: number }> {
  const supabase = getServiceSupabase();
  const state = await getWatchState(userId);

  let charged = 0;
  if (!state.canAddFree) {
    const res = await spendStars(userId, WATCH_EXTRA_COST, { source: "byeolmaru_watch" });
    if (!res.success) return { success: false, charged: 0, reason: res.reason, balance: res.balance };
    charged = WATCH_EXTRA_COST;
  }

  // 멱등: 이미 담은 상대면 재삽입 무해(PK 충돌 무시). 단 이 경우 위에서 별을 태우면 안 되므로
  // 라우트에서 중복을 먼저 걸러 addWatch 를 호출하지 않는다(Task 5 GET 로 확인).
  const { error } = await supabase.from("byeolmaru_watch").insert({ user_id: userId, profile_id: profileId });
  if (error) return { success: false, charged, reason: error.message };
  return { success: true, charged };
}

export async function removeWatch(userId: string, profileId: string): Promise<{ success: boolean }> {
  const supabase = getServiceSupabase();
  const { error } = await supabase.from("byeolmaru_watch")
    .delete().eq("user_id", userId).eq("profile_id", profileId);
  return { success: !error };
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx tsx --test lib/byeolmaru/watch.test.ts`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/watch.ts lib/byeolmaru/watch.test.ts
git commit -m "feat(byeolmaru): watch 래퍼(2무료+5별, slots.ts 미러)"
```

---

## Task 5: API — `app/api/byeolmaru/watch/route.ts`

**Files:**
- Create: `app/api/byeolmaru/watch/route.ts`

GET = 담은 목록 + 추천(아직 안 담은 비-self 프로필) + 현황. POST = 담기(소유·비-self·중복·생일 검증 → addWatch). DELETE = 빼기. **전 메서드 구독 게이트**(비구독 = 담기/조회 불가 = 완전 블러). `calendar/route.ts`의 세션·프로필 조회 패턴 재사용.

- [ ] **Step 1: 라우트 구현**

```ts
// app/api/byeolmaru/watch/route.ts — 우리 오늘 지켜보는 상대 관리. 구독 전용(비구독=차단).
import { NextRequest, NextResponse } from "next/server";
import { getSession } from "@/lib/session";
import { getServiceSupabase } from "@/lib/supabase";
import { getEntitlement } from "@/lib/byeolmaru/entitlement";
import { getWatchState, addWatch, removeWatch } from "@/lib/byeolmaru/watch";
import { logError, ctxFromRequest } from "@/lib/logger";

export const dynamic = "force-dynamic";

async function requireEntitled(userId: string | null) {
  if (!userId) return { code: 401 as const, body: { error: "Login required", code: "LOGIN_REQUIRED" } };
  const ent = await getEntitlement(userId);
  if (!ent.entitled) return { code: 403 as const, body: { error: "subscription_required", code: "LOCKED" } };
  return null;
}

export async function GET(req: NextRequest) {
  const { userId } = await getSession();
  const gate = await requireEntitled(userId);
  if (gate) return NextResponse.json(gate.body, { status: gate.code });
  try {
    const supa = getServiceSupabase();
    const { data: profiles } = await supa.from("user_profiles")
      .select("id, display_name, relation_type, birth_date, is_primary")
      .eq("user_id", userId);
    const { data: watched } = await supa.from("byeolmaru_watch")
      .select("profile_id, created_at").eq("user_id", userId).order("created_at", { ascending: true });
    const watchedIds = new Set((watched ?? []).map((w) => w.profile_id));
    const candidates = (profiles ?? []).filter((p) => !p.is_primary && p.birth_date);
    return NextResponse.json({
      watched: candidates.filter((p) => watchedIds.has(p.id)).map((p) => ({ id: p.id, name: p.display_name })),
      suggestions: candidates.filter((p) => !watchedIds.has(p.id)).map((p) => ({ id: p.id, name: p.display_name })),
      state: await getWatchState(userId!),
    });
  } catch (err) {
    await logError(err, ctxFromRequest(req, { route: "/api/byeolmaru/watch", userId: userId! }));
    return NextResponse.json({ error: "internal" }, { status: 500 });
  }
}

export async function POST(req: NextRequest) {
  const { userId } = await getSession();
  const gate = await requireEntitled(userId);
  if (gate) return NextResponse.json(gate.body, { status: gate.code });
  let profileId: string;
  try { profileId = String((await req.json())?.profileId ?? ""); } catch { return NextResponse.json({ error: "invalid_json" }, { status: 400 }); }
  if (!profileId) return NextResponse.json({ error: "profileId_required" }, { status: 400 });
  try {
    const supa = getServiceSupabase();
    // 소유 + 비-self + 생일 검증(내 프로필/남의 프로필/생일없음 담기 방지)
    const { data: p } = await supa.from("user_profiles")
      .select("id, is_primary, birth_date").eq("id", profileId).eq("user_id", userId).maybeSingle();
    if (!p || p.is_primary || !p.birth_date) return NextResponse.json({ error: "invalid_profile" }, { status: 400 });
    // 중복이면 별 안 태우고 성공 처리(addWatch 호출 안 함)
    const { data: dup } = await supa.from("byeolmaru_watch")
      .select("profile_id", { head: true, count: "exact" }).eq("user_id", userId).eq("profile_id", profileId);
    if ((dup as unknown as { count?: number })?.count) return NextResponse.json({ ok: true, charged: 0 });
    const res = await addWatch(userId!, profileId);
    if (!res.success && res.reason === "insufficient") return NextResponse.json({ error: "insufficient_balance", code: "NEED_STARS" }, { status: 402 }); // spend_stars RPC 는 'insufficient' 반환(확인됨)
    if (!res.success) return NextResponse.json({ error: res.reason ?? "add_failed" }, { status: 500 });
    return NextResponse.json({ ok: true, charged: res.charged });
  } catch (err) {
    await logError(err, ctxFromRequest(req, { route: "/api/byeolmaru/watch", userId: userId! }));
    return NextResponse.json({ error: "internal" }, { status: 500 });
  }
}

export async function DELETE(req: NextRequest) {
  const { userId } = await getSession();
  const gate = await requireEntitled(userId);
  if (gate) return NextResponse.json(gate.body, { status: gate.code });
  const profileId = new URL(req.url).searchParams.get("profileId");
  if (!profileId) return NextResponse.json({ error: "profileId_required" }, { status: 400 });
  const res = await removeWatch(userId!, profileId);
  return NextResponse.json({ ok: res.success });
}
```

> ✅ 확인됨: `spendStars`(→ `spend_stars` RPC, `supabase/migrations/20260604000000_stars.sql`)는 잔액 부족 시 `reason: 'insufficient'` 반환(재정의본·pass·subscription RPC 전부 동일). 위 402 분기에 반영됨. 별 잔액은 `lib/stars.ts` 의 `spendStars` 만 경유(RPC 직접 호출 금지).

- [ ] **Step 2: tsc + 커밋**

Run: `npx tsc --noEmit` → 에러 0.
```bash
git add app/api/byeolmaru/watch/route.ts
git commit -m "feat(byeolmaru): watch API(GET 목록·추천 / POST 담기 / DELETE, 구독 게이트)"
```

---

## Task 6: 캘린더 라우트 — `subject` 파라미터 + 우리 경로 + 게이트

**Files:**
- Modify: `app/api/byeolmaru/calendar/route.ts`

`?subject` 없거나 `me` = 기존 self 경로 그대로. `?subject=<profileId>` = 우리 경로: ① 구독 게이트(비자격 → `{ locked:true }`, 점수 미포함 = 완전 블러, 데이터 누출 0) ② 그 profileId 소유·비-self·생일 검증 ③ self+partner 사주 → `buildPairCalendar` + `pairBackdrop`.

- [ ] **Step 1: self 경로를 함수로 분리 + subject 분기 추가**

기존 GET 본문 상단(세션·is_primary 조회까지)은 공통. `subject` 파라미터를 읽어 분기한다. 우리 경로 핵심:

```ts
// GET 안, self 사주(saju)·todayKst·temporal 계산 재사용 후:
const subject = new URL(req.url).searchParams.get("subject");
if (subject && subject !== "me") {
  const ent = await getEntitlement(userId);
  if (!ent.entitled) {
    // 완전 블러: 구조만, 점수·태그 없음(데이터 누출 0). 클라가 블러+CTA 렌더.
    return NextResponse.json({ subject, locked: true, entitled: false, today: todayKst });
  }
  const { data: pRow } = await getServiceSupabase().from("user_profiles")
    .select("birth_date, birth_time, is_lunar_input, is_leap_month, gender, is_primary, display_name")
    .eq("id", subject).eq("user_id", userId).maybeSingle();
  if (!pRow || pRow.is_primary || !pRow.birth_date) {
    return NextResponse.json({ error: "invalid_profile" }, { status: 400 });
  }
  const partnerSaju = calcSaju(profileRowToSajuInput(pRow));
  const pairCells = buildPairCalendar(saju, partnerSaju, temporal.dailyLuck, todayKst);
  return NextResponse.json({
    subject, entitled: true, locked: false,
    today: todayKst, todayGanji: temporal.day.stem + temporal.day.branch,
    partnerName: pRow.display_name,
    cells: pairCells,
    backdrop: pairBackdrop(saju, partnerSaju),
  });
}
// (여기부터 기존 self 응답)
```

Import 추가: `import { buildPairCalendar, pairBackdrop } from "@/lib/byeolmaru/pair-day";`

- [ ] **Step 2: 라이브 검증(dev, 세션 주입)**

`byeolkong_user_id` 쿠키로 세션 주입([[browser-e2e-session-injection]]) 후:
- `GET /api/byeolmaru/calendar` → 기존 self 응답 무회귀.
- 비구독 유저 `?subject=<partnerId>` → `{ locked:true, entitled:false }`, cells 없음.
- 구독(또는 trial) 유저 `?subject=<partnerId>` → `cells` 30개 + `backdrop`.
- 남의 profileId → 400 `invalid_profile`.

- [ ] **Step 3: 커밋**

```bash
git add app/api/byeolmaru/calendar/route.ts
git commit -m "feat(byeolmaru): 캘린더 subject 파라미터(우리 경로 + 완전 블러 게이트)"
```

---

## Task 7: `SubjectToggle` 컴포넌트

**Files:**
- Create: `components/byeolmaru/SubjectToggle.tsx`

칩 행: `나`(항상) · 담은 상대들 · `＋`. 현재 선택 하이라이트. Tailwind — 상태 색 충돌 방지로 **배타 삼항**(AGENTS.md/스펙 리뷰 교훈: `ring-2` 색이 두 클래스로 붙으면 승자가 빌드 스캔 순서로 갈림).

- [ ] **Step 1: 구현**

```tsx
"use client";
interface Partner { id: string; name: string; }
export default function SubjectToggle({
  partners, selected, onSelect, onAdd,
}: { partners: Partner[]; selected: string; onSelect: (id: string) => void; onAdd: () => void; }) {
  const chip = (active: boolean) =>
    active
      ? "bg-lilac-deep text-cream font-bold"
      : "border border-lilac-mid text-text-light";
  return (
    <div className="flex flex-wrap gap-2" role="tablist" aria-label="캘린더 대상">
      <button role="tab" aria-selected={selected === "me"} onClick={() => onSelect("me")}
        className={`rounded-full px-3 py-1 text-sm ${chip(selected === "me")}`}>나</button>
      {partners.map((p) => (
        <button key={p.id} role="tab" aria-selected={selected === p.id} onClick={() => onSelect(p.id)}
          className={`rounded-full px-3 py-1 text-sm ${chip(selected === p.id)}`}>{p.name}</button>
      ))}
      <button onClick={onAdd} aria-label="상대 걸어두기"
        className="rounded-full border border-dashed border-lilac-mid px-3 py-1 text-sm text-text-light">＋</button>
    </div>
  );
}
```

- [ ] **Step 2: tsc + 커밋**

Run: `npx tsc --noEmit`
```bash
git add components/byeolmaru/SubjectToggle.tsx
git commit -m "feat(byeolmaru): SubjectToggle(나/상대/＋)"
```

---

## Task 8: `WatchAddModal` 컴포넌트

**Files:**
- Create: `components/byeolmaru/WatchAddModal.tsx`

두 모드: ① "이미 아는 사람"(GET /watch suggestions에서 pick) ② "새로 등록"(생일 폼 → `POST /api/profiles` relation_type='partner' → 반환 id로 `POST /watch`). 3번째부터 `nextCost=5` 표기 + `StarConfirmModal` 확인(②-a 결제 패턴, `subBalance` fetch 포함 — `ByeolmaruView`의 구독 결제 흐름 참고).

- [ ] **Step 1: 구현**

- 마운트 시 `GET /api/byeolmaru/watch`로 `suggestions` + `state`(nextCost) 로드.
- suggestion 클릭 → `nextCost>0`면 `StarConfirmModal`(cost=nextCost, balance=`/api/stars/balance` 실조회 — ⚠️`balance={null}`이면 확인버튼 영구 disabled, ②-a 실버그) → 확인 시 `POST /watch {profileId}` → 402면 `/shop`.
- "새로 등록" → 생일 입력(기존 상대 등록 폼 컴포넌트 재사용 가능한지 `app/api/relationship/route.ts` inline 등록/`SelfSajuEditModal` 패턴 확인) → `POST /api/profiles` → 반환 `profile.id`로 `POST /watch`.
- 성공 시 `onAdded(profileId)` 콜백.

> 코드 스니펫은 `ByeolmaruView`의 `handleSubscribeClick`/`StarConfirmModal` 사용부(잔액 실조회 포함)를 그대로 본떠라. 생일 폼은 신규 작성 대신 기존 등록 컴포넌트 재사용을 우선 조사.

- [ ] **Step 2: tsc + 커밋**

```bash
git add components/byeolmaru/WatchAddModal.tsx
git commit -m "feat(byeolmaru): WatchAddModal(이미 아는 사람/새 등록 + 5별 확인)"
```

---

## Task 9: `PairDayDetailCard` 컴포넌트

**Files:**
- Create: `components/byeolmaru/PairDayDetailCard.tsx`

우리 셀 상세: "상대이름과 나 · M월 D일" + tone 라벨 + score + 오늘 태그(끌림↑·결속·삐걱·리드) + "너희 결" 배경(labelAtoB/BtoA·끌림·결속·연월조화). `DayDetailCard`의 마크업·톤 색을 참고하되 우리용 필드(tags/backdrop)를 받는다. 서술 영역은 ③-b(LLM)에서 채움 — 지금은 룰 태그까지만.

- [ ] **Step 1: 구현**

Props: `{ cell: PairDayCell; backdrop: PairBackdrop; partnerName: string }`. tone→라벨/색 매핑은 `dayGrade`(day-score.ts)의 라벨 규칙과 톤을 맞춘다("잘 맞는 날/무난한 날/살짝 챙길 날"). 태그는 boolean→칩, `lead`는 `me`→"네가 리드"/`partner`→`${partnerName}가 리드`.

- [ ] **Step 2: tsc + 커밋**

```bash
git add components/byeolmaru/PairDayDetailCard.tsx
git commit -m "feat(byeolmaru): PairDayDetailCard(우리 셀 상세 + 너희 결)"
```

---

## Task 10: `ByeolmaruView` 통합 + `PartnerSlot` 제거

**Files:**
- Modify: `components/byeolmaru/ByeolmaruView.tsx`

- [ ] **Step 1: subject 상태 + 토글 + 우리 경로 배선**

- `const [subject, setSubject] = useState<string>("me")`.
- `SubjectToggle`를 헤더 아래(출석 스트립 위 또는 캘린더 위)에 배치. `partners` = `GET /api/byeolmaru/watch`의 `watched`(구독자만 로드; 비구독은 빈 배열 → 토글엔 `나` + `＋`만).
- `subject`가 바뀌면 `GET /api/byeolmaru/calendar?subject=<subject>` 재조회:
  - `me` → 기존 self 셀(`DayDetailCard`).
  - partner + `locked:true`(비구독) → 캘린더/상세 자리에 **완전 블러 + 3일 체험 CTA**(기존 `PremiumBlock`/시안 재활용 or 신규 블러 오버레이). `＋`/토글 partner 클릭도 비구독이면 구독 CTA로.
  - partner + `entitled` → `CalendarGrid`(우리 tone)+`PairDayDetailCard`.
- `＋` → `WatchAddModal`. 담기 성공 → watched 재조회 + 그 상대로 `setSubject`.

- [ ] **Step 2: `PartnerSlot` 제거**

`import PartnerSlot`와 `<PartnerSlot />` 삭제(백로그 #14 — 스레드/시뮬 진입은 별마루서 은퇴). `components/byeolmaru/PartnerSlot.tsx` 파일은 **남겨둔다**(다른 참조 없으면 dead지만 삭제는 별건 — grep로 참조 0 확인만 하고 언급).

Run: `npx tsc --noEmit`

- [ ] **Step 3: 라이브 검증(dev)**

세션 주입으로:
- 비구독: `나`=무료 캘린더 정상. partner 토글/＋ → 완전 블러 + 구독 CTA(점수 안 보임).
- 구독(trial): `＋`로 이미 아는 사람 담기 → 토글에 뜸 → 선택 시 우리 캘린더+상세(태그/너희 결). 3번째 담기 → 5별 확인 모달.
- 스크린샷 1장(구독자 우리 오늘) 확보.

- [ ] **Step 4: 커밋**

```bash
git add components/byeolmaru/ByeolmaruView.tsx
git commit -m "feat(byeolmaru): 우리 오늘 통합(토글·우리 경로·블러 게이트) + PartnerSlot 진입 제거"
```

---

## Task 11: 회귀 게이트 + 마이그 적용 확인

- [ ] **Step 1: 전체 테스트 + 빌드**

Run: `npm test`(또는 리포 테스트 스크립트) → 기존 + 신규(pair-day·watch) GREEN.
Run: `npx tsc --noEmit` → 0.
Run: `npm run build` → exit 0, 라우트 표에 `ƒ Proxy` 유지(AGENTS.md 검증).

- [ ] **Step 2: dev push + 마이그 적용 확인**

```bash
git push origin dev
```
Supabase dev Workflow SUCCESS 확인 후 `scripts/run-prod-query.mjs`(read-only, dev 대상)로:
`select to_regclass('public.byeolmaru_watch');` → 테이블 존재. `select proname, proacl from pg_proc ...`(AGENTS.md 한 줄 감사)로 OPEN_ACL 0(신규 RPC 없으니 해당 없음, 테이블 ACL은 service_role만).

- [ ] **Step 3: dev 라이브 최종 검증**

Task 6·10의 검증 재확인(무회귀 + 우리 경로 + 블러). 스크린샷·응답으로 증거 남김([[verify-by-exercising-not-reading]]).

---

## Self-Review (작성자 체크)

**1. Spec 커버리지:**
- §2 범위 B(우리 30일) → Task 3(buildPairCalendar)+6(라우트) ✅ / A(오늘셀)=30일의 isToday ✅
- §3 토글·나 기본·상대0=나만 → Task 7·10 ✅ / 완전 블러 → Task 6(locked)·10 ✅
- §4 게이트(비구독 완전 블러, entitlement 재사용) → Task 5·6 ✅ / 인물 데이터 구독 독립 → 테이블 FK만, 구독 참조 0 ✅
- §5 user_profiles 단일원천 + byeolmaru_watch 링크 + is_primary 가드 → Task 1·5·6 ✅
- §6 여러 명·2무료+5별·인라인 등록·이미아는사람 → Task 2·4·5·8 ✅
- §7 판정 C 3층·단일 우리-score+태그·너희 결·튜닝상수 → Task 3 ✅
- §8 서술 → ③-b(범위 밖, 명시) / §9 계측 → ③-b(범위 밖)
- §10 택일 제외 → 미구현(의도) ✅

**2. Placeholder 스캔:** Task 8은 코드 대신 "기존 패턴 본떠라"로 위임 — UI 결제/폼은 리포 실컴포넌트 재사용이 정답이라 억지 코드보다 지시가 옳음. 단 재사용 대상(StarConfirmModal·잔액 fetch·등록 폼)을 명시했다. Task 5의 `spendStars` reason 문자열은 "실행 시 확인" 경고로 못박음(추정 금지).

**3. 타입 일관성:** `PairDayCell`/`PairBackdrop`/`PairDayTags`(Task 3) ↔ Task 6 응답 ↔ Task 9 props 일치. `WatchState`(Task 4) ↔ Task 5 GET `state` 일치. `watchAllowance`·`getWatchState`·`addWatch`·`removeWatch` 시그니처 Task 4→5 일치.

---

## Execution Handoff

3-b(LLM 서술 + 계측 5종 + 어드민)는 이 플랜 완료 후 **실제 시그니처 확정 뒤** 별도 작성한다(3-a의 PairDayCell·watch API에 의존).
