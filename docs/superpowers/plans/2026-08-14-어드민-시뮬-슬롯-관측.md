# 어드민 시뮬·슬롯 관측 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 방금 배포한 시뮬 무료 런웨이·1:N 슬롯의 "무료 축적→유료 전환"을 볼 어드민 지표를 `/admin/relationship`(집계)과 `relationship-readings/[id]`(개별 드릴다운)에 신설한다.

**Architecture:** 집계는 새 Postgres RPC 2개(`admin_sim_summary`·`admin_slots_summary`)가 한다 — 원본 행을 앱으로 끌어오지 않는다(2026-07-28 `Max rows` cap 사고 회피). 개별 드릴다운은 관계당 판수·프로필 1행으로 유계라 페이지 직접 쿼리(기존 상세와 동일 패턴). 새 라우트·nav·proxy 변경 0.

**Tech Stack:** Next 16 App Router 서버 컴포넌트, Supabase RPC(SQL), TypeScript strict. 테스트 하네스는 `node --import tsx --test`(vitest 미설치).

**검증 관점(정직):** 이 판은 SQL 집계 + 서버 렌더가 전부라 순수 유닛 표면이 없다. 라벨 맵은 기존 코드처럼 인라인한다(가짜 lib+테스트 안 만듦 — 사용자 지시 "신규 지표 추가만·오버엔지니어링 금지"). 검증 3종 = ①tsc 클린 + 기존 유닛 회귀 0 ②새 RPC inner SELECT 를 read-only 로 실행해 컬럼 참조 오류 사전 차단(2026-07-31 "파일 통째 실패" 사고 방지) ③dev 마이그레이션 적용 후 어드민 세션 주입 브라우저 렌더.

---

## 데이터 소스 · 상수 (정본)

`lib/relationship/types.ts`: `SIM_COST=15` · `SIM_SUGGEST_COST=5` · `SIM_FREE_RUNWAY=3` · `SLOT_COST=50`.

| 개념 | 출처 |
|---|---|
| 시뮬 판 | `readings.consultation_type='relationship_sim'`, `relationship_id` 귀속 |
| 자금원 | `readings.saju_data->>'funding'` ∈ {runway,hook,paid}, **NULL→paid** coalesce |
| 완주 | `readings.saju_data->>'phase' = 'debriefed'` |
| 답변추천 구매 | `star_transactions.source='relationship_sim_suggest'`, `type='spend'` |
| 슬롯 구매 | `star_transactions.source='relationship_slot'`, `type='spend'` |
| 관계 | `relationships` (user_id 귀속, `partner_profile_id` FK) |
| 초상화 | `relationships.partner_profile_id` → `user_profiles.personality` |

NULL 가능성(기존 마이그레이션에서 확인, 추정 아님): `readings.relationship_id`·`saju_data` NULLABLE · `relationships.partner_profile_id` NULLABLE · `user_profiles.personality` NULLABLE · `star_transactions.user_id/type/source` NOT NULL.

---

## Task 1: 집계 RPC 마이그레이션

**Files:**
- Create: `supabase/migrations/20260814010000_admin_sim_slots_aggregates.sql`

패턴은 `supabase/migrations/20260731030000_admin_relationship_aggregates.sql` 을 그대로 따른다: `LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public`, 본문 컬럼 전부 별칭 수식, `user_id <> ALL(p_exclude)`, BIGINT 반환, REVOKE 3종 + GRANT service_role(시그니처 전부 명시).

- [ ] **Step 1: 마이그레이션 파일 작성**

```sql
-- 어드민 시뮬·슬롯 집계 RPC (2026-08-14) — 로드맵 ③ 어드민 구성 한판.
--
-- 왜: 시뮬 무료 런웨이(관계당 3판)·1:N 슬롯(첫 관계 무료)을 방금 배포했는데
-- '무료 축적→유료 전환'을 볼 창이 0이다. funding 태그(readings.saju_data->>'funding')가
-- 무료/유료 판정의 유일 소스(무료 판은 star_transaction 이 없다).
--
-- 원본 행을 앱으로 끌어오지 않는다 — /admin/relationship 은 이미 RPC 집계다(20260731030000).
-- 날짜 창이 없는 누적 카운트라 KST 날짜식은 불필요하다(창이 없으므로).
-- ⚠️ 본문 컬럼은 전부 별칭 수식 — RETURNS TABLE OUT 파라미터가 본문 스코프에 들어와
--    funding·phase·id 처럼 흔한 이름과 겹친다.
--
-- 컬럼 NULL 가능성(추정 아님):
--   readings:       user_id NOT NULL · relationship_id·saju_data NULLABLE · consultation_type NOT NULL
--   relationships:  user_id NOT NULL · partner_profile_id NULLABLE
--   user_profiles:  personality NULLABLE
--   star_transactions: user_id/type/source/amount NOT NULL · reading_id NULLABLE

-- ── 1. 시뮬 요약 (funding 분포·전환·완주·초상화 축적) ──
-- funding: 무료 판은 트랜잭션이 없어 태그가 유일 소스. NULL(레거시)=paid 취급(SimMeta 주석 정합).
-- 런웨이 소진 관계 = funding='runway' 판이 SIM_FREE_RUNWAY(3)개인 관계(무료벽 도달). >= 3 로 안전하게.
-- 초상화 모집단 = 시뮬 플레이한 관계 distinct → partner 프로필 personality 가 비어있지 않은 것.
--   (personality 는 시뮬 관찰 append 외에 등록 입력으로도 채워질 수 있어 근사 프록시다 — 스펙 명시.)
-- 답변추천 = source='relationship_sim_suggest' spend 건수(환불은 별도 source 라 자동 제외).
CREATE OR REPLACE FUNCTION admin_sim_summary(p_exclude UUID[])
RETURNS TABLE (
  total_plays BIGINT, runway_plays BIGINT, hook_plays BIGINT, paid_plays BIGINT,
  debriefed_plays BIGINT, play_users BIGINT, play_rels BIGINT,
  runway_exhausted_rels BIGINT, suggest_purchases BIGINT,
  portrait_rels BIGINT, portrait_avg_len BIGINT
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH sim AS (
    SELECT rd.id AS id, rd.user_id AS user_id, rd.relationship_id AS relationship_id,
           coalesce(rd.saju_data->>'funding', 'paid') AS funding,
           (rd.saju_data->>'phase') AS phase
    FROM readings rd
    WHERE rd.consultation_type = 'relationship_sim'
      AND rd.user_id <> ALL(p_exclude)
  ), runway_by_rel AS (
    SELECT s.relationship_id AS relationship_id, count(*) AS n
    FROM sim s
    WHERE s.funding = 'runway' AND s.relationship_id IS NOT NULL
    GROUP BY s.relationship_id
  ), sim_rel AS (
    SELECT DISTINCT s.relationship_id AS relationship_id
    FROM sim s WHERE s.relationship_id IS NOT NULL
  ), portrait AS (
    SELECT up.personality AS personality
    FROM sim_rel sr
    JOIN relationships r ON r.id = sr.relationship_id
    JOIN user_profiles up ON up.id = r.partner_profile_id
    WHERE up.personality IS NOT NULL AND btrim(up.personality) <> ''
  )
  SELECT
    (SELECT count(*) FROM sim)::BIGINT,
    (SELECT count(*) FROM sim s WHERE s.funding = 'runway')::BIGINT,
    (SELECT count(*) FROM sim s WHERE s.funding = 'hook')::BIGINT,
    (SELECT count(*) FROM sim s WHERE s.funding = 'paid')::BIGINT,
    (SELECT count(*) FROM sim s WHERE s.phase = 'debriefed')::BIGINT,
    (SELECT count(DISTINCT s.user_id) FROM sim s)::BIGINT,
    (SELECT count(DISTINCT s.relationship_id) FROM sim s WHERE s.relationship_id IS NOT NULL)::BIGINT,
    (SELECT count(*) FROM runway_by_rel rb WHERE rb.n >= 3)::BIGINT,
    (SELECT count(*) FROM star_transactions t
       WHERE t.source = 'relationship_sim_suggest' AND t.type = 'spend'
         AND t.user_id <> ALL(p_exclude))::BIGINT,
    (SELECT count(*) FROM portrait)::BIGINT,
    (SELECT coalesce(round(avg(char_length(p.personality))), 0) FROM portrait p)::BIGINT;
$$;

-- ── 2. 슬롯 요약 (구매·다중관계·관계수 분포) ──
-- 슬롯 구매 = source='relationship_slot' spend 건수(각 SLOT_COST=50, 매출은 앱에서 ×50).
-- 관계수 분포 = 어드민 제외 유저를 관계 수로 버킷(1 / 2 / 3+). 다중관계 = 2+ (1:N 실현).
CREATE OR REPLACE FUNCTION admin_slots_summary(p_exclude UUID[])
RETURNS TABLE (
  slot_purchases BIGINT, multi_rel_users BIGINT,
  rels_1 BIGINT, rels_2 BIGINT, rels_3plus BIGINT
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH by_user AS (
    SELECT r.user_id AS user_id, count(*) AS n
    FROM relationships r
    WHERE r.user_id <> ALL(p_exclude)
    GROUP BY r.user_id
  )
  SELECT
    (SELECT count(*) FROM star_transactions t
       WHERE t.source = 'relationship_slot' AND t.type = 'spend'
         AND t.user_id <> ALL(p_exclude))::BIGINT,
    (SELECT count(*) FROM by_user b WHERE b.n >= 2)::BIGINT,
    (SELECT count(*) FROM by_user b WHERE b.n = 1)::BIGINT,
    (SELECT count(*) FROM by_user b WHERE b.n = 2)::BIGINT,
    (SELECT count(*) FROM by_user b WHERE b.n >= 3)::BIGINT;
$$;

-- ── 권한: service_role 전용 ──
-- 🔴 AGENTS.md 규칙 — 새 SECURITY DEFINER RPC 는 PUBLIC·anon·authenticated 셋 다 명시 회수 + 시그니처 명시.
REVOKE EXECUTE ON FUNCTION admin_sim_summary(UUID[]) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION admin_slots_summary(UUID[]) FROM PUBLIC, anon, authenticated;

GRANT EXECUTE ON FUNCTION admin_sim_summary(UUID[]) TO service_role;
GRANT EXECUTE ON FUNCTION admin_slots_summary(UUID[]) TO service_role;
```

- [ ] **Step 2: 두 함수 inner SELECT 를 read-only 로 실행 검증**

⚠️ 이 단계가 2026-07-31 "파일 통째 실패" 사고를 막는다 — 없는 컬럼·오타를 적용 전에 잡는다. `CREATE FUNCTION` 자체는 쓰기라 read-only 로 못 돌린다 → **inner SELECT 본문만** 뽑아 `p_exclude` 를 `'{}'::uuid[]` 로 치환해 돌린다.

`admin_sim_summary` 검증(`scripts/run-prod-query.mjs` 는 read_only 고정 — prod 스키마=dev 스키마라 컬럼 검증엔 충분):

```bash
node scripts/run-prod-query.mjs "WITH sim AS (SELECT rd.id AS id, rd.user_id AS user_id, rd.relationship_id AS relationship_id, coalesce(rd.saju_data->>'funding','paid') AS funding, (rd.saju_data->>'phase') AS phase FROM readings rd WHERE rd.consultation_type='relationship_sim' AND rd.user_id <> ALL('{}'::uuid[])), runway_by_rel AS (SELECT s.relationship_id AS relationship_id, count(*) AS n FROM sim s WHERE s.funding='runway' AND s.relationship_id IS NOT NULL GROUP BY s.relationship_id), sim_rel AS (SELECT DISTINCT s.relationship_id AS relationship_id FROM sim s WHERE s.relationship_id IS NOT NULL), portrait AS (SELECT up.personality AS personality FROM sim_rel sr JOIN relationships r ON r.id=sr.relationship_id JOIN user_profiles up ON up.id=r.partner_profile_id WHERE up.personality IS NOT NULL AND btrim(up.personality)<>'') SELECT (SELECT count(*) FROM sim), (SELECT count(*) FROM sim s WHERE s.funding='paid'), (SELECT count(*) FROM sim s WHERE s.phase='debriefed'), (SELECT count(*) FROM portrait), (SELECT coalesce(round(avg(char_length(p.personality))),0) FROM portrait p)"
```

Expected: 에러 없이 한 행 반환(값은 0 이어도 됨 — dev 에 시뮬 데이터가 적을 수 있다). `column ... does not exist` 가 나오면 SQL 을 고친다.

`admin_slots_summary` 검증:

```bash
node scripts/run-prod-query.mjs "WITH by_user AS (SELECT r.user_id AS user_id, count(*) AS n FROM relationships r WHERE r.user_id <> ALL('{}'::uuid[]) GROUP BY r.user_id) SELECT (SELECT count(*) FROM star_transactions t WHERE t.source='relationship_slot' AND t.type='spend'), (SELECT count(*) FROM by_user b WHERE b.n>=2), (SELECT count(*) FROM by_user b WHERE b.n=1)"
```

Expected: 에러 없이 한 행. `SUPABASE_PAT` 가 로컬에 없으면 이 단계는 사용자에게 실행을 요청한다(쓰기 아님 = 안전).

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260814010000_admin_sim_slots_aggregates.sql
git commit -m "feat(admin): 시뮬·슬롯 집계 RPC (admin_sim_summary·admin_slots_summary)"
```

---

## Task 2: `/admin/relationship` 에 시뮬·슬롯 섹션

**Files:**
- Modify: `app/admin/relationship/page.tsx`

기존 4섹션(등록/스레드·패스·스킬·대화흐름)은 무편집. `load()` 에 RPC 2개 추가 + JSX 끝에 2섹션 추가.

- [ ] **Step 1: 상수 import 추가**

파일 상단 import 블록에 추가:

```tsx
import { SIM_COST, SIM_SUGGEST_COST, SLOT_COST } from "@/lib/relationship/types";
```

- [ ] **Step 2: `load()` 에 RPC 2개 추가**

`load()` 안 `Promise.all` 배열에 두 호출을 추가하고 결과를 구조분해한다. 기존:

```tsx
  const [summaryRes, distRes, flowRes] = await Promise.all([
    supa.rpc("admin_relationship_summary", { p_exclude, p_now, p_week_ago }),
    supa.rpc("admin_relationship_dist", { p_exclude, p_skill_keys: [...SKILL_KEYS] }),
    supa.rpc("admin_relationship_flow", { p_exclude }),
  ]);
```

로 교체:

```tsx
  const [summaryRes, distRes, flowRes, simRes, slotsRes] = await Promise.all([
    supa.rpc("admin_relationship_summary", { p_exclude, p_now, p_week_ago }),
    supa.rpc("admin_relationship_dist", { p_exclude, p_skill_keys: [...SKILL_KEYS] }),
    supa.rpc("admin_relationship_flow", { p_exclude }),
    supa.rpc("admin_sim_summary", { p_exclude }),
    supa.rpc("admin_slots_summary", { p_exclude }),
  ]);
```

- [ ] **Step 3: `load()` 반환에 시뮬·슬롯 파생값 추가**

`load()` 안, 기존 `flowFailed` 계산 근처에 추가:

```tsx
  const simFailed = !!simRes.error;
  const slotsFailed = !!slotsRes.error;
  const sim = (
    (simRes.data ?? []) as {
      total_plays: number; runway_plays: number; hook_plays: number; paid_plays: number;
      debriefed_plays: number; play_users: number; play_rels: number;
      runway_exhausted_rels: number; suggest_purchases: number;
      portrait_rels: number; portrait_avg_len: number;
    }[]
  )[0];
  const slots = (
    (slotsRes.data ?? []) as {
      slot_purchases: number; multi_rel_users: number;
      rels_1: number; rels_2: number; rels_3plus: number;
    }[]
  )[0];
```

`return { ... }` 객체 끝에 추가:

```tsx
    simFailed,
    slotsFailed,
    sim: {
      totalPlays: Number(sim?.total_plays ?? 0),
      runwayPlays: Number(sim?.runway_plays ?? 0),
      hookPlays: Number(sim?.hook_plays ?? 0),
      paidPlays: Number(sim?.paid_plays ?? 0),
      debriefedPlays: Number(sim?.debriefed_plays ?? 0),
      playUsers: Number(sim?.play_users ?? 0),
      playRels: Number(sim?.play_rels ?? 0),
      runwayExhaustedRels: Number(sim?.runway_exhausted_rels ?? 0),
      suggestPurchases: Number(sim?.suggest_purchases ?? 0),
      portraitRels: Number(sim?.portrait_rels ?? 0),
      portraitAvgLen: Number(sim?.portrait_avg_len ?? 0),
    },
    slots: {
      slotPurchases: Number(slots?.slot_purchases ?? 0),
      multiRelUsers: Number(slots?.multi_rel_users ?? 0),
      rels1: Number(slots?.rels_1 ?? 0),
      rels2: Number(slots?.rels_2 ?? 0),
      rels3plus: Number(slots?.rels_3plus ?? 0),
    },
```

- [ ] **Step 4: 컴포넌트에 파생 비율 계산 + 2섹션 JSX 추가**

`AdminRelationshipPage` 안, 기존 `renewRate` 계산 아래에 추가:

```tsx
  const s2 = s.sim;
  const paidRate = s2.totalPlays ? Math.round((s2.paidPlays / s2.totalPlays) * 1000) / 10 : 0;
  const debriefRate = s2.totalPlays ? Math.round((s2.debriefedPlays / s2.totalPlays) * 1000) / 10 : 0;
  const portraitRate = s2.playRels ? Math.round((s2.portraitRels / s2.playRels) * 1000) / 10 : 0;
  const simRevenue = s2.paidPlays * SIM_COST + s2.suggestPurchases * SIM_SUGGEST_COST;
  const slotRevenue = s.slots.slotPurchases * SLOT_COST;
```

기존 마지막 `</section>`(대화 흐름) 다음, 최상위 `</div>` 앞에 두 섹션 추가:

```tsx
      <section>
        <h2 className="text-sm text-white/60 mb-3">연애 시뮬 <span className="text-white/35">(무료 런웨이 → 유료 전환)</span></h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          <Stat label="총 판 수" value={s.simFailed ? "—" : s2.totalPlays} />
          <Stat label="유료 전환율" value={s.simFailed ? "—" : `${paidRate}%`} sub={s.simFailed ? undefined : `유료 ${s2.paidPlays}판`} />
          <Stat label="시뮬 매출(별)" value={s.simFailed ? "—" : simRevenue.toLocaleString()} sub={s.simFailed ? undefined : `판 ${s2.paidPlays}·추천 ${s2.suggestPurchases}`} />
          <Stat label="디브리핑 도달률" value={s.simFailed ? "—" : `${debriefRate}%`} sub={s.simFailed ? undefined : `${s2.debriefedPlays}/${s2.totalPlays}판`} />
          <Stat label="플레이 유저" value={s.simFailed ? "—" : s2.playUsers} sub={s.simFailed ? undefined : `관계 ${s2.playRels}`} />
          <Stat label="런웨이 소진 관계" value={s.simFailed ? "—" : s2.runwayExhaustedRels} sub="무료 3판 다 씀" />
          <Stat label="주간훅 사용 판" value={s.simFailed ? "—" : s2.hookPlays} sub="재방문 케이던스" />
          <Stat label="초상화 축적" value={s.simFailed ? "—" : `${portraitRate}%`} sub={s.simFailed ? undefined : `${s2.portraitRels}/${s2.playRels}관계 · 평균 ${s2.portraitAvgLen}자`} />
        </div>
        <div className="mt-2 text-[12px] text-white/40">
          {s.simFailed ? "" : `자금원 분포 — 무료런웨이 ${s2.runwayPlays} · 주간훅 ${s2.hookPlays} · 유료 ${s2.paidPlays}`}
        </div>
        {s.simFailed && <LoadFailed block="admin_sim_summary" className="mt-2" />}
      </section>

      <section>
        <h2 className="text-sm text-white/60 mb-3">관계 슬롯 <span className="text-white/35">(첫 관계 무료 → 2번째+ 유료)</span></h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          <Stat label="슬롯 구매" value={s.slotsFailed ? "—" : s.slots.slotPurchases} sub={`개당 ${SLOT_COST}별`} />
          <Stat label="슬롯 매출(별)" value={s.slotsFailed ? "—" : slotRevenue.toLocaleString()} />
          <Stat label="다중 관계 유저" value={s.slotsFailed ? "—" : s.slots.multiRelUsers} sub="2명+ 등록 (1:N 실현)" />
          <Stat label="관계 수 분포" value={s.slotsFailed ? "—" : `${s.slots.rels1}·${s.slots.rels2}·${s.slots.rels3plus}`} sub="1명·2명·3명+" />
        </div>
        {s.slotsFailed && <LoadFailed block="admin_slots_summary" className="mt-2" />}
      </section>
```

- [ ] **Step 5: tsc + 기존 유닛 회귀 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 0.

```bash
node --import tsx --test lib/admin-time.test.ts
```

Expected: 기존 통과 유지(이 파일은 admin-time 계약 — 회귀 감시용). 실패 0.

- [ ] **Step 6: 커밋**

```bash
git add app/admin/relationship/page.tsx
git commit -m "feat(admin): /admin/relationship 에 시뮬·슬롯 지표 섹션"
```

---

## Task 3: `relationship-readings/[id]` 에 초상화·시뮬 판 드릴다운

**Files:**
- Modify: `app/admin/relationship-readings/[id]/page.tsx`

기존 스레드 타임라인은 무편집. 관계 select 에 `partner_profile_id` 추가 + 초상화·시뮬 판 쿼리 2개 추가 + 타임라인 아래 2블록 렌더.

- [ ] **Step 1: import 추가**

파일 상단에 추가:

```tsx
import { getSituation } from "@/lib/relationship/situations";
```

`fmt` 아래에 funding 라벨 맵 추가(기존 STATUS_LABEL 패턴 — 인라인):

```tsx
const FUNDING_LABEL: Record<string, string> = { runway: "무료 런웨이", hook: "주간훅 무료", paid: "유료" };
```

- [ ] **Step 2: 관계 select 에 `partner_profile_id` 추가**

기존:

```tsx
    .select("id, user_id, label, status, thread_reading_id, rolling_summary, summarized_msg_count, memo, last_visited_at, created_at")
```

를:

```tsx
    .select("id, user_id, label, status, thread_reading_id, partner_profile_id, rolling_summary, summarized_msg_count, memo, last_visited_at, created_at")
```

- [ ] **Step 3: 초상화·시뮬 판 쿼리 추가**

기존 `Promise.all`(msgs·passes·extendTxs·skillReadings) **다음** 줄에 추가:

```tsx
  const [{ data: partnerProfile }, { data: simPlays }] = await Promise.all([
    rel.partner_profile_id
      ? supabase.from("user_profiles").select("personality").eq("id", rel.partner_profile_id).maybeSingle()
      : Promise.resolve({ data: null as { personality: string | null } | null }),
    supabase
      .from("readings")
      .select("id, saju_data, created_at")
      .eq("relationship_id", id)
      .eq("consultation_type", "relationship_sim")
      .order("created_at", { ascending: true }),
  ]);
  const portrait = partnerProfile?.personality?.trim() || null;
  const plays = (simPlays ?? []) as { id: string; saju_data: Record<string, unknown> | null; created_at: string }[];
```

- [ ] **Step 4: 타임라인 블록 다음에 초상화·시뮬 판 2블록 렌더**

기존 스레드 타임라인 `</div>`(최상위 컨테이너 닫기 직전) 뒤에 추가:

```tsx
      <div className="rounded-lg border border-white/10 bg-white/5 p-3">
        <div className="mb-2 text-xs font-bold text-white/60">초상화 (상대 성격 서술 · 시뮬 관찰 누적)</div>
        {portrait ? (
          <div className="whitespace-pre-wrap text-[13px] text-white/80">{portrait}</div>
        ) : (
          <div className="py-2 text-[13px] text-white/40">아직 없음</div>
        )}
      </div>

      <div className="rounded-lg border border-white/10 bg-white/5 p-3">
        <div className="mb-2 text-xs font-bold text-white/60">시뮬 판 ({plays.length}판)</div>
        {plays.length === 0 && (
          <div className="py-2 text-[13px] text-white/40">시뮬 기록 없음</div>
        )}
        <div className="space-y-2">
          {plays.map((p) => {
            const meta = (p.saju_data ?? {}) as {
              situationId?: string; funding?: string; phase?: string; insight?: string; sendMessage?: string;
            };
            const sit = meta.situationId ? getSituation(meta.situationId) : null;
            const funding = meta.funding ?? "paid";
            return (
              <div key={p.id} className="rounded-lg bg-white/5 p-3 text-sm">
                <div className="mb-1 flex flex-wrap items-center gap-2 text-[11px]">
                  <span className="rounded-full border border-gold/30 bg-gold/10 px-2 py-0.5 text-gold">{FUNDING_LABEL[funding] ?? funding}</span>
                  {sit && <span className="text-white/70">{sit.emoji} {sit.label}</span>}
                  <span className={meta.phase === "debriefed" ? "text-lilac" : "text-white/40"}>
                    {meta.phase === "debriefed" ? "✓ 완주" : "미완주"}
                  </span>
                  <span className="text-white/40">{fmt(p.created_at)}</span>
                  <Link href={`/admin/readings/${p.id}`} className="text-lilac underline">리딩 보기</Link>
                </div>
                {meta.insight && <div className="text-[13px] text-white/70">통찰: {meta.insight}</div>}
                {meta.sendMessage && <div className="mt-0.5 text-[13px] text-white/60">💌 {meta.sendMessage}</div>}
              </div>
            );
          })}
        </div>
      </div>
```

- [ ] **Step 5: tsc 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 0.

- [ ] **Step 6: 커밋**

```bash
git add "app/admin/relationship-readings/[id]/page.tsx"
git commit -m "feat(admin): 관계 상세에 초상화·시뮬 판 드릴다운"
```

---

## Task 4: dev 배포 + 브라우저 렌더 검증

**Files:** 없음 (검증만)

- [ ] **Step 1: dev push (마이그레이션 자동 적용)**

```bash
git push origin dev
```

`dev` push → Supabase dev 에 `20260814010000` 자동 적용. **main Workflow logs SUCCESS 확인 습관**(AGENTS.md). 적용 실패면 어드민 2화면이 없는 RPC 를 호출한다.

- [ ] **Step 2: 어드민 세션 주입 브라우저 렌더**

메모리 `admin-local-verify-trick` 절차: 임시 `ADMIN_USER_IDS` + HMAC 쿠키 주입 후 `/admin/relationship` 열어 새 2섹션(시뮬·슬롯) 렌더 확인. 이어 시뮬 판이 있는 관계의 `/admin/relationship-readings/[id]` 에서 초상화 블록 + 시뮬 판 목록 + "리딩 보기" 링크 동작 확인. **검증 후 임시 ADMIN_USER_IDS 원복 필수.**

Expected: 두 섹션이 숫자로 렌더(dev 데이터가 적으면 0/`—` 가능 — 크래시 없음이 통과 기준). 콘솔 에러 0.

- [ ] **Step 3: 메모리 갱신**

`w1-w7-work-queue` 의 로드맵 ③ 항목을 "어드민 시뮬·슬롯 관측 ✅ dev 완료"로 갱신(커밋 해시 기록). prod(main)은 로드맵 ④.

---

## Self-Review 결과

- **스펙 커버리지**: A①시뮬 8지표 → Task 2 Step 4(8 Stat + 자금원 분포 줄) · A②슬롯 4지표 → Task 2 Step 4(2번째 섹션) · B 초상화+시뮬 판 드릴다운 → Task 3 · RPC 2개+REVOKE 3종 → Task 1 · 범용 뷰어 재사용 → Task 3 Step 4(`/admin/readings/[id]` 링크). 갭 없음.
- **플레이스홀더**: 없음(모든 SQL·TSX 완전 코드).
- **타입 정합**: RPC 반환 컬럼명(snake) ↔ 페이지 구조분해 ↔ `Number()` 래핑 일치. funding 키(runway/hook/paid) ↔ FUNDING_LABEL ↔ SQL coalesce 일치. SimMeta 필드(situationId/funding/phase/insight/sendMessage) ↔ Task 3 렌더 일치.
- **YAGNI**: 라벨 맵 인라인(가짜 lib 안 만듦) · 날짜 창 없음 · 운영 조작 없음. 사용자 지시 "신규 지표 추가만" 준수.
