# 무료상품 어드민 유료화 지표 추가 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** byeoljari·saju-mbti 어드민에 유료화 관점 지표 4종을 추가한다 — 신규/기존 코호트 분해, 순 기여마진, 완료→재방문 리텐션(D1/D7/D30), 결과유형×결제(MBTI).

**Architecture:** 기존 어드민 RPC 패턴(service_role 전용, KST, REVOKE 3종) 그대로. 코호트 resolver를 `(user_id, is_new)`로 확장(DROP+CREATE, 기존 `.map(r=>r.user_id)` 호출부 무영향), retention·type_payment RPC 신설, 페이지는 신규/기존 2열 + 순마진 + 리텐션(+MBTI 유형×결제) 추가.

**Tech Stack:** Next.js 16 RSC, Supabase RPC, 테스트=inline read-only 검증(`scripts/run-prod-query.mjs`, dev `SUPABASE_PROJECT_REF=vtdmxdcetziileynjaxi`).

**선행:** 무료상품 어드민 본체 완료·dev 적용됨(`a17d572`..`d8eabdc`). data-analysis 세션에서 지표 정의·승인 완료.

---

## 설계 요약 (승인됨, data-analysis 기준)

| 지표 | 정의 | 측정 caveat |
|---|---|---|
| **A. 신규/기존 분해** | 신규 = `user_acquisition.utm_source = 이 상품`(공유·초대 획득) / 기존 = 코호트 − 신규. 구매여정을 2열로. | utm first-touch 기준. 익명 완료 후 미가입은 코호트 밖(하한). 바이럴 결제(D)는 신규 열에 흡수. |
| **B. 순 기여마진** | 신규 매출(₩) − 신규 무료별 소모 × `ASSUMED_FREE_STAR_COST_WON`(₩2 가정). | 별당원가 가정치(luna 후 극저 → 순마진≈매출, 그래서 A가 더 결정적). 화면에 "가정 ₩2/별" 명시. |
| **C. 리텐션 D1/D7/D30** | 완료(MBTI)/생성(byeoljari) anon이 이후 **더 늦은 KST 날**에 N일 내 재방문(page_views) 비율. | 관측창 성숙 가드(eligible=action이 N일 전). 같은 기기 하한. |
| **E. 유형×결제 (MBTI만)** | 로그인 완료자 meta.band/palja × payments → 밴드(3)·팔자(16)별 결제율. | 로그인 완료자만(익명 제외, 표본편향)·소표본 → 밴드 우선, 팔자 보조. |

별당원가 상수: 코드에 없음 확인 → `lib/admin.ts`에 `ASSUMED_FREE_STAR_COST_WON = 2` 신설(라벨 표시).

---

## Task 1: 코호트 resolver를 (user_id, is_new)로 확장

**Files:** Create `supabase/migrations/20260829030000_cohort_users_is_new.sql`

기존 `admin_byeoljari_cohort_users`/`admin_saju_mbti_cohort_users`는 `RETURNS TABLE(user_id UUID)`. 반환 컬럼 추가는 CREATE OR REPLACE 불가 → **DROP+CREATE**. `is_new` = 이 유저가 해당 상품 utm으로 **획득**됐나. 기존 페이지 호출부는 `.map(r=>r.user_id)`라 컬럼 추가에 무영향.

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- 코호트 resolver 확장 (2026-08-29) — 신규/기존 분해용 is_new 플래그 추가.
-- is_new = user_acquisition.utm_source = 이 상품 (공유·초대 링크로 신규 획득).
-- 반환 컬럼 추가라 CREATE OR REPLACE 불가 → DROP+CREATE. 기존 페이지는 r.user_id만 읽어 무영향.

DROP FUNCTION IF EXISTS admin_byeoljari_cohort_users(UUID[]);
CREATE OR REPLACE FUNCTION admin_byeoljari_cohort_users(p_exclude UUID[])
RETURNS TABLE (user_id UUID, is_new BOOLEAN)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH member_anons AS (
    SELECT DISTINCT m.member_anon_id AS anon_id
    FROM star_map_members m JOIN star_maps sm ON sm.id = m.map_id
    WHERE m.is_host = false AND m.member_anon_id IS NOT NULL
      AND m.member_anon_id <> sm.creator_anon_id
  ), member_users AS (
    SELECT DISTINCT pv.user_id AS uid
    FROM page_views pv JOIN member_anons ma ON ma.anon_id = pv.anon_id
    WHERE pv.user_id IS NOT NULL AND pv.user_id <> ALL(p_exclude) AND pv.is_bot = false
  ), utm_users AS (
    SELECT DISTINCT ua.user_id AS uid
    FROM user_acquisition ua
    WHERE ua.utm_source = 'byeoljari' AND ua.user_id <> ALL(p_exclude)
  ), cohort AS (
    SELECT uid FROM member_users UNION SELECT uid FROM utm_users
  )
  SELECT c.uid,
         EXISTS(SELECT 1 FROM user_acquisition ua WHERE ua.user_id = c.uid AND ua.utm_source = 'byeoljari')
  FROM cohort c;
$$;

DROP FUNCTION IF EXISTS admin_saju_mbti_cohort_users(UUID[]);
CREATE OR REPLACE FUNCTION admin_saju_mbti_cohort_users(p_exclude UUID[])
RETURNS TABLE (user_id UUID, is_new BOOLEAN)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH utm_users AS (
    SELECT DISTINCT ua.user_id AS uid
    FROM user_acquisition ua
    WHERE ua.utm_source = 'saju_mbti' AND ua.user_id <> ALL(p_exclude)
  ), completed_logged_in AS (
    SELECT DISTINCT e.user_id AS uid
    FROM ui_events e
    WHERE e.event = 'saju_mbti_completed' AND e.user_id IS NOT NULL
      AND e.user_id <> ALL(p_exclude)
  ), completed_anons AS (
    SELECT DISTINCT e.anon_id AS anon_id
    FROM ui_events e
    WHERE e.event = 'saju_mbti_completed' AND e.anon_id IS NOT NULL
  ), anon_bridge AS (
    SELECT DISTINCT pv.user_id AS uid
    FROM page_views pv JOIN completed_anons ca ON ca.anon_id = pv.anon_id
    WHERE pv.user_id IS NOT NULL AND pv.user_id <> ALL(p_exclude) AND pv.is_bot = false
  ), cohort AS (
    SELECT uid FROM utm_users UNION SELECT uid FROM completed_logged_in UNION SELECT uid FROM anon_bridge
  )
  SELECT c.uid,
         EXISTS(SELECT 1 FROM user_acquisition ua WHERE ua.user_id = c.uid AND ua.utm_source = 'saju_mbti')
  FROM cohort c;
$$;

REVOKE EXECUTE ON FUNCTION admin_byeoljari_cohort_users(UUID[]) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION admin_saju_mbti_cohort_users(UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_byeoljari_cohort_users(UUID[]) TO service_role;
GRANT EXECUTE ON FUNCTION admin_saju_mbti_cohort_users(UUID[]) TO service_role;
```

- [ ] **Step 2: 두 본문 read-only 검증**

```bash
set -a; source .env.local 2>/dev/null; set +a
SUPABASE_PROJECT_REF=vtdmxdcetziileynjaxi node scripts/run-prod-query.mjs --sql "
WITH utm_users AS (SELECT DISTINCT ua.user_id AS uid FROM user_acquisition ua WHERE ua.utm_source='saju_mbti'),
completed_logged_in AS (SELECT DISTINCT e.user_id AS uid FROM ui_events e WHERE e.event='saju_mbti_completed' AND e.user_id IS NOT NULL),
completed_anons AS (SELECT DISTINCT e.anon_id AS anon_id FROM ui_events e WHERE e.event='saju_mbti_completed' AND e.anon_id IS NOT NULL),
anon_bridge AS (SELECT DISTINCT pv.user_id AS uid FROM page_views pv JOIN completed_anons ca ON ca.anon_id=pv.anon_id WHERE pv.user_id IS NOT NULL AND pv.is_bot=false),
cohort AS (SELECT uid FROM utm_users UNION SELECT uid FROM completed_logged_in UNION SELECT uid FROM anon_bridge)
SELECT c.uid, EXISTS(SELECT 1 FROM user_acquisition ua WHERE ua.user_id=c.uid AND ua.utm_source='saju_mbti') AS is_new FROM cohort c LIMIT 5;
"
```
Expected: HTTP 200, `(uid, is_new)` 형태(초기엔 빈 결과 가능). byeoljari도 동일 패턴으로 1회.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260829030000_cohort_users_is_new.sql
git commit -m "feat(admin): 코호트 resolver에 is_new(신규 획득) 플래그 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: retention RPC 2종 + type_payment RPC

**Files:** Create `supabase/migrations/20260829040000_admin_free_monetization_rpc.sql`

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- 무료상품 유료화 지표 RPC (2026-08-29) — 리텐션(양쪽) + 결과유형×결제(MBTI).
-- 리텐션: action(완료/생성) anon 이 이후 '더 늦은 KST 날'에 N일 내 재방문(page_views) 했나.
--   eligible = action 이 N일 전(관측창 성숙). returned = eligible ∩ N일내 재방문. 같은 기기 하한.
-- 유형×결제: 로그인 완료자(meta.band/palja) × payments(status=completed). 로그인만=표본편향.

CREATE OR REPLACE FUNCTION admin_saju_mbti_retention(p_exclude UUID[])
RETURNS TABLE (horizon TEXT, eligible BIGINT, returned BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH base AS (
    SELECT e.anon_id AS anon, min(e.created_at) AS action_at
    FROM ui_events e
    WHERE e.event = 'saju_mbti_completed' AND e.anon_id IS NOT NULL
      AND (e.user_id IS NULL OR e.user_id <> ALL(p_exclude))
    GROUP BY e.anon_id
  ), flagged AS (
    SELECT b.anon, b.action_at,
      EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id = b.anon AND pv.is_bot = false
        AND (pv.created_at AT TIME ZONE 'UTC' + interval '9 hours')::date
            > (b.action_at AT TIME ZONE 'UTC' + interval '9 hours')::date
        AND pv.created_at <= b.action_at + interval '1 day') AS r1,
      EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id = b.anon AND pv.is_bot = false
        AND (pv.created_at AT TIME ZONE 'UTC' + interval '9 hours')::date
            > (b.action_at AT TIME ZONE 'UTC' + interval '9 hours')::date
        AND pv.created_at <= b.action_at + interval '7 days') AS r7,
      EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id = b.anon AND pv.is_bot = false
        AND (pv.created_at AT TIME ZONE 'UTC' + interval '9 hours')::date
            > (b.action_at AT TIME ZONE 'UTC' + interval '9 hours')::date
        AND pv.created_at <= b.action_at + interval '30 days') AS r30
    FROM base b
  )
  SELECT 'd1', count(*) FILTER (WHERE action_at <= now() - interval '1 day')::BIGINT,
              count(*) FILTER (WHERE action_at <= now() - interval '1 day' AND r1)::BIGINT FROM flagged
  UNION ALL
  SELECT 'd7', count(*) FILTER (WHERE action_at <= now() - interval '7 days')::BIGINT,
              count(*) FILTER (WHERE action_at <= now() - interval '7 days' AND r7)::BIGINT FROM flagged
  UNION ALL
  SELECT 'd30', count(*) FILTER (WHERE action_at <= now() - interval '30 days')::BIGINT,
              count(*) FILTER (WHERE action_at <= now() - interval '30 days' AND r30)::BIGINT FROM flagged;
$$;

CREATE OR REPLACE FUNCTION admin_byeoljari_retention(p_exclude UUID[])
RETURNS TABLE (horizon TEXT, eligible BIGINT, returned BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH base AS (
    SELECT sm.creator_anon_id AS anon, min(sm.created_at) AS action_at
    FROM star_maps sm
    WHERE sm.creator_anon_id IS NOT NULL
      AND (sm.owner_user_id IS NULL OR sm.owner_user_id <> ALL(p_exclude))
    GROUP BY sm.creator_anon_id
  ), flagged AS (
    SELECT b.anon, b.action_at,
      EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id = b.anon AND pv.is_bot = false
        AND (pv.created_at AT TIME ZONE 'UTC' + interval '9 hours')::date
            > (b.action_at AT TIME ZONE 'UTC' + interval '9 hours')::date
        AND pv.created_at <= b.action_at + interval '1 day') AS r1,
      EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id = b.anon AND pv.is_bot = false
        AND (pv.created_at AT TIME ZONE 'UTC' + interval '9 hours')::date
            > (b.action_at AT TIME ZONE 'UTC' + interval '9 hours')::date
        AND pv.created_at <= b.action_at + interval '7 days') AS r7,
      EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id = b.anon AND pv.is_bot = false
        AND (pv.created_at AT TIME ZONE 'UTC' + interval '9 hours')::date
            > (b.action_at AT TIME ZONE 'UTC' + interval '9 hours')::date
        AND pv.created_at <= b.action_at + interval '30 days') AS r30
    FROM base b
  )
  SELECT 'd1', count(*) FILTER (WHERE action_at <= now() - interval '1 day')::BIGINT,
              count(*) FILTER (WHERE action_at <= now() - interval '1 day' AND r1)::BIGINT FROM flagged
  UNION ALL
  SELECT 'd7', count(*) FILTER (WHERE action_at <= now() - interval '7 days')::BIGINT,
              count(*) FILTER (WHERE action_at <= now() - interval '7 days' AND r7)::BIGINT FROM flagged
  UNION ALL
  SELECT 'd30', count(*) FILTER (WHERE action_at <= now() - interval '30 days')::BIGINT,
              count(*) FILTER (WHERE action_at <= now() - interval '30 days' AND r30)::BIGINT FROM flagged;
$$;

CREATE OR REPLACE FUNCTION admin_saju_mbti_type_payment(p_exclude UUID[])
RETURNS TABLE (dim TEXT, key TEXT, completers BIGINT, payers BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH comp AS (
    SELECT DISTINCT e.user_id AS uid, e.meta->>'band' AS band, e.meta->>'palja' AS palja
    FROM ui_events e
    WHERE e.event = 'saju_mbti_completed' AND e.user_id IS NOT NULL
      AND e.user_id <> ALL(p_exclude)
  ), payers AS (
    SELECT DISTINCT p.user_id AS uid FROM payments p
    WHERE p.status = 'completed' AND p.user_id IS NOT NULL
  )
  SELECT 'band', c.band, count(DISTINCT c.uid)::BIGINT,
         count(DISTINCT c.uid) FILTER (WHERE c.uid IN (SELECT uid FROM payers))::BIGINT
  FROM comp c WHERE c.band IS NOT NULL GROUP BY c.band
  UNION ALL
  SELECT 'palja', c.palja, count(DISTINCT c.uid)::BIGINT,
         count(DISTINCT c.uid) FILTER (WHERE c.uid IN (SELECT uid FROM payers))::BIGINT
  FROM comp c WHERE c.palja IS NOT NULL GROUP BY c.palja
  ORDER BY 1, 4 DESC;
$$;

REVOKE EXECUTE ON FUNCTION admin_saju_mbti_retention(UUID[]) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION admin_byeoljari_retention(UUID[]) FROM PUBLIC, anon, authenticated;
REVOKE EXECUTE ON FUNCTION admin_saju_mbti_type_payment(UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_saju_mbti_retention(UUID[]) TO service_role;
GRANT EXECUTE ON FUNCTION admin_byeoljari_retention(UUID[]) TO service_role;
GRANT EXECUTE ON FUNCTION admin_saju_mbti_type_payment(UUID[]) TO service_role;
```

- [ ] **Step 2: 세 함수 본문 전부 read-only 검증** (적용 전 필수 — 하나라도 에러면 파일 전체 실패)

```bash
set -a; source .env.local 2>/dev/null; set +a
# retention (mbti) — 본문 인라인
SUPABASE_PROJECT_REF=vtdmxdcetziileynjaxi node scripts/run-prod-query.mjs --sql "
WITH base AS (SELECT e.anon_id AS anon, min(e.created_at) AS action_at FROM ui_events e WHERE e.event='saju_mbti_completed' AND e.anon_id IS NOT NULL GROUP BY e.anon_id),
flagged AS (SELECT b.anon, b.action_at, EXISTS(SELECT 1 FROM page_views pv WHERE pv.anon_id=b.anon AND pv.is_bot=false AND (pv.created_at AT TIME ZONE 'UTC'+interval '9 hours')::date>(b.action_at AT TIME ZONE 'UTC'+interval '9 hours')::date AND pv.created_at<=b.action_at+interval '7 days') AS r7 FROM base b)
SELECT 'd7', count(*) FILTER (WHERE action_at<=now()-interval '7 days'), count(*) FILTER (WHERE action_at<=now()-interval '7 days' AND r7) FROM flagged;
"
# retention (byeoljari) — creator_anon_id 존재 확인 포함
SUPABASE_PROJECT_REF=vtdmxdcetziileynjaxi node scripts/run-prod-query.mjs --sql "
SELECT count(DISTINCT sm.creator_anon_id) FROM star_maps sm WHERE sm.creator_anon_id IS NOT NULL;
"
# type_payment (mbti)
SUPABASE_PROJECT_REF=vtdmxdcetziileynjaxi node scripts/run-prod-query.mjs --sql "
WITH comp AS (SELECT DISTINCT e.user_id AS uid, e.meta->>'band' AS band FROM ui_events e WHERE e.event='saju_mbti_completed' AND e.user_id IS NOT NULL), payers AS (SELECT DISTINCT p.user_id AS uid FROM payments p WHERE p.status='completed' AND p.user_id IS NOT NULL)
SELECT 'band', c.band, count(DISTINCT c.uid), count(DISTINCT c.uid) FILTER (WHERE c.uid IN (SELECT uid FROM payers)) FROM comp c WHERE c.band IS NOT NULL GROUP BY c.band;
"
```
Expected: 3개 모두 HTTP 200, 에러 0. (초기 표본 작아 빈/0 결과 정상.) `star_maps.creator_anon_id` 컬럼 존재가 두 번째로 증명됨.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260829040000_admin_free_monetization_rpc.sql
git commit -m "feat(admin): 무료상품 리텐션(D1/D7/D30) + 유형×결제 RPC

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: MBTI 페이지 — 신규/기존 2열 + 순마진 + 리텐션 + 유형×결제

**Files:** Modify `lib/admin.ts` (상수 추가) · Modify `app/admin/free/saju-mbti/page.tsx`

- [ ] **Step 1: 별당원가 상수 추가**

`lib/admin.ts`에 추가(export):
```ts
/** 순 기여마진 계산용 무료별 변동원가 가정치(₩/별). luna 후 극저라 보수적. 화면에 "가정" 라벨 표시. */
export const ASSUMED_FREE_STAR_COST_WON = 2;
```

- [ ] **Step 2: page.tsx `load()` 개편 — 신규/기존 분해 + 순마진 + 리텐션 + 유형×결제**

`load()`에서 기존 cohort 처리(현재 `cohort = (cohortRes.data ?? []).map(r=>r.user_id)`)를 신규/기존 분리로 바꾸고 RPC를 추가한다. import에 `ASSUMED_FREE_STAR_COST_WON` 추가(`@/lib/admin`). 기존 `cohortRes = admin_saju_mbti_cohort_users` 결과가 이제 `{user_id, is_new}[]`.

```ts
  const cohortRows = (cohortRes.data ?? []) as { user_id: string; is_new: boolean }[];
  const newIds = cohortRows.filter((r) => r.is_new).map((r) => r.user_id);
  const oldIds = cohortRows.filter((r) => !r.is_new).map((r) => r.user_id);
  const cohort = cohortRows.map((r) => r.user_id); // 전체(리텐션 표시용 코호트 규모 등 기존 용도 유지)

  const [payNewRes, payOldRes, spendNewRes, retRes, typeRes] = await Promise.all([
    newIds.length ? supa.rpc("admin_cohort_payments", { p_users: newIds }) : Promise.resolve({ data: [], error: null }),
    oldIds.length ? supa.rpc("admin_cohort_payments", { p_users: oldIds }) : Promise.resolve({ data: [], error: null }),
    newIds.length ? supa.rpc("admin_star_spend_breakdown", { p_since: PURCHASE_FLOOR, p_until: null, p_exclude, p_fortune_types, p_users: newIds }) : Promise.resolve({ data: [], error: null }),
    supa.rpc("admin_saju_mbti_retention", { p_exclude }),
    supa.rpc("admin_saju_mbti_type_payment", { p_exclude }),
  ]);
```
그리고 기존 단일 `payRes`/`spendRes` 사용부를 제거하고, return 객체를 아래로 교체(신규/기존 결제, 신규 무료별 소모, 리텐션, 유형×결제):
```ts
  const sumPay = (rows: unknown) => {
    const a = (rows ?? []) as { payers: number; revenue_won: number }[];
    return { payers: a.reduce((s, p) => s + Number(p.payers), 0), revenue: a.reduce((s, p) => s + Number(p.revenue_won), 0) };
  };
  const newPay = sumPay(payNewRes.data), oldPay = sumPay(payOldRes.data);
  const newFreeStars = ((spendNewRes.data ?? []) as { free_stars: number }[]).reduce((s, r) => s + Number(r.free_stars), 0);
  const retRows = (retRes.data ?? []) as { horizon: string; eligible: number; returned: number }[];
  const typeRows = (typeRes.data ?? []) as { dim: string; key: string; completers: number; payers: number }[];
```
return 에 추가:
```ts
    newN: newIds.length, oldN: oldIds.length,
    newPay, oldPay, newFreeStars,
    payFailed: !!payNewRes.error || !!payOldRes.error, spendFailed: !!spendNewRes.error,
    retFailed: !!retRes.error, typeFailed: !!typeRes.error,
    ret: Object.fromEntries(retRows.map((r) => [r.horizon, { eligible: Number(r.eligible), returned: Number(r.returned) }])) as Record<string, { eligible: number; returned: number }>,
    band: typeRows.filter((r) => r.dim === "band").map((r) => ({ key: r.key, completers: Number(r.completers), payers: Number(r.payers) })),
    paljaPay: typeRows.filter((r) => r.dim === "palja").map((r) => ({ key: r.key, completers: Number(r.completers), payers: Number(r.payers) })),
```
⚠️ 기존 ⑥ 섹션이 쓰던 `s.pay`/`s.spend`/`s.cohortN` 참조는 아래 Step 3에서 교체하므로, return 에서 옛 `pay`/`spend` 필드는 제거하고 `cohortN: cohort.length`는 유지.

- [ ] **Step 3: ⑥ 구매 여정 섹션 교체 + ⑦ 유형×결제 신설**

⑥ 섹션 내용을 신규/기존 2열 + 순마진 + 리텐션으로 교체. (신규 순마진 = `newPay.revenue - newFreeStars * ASSUMED_FREE_STAR_COST_WON`.) `Stat` 재사용. band 결제율은 `BarRow` 재사용 가능(`code=밴드, value=결제율%`). 구체 JSX:
```tsx
      <section>
        <h2 className="text-sm text-white/60 mb-3">⑥ 구매 여정 <span className="text-white/35">(신규=공유·초대 획득 / 기존=이미 유저)</span></h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          <Stat label="신규 결제자" value={s.payFailed ? "—" : s.newPay.payers} sub={s.payFailed ? undefined : `신규 ${s.newN}명 중`} />
          <Stat label="신규 매출(원)" value={s.payFailed ? "—" : s.newPay.revenue.toLocaleString()} />
          <Stat label="기존 결제자" value={s.payFailed ? "—" : s.oldPay.payers} sub={s.payFailed ? undefined : `기존 ${s.oldN}명 중`} />
          <Stat label="기존 매출(원)" value={s.payFailed ? "—" : s.oldPay.revenue.toLocaleString()} />
        </div>
        {s.payFailed && <LoadFailed block="admin_cohort_payments" className="mt-2" />}
        <div className="grid grid-cols-2 md:grid-cols-3 gap-3 mt-3">
          <Stat label="신규 무료별 소모" value={s.spendFailed ? "—" : s.newFreeStars} sub="개" />
          <Stat label="신규 순 기여마진(원)" value={s.spendFailed || s.payFailed ? "—" : (s.newPay.revenue - s.newFreeStars * ASSUMED_FREE_STAR_COST_WON).toLocaleString()} sub={`매출−무료별×₩${ASSUMED_FREE_STAR_COST_WON} 가정`} />
        </div>
        {s.spendFailed && <LoadFailed block="admin_star_spend_breakdown" className="mt-2" />}
        <h3 className="text-[13px] text-white/50 mt-4 mb-2">완료→재방문 리텐션 <span className="text-white/30">(같은 기기 하한 · 성숙분모)</span></h3>
        {s.retFailed ? (
          <LoadFailed block="admin_saju_mbti_retention" />
        ) : (
          <div className="grid grid-cols-3 gap-3">
            {["d1", "d7", "d30"].map((h) => {
              const r = s.ret[h] ?? { eligible: 0, returned: 0 };
              const pctv = r.eligible ? Math.round((r.returned / r.eligible) * 1000) / 10 : 0;
              return <Stat key={h} label={h.toUpperCase()} value={`${pctv}%`} sub={`${r.returned}/${r.eligible}`} />;
            })}
          </div>
        )}
      </section>

      <section>
        <h2 className="text-sm text-white/60 mb-3">⑦ 유형 × 결제 <span className="text-white/35">(로그인 완료자만 · 표본편향 · 밴드 우선)</span></h2>
        {s.typeFailed ? (
          <LoadFailed block="admin_saju_mbti_type_payment" />
        ) : (
          <div className="space-y-4">
            <div>
              <h3 className="text-[13px] text-white/50 mb-2">밴드별 결제율</h3>
              {s.band.length ? ["천명", "절충", "거스름"].map((k) => {
                const row = s.band.find((b) => b.key === k) ?? { completers: 0, payers: 0 };
                const rate = row.completers ? Math.round((row.payers / row.completers) * 1000) / 10 : 0;
                return <BarRow key={k} code={k} value={rate} max={100} color="bg-gold-soft" />;
              }) : <div className="text-[12px] text-white/40">데이터 없음</div>}
            </div>
            <div>
              <h3 className="text-[13px] text-white/50 mb-2">팔자 유형별 결제율 <span className="text-white/30">(참고, 소표본)</span></h3>
              {s.paljaPay.length ? (
                <div className="space-y-1.5">
                  {s.paljaPay.map((r) => {
                    const rate = r.completers ? Math.round((r.payers / r.completers) * 1000) / 10 : 0;
                    return <BarRow key={r.key} code={r.key} meta={TYPE_CONTENT[r.key]?.character} value={rate} max={100} color="bg-lilac-mid" />;
                  })}
                </div>
              ) : <div className="text-[12px] text-white/40">데이터 없음</div>}
            </div>
          </div>
        )}
      </section>
```
⚠️ `BarRow`는 `value/max`로 폭을 그리므로 결제율(%)엔 `max={100}`. 카운트(`n/n`)는 `sub`이 아니라 별도 — 여기선 rate만 막대로, 표본 수는 필요 시 후속. (BarRow 우측 숫자는 rate 값이 됨 — 허용.)

- [ ] **Step 4: tsc + build**

Run: `npx tsc --noEmit && npx next build` → exit 0, `/admin/free/saju-mbti` 라우트 유지.

- [ ] **Step 5: 커밋**

```bash
git add lib/admin.ts app/admin/free/saju-mbti/page.tsx
git commit -m "feat(admin): MBTI 구매여정 신규/기존 분해+순마진+리텐션+유형×결제

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: byeoljari 페이지 — 신규/기존 2열 + 순마진 + 리텐션

**Files:** Modify `app/admin/free/byeoljari/page.tsx`

byeoljari는 유형×결제 없음(결과 유형 개념 없음). ④결제→구매여정 섹션에 신규/기존 2열 + 순마진 + 리텐션 추가. Task 3의 load() 패턴을 동일 적용(단 type_payment 없음, retention은 `admin_byeoljari_retention`).

- [ ] **Step 1: load() 개편** — Task 3 Step 2와 동일하게 `cohortRes.data`를 `{user_id,is_new}[]`로 받아 newIds/oldIds 분리, `admin_cohort_payments`(신규/기존), `admin_star_spend_breakdown`(신규), `admin_byeoljari_retention` 호출. import `ASSUMED_FREE_STAR_COST_WON`. 기존 단일 pay/spend 처리 제거.

- [ ] **Step 2: 섹션 교체** — 기존 "④ 결제 → 구매 여정"의 패키지/소비 블록 다음(또는 대체)에 Task 3 Step 3의 ⑥ 블록(신규/기존 4 Stat + 순마진 + 리텐션)을 이식. block 이름은 `admin_byeoljari_retention`. 기존 별 패키지 분포/운세·타로 소비 블록은 유지(신규 코호트 기준으로 바꾸려면 `p_users=newIds`로; 단순화를 위해 이번엔 **전체 코호트 유지 + 신규/기존 매출 요약만 추가**해도 됨 — 구현자 판단, 단 순마진은 신규 기준).

- [ ] **Step 3: tsc + build** → exit 0, byeoljari 라우트 유지.

- [ ] **Step 4: 커밋**

```bash
git add app/admin/free/byeoljari/page.tsx
git commit -m "feat(admin): 별자리 구매여정 신규/기존 분해+순마진+리텐션

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 통합 검증 (push 후)

- [ ] **Step 1: push** (사용자 승인 후) `git push origin dev` → Supabase dev 마이그 2개 자동 적용.
- [ ] **Step 2: ACL 감사** — 신규/변경 함수 6개(cohort_users ×2 재생성, retention ×2, type_payment, +기존) 전부 `{postgres, service_role}`. cohort_users는 5-arg 아님 확인(1-arg UUID[]).
- [ ] **Step 3: PostgREST 검증** (service_role 직접 호출, getServiceSupabase 경로 재현): `admin_saju_mbti_cohort_users`(2컬럼 반환), `admin_saju_mbti_retention`, `admin_byeoljari_retention`, `admin_saju_mbti_type_payment` 각 200. 기존 페이지 회귀(`.map(r=>r.user_id)` 여전히 동작).
- [ ] **Step 4: 렌더/E2E** — 로컬 pane 제약 시 PostgREST 결과 shape 확인으로 대체(어드민 auth 로컬 불가). 두 페이지 tsc/build clean 재확인.

---

## Self-Review

- **커버리지**: A→Task1(resolver is_new)+Task3/4(2열) · B→Task3 Step1(상수)+Step3(순마진) · C→Task2(retention RPC)+Task3/4(표시) · E→Task2(type_payment)+Task3(⑦). 갭 없음.
- **Placeholder**: 없음(전 SQL·JSX·명령 실코드). Task4는 Task3 패턴 참조 + block명만 다름(구현자 이식).
- **타입/시그니처 정합**: resolver `(user_id, is_new)` ↔ 페이지 `r.is_new` 분리. retention `(horizon, eligible, returned)` ↔ `s.ret[h]`. type_payment `(dim, key, completers, payers)` ↔ band/paljaPay. `admin_cohort_payments(p_users)`·`admin_star_spend_breakdown(...,p_users)` 재사용 시그니처 일치.
- **함정 방어**: resolver DROP+CREATE(반환컬럼 변경) + 기존 `.map(r.user_id)` 무영향 확인 · retention KST date + 성숙분모 + is_bot · event IN/= (LIKE 금지) · 적용 전 전 본문 실행 · REVOKE 3종.
