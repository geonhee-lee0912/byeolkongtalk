# 별마루 P5-3 — 허브 재조립 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루 허브(`/byeolmaru`)를 **달력 판이 히어로인 화면**으로 다시 짠다 — 라이트 B 비주얼 · 오늘 히어로(골드 타일) · 인연 칩 통합 · 무료 목록 3종(2탭 동형) · 상태별 4화면.

**Architecture:** 기능은 전부 보존하고 **화면·정보구조·동선만** 바꾼다. 지금 허브는 `rounded-2xl bg-cream-warm` 박스를 `space-y-4` 로 쌓은 평평한 화면인데, **하나의 달력 판**을 강조점으로 세우고 나머지를 그 아래로 내린다. 허브에 인라인으로 박혀 있던 `DailyCardBlock`(347줄)은 전용 라우트 `/byeolmaru/tarot` 로 옮겨 목록의 한 줄이 진입점이 된다. 격자에서 오늘이 아닌 날을 누르면 `/byeolmaru/saju?date=` 로 간다.

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript strict · Tailwind v4 `@theme` · `node --test`(tsx)

---

## 스코프

**이 플랜이 건드리는 것:** `/byeolmaru` · `/byeolmaru/saju` · `/byeolmaru/tarot`(신설) · `components/byeolmaru/*`

**🚫 건드리지 않는 것 (스펙 스코프 — "홈·2탭·마이는 건드리지 않는다"):**
- `app/fortune/*` · `components/fortune/*` · `lib/fortune/types.ts` — 2탭 카드를 **공통 컴포넌트로 추출하지 않는다.** 별마루 목록은 시각만 복제한다(아래 T5 근거)
- `app/page.tsx`(홈) · `app/mypage/*`
- 판정 엔진(`day-score.ts`·`pair-day.ts`) · 무료선(`splitByFreeLine`) · 라우트 응답 계약 — P5-2 결과를 그대로 쓴다

**다음 단계 몫:**
- **P5-4** 페이월 — 맥락형 미끼 3자리의 **내용**·톤/CTA·가격 2단·타이밍 제한·`meta.slot` 계측. 이 플랜은 **자리만** 잡는다(T7)
- **P5-5** 우리 오늘 분량(무료 350자/유료 1,200자) + `pair-narrative` 일별 캐시

## 사전 지식

- 테스트 전체: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")` · 개별: `node --import tsx --test <파일>`
- 타입체크 `npx tsc --noEmit` · 빌드 `npm run build`
- 🔴 **`npm run dev` 를 Bash 로 돌리지 마라** — 다른 세션의 dev 서버가 포트 3000 에 떠 있을 수 있다. Browser pane 의 `navigate` 로 `http://localhost:3000` 에 먼저 붙어보고, 안 되면 `preview_start` 를 쓴다
- 🔴 **이 리포에서 다른 세션이 동시에 커밋할 수 있다.** `git add` 를 쓰지 말고 **pathspec 커밋**(`git commit <경로> -m "..."`)을 쓰고, 커밋 직후 `git show --stat HEAD` 로 내 파일만 들어갔는지 확인할 것
- 디자인 토큰 정본은 `app/globals.css` 의 `@theme`: `cream #FAF6F0` · `cream-warm #FFF8F0` · `lilac-soft #E8DEF5` · `lilac #D4C7EE` · `lilac-mid #B8A8D8` · `lilac-deep #9F8AD0` · `gold #E8C26A` · `gold-soft #F2D78A` · `eye-purple #5A3E8C` · `text-light #7A6BA0` · `night #1F1735`
  - **새 `@theme` 토큰을 만들지 않는다.** 스펙 §4 가 직접 준 hex(판 배경 `#FFFBF2`→`#EFE6FA`, 히어로 `#FFF3D6`→`#F2D78A`)는 **컴포넌트 파일 안의 상수**로 둔다 — `components/saju/SajuBoard` 의 `ELEMENT_COLORS` 가 같은 선례다
- 주석은 "왜"를 남기는 밀도 높은 한글. 커밋 메시지도 한글(`feat(byeolmaru):` / `fix(byeolmaru):`)

## 현재 상태 (P5-2 직후)

`components/byeolmaru/ByeolmaruHub.tsx` 가 이 순서로 렌더한다:
1. `<header>` 별마루 + 오늘 간지
2. `<AttendanceStrip attendance filledDays entitled />`
3. `<Link href="/byeolmaru/saju">` 오늘 사주 카드 — 십신 이름 + 등급 + **7점 스트립(`strip7`)** + `DAY_LINE` + "이번 달 전체 보기 →"
4. `<DailyCardBlock entitled trialUsed onStartTrial onSubscribe />` — 오늘의 카드(인라인 347줄)
5. 2열 그리드 3칸 — 우리 오늘 / 사주 MBTI / 별 인연 지도
6. `<CrossSellCard item={crossSell} />`

상태 분기는 `loading` / `need_login`(=`GuestPeek`, 카드 그리드 5장) / `no_profile`(생일 입력 CTA) / `error` / `ready`.

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `components/byeolmaru/CalendarGrid.tsx` (수정) | 라이트 B 판 비주얼 — 판 배경·셀 색·오늘 강조. self·pair 공용이라 **두 탭이 같은 판**을 쓴다 | T1 |
| `components/byeolmaru/TodayHeroTile.tsx` (신규) | 오늘 히어로 골드 타일 — 간지·일지캐릭터·하루 이름·마크·한 줄·"자세히 보기 →" | T2 |
| `components/byeolmaru/SajuTodayView.tsx` (수정) | `?date=` 초기 선택 지원 | T3 |
| `app/byeolmaru/saju/page.tsx` (수정) | `useSearchParams` 를 쓰는 자식을 `<Suspense>` 로 감싼다 | T3 |
| `app/byeolmaru/tarot/page.tsx` (신규) | 오늘 타로 전용 라우트 | T4 |
| `components/byeolmaru/DailyCardBlock.tsx` (수정) | 허브 인라인 → 전용 페이지용. 자격 props 를 자체 조회로 | T4 |
| `components/byeolmaru/FreeList.tsx` (신규) | 무료 목록 3종 — 2탭 카드 시각 복제 + 좌측 4px 바 | T5 |
| `components/byeolmaru/PartnerChips.tsx` (신규) | 허브 인연 칩 — `SubjectToggle` 을 관계 색·"＋ 인연 걸어두기"로 확장 | T6 |
| `components/byeolmaru/ByeolmaruHub.tsx` (수정) | 판 조립 + 목록 + 낙수 + 미끼 자리 + 상태별 화면 | T6·T7·T8 |

---

### Task 1: 달력 판 비주얼 — 라이트 B

스펙 §4. 지금 격자는 `bg-cream-warm` 카드 위에 `gold-soft`/`lilac-soft`/`cream-warm` 셀이라 **판과 셀의 명도 차가 거의 없다**. 라이트로 가면서 잃은 "발광" 효과를 판 안의 명암으로 보상한다.

**Files:**
- Modify: `components/byeolmaru/CalendarGrid.tsx`

- [ ] **Step 1: 색 상수를 파일 상단에 둔다**

현재 `TONE_BG` · `TONE_RING` 정의를 찾아 아래로 교체한다(둘 다 지우고 이걸 넣는다):

```tsx
// 🔴 라이트 B 팔레트(스펙 §4) — @theme 토큰에 없는 값은 여기 상수로 둔다. 새 토큰을 만들지
//    않는 이유: 이 색들은 "달력 판 안에서만" 쓰는 국소 팔레트라 전역 토큰으로 올리면 다른 지면이
//    실수로 집어 쓴다(ELEMENT_COLORS 가 SajuBoard 안에 사는 것과 같은 이유).
// 판 안에 명암을 만드는 게 핵심이다 — 무난한 날이 순백이라 좋은 날(골드 솔리드)이 떠 보인다.
const PANEL_BG = "linear-gradient(160deg, #FFFBF2 0%, #EFE6FA 100%)";
const PANEL_BORDER = "1px solid rgba(184,168,216,.35)";
const PANEL_SHADOW = "0 4px 18px rgba(159,138,208,0.10)";

const TONE_STYLE: Record<DayTone, { background: string; border?: string; boxShadow?: string }> = {
  good: { background: "#E8C26A", boxShadow: "0 2px 6px rgba(232,194,106,0.45)" },
  normal: { background: "#ffffff" },
  caution: { background: "rgba(255,255,255,.45)", border: "1px solid rgba(184,168,216,.30)" },
};
```

- [ ] **Step 2: 판(바깥 컨테이너)에 라이트 B 배경을 입힌다**

바깥 `<div className="rounded-2xl bg-cream-warm p-3">` 를 아래로 교체:

```tsx
    <div
      className="rounded-2xl p-3"
      style={{ background: PANEL_BG, border: PANEL_BORDER, boxShadow: PANEL_SHADOW }}
    >
```

- [ ] **Step 3: 열린 셀의 색을 교체하고 오늘을 띄운다**

열린 셀 `<button>` 의 `className` 에서 `${TONE_BG[c.tone]}` 과 ring 삼항을 **빼고**, 대신 `style` 로 넘긴다. `className` 은 레이아웃만 남긴다:

```tsx
              className="relative flex aspect-square flex-col items-center justify-center rounded-xl transition-transform"
              style={{
                ...TONE_STYLE[c.tone],
                // 🔴 오늘/선택 링을 **하나의 boxShadow 문자열**로 합친다. 클래스 ring 을 겹쳐 쓰면
                //    승자가 스타일시트 순서로 갈려 caution 톤의 오늘 칸이 링을 잃던 버그가 있었다
                //    (P5-1 주석). 인라인 스타일이면 우선순위가 코드 순서로 결정돼 그 문제가 없다.
                ...(c.isToday
                  ? { boxShadow: `0 0 0 2px #5A3E8C${TONE_STYLE[c.tone].boxShadow ? `, ${TONE_STYLE[c.tone].boxShadow}` : ""}`, transform: "scale(1.07)", zIndex: 1 }
                  : selected
                    ? { boxShadow: `0 0 0 2px rgba(159,138,208,.75)` }
                    : {}),
              }}
```

⚠️ `transform: scale(1.07)` 이 격자 간격을 먹는다. `gap-1` 이 4px 인데 42px 셀의 7% 는 약 3px 이라 겹치지 않는다 — **375px 에서 눈으로 확인할 것**(Step 6).

- [ ] **Step 4: 안 온 날 칸을 스펙 값으로 맞춘다**

잠긴 칸 `<div>` 의 `className` 에서 `bg-white/30` 을 빼고 `style` 을 준다:

```tsx
                className="flex aspect-square flex-col items-center justify-center rounded-xl border border-dashed"
                style={{ background: "rgba(255,255,255,.28)", borderColor: "rgba(184,168,216,.40)" }}
```

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: 출력 없음. `DayTone` import 가 이미 있는지 확인하고, 없으면 `import type { DayTone } from "@/lib/byeolmaru/day-score";` 를 추가한다.

- [ ] **Step 6: 브라우저 실렌더 — 판 안의 명암을 눈으로 확인**

`/byeolmaru/saju` 를 375px(`resize_window` mobile)로 연다. 확인할 것:
1. 판 배경이 좌상→우하 크림→연보라 그라데이션이다
2. **무난한 날이 순백이라 판 위에 떠 보이고**, 잘 맞는 날이 골드 솔리드로 가장 강하다
3. 오늘 칸이 보라 링 + 살짝 커져 있고 **이웃 칸과 겹치지 않는다**
4. 챙길 날이 반투명이라 가장 약하다
5. 안 온 날(점선)이 여전히 구분된다

스크린샷을 남길 것.

- [ ] **Step 7: 커밋**

```bash
git commit components/byeolmaru/CalendarGrid.tsx -m "feat(byeolmaru): 달력 판 라이트 B 비주얼 — 판 그라데이션·순백 무난·골드 좋은날 (P5-3)"
git show --stat HEAD
```

---

### Task 2: 오늘 히어로 타일

스펙 §3·§4. 골드 타일로 **배경에 녹이지 않고 띄운다.**

**Files:**
- Create: `components/byeolmaru/TodayHeroTile.tsx`

- [ ] **Step 1: 컴포넌트를 만든다**

```tsx
"use client";

// components/byeolmaru/TodayHeroTile.tsx — 달력 판 안의 오늘 히어로(스펙 §3·§4).
// 🔴 배경에 녹이지 않고 **골드 타일로 띄운다**. 라이트 판에선 이래야 오늘이 산다(다크 판이었다면
//    어둠 위 발광으로 충분했겠지만 §4 가 라이트를 택했다).
import Image from "next/image";
import Link from "next/link";
import type { DayCell } from "@/lib/byeolmaru/calendar";
import { DAY_NAME, DAY_LINE } from "@/lib/byeolmaru/day-label";
import { branchAnimal } from "@/lib/byeolmaru/branch-animal";

const TILE_BG = "linear-gradient(135deg, #FFF3D6 0%, #F2D78A 100%)";
const TILE_SHADOW = "0 3px 14px rgba(232,194,106,0.40)";

export default function TodayHeroTile({ cell, href }: { cell: DayCell; href: string }) {
  const animal = branchAnimal(cell.ganji);
  return (
    <Link
      href={href}
      className="block rounded-2xl p-3.5"
      style={{ background: TILE_BG, boxShadow: TILE_SHADOW }}
    >
      <div className="flex items-center gap-3">
        {animal ? (
          <Image src={animal.assetSrc} alt={animal.animal} width={48} height={48} className="h-12 w-12 shrink-0 object-contain" />
        ) : null}
        <div className="min-w-0 flex-1">
          <p className="text-[11px] font-medium text-eye-purple/70">오늘 · {cell.ganji}</p>
          {/* 🔴 이름과 등급을 형제 span 으로 분리한다 — 등급을 이름 span 안에 중첩하면 font-display
              가 상속돼 본문이 타이틀 폰트로 렌더된다(text-xs 는 크기만 덮고 font-family 는 못 덮는다).
              좌우 배치(justify-between)도 쓰지 않는다: 375px 에서 이름×등급 30개 조합 중 12개가
              줄바꿈되며 마주보는 것 없이 둘째 줄이 뜬다. 둘 다 P5-1 에서 실측한 것이다. */}
          <div className="mt-0.5 flex flex-wrap items-center gap-x-2 gap-y-0.5">
            <span className="font-display text-lg leading-tight text-eye-purple">{DAY_NAME[cell.tenGod]}</span>
            <span className="text-xs text-eye-purple/70">· {cell.grade.label}</span>
          </div>
          {cell.marks.length > 0 && (
            <div className="mt-1 flex flex-wrap gap-1">
              {cell.marks.map((m) => (
                <span key={m.glyph} className="rounded-full bg-white/55 px-1.5 py-0.5 text-[10px] font-bold text-eye-purple">
                  <span aria-hidden>{m.glyph}</span> {m.label}
                </span>
              ))}
            </div>
          )}
        </div>
      </div>
      <p className="mt-2 text-xs leading-relaxed text-eye-purple/85">{DAY_LINE[cell.tenGod]}</p>
      <p className="mt-1.5 text-right text-xs font-bold text-eye-purple">자세히 보기 →</p>
    </Link>
  );
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 출력 없음(아직 아무도 안 쓰므로 미사용 경고는 안 난다 — 이 저장소는 noUnusedLocals 를 파일 단위로 걸지 않는다. 에러가 나면 그 내용을 보고하라)

- [ ] **Step 3: 커밋**

```bash
git commit components/byeolmaru/TodayHeroTile.tsx -m "feat(byeolmaru): 오늘 히어로 골드 타일 (P5-3)"
git show --stat HEAD
```

---

### Task 3: `/byeolmaru/saju?date=` 초기 선택 지원

허브 격자에서 오늘이 아닌 날을 누르면 이 라우트로 온다(사용자 결정). 지금은 항상 오늘이 선택된 채 열린다.

**Files:**
- Modify: `components/byeolmaru/SajuTodayView.tsx`
- Modify: `app/byeolmaru/saju/page.tsx`

- [ ] **Step 1: `SajuTodayView` 가 초기 날짜를 prop 으로 받게 한다**

시그니처를 바꾼다(현재 `export default function SajuTodayView()`):

```tsx
export default function SajuTodayView({ initialDate }: { initialDate?: string }) {
```

그리고 `refresh()` 안의 `setSelected((prev) => prev ?? data.today);` 를 아래로 교체:

```tsx
      // 허브 격자에서 넘어온 ?date= 가 있으면 그 날로 연다(스펙 §7 "요약은 허브, 전문은 밖").
      // 🔴 응답에 없는 날짜(무료 유저가 손으로 미래 날짜를 친 경우)면 무시하고 오늘로 — 서버가
      //    안 내려준 날을 선택 상태로 두면 cell 폴백이 타서 엉뚱한 날 상세가 열린다.
      const wanted = initialDate && data.cells.some((c) => c.date === initialDate) ? initialDate : data.today;
      setSelected((prev) => prev ?? wanted);
```

⚠️ `refresh` 가 `initialDate` 를 읽으므로 `useEffect(() => { void refresh(); }, [])` 의 deps 경고가 날 수 있다. 이 저장소의 기존 패턴을 따라 **deps 를 늘리지 말고** 그대로 둔다(최초 1회만 읽는 값이다). 린트가 없으므로 빌드는 통과한다.

- [ ] **Step 2: 페이지가 `?date=` 를 읽어 넘긴다**

`app/byeolmaru/saju/page.tsx` 를 아래로 교체:

```tsx
import { Suspense } from "react";
import { noindexMetadata } from "@/lib/seo/metadata";
import SajuTodayView from "@/components/byeolmaru/SajuTodayView";
import SajuDateBridge from "@/components/byeolmaru/SajuDateBridge";

export const metadata = noindexMetadata({
  title: "오늘 사주 · 별마루",
  description: "이번 달, 너에게 맞는 날과 챙길 날을 별콩이가 짚어줄게.",
});

export default function SajuTodayPage() {
  // 🔴 useSearchParams 를 쓰는 클라 컴포넌트는 Suspense 경계가 필요하다 — 없으면 빌드가
  //    "missing suspense boundary with useSearchParams" 로 실패한다(Next 앱 라우터 규칙).
  return (
    <Suspense fallback={<SajuTodayView />}>
      <SajuDateBridge />
    </Suspense>
  );
}
```

- [ ] **Step 3: 브리지 컴포넌트를 만든다**

Create: `components/byeolmaru/SajuDateBridge.tsx`

```tsx
"use client";

// components/byeolmaru/SajuDateBridge.tsx — ?date= 를 읽어 SajuTodayView 에 넘기는 얇은 다리.
// 🔴 페이지(서버 컴포넌트)가 직접 못 읽는다 — useSearchParams 는 클라 훅이고 Suspense 경계를
//    요구한다. 뷰 자체를 클라로 바꾸는 대신 다리 하나만 클라로 둬서 경계를 좁게 유지한다.
import { useSearchParams } from "next/navigation";
import SajuTodayView from "./SajuTodayView";

export default function SajuDateBridge() {
  const date = useSearchParams().get("date");
  // "2026-09-05" 모양만 통과시킨다 — 임의 문자열이 선택 상태로 들어가면 cell 폴백이 탄다.
  const ok = date && /^\d{4}-\d{2}-\d{2}$/.test(date) ? date : undefined;
  return <SajuTodayView initialDate={ok} />;
}
```

- [ ] **Step 4: 빌드로 Suspense 경계를 검증한다**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0. **`useSearchParams` 관련 에러가 나면 Suspense 경계가 잘못된 것이다** — 그 경우 에러 전문을 보고하라.

- [ ] **Step 5: 브라우저 확인**

1. `http://localhost:3000/byeolmaru/saju` — 오늘이 선택된 채 열린다(회귀 없음)
2. `http://localhost:3000/byeolmaru/saju?date=<이번 달 과거 날짜>` — 그 날이 선택된 채 열리고 상세 헤더가 "N월 N일"이다
3. `http://localhost:3000/byeolmaru/saju?date=아무말` — 무시되고 오늘이 열린다
4. `http://localhost:3000/byeolmaru/saju?date=2030-01-01` — 응답에 없는 날짜라 무시되고 오늘이 열린다

- [ ] **Step 6: 커밋**

```bash
git commit components/byeolmaru/SajuTodayView.tsx components/byeolmaru/SajuDateBridge.tsx app/byeolmaru/saju/page.tsx -m "feat(byeolmaru): 오늘 사주에 ?date= 초기 선택 (P5-3)"
git show --stat HEAD
```

---

### Task 4: `/byeolmaru/tarot` 신설 — 오늘의 카드 이전

지금 `DailyCardBlock`(347줄)은 허브에 인라인이다. 무료 목록의 "오늘 타로" 행이 진입점이 되므로(사용자 결정) 전용 라우트가 필요하다 — `/byeolmaru/saju` 와 대칭이고 공유 링크도 생긴다.

**Files:**
- Create: `app/byeolmaru/tarot/page.tsx`
- Create: `components/byeolmaru/TarotTodayView.tsx`
- Modify: `components/byeolmaru/DailyCardBlock.tsx` (props 는 그대로, 헤더만 추가되지 않게)

- [ ] **Step 1: 먼저 `DailyCardBlock` 의 props 를 확인한다**

`components/byeolmaru/DailyCardBlock.tsx` 를 읽어 `entitled`·`trialUsed`·`onStartTrial`·`onSubscribe` 네 prop 의 정확한 타입을 확인하라. 이 값들은 지금 허브가 캘린더 응답에서 받아 넘긴다.

- [ ] **Step 2: 뷰 컴포넌트를 만든다**

Create: `components/byeolmaru/TarotTodayView.tsx`

```tsx
"use client";

// components/byeolmaru/TarotTodayView.tsx — 오늘 타로 전용 화면(P5-3).
// 🔴 DailyCardBlock 은 자격(entitled/trialUsed)을 props 로 받는다 — 허브가 넘겨주던 값이다.
//    전용 페이지가 되면서 스스로 캘린더 응답에서 자격을 읽어야 한다(같은 라우트를 쓴다 —
//    자격 전용 API 를 새로 만들지 않는다. 응답이 이미 entitled/trialUsed 를 싣고 있다).
import { useEffect, useState } from "react";
import Link from "next/link";
import DailyCardBlock from "./DailyCardBlock";
import BackHeader from "./BackHeader";
import { useByeolmaruSubscribe } from "./useByeolmaruSubscribe";
import { trackUiEvent } from "@/lib/analytics/ui-events";

type State =
  | { kind: "loading" }
  | { kind: "need_login" }
  | { kind: "ready"; entitled: boolean; trialUsed: boolean };

export default function TarotTodayView() {
  const [state, setState] = useState<State>({ kind: "loading" });

  async function refresh() {
    try {
      const res = await fetch("/api/byeolmaru/calendar", { cache: "no-store" });
      if (res.status === 401) { trackUiEvent("byeolmaru_need_login"); setState({ kind: "need_login" }); return; }
      // 🔴 404(생일 미입력)도 ready 로 넘긴다 — 오늘 타로는 사주와 달리 **생일이 필요 없다**.
      //    프로필이 없으면 자격만 없는 것으로 보고(무료 뽑기는 열린다) 화면을 띄운다.
      if (res.status === 404) { setState({ kind: "ready", entitled: false, trialUsed: false }); return; }
      if (!res.ok) { setState({ kind: "ready", entitled: false, trialUsed: false }); return; }
      const j = await res.json();
      setState({ kind: "ready", entitled: !!j.entitled, trialUsed: !!j.trialUsed });
    } catch {
      setState({ kind: "ready", entitled: false, trialUsed: false });
    }
  }
  useEffect(() => { void refresh(); }, []);

  const { startTrial, openSubscribe, subscribeModal } = useByeolmaruSubscribe(refresh);

  if (state.kind === "loading") return <main className="mx-auto w-full max-w-md p-6 text-center text-text-light">펼치는 중…</main>;
  if (state.kind === "need_login") return (
    <main className="mx-auto w-full max-w-md p-6 text-center">
      <p className="mb-4 text-eye-purple">로그인하면 오늘 카드를 뽑을 수 있어.</p>
      <Link href="/login?next=/byeolmaru/tarot" className="rounded-xl bg-lilac-deep px-4 py-2 text-cream">로그인하러 가기</Link>
    </main>
  );

  return (
    <main className="mx-auto w-full max-w-md space-y-4 p-4">
      <BackHeader title="오늘 타로" />
      <DailyCardBlock
        entitled={state.entitled}
        trialUsed={state.trialUsed}
        onStartTrial={startTrial}
        onSubscribe={openSubscribe}
      />
      {subscribeModal}
    </main>
  );
}
```

⚠️ Step 1 에서 확인한 실제 prop 이름·타입과 다르면 **그쪽에 맞춰라**(위 코드는 허브의 현재 호출을 옮긴 것이다).

- [ ] **Step 3: 페이지를 만든다**

Create: `app/byeolmaru/tarot/page.tsx`

```tsx
import { noindexMetadata } from "@/lib/seo/metadata";
import TarotTodayView from "@/components/byeolmaru/TarotTodayView";

export const metadata = noindexMetadata({
  title: "오늘 타로 · 별마루",
  description: "카드 한 장으로 오늘을 가볍게 짚어볼게.",
});

export default function TarotTodayPage() {
  return <TarotTodayView />;
}
```

`lib/seo/metadata.ts` 의 `noindexMetadata` 시그니처를 읽어 인자 모양이 맞는지 확인하라(`/byeolmaru/saju/page.tsx` 가 같은 함수를 쓴다).

- [ ] **Step 4: 허브에서 `DailyCardBlock` 을 뺀다**

`components/byeolmaru/ByeolmaruHub.tsx` 에서 아래 줄을 **삭제**한다:

```tsx
      <DailyCardBlock entitled={data.entitled} trialUsed={data.trialUsed} onStartTrial={startTrial} onSubscribe={openSubscribe} />
```

그리고 `import DailyCardBlock from "./DailyCardBlock";` 도 삭제한다.

⚠️ `startTrial`·`openSubscribe` 가 허브에서 다른 데 안 쓰이면 `useByeolmaruSubscribe` 호출도 미사용이 된다 — **아직 지우지 마라.** T7 에서 미끼 자리가 그걸 쓴다. tsc 가 미사용을 에러로 내면 그 사실을 보고하라.

- [ ] **Step 5: 빌드·브라우저 확인**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0. 라우트 표에 `/byeolmaru/tarot` 가 **새로 보인다**.

브라우저:
1. `/byeolmaru/tarot` — 뒤로가기 헤더 + 오늘의 카드 블록. 안 뽑았으면 뽑기 버튼, 뽑았으면 카드와 해석
2. 실제로 **뽑기를 한 번 해본다** — 의식 모달이 뜨고 카드가 저장되는지(이미 뽑은 날이면 이 항목은 건너뛰고 보고하라)
3. `/byeolmaru` — 오늘의 카드 블록이 **사라졌다**(목록은 T5 에서 온다)

- [ ] **Step 6: 커밋**

```bash
git commit app/byeolmaru/tarot/page.tsx components/byeolmaru/TarotTodayView.tsx components/byeolmaru/ByeolmaruHub.tsx -m "feat(byeolmaru): 오늘 타로 전용 라우트 — 허브 인라인에서 이전 (P5-3)"
git show --stat HEAD
```

---

### Task 5: 무료 목록 3종 — 2탭 동형

스펙 §3. 지금 허브의 2열 그리드 3칸(우리 오늘/MBTI/별자리)을 **가로 행 카드 3종**(오늘 타로/사주 MBTI/별 인연 지도)으로 바꾼다. 우리 오늘은 목록에서 빠지고 달력 칩으로 간다(T6).

🔴 **왜 2탭 컴포넌트를 재사용하지 않는가:** ①이 3종은 `FORTUNE_LIST` 밖이라 `f.type`·`FORTUNE_GRADIENTS`·`FORTUNE_HASHTAGS` 키가 없다 ②`FortuneIcon` 의 `HAS_ICON` 에 `saju_mbti`·`byeoljari` 가 **없어서**(파일은 있는데 목록에 누락) 그대로 쓰면 이모지 폴백이 뜬다 ③스펙 스코프가 "2탭을 건드리지 않는다"라 `HAS_ICON` 에 추가하는 것도 범위 밖이다. → **시각만 복제**한다.

**Files:**
- Create: `components/byeolmaru/FreeList.tsx`

- [ ] **Step 1: 컴포넌트를 만든다**

```tsx
"use client";

// components/byeolmaru/FreeList.tsx — 허브 "무료로 더 볼 것" 목록 3종(스펙 §3).
// 🔴 2탭(/fortune) 리스트와 **동형**이지만 컴포넌트를 공유하지 않는다:
//    ①이 3종은 FORTUNE_LIST 밖이라 FortuneType 키가 없고 ②FortuneIcon 의 HAS_ICON 에
//    saju_mbti·byeoljari 가 누락돼 이모지 폴백이 뜨며 ③P5 스코프가 2탭을 건드리지 말라고 한다.
//    시각만 복제하고, 좌측 4px 컬러 바를 더한다 — 위 달력 판의 순백 칸과 이 흰 카드가 섞여
//    보이지 않게 하는 구분자다(스펙 §4 경고).
import Image from "next/image";
import Link from "next/link";
import { getCardImagePath, CARD_BACK_IMAGE } from "@/lib/tarot/cards";
import { trackUiEvent } from "@/lib/analytics/ui-events";

export interface FreeListItem {
  /** 계측 안정 축 — 라벨이 바뀌어도 집계가 유지된다. */
  key: string;
  href: string;
  label: string;
  tagline: string;
  hashtags: string[];
  /** 좌측 4px 바 + 타일 배경. 오늘 타로만 골드(스펙 §4). */
  barColor: string;
  tileBg: string;
  /** 상태 칩 — 오늘 타로는 "뽑음/아직"이 데일리 라이브 성격을 한 줄에서 살린다. */
  chip: string;
  chipTone: "gold" | "lilac";
  /** 타일에 들어갈 것. 아이콘 파일 경로이거나, 타로면 카드 이미지. */
  image: string;
}

export default function FreeList({ items }: { items: FreeListItem[] }) {
  return (
    <section>
      {/* 섹션 타이틀 — 골드 3px 바 + 밑줄(스펙 §4) */}
      <div className="mb-2 flex items-center gap-2">
        <span aria-hidden className="h-[3px] w-4 rounded-full bg-gold" />
        <h2 className="font-display text-base text-eye-purple">무료로 더 볼 것</h2>
      </div>
      <div className="flex flex-col gap-3">
        {items.map((it) => (
          <Link
            key={it.key}
            href={it.href}
            onClick={() => trackUiEvent("byeolmaru_free_item_clicked", { meta: { item: it.key } })}
            className="relative w-full overflow-hidden rounded-2xl border border-lilac-mid/20 bg-white p-4 pl-5 shadow-[0_2px_10px_rgba(159,138,208,0.08)] transition hover:border-lilac-deep/60 active:scale-[0.99]"
          >
            <span aria-hidden className="absolute left-0 top-0 h-full w-1" style={{ background: it.barColor }} />
            <div className="flex items-center gap-3.5">
              <div className="flex h-12 w-12 shrink-0 items-center justify-center overflow-hidden rounded-xl" style={{ background: it.tileBg }}>
                <Image src={it.image} alt="" width={44} height={44} className="h-11 w-11 object-contain" />
              </div>
              <div className="min-w-0 flex-1">
                <div className="flex items-center gap-2">
                  <span className="text-[15px] font-bold text-eye-purple">{it.label}</span>
                  <span
                    className={`rounded-full px-1.5 py-0.5 text-[10px] font-bold ${
                      it.chipTone === "gold" ? "bg-gold-soft/30 text-eye-purple" : "bg-lilac-soft/60 text-lilac-deep"
                    }`}
                  >
                    {it.chip}
                  </span>
                </div>
                <p className="mt-1 line-clamp-2 text-[12.5px] leading-snug text-text-light/80">{it.tagline}</p>
                <div className="mt-2 flex flex-wrap gap-1">
                  {it.hashtags.map((h) => (
                    <span key={h} className="rounded-full bg-lilac-soft/60 px-2 py-0.5 text-[11px] font-bold text-lilac-deep">
                      #{h}
                    </span>
                  ))}
                </div>
              </div>
            </div>
          </Link>
        ))}
      </div>
    </section>
  );
}

/** 목록 3종의 고정 데이터 — 오늘 타로만 뽑기 상태에 따라 타일 이미지와 칩이 바뀐다(스펙 §12). */
export function buildFreeItems(drawn: { cardId: number } | null): FreeListItem[] {
  return [
    {
      key: "tarot",
      href: "/byeolmaru/tarot",
      label: "오늘 타로",
      tagline: "카드 한 장으로 오늘을 가볍게 짚어봐",
      hashtags: ["하루한장", "오늘의카드"],
      barColor: "#E8C26A",
      tileBg: "linear-gradient(135deg, #FFF3D6 0%, #F2D78A 100%)",
      chip: drawn ? "오늘 뽑음" : "하루 1회 무료",
      chipTone: "gold",
      // 🔴 신규 아이콘을 만들지 않는다 — 타일 자리에 카드 이미지를 직접 넣는다(스펙 §3-1·§12).
      //    안 뽑음 = 뒷면, 뽑음 = 그 카드 앞면. "오늘 뽑았나"가 한 줄에서 보인다.
      image: drawn ? getCardImagePath(drawn.cardId) : CARD_BACK_IMAGE,
    },
    {
      key: "mbti",
      href: "/fortune/saju-mbti",
      label: "사주 MBTI",
      tagline: "사주로 보는 내 유형 — 문항에 답하면 바로 나와",
      hashtags: ["무료", "16유형"],
      barColor: "#9F8AD0",
      tileBg: "linear-gradient(135deg, #EFEAF6 0%, #DACFEC 100%)",
      chip: "무료",
      chipTone: "lilac",
      image: "/icons/fortune/saju_mbti.webp",
    },
    {
      key: "byeoljari",
      href: "/fortune/byeoljari",
      label: "별 인연 지도",
      tagline: "내 인연들을 별자리로 펼쳐볼게",
      hashtags: ["무료", "인연지도"],
      barColor: "#B8A8D8",
      tileBg: "linear-gradient(135deg, #E8DEF5 0%, #D4C7EE 100%)",
      chip: "무료",
      chipTone: "lilac",
      image: "/icons/fortune/byeoljari.webp",
    },
  ];
}
```

- [ ] **Step 2: 계측 이벤트를 등록한다**

`lib/analytics/ui-events.ts` 의 `UI_EVENTS` 배열에 아래를 추가한다(주변 항목의 JSDoc 형식을 그대로 따를 것):

```ts
  /** 별마루 — 무료 목록 3종 행 클릭. meta:{item:"tarot"|"mbti"|"byeoljari"} */
  "byeolmaru_free_item_clicked",
```

그리고 `lib/analytics/ui-events.test.ts` 에 등록 확인 단언을 하나 추가한다(기존 별마루 이벤트 테스트와 같은 결로):

```ts
  assert.equal(isUiEvent("byeolmaru_free_item_clicked"), true);
```

- [ ] **Step 3: 테스트·타입체크**

Run: `node --import tsx --test lib/analytics/ui-events.test.ts && npx tsc --noEmit`
Expected: PASS · 출력 없음

- [ ] **Step 4: 커밋**

```bash
git commit components/byeolmaru/FreeList.tsx lib/analytics/ui-events.ts lib/analytics/ui-events.test.ts -m "feat(byeolmaru): 무료 목록 3종 카드(2탭 동형 + 좌측 바) (P5-3)"
git show --stat HEAD
```

---

### Task 6: 인연 칩을 허브 달력 판으로

스펙 §8. 목록의 "우리 오늘" 줄을 없애고 **달력 판 상단 칩 행**(`나` / `지민` / `＋`)으로 올린다. 상대 칩을 누르면 **달력이 통째로 우리 버전**이 된다. 칸을 누르면 `/byeolmaru/woori` 로 간다.

🔴 이 태스크가 이 플랜에서 가장 크다. 허브가 pair 캘린더 응답도 받아야 한다.

**Files:**
- Create: `components/byeolmaru/PartnerChips.tsx`
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`

- [ ] **Step 1: 기존 `SubjectToggle` 을 읽고 무엇을 재사용할지 정한다**

`components/byeolmaru/SubjectToggle.tsx` 를 읽어라. `/byeolmaru/woori` 가 쓰는 칩 행이고, 배타 삼항으로 활성 스타일을 고정한 주석이 있다(클래스를 겹쳐 붙이면 승자가 빌드 스캔 순서로 갈린다).

허브용은 두 가지가 다르다: ①관계 유형 색(`DOLL_COLORS`)을 쓴다 ②상대 0명이면 `＋ 인연 걸어두기`(금색 점선)로 크게 뜬다. **`SubjectToggle` 을 고치지 말고 새로 만든다** — `/byeolmaru/woori` 의 칩은 그대로 둔다(그 화면은 P5-3 스코프가 아니다).

- [ ] **Step 2: `PartnerChips` 를 만든다**

```tsx
"use client";

// components/byeolmaru/PartnerChips.tsx — 허브 달력 판 상단 인연 칩(스펙 §8).
// /byeolmaru/woori 의 SubjectToggle 과 역할은 같지만 두 가지가 다르다: ①관계 유형 색(DOLL_COLORS)
// ②상대 0명일 때 "＋ 인연 걸어두기"가 금색 점선으로 크게 뜬다(비어 있는 게 아니라 "여기 사람을
// 걸 수 있다"가 보이게 — 초대 루프 입구).
// 🔴 활성 스타일은 배타 삼항으로만 결정한다 — active/inactive 클래스를 겹쳐 붙이면 승자가 빌드
//    스캔 순서로 갈린다(SubjectToggle·CalendarGrid 가 같은 이유로 같은 규율을 쓴다).
import { DOLL_COLORS, type RelationshipStatus } from "@/lib/relationship/types";

export interface PartnerChip {
  id: string;
  name: string;
  status: RelationshipStatus | null;
}

export default function PartnerChips({
  partners,
  selected,
  onSelect,
  onAdd,
}: {
  partners: PartnerChip[];
  selected: string;
  onSelect: (id: string) => void;
  onAdd: () => void;
}) {
  if (partners.length === 0) {
    return (
      <button
        onClick={onAdd}
        className="w-full rounded-xl border border-dashed border-gold px-3 py-2 text-sm font-medium text-eye-purple/80"
      >
        ＋ 인연 걸어두기
      </button>
    );
  }

  return (
    <div className="flex flex-wrap gap-2" role="tablist" aria-label="달력 대상">
      <button
        role="tab"
        aria-selected={selected === "me"}
        onClick={() => onSelect("me")}
        className={`rounded-full px-3 py-1 text-sm ${
          selected === "me" ? "bg-lilac-deep font-bold text-cream" : "border border-lilac-mid text-text-light"
        }`}
      >
        나
      </button>
      {partners.map((p) => {
        const active = selected === p.id;
        // DOLL_COLORS 는 [from, to] 두 색이다 — 칩엔 첫 색만 쓴다(작은 면적에 그라데이션은 탁해진다).
        const color = p.status ? DOLL_COLORS[p.status][0] : "#B8A8D8";
        return (
          <button
            key={p.id}
            role="tab"
            aria-selected={active}
            onClick={() => onSelect(p.id)}
            className={`rounded-full px-3 py-1 text-sm ${active ? "font-bold text-cream" : "border text-text-light"}`}
            style={active ? { background: color } : { borderColor: color, color: "#7A6BA0" }}
          >
            {p.name}
          </button>
        );
      })}
      <button
        onClick={onAdd}
        aria-label="인연 걸어두기"
        className="rounded-full border border-dashed border-gold px-3 py-1 text-sm text-eye-purple/70"
      >
        ＋
      </button>
    </div>
  );
}
```

⚠️ `DOLL_COLORS` 의 실제 타입은 `Record<RelationshipStatus, [string, string]>` 이다(`lib/relationship/types.ts:13`). 인덱스가 맞는지 파일을 열어 확인하라.

- [ ] **Step 3: watch 목록에 `status` 가 실리는지 확인한다**

`app/api/byeolmaru/watch/route.ts` 의 GET 응답을 읽어라. `WooriTodayView` 는 `j.watched` 를 `{id, name}` 으로만 쓴다. **`status` 가 응답에 없으면** 칩 색을 칠할 수 없다.

- 있으면: 그대로 쓴다
- 없으면: 라우트에 `status` 를 **추가**한다(`byeolmaru_watch.status` 컬럼은 P4-a 에서 이미 있다). 이건 응답 필드 추가라 기존 소비처를 안 깬다. 추가했다면 보고하라

- [ ] **Step 4: 허브에 pair 상태를 붙인다**

`ByeolmaruHub.tsx` 의 `ready` 분기에 상태를 추가한다. 아래는 **추가할 state·effect** 이고, 기존 `refresh`/`state` 는 그대로 둔다:

```tsx
  const [partners, setPartners] = useState<PartnerChip[]>([]);
  const [subject, setSubject] = useState<string>("me");
  const [pairCells, setPairCells] = useState<PairDayCell[] | null>(null);
  const [pairLocked, setPairLocked] = useState<string[]>([]);
  const [addOpen, setAddOpen] = useState(false);

  async function loadPartners() {
    try {
      const res = await fetch("/api/byeolmaru/watch", { cache: "no-store" });
      if (!res.ok) { setPartners([]); return; }
      const j = await res.json();
      setPartners(Array.isArray(j?.watched) ? j.watched : []);
    } catch { setPartners([]); }
  }
  useEffect(() => { void loadPartners(); }, []);

  // 상대 칩을 고르면 달력이 통째로 우리 버전이 된다(스펙 §8). 룰 100% 라 원가 0.
  // 🔴 cancelled 가드 — 칩을 빠르게 번갈아 누르면 낡은 응답이 최신 상태를 덮어쓴다
  //    (WooriTodayView 가 같은 이유로 같은 가드를 쓴다).
  useEffect(() => {
    if (subject === "me") { setPairCells(null); setPairLocked([]); return; }
    let cancelled = false;
    void (async () => {
      try {
        const res = await fetch(`/api/byeolmaru/calendar?subject=${encodeURIComponent(subject)}`, { cache: "no-store" });
        const j = await res.json().catch(() => null);
        if (cancelled || !res.ok || !j) return;
        setPairCells(Array.isArray(j.cells) ? j.cells : null);
        setPairLocked(Array.isArray(j.lockedDates) ? j.lockedDates : []);
      } catch { /* 실패 시 나 달력 유지 — 칩만 눌린 상태로 두지 않게 아래에서 subject 를 되돌린다 */
        if (!cancelled) setSubject("me");
      }
    })();
    return () => { cancelled = true; };
  }, [subject]);
```

import 를 추가한다:

```tsx
import type { PairDayCell } from "@/lib/byeolmaru/pair-day";
import { PAIR_TONE_LABEL } from "@/lib/byeolmaru/pair-day";
import PartnerChips, { type PartnerChip } from "./PartnerChips";
import WatchAddModal from "./WatchAddModal";
```

- [ ] **Step 5: 타입체크**

Run: `npx tsc --noEmit`
Expected: 출력 없음. (아직 렌더에 안 쓰므로 미사용 변수 경고가 날 수 있다 — 에러면 T7 이 쓸 때까지 임시로 남기지 말고 **보고하라**)

- [ ] **Step 6: 커밋**

```bash
git commit components/byeolmaru/PartnerChips.tsx components/byeolmaru/ByeolmaruHub.tsx -m "feat(byeolmaru): 허브 인연 칩 + 우리 달력 전환 상태 (P5-3)"
git show --stat HEAD
```

(`app/api/byeolmaru/watch/route.ts` 를 고쳤으면 경로를 추가하라)

---

### Task 7: 허브 조립 — 달력 판 · 목록 · 낙수 · 미끼 자리

앞선 조각들을 스펙 §3 IA 순서로 합친다.

**Files:**
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`

- [ ] **Step 1: `ready` 분기의 `return` 을 통째로 교체한다**

```tsx
  const { data } = state;
  const todayCell = data.cells.find((c) => c.isToday) ?? data.cells[data.cells.length - 1];
  const crossSell = pickCrossSell(todayCell);
  const isPair = subject !== "me" && pairCells !== null;

  // 격자에 넘길 정규화 셀. 나·우리 두 판정 엔진이 같은 GridCell 계약으로 수렴한다.
  // 🔴 우리 셀엔 마크가 없다(우리 판정은 ✨끌림·🔗결속 태그를 쓴다) — 빈 배열을 **명시**한다.
  //    GridCell.marks 가 필수인 이유가 그것이다(옵셔널이면 조용히 사라진다).
  const gridCells: GridCell[] = isPair
    ? pairCells.map((c) => ({ date: c.date, ganji: c.ganji, tone: c.tone, label: PAIR_TONE_LABEL[c.tone], isToday: c.isToday, marks: [] }))
    : data.cells.map((c) => ({ date: c.date, ganji: c.ganji, tone: c.grade.tone, label: c.grade.label, isToday: c.isToday, marks: c.marks }));
  const gridLocked = isPair ? pairLocked : data.lockedDates;
  const filled = isPair ? pairCells.length : data.cells.length;

  return (
    <main className="mx-auto w-full max-w-md space-y-4 p-4">
      <header>
        <h1 className="font-display text-2xl text-eye-purple">별마루</h1>
        <p className="text-sm text-text-light">무료로 다 보는 곳 · 오늘 {data.todayGanji}</p>
      </header>

      {/* ── 달력 판: 이 화면의 단 하나의 강조점(스펙 §3) ── */}
      <section className="space-y-3">
        <AttendanceStrip attendance={attendance} filledDays={filled} entitled={data.entitled} />

        {/* 오늘 히어로는 **나 탭에서만**. 우리 탭의 오늘은 톤·태그 성격이 달라 같은 타일에 안 들어간다
            — 우리 쪽 상세는 /byeolmaru/woori 가 받는다(스펙 §8). */}
        {!isPair && <TodayHeroTile cell={todayCell} href="/byeolmaru/saju" />}

        <PartnerChips
          partners={partners}
          selected={subject}
          onSelect={(id) => { if (id !== "me") trackUiEvent("byeolmaru_partner_selected"); setSubject(id); }}
          onAdd={() => setAddOpen(true)}
        />

        <CalendarGrid
          cells={gridCells}
          lockedDates={gridLocked}
          todayDate={data.today}
          selectedDate={data.today}
          onSelect={(date) => {
            // 🔴 허브 격자는 "고르는" 곳이 아니라 "여는" 곳이다(스펙 §7 요약은 안, 전문은 밖).
            //    나 탭은 그 날의 상세로, 우리 탭은 우리 상세로 보낸다.
            if (isPair) { router.push("/byeolmaru/woori"); return; }
            router.push(date === data.today ? "/byeolmaru/saju" : `/byeolmaru/saju?date=${date}`);
          }}
        />

        {/* 🔴 미끼 자리 — 내용·톤·CTA·계측은 P5-4 몫이다(스펙 §9). 지금은 자리만 잡는다.
            비로그인·생일 미입력에게는 애초에 이 분기까지 안 온다(위 상태 분기에서 갈린다). */}
        {!data.entitled && (
          <PremiumBlock
            entitled={false}
            trialUsed={data.trialUsed}
            narrative={null}
            teaser={null}
            loading={false}
            onStartTrial={startTrial}
            onSubscribe={openSubscribe}
          />
        )}
      </section>

      <FreeList items={buildFreeItems(dailyCard)} />

      <CrossSellCard item={crossSell} />
      {subscribeModal}
      {addOpen && (
        <WatchAddModal
          onClose={() => setAddOpen(false)}
          onAdded={(id) => { setAddOpen(false); void loadPartners(); setSubject(id); }}
        />
      )}
    </main>
  );
```

- [ ] **Step 2: 오늘 뽑은 카드를 읽어 목록 타일에 반영한다**

`FreeList` 의 `buildFreeItems(drawn)` 이 오늘 뽑은 카드를 필요로 한다(스펙 §12: 안 뽑음=뒷면, 뽑음=앞면). 허브에 상태를 추가한다:

```tsx
  const [dailyCard, setDailyCard] = useState<{ cardId: number } | null>(null);

  // 오늘 뽑은 카드 — 목록 행 타일에만 쓴다(뽑기 자체는 /byeolmaru/tarot 가 한다).
  useEffect(() => {
    void (async () => {
      try {
        const res = await fetch("/api/byeolmaru/daily-card", { cache: "no-store" });
        if (!res.ok) return;
        const j = await res.json();
        if (j?.card?.cardId != null) setDailyCard({ cardId: j.card.cardId });
      } catch { /* 목록 타일이 뒷면으로 남을 뿐이라 조용히 넘긴다 */ }
    })();
  }, []);
```

⚠️ **`/api/byeolmaru/daily-card` 의 실제 GET 응답 모양을 먼저 확인하라** — `DailyCardBlock` 이 이미 이 라우트를 쓰고 있으니 그 코드에서 필드 경로를 그대로 가져올 것. 위 `j?.card?.cardId` 는 추정이다.

- [ ] **Step 3: import 를 정리한다**

추가: `import { useRouter } from "next/navigation";` · `import TodayHeroTile from "./TodayHeroTile";` · `import FreeList, { buildFreeItems } from "./FreeList";` · `import CalendarGrid, { type GridCell } from "./CalendarGrid";` · `import PremiumBlock from "./PremiumBlock";`
그리고 컴포넌트 안에 `const router = useRouter();`

**삭제**: `DAY_NAME`·`DAY_LINE` import(히어로 타일로 옮겨갔다) · `DOT` 상수 · `strip7` 계산 · `WeekBucket` 타입 import(안 쓰면).

- [ ] **Step 4: 검증**

Run: `npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*") 2>&1 | tail -4 && npm run build`
Expected: tsc 출력 없음 · `# fail 0` · build exit 0

- [ ] **Step 5: 브라우저 실렌더 — 이 태스크의 핵심 검증**

`/byeolmaru` 를 375px 로 연다:
1. 달력 판이 **화면의 강조점**이다 — 판 그라데이션 안에 출석 스트립·골드 히어로·칩·격자가 들어 있다
2. 히어로 골드 타일에 간지·일지캐릭터·하루 이름·등급·마크 칩·한 줄·"자세히 보기 →"가 있다
3. **격자에서 과거 날을 누르면** `/byeolmaru/saju?date=` 로 이동하고 그 날 상세가 열린다
4. **오늘 칸을 누르면** `/byeolmaru/saju` 로 간다(쿼리 없이)
5. 상대가 있으면 칩이 관계 색으로 뜨고, 누르면 **격자가 우리 버전으로 바뀌며 히어로가 사라진다**. 그 상태에서 칸을 누르면 `/byeolmaru/woori` 로 간다
6. 상대가 없으면 `＋ 인연 걸어두기`가 금색 점선으로 뜨고, 누르면 `WatchAddModal` 이 열린다
7. 무료 목록 3종이 가로 행 카드다. **오늘 타로 타일에 카드 이미지**(안 뽑았으면 뒷면)가 들어 있다
8. 판 안의 순백 칸과 아래 목록의 흰 카드가 **섞여 보이지 않는다**(좌측 컬러 바·그림자가 구분자)
9. 콘솔 에러 0 · 네트워크 5xx 0

스크린샷을 남길 것.

- [ ] **Step 6: 커밋**

```bash
git commit components/byeolmaru/ByeolmaruHub.tsx -m "feat(byeolmaru): 허브를 달력 판 히어로 구조로 재조립 (P5-3)"
git show --stat HEAD
```

---

### Task 8: 상태별 화면 — 비로그인·생일 미입력

스펙 §12. 비로그인 히어로를 **"안 칠해진 이번 달"**로 채운다. 가짜 데이터가 아니라 진짜 빈 상태라 정직하고, "내 칸이 비어 있다"가 그 자체로 로그인 유인이 된다(개인화 0).

**Files:**
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`

- [ ] **Step 1: 빈 달력을 그리는 헬퍼를 만든다**

`lib/byeolmaru/calendar.ts` 의 `monthRange` 를 재사용한다. `ByeolmaruHub.tsx` 안에 둔다(다른 데서 안 쓴다 — 단일 사용처에 추상화하지 않는 관례):

```tsx
// 비로그인·생일 미입력이 보는 "안 칠해진 이번 달"(스펙 §12). 판정이 없으므로 전부 잠긴 칸으로
// 그린다 — CalendarGrid 가 cells 없이 lockedDates 만 받으면 정확히 그 모양이 된다.
// 🔴 개인화 0 — 서버를 안 부르고 클라 날짜로만 만든다(비로그인은 세션이 없어 부를 것도 없다).
function emptyMonthDates(): { dates: string[]; today: string } {
  const now = new Date();
  const kst = new Date(now.getTime() + 9 * 3600_000);
  const y = kst.getUTCFullYear();
  const m = kst.getUTCMonth() + 1;
  const last = new Date(Date.UTC(y, m, 0)).getUTCDate();
  const ym = `${y}-${String(m).padStart(2, "0")}`;
  return {
    dates: Array.from({ length: last }, (_, i) => `${ym}-${String(i + 1).padStart(2, "0")}`),
    today: `${ym}-${String(kst.getUTCDate()).padStart(2, "0")}`,
  };
}
```

- [ ] **Step 2: `GuestPeek` 을 교체한다**

기존 `GUEST_CARDS` 상수와 `GuestPeek` 함수를 **통째로** 아래로 교체한다:

```tsx
// 비로그인·생일 미입력 공통 껍데기(스펙 §12). 달력은 "안 칠해진 이번 달"이고, 목록은
// MBTI·별자리를 **위로** 올린다(로그인 없이 되므로 먼저 맛보게). 페이월은 숨긴다 —
// 아직 자기 달력을 받아본 적이 없는 사람에게 미끼는 광고로 읽힌다(스펙 §9).
function EmptyMonthShell({ cta }: { cta: React.ReactNode }) {
  const { dates, today } = emptyMonthDates();
  const items = buildFreeItems(null);
  // 로그인 없이 되는 둘을 앞으로, 로그인이 필요한 오늘 타로를 뒤로.
  const reordered = [...items.filter((i) => i.key !== "tarot"), ...items.filter((i) => i.key === "tarot")];
  return (
    <main className="mx-auto w-full max-w-md space-y-4 p-4">
      <header>
        <h1 className="font-display text-2xl text-eye-purple">별마루</h1>
        <p className="text-sm text-text-light">무료로 다 보는 곳</p>
      </header>
      <section className="space-y-3">
        <CalendarGrid cells={[]} lockedDates={dates} todayDate={today} selectedDate={today} onSelect={() => {}} />
        <p className="text-center text-[13px] text-text-light">네 생일만 있으면 이 칸이 다 칠해져.</p>
        {cta}
      </section>
      <FreeList items={reordered} />
    </main>
  );
}
```

그리고 상태 분기 두 곳을 교체한다:

```tsx
  if (state.kind === "need_login") return (
    <EmptyMonthShell
      cta={
        <Link
          href="/login?next=/byeolmaru"
          onClick={() => trackUiEvent("byeolmaru_guest_peek_clicked", { meta: { card: "login_cta", gated: true } })}
          className="block rounded-xl bg-lilac-deep px-4 py-3 text-center text-cream"
        >
          카카오로 시작하고 내 달력 받기
        </Link>
      }
    />
  );
  if (state.kind === "no_profile") return (
    <EmptyMonthShell
      cta={
        <Link href="/mypage" className="block rounded-xl bg-lilac-deep px-4 py-3 text-center text-cream">
          생년월일 입력하러 가기
        </Link>
      }
    />
  );
```

⚠️ `CalendarGrid` 가 `cells: []` + `lockedDates` 만으로 그려지는지 **확인하라.** P5-2 구현에서 `slots.length === 0` 이면 `null` 을 반환하는데, `lockedDates` 가 있으면 slots 가 채워지므로 정상이다 — 코드를 읽어 확인할 것.

- [ ] **Step 3: 검증**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0

브라우저(쿠키를 지우고 비로그인 상태로):
1. `/byeolmaru` — **이번 달 전체가 점선 칸**으로 뜨고 날짜 숫자만 보인다
2. "네 생일만 있으면 이 칸이 다 칠해져." + 카카오 CTA
3. 목록이 **MBTI·별자리 → 오늘 타로** 순이다
4. **페이월이 없다**
5. 점선 칸을 눌러도 아무 일이 없다
6. 콘솔 에러 0

- [ ] **Step 4: 커밋**

```bash
git commit components/byeolmaru/ByeolmaruHub.tsx -m "feat(byeolmaru): 비로그인·생일 미입력에 안 칠해진 이번 달 (P5-3)"
git show --stat HEAD
```

---

## 최종 검증

- [ ] **전체 테스트 · 타입체크 · 빌드**

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*") 2>&1 | tail -4`
Expected: `# fail 0`

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0. 라우트 표에 `/byeolmaru/tarot` 가 있고 `ƒ Proxy (Middleware)` 가 보인다.

- [ ] **브라우저 — 상태 4종을 전부 본다**

| 상태 | 확인할 것 |
|---|---|
| 비로그인 | 안 칠해진 이번 달 · 생일 문구 · 카카오 CTA · 목록 MBTI/별자리 먼저 · 페이월 없음 |
| 생일 미입력 | 위와 같고 CTA 만 "생년월일 입력하러 가기" |
| 로그인·상대 0명 | 과거 칠해짐 + 오늘 히어로 + `＋ 인연 걸어두기` 금색 점선 · 목록 3종 · 미끼 노출 |
| 로그인·상대 1명+ | 상대 칩이 관계 색 · 칩 누르면 격자가 우리 버전 + 히어로 사라짐 |

- [ ] **동선 3개를 실제로 밟는다**

1. 허브 격자 과거 날 → `/byeolmaru/saju?date=` 로 그 날 상세
2. 목록 "오늘 타로" → `/byeolmaru/tarot` → 뽑기(안 뽑았으면) → 허브로 돌아오면 **목록 타일이 앞면**으로 바뀐다
3. 상대 칩 → 격자 우리 버전 → 칸 클릭 → `/byeolmaru/woori`

- [ ] **계측**

격자에서 과거 날을 누른 뒤 `/api/event` 페이로드에 `byeolmaru_day_selected` 의 `meta.offset` 이 음수로 찍히는지 확인한다(P5-2 가 살려둔 지표).
목록 행을 누르면 `byeolmaru_free_item_clicked` 가 `meta.item` 과 함께 찍히는지 확인한다.

- [ ] **375px 모바일**

달력 셀의 마크 글리프와 두 자리 날짜가 안 겹치는지, 오늘 칸 `scale(1.07)` 이 이웃과 안 겹치는지, 목록 카드 텍스트가 자연스럽게 흐르는지.

---

## Self-Review 기록

**스펙 §14 "P5-3" 항목별 커버리지**

| 스펙 항목 | 태스크 |
|---|---|
| 라이트 B 달력 판 (§4) | T1 |
| 오늘 히어로 (§3·§4) | T2 |
| 인연 칩 통합 (§8) | T6 |
| 무료 목록 3종 2탭 동형 (§3·§3-1) | T5 |
| 상태별 4화면 (§12) | T8 (+T7 이 로그인 2종을 덮는다) |
| 달력 히어로·미니 프리뷰 폐기 (§3) | T7 — `strip7` 삭제 |
| 요약은 안, 전문은 밖 (§7) | T3(`?date=`) · T4(`/byeolmaru/tarot`) · T7(격자 클릭 → 라우팅) |
| 오늘 타로 타일에 카드 이미지 (§12) | T5 `buildFreeItems` + T7 Step 2 |

**의도적으로 남긴 것**

- **미끼 내용은 P5-4 몫.** T7 은 `PremiumBlock` 을 그대로 끼워 자리만 잡는다. 스펙 §9 의 분량 칩·5섹션 이름·실제 첫 줄·"3일 무료로 열어보기" CTA·`meta.slot` 계측·당일 접힘은 P5-4 가 채운다.
  - ⚠️ **그 결과 비자격자는 같은 `PremiumBlock`("별콩이의 개인화")을 허브와 `/byeolmaru/saju` 두 곳에서 보게 된다.** P5-3 단계의 의도된 중간 상태다 — 스펙 §9 가 미끼를 자리별로 **다른 내용**으로 분산(나 탭 달력 아래 = 오늘 사주 리포트 / 인연 탭 = 우리 오늘 30일 / 타로 화면 = 깊은 해석)하라고 했고 그 분산이 P5-4 몫이라, 지금 한쪽을 지우면 P5-4 가 다시 만들어야 한다. **dev 검증에서 "같은 블록이 두 번 보인다"는 보고가 올라오면 버그가 아니라 이 상태임을 알릴 것.**
- **`/byeolmaru/woori` 화면 자체는 안 건드린다.** 칩에서 진입만 시킨다. 그 화면의 분량 확장은 P5-5.
- **`SubjectToggle` 은 살려둔다** — `/byeolmaru/woori` 가 쓴다. 허브는 `PartnerChips` 를 따로 쓴다(관계 색·빈 상태가 달라서다).
- **P5-2 가 넘긴 "허브 7점 스트립 월 경계" 문제는 이 플랜이 스트립 자체를 없애 소멸한다.**
- `saju-taste.json`·`getPairStaticLine` 의 시제 정정은 P5-2 에서 이미 끝났다.
