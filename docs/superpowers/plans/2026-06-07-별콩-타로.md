# 별콩 운세 타로 신상품 5종 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별콩 운세 섹션에 타로 신상품 5종(무료 오늘의 타로 1종 + 유료 도메인 4종)을 원샷 구조화 JSON 리포트 모델로 추가하고, /tarot/draw 덱 UI를 공유 컴포넌트로 추출해 재사용한다.

**Architecture:** 사주 운세와 동일한 백그라운드 생성 파이프라인(`readings` 행 즉시 삽입 → `after()`에서 Claude 호출·파싱·메시지 삽입)을 재사용한다. 타로는 사용자가 직접 카드를 뽑는 의식(ritual)을 거치며, 뽑힌 카드를 `drawn_cards`로 저장하고 프롬프트에 주입해 AI가 카드별 reading을 작성한다. 기존 `/tarot/draw`의 덱 UI를 `CardDrawRitual` 공유 컴포넌트로 추출해 채팅 타로와 운세 타로가 함께 쓴다.

**Tech Stack:** Next.js 16.2.6 App Router, React 19.2.4, TypeScript strict, Tailwind CSS v4, Claude API (@anthropic-ai/sdk). 테스트 러너 없음 → `npx tsc --noEmit` + `npm run build` + 수동 런타임 검증.

---

## File Structure

**신규 파일**
- `lib/fortune/tarot-report.ts` — 타로 리포트 JSON 파싱/직렬화 모듈 (compat-report.ts 미러)
- `components/tarot/CardDrawRitual.tsx` — /tarot/draw에서 추출한 카드 뽑기 의식 공유 컴포넌트
- `components/fortune/TarotReportView.tsx` — 타로 리포트 렌더링
- `components/fortune/tarot/TarotInput.tsx` — 타로 운세 입력(카드 뽑기) 진입 컴포넌트
- `app/fortune/tarot/[type]/page.tsx` — 타로 운세 페이지 라우트
- `app/api/fortune/tarot-daily-status/route.ts` — 오늘의 타로 무료 잔여 조회

**수정 파일**
- `lib/fortune/types.ts` — tarot_oneshot 제거, 타로 5종 추가, TAROT_POSITIONS
- `lib/fortune/prompt.ts` — tarotBlock + tarotGuide + FortuneInput.tarotCards
- `app/api/fortune/create/route.ts` — drawnCards 검증 + tarot 분기 + 파싱
- `app/fortune/result/page.tsx` — tarot 렌더 분기 + 공유 텍스트
- `app/tarot/draw/page.tsx` — CardDrawRitual을 쓰는 thin wrapper로 리팩터

**스키마 변경 없음** (`readings.drawn_cards` JSONB 이미 존재).

---

### Task 1: 타로 타입 정의 추가 (lib/fortune/types.ts)

**Files:**
- Modify: `lib/fortune/types.ts`

- [ ] **Step 1: FortuneType 유니온에서 tarot_oneshot 제거, 타로 5종 추가**

`FortuneType` 정의를 찾아 교체한다:

```typescript
export type FortuneType =
  | "daily"
  | "monthly"
  | "saju_full"
  | "tarot_daily"
  | "tarot_love"
  | "tarot_money"
  | "tarot_career"
  | "tarot_relation"
  | "compat"
  | "compat_social";

export type TarotFortuneType =
  | "tarot_daily"
  | "tarot_love"
  | "tarot_money"
  | "tarot_career"
  | "tarot_relation";
```

- [ ] **Step 2: FORTUNE_CONFIG에서 tarot_oneshot 항목 제거, 타로 5종 추가**

`tarot_oneshot` 항목을 통째로 삭제하고, 그 자리에 5개 항목을 추가한다 (FortuneConfig 필드: type, label, emoji, tagline, base, cost, freeLimit?, paidCost?, emotionTag, href, active):

```typescript
  tarot_daily: {
    type: "tarot_daily",
    label: "오늘의 타로",
    emoji: "🃏",
    tagline: "오늘 너에게 오는 한 장의 메시지",
    base: "tarot",
    cost: 0,
    freeLimit: 5,
    paidCost: 5,
    emotionTag: "fortune:tarot_daily",
    href: "/fortune/tarot/tarot_daily",
    active: true,
  },
  tarot_love: {
    type: "tarot_love",
    label: "연애운 타로",
    emoji: "💗",
    tagline: "지금 너의 연애, 카드 세 장으로 풀어볼게",
    base: "tarot",
    cost: 15,
    emotionTag: "fortune:tarot_love",
    href: "/fortune/tarot/tarot_love",
    active: true,
  },
  tarot_money: {
    type: "tarot_money",
    label: "금전운 타로",
    emoji: "💰",
    tagline: "돈의 흐름과 기회를 카드로 짚어줄게",
    base: "tarot",
    cost: 15,
    emotionTag: "fortune:tarot_money",
    href: "/fortune/tarot/tarot_money",
    active: true,
  },
  tarot_career: {
    type: "tarot_career",
    label: "직장·진로 타로",
    emoji: "💼",
    tagline: "일과 진로의 갈림길, 카드가 길을 보여줄게",
    base: "tarot",
    cost: 15,
    emotionTag: "fortune:tarot_career",
    href: "/fortune/tarot/tarot_career",
    active: true,
  },
  tarot_relation: {
    type: "tarot_relation",
    label: "인간관계 타로",
    emoji: "🤝",
    tagline: "사람 사이의 거리, 카드로 들여다볼게",
    base: "tarot",
    cost: 15,
    emotionTag: "fortune:tarot_relation",
    href: "/fortune/tarot/tarot_relation",
    active: true,
  },
```

- [ ] **Step 3: FORTUNE_LIST에서 tarot_oneshot 제거, 타로 5종 추가**

`FORTUNE_LIST` 배열에서 `FORTUNE_CONFIG.tarot_oneshot`을 제거하고 5종을 적절한 위치(사주 항목 뒤, compat 앞 권장)에 추가:

```typescript
  FORTUNE_CONFIG.tarot_daily,
  FORTUNE_CONFIG.tarot_love,
  FORTUNE_CONFIG.tarot_money,
  FORTUNE_CONFIG.tarot_career,
  FORTUNE_CONFIG.tarot_relation,
```

- [ ] **Step 4: MAX_TOKENS_BY_FORTUNE에서 tarot_oneshot 제거, 타로 5종 추가**

`MAX_TOKENS_BY_FORTUNE`에서 `tarot_oneshot: 2048` 줄을 삭제하고 추가:

```typescript
  tarot_daily: 1024,
  tarot_love: 2048,
  tarot_money: 2048,
  tarot_career: 2048,
  tarot_relation: 2048,
```

- [ ] **Step 5: TAROT_POSITIONS 맵과 getTarotPositions 헬퍼 추가**

파일 하단(FORTUNE_LIST 등 export 뒤)에 추가:

```typescript
// 타로 운세 타입별 카드 포지션. cardCount의 단일 진실 원천(length로 카드 수 결정).
export const TAROT_POSITIONS: Record<TarotFortuneType, string[]> = {
  tarot_daily: ["오늘의 메시지"],
  tarot_love: ["나의 마음", "상대의 마음", "관계의 흐름"],
  tarot_money: ["현재 흐름", "장애물", "기회·조언"],
  tarot_career: ["현재 위치", "변화의 기류", "나아갈 길"],
  tarot_relation: ["나", "상대·무리", "관계의 방향"],
};

export function getTarotPositions(type: string): string[] | null {
  return type in TAROT_POSITIONS
    ? TAROT_POSITIONS[type as TarotFortuneType]
    : null;
}
```

- [ ] **Step 6: 타입 체크**

Run: `npx tsc --noEmit`
Expected: tarot_oneshot 참조가 남은 파일(prompt.ts, create route 등)에서 에러가 날 수 있음 — 이는 후속 태스크에서 해결. types.ts 자체 문법 오류는 없어야 함. (이 시점에 다른 파일 에러만 있고 types.ts 줄 에러가 없으면 통과로 간주.)

- [ ] **Step 7: 커밋**

```bash
git add lib/fortune/types.ts
git commit -m "$(cat <<'EOF'
feat: add 5 tarot fortune types to fortune config

Replace placeholder tarot_oneshot with tarot_daily (free) and 4 paid
domain products (love/money/career/relation). Add TAROT_POSITIONS as
the single source of truth for card count per type.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 타로 리포트 파싱 모듈 (lib/fortune/tarot-report.ts)

**Files:**
- Create: `lib/fortune/tarot-report.ts`

이 모듈은 `lib/fortune/compat-report.ts`를 정확히 미러한다. 헬퍼 `isNonEmptyString`/`cleanStringArray`는 compat-report.ts와 동일 구현을 복제한다(파일 간 공유 import를 새로 만들지 않음 — 기존 패턴 유지).

- [ ] **Step 1: 파일 생성**

```typescript
// 타로 운세 리포트 JSON 파싱/직렬화. compat-report.ts와 동일 패턴.
// 저장 형식: messages.content 에 JSON 문자열, v:1.

export interface TarotReportCardAI {
  position: string;
  cardName: string;
  direction: "upright" | "reversed";
  reading: string;
}

export interface TarotReportAI {
  headline: string;
  cards: TarotReportCardAI[];
  summary: string;
  advice: string;
}

export interface TarotReport extends TarotReportAI {
  v: 1;
}

function isNonEmptyString(v: unknown): v is string {
  return typeof v === "string" && v.trim().length > 0;
}

/**
 * AI 원문에서 JSON 본문을 잘라 파싱한다. 형식이 어긋나면 null.
 */
export function parseTarotReportJson(raw: string): TarotReportAI | null {
  const start = raw.indexOf("{");
  const end = raw.lastIndexOf("}");
  if (start === -1 || end === -1 || end <= start) return null;

  let parsed: unknown;
  try {
    parsed = JSON.parse(raw.slice(start, end + 1));
  } catch {
    return null;
  }
  if (typeof parsed !== "object" || parsed === null) return null;
  const o = parsed as Record<string, unknown>;

  if (!isNonEmptyString(o.headline)) return null;
  if (!isNonEmptyString(o.summary)) return null;
  if (!isNonEmptyString(o.advice)) return null;
  if (!Array.isArray(o.cards) || o.cards.length === 0) return null;

  const cards: TarotReportCardAI[] = [];
  for (const c of o.cards) {
    if (typeof c !== "object" || c === null) return null;
    const cc = c as Record<string, unknown>;
    if (!isNonEmptyString(cc.position)) return null;
    if (!isNonEmptyString(cc.cardName)) return null;
    if (!isNonEmptyString(cc.reading)) return null;
    if (cc.direction !== "upright" && cc.direction !== "reversed") return null;
    cards.push({
      position: cc.position.trim(),
      cardName: cc.cardName.trim(),
      direction: cc.direction,
      reading: cc.reading.trim(),
    });
  }

  return {
    headline: o.headline.trim(),
    cards,
    summary: o.summary.trim(),
    advice: o.advice.trim(),
  };
}

export function buildTarotReport(ai: TarotReportAI): TarotReport {
  return { v: 1, ...ai };
}

export function serializeTarotReport(report: TarotReport): string {
  return JSON.stringify(report);
}

/**
 * 저장된 content 문자열을 TarotReport로 복원. 실패 시 null.
 */
export function tryParseStoredTarotReport(content: string): TarotReport | null {
  const trimmed = content.trimStart();
  if (!trimmed.startsWith("{")) return null;
  let parsed: unknown;
  try {
    parsed = JSON.parse(trimmed);
  } catch {
    return null;
  }
  if (typeof parsed !== "object" || parsed === null) return null;
  const o = parsed as Record<string, unknown>;
  if (o.v !== 1) return null;
  const ai = parseTarotReportJson(JSON.stringify(o));
  if (!ai) return null;
  return { v: 1, ...ai };
}
```

> 참고: `cleanStringArray`는 타로 리포트에서 쓰이지 않으므로 복제하지 않는다(YAGNI). compat-report.ts에는 advice가 배열이라 필요했지만 타로 advice는 단일 문자열.

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 이 파일 자체 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add lib/fortune/tarot-report.ts
git commit -m "$(cat <<'EOF'
feat: add tarot report parse/serialize module

Mirror of compat-report.ts for the tarot one-shot JSON report
(headline/cards/summary/advice, v:1).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 프롬프트에 타로 블록·가이드 추가 (lib/fortune/prompt.ts)

**Files:**
- Modify: `lib/fortune/prompt.ts`

- [ ] **Step 1: FortuneInput에 tarotCards 추가 + TarotDrawnForPrompt export**

`FortuneInput` 타입 정의를 찾아 필드 추가하고, 그 위(또는 적절한 위치)에 새 export 타입을 둔다:

```typescript
export interface TarotDrawnForPrompt {
  position: string;
  cardName: string;
  direction: "upright" | "reversed";
  uprightKeywords: string[];
  reversedKeywords: string[];
}
```

`FortuneInput`에 필드 추가:

```typescript
  tarotCards?: TarotDrawnForPrompt[];
```

- [ ] **Step 2: tarotBlock 헬퍼 추가**

기존 `sajuBlock` 함수 근처에 추가:

```typescript
function tarotBlock(cards: TarotDrawnForPrompt[]): string {
  const lines = cards.map((c, i) => {
    const kw =
      c.direction === "upright"
        ? c.uprightKeywords.join(", ")
        : c.reversedKeywords.join(", ");
    const dirKr = c.direction === "upright" ? "정방향" : "역방향";
    return `${i + 1}. [${c.position}] ${c.cardName} (${dirKr}) — 키워드: ${kw}`;
  });
  return [
    "## 뽑힌 타로 카드",
    "아래는 사용자가 직접 뽑은 카드다. position/cardName/direction은 그대로 echo 하고, reading만 새로 작성해라.",
    ...lines,
  ].join("\n");
}
```

- [ ] **Step 3: tarotGuide 헬퍼 + SECTION_GUIDE 5개 항목 추가, tarot_oneshot 제거**

`SECTION_GUIDE` 객체 정의 위에 헬퍼 추가:

```typescript
function tarotGuide(opts: { domainLabel: string; oneCard?: boolean }): string {
  const { domainLabel, oneCard } = opts;
  const cardsNote = oneCard
    ? "카드는 1장이다. reading은 3~5문장으로 따뜻하게."
    : "카드는 3장이다. 각 카드 reading은 4~6문장, 포지션의 의미를 살려서.";
  const lengthNote = oneCard
    ? "summary와 advice는 각각 2~3문장으로 짧게."
    : "summary는 세 카드를 엮은 5~7문장, advice는 3~5문장의 구체적 행동 제안.";
  return [
    `너는 별콩이야. ${domainLabel} 주제로 타로 리포트를 JSON으로만 출력해라.`,
    "마크다운/설명/코드펜스 없이 순수 JSON 객체 하나만 출력해라.",
    cardsNote,
    lengthNote,
    "스키마:",
    `{
  "headline": "한 줄 요약 (15자 내외)",
  "cards": [
    { "position": "주입된 position 그대로", "cardName": "주입된 cardName 그대로", "direction": "upright|reversed (주입값 그대로)", "reading": "이 카드 해석" }
  ],
  "summary": "전체 종합 해석",
  "advice": "구체적인 조언"
}`,
    "cards 배열 길이와 순서는 주입된 '뽑힌 타로 카드'와 정확히 일치시켜라.",
  ].join("\n");
}
```

`SECTION_GUIDE`에서 `tarot_oneshot` 항목을 삭제하고 5개 추가:

```typescript
  tarot_daily: tarotGuide({ domainLabel: "오늘의 운세", oneCard: true }),
  tarot_love: tarotGuide({ domainLabel: "연애운" }),
  tarot_money: tarotGuide({ domainLabel: "금전운" }),
  tarot_career: tarotGuide({ domainLabel: "직장·진로" }),
  tarot_relation: tarotGuide({ domainLabel: "인간관계" }),
```

- [ ] **Step 4: buildFortuneSystem에 tarot 분기 추가**

기존 분기 (`if ((type==="compat"||type==="compat_social")&&input.saju&&input.sajuB){...} else if (input.saju){parts.push(sajuBlock(input.saju))}`)에 `else if`를 추가:

```typescript
  } else if (input.tarotCards) {
    parts.push(tarotBlock(input.tarotCards));
  }
```

(기존 `else if (input.saju)` 분기 뒤에 이어 붙인다.)

- [ ] **Step 5: 타입 체크**

Run: `npx tsc --noEmit`
Expected: prompt.ts의 tarot_oneshot 에러 해소됨. types.ts에서 tarot 5종이 모두 정의됐으므로 SECTION_GUIDE/MAX_TOKENS Record<FortuneType> 완전성 통과. create route는 아직 tarot_oneshot 미참조이므로 무관.

- [ ] **Step 6: 커밋**

```bash
git add lib/fortune/prompt.ts
git commit -m "$(cat <<'EOF'
feat: inject drawn tarot cards into fortune prompt

Add tarotBlock (echoes position/cardName/direction + keywords) and
tarotGuide producing JSON section guides for all 5 tarot types.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: create 라우트에 타로 생성 분기 추가 (app/api/fortune/create/route.ts)

**Files:**
- Modify: `app/api/fortune/create/route.ts`

- [ ] **Step 1: import 추가**

상단 import 블록에 추가:

```typescript
import { getTarotPositions } from "@/lib/fortune/types";
import { getCard } from "@/lib/tarot/cards";
import type { TarotDrawnForPrompt } from "@/lib/fortune/prompt";
import {
  parseTarotReportJson,
  buildTarotReport,
  serializeTarotReport,
} from "@/lib/fortune/tarot-report";
```

(이미 import된 심볼이 있으면 중복 추가하지 말 것. `FORTUNE_CONFIG`/`MAX_TOKENS_BY_FORTUNE`/`buildFortuneSystem`/`FORTUNE_KICKOFF`/`isInt` 등은 기존 import 유지.)

- [ ] **Step 2: drawnCards 검증 헬퍼 추가**

파일 상단 헬퍼 영역(`isInt` 근처)에 추가:

```typescript
interface ValidDrawn {
  position: number;
  label: string;
  card_id: number;
  direction: "upright" | "reversed";
}

// 클라이언트가 보낸 drawnCards를 포지션 정의에 맞춰 검증·정규화.
function validateDrawnCards(
  raw: unknown,
  positions: string[]
): ValidDrawn[] | null {
  if (!Array.isArray(raw)) return null;
  if (raw.length !== positions.length) return null;
  const seen = new Set<number>();
  const out: ValidDrawn[] = [];
  for (let i = 0; i < raw.length; i++) {
    const c = raw[i];
    if (typeof c !== "object" || c === null) return null;
    const cc = c as Record<string, unknown>;
    const cardId = cc.card_id;
    if (!isInt(cardId) || cardId < 0 || cardId > 77) return null;
    if (seen.has(cardId)) return null;
    seen.add(cardId);
    const dir = cc.direction;
    if (dir !== "upright" && dir !== "reversed") return null;
    out.push({
      position: i,
      label: positions[i],
      card_id: cardId,
      direction: dir,
    });
  }
  return out;
}
```

- [ ] **Step 3: tarot 입력 파싱 + 검증을 cfg 결정 직후에 추가**

`cfg`가 유효(active)하다고 확인된 뒤, saju 계산 분기 앞에 타로 카드 검증을 배치한다:

```typescript
  let drawnCardsToStore: ValidDrawn[] | null = null;
  let tarotCardsForPrompt: TarotDrawnForPrompt[] | null = null;
  if (cfg.base === "tarot") {
    const positions = getTarotPositions(cfg.type);
    if (!positions) {
      return NextResponse.json({ error: "invalid_type" }, { status: 400 });
    }
    const validated = validateDrawnCards(body.drawnCards, positions);
    if (!validated) {
      return NextResponse.json({ error: "invalid_cards" }, { status: 400 });
    }
    drawnCardsToStore = validated;
    tarotCardsForPrompt = validated.map((d) => {
      const card = getCard(d.card_id);
      return {
        position: d.label,
        cardName: card.name_kr,
        direction: d.direction,
        uprightKeywords: card.upright,
        reversedKeywords: card.reversed,
      };
    });
  }
```

> `body.drawnCards`는 `body`가 이미 파싱된 객체이므로 `body.drawnCards as unknown`로 접근. body 타입에 `drawnCards?: unknown` 명시가 필요하면 body 파싱부 타입에 추가.

- [ ] **Step 4: systemInput에 tarot 분기 추가**

기존:
```typescript
  const systemInput = cfg.type === "compat" || cfg.type === "compat_social"
    ? { saju, sajuB, names }
    : { saju };
```
를 다음으로 교체:
```typescript
  const systemInput =
    cfg.type === "compat" || cfg.type === "compat_social"
      ? { saju, sajuB, names }
      : cfg.base === "tarot"
        ? { tarotCards: tarotCardsForPrompt! }
        : { saju };
```

- [ ] **Step 5: reading insert에 drawn_cards 추가**

reading insert 객체에 필드 추가:

```typescript
    drawn_cards: drawnCardsToStore,
```

(tarot가 아니면 null이 저장됨 — 기존 사주 동작과 동일.)

- [ ] **Step 6: after() 블록에 tarot 파싱 분기 추가**

기존 per-type 파싱 분기(daily/monthly/saju_full/compat) 옆에 추가. 1회 재시도 패턴은 기존과 동일:

```typescript
        } else if (cfg.base === "tarot") {
          let ai = parseTarotReportJson(rawText);
          if (!ai) {
            const retry = await generateOnce(
              system,
              [{ role: "user", content: FORTUNE_KICKOFF }],
              MAX_TOKENS_BY_FORTUNE[cfg.type]
            );
            ai = parseTarotReportJson(retry);
          }
          if (!ai) {
            await failGeneration("tarot_parse_failed");
            return;
          }
          storedContent = serializeTarotReport(buildTarotReport(ai));
        }
```

> `system`, `generateOnce`, `rawText`, `failGeneration`, `storedContent` 변수명은 기존 after() 블록의 것을 그대로 사용한다. 기존 분기와 동일한 변수 흐름을 따를 것.

- [ ] **Step 7: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 8: 커밋**

```bash
git add app/api/fortune/create/route.ts
git commit -m "$(cat <<'EOF'
feat: handle tarot fortune generation in create route

Validate drawnCards against TAROT_POSITIONS, inject card keywords into
the prompt, store drawn_cards, and parse the tarot JSON report in after().

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: CardDrawRitual 공유 컴포넌트 추출 (components/tarot/CardDrawRitual.tsx)

**Files:**
- Create: `components/tarot/CardDrawRitual.tsx`
- Reference: `app/tarot/draw/page.tsx` (1238 lines — 추출 원본)

**목표:** /tarot/draw의 덱 UI(HorizontalDeck, DeckScrollbar, SlotRow, Slot, DirectionToggle, FlyingCardOverlay)와 상수(CARD_W 등)·뽑기/방향 로직을 그대로 옮긴다. 결제·sessionStorage·라우팅은 가져오지 않는다(호출자 책임).

**Props:**
```typescript
interface CardDrawRitualProps {
  cardCount: number;
  slotLabels: string[];
  accent: string;
  ritualLabel: string;          // breathing pill + section divider 표시용
  completeLabel?: string;       // direction phase 완료 버튼 라벨 (기본 "결과 보기")
  relationshipLayout?: boolean; // true면 2x2 + 중앙 5번 배치
  backLabel?: string;           // pick phase 좌측 버튼 라벨 (없으면 버튼 숨김)
  onBack?: () => void;
  onComplete: (drawn: DrawnCard[]) => void;
}
```

- [ ] **Step 1: draw 페이지에서 순수 UI 조각을 복사**

`app/tarot/draw/page.tsx`에서 다음을 CardDrawRitual.tsx로 복사한다:
- 상수: `CARD_W=52, CARD_H=80, CARD_OVERLAP=18, CARD_STRIDE=34, FLIGHT_MS=440, SLOT_W=56, SLOT_H=84`
- 타입: `CardDirection`, `Phase`, `Rect`, `Flight`
- 서브컴포넌트: `FlyingCardOverlay`, `HorizontalDeck`, `DeckScrollbar`, `SlotRow`, `Slot`, `DirectionToggle`
- import: `getCard`, `getCardImagePath`, `CARD_BACK_IMAGE`, `shuffleDeck` (lib/tarot/cards), `DrawnCard` (lib/tarot/spreads)

`"use client"` 최상단 선언.

- [ ] **Step 2: 의식 상태·로직을 CardDrawRitual 함수로 옮김**

다음 상태와 핸들러를 그대로 옮긴다 (TarotDrawPage에서 결제/sessionStorage/라우팅 제외):
- 상태: `mounted, deck, isShuffling, slotCards, directions, pendingCardId, flight, activeSlotIndex, phase, isCompact`
- effect: isCompact resize, compact auto-scroll, **mount 시 `shuffleDeck()` + slotCards(`Array(cardCount).fill(null)`) + directions(`Array(cardCount).fill("upright")`) 초기화** (selection load 로직은 제거 — props로 cardCount/slotLabels 받음)
- derived: `pickedSlotByCard, firstEmptySlot, allFilled, activeSlot, canProceed`
- 핸들러: `commitPick, confirmPick(FLIP), removeCard, selectSlot, setDir, backToPick, goToDirection`

`labels`는 prop `slotLabels`를 사용. `accent`는 prop. `info.label` 대신 prop `ritualLabel`.

- [ ] **Step 3: 완료(onComplete) 연결**

direction phase의 "고민 상담 시작하기"에 해당하는 완료 버튼을 다음으로 바꾼다:

```typescript
  const handleComplete = () => {
    const drawn: DrawnCard[] = slotCards.map((cardId, i) => ({
      position: i,
      label: slotLabels[i] ?? `카드 ${i + 1}`,
      card_id: cardId as number,
      direction: directions[i] ?? "upright",
    }));
    onComplete(drawn);
  };
```

버튼 라벨은 `completeLabel ?? "결과 보기"`. pick phase 좌측 버튼은 `backLabel`이 있을 때만 렌더하고 `onClick={onBack}`.

- [ ] **Step 4: 레이아웃·하단 패딩 이식**

draw 페이지의 컨테이너 하단 패딩(containerPb) 로직, breathing pill(`ritualLabel`), section divider(phase 라벨), 별콩이 guide(`/profile.png`), SlotRow(relationshipLayout 분기), HorizontalDeck, FlyingCardOverlay를 그대로 이식한다. `relationshipLayout` prop이 SlotRow의 `isRelationship` 역할.

- [ ] **Step 5: 타입 체크**

Run: `npx tsc --noEmit`
Expected: CardDrawRitual.tsx 자체 에러 없음. (draw 페이지는 아직 미수정이라 중복 정의가 있어도 별개 파일이라 무관.)

- [ ] **Step 6: 커밋**

```bash
git add components/tarot/CardDrawRitual.tsx
git commit -m "$(cat <<'EOF'
feat: extract CardDrawRitual shared deck component

Pull deck/slot/direction UI and pick logic out of /tarot/draw into a
reusable component. Payment, sessionStorage, and routing stay with the
caller; the ritual emits drawn cards via onComplete.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: /tarot/draw를 thin wrapper로 리팩터 (app/tarot/draw/page.tsx)

**Files:**
- Modify: `app/tarot/draw/page.tsx`

**목표:** 추출한 UI/로직을 CardDrawRitual로 대체하고, 페이지는 selection 로드 + ProgressSteps + StarConfirmModal + sessionStorage + 라우팅만 남긴다. **채팅 타로 플로우는 동작이 완전히 동일해야 한다.**

- [ ] **Step 1: 중복 코드 제거, CardDrawRitual import**

CardDrawRitual로 옮긴 서브컴포넌트/상수/타입(Step 1, Task 5 목록)을 page.tsx에서 삭제하고:

```typescript
import CardDrawRitual from "@/components/tarot/CardDrawRitual";
```

- [ ] **Step 2: wrapper 상태·로직 정리**

페이지에 남길 것:
- selection 로드 effect (`TAROT_SPREAD_KEY` 읽기, 없으면 `router.replace("/tarot")`), `info`, `accent`, `cardCount`, `labels`(getPositionLabels)
- 결제: `showConfirm, balance, balanceLoading, openConfirm(fetch /api/stars/balance)`
- `pendingDrawn` 상태 추가: `const [pendingDrawn, setPendingDrawn] = useState<DrawnCard[] | null>(null);`
- `goToReading`: `pendingDrawn`을 `TAROT_DRAW_KEY`에 write 후 `router.push("/tarot/reading")`
- ProgressSteps (기존 유지)

- [ ] **Step 3: CardDrawRitual 렌더로 교체**

기존 덱/슬롯/CTA 마크업을 다음으로 대체:

```tsx
      <CardDrawRitual
        cardCount={cardCount}
        slotLabels={labels}
        accent={accent}
        ritualLabel={info.label}
        completeLabel="고민 상담 시작하기"
        relationshipLayout={info.spreadType === "relationship_5"}
        backLabel="리딩 방법 선택"
        onBack={() => router.push("/select")}
        onComplete={(drawn) => {
          setPendingDrawn(drawn);
          openConfirm();
        }}
      />
```

(`info.spreadType` 접근자가 실제 SPREAD_INFO 구조와 다르면 기존 `spreadType` 파생값을 사용.)

StarConfirmModal은 그대로 두되 `onConfirm={goToReading}`이 `pendingDrawn`을 사용하도록 한다.

- [ ] **Step 4: 타입 체크 + 빌드**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

Run: `npm run build`
Expected: 빌드 성공.

- [ ] **Step 5: 수동 회귀 검증**

`npm run dev` 후 채팅 타로 플로우 확인: /select에서 스프레드 선택 → /tarot/draw 진입 → 카드 pick → 방향 선택 → "고민 상담 시작하기" → StarConfirmModal → 결제 → /tarot/reading 정상. relationship_5(5장) 배치도 확인.
Expected: 추출 전과 동작 동일.

- [ ] **Step 6: 커밋**

```bash
git add app/tarot/draw/page.tsx
git commit -m "$(cat <<'EOF'
refactor: use CardDrawRitual in /tarot/draw page

Thin the page to selection load + ProgressSteps + StarConfirmModal +
sessionStorage + routing; the ritual now owns the deck UI. Chat tarot
flow unchanged.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 타로 리포트 뷰 (components/fortune/TarotReportView.tsx)

**Files:**
- Create: `components/fortune/TarotReportView.tsx`

- [ ] **Step 1: 파일 생성**

`TarotReport`(lib/fortune/tarot-report.ts)와 뽑힌 카드(이미지용 `DrawnCard[]`)를 받아 렌더. 카드 이미지는 `getCardImagePath(drawnCards[i].card_id)`. report.cards[i]와 drawnCards[i]를 인덱스로 zip.

```tsx
"use client";

import Image from "next/image";
import type { TarotReport } from "@/lib/fortune/tarot-report";
import { getCardImagePath } from "@/lib/tarot/cards";
import type { DrawnCard } from "@/lib/tarot/spreads";

export default function TarotReportView({
  report,
  drawnCards,
  accent = "#7c5cff",
}: {
  report: TarotReport;
  drawnCards: DrawnCard[];
  accent?: string;
}) {
  return (
    <div className="w-full max-w-md mx-auto px-5 flex flex-col gap-5 animate-fade-in">
      <h1 className="text-[20px] font-bold text-eye-purple text-center leading-snug">
        {report.headline}
      </h1>

      <div className="flex flex-col gap-4">
        {report.cards.map((card, i) => {
          const drawn = drawnCards[i];
          return (
            <div
              key={i}
              className="bg-cream-warm rounded-2xl p-4 border border-lilac-mid/30"
            >
              <div className="flex gap-3">
                {drawn && (
                  <div className="shrink-0">
                    <Image
                      src={getCardImagePath(drawn.card_id)}
                      alt={card.cardName}
                      width={64}
                      height={99}
                      className={`rounded-lg ${
                        card.direction === "reversed" ? "rotate-180" : ""
                      }`}
                      style={{ objectFit: "cover" }}
                    />
                  </div>
                )}
                <div className="flex-1 min-w-0">
                  <div className="text-[11px] text-text-light/70">
                    {card.position}
                  </div>
                  <div className="text-[14px] font-bold text-eye-purple">
                    {card.cardName}
                    <span className="ml-1.5 text-[11px] font-normal text-text-light/70">
                      {card.direction === "upright" ? "정방향" : "역방향"}
                    </span>
                  </div>
                </div>
              </div>
              <p className="text-[13px] text-text-light leading-relaxed mt-3 whitespace-pre-wrap">
                {card.reading}
              </p>
            </div>
          );
        })}
      </div>

      <div className="bg-cream-warm rounded-2xl p-4 border border-lilac-mid/30">
        <div className="text-[12px] font-bold mb-1.5" style={{ color: accent }}>
          종합 해석
        </div>
        <p className="text-[13px] text-text-light leading-relaxed whitespace-pre-wrap">
          {report.summary}
        </p>
      </div>

      <div className="bg-gradient-to-br from-eye-purple via-lilac-deep to-eye-purple rounded-2xl p-4 shadow-lg shadow-lilac-deep/30">
        <div className="text-[12px] font-bold text-gold-soft mb-1.5">
          별콩이의 조언
        </div>
        <p className="text-[13px] text-white/90 leading-relaxed whitespace-pre-wrap">
          {report.advice}
        </p>
      </div>
    </div>
  );
}
```

> 색/클래스는 기존 mypage/payments 등에서 쓰는 토큰(eye-purple, cream-warm, lilac-mid, gold-soft 등)을 따른다. 다른 FortuneReportView가 있으면 그 톤에 맞춰 조정.

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add components/fortune/TarotReportView.tsx
git commit -m "$(cat <<'EOF'
feat: add TarotReportView component

Render headline + per-card (image/position/direction/reading) +
summary + advice from a stored tarot report.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: 타로 입력 컴포넌트 + 라우트 (TarotInput + page)

**Files:**
- Create: `components/fortune/tarot/TarotInput.tsx`
- Create: `app/fortune/tarot/[type]/page.tsx`
- Reference: `components/fortune/compat/CompatInput.tsx`, `app/api/fortune/daily-status/route.ts`

- [ ] **Step 1: 라우트 페이지 생성 (Next 16 params Promise)**

```tsx
import { notFound } from "next/navigation";
import { FORTUNE_CONFIG, TAROT_POSITIONS } from "@/lib/fortune/types";
import TarotInput from "@/components/fortune/tarot/TarotInput";

const TAROT_TYPES = Object.keys(TAROT_POSITIONS);

export default async function TarotFortunePage({
  params,
}: {
  params: Promise<{ type: string }>;
}) {
  const { type } = await params;
  if (!TAROT_TYPES.includes(type)) notFound();
  const cfg = FORTUNE_CONFIG[type as keyof typeof FORTUNE_CONFIG];
  if (!cfg || cfg.base !== "tarot" || !cfg.active) notFound();
  return <TarotInput type={type as keyof typeof FORTUNE_CONFIG} />;
}
```

- [ ] **Step 2: TarotInput 생성**

CompatInput.tsx 패턴(auth 체크 → balance → confirm → POST create → balance-updated 이벤트 → router.push result)을 따른다. 차이점:
- 카드 입력은 `CardDrawRitual`로 받는다(질문 텍스트 없음).
- 무료 잔여가 있는 tarot_daily는 모달 없이 바로 생성.
- `cost === 0`이고 잔여 소진 시 paidCost로 모달.

```tsx
"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import CardDrawRitual from "@/components/tarot/CardDrawRitual";
import StarConfirmModal from "@/components/tarot/StarConfirmModal";
import {
  FORTUNE_CONFIG,
  TAROT_POSITIONS,
  type FortuneType,
} from "@/lib/fortune/types";
import type { DrawnCard } from "@/lib/tarot/spreads";

const ACCENT = "#7c5cff";

export default function TarotInput({ type }: { type: FortuneType }) {
  const router = useRouter();
  const cfg = FORTUNE_CONFIG[type];
  const positions = TAROT_POSITIONS[type as keyof typeof TAROT_POSITIONS];

  const [balance, setBalance] = useState(0);
  const [balanceLoading, setBalanceLoading] = useState(false);
  const [showConfirm, setShowConfirm] = useState(false);
  const [pendingDrawn, setPendingDrawn] = useState<DrawnCard[] | null>(null);
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // tarot_daily 무료 잔여 (다른 타입은 항상 유료)
  const [freeRemaining, setFreeRemaining] = useState<number | null>(null);
  const [effectiveCost, setEffectiveCost] = useState(cfg.cost);

  useEffect(() => {
    if (type !== "tarot_daily") return;
    void fetch("/api/fortune/tarot-daily-status", { cache: "no-store" })
      .then((x) => (x.ok ? x.json() : null))
      .then((d) => {
        if (!d) return;
        setFreeRemaining(d.remaining);
        setEffectiveCost(d.remaining > 0 ? 0 : d.nextCost);
      })
      .catch(() => {});
  }, [type]);

  const submit = async (drawn: DrawnCard[]) => {
    setSubmitting(true);
    setError(null);
    const res = await fetch("/api/fortune/create", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ type, drawnCards: drawn }),
    }).then((x) => x.json().then((j) => ({ ok: x.ok, status: x.status, j })));
    setSubmitting(false);
    if (!res.ok) {
      if (res.j?.error === "INSUFFICIENT_STARS") {
        setError("별이 부족해");
        setShowConfirm(true);
        return;
      }
      setError(res.j?.error ?? "생성에 실패했어");
      return;
    }
    window.dispatchEvent(new Event("byeolkong:balance-updated"));
    router.push(`/fortune/result?id=${res.j.id}`);
  };

  const onComplete = async (drawn: DrawnCard[]) => {
    // 인증 확인
    const me = await fetch("/api/auth/me", { cache: "no-store" })
      .then((x) => (x.ok ? x.json() : null))
      .catch(() => null);
    if (!me?.isAuthenticated) {
      window.location.href = "/login?next=" + encodeURIComponent(cfg.href);
      return;
    }
    if (effectiveCost === 0) {
      void submit(drawn);
      return;
    }
    // 유료 — 잔액 조회 후 모달
    setPendingDrawn(drawn);
    setBalanceLoading(true);
    const bal = await fetch("/api/stars/balance", { cache: "no-store" })
      .then((x) => (x.ok ? x.json() : null))
      .catch(() => null);
    setBalanceLoading(false);
    setBalance(bal?.balance ?? 0);
    setShowConfirm(true);
  };

  return (
    <main className="flex flex-1 flex-col items-center py-6 w-full animate-fade-in">
      <div className="w-full max-w-md mx-auto px-5 mb-4 text-center">
        <div className="text-[28px]">{cfg.emoji}</div>
        <h1 className="text-[20px] font-bold text-eye-purple mt-1">
          {cfg.label}
        </h1>
        <p className="text-[12px] text-text-light/80 mt-1">{cfg.tagline}</p>
        {type === "tarot_daily" && freeRemaining !== null && (
          <p className="text-[11px] text-gold-soft mt-1">
            {freeRemaining > 0
              ? `무료 ${freeRemaining}회 남음`
              : `무료 소진 · ⭐${cfg.paidCost}별`}
          </p>
        )}
      </div>

      <CardDrawRitual
        cardCount={positions.length}
        slotLabels={positions}
        accent={ACCENT}
        ritualLabel={cfg.label}
        completeLabel="리포트 받기"
        onComplete={onComplete}
      />

      {error && (
        <p className="text-[12px] text-rose-500 mt-3 px-5 text-center">
          {error}
        </p>
      )}

      {showConfirm && (
        <StarConfirmModal
          cost={effectiveCost}
          balance={balance}
          loading={balanceLoading || submitting}
          accent={ACCENT}
          title={cfg.label}
          spreadLabel={cfg.label}
          confirmLabel="리포트 받기"
          onConfirm={() => {
            if (pendingDrawn) void submit(pendingDrawn);
          }}
          onCharge={() => router.push("/shop")}
          onClose={() => setShowConfirm(false)}
        />
      )}
    </main>
  );
}
```

> StarConfirmModal의 실제 prop 시그니처(cost/balance/loading/accent/title/subtitle/confirmLabel/targetName/spreadLabel/onConfirm/onCharge/onClose)에 맞춰 조정. CompatInput이 쓰는 FortuneRefundModal/FortuneGeneratingScreen은 result 페이지가 생성 중 화면을 처리하므로 여기서는 생략(즉시 result로 push, 폴링은 result가 담당).

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add components/fortune/tarot/TarotInput.tsx app/fortune/tarot/[type]/page.tsx
git commit -m "$(cat <<'EOF'
feat: add tarot fortune input page and route

CardDrawRitual-based card draw → auth check → free/paid branch →
POST create → push to result. tarot_daily reads free-remaining status.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: result 페이지에 타로 렌더 분기 (app/fortune/result/page.tsx)

**Files:**
- Modify: `app/fortune/result/page.tsx`

- [ ] **Step 1: import 추가**

```typescript
import { tryParseStoredTarotReport, type TarotReport } from "@/lib/fortune/tarot-report";
import TarotReportView from "@/components/fortune/TarotReportView";
import type { DrawnCard } from "@/lib/tarot/spreads";
```

- [ ] **Step 2: tarot 파싱 + drawnCards 상태**

ft 계산 후, `isTarot` 판별과 파싱을 추가한다. 기존 compat/daily 파싱 흐름 옆에:

```typescript
  const isTarot = ft ? FORTUNE_CONFIG[ft].base === "tarot" : false;
```

리포트 파싱 영역에 tarot 분기 추가(assistant message content를 `tryParseStoredTarotReport`로):

```typescript
  const tarotReport: TarotReport | null = isTarot
    ? tryParseStoredTarotReport(assistantContent)
    : null;
  const tarotDrawn: DrawnCard[] =
    (r.reading.drawnCards as DrawnCard[] | null) ?? [];
```

(`assistantContent`는 기존 파싱부에서 쓰는 변수명에 맞춤. `r.reading.drawnCards`는 readings API가 이미 반환.)

- [ ] **Step 3: 렌더 dispatch에 tarot 추가**

기존 `isDaily/isMonthly/isSajuFull/isCompat` 렌더 분기에 추가:

```tsx
        {isTarot && tarotReport && (
          <TarotReportView report={tarotReport} drawnCards={tarotDrawn} />
        )}
```

헤더 숨김 조건(구조화 리포트일 때 헤더 숨기는 로직)에 `isTarot` 추가.

- [ ] **Step 4: buildTarotShareText 추가 + handleShare 분기**

기존 share 헬퍼 옆에 추가:

```typescript
function buildTarotShareText(label: string, report: TarotReport): string {
  const cards = report.cards
    .map((c) => `${c.position}: ${c.cardName}(${c.direction === "upright" ? "정" : "역"})`)
    .join("\n");
  return [
    `[별콩 타로] ${label}`,
    report.headline,
    "",
    cards,
    "",
    `✨ ${report.summary}`,
  ].join("\n");
}
```

`handleShare`에서 isTarot일 때 `buildTarotShareText(FORTUNE_CONFIG[ft].label, tarotReport)` 사용.

- [ ] **Step 5: 타입 체크 + 빌드**

Run: `npx tsc --noEmit`
Expected: 에러 없음.

Run: `npm run build`
Expected: 빌드 성공.

- [ ] **Step 6: 커밋**

```bash
git add app/fortune/result/page.tsx
git commit -m "$(cat <<'EOF'
feat: render tarot report on fortune result page

Parse stored tarot report, render TarotReportView with drawn cards,
and add tarot share text.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: 오늘의 타로 무료 잔여 API (app/api/fortune/tarot-daily-status/route.ts)

**Files:**
- Create: `app/api/fortune/tarot-daily-status/route.ts`
- Reference: `app/api/fortune/daily-status/route.ts`

- [ ] **Step 1: daily-status를 복사·수정**

daily-status/route.ts를 베이스로, `FORTUNE_CONFIG.daily` → `FORTUNE_CONFIG.tarot_daily`로 바꾸고, per-day 중복(todayId) 로직을 제거(평생 누적 무료):

```typescript
import { NextResponse } from "next/server";
import { getServerSession } from "@/lib/session"; // 기존 daily-status의 세션 헬퍼명에 맞춤
import { supabaseAdmin } from "@/lib/supabase";    // 기존 import에 맞춤
import { FORTUNE_CONFIG } from "@/lib/fortune/types";

export const dynamic = "force-dynamic";

export async function GET() {
  const session = await getServerSession();
  const cfg = FORTUNE_CONFIG.tarot_daily;
  const limit = cfg.freeLimit ?? 0;
  const nextCost = cfg.paidCost ?? 0;

  if (!session?.userId) {
    return NextResponse.json({ used: 0, limit, remaining: limit, nextCost });
  }

  const { count } = await supabaseAdmin
    .from("readings")
    .select("id", { count: "exact", head: true })
    .eq("user_id", session.userId)
    .eq("emotion_tag", cfg.emotionTag)
    .eq("stars_spent", 0);

  const used = count ?? 0;
  const remaining = Math.max(0, limit - used);
  return NextResponse.json({ used, limit, remaining, nextCost });
}
```

> import 경로/세션 헬퍼/supabase 클라이언트 이름은 기존 daily-status/route.ts와 동일하게 맞춘다(복사 베이스).

- [ ] **Step 2: 타입 체크 + 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 성공.

- [ ] **Step 3: 커밋**

```bash
git add app/api/fortune/tarot-daily-status/route.ts
git commit -m "$(cat <<'EOF'
feat: add tarot-daily-status free-remaining endpoint

Count lifetime free tarot_daily readings to decide free vs paidCost.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: 최종 검증 + 배포

**Files:** 없음 (검증/배포만)

- [ ] **Step 1: 전체 타입 체크 + 빌드**

Run: `npx tsc --noEmit`
Expected: 에러 0.

Run: `npm run build`
Expected: 빌드 성공.

- [ ] **Step 2: 수동 런타임 검증**

`npm run dev` 후:
1. `/fortune/tarot/tarot_daily` → 카드 1장 뽑기 → 무료 생성 → result에 1카드 리포트
2. `/fortune/tarot/tarot_love` → 카드 3장 → 방향 선택 → 모달(15별) → 생성 → result에 3카드 리포트
3. money/career/relation 각 진입·라벨 확인
4. 무료 5회 소진 후 tarot_daily가 5별 모달로 전환되는지(어려우면 status API 응답으로 확인)
5. 채팅 타로(/select→/tarot/draw) 회귀 정상

- [ ] **Step 3: 배포 (dev → main fast-forward)**

```bash
git push origin dev
git push origin dev:main
```

Expected: 두 push 모두 성공. (`.gitignore`는 스테이징하지 않음. 파일은 이름으로만 add.)

---

## Self-Review Notes

**의도된 스펙 이탈:**
- 별도 `TAROT_DAILY_FREE_LIMIT`/`TAROT_DAILY_PAID_COST` 상수를 만들지 않고 `FORTUNE_CONFIG.tarot_daily.freeLimit`/`paidCost`에서 읽음 (DRY).
- `cleanStringArray`는 타로 advice가 단일 문자열이라 tarot-report.ts에 복제하지 않음 (YAGNI).
- 랜딩 `app/fortune/page.tsx`는 스펙 §8 수정 목록에 없어 손대지 않음 — tarot_daily 무료 잔여 뱃지는 랜딩에 표시되지 않음(경미한 비일관성, 의도적).

**가격(임시):** 도메인 4종 15별, tarot_daily 유료 5별 — 사용자가 추후 전체 재브레인스토밍 예정.
