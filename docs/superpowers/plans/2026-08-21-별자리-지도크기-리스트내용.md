# 별자리 지도 크기·리스트 내용 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 포커스 지도 스케일 고정+조금 크게+자동스크롤 제거, "무리"→"같은 결", 순위 리스트 재디자인(순위·점수·분류 칩·등급→상세), 상세 A(줄글)+B(키워드)+D(잘맞는점/조심).

**Spec:** `docs/superpowers/specs/2026-08-21-별자리-지도크기-리스트내용-design.md`

## 파일
- `lib/byeoljari/relation-detail.ts`(신규)+test · `lib/byeoljari/bond-filter.ts` · `lib/byeoljari/scale.ts` · `components/byeoljari/InyeonDetail.tsx` · `components/byeoljari/ConstellationView.tsx`

---

### Task 1: relation-detail.ts (신규, TDD)

**Files:** Create `lib/byeoljari/relation-detail.ts`, `lib/byeoljari/relation-detail.test.ts`

- [ ] **Step 1: 실패 테스트**
```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { relationDetail } from "./relation-detail.ts";

test("relationDetail — 생아 기본", () => {
  const d = relationDetail("생아", {});
  assert.equal(d.prose, "곁에 있으면 기운을 받아 힘이 나는 사이");
  assert.ok(d.good && d.caution);
  assert.deepEqual(d.keywords, ["보완", "안정"]);
});
test("relationDetail — 특별 인연 키워드 append", () => {
  const d = relationDetail("극아", { heavenlyCombo: true, triadShared: true });
  assert.ok(d.keywords.includes("자극"));
  assert.ok(d.keywords.includes("끌림"));
  assert.ok(d.keywords.includes("같은 결"));
  assert.ok(!d.keywords.includes("결속"));
});
test("relationDetail — 미지 element 폴백", () => {
  const d = relationDetail("?", {});
  assert.equal(d.prose, "이어져 있는 사이");
  assert.deepEqual(d.keywords, []);
});
```
- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/byeoljari/relation-detail.test.ts`
- [ ] **Step 3: 구현**
```ts
// 오행 관계 기반 상세 콘텐츠(담백 서술·"나" 명시 없이 pivot 관점). 순수.
export interface RelationDetail {
  prose: string;
  keywords: string[];
  good: string;
  caution: string;
}

const BASE: Record<string, RelationDetail> = {
  생아: { prose: "곁에 있으면 기운을 받아 힘이 나는 사이", good: "함께면 안정되고 북돋아져", caution: "자꾸 기대게 될 수도 있어", keywords: ["보완", "안정"] },
  아생: { prose: "챙기고 베풀게 되는 사이", good: "돌봐주며 뿌듯한 사이", caution: "혼자 애쓰다 지칠 수도 있어", keywords: ["돌봄", "베풂"] },
  극아: { prose: "팽팽하게 자극이 오가는 사이", good: "긴장이 성장으로 이어져", caution: "세게 부딪힐 수 있어", keywords: ["자극", "긴장"] },
  아극: { prose: "내가 끌고 나가게 되는 흐름", good: "주도적으로 이끌어가", caution: "밀어붙이게 될 수도 있어", keywords: ["주도", "추진"] },
  비화: { prose: "닮아서 말 안 해도 통하는 사이", good: "편하고 공감이 잘 돼", caution: "비슷해서 무뎌질 수도 있어", keywords: ["공감", "편안"] },
};
const FALLBACK: RelationDetail = { prose: "이어져 있는 사이", good: "", caution: "", keywords: [] };

export function relationDetail(
  element: string,
  bond: { heavenlyCombo?: boolean; sixCombo?: boolean; triadShared?: boolean }
): RelationDetail {
  const base = BASE[element] ?? FALLBACK;
  const keywords = [...base.keywords];
  if (bond.heavenlyCombo) keywords.push("끌림");
  if (bond.sixCombo) keywords.push("결속");
  if (bond.triadShared) keywords.push("같은 결");
  return { prose: base.prose, good: base.good, caution: base.caution, keywords };
}
```
- [ ] **Step 4: 통과 확인** + 회귀 green.
- [ ] **Step 5: 커밋** — `feat(byeoljari): relation-detail — 오행 관계별 줄글·키워드·잘맞는점/조심 TDD`

---

### Task 2: "무리"→"같은 결" + 지도 조금 크게

**Files:** `lib/byeoljari/bond-filter.ts`, `lib/byeoljari/scale.ts`

- [ ] **Step 1: bond-filter 라벨** — `BOND_FILTER_LABEL` 의 `triad: "무리"` → `triad: "같은 결"`.
- [ ] **Step 2: scale 소폭 상향**(지도 조금 크게, 반지름 34 유지) — FEW/MANY 의 별·라벨·나 크기를 ~15% 상향. 예:
  - FEW: `hostOuter 6→6.8, hostInner 3.3→3.7, starOuter 4→4.6, starInner 2.2→2.5, meR 4→4.6, hitR 6→6.8, labelFont 2.8→3, hostLabelFont 3.4→3.6` (goldLineWidth/lineWidth 유지 또는 소폭↑).
  - MANY: `hostOuter 4.5→5.2, hostInner 2.48→2.85, starOuter 2.6→3, starInner 1.43→1.65, meR 2.8→3.2, hitR 4.5→5.2, labelFont 2→2.3, hostLabelFont 2.6→2.9`.
  - `scale.test.ts`가 hostOuter/starOuter/lineWidth 값을 단정하므로 **테스트도 새 값으로 갱신**(경계·보간·showLabels 로직은 유지). t=0.5(11명) 보간 기대값도 재계산.
- [ ] **Step 3: 검증** — `node --import tsx --test lib/byeoljari/scale.test.ts` green(갱신값), `bond-filter.test.ts`는 라벨 단정 있으면 "같은 결"로 갱신.
- [ ] **Step 4: 커밋** — `feat(byeoljari): 삼합 라벨 '같은 결' + 지도 크기 소폭 확대`

---

### Task 3: InyeonDetail — A/B/D 콘텐츠 + 등급 위치 + "같은 결" 메시지

**Files:** `components/byeoljari/InyeonDetail.tsx`

- [ ] **Step 1** — import `relationDetail`. 삼합-only 폴백 메시지 "같은 무리 (삼합)" → **"같은 결 (삼합)"**.
- [ ] **Step 2** — `oriented` 있을 때, `인연 점수 블록`(등급 포함, 유지) 다음에 A/B/D 삽입:
```tsx
      {(() => {
        const rd = relationDetail(oriented.element, { heavenlyCombo, sixCombo, triadShared });
        return (
          <>
            <p className="text-sm text-eye-purple">{rd.prose}</p>
            {rd.keywords.length > 0 && (
              <div className="flex flex-wrap gap-1">
                {rd.keywords.map((k) => (
                  <span key={k} className="rounded-full bg-lilac-soft px-2 py-0.5 text-xs text-eye-purple">#{k}</span>
                ))}
              </div>
            )}
          </>
        );
      })()}
```
  - **pivotIsMe true**: 위(A/B) + 기존 40칸 카피 두 박스(내가 보는/그가 보는) 유지 + D + 뱃지.
  - **pivotIsMe false**: 위(A/B) 뒤 기존 "오행 라벨 단독 줄" **제거**(A prose가 대체) + D + 뱃지.
  - **D**: 카피/오행 박스 다음, 뱃지 앞:
```tsx
            {(rd.good || rd.caution) && (
              <div className="rounded-xl bg-white/60 p-3 text-sm">
                {rd.good && <div className="text-eye-purple">잘 맞는 점 · {rd.good}</div>}
                {rd.caution && <div className="mt-1 text-text-light">살짝 조심 · {rd.caution}</div>}
              </div>
            )}
```
  (rd 한 번만 계산해 A/B/D 공용 — 위 IIFE 대신 컴포넌트 상단 `const rd = oriented ? relationDetail(...) : null;` 로 올려도 됨.)
- [ ] **Step 3** — `triadShared` prop 는 이미 있음(직전 폴리시). element·bond 는 `oriented.element`/`heavenlyCombo`/`sixCombo`/`triadShared`.
- [ ] **Step 4: 검증 + 커밋** — tsc clean. `feat(byeoljari): 상세 콘텐츠 풍성화 — 줄글+키워드+잘맞는점/조심, '같은 결'`

---

### Task 4: ConstellationView — 스케일 고정·자동스크롤 제거·순위 재디자인·"같은 결" sweep

**Files:** `components/byeoljari/ConstellationView.tsx`

- [ ] **Step 1: 스케일 고정** — `sizes = useMemo(() => scaleForCount(graph.nodes.length), [graph.nodes.length])` (viewGraph → graph). 포커스해도 노드 크기 동일.
- [ ] **Step 2: 자동 스크롤 제거(포커스)** — scrollIntoView 이펙트에서 `focusId` 트리거 제거. `expandedNeighborId`(카드 내 행 펼침)만 스크롤(지도 안 밀게 `block:"nearest"`), `listOpenId`는 유지. 예:
```tsx
  useEffect(() => {
    if (expandedNeighborId && detailRef.current) detailRef.current.scrollIntoView({ block: "nearest" });
  }, [expandedNeighborId]);
```
  (focusId 진입 시엔 스크롤 안 함 → 지도 그대로.)
- [ ] **Step 3: 순위 재디자인** — ranking `.map` 행:
  - 표시: **순위 · 이름 · 점수(강조) · 분류 칩**. `등급(grade.label)`은 행에서 제거(펼침 상세로 이동 — 이미 InyeonDetail 인연 점수 블록에 등급 있음).
  - 분류 칩: 그 사람 edge(원본 `graph.edges`)의 `heavenlyCombo`→끌림/`sixCombo`→결속/`triadShared`→같은 결. 색: 끌림 `bg-gold-soft/60`, 결속 `bg-lilac-soft`, 같은결 `bg-[#DCF3EC] text-[#1f6b57]`(그 외 텍스트 `text-eye-purple`). 1위(다크) 행에선 칩을 `bg-white/15 text-cream-warm` 등 대비 유지.
  - ranking 항목에 각 종류 flag 필요 → ranking useMemo가 이미 `special` 계산 중이니, 대신 `heavenlyCombo`/`sixCombo`/`triadShared`도 담아 반환하도록 확장(또는 행에서 edge 재조회).
- [ ] **Step 4: "무리"→"같은 결" sweep(View)** — 선 범례의 "무리" → "같은 결". focusSummary 조립 문자열 "같은 무리 ${triad}" → "같은 결 ${triad}". 이웃 태그 계산의 "무리" → "같은 결"(또는 `BOND_FILTER_LABEL.triad` 참조).
- [ ] **Step 5: 검증 + 커밋** — tsc clean, grep "무리" 잔재 없음(View). `feat(byeoljari): 포커스 스케일 고정·자동스크롤 제거·순위 분류칩 재디자인·같은 결`

---

### Task 5: 브라우저 E2E

- [ ] dev 로엔 `7uBv0cLKet`. 별 탭 → **지도 크기 안 튐**(overview↔포커스 노드 동일) + **지도 안 밀림**(자동스크롤 없음).
- [ ] 순위 행: 순위·이름·점수·**분류 칩(끌림 골드/결속 라일락/같은결 민트)**, 등급은 펼침에.
- [ ] 상세 펼침: A 줄글 + B `#키워드` 칩 + D "잘 맞는 점/살짝 조심". "무리" 없고 "같은 결".
- [ ] 콘솔0.

---

## Self-Review
- **커버리지:** 지도 스케일 고정(T4-1)·조금 크게(T2-2)·자동스크롤 제거(T4-2)·"같은 결"(T2-1+T3+T4-4)·순위 분류칩(T4-3)·상세 A/B/D(T1+T3). 매핑.
- **타입 일관성:** `relationDetail`(T1) ↔ InyeonDetail(T3). `BOND_FILTER_LABEL.triad`(T2) 단일 원천. scale 값 변경 ↔ scale.test 갱신(T2).
- **빌드 유지:** T1·T2 독립. T3(InyeonDetail) T1 사용. T4(View) 독립적 파일. 각 tsc/유닛 green.
- **주의:** scale.test.ts 는 값 단정 있음 → T2에서 반드시 동반 갱신(보간 t=0.5 기대값 재계산). ranking 행에 bond flag 넘기려면 useMemo 반환 확장.
