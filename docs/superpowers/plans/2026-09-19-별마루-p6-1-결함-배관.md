# 별마루 P6-1 — 결함 6건 수정 + MarkdownLite 배선 + 카드 해석 캐시 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루 유료 콘텐츠 경로의 확증된 결함 6건을 고치고, 뒤따르는 P6-2(모델 상향·분량·구조화)가 올라탈 배관(날짜 파라미터 · 일별 캐시 · 마크다운 렌더)을 깐다.

**Architecture:** 화면 구조는 건드리지 않는다. ①프롬프트 토큰 치환을 순수 함수로 뽑아 다중 치환 버그를 테스트로 고정하고 ②날짜 정책(과거=캐시만 / 오늘·미래3일=생성 / 그 밖=거부)을 순수 모듈로 신설해 라우트 두 곳이 같은 규칙을 쓰게 하며 ③`card-narrative` 에 `pair-narrative` 와 동형인 (유저,날짜) 캐시를 얹고 ④`MarkdownLite` 를 별마루 서술 렌더 2곳에 배선한다. DB 접근 래퍼는 유닛 테스트가 어려우므로, **판정 로직을 전부 순수 함수로 분리해 테스트하고** 라우트는 수동 curl 로 검증한다.

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript strict · `node --test`(tsx) · Supabase(마이그레이션 1장) · `gpt-5-nano`(이 단계에선 모델 변경 없음 — P6-2 몫)

**스펙:** `docs/superpowers/specs/2026-09-19-별마루-p6-화면개편-콘텐츠-design.md` §7(결함 6건) · §6-5(마크다운) · §11(단계 분해)

---

## 파일 구조

| 파일 | 책임 | 작업 |
|---|---|---|
| `lib/fortune/prompt.ts` | 운세 프롬프트 빌드 | 수정 — 토큰 치환 순수 함수 추출 + 날짜 컨텍스트 줄 |
| `lib/fortune/prompt.test.ts` | 위 순수 함수 계약 | **신규** |
| `lib/byeolmaru/report-date.ts` | 리포트 날짜 정책(순수) | **신규** |
| `lib/byeolmaru/report-date.test.ts` | 정책 계약 | **신규** |
| `lib/fortune/daily-report.ts` | daily JSON 파서 | 수정 — `note` 접두 중복 제거 |
| `lib/fortune/daily-report.test.ts` | 파서 계약 | **신규** |
| `lib/byeolmaru/card-narrative.ts` | 카드 해석 캐시 래퍼 | **신규** (`pair-narrative.ts` 동형) |
| `supabase/migrations/20260919000000_byeolmaru_card_narrative.sql` | 캐시 테이블 | **신규** |
| `lib/byeolmaru/daily-card.ts` | 카드 조회/기록 래퍼 | 수정 — `getTodayCard` → `getCardOn` |
| `app/api/byeolmaru/daily-report/route.ts` | 유료 사주 리포트 | 수정 — `?date=` 수용 |
| `app/api/byeolmaru/daily-card/route.ts` | 오늘 카드 | 수정 — GET `?date=` 수용 |
| `app/api/byeolmaru/card-narrative/route.ts` | 카드 해석 | 수정 — 캐시 조회/저장 |
| `components/fortune/DailyReportCard.tsx` | 리포트 카드 | 수정 — `dayWord` prop |
| `components/byeolmaru/SajuTodayView.tsx` | 오늘 사주 화면 | 수정 — 선택 날짜 전달 |
| `components/byeolmaru/PremiumBlock.tsx` | 서술 블록 | 수정 — `MarkdownLite` |
| `components/byeolmaru/DailyCardBlock.tsx` | 오늘 카드 블록 | 수정 — `MarkdownLite` |

**전체 게이트(모든 태스크 후 Task 10에서 1회):**
```bash
node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")
npx tsc --noEmit
npm run build
```

---

## Task 1: 프롬프트 토큰 치환 버그 (결함 1)

`lib/fortune/prompt.ts:691` 이 `.replace("{{TODAY_PILLAR}}", …)` 인데 daily 템플릿에 `{{TODAY_PILLAR}}` 가 두 번 나온다(192행 `stars`, 195행 `intro`). `String.replace` 는 첫 번째만 바꾸므로 `intro` 쪽이 치환되지 않은 채 모델에 들어가고, 모델이 그걸 메우며 `오늘 들어온 두 글자({"병신"})는` 이 나온다.

**Files:**
- Modify: `lib/fortune/prompt.ts:686-693`
- Test: `lib/fortune/prompt.test.ts` (신규)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/fortune/prompt.test.ts` 를 새로 만든다:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { fillGuideTokens } from "./prompt.ts";

test("fillGuideTokens: 같은 토큰이 여러 번 나와도 전부 치환된다", () => {
  const guide = [
    `"stars": <오늘 일진({{TODAY_PILLAR}})과 조화>`,
    `"intro": "<'오늘 들어온 두 글자'({{TODAY_PILLAR}}) 풀이>"`,
    `오늘 날짜: {{TODAY}}`,
    `이번 달: {{THIS_MONTH}} / 월건 {{THIS_MONTH_PILLAR}} / 재확인 {{THIS_MONTH_PILLAR}}`,
  ].join("\n");

  const out = fillGuideTokens(guide, {
    today: "2026년 9월 19일 토요일",
    thisMonth: "2026년 9월",
    todayPillar: "병신",
    thisMonthPillar: "정유",
  });

  assert.equal(/\{\{[A-Z_]+\}\}/.test(out), false, `치환 안 된 토큰 잔여: ${out}`);
  assert.equal(out.match(/병신/g)?.length, 2);
  assert.equal(out.match(/정유/g)?.length, 2);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/fortune/prompt.test.ts`
Expected: FAIL — `fillGuideTokens` 가 없어 import 에서 터진다.

- [ ] **Step 3: 순수 함수를 추출하고 `replaceAll` 로 고친다**

`lib/fortune/prompt.ts` 에서 `buildFortuneSystem` 바로 위(현재 658행 근처)에 함수를 추가한다:

```ts
/** SECTION_GUIDE 의 `{{TOKEN}}` 치환. 🔴 전부 `replaceAll` — 같은 토큰이 한 템플릿에 여러 번 나온다
 *  (daily 의 {{TODAY_PILLAR}} 는 stars·intro 두 곳). `replace` 는 첫 개만 바꿔서, 남은 토큰을
 *  모델이 제 나름대로 메우며 `({"병신"})` 같은 JSON 찌꺼기가 본문에 새어 나왔다. */
export function fillGuideTokens(
  guide: string,
  v: { today: string; thisMonth: string; todayPillar: string; thisMonthPillar: string }
): string {
  return guide
    .replaceAll("{{TODAY}}", v.today)
    .replaceAll("{{THIS_MONTH}}", v.thisMonth)
    .replaceAll("{{TODAY_PILLAR}}", v.todayPillar)
    .replaceAll("{{THIS_MONTH_PILLAR}}", v.thisMonthPillar);
}
```

그리고 기존 치환부(686-693행의 `parts.push(SECTION_GUIDE[type].replace(...)...)`)를 이걸로 바꾼다:

```ts
  parts.push(
    fillGuideTokens(SECTION_GUIDE[type], {
      today: TODAY_KR(),
      thisMonth: THIS_MONTH_KR(),
      todayPillar,
      thisMonthPillar,
    })
  );
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/fortune/prompt.test.ts`
Expected: PASS (1 test)

- [ ] **Step 5: 커밋**

```bash
git add lib/fortune/prompt.ts lib/fortune/prompt.test.ts
git commit -F - <<'EOF'
fix(fortune): 프롬프트 토큰을 replaceAll 로 치환 (P6-1 결함1)

daily 템플릿의 {{TODAY_PILLAR}} 가 stars·intro 두 곳에 있는데 replace 가
첫 개만 바꿔서, 남은 토큰을 모델이 메우며 본문에 ({"병신"}) 같은 JSON
찌꺼기가 새어 나왔다. 치환을 순수 함수로 뽑고 전부 replaceAll 로.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 2: 리포트 날짜 정책 모듈 (결함 2·3의 공통 규칙)

스펙 §4 — 과거는 **있던 것만**(소급 생성 금지), 오늘과 **앞으로 3일**은 생성, 그 너머는 거부.

**Files:**
- Create: `lib/byeolmaru/report-date.ts`
- Test: `lib/byeolmaru/report-date.test.ts`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { isIsoDate, reportDatePolicy, dayWordFor, FUTURE_REPORT_DAYS } from "./report-date.ts";

const T = "2026-09-19";

test("isIsoDate: YYYY-MM-DD 만 허용", () => {
  assert.equal(isIsoDate("2026-09-19"), true);
  assert.equal(isIsoDate("2026-9-19"), false);
  assert.equal(isIsoDate("20260919"), false);
  assert.equal(isIsoDate(""), false);
  assert.equal(isIsoDate("2026-13-01"), false); // 달력상 없는 날짜
  assert.equal(isIsoDate("2026-02-30"), false);
});

test("reportDatePolicy: 과거는 캐시만, 오늘·앞으로 3일은 생성, 그 너머는 거부", () => {
  assert.equal(reportDatePolicy("2026-09-18", T), "cache_only");
  assert.equal(reportDatePolicy("2026-08-01", T), "cache_only");
  assert.equal(reportDatePolicy(T, T), "generate");
  assert.equal(reportDatePolicy("2026-09-20", T), "generate");
  assert.equal(reportDatePolicy("2026-09-22", T), "generate"); // 오늘+3
  assert.equal(reportDatePolicy("2026-09-23", T), "out_of_range"); // 오늘+4
  assert.equal(reportDatePolicy("nope", T), "out_of_range");
});

test("reportDatePolicy: 월 경계를 건너도 일수로 센다", () => {
  assert.equal(reportDatePolicy("2026-10-02", "2026-09-30"), "generate"); // +2
  assert.equal(reportDatePolicy("2026-10-04", "2026-09-30"), "out_of_range"); // +4
});

test("dayWordFor: 오늘만 '오늘', 나머지는 '그날'", () => {
  assert.equal(dayWordFor(T, T), "오늘");
  assert.equal(dayWordFor("2026-09-18", T), "그날");
  assert.equal(dayWordFor("2026-09-20", T), "그날");
});

test("FUTURE_REPORT_DAYS 는 스트립 블러 칸 수와 같은 3", () => {
  assert.equal(FUTURE_REPORT_DAYS, 3);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/report-date.test.ts`
Expected: FAIL — 모듈이 없다.

- [ ] **Step 3: 모듈을 만든다**

`lib/byeolmaru/report-date.ts`:

```ts
// lib/byeolmaru/report-date.ts — 리포트 대상 날짜 정책(순수 · 네트워크 0).
// 스펙 §4: 과거는 "있던 것만"(소급 생성 금지 — 그때 받은 글이 아니면 기록이 아니다),
// 오늘과 앞으로 3일은 생성. 3일은 허브 롤링 스트립의 블러 칸 수와 같은 숫자다.

/** 미래 리포트를 만들어주는 일수. 허브 스트립의 블러 칸 수와 반드시 같게 유지할 것. */
export const FUTURE_REPORT_DAYS = 3;

/** YYYY-MM-DD 이고 달력상 실재하는 날짜인가. */
export function isIsoDate(s: unknown): s is string {
  if (typeof s !== "string" || !/^\d{4}-\d{2}-\d{2}$/.test(s)) return false;
  // "2026-02-30" 처럼 형식은 맞지만 없는 날짜를 거른다 — Date 가 다음 달로 굴려버리므로 되돌려 대조한다.
  const d = new Date(`${s}T00:00:00Z`);
  return !Number.isNaN(d.getTime()) && d.toISOString().slice(0, 10) === s;
}

function diffDays(a: string, b: string): number {
  return Math.round((Date.parse(`${a}T00:00:00Z`) - Date.parse(`${b}T00:00:00Z`)) / 86400000);
}

export type ReportDatePolicy = "generate" | "cache_only" | "out_of_range";

/** 이 날짜를 어떻게 다룰지. todayKst 는 KST 기준 오늘(kstDate() 산출물). */
export function reportDatePolicy(date: unknown, todayKst: string): ReportDatePolicy {
  if (!isIsoDate(date) || !isIsoDate(todayKst)) return "out_of_range";
  const d = diffDays(date, todayKst);
  if (d < 0) return "cache_only";
  if (d <= FUTURE_REPORT_DAYS) return "generate";
  return "out_of_range";
}

/** 화면·프롬프트에서 그 날을 부르는 말. */
export function dayWordFor(date: string, todayKst: string): "오늘" | "그날" {
  return date === todayKst ? "오늘" : "그날";
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/report-date.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/report-date.ts lib/byeolmaru/report-date.test.ts
git commit -F - <<'EOF'
feat(byeolmaru): 리포트 날짜 정책 순수 모듈 (P6-1)

과거=캐시만(소급 생성 금지) · 오늘과 앞으로 3일=생성 · 그 너머=거부.
3일은 허브 롤링 스트립의 블러 칸 수와 같은 숫자다. 라우트 두 곳이
같은 규칙을 쓰도록 판정을 순수 함수로 분리했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 3: 프롬프트 날짜 컨텍스트 (결함 5)

`lib/fortune/prompt.ts` 의 daily 템플릿은 `"오늘 들어온 두 글자"`, `"오늘 하루에 어떤 영향"` 처럼 전부 오늘 기준이다. 다른 날짜 리포트를 만들면 **지난 날을 "오늘"이라고 부른다.** 템플릿의 "오늘"을 전부 토큰화하는 대신, **대상 날짜와 시제를 못 박는 한 줄을 앞에 세운다**(템플릿 수술 없이 해결 · 다른 운세 타입에 파급 0).

**Files:**
- Modify: `lib/fortune/prompt.ts` (`FortuneInput` 103-109행, `buildFortuneSystem` 658행 이후)
- Test: `lib/fortune/prompt.test.ts` (Task 1에서 만든 파일에 추가)

- [ ] **Step 1: 실패하는 테스트를 추가한다**

`lib/fortune/prompt.test.ts` 끝에 붙인다:

```ts
import { dailyDateContextLine } from "./prompt.ts";

test("dailyDateContextLine: 오늘이면 줄을 넣지 않는다", () => {
  assert.equal(dailyDateContextLine("2026-09-19", "2026-09-19"), null);
});

test("dailyDateContextLine: 지난 날이면 과거 시제를 지시한다", () => {
  const line = dailyDateContextLine("2026-09-12", "2026-09-19")!;
  assert.match(line, /2026-09-12/);
  assert.match(line, /지난 날/);
  assert.match(line, /그날/);
  assert.equal(line.includes("앞으로"), false);
});

test("dailyDateContextLine: 앞으로의 날이면 미래 시제를 지시한다", () => {
  const line = dailyDateContextLine("2026-09-21", "2026-09-19")!;
  assert.match(line, /2026-09-21/);
  assert.match(line, /앞으로/);
  assert.match(line, /그날/);
  assert.equal(line.includes("지난 날"), false);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/fortune/prompt.test.ts`
Expected: FAIL — `dailyDateContextLine` 이 없다.

- [ ] **Step 3: 함수와 배선을 추가한다**

`lib/fortune/prompt.ts` — `fillGuideTokens` 옆에 추가:

```ts
/** daily 리포트의 대상 날짜가 오늘이 아닐 때 앞에 세우는 지시 줄. 오늘이면 null(기존 동작 그대로).
 *  🔴 템플릿의 "오늘"을 전부 토큰으로 바꾸지 않는 이유 — daily 외 운세 타입이 같은 SECTION_GUIDE
 *     구조를 쓰고 있어 파급이 크고, 한 줄로 시제를 덮는 편이 회귀 위험이 작다. */
export function dailyDateContextLine(reportDate: string, todayKst: string): string | null {
  if (reportDate === todayKst) return null;
  const past = reportDate < todayKst;
  const rel = past ? "지난 날" : "앞으로의 날";
  const tense = past
    ? "이미 지나간 날이니 '~였어/~했던 결이야'처럼 과거로 말해."
    : "아직 오지 않은 날이니 '~할 흐름이야/~해보면 좋아'처럼 앞을 보고 말해.";
  return [
    `🔴 이 리포트의 대상 날짜는 ${reportDate} 이고, 오늘(${todayKst}) 기준 ${rel}이다.`,
    `아래 형식에서 '오늘'이라고 적힌 말은 전부 그날을 가리킨다 — 본문에서는 '오늘' 대신 '그날'로 부르고, ${tense}`,
  ].join(" ");
}
```

`FortuneInput`(103행)에 필드를 더한다:

```ts
interface FortuneInput {
  saju?: SajuResult;
  sajuB?: SajuResult;
  names?: { a: string; b: string };
  tarotCards?: TarotDrawnForPrompt[];
  daeun?: DaeunPillar[]; // 평생사주·인생그래프 — 대운 10년 흐름(결정론) 주입
  /** daily 전용 — 리포트 대상 날짜(YYYY-MM-DD). 오늘이 아니면 시제 지시 줄이 붙는다. */
  reportDate?: string;
  /** daily 전용 — KST 오늘(YYYY-MM-DD). reportDate 와 짝으로만 쓴다. */
  todayKst?: string;
}
```

`buildFortuneSystem` 안, Task 1에서 고친 `parts.push(fillGuideTokens(...))` **바로 앞**에 넣는다:

```ts
  if (type === "daily" && input.reportDate && input.todayKst) {
    const ctx = dailyDateContextLine(input.reportDate, input.todayKst);
    if (ctx) parts.push(ctx, "");
  }
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/fortune/prompt.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/fortune/prompt.ts lib/fortune/prompt.test.ts
git commit -F - <<'EOF'
feat(fortune): daily 리포트에 대상 날짜·시제 지시 줄 (P6-1 결함5)

프롬프트가 "오늘 들어온 두 글자"처럼 전부 오늘 기준이라, 다른 날짜
리포트를 만들면 지난 날을 "오늘"이라 부른다. 템플릿의 "오늘"을 전부
토큰화하는 대신(다른 운세 타입에 파급) 대상 날짜와 시제를 못 박는
한 줄을 앞에 세운다. 오늘이면 줄이 안 붙어 기존 동작 그대로.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 4: `note` 접두 중복 제거 (결함 6)

출력이 `"별콩이의 한마디: 오늘은…"` 로 시작해 섹션 제목과 겹친다. 모델을 바꿔도 재발할 수 있으니 파서에서 방어한다.

**Files:**
- Modify: `lib/fortune/daily-report.ts` (`parseDailyReportJson` 의 `note` 처리)
- Test: `lib/fortune/daily-report.test.ts` (신규)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { stripNoteHeading } from "./daily-report.ts";

test("stripNoteHeading: '별콩이의 한마디:' 접두를 떼어낸다", () => {
  assert.equal(stripNoteHeading("별콩이의 한마디: 오늘은 가볍게."), "오늘은 가볍게.");
  assert.equal(stripNoteHeading("별콩이의 한마디 — 오늘은 가볍게."), "오늘은 가볍게.");
  assert.equal(stripNoteHeading("**별콩이의 한마디**: 오늘은 가볍게."), "오늘은 가볍게.");
  assert.equal(stripNoteHeading("  별콩이의 한마디:오늘은 가볍게."), "오늘은 가볍게.");
});

test("stripNoteHeading: 접두가 없으면 그대로 둔다", () => {
  assert.equal(stripNoteHeading("오늘은 가볍게."), "오늘은 가볍게.");
  assert.equal(stripNoteHeading("별콩이가 오늘 하고 싶은 말은 이거야."), "별콩이가 오늘 하고 싶은 말은 이거야.");
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/fortune/daily-report.test.ts`
Expected: FAIL — `stripNoteHeading` 이 없다.

- [ ] **Step 3: 함수를 만들고 파서에 건다**

`lib/fortune/daily-report.ts` 에 추가한다:

```ts
/** 모델이 본문 앞에 섹션 제목을 한 번 더 붙이는 습관 방어 — "별콩이의 한마디: …" → "…".
 *  볼드(**…**)로 감싼 형태와 콜론·대시·전각 콜론을 모두 받는다. */
export function stripNoteHeading(s: string): string {
  return s.replace(/^\s*\*{0,2}별콩이의\s*한마디\*{0,2}\s*[:：—-]?\s*/, "").trim();
}
```

`parseDailyReportJson` 의 return 문에서 `note` 를 감싼다 — 기존:

```ts
    note: o.note.trim(),
```

바꿔서:

```ts
    note: stripNoteHeading(o.note),
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/fortune/daily-report.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/fortune/daily-report.ts lib/fortune/daily-report.test.ts
git commit -F - <<'EOF'
fix(fortune): 리포트 note 의 '별콩이의 한마디:' 접두 중복 제거 (P6-1 결함6)

섹션 제목이 이미 '별콩이의 한마디'인데 본문이 같은 말로 다시 시작한다.
모델을 바꿔도 재발할 수 있어 파서에서 방어한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 5: 카드 해석 캐시 테이블 + 래퍼 (결함 4)

`card-narrative` 는 매 요청마다 새로 생성해서, 같은 날 다시 들어오면 해석이 달라진다. `pair-narrative`(P5-5)가 같은 이유로 캐시를 붙였는데 card 에는 안 붙었다.

**Files:**
- Create: `supabase/migrations/20260919000000_byeolmaru_card_narrative.sql`
- Create: `lib/byeolmaru/card-narrative.ts`

- [ ] **Step 1: 마이그레이션을 쓴다**

`supabase/migrations/20260919000000_byeolmaru_card_narrative.sql`:

```sql
-- 20260919000000_byeolmaru_card_narrative.sql — 별마루 유료 "오늘 타로" 해석 캐시(유저·날짜당 1행).
-- byeolmaru_pair_narrative(20260914000000) 패턴을 그대로 미러한다. 근거는 원가가 아니라
-- ①일관성 — 같은 날 다시 들어왔는데 카드 해석이 바뀌면 운세 앱의 신뢰가 깨진다(pair 캐시와 같은 사유인데
-- card 에만 빠져 있었다) ②속도 — 생성 실측 6.5초. 스펙 §7-4.
-- 하루 1장 고정이라 카드 id 를 키에 넣지 않는다(byeolmaru_daily_card 가 그날 카드의 단일 원천).
CREATE TABLE IF NOT EXISTS byeolmaru_card_narrative (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  narrative_date DATE NOT NULL,
  narrative TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, narrative_date)
);
CREATE INDEX IF NOT EXISTS idx_byeolmaru_card_narrative_user
  ON byeolmaru_card_narrative(user_id, narrative_date DESC);
ALTER TABLE byeolmaru_card_narrative ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE byeolmaru_card_narrative FROM PUBLIC, anon, authenticated;
GRANT ALL ON TABLE byeolmaru_card_narrative TO service_role;
```

- [ ] **Step 2: 래퍼를 쓴다**

`lib/byeolmaru/card-narrative.ts`:

```ts
// lib/byeolmaru/card-narrative.ts — 유료 "오늘 타로" 해석 캐시((유저,날짜)별 1회 생성).
// pair-narrative.ts 와 같은 모양·같은 규율(23505 무시 = 동시 생성 시 먼저 쓴 쪽을 남긴다).
import { getServiceSupabase } from "@/lib/supabase";

/** 캐시된 해석 조회. 없으면 null. */
export async function getCachedCardNarrative(userId: string, dateStr: string): Promise<string | null> {
  const { data, error } = await getServiceSupabase()
    .from("byeolmaru_card_narrative")
    .select("narrative")
    .eq("user_id", userId)
    .eq("narrative_date", dateStr)
    .maybeSingle();
  if (error || !data) return null;
  return data.narrative as string;
}

/** 해석 저장. 이미 있으면(동시 생성) 덮지 않는다(23505 무시). */
export async function saveCardNarrative(userId: string, dateStr: string, narrative: string): Promise<void> {
  const { error } = await getServiceSupabase()
    .from("byeolmaru_card_narrative")
    .insert({ user_id: userId, narrative_date: dateStr, narrative });
  if (error && (error as { code?: string }).code !== "23505") {
    throw error;
  }
}
```

- [ ] **Step 3: 타입체크로 확인한다**

Run: `npx tsc --noEmit`
Expected: 오류 0

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260919000000_byeolmaru_card_narrative.sql lib/byeolmaru/card-narrative.ts
git commit -F - <<'EOF'
feat(byeolmaru): 오늘 타로 해석 (유저·날짜) 일별 캐시 신설 (P6-1 결함4)

card-narrative 는 캐시가 아예 없어 같은 날 다시 들어오면 해석이 매번
달라졌다. pair-narrative(P5-5)가 "같은 날 서술이 달라지면 신뢰가 깨진다"는
사유로 붙인 캐시가 card 에만 빠져 있었다. 같은 패턴으로 복제.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 6: `card-narrative` 라우트에 캐시 연결

**Files:**
- Modify: `app/api/byeolmaru/card-narrative/route.ts`

- [ ] **Step 1: 캐시 조회를 자격 게이트 바로 뒤에 넣는다**

import 에 추가:

```ts
import { getCachedCardNarrative, saveCardNarrative } from "@/lib/byeolmaru/card-narrative";
```

`const todayKst = kstDate(new Date().toISOString());` **바로 뒤**에 넣는다 — 히트하면 프로필 조회·`calcSaju`·일진 계산을 통째로 건너뛴다(pair-narrative 와 같은 순서):

```ts
    // 캐시 히트 — (유저,오늘) 해석이 이미 있으면 재생성 없이 반환. 자격 게이트 뒤라 비자격자는 여기 못 온다.
    const cached = await getCachedCardNarrative(userId, todayKst);
    if (cached) return NextResponse.json({ entitled: true, narrative: cached });
```

- [ ] **Step 2: 생성 성공 지점에 저장을 넣는다**

기존:

```ts
      return NextResponse.json({ entitled: true, narrative });
```

바꿔서:

```ts
      // 캐시 저장은 best-effort — 실패해도 이미 만든 해석은 그대로 응답한다(daily-report 와 동일 규율).
      try {
        await saveCardNarrative(userId, todayKst, narrative);
      } catch (e) {
        await logError(e, { ...logCtx, extra: { stage: "cache_save" } });
      }
      return NextResponse.json({ entitled: true, narrative });
```

- [ ] **Step 3: 타입체크**

Run: `npx tsc --noEmit`
Expected: 오류 0

- [ ] **Step 4: 실호출로 검증한다**

dev 서버가 떠 있어야 한다(`npm run dev`). QA봇 쿠키로 두 번 호출해 **같은 문장**이 오는지, 두 번째가 빠른지 본다:

```bash
C="byeolkong_user_id=11111111-1111-4111-8111-111111111111"
time curl -s "http://localhost:3000/api/byeolmaru/card-narrative" -H "Cookie: $C" -o /tmp/n1.json
time curl -s "http://localhost:3000/api/byeolmaru/card-narrative" -H "Cookie: $C" -o /tmp/n2.json
diff /tmp/n1.json /tmp/n2.json && echo "SAME (캐시 히트)"
```

Expected: 1회차 5~8초 · 2회차 1초 미만 · `SAME (캐시 히트)` 출력.
⚠️ 오늘 카드가 없으면 `no_card` 로 떨어진다 — 먼저 `curl -X POST .../daily-card -H "Cookie: $C" -H "Content-Type: application/json" -d '{"cardId":7,"reversed":false}'` 로 뽑아둔다.

- [ ] **Step 5: 커밋**

```bash
git add app/api/byeolmaru/card-narrative/route.ts
git commit -F - <<'EOF'
feat(byeolmaru): card-narrative 에 일별 캐시 연결 (P6-1 결함4)

캐시 조회를 자격 게이트 바로 뒤에 둬서, 히트하면 프로필 조회·calcSaju·
일진 계산을 통째로 건너뛴다(pair-narrative 와 같은 순서). 저장은
best-effort — 실패해도 이미 만든 해석은 그대로 응답한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 7: `daily-card` 날짜 조회 (결함 3)

`byeolmaru_daily_card(user_id, card_date)` PK 라 지난 카드가 전부 남아 있는데 조회 함수가 오늘로 고정이라 꺼낼 길이 없다. **뽑기(POST)는 오늘만** 그대로 두고 **조회(GET)만** 날짜를 받는다.

**Files:**
- Modify: `lib/byeolmaru/daily-card.ts`
- Modify: `app/api/byeolmaru/daily-card/route.ts`
- Modify: `app/api/byeolmaru/card-narrative/route.ts` (호출부 이름 변경)

- [ ] **Step 1: 래퍼 함수 이름을 날짜 중립으로 바꾼다**

`lib/byeolmaru/daily-card.ts` — `getTodayCard` 를 `getCardOn` 으로 바꾸고 인자 이름도 바꾼다:

```ts
/** 그날의 카드 조회. 하루 1장이라 (유저,날짜)가 곧 키다. 지난 날짜도 그대로 조회된다. */
export async function getCardOn(userId: string, dateKst: string): Promise<DailyCard | null> {
  const { data } = await getServiceSupabase()
    .from("byeolmaru_daily_card")
    .select("card_id, reversed")
    .eq("user_id", userId).eq("card_date", dateKst).maybeSingle();
  return data ? { cardId: data.card_id, reversed: data.reversed } : null;
}
```

같은 파일의 `recordDraw` 안 두 곳 `getTodayCard(userId, todayKst)` 를 `getCardOn(userId, todayKst)` 로 바꾼다. **`recordDraw` 자체는 오늘 전용으로 남긴다**(과거·미래 뽑기 금지).

- [ ] **Step 2: GET 라우트가 `?date=` 를 받게 한다**

`app/api/byeolmaru/daily-card/route.ts` — import 를 바꾸고:

```ts
import { getCardOn, recordDraw, isValidCardId } from "@/lib/byeolmaru/daily-card";
import { isIsoDate } from "@/lib/byeolmaru/report-date";
```

GET 본문의 `const card = await getTodayCard(...)` 를 바꾼다:

```ts
    // ?date= 가 있으면 그날 카드를, 없으면 오늘 카드를. 카드 조회는 룰도 LLM 도 아닌 단순 기록 조회라
    // 미래 제한(reportDatePolicy)을 걸지 않는다 — 미래 날짜엔 어차피 행이 없어 null 이 나온다.
    const q = req.nextUrl.searchParams.get("date");
    if (q !== null && !isIsoDate(q)) {
      return NextResponse.json({ error: "invalid_date" }, { status: 400 });
    }
    const dateKst = q ?? kstDate(new Date().toISOString());
    const card = await getCardOn(userId, dateKst);
    return NextResponse.json({ card });
```

POST 본문의 `recordDraw(userId, kstDate(...), ...)` 는 **그대로 둔다.**

- [ ] **Step 3: `card-narrative` 의 호출부 이름을 맞춘다**

`app/api/byeolmaru/card-narrative/route.ts` 의 import 와 호출을 바꾼다:

```ts
import { getCardOn } from "@/lib/byeolmaru/daily-card";
```

본문의 `getTodayCard(userId, todayKst)` → `getCardOn(userId, todayKst)`.

- [ ] **Step 4: 잔여 호출부가 없는지 확인한다**

Run: `grep -rn "getTodayCard" --include=*.ts --include=*.tsx . | grep -v node_modules`
Expected: 출력 없음

- [ ] **Step 5: 타입체크 + 실호출 검증**

```bash
npx tsc --noEmit
C="byeolkong_user_id=11111111-1111-4111-8111-111111111111"
curl -s "http://localhost:3000/api/byeolmaru/daily-card" -H "Cookie: $C"
curl -s "http://localhost:3000/api/byeolmaru/daily-card?date=2026-09-01" -H "Cookie: $C"
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/api/byeolmaru/daily-card?date=nope" -H "Cookie: $C"
```

Expected: tsc 오류 0 · 첫 줄 오늘 카드 · 둘째 줄 `{"card":null}`(그날 안 뽑았으면) · 셋째 줄 `400`

- [ ] **Step 6: 커밋**

```bash
git add lib/byeolmaru/daily-card.ts app/api/byeolmaru/daily-card/route.ts app/api/byeolmaru/card-narrative/route.ts
git commit -F - <<'EOF'
feat(byeolmaru): 오늘 카드 조회에 날짜 파라미터 (P6-1 결함3)

byeolmaru_daily_card 는 (user,date) PK 라 지난 카드가 전부 남아 있는데
조회 함수가 오늘로 고정이라 꺼낼 길이 없었다. getTodayCard → getCardOn
으로 날짜 중립화하고 GET 에 ?date= 를 받는다. 뽑기(POST)는 오늘 전용 유지.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 8: `daily-report` 날짜 파라미터 (결함 2 · 서버)

지금은 항상 `kstDate(now)` 라, 구독자가 9월 12일 칸을 눌러도 **오늘 리포트**가 붙는다.

**Files:**
- Modify: `app/api/byeolmaru/daily-report/route.ts`

- [ ] **Step 1: 날짜를 해석하고 정책을 적용한다**

import 에 추가:

```ts
import { reportDatePolicy } from "@/lib/byeolmaru/report-date";
```

기존 `const todayKst = kstDate(new Date().toISOString());` 아래(현재 61행 근처, `temporal` 계산 **앞**)를 이렇게 바꾼다:

```ts
    const todayKst = kstDate(new Date().toISOString());
    // ?date= 없으면 오늘. 정책은 lib/byeolmaru/report-date.ts 가 단일 원천(스펙 §4).
    const reqDate = req.nextUrl.searchParams.get("date");
    const reportDate = reqDate ?? todayKst;
    const policy = reportDatePolicy(reportDate, todayKst);
    if (policy === "out_of_range") {
      return NextResponse.json({ error: "date_out_of_range" }, { status: 400 });
    }

    // 🔴 일진은 대상 날짜 기준으로 계산한다 — todayKst 로 계산하면 화면은 9/12 인데 본문은 오늘 일진이 된다.
    const temporal = calcTemporalLuck(baseDateForKst(reportDate), input.year);
```

(기존 `calcTemporalLuck(baseDateForKst(todayKst), input.year)` 줄을 위 줄로 대체한다.)

- [ ] **Step 2: 캐시 키와 과거 규칙을 적용한다**

기존 캐시 조회부:

```ts
    const cached = await getCachedDailyReport(userId, todayKst);
    if (cached) {
      return NextResponse.json({ report: cached });
    }
```

바꿔서:

```ts
    const cached = await getCachedDailyReport(userId, reportDate);
    if (cached) {
      return NextResponse.json({ report: cached });
    }
    // 과거는 "있던 것만" — 소급 생성하지 않는다(스펙 §4). 그때 받은 글이 아니면 기록이 아니다.
    if (policy === "cache_only") {
      return NextResponse.json({ report: null, reason: "not_generated" });
    }
```

- [ ] **Step 3: 프롬프트에 대상 날짜를 넘긴다**

기존 `const system = buildFortuneSystem("daily", { saju });` 를 바꾼다:

```ts
    const system = buildFortuneSystem("daily", { saju, reportDate, todayKst });
```

- [ ] **Step 4: 저장 키를 맞춘다**

기존 `await saveDailyReport(userId, todayKst, report);` 를 바꾼다:

```ts
      await saveDailyReport(userId, reportDate, report);
```

- [ ] **Step 5: 타입체크 + 실호출 검증**

```bash
npx tsc --noEmit
C="byeolkong_user_id=11111111-1111-4111-8111-111111111111"
# 오늘 — 기존대로 생성/캐시
curl -s "http://localhost:3000/api/byeolmaru/daily-report" -H "Cookie: $C" | head -c 120; echo
# 과거 — 리포트가 없으면 not_generated
curl -s "http://localhost:3000/api/byeolmaru/daily-report?date=2026-09-01" -H "Cookie: $C" | head -c 120; echo
# 미래 3일 — 생성된다. 본문에 "오늘"이 아니라 "그날"이 나오는지 눈으로 확인
curl -s "http://localhost:3000/api/byeolmaru/daily-report?date=$(date -d '+2 days' +%F)" -H "Cookie: $C" | head -c 400; echo
# 미래 4일 — 400
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/api/byeolmaru/daily-report?date=$(date -d '+4 days' +%F)" -H "Cookie: $C"
```

Expected: tsc 오류 0 · 오늘 `{"report":{…}}` · 과거 `{"report":null,"reason":"not_generated"}` · 미래 2일 리포트 생성(본문 시제가 미래) · 미래 4일 `400`

- [ ] **Step 6: 커밋**

```bash
git add app/api/byeolmaru/daily-report/route.ts
git commit -F - <<'EOF'
feat(byeolmaru): daily-report 에 날짜 파라미터 (P6-1 결함2)

항상 kstDate(now) 로 오늘만 만들어서, 구독자가 9월 12일 칸을 눌러도
화면 위는 12일 아래는 오늘 리포트인 두 날짜 혼재가 났다. ?date= 를 받아
일진·캐시키·프롬프트 시제를 전부 대상 날짜로 맞춘다. 과거는 있던 것만
(소급 생성 금지), 미래는 3일까지.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 9: 리포트 카드 날짜 라벨 + 화면 배선 (결함 2 · 렌더)

`DailyReportCard` 는 `"오늘의 운세"`, `"오늘 들어온 두 글자"`, `"오늘 종합운"`, `"오늘의 균형"` 이 하드코딩이라 9월 12일 리포트에도 "오늘"이 붙는다.

**Files:**
- Modify: `components/fortune/DailyReportCard.tsx`
- Modify: `components/byeolmaru/SajuTodayView.tsx`

- [ ] **Step 1: `dayWord` prop 을 더한다**

`components/fortune/DailyReportCard.tsx` 의 시그니처:

```tsx
export default function DailyReportCard({
  report,
  dateLabel,
  dayWord = "오늘",
}: {
  report: DailyReport;
  dateLabel: string | null;
  /** 그 날을 부르는 말. 오늘이 아닌 날짜의 리포트면 "그날"(lib/byeolmaru/report-date.ts dayWordFor). */
  dayWord?: "오늘" | "그날";
}) {
```

본문의 하드코딩 4곳을 바꾼다:

| 기존 | 바꿀 것 |
|---|---|
| `<span …>오늘의 운세</span>` | `<span …>{dayWord}의 운세</span>` |
| `{iljin.branch} · 오늘 들어온 기운` | `{iljin.branch} · {dayWord} 들어온 기운` |
| `<span …>오늘 종합운</span>` | `<span …>{dayWord} 종합운</span>` |
| `<span …>오늘의 균형</span>` | `<span …>{dayWord}의 균형</span>` |

- [ ] **Step 2: `SajuTodayView` 가 선택 날짜로 리포트를 받아오게 한다**

`components/byeolmaru/SajuTodayView.tsx` — import 를 더한다:

```tsx
import { dayWordFor } from "@/lib/byeolmaru/report-date";
```

`refresh()` 안의 리포트 fetch 는 **선택 날짜를 모른 채** 돌고 있다. 리포트 로딩을 `refresh` 에서 떼어 별도 effect 로 옮긴다. `refresh()` 안의 `if (data.entitled) { … }` 블록 전체를 지우고 `setReport(null); setReportLoading(false);` 만 남긴 뒤, `useEffect(() => { void refresh(); }, []);` **아래**에 넣는다:

```tsx
  // 리포트는 선택 날짜 기준으로 따로 받아온다 — refresh() 안에 두면 날짜를 바꿔도 오늘 것만 계속 붙는다.
  // 🔴 비자격자는 아예 호출하지 않는다(403 방지 = 원가 0).
  const entitled = state.kind === "ready" && state.data.entitled;
  useEffect(() => {
    if (!entitled || !selected) { setReport(null); setReportLoading(false); return; }
    let cancelled = false;
    setReport(null);
    setReportLoading(true);
    void (async () => {
      try {
        const res = await fetch(`/api/byeolmaru/daily-report?date=${selected}`, { cache: "no-store" });
        const j = await res.json();
        if (!cancelled) setReport(j.report ?? null);
      } catch {
        if (!cancelled) setReport(null);
      } finally {
        if (!cancelled) setReportLoading(false);
      }
    })();
    return () => { cancelled = true; };
  }, [entitled, selected]);
```

- [ ] **Step 3: 렌더에 날짜 라벨을 넘긴다**

기존:

```tsx
        <div className="-mx-4"><DailyReportCard report={report} dateLabel="오늘" /></div>
```

바꿔서:

```tsx
        <div className="-mx-4">
          <DailyReportCard
            report={report}
            dateLabel={cell.isToday ? "오늘" : fmtMD(cell.date)}
            dayWord={dayWordFor(cell.date, data.today)}
          />
        </div>
```

- [ ] **Step 4: 리포트가 없는 지난 날에 맞는 문구를 띄운다**

🔴 이 처리가 없으면 **구독자가 지난 날짜를 눌렀을 때 `PremiumBlock` 의 자격자 폴백("별콩이가 잠깐 숨 고르는 중이야, 조금 뒤에 다시 와줄래?")이 뜬다.** 생성 실패가 아니라 **원래 안 받은 날**이므로 틀린 안내다.

Step 2에서 만든 effect 에 사유를 담을 상태를 하나 더 둔다. `const [reportLoading, setReportLoading] = useState(false);` 아래에 추가:

```tsx
  // 지난 날인데 그때 받은 리포트가 없는 경우 — 생성 실패(null)와 구분해야 안내 문구가 맞는다.
  const [notGenerated, setNotGenerated] = useState(false);
```

Step 2의 effect 안에서 `setReport(j.report ?? null);` 을 바꾼다:

```tsx
        if (!cancelled) {
          setReport(j.report ?? null);
          setNotGenerated(j.reason === "not_generated");
        }
```

그리고 같은 effect 의 리셋·catch 두 곳에도 `setNotGenerated(false);` 를 더한다(초기화 누락 시 다른 날짜로 옮겨도 문구가 남는다).

렌더의 리포트 분기에 가지를 하나 더 넣는다 — 기존 `) : (` 로 `PremiumBlock` 으로 떨어지던 자리 **앞**에:

```tsx
      ) : data.entitled && notGenerated ? (
        <section className="rounded-2xl bg-cream-warm p-4 text-center text-sm text-text-light">
          그날은 리포트를 안 받았어. 지난 날은 그때 받은 것만 보여줄 수 있어.
        </section>
      ) : (
```

- [ ] **Step 5: 타입체크 + 브라우저 확인**

Run: `npx tsc --noEmit`
Expected: 오류 0

브라우저로 `http://localhost:3000/byeolmaru/saju` 에 QA봇 쿠키를 넣고 들어가 확인한다:
① 달력에서 **오늘** → "오늘의 운세" 리포트
② **지난 날짜 중 리포트가 있는 날** → "그날의 운세"로 라벨이 바뀜
③ **지난 날짜 중 리포트가 없는 날** → "그날은 리포트를 안 받았어" (❌ "별콩이가 잠깐 숨 고르는 중"이 뜨면 Step 4 누락)

- [ ] **Step 6: 커밋**

```bash
git add components/fortune/DailyReportCard.tsx components/byeolmaru/SajuTodayView.tsx
git commit -F - <<'EOF'
feat(byeolmaru): 오늘 사주 화면이 선택 날짜의 리포트를 받아온다 (P6-1 결함2)

리포트 fetch 가 refresh() 안에 있어 날짜를 바꿔도 오늘 것만 붙었다.
선택 날짜를 deps 로 갖는 별도 effect 로 떼고, DailyReportCard 의
하드코딩된 "오늘" 4곳을 dayWord prop 으로 바꾼다. 지난 날인데 그때 받은
리포트가 없으면 생성 실패 폴백 대신 "그날은 리포트를 안 받았어"로.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 10: `MarkdownLite` 배선 (스펙 §6-5 · P6-2 전제조건)

오늘 타로·우리 오늘 서술은 `whitespace-pre-wrap` 으로 그려서 `**볼드**` 가 **별표까지 그대로 화면에 뜬다.** P6-2에서 모델을 올리면 볼드가 더 자주 나오므로, 이 배선이 먼저 있어야 화면이 나빠지지 않는다.

**Files:**
- Modify: `components/byeolmaru/PremiumBlock.tsx`
- Modify: `components/byeolmaru/DailyCardBlock.tsx`

- [ ] **Step 1: `PremiumBlock` 의 자격자 서술을 바꾼다**

import 를 더한다:

```tsx
import { MarkdownLite } from "@/lib/markdown-lite";
```

기존:

```tsx
          <p className="whitespace-pre-wrap text-sm leading-relaxed text-eye-purple">{narrative}</p>
```

바꿔서:

```tsx
          <MarkdownLite text={narrative} className="text-sm leading-relaxed text-eye-purple" />
```

- [ ] **Step 2: `DailyCardBlock` 의 자격자 서술을 바꾼다**

import 를 더한다:

```tsx
import { MarkdownLite } from "@/lib/markdown-lite";
```

기존:

```tsx
                    <p className="mt-3 whitespace-pre-wrap text-sm leading-relaxed text-eye-purple">{narrative}</p>
```

바꿔서:

```tsx
                    <MarkdownLite text={narrative} className="mt-3 text-sm leading-relaxed text-eye-purple" />
```

🔴 같은 파일의 **무료 taste 문단(`{taste}`)은 건드리지 않는다** — 정적 뱅크는 마크다운을 쓰지 않는다.

- [ ] **Step 3: 타입체크 + 브라우저 확인**

Run: `npx tsc --noEmit`
Expected: 오류 0

브라우저에서 QA봇으로 `/byeolmaru/tarot` 에 들어가, 서술에 `**` 별표가 **문자 그대로 보이지 않고** 볼드+골드 형광펜으로 렌더되는지 확인한다.
⚠️ 현재 nano 는 볼드를 낼 때도 있고 안 낼 때도 있다. 별표가 안 보이면 캐시를 지우고(`DELETE FROM byeolmaru_card_narrative WHERE user_id='…'` — 사용자 손으로) 다시 받아 확인하거나, P6-2 검수로 넘긴다.

- [ ] **Step 4: 커밋**

```bash
git add components/byeolmaru/PremiumBlock.tsx components/byeolmaru/DailyCardBlock.tsx
git commit -F - <<'EOF'
fix(byeolmaru): 서술 렌더를 MarkdownLite 로 (P6-1 · 스펙 6-5)

오늘 타로·우리 오늘 서술이 whitespace-pre-wrap 이라 **볼드** 가 별표까지
그대로 화면에 떴다. P6-2 모델 상향은 볼드를 더 자주 내므로 이 배선이
먼저 있어야 화면이 나빠지지 않는다. 무료 정적 taste 는 그대로 둔다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
```

---

## Task 11: 전체 게이트 + 회귀 확인

- [ ] **Step 1: 유닛 전체**

Run:
```bash
node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")
```
Expected: 전부 PASS. 기존 개수 + 이번에 더한 11개(prompt 4 · report-date 5 · daily-report 2).

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: 오류 0

- [ ] **Step 3: 프로덕션 빌드**

Run: `npm run build`
Expected: exit 0. 라우트 표에 `ƒ Proxy` 가 그대로 있는지도 확인(전환 회귀 감시, AGENTS.md).

- [ ] **Step 4: 결함 6건이 실제로 사라졌는지 확인**

| # | 확인 방법 | 기대 |
|---|---|---|
| 1 | 오늘 리포트를 새로 받아 `intro` 를 본다 | `{"병신"}`·`{병신}` 같은 중괄호 찌꺼기 없음 |
| 2 | `/byeolmaru/saju` 에서 지난 날짜 선택 | 리포트가 "그날의 운세"거나 아예 안 뜸 — 오늘 리포트가 붙지 않음 |
| 3 | `curl '…/daily-card?date=<지난날>'` | 그날 뽑았으면 카드가 나옴 |
| 4 | card-narrative 2회 호출 | 같은 문장 · 2회차 1초 미만 |
| 5 | `curl '…/daily-report?date=<모레>'` | 본문이 "오늘" 대신 "그날"·미래 시제 |
| 6 | 오늘 리포트 `note` | "별콩이의 한마디:" 접두 없음 |

⚠️ 1·5·6은 LLM 출력이라 1회 관측으로 단정하지 말 것. 캐시를 비우고 2~3회 받아 확인한다.

- [ ] **Step 5: 마이그레이션 적용 확인 (push 후)**

`dev` 브랜치에 push 하면 Supabase dev 에 자동 적용된다. GitHub Actions 로그가 SUCCESS 인지 보고, 적용 후 ACL 을 감사한다(AGENTS.md 규칙 — 두 줄 다 0행이어야 한다):

```sql
select relname, relacl from pg_class
where relname = 'byeolmaru_card_narrative'
  and (relacl::text like '%anon=%' or relacl::text like '%authenticated=%');
```

- [ ] **Step 6: 아무것도 커밋되지 않았는지 확인하고 마무리**

Run: `git status --porcelain`
Expected: 이 플랜이 만든 파일 중 미커밋 잔여 없음. (다른 세션 작업물은 건드리지 않는다.)

---

## 주의 · 함정

- 🔴 **`prod` 배포 금지.** 사용자가 명시적으로 "배포하자"라고 할 때까지 `main` 은 건드리지 않는다.
- 🔴 **다른 세션 작업물을 커밋하지 말 것.** 작업트리에 `2026-09-19-흑자전환-로드맵-v2`·`서비스-성적표` 등 다른 세션 파일이 있다. `git add` 는 항상 파일 경로를 명시한다(`git add .` 금지).
- **페르소나·프롬프트 수정 후 dev 서버 재시작** — 모듈 캐시 때문에 안 바뀐 것처럼 보인다(AGENTS.md).
- **이 단계에서 모델은 바꾸지 않는다.** `gpt-5-nano` 그대로 — 모델 상향·분량·구조화는 P6-2다.
- **`MAX_TOKENS_BY_FORTUNE.daily` 도 이 단계에선 그대로.** 분량 확장은 P6-2.
- `reportDatePolicy` 의 `FUTURE_REPORT_DAYS = 3` 은 **허브 스트립의 블러 칸 수와 같은 숫자**다. 한쪽만 바꾸면 "구독하면 3일 열린다"는 약속과 실제가 어긋난다.
