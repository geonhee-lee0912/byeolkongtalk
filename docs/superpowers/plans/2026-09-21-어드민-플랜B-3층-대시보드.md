# 어드민 플랜 B — 3층 대시보드 (1층 손익 · 2층 분해 · 3층 판정 · 소표본 게이트)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 어드민 대시보드를 "카운터"에서 "손익 계기판"으로 바꾼다 — 1층은 `기여 = 매출 − 광고비 − API원가` 한 줄과 8주 밴드·가드레일, 2층은 그 값이 왜 움직였는지의 분해, 3층은 주 1회 로드맵 판정.

**Architecture:** 집계는 전부 Postgres RPC 가 한다(원본 행을 앱으로 끌어오지 않는다 — AGENTS.md). RPC 는 **원시 분자/분모만** 반환하고, 비율·롤링·분위수 같은 산식은 `lib/admin/*` 의 **순수 모듈**이 계산한다 — 그래야 유닛 테스트로 잠글 수 있다. 화면은 `lib/admin-metrics.ts`(플랜 A 산출물)에서만 정의·경보선·소표본 임계를 가져온다.

**Tech Stack:** TypeScript strict · Next 16 (Server Components) · Supabase(PostgreSQL) · Tailwind v4 · `node:test` + tsx

**스펙:** `docs/superpowers/specs/2026-09-20-어드민-재설계-design.md` §3 · §5 · §6 · §7 · §8 · §10(단계 3·4·5·6)
**선행 플랜:** `docs/superpowers/plans/2026-09-20-어드민-플랜A-정의정본화-원가로깅.md` (완료)

---

## 🔴 실행 규율

1. **`git add -A` / `git add .` 금지.** 별마루 P6 세션이 같은 `dev` 워킹 트리에서 동시에 일한다. 각 Step 의 `git add` 는 경로를 명시했으니 그대로 쓴다.
2. **이 플랜은 `components/byeolmaru/*` · `lib/byeolmaru/*` · `app/api/byeolmaru/*` · `lib/analytics/ui-events.ts` 를 건드리지 않는다.** 별마루 소유다(스펙 §11).
3. **prod 배포 없음.** `dev` 커밋까지만. 푸시·배포는 사용자 지시를 따른다.
4. 게이트 3종: `npx tsc --noEmit` · `node --import tsx --test "lib/**/*.test.ts"` · `npm run build`
5. 🔴 **마이그레이션은 적용 전에 함수 본문을 prod 에서 **전부** 돌려본다.** 일부만 검증하면 나머지 하나의 오류로 **파일 전체가 실패**하고 코드만 배포돼 화면이 깨진다(2026-07-31 실사고, 어드민 25분 다운). 조회는 `SUPABASE_PAT=... node scripts/run-prod-query.mjs <file.sql>` (`read_only:true` 고정).
   - RPC 자체는 `service_role` 전용이라 `run-prod-query.mjs` 로 **직접 호출이 안 된다**(permission denied 가 정상). **본문을 인라인한 SELECT** 로 돌릴 것.
6. **5대 함정 체크리스트** — 모든 SQL 에 적용(스펙 §9):
   ① Supabase `Max rows` cap ② NULL 3값 논리(`<> ALL` 이 NULL 행을 버린다) ③ `at time zone 'UTC'` 누락 ④ `LIKE` 의 `_` 와일드카드 ⑤ 어드민 제외 누락/과잉
7. 커밋 메시지는 리포 관례(`feat(admin): …` · `fix(admin): …`)를 따르고 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` 를 붙인다.

---

## 🔴 스펙에서 정정된 것 2건 (플랜 작성 중 실측)

### 1. **3층은 `messages.turn_close` 를 전제하지 않는다** — 스펙 §3·플랜 A 메모의 전제가 틀렸다

- **근거**: `scripts/roadmap-kpi-snapshot.sql` 의 무언 이탈은 `bool_or(role='assistant' AND content LIKE '%[END]%')` 로 계산하고, 유저 턴은 `count(*) filter (where role='user')` 다. **둘 다 `turn_close` 를 읽지 않는다**(파일 전문 84줄 확인).
- `20260912020000_messages_turn_close.sql` 의 주석도 그 컬럼을 "상태 × 실제 행동 × 다음 턴 생존 **교차**" 용 추가 계측으로 설명하며, `[END]` 를 대체한다고 하지 않는다.
- **결론**: 3층(Task 14·15)은 **지금 prod 데이터로 그대로 구현·검증된다.** `turn_close` 는 여전히 "코드보다 먼저 적용"이라는 **배포 순서 규율**로만 남는다(그 컬럼에 INSERT 하는 chat 라우트가 이미 배포 대기 중이므로 — [[prod-deploy-pending-migrations]]).
- 판정: **확인**(코드 전문 대조).

### 2. **API 원가는 `llm_usage` 축적 시작일(2026-09-20) 이전이 구조적으로 비어 있다**

- 1층 히어로가 `매출 − 광고비 − API원가` 인데 8주 밴드는 56일치를 요구한다. 과거 원가는 **영원히 없다**(소급 불가).
- **처리**: `admin_layer1_pnl` 이 날짜별 `cost_rows`(원가 행 수)를 함께 반환한다. 앱은 `cost_rows = 0` 인 날을 **"0원"이 아니라 "미축적"** 으로 구분한다.
  - 창 전체가 미축적이면 API 원가 칸은 `—` + "축적 N일차" 배지(스펙 §3: "0으로 위장하지 않는다").
  - **밴드는 `매출 − 광고비`(마케팅 기여) 축으로 그린다** — 성분 구성이 다른 값을 한 분포에 섞으면 밴드가 거짓말을 한다. 원가가 56일을 덮으면 자동으로 완전 기여 축으로 전환된다(`lib/admin/band.ts` 가 판정).
  - 히어로 숫자 자체는 언제나 **가용한 성분의 합**이고, 빠진 성분을 캡션에 명시한다.

---

## 파일 구조

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `components/admin/Metric.tsx` | 소표본 게이트 + 단위 포맷을 한 곳에서. 전 화면 공통 | 신규 |
| `lib/admin/format.ts` | `MetricUnit` 별 표시 포맷 (순수) | 신규 |
| `lib/admin/format.test.ts` | 포맷 계약 | 신규 |
| `lib/admin/band.ts` | 7일 롤링 · P10/P90 분위수 · 현재값 위치 · 원가 커버리지 판정 (순수) | 신규 |
| `lib/admin/band.test.ts` | 밴드 계약 | 신규 |
| `lib/admin/layer1.ts` | RPC 원시행 → 1층 지표(결제율·ARPPU·CAC·가드레일) (순수) | 신규 |
| `lib/admin/layer1.test.ts` | 산식 계약 | 신규 |
| `supabase/migrations/20260921010000_admin_layer1_pnl.sql` | 일별 손익 3소스 | 신규 |
| `supabase/migrations/20260921011000_admin_layer1_rates.sql` | 단가(코호트) + 가드레일 6종 | 신규 |
| `supabase/migrations/20260921012000_admin_layer1_quality_flow.sql` | 매출의 질(신규/재결제·구독) + 흐름(가입·리딩·UV·D7·2일+) | 신규 |
| `components/admin/ContributionHero.tsx` | 기여 히어로 숫자 + 성분 3칸 | 신규 |
| `components/admin/BandGauge.tsx` | P10~P90 분포 + 현재값 골드 마커 | 신규 |
| `components/admin/GuardrailRow.tsx` | 가드레일 6종 (평소 조용 · 임계 밖 빨강) | 신규 |
| `app/admin/page.tsx` | 1층으로 재작성 (별 소모·연애·탈퇴는 2층으로 이동) | 수정 |
| `lib/admin/layer2-types.ts` | 2층 블록 공통 스키마 (bars·table·link) + 섹션 enum | 신규 |
| `lib/admin/product-map.ts` | `llm_usage.route` ↔ 별 소모 domain → 상품 라벨 (순수) | 신규 |
| `lib/admin/product-map.test.ts` | 매핑 계약 — 실제 route 문자열 고정 | 신규 |
| `components/admin/Drilldown.tsx` | 2층 펼침 셸 (client · 펼칠 때만 fetch) | 신규 |
| `components/admin/DivergingBar.tsx` | 발산 가로 막대 (기여도 분해) | 신규 |
| `app/api/admin/layer2/route.ts` | 2층 데이터 (섹션별 lazy fetch) | 신규 |
| `supabase/migrations/20260921020000_admin_layer2_contribution.sql` | route 별 원가 + 별 실효 단가 | 신규 |
| `supabase/migrations/20260921021000_admin_layer2_revenue.sql` | 패키지 믹스 + 구독 분해 | 신규 |
| `supabase/migrations/20260921022000_admin_free_share_funnel.sql` | 유입 경로별 가입 + 무료 상품 공유 퍼널(§5) | 신규 |
| `supabase/migrations/20260921023000_admin_layer2_readings.sql` | 종목별 리딩·완료율·결과 열람 | 신규 |
| `supabase/migrations/20260921024000_admin_path_reader.sql` | 경로 판독기(§6) — 시퀀스 + 이탈 지점 | 신규 |
| `supabase/migrations/20260921025000_admin_layer2_retention.sql` | D7 코호트 곡선 + 무료 상품 리프트 | 신규 |
| `supabase/migrations/20260921030000_admin_roadmap_kpi.sql` | 3층 판정 KPI (roadmap SQL 이식) | 신규 |
| `lib/admin/roadmap.ts` | 베이스라인·목표선·판정 규칙 (순수) | 신규 |
| `lib/admin/roadmap.test.ts` | 판정 계약 | 신규 |
| `app/admin/roadmap/page.tsx` | 3층 화면 | 신규 |
| `components/admin/AdminNav.tsx` · `AdminMobileNav.tsx` | 판정(3층) 항목 추가 | 수정 |
| `lib/admin-metrics.ts` · `lib/admin-metrics.test.ts` | `isAlerting` 추가 · `result_viewed` drift 해소 · 신규 키 5개 | 수정 |

**경계** — `lib/admin/*` 는 전부 순수(DB·React import 0)다. RPC 는 원시 카운트만 주고 산식을 갖지 않는다. 화면은 산식을 갖지 않는다. 이 3분할이 "숫자가 왜 저렇게 나왔나"를 유닛 테스트로 답할 수 있게 하는 유일한 구조다.

---

## 단계 6 — 소표본 게이트 (1층이 쓰므로 먼저 만든다)

### Task 1: 단위 포맷 + 게이트 컴포넌트

**Files:**
- Create: `lib/admin/format.ts`
- Create: `lib/admin/format.test.ts`
- Create: `components/admin/Metric.tsx`
- Modify: `lib/admin-metrics.ts` (경보 판정 함수 추가)

- [ ] **Step 1: 실패 테스트를 쓴다**

`lib/admin/format.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { formatMetric, formatSignedWon } from "./format.ts";

test("won 은 반올림 + ko-KR 천단위 + '원'", () => {
  assert.equal(formatMetric(12345.6, "won"), "12,346원");
  assert.equal(formatMetric(0, "won"), "0원");
});

test("percent 는 소수 1자리", () => {
  assert.equal(formatMetric(67.55, "percent"), "67.6%");
  assert.equal(formatMetric(100, "percent"), "100.0%");
});

test("count 는 정수 천단위, ratio 는 소수 2자리", () => {
  assert.equal(formatMetric(1234, "count"), "1,234");
  assert.equal(formatMetric(0.1234, "ratio"), "0.12");
});

// 🔴 로케일을 고정하지 않으면 서버(UTC/en-US)와 브라우저에서 구분자가 갈려 하이드레이션이 깨진다.
test("로케일이 고정돼 있다 — 시스템 로케일에 좌우되지 않는다", () => {
  assert.ok(formatMetric(1000, "count").includes(","));
});

test("부호 있는 금액은 유니코드 마이너스와 + 를 명시한다", () => {
  assert.equal(formatSignedWon(-12400), "−12,400원");
  assert.equal(formatSignedWon(64200), "+64,200원");
  assert.equal(formatSignedWon(0), "+0원");
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/admin/format.test.ts`
Expected: FAIL — `Cannot find module './format.ts'`

- [ ] **Step 3: `lib/admin/format.ts` 를 쓴다**

```ts
// lib/admin/format.ts — 지표 값의 표시 포맷. 순수(React·DB import 0).
//
// 왜 한 곳인가: 화면마다 toFixed 자릿수와 천단위 구분자를 따로 쓰면 같은 지표가 화면마다 다른
// 숫자로 보인다. 어드민의 존재 이유가 "두 화면 숫자를 나란히 놓고 읽는 것"이라 이게 치명적이다.
import type { MetricUnit } from "@/lib/admin-metrics";

// 🔴 로케일 고정 — 서버(Vercel, UTC/en-US)와 클라이언트가 다른 구분자를 쓰면 하이드레이션 불일치가 난다.
const LOCALE = "ko-KR";

export function formatMetric(value: number, unit: MetricUnit): string {
  switch (unit) {
    case "won":
      return `${Math.round(value).toLocaleString(LOCALE)}원`;
    case "percent":
      // 🔴 toFixed 만 쓰면 67.55 가 "67.5" 가 된다 — 배정도로 67.5499… 라서다(Task 1 실측).
      //    먼저 반올림해 정확한 1자리 값으로 만든 뒤 자릿수를 고정한다.
      return `${(Math.round(value * 10) / 10).toFixed(1)}%`;
    case "count":
      return Math.round(value).toLocaleString(LOCALE);
    case "ratio":
      return value.toFixed(2);
  }
}

/**
 * 부호를 항상 드러내는 금액 — 기여(매출−광고비−원가)는 음수가 기본값이다.
 * 하이픈(-)이 아니라 U+2212 MINUS SIGN 을 쓴다: 숫자 폭과 맞아 표가 어긋나지 않는다.
 */
export function formatSignedWon(value: number): string {
  const rounded = Math.round(value);
  const sign = rounded < 0 ? "−" : "+";
  return `${sign}${Math.abs(rounded).toLocaleString(LOCALE)}원`;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/admin/format.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 경보 판정을 `lib/admin-metrics.ts` 에 추가한다**

`sampleGate` 바로 아래에 붙인다(정의와 판정이 같은 파일에 있는 기존 관례):

```ts
/**
 * 가드레일 경보 판정. 경보선이 없는 지표는 항상 false.
 * alertBelow 와 alertAbove 가 둘 다 있으면 OR — 양방향 밴드를 벗어났는가.
 */
export function isAlerting(key: MetricKey, value: number | null): boolean {
  if (value === null) return false; // 값이 없는 것은 경보가 아니다 — 조회 실패는 따로 표시한다
  const m: MetricDef = METRICS[key];
  const below = m.alertBelow !== undefined && value < m.alertBelow;
  const above = m.alertAbove !== undefined && value > m.alertAbove;
  return below || above;
}
```

`lib/admin-metrics.test.ts` 끝에 계약을 추가한다(import 줄에 `isAlerting` 추가):

```ts
test("isAlerting — 경보선 양쪽과 경계값", () => {
  assert.equal(isAlerting("first_reading_rate", 79.9), true);   // alertBelow 80
  assert.equal(isAlerting("first_reading_rate", 80), false);    // 경계는 경보 아님
  assert.equal(isAlerting("new_error_classes", 1), true);       // alertAbove 0
  assert.equal(isAlerting("new_error_classes", 0), false);
  assert.equal(isAlerting("revenue_won", -999), false);         // 경보선 없는 지표
  assert.equal(isAlerting("first_reading_rate", null), false);  // 값 없음 ≠ 경보
});
```

- [ ] **Step 6: `components/admin/Metric.tsx` 를 쓴다**

```tsx
// components/admin/Metric.tsx — 소표본 게이트가 걸린 지표 카드.
//
// 🔴 왜 "흐리게"가 아니라 "대체"인가 — 표본 12명으로 낸 전환율은 틀린 게 아니라 **판단 근거가 될
//    수 없다.** 흐리게 그리면 사람은 결국 읽고 판단한다. 숫자를 아예 치우고 n 을 보여주는 것만이
//    오판을 막는다(스펙 §7).
import { METRICS, sampleGate, isAlerting, type MetricDef, type MetricKey } from "@/lib/admin-metrics";
import { formatMetric } from "@/lib/admin/format";

export function Metric({
  metricKey,
  value,
  n,
  sub,
}: {
  metricKey: MetricKey;
  /** null = 조회 실패 또는 계산 불가(0 나눗셈). 0 과 구분된다. */
  value: number | null;
  /** 이 값을 만든 표본 크기. 카운트 지표는 minSample 이 0 이라 무시된다. */
  n: number;
  sub?: string;
}) {
  // 🔴 `MetricDef` 명시가 필수다 — METRICS 는 `as const satisfies` 라 색인 결과가 **원소별 좁은
  //    유니온**이고, caveat/alertBelow 를 안 쓴 지표는 그 키 자체가 타입에 없어 TS2339 가 난다.
  //    (Task 1 실측. 이 파일에서 `METRICS[...]` 를 꺼내는 모든 곳에 같은 규칙이 적용된다.)
  const m: MetricDef = METRICS[metricKey];
  const gate = sampleGate(metricKey, n);
  const alerting = gate.show && isAlerting(metricKey, value);

  return (
    <div
      className={`rounded-xl border p-4 ${
        alerting ? "border-[#d03b3b] bg-[#d03b3b]/10" : "border-white/10 bg-white/5"
      }`}
    >
      <div className="text-[12px] text-white/60" title={m.definition}>
        {m.label}
      </div>
      <div className={`text-2xl font-bold mt-1 ${alerting ? "text-[#ec835a]" : ""}`}>
        {!gate.show ? (
          <span className="text-base font-normal text-white/40">{gate.note}</span>
        ) : value === null ? (
          <span className="text-white/40">—</span>
        ) : (
          formatMetric(value, m.unit)
        )}
      </div>
      {sub && <div className="text-[12px] text-white/50 mt-1.5">{sub}</div>}
      {m.caveat && (
        <div className="text-[11px] text-white/35 mt-1.5 leading-snug">{m.caveat}</div>
      )}
    </div>
  );
}
```

- [ ] **Step 7: 게이트를 돌린다**

Run: `npx tsc --noEmit`
Expected: 오류 0

Run: `node --import tsx --test "lib/**/*.test.ts"`
Expected: 전부 pass (플랜 A 기준 736 + 신규 6)

- [ ] **Step 8: 커밋**

```bash
git add lib/admin/format.ts lib/admin/format.test.ts components/admin/Metric.tsx lib/admin-metrics.ts lib/admin-metrics.test.ts
git commit -F- <<'MSG'
feat(admin): 소표본 게이트 컴포넌트 + 단위 포맷 정본 (플랜B 단계6)

표본 미달이면 숫자를 흐리게가 아니라 "n=N · 판단 보류"로 대체한다.
로케일을 ko-KR 로 고정해 서버/클라 구분자 불일치를 막는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## 단계 3 — 1층

### Task 2: 밴드 계산 (`lib/admin/band.ts`)

**Files:**
- Create: `lib/admin/band.ts`
- Create: `lib/admin/band.test.ts`

- [ ] **Step 1: 실패 테스트를 쓴다**

`lib/admin/band.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { rolling7, quantile, computeBand, costCoverage, pickBandAxis, dailyValues, type DailyPnl } from "./band.ts";

const day = (bucket: string, revenueWon: number, adSpendWon: number, apiCostWon: number, costRows: number): DailyPnl =>
  ({ bucket, revenueWon, adSpendWon, apiCostWon, costRows });

test("rolling7 — 앞 6일은 창이 안 차므로 버린다", () => {
  assert.deepEqual(rolling7([1, 1, 1, 1, 1, 1, 1, 1]), [7, 7]); // 8일 입력 → 2개
  assert.deepEqual(rolling7([1, 2, 3]), []); // 7일 미만이면 빈 배열
});

test("quantile — 선형보간, 경계", () => {
  const s = [10, 20, 30, 40, 50];
  assert.equal(quantile(s, 0), 10);
  assert.equal(quantile(s, 1), 50);
  assert.equal(quantile(s, 0.5), 30);
  assert.equal(quantile(s, 0.25), 20);
});

test("computeBand — 현재값 위치와 범위 이탈", () => {
  const rolling = Array.from({ length: 56 }, (_, i) => i); // 0..55, 현재값 = 마지막 = 55
  const b = computeBand(rolling);
  assert.ok(b);
  assert.equal(b.current, 55);
  assert.ok(b.p10 < b.p90);
  assert.equal(b.outside, true); // 55 > p90
});

test("computeBand — 표본이 8개 미만이면 null (밴드를 그리지 않는다)", () => {
  assert.equal(computeBand([1, 2, 3]), null);
});

test("costCoverage — cost_rows 가 0 인 날은 '0원'이 아니라 '미축적'", () => {
  const days = [day("2026-09-19", 1000, 2000, 0, 0), day("2026-09-20", 1000, 2000, 50, 12)];
  const c = costCoverage(days);
  assert.equal(c.covered, 1);
  assert.equal(c.total, 2);
  assert.equal(c.full, false);
});

test("pickBandAxis — 원가가 창 전체를 덮어야 완전 기여 축", () => {
  const partial = [day("2026-09-19", 0, 0, 0, 0), day("2026-09-20", 0, 0, 50, 12)];
  assert.equal(pickBandAxis(partial), "marketing");
  const full = [day("2026-09-19", 0, 0, 10, 3), day("2026-09-20", 0, 0, 50, 12)];
  assert.equal(pickBandAxis(full), "contribution");
});

test("dailyValues — 축에 따라 원가를 빼거나 뺀다/안 뺀다", () => {
  const days = [day("2026-09-20", 10_000, 7_000, 500, 12)];
  assert.deepEqual(dailyValues(days, "marketing"), [3_000]);
  assert.deepEqual(dailyValues(days, "contribution"), [2_500]);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/admin/band.test.ts`
Expected: FAIL — `Cannot find module './band.ts'`

- [ ] **Step 3: `lib/admin/band.ts` 를 쓴다**

```ts
// lib/admin/band.ts — 7일 롤링과 8주 분포 밴드. 순수(DB·React import 0).
//
// 🔴 왜 롤링인가 — 결제가 하루 3.5건이라 일별 손익이 −₩11k~+₩40k 로 요동한다. 일별 값을 1층
//    주 지표로 쓰면 매일 "큰 변화"가 보이고 그게 전부 노이즈다(스펙 §3).
// 🔴 왜 밴드인가 — "이 변동이 신호인가 노이즈인가"에 답하는 유일한 장치. 최근 8주의 롤링 값
//    분포에서 오늘이 어디쯤인지 보여준다.

export interface DailyPnl {
  /** KST 날짜 YYYY-MM-DD */
  bucket: string;
  revenueWon: number;
  adSpendWon: number;
  apiCostWon: number;
  /** 🔴 그 날 llm_usage 행 수. 0 = 미축적(원가 0원과 구별된다). */
  costRows: number;
}

/** 7일 합의 이동 배열. 창이 차지 않는 앞 6일은 버린다(부분 합은 낮게 나와 밴드를 왜곡한다). */
export function rolling7(values: number[]): number[] {
  if (values.length < 7) return [];
  const out: number[] = [];
  let sum = values.slice(0, 7).reduce((a, b) => a + b, 0);
  out.push(sum);
  for (let i = 7; i < values.length; i++) {
    sum += values[i] - values[i - 7];
    out.push(sum);
  }
  return out;
}

/** 선형보간 분위수. `sorted` 는 오름차순이어야 한다. */
export function quantile(sorted: number[], q: number): number {
  if (sorted.length === 0) return 0;
  if (sorted.length === 1) return sorted[0];
  const pos = (sorted.length - 1) * q;
  const lo = Math.floor(pos);
  const hi = Math.ceil(pos);
  if (lo === hi) return sorted[lo];
  return sorted[lo] + (sorted[hi] - sorted[lo]) * (pos - lo);
}

export interface Band {
  p10: number;
  p90: number;
  current: number;
  /** 현재값의 백분위(0~100). 화면 캡션("하위 27%")에 쓴다. */
  pctRank: number;
  /** P10~P90 밖인가 — 마커가 빨강이 된다. */
  outside: boolean;
}

/**
 * 밴드 계산. 마지막 값이 "현재"다.
 * 표본 8개 미만이면 null — 분위수가 사실상 최소/최대가 되어 "범위 안"이 무의미해진다.
 */
export function computeBand(rolling: number[]): Band | null {
  if (rolling.length < 8) return null;
  const current = rolling[rolling.length - 1];
  const sorted = [...rolling].sort((a, b) => a - b);
  const p10 = quantile(sorted, 0.1);
  const p90 = quantile(sorted, 0.9);
  // 백분위 = 현재값 이하인 표본의 비율. 동률은 아래로 센다(보수적).
  const pctRank = (sorted.filter((v) => v <= current).length / sorted.length) * 100;
  return { p10, p90, current, pctRank, outside: current < p10 || current > p90 };
}

export interface CostCoverage {
  /** 원가 행이 하나라도 있는 날의 수. */
  covered: number;
  total: number;
  /** 창의 모든 날이 덮였는가. */
  full: boolean;
}

/**
 * 🔴 `apiCostWon = 0` 과 "그 날 원가를 기록하지 않았다"는 전혀 다른 말이다.
 * llm_usage 는 2026-09-20 부터 쌓이므로 그 이전은 **영원히 비어 있다**(소급 불가).
 */
export function costCoverage(days: DailyPnl[]): CostCoverage {
  const covered = days.filter((d) => d.costRows > 0).length;
  return { covered, total: days.length, full: days.length > 0 && covered === days.length };
}

/** 밴드를 어느 축으로 그릴지. 성분 구성이 다른 값을 한 분포에 섞으면 밴드가 거짓말을 한다. */
export type BandAxis = "contribution" | "marketing";

export function pickBandAxis(days: DailyPnl[]): BandAxis {
  return costCoverage(days).full ? "contribution" : "marketing";
}

/** 축에 맞는 일별 값 배열. `contribution` 은 원가까지 뺀다. */
export function dailyValues(days: DailyPnl[], axis: BandAxis): number[] {
  return days.map((d) =>
    axis === "contribution" ? d.revenueWon - d.adSpendWon - d.apiCostWon : d.revenueWon - d.adSpendWon
  );
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/admin/band.test.ts`
Expected: PASS (7 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/admin/band.ts lib/admin/band.test.ts
git commit -F- <<'MSG'
feat(admin): 7일 롤링 + 8주 P10~P90 밴드 (플랜B 단계3)

결제 하루 3.5건이라 일별 손익은 노이즈다. 롤링으로 평활하고
최근 8주 분포에서 현재 위치를 보여 "신호인가 노이즈인가"에 답한다.
원가 미축적 날은 0원이 아니라 미축적으로 구분해 밴드 축을 고른다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 3: 1층 산식 (`lib/admin/layer1.ts`)

**Files:**
- Create: `lib/admin/layer1.ts`
- Create: `lib/admin/layer1.test.ts`

- [ ] **Step 1: 실패 테스트를 쓴다**

`lib/admin/layer1.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { computeUnit, computeGuardrails, type UnitRow, type GuardRow } from "./layer1.ts";

test("computeUnit — 가입당 매출 = 결제율 × ARPPU 가 항등식으로 성립한다", () => {
  const r: UnitRow = { signups: 914, payers: 82, revenueWon: 274_900, adSpendWon: 328_042 };
  const u = computeUnit(r);
  assert.ok(u.payRate !== null && u.arppuWon !== null && u.revPerSignup !== null);
  assert.ok(Math.abs((u.payRate / 100) * u.arppuWon - u.revPerSignup) < 0.01);
});

test("computeUnit — CAC 는 광고비 ÷ 가입", () => {
  const u = computeUnit({ signups: 914, payers: 82, revenueWon: 274_900, adSpendWon: 328_042 });
  assert.equal(Math.round(u.cacWon!), 359);
});

// 🔴 0 나눗셈이 Infinity/NaN 으로 새면 화면에 "Infinity원"이 뜬다. null 로 막는다.
test("computeUnit — 분모가 0 이면 null (0 이나 Infinity 가 아니다)", () => {
  const u = computeUnit({ signups: 0, payers: 0, revenueWon: 0, adSpendWon: 5000 });
  assert.equal(u.payRate, null);
  assert.equal(u.arppuWon, null);
  assert.equal(u.cacWon, null);
  assert.equal(u.revPerSignup, null);
});

test("computeGuardrails — 비율 지표는 num/den, 카운트 지표는 num 그대로", () => {
  const rows: GuardRow[] = [
    { metric: "first_reading_rate", num: 806, den: 914 },
    { metric: "new_error_classes", num: 2, den: 0 },
  ];
  const out = computeGuardrails(rows);
  const fr = out.find((g) => g.key === "first_reading_rate")!;
  assert.equal(Math.round(fr.value! * 10) / 10, 88.2);
  assert.equal(fr.n, 914);
  assert.equal(fr.alerting, false); // 88.2 >= 80

  const ne = out.find((g) => g.key === "new_error_classes")!;
  assert.equal(ne.value, 2);
  assert.equal(ne.alerting, true); // alertAbove 0
});

test("computeGuardrails — 가드레일 6종이 모두 나오고, 행이 없으면 value=null", () => {
  const out = computeGuardrails([]);
  assert.equal(out.length, 6);
  assert.ok(out.every((g) => g.value === null));
  assert.ok(out.every((g) => g.alerting === false)); // 값 없음은 경보가 아니다
});

test("computeGuardrails — 모르는 metric 문자열은 버린다 (크래시하지 않는다)", () => {
  const out = computeGuardrails([{ metric: "made_up_key", num: 1, den: 1 }]);
  assert.equal(out.length, 6);
});

// 소표본 게이트가 비율 지표에만 걸리는지 — 카운트는 n 과 무관하게 항상 보여야 한다.
test("computeGuardrails — 비율은 n<30 이면 가려지고 카운트는 안 가려진다", () => {
  const out = computeGuardrails([
    { metric: "login_success_rate", num: 7, den: 12 },
    { metric: "unreviewed_sensitive", num: 3, den: 0 },
  ]);
  assert.equal(out.find((g) => g.key === "login_success_rate")!.show, false);
  assert.equal(out.find((g) => g.key === "unreviewed_sensitive")!.show, true);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/admin/layer1.test.ts`
Expected: FAIL — `Cannot find module './layer1.ts'`

- [ ] **Step 3: `lib/admin/layer1.ts` 를 쓴다**

```ts
// lib/admin/layer1.ts — 1층 지표 산식. 순수(DB·React import 0).
//
// RPC 는 **원시 분자/분모만** 준다. 비율을 SQL 안에서 만들면 "이 숫자가 왜 이렇게 나왔나"를
// 유닛 테스트로 물을 수 없고, 소표본 게이트에 필요한 n(분모)도 잃는다.
import {
  GUARDRAILS,
  METRICS,
  isAlerting,
  isMetricKey,
  sampleGate,
  type MetricKey,
} from "@/lib/admin-metrics";

export interface UnitRow {
  signups: number;
  payers: number;
  revenueWon: number;
  adSpendWon: number;
}

export interface UnitMetrics {
  /** 퍼센트(0~100). 분모 0 이면 null. */
  payRate: number | null;
  arppuWon: number | null;
  cacWon: number | null;
  revPerSignup: number | null;
}

/** 0 나눗셈은 null — Infinity·NaN 이 화면까지 새는 걸 막는다. */
const div = (a: number, b: number): number | null => (b > 0 ? a / b : null);

export function computeUnit(r: UnitRow): UnitMetrics {
  return {
    payRate: div(r.payers * 100, r.signups),
    arppuWon: div(r.revenueWon, r.payers),
    cacWon: div(r.adSpendWon, r.signups),
    revPerSignup: div(r.revenueWon, r.signups),
  };
}

/** RPC `admin_layer1_guard` 의 한 행. 카운트 지표는 den=0 으로 온다. */
export interface GuardRow {
  metric: string;
  num: number;
  den: number;
}

export interface GuardView {
  key: MetricKey;
  label: string;
  /** null = 그 지표 행이 없었다(조회 실패 또는 해당 없음). */
  value: number | null;
  /** 소표본 게이트의 표본 크기. */
  n: number;
  alerting: boolean;
  show: boolean;
  note?: string;
}

/**
 * 가드레일 6종을 GUARDRAILS 순서대로 항상 6개 반환한다.
 * 행이 없어도 자리를 비우지 않는 이유: 가드레일이 조용히 사라지면 "이상 없음"으로 오독된다.
 */
export function computeGuardrails(rows: GuardRow[]): GuardView[] {
  const byKey = new Map<string, GuardRow>();
  for (const r of rows) {
    if (isMetricKey(r.metric)) byKey.set(r.metric, r); // enum 밖 값은 버린다
  }
  return GUARDRAILS.map((key) => {
    const row = byKey.get(key);
    const m = METRICS[key];
    // count 단위는 분자가 곧 값이다. 비율은 num/den×100.
    const value = row === undefined ? null : m.unit === "count" ? row.num : div(row.num * 100, row.den);
    const n = row?.den ?? 0;
    // 카운트 지표는 표본 개념이 없다(minSample 0) — n 을 0 으로 넘겨도 게이트가 통과된다.
    const gate = sampleGate(key, m.unit === "count" ? 0 : n);
    return {
      key,
      label: m.label,
      value,
      n,
      alerting: gate.show && isAlerting(key, value),
      show: gate.show,
      note: gate.note,
    };
  });
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/admin/layer1.test.ts`
Expected: PASS (7 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/admin/layer1.ts lib/admin/layer1.test.ts
git commit -F- <<'MSG'
feat(admin): 1층 산식 모듈 — 단가 분해 + 가드레일 6종 (플랜B 단계3)

RPC 는 원시 분자/분모만 주고 비율은 여기서 만든다.
0 나눗셈은 null 로 막아 Infinity 가 화면에 새지 않게 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 4: 일별 손익 RPC (`admin_layer1_pnl`)

**Files:**
- Create: `supabase/migrations/20260921010000_admin_layer1_pnl.sql`

- [ ] **Step 1: 마이그레이션을 쓴다**

```sql
-- admin_layer1_pnl — 1층 손익의 일별 원시행. 매출·광고비·API원가 3소스를 KST 날짜축에 맞춘다.
--
-- 🔴 왜 일별 행을 주고 롤링을 앱에서 하나 — 밴드(P10~P90)는 56개의 7일 롤링 값이 필요하고,
--    그 분위수 계산은 순수 함수여야 유닛 테스트로 잠긴다(lib/admin/band.ts). SQL 안에서 만들면
--    "이 숫자가 왜 이렇게 나왔나"를 테스트로 물을 수 없다. 행 수는 창 일수(≈63)라 cap 무관.
--
-- 🔴 cost_rows 를 같이 주는 이유 — llm_usage 는 2026-09-20 부터 쌓인다. 그 이전 날의 SUM 은
--    0 이지만 그건 "원가가 0원"이 아니라 "기록이 없다"이고, 둘을 구분하지 못하면 1층이
--    적자를 흑자로 위장한다. 행 수가 0 이면 미축적이다.
--
-- ⚠️ 어드민 제외는 payments/llm_usage 에만 건다. ad_spend 는 유저 개념이 없다.
-- ⚠️ payments.user_id 는 탈퇴 시 NULL 로 익명 보존된다 → `(IS NULL OR <> ALL)` 필수.
--    `<> ALL` 만 쓰면 3값 논리로 NULL 행이 통째로 빠져 탈퇴자 매출이 증발한다(2026-08-02 사례).
-- ⚠️ route IS NULL 인 llm_usage 행은 QA·probe 스크립트가 만든 것이다 — 손익에서 뺀다.
CREATE OR REPLACE FUNCTION admin_layer1_pnl(
  p_since TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (
  bucket DATE,
  revenue_won BIGINT,
  ad_spend_won BIGINT,
  api_cost_won NUMERIC,
  cost_rows BIGINT
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH axis AS (
    -- 데이터가 없는 날도 행이 있어야 롤링 창이 어긋나지 않는다.
    -- `at time zone 'UTC'` 를 빼면 캐스트가 세션 TimeZone 에 좌우된다(AGENTS.md).
    SELECT generate_series(
             (p_since AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date,
             ((now() AT TIME ZONE 'UTC') + INTERVAL '9 hours')::date,
             INTERVAL '1 day'
           )::date AS d
  ), rev AS (
    SELECT (p.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date AS d,
           SUM(p.amount_won)::BIGINT AS won
    FROM payments p
    WHERE p.status = 'completed'
      AND p.created_at >= p_since
      AND (p.user_id IS NULL OR p.user_id <> ALL(p_exclude))
    GROUP BY 1
  ), ad AS (
    -- spend_date 는 이미 KST 날짜다(사용자가 손으로 입력) — 변환하지 않는다.
    SELECT a.spend_date AS d, SUM(a.spend_won)::BIGINT AS won
    FROM ad_spend a
    WHERE a.spend_date >= (p_since AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date
    GROUP BY 1
  ), cost AS (
    SELECT (l.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date AS d,
           -- 단가 미확정 모델은 cost_won 이 NULL 이라 SUM 에서 빠진다(0 아님) — 토큰은 남아 소급 가능.
           COALESCE(SUM(l.cost_won), 0) AS won,
           COUNT(*)::BIGINT AS rows_n
    FROM llm_usage l
    WHERE l.created_at >= p_since
      AND l.route IS NOT NULL
      AND (l.user_id IS NULL OR l.user_id <> ALL(p_exclude))
    GROUP BY 1
  )
  SELECT x.d,
         COALESCE(rev.won, 0)::BIGINT,
         COALESCE(ad.won, 0)::BIGINT,
         COALESCE(cost.won, 0)::NUMERIC,
         COALESCE(cost.rows_n, 0)::BIGINT
  FROM axis x
  LEFT JOIN rev  ON rev.d  = x.d
  LEFT JOIN ad   ON ad.d   = x.d
  LEFT JOIN cost ON cost.d = x.d
  ORDER BY x.d;
$$;

-- AGENTS.md: 새 SECURITY DEFINER RPC 는 PUBLIC·anon·authenticated 셋 다 명시 REVOKE.
REVOKE ALL ON FUNCTION admin_layer1_pnl(TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer1_pnl(TIMESTAMPTZ, UUID[]) TO service_role;
```

- [ ] **Step 2: 🔴 본문을 prod 에서 실행해 검증한다 (적용 전)**

함수는 `service_role` 전용이라 `run-prod-query.mjs` 로 **직접 호출이 안 된다**. 본문을 인라인해서 돌린다.

`/tmp/verify-pnl.sql` 을 만들어 위 CTE 체인을 그대로 붙이고, `p_since` 를 리터럴로, `p_exclude` 를 지인 6명으로 치환한다:

```sql
WITH params AS (
  SELECT timestamptz '2026-07-21 00:00+09' AS p_since
), axis AS (
  SELECT generate_series(
           (p.p_since AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date,
           ((now() AT TIME ZONE 'UTC') + INTERVAL '9 hours')::date,
           INTERVAL '1 day')::date AS d
  FROM params p
), rev AS (
  SELECT (p.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date AS d,
         SUM(p.amount_won)::BIGINT AS won
  FROM payments p, params pr
  WHERE p.status = 'completed' AND p.created_at >= pr.p_since
    AND (p.user_id IS NULL OR left(p.user_id::text,8) NOT IN
         ('9ff43266','b9e5dd5a','7f83a4d7','a3bcc2c7','3d648ebe','d8fdcdd0'))
  GROUP BY 1
), ad AS (
  SELECT a.spend_date AS d, SUM(a.spend_won)::BIGINT AS won
  FROM ad_spend a, params pr
  WHERE a.spend_date >= (pr.p_since AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date
  GROUP BY 1
), cost AS (
  SELECT (l.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date AS d,
         COALESCE(SUM(l.cost_won), 0) AS won, COUNT(*)::BIGINT AS rows_n
  FROM llm_usage l, params pr
  WHERE l.created_at >= pr.p_since AND l.route IS NOT NULL
  GROUP BY 1
)
SELECT x.d, COALESCE(rev.won,0) AS revenue_won, COALESCE(ad.won,0) AS ad_spend_won,
       COALESCE(cost.won,0) AS api_cost_won, COALESCE(cost.rows_n,0) AS cost_rows
FROM axis x
LEFT JOIN rev ON rev.d = x.d
LEFT JOIN ad ON ad.d = x.d
LEFT JOIN cost ON cost.d = x.d
ORDER BY x.d DESC LIMIT 15;
```

Run: `SUPABASE_PAT=... node scripts/run-prod-query.mjs /tmp/verify-pnl.sql`

**합격 조건 4가지 — 하나라도 어긋나면 SQL 을 고친다:**
1. 날짜 행이 **빠짐없이 연속**이다(결제 없는 날도 행이 있다)
2. `ad_spend_won` 의 마지막 입력일이 스펙 §2-4 와 맞는다(09-19까지 입력됨)
3. 🔴 **`llm_usage` 는 prod 에 아직 없다** — 이 CTE 가 `relation "llm_usage" does not exist` 로 죽는 게 **정상**이다. 그 경우 `cost` CTE 만 빼고 나머지 3개를 검증한 뒤, `cost` 는 **dev 에서** 같은 방식으로 확인한다(플랜 A 가 dev 에만 적용했다).
4. 최근 30일 `SUM(revenue_won)` 이 **₩274,900** 근처다(스펙 §2-4 실측치 — 창이 다르면 값도 다르니 창을 09-20 기준 30일로 맞춰 대조할 것)

- [ ] **Step 3: dev 에 적용 — 푸시로 자동 적용된다**

이 리포는 Supabase Git sync 라 `dev` push 시 자동 적용된다. 커밋만 하고 푸시 타이밍은 사용자 지시를 따른다.

- [ ] **Step 4: 커밋**

```bash
git add supabase/migrations/20260921010000_admin_layer1_pnl.sql
git commit -F- <<'MSG'
feat(admin): 일별 손익 RPC — 매출·광고비·API원가를 KST 축에 정렬 (플랜B 단계3)

롤링·분위수는 앱의 순수 모듈이 한다. RPC 는 원시 일별 행만 준다.
cost_rows 로 "원가 0원"과 "원가 미기록"을 구분한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 5: 단가·가드레일 RPC (`admin_layer1_unit` · `admin_layer1_guard`)

**Files:**
- Create: `supabase/migrations/20260921011000_admin_layer1_rates.sql`
- Modify: `lib/admin-metrics.ts` (`result_viewed` 의 definition 정밀화 + drift 해소 표기)

- [ ] **Step 1: 마이그레이션을 쓴다**

```sql
-- admin_layer1_unit / admin_layer1_guard — 1층의 단가 분해와 가드레일 6종.
--
-- 🔴 비율을 만들지 않는다. num/den 원시값만 준다 — 산식은 lib/admin/layer1.ts 가 갖고,
--    den 은 소표본 게이트의 n 으로도 쓰인다. SQL 이 round() 해버리면 그 n 을 잃는다.

-- ── 단가 (가입 코호트 귀속) ────────────────────────────────────────────────
-- 🔴 코호트 귀속이다 — 창에 **가입한** 사람이 **전 기간** 낸 매출. 캘린더 귀속(창에 발생한
--    매출 ÷ 창의 가입자)과 다르며, 2026-09-19 분석이 코호트로 정정했다(스펙 §7).
-- ⚠️ 탈퇴자는 users 행이 사라져 코호트에서 빠진다. 그 사람의 결제는 payments 에 익명으로
--    남지만 이 창에는 안 잡힌다 — 코호트 지표의 구조적 한계다(화면 caveat 에 있음).
CREATE OR REPLACE FUNCTION admin_layer1_unit(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (signups BIGINT, payers BIGINT, revenue_won BIGINT, ad_spend_won BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH coh AS (
    SELECT u.id
    FROM users u
    WHERE u.created_at >= p_since
      AND (p_until IS NULL OR u.created_at < p_until)
      AND u.id <> ALL(p_exclude)
  ), pay AS (
    SELECT p.user_id, SUM(p.amount_won)::BIGINT AS won
    FROM payments p
    JOIN coh ON coh.id = p.user_id
    WHERE p.status = 'completed'
    GROUP BY 1
  ), ad AS (
    SELECT COALESCE(SUM(a.spend_won), 0)::BIGINT AS won
    FROM ad_spend a
    WHERE a.spend_date >= (p_since AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date
      AND (p_until IS NULL
           OR a.spend_date < (p_until AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date)
  )
  SELECT (SELECT COUNT(*) FROM coh)::BIGINT,
         (SELECT COUNT(*) FROM pay)::BIGINT,
         (SELECT COALESCE(SUM(won), 0) FROM pay)::BIGINT,
         (SELECT won FROM ad);
$$;

REVOKE ALL ON FUNCTION admin_layer1_unit(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer1_unit(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;

-- ── 가드레일 6종 ───────────────────────────────────────────────────────────
-- 반환은 (metric, num, den) 의 긴 형식이다. 지표마다 컬럼을 만들면 하나 추가할 때마다
-- 시그니처가 바뀌고 앱의 타입이 따라 흔들린다.
-- 카운트 지표(new_error_classes · unreviewed_sensitive)는 den = 0 으로 온다.
--
-- ⚠️ 창 적용이 지표마다 다르고 그게 의도된 것이다:
--   · 코호트 3종(첫리딩·결과열람·결제완료)은 창 기반
--   · 로그인 성공률은 창 기반(anon 단위라 코호트가 아니다)
--   · 신규 에러 클래스는 **최근 24시간 고정** — "지금 뭔가 터졌나"를 묻는 지표다
--   · 미검토 민감알림은 **현재 상태** — 창 개념이 없다
CREATE OR REPLACE FUNCTION admin_layer1_guard(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (metric TEXT, num BIGINT, den BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH coh AS (
    SELECT u.id
    FROM users u
    WHERE u.created_at >= p_since
      AND (p_until IS NULL OR u.created_at < p_until)
      AND u.id <> ALL(p_exclude)
  ), first_reading AS (
    -- 유저당 첫 리딩 1행. DISTINCT ON 은 ORDER BY 의 선두가 파티션 키여야 한다.
    SELECT DISTINCT ON (r.user_id) r.user_id, r.stars_spent, r.result_viewed_at
    FROM readings r
    JOIN coh ON coh.id = r.user_id
    ORDER BY r.user_id, r.created_at
  ), login_reach AS (
    -- /login 을 본 비봇 anon. 어드민 제외를 걸지 않는다 — 분모가 anon 단위라 판별이 불가능하고,
    -- 분자에만 걸면 비율이 구조적으로 낮아진다(같은 규칙을 양쪽에 거는 쪽을 택했다).
    SELECT DISTINCT pv.anon_id
    FROM page_views pv
    WHERE pv.path = '/login'
      AND pv.anon_id IS NOT NULL
      AND NOT COALESCE(pv.is_bot, false)
      AND pv.created_at >= p_since
      AND (p_until IS NULL OR pv.created_at < p_until)
  ), login_ok AS (
    SELECT DISTINCT pv.anon_id
    FROM page_views pv
    JOIN login_reach lr ON lr.anon_id = pv.anon_id
    WHERE pv.user_id IS NOT NULL
      AND NOT COALESCE(pv.is_bot, false)
      AND pv.created_at >= p_since
      AND (p_until IS NULL OR pv.created_at < p_until)
  ), checkout_started AS (
    SELECT DISTINCT e.user_id
    FROM ui_events e
    WHERE e.event = 'recharge_payment_started'
      AND e.user_id IS NOT NULL
      AND e.created_at >= p_since
      AND (p_until IS NULL OR e.created_at < p_until)
      AND e.user_id <> ALL(p_exclude)
  ), checkout_done AS (
    SELECT cs.user_id
    FROM checkout_started cs
    WHERE EXISTS (
      SELECT 1 FROM payments p
      WHERE p.user_id = cs.user_id
        AND p.status = 'completed'
        AND p.created_at >= p_since
        AND (p_until IS NULL OR p.created_at < p_until)
    )
  ), error_classes AS (
    -- 그룹 키는 /admin/errors 화면과 같은 규칙: fingerprint, 없으면 행 자체가 한 그룹.
    SELECT COALESCE(e.fingerprint, e.id::text) AS k, MIN(e.created_at) AS first_at
    FROM error_logs e
    WHERE e.level = 'error'
    GROUP BY 1
  )
  SELECT 'first_reading_rate'::TEXT,
         (SELECT COUNT(*) FROM coh c WHERE EXISTS (SELECT 1 FROM readings r WHERE r.user_id = c.id))::BIGINT,
         (SELECT COUNT(*) FROM coh)::BIGINT
  UNION ALL
  SELECT 'result_viewed'::TEXT,
         (SELECT COUNT(result_viewed_at) FROM first_reading WHERE stars_spent > 0)::BIGINT,
         (SELECT COUNT(*) FROM first_reading WHERE stars_spent > 0)::BIGINT
  UNION ALL
  SELECT 'login_success_rate'::TEXT,
         (SELECT COUNT(*) FROM login_ok)::BIGINT,
         (SELECT COUNT(*) FROM login_reach)::BIGINT
  UNION ALL
  SELECT 'checkout_completion'::TEXT,
         (SELECT COUNT(*) FROM checkout_done)::BIGINT,
         (SELECT COUNT(*) FROM checkout_started)::BIGINT
  UNION ALL
  SELECT 'new_error_classes'::TEXT,
         (SELECT COUNT(*) FROM error_classes WHERE first_at >= now() - INTERVAL '24 hours')::BIGINT,
         0::BIGINT
  UNION ALL
  SELECT 'unreviewed_sensitive'::TEXT,
         (SELECT COUNT(*) FROM sensitive_alerts WHERE reviewed_at IS NULL)::BIGINT,
         0::BIGINT;
$$;

REVOKE ALL ON FUNCTION admin_layer1_guard(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer1_guard(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;
```

- [ ] **Step 2: 🔴 두 함수의 본문을 **전부** prod 에서 실행한다**

한 함수만 검증하고 넘어가면 다른 하나의 오류로 **파일 전체가 실패**한다(2026-07-31 실사고).

`/tmp/verify-rates.sql` 에 두 본문을 리터럴 치환해 넣고 실행한다. `p_since`/`p_until` 은 09-20 기준 30일(= `timestamptz '2026-08-21 00:00+09'` ~ `'2026-09-20 00:00+09'`), `p_exclude` 는 `left(id::text,8) NOT IN (...)` 형태로 바꾼다.

Run: `SUPABASE_PAT=... node scripts/run-prod-query.mjs /tmp/verify-rates.sql`

**합격 조건 — 스펙 §2 실측치와 대조:**
| 값 | 기대 | 출처 |
|---|---|---|
| `unit.signups` (30일) | **914** | §2-4 |
| `unit.ad_spend_won` (30일) | **328,042** | §2-4 |
| `guard.login_success_rate` | num/den ≈ **1,035 / 1,531 = 67.6%** | §2-6 |
| `guard.first_reading_rate` | ≈ **88.2%** | §3 표 |
| `guard.result_viewed` | ≈ **65%** (리딩 기준 50.7% 가 아니다) | §7 |

🔴 **`result_viewed` 가 50%대로 나오면 정의가 리딩 기준으로 새어든 것이다** — `first_reading` CTE 가 유저당 1행인지, `stars_spent > 0` 필터가 양쪽(분자·분모)에 다 걸렸는지 확인한다.

- [ ] **Step 3: `lib/admin-metrics.ts` 의 `result_viewed` drift 를 해소한다**

`definition` 을 구현과 정확히 일치시키고 `drift` 를 지운다:

```ts
  result_viewed: {
    key: "result_viewed",
    label: "결과 열람",
    definition:
      "가입 코호트 기준 — 그 코호트의 **첫 리딩이 유료인 사람** 중 그 첫 리딩의 result_viewed_at 이 채워진 비율.",
    unit: "percent",
    source: "admin_layer1_guard / result_viewed",
    alertBelow: 55,
    minSample: MIN_SAMPLE.RATE,
    caveat:
      "result_viewed_at 은 '[결과 보기 →] 버튼을 눌렀나'를 잰다. 자동 이동이 아니라 수동 버튼이다. 무료 첫 리딩은 분모에서 빠진다(3층 roadmap KPI 와 같은 정의).",
  },
```

`lib/admin-metrics.test.ts` 에 계약을 추가한다:

```ts
test("plan B 에서 해소된 drift 는 남아 있지 않다", () => {
  assert.equal(METRICS.result_viewed.drift, undefined);
  assert.equal(METRICS.result_viewed.source, "admin_layer1_guard / result_viewed");
});
```

- [ ] **Step 4: 게이트**

Run: `node --import tsx --test lib/admin-metrics.test.ts`
Expected: PASS

Run: `npx tsc --noEmit`
Expected: 오류 0

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/20260921011000_admin_layer1_rates.sql lib/admin-metrics.ts lib/admin-metrics.test.ts
git commit -F- <<'MSG'
feat(admin): 단가·가드레일 RPC + result_viewed 정의 정본화 (플랜B 단계3)

RPC 는 num/den 원시값만 준다 — den 이 소표본 게이트의 n 이기도 하다.
결과 열람을 리딩 기준(50.7%)에서 코호트 기준(65%)으로 교체해
1층 가드레일과 3층 판정이 같은 말을 하게 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 6: 1층 화면 조립

**Files:**
- Create: `components/admin/ContributionHero.tsx`
- Create: `components/admin/BandGauge.tsx`
- Create: `components/admin/GuardrailRow.tsx`
- Modify: `app/admin/page.tsx`

> 🔴 **이 태스크는 기존 섹션(별 소모·연애 상담·처리 대기)을 지우지 않는다.** 1층을 **위에 얹기만** 한다. 2층으로 옮기는 것은 Task 7 이다 — 중간 상태에서도 화면이 온전해야 한다.

- [ ] **Step 1: `components/admin/ContributionHero.tsx`**

```tsx
// components/admin/ContributionHero.tsx — 1층 히어로. "지금 장사가 되고 있나" 한 줄.
//
// 스펙 §8: 손익 한 줄은 차트가 아니라 **히어로 숫자**다. 값 하나를 크게 보여주는 게
// 스파크라인보다 정확하다 — 추세는 바로 아래 밴드가 답한다.
import { formatSignedWon, formatMetric } from "@/lib/admin/format";
import type { CostCoverage } from "@/lib/admin/band";

export function ContributionHero({
  revenueWon,
  adSpendWon,
  apiCostWon,
  coverage,
}: {
  revenueWon: number;
  adSpendWon: number;
  apiCostWon: number;
  coverage: CostCoverage;
}) {
  // 🔴 원가가 창을 다 덮지 못하면 기여에서 **빼지 않는다.** 일부 날만 빼면 그 숫자는 어떤 창의
  //    손익도 아니다. 대신 빠졌다는 사실을 캡션이 말한다(0 으로 위장하지 않는다 — 스펙 §3).
  const contribution = coverage.full
    ? revenueWon - adSpendWon - apiCostWon
    : revenueWon - adSpendWon;
  const negative = contribution < 0;

  return (
    <div className="rounded-2xl border border-white/10 bg-white/5 p-5">
      <div className="text-[12px] text-white/60">
        기여 <span className="text-white/35">(최근 7일 · KST 자정 기준)</span>
      </div>
      <div className={`text-4xl font-bold mt-1 ${negative ? "text-[#ec835a]" : "text-[#0ca30c]"}`}>
        {formatSignedWon(contribution)}
      </div>
      <div className="flex flex-wrap gap-x-5 gap-y-1 mt-3 text-[13px]">
        <span className="text-white/70">
          매출 <span className="text-white">{formatSignedWon(revenueWon)}</span>
        </span>
        <span className="text-white/70">
          광고비 <span className="text-white">{formatSignedWon(-adSpendWon)}</span>
        </span>
        <span className="text-white/70">
          API 원가{" "}
          <span className="text-white">
            {coverage.covered === 0 ? "—" : formatSignedWon(-apiCostWon)}
          </span>
        </span>
      </div>
      {!coverage.full && (
        <div className="text-[11px] text-white/40 mt-2 leading-snug">
          API 원가는 축적 {coverage.covered}/{coverage.total}일 — 창을 다 덮지 못해{" "}
          <b className="text-white/60">기여에서 빠져 있다</b>. 위 값은 매출 − 광고비다.
          {coverage.covered > 0 && ` (덮인 날의 원가 합 ${formatMetric(apiCostWon, "won")})`}
        </div>
      )}
      <div className="text-[11px] text-white/35 mt-1.5 leading-snug">
        인프라 비용(Vercel·Supabase)은 빠져 있다 — 월 단위로만 파악된다.
      </div>
    </div>
  );
}
```

- [ ] **Step 2: `components/admin/BandGauge.tsx`**

```tsx
// components/admin/BandGauge.tsx — 최근 8주 분포 위의 현재 위치.
//
// 스펙 §8: 브랜드 골드(#E8C26A)는 시리즈색이 아니라 **현재값 마커 전용**이다.
// 범위를 벗어나면 critical(#d03b3b)로 바뀐다 — "이건 평소 변동이 아니다".
import type { Band, BandAxis } from "@/lib/admin/band";
import { formatSignedWon } from "@/lib/admin/format";

export function BandGauge({ band, rolling, axis }: { band: Band; rolling: number[]; axis: BandAxis }) {
  const min = Math.min(...rolling);
  const max = Math.max(...rolling);
  const span = max - min || 1; // 전부 같은 값이면 0 나눗셈 — 1로 막는다
  const pos = (v: number) => ((v - min) / span) * 100;

  const left = pos(band.p10);
  const width = pos(band.p90) - left;
  const markerColor = band.outside ? "#d03b3b" : "#E8C26A";

  return (
    <div className="mt-4">
      <div className="relative h-8" aria-hidden>
        {/* 전체 범위 */}
        <div className="absolute inset-x-0 top-1/2 h-[2px] -translate-y-1/2 rounded bg-white/10" />
        {/* P10~P90 */}
        <div
          className="absolute top-1/2 h-2 -translate-y-1/2 rounded bg-white/25"
          style={{ left: `${left}%`, width: `${width}%` }}
        />
        {/* 현재값 */}
        <div
          className="absolute top-1/2 h-5 w-[3px] -translate-x-1/2 -translate-y-1/2 rounded"
          style={{ left: `${pos(band.current)}%`, background: markerColor }}
        />
      </div>
      <div className="text-[12px] text-white/50 leading-snug">
        최근 8주 분포 중 <b className="text-white/75">{band.pctRank.toFixed(0)}%</b> 위치 —{" "}
        {band.outside ? (
          <b style={{ color: "#ec835a" }}>평소 범위 밖</b>
        ) : (
          "평소 범위 안"
        )}
        <span className="text-white/35">
          {" "}
          · P10 {formatSignedWon(band.p10)} ~ P90 {formatSignedWon(band.p90)}
          {axis === "marketing" && " · 축 = 매출 − 광고비(원가 미축적)"}
        </span>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: `components/admin/GuardrailRow.tsx`**

```tsx
// components/admin/GuardrailRow.tsx — 가드레일 6종. 평소엔 조용하고 임계를 벗어나면 빨강.
//
// 로드맵 v2 §3 에 정의돼 있으나 어드민에 하나도 없던 줄이다. 값이 아니라 **상태**를 읽는 자리라
// 카드가 아니라 한 줄로 늘어놓는다.
import { METRICS } from "@/lib/admin-metrics";
import { formatMetric } from "@/lib/admin/format";
import type { GuardView } from "@/lib/admin/layer1";

export function GuardrailRow({ guards }: { guards: GuardView[] }) {
  return (
    <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-2">
      {guards.map((g) => {
        // 🔴 `MetricDef` 명시 — Task 1 에서 확인된 규칙이다. 빼면 alertBelow/alertAbove 접근이
        //    TS2339 로 죽는다(경보선 없는 지표가 유니온에 섞여 있다).
        const m: MetricDef = METRICS[g.key];
        const threshold =
          m.alertBelow !== undefined ? `< ${m.alertBelow}` : m.alertAbove !== undefined ? `> ${m.alertAbove}` : "";
        return (
          <div
            key={g.key}
            className={`rounded-lg border px-3 py-2 ${
              g.alerting ? "border-[#d03b3b] bg-[#d03b3b]/15" : "border-white/10 bg-white/[0.03]"
            }`}
            title={m.definition}
          >
            <div className="text-[11px] text-white/55">{g.label}</div>
            <div className={`text-base font-bold ${g.alerting ? "text-[#ec835a]" : "text-white/85"}`}>
              {!g.show ? (
                <span className="text-[12px] font-normal text-white/40">{g.note}</span>
              ) : g.value === null ? (
                <span className="text-white/40">—</span>
              ) : (
                formatMetric(g.value, m.unit)
              )}
            </div>
            <div className="text-[10px] text-white/30">경보 {threshold}</div>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 4: `app/admin/page.tsx` 에 1층을 얹는다**

`loadStats()` 아래에 **1층 전용 로더**를 추가한다(기존 로더는 건드리지 않는다):

```ts
import { Metric } from "@/components/admin/Metric";
import { ContributionHero } from "@/components/admin/ContributionHero";
import { BandGauge } from "@/components/admin/BandGauge";
import { GuardrailRow } from "@/components/admin/GuardrailRow";
import { rolling7, computeBand, costCoverage, pickBandAxis, dailyValues, type DailyPnl } from "@/lib/admin/band";
import { computeUnit, computeGuardrails, type GuardRow } from "@/lib/admin/layer1";
import { daysAgoKstIso } from "@/lib/admin-time";

// 밴드는 8주(56개) 롤링 값이 필요하고, 롤링 한 개가 7일을 먹는다 → 62일치 일별 행.
const BAND_DAYS = 62;

async function loadLayer1() {
  const supa = getServiceSupabase();
  const p_exclude = adminExclusionArray();
  const since = daysAgoKstIso(BAND_DAYS - 1);
  const win7 = daysAgoKstIso(6); // 오늘 포함 7일

  const [pnlRes, unitRes, guardRes] = await Promise.all([
    supa.rpc("admin_layer1_pnl", { p_since: since, p_exclude }),
    supa.rpc("admin_layer1_unit", { p_since: win7, p_until: null, p_exclude }),
    supa.rpc("admin_layer1_guard", { p_since: win7, p_until: null, p_exclude }),
  ]);

  // 🔴 NUMERIC 은 PostgREST 를 지나며 **문자열**로 온다 — BIGINT 와 같은 함정이다.
  //    Number() 를 빼면 cost 가 문자열 연결("0" + "12")로 더해져 조용히 틀린다.
  const days: DailyPnl[] = ((pnlRes.data ?? []) as Record<string, string>[]).map((r) => ({
    bucket: r.bucket,
    revenueWon: Number(r.revenue_won),
    adSpendWon: Number(r.ad_spend_won),
    apiCostWon: Number(r.api_cost_won),
    costRows: Number(r.cost_rows),
  }));

  const last7 = days.slice(-7);
  const coverage = costCoverage(last7);
  const axis = pickBandAxis(days);
  const rolling = rolling7(dailyValues(days, axis));
  const band = computeBand(rolling);

  const unitRow = ((unitRes.data ?? []) as Record<string, string>[])[0];
  const unit = computeUnit({
    signups: Number(unitRow?.signups ?? 0),
    payers: Number(unitRow?.payers ?? 0),
    revenueWon: Number(unitRow?.revenue_won ?? 0),
    adSpendWon: Number(unitRow?.ad_spend_won ?? 0),
  });

  const guards = computeGuardrails(
    ((guardRes.data ?? []) as Record<string, string>[]).map(
      (r): GuardRow => ({ metric: r.metric, num: Number(r.num), den: Number(r.den) })
    )
  );

  return {
    sum7: {
      revenueWon: last7.reduce((s, d) => s + d.revenueWon, 0),
      adSpendWon: last7.reduce((s, d) => s + d.adSpendWon, 0),
      apiCostWon: last7.reduce((s, d) => s + d.apiCostWon, 0),
    },
    coverage,
    axis,
    rolling,
    band,
    unit,
    signups: Number(unitRow?.signups ?? 0),
    guards,
    failed: { pnl: Boolean(pnlRes.error), unit: Boolean(unitRes.error), guard: Boolean(guardRes.error) },
  };
}
```

컴포넌트 본체 맨 위(`<h1>대시보드</h1>` 바로 아래)에 1층 섹션을 넣는다:

```tsx
export default async function AdminDashboard() {
  const [s, L1] = await Promise.all([loadStats(), loadLayer1()]);
  // …(기존 starCard 정의는 그대로)…
  return (
    <div className="space-y-8">
      <h1 className="text-xl font-bold">대시보드</h1>

      <section>
        {L1.failed.pnl && <LoadFailed className="mb-3" block="손익(admin_layer1_pnl)" />}
        {!L1.failed.pnl && (
          <>
            <ContributionHero
              revenueWon={L1.sum7.revenueWon}
              adSpendWon={L1.sum7.adSpendWon}
              apiCostWon={L1.sum7.apiCostWon}
              coverage={L1.coverage}
            />
            {L1.band ? (
              <BandGauge band={L1.band} rolling={L1.rolling} axis={L1.axis} />
            ) : (
              <div className="text-[12px] text-white/40 mt-3">
                밴드는 8주치 롤링 값이 모여야 그린다 (현재 {L1.rolling.length}개).
              </div>
            )}
          </>
        )}
      </section>

      <section>
        <h2 className="text-sm text-white/60 mb-3">
          단가 <span className="text-white/35">(최근 7일 가입 코호트 귀속)</span>
        </h2>
        {L1.failed.unit ? (
          <LoadFailed block="단가(admin_layer1_unit)" />
        ) : (
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
            <Metric metricKey="cac_won" value={L1.unit.cacWon} n={L1.signups} />
            <Metric
              metricKey="rev_per_signup"
              value={L1.unit.revPerSignup}
              n={L1.signups}
              sub="= 결제율 × ARPPU"
            />
            <Metric metricKey="pay_rate" value={L1.unit.payRate} n={L1.signups} />
            <Metric metricKey="arppu_won" value={L1.unit.arppuWon} n={L1.signups} />
          </div>
        )}
      </section>

      <section>
        <h2 className="text-sm text-white/60 mb-3">가드레일</h2>
        {L1.failed.guard ? <LoadFailed block="가드레일(admin_layer1_guard)" /> : <GuardrailRow guards={L1.guards} />}
      </section>

      {/* …기존 섹션들(오늘·전체·별 소모·연애 상담·처리 대기)은 이 아래에 그대로 둔다… */}
```

- [ ] **Step 5: 게이트 + 실렌더 검증**

Run: `npx tsc --noEmit`
Expected: 오류 0

Run: `npm run build`
Expected: exit 0

로컬 실렌더는 임시 `ADMIN_USER_IDS` + HMAC 쿠키 주입으로 확인한다([[admin-local-verify-trick]] — 검증 후 **원복 필수**). dev 서버를 띄우고 `/admin` 을 열어 다음을 눈으로 확인한다:
- 기여 히어로가 음수면 주황, 양수면 초록
- API 원가 칸이 `—` 이고 "축적 N/62일" 캡션이 뜬다(dev 는 플랜 A 적용 후라 일부 날만 덮인다)
- 가드레일 6칸이 전부 있고, 임계를 벗어난 칸만 빨강
- 기존 섹션(별 소모·연애 상담·처리 대기)이 그대로 살아 있다

- [ ] **Step 6: 커밋**

```bash
git add components/admin/ContributionHero.tsx components/admin/BandGauge.tsx components/admin/GuardrailRow.tsx app/admin/page.tsx
git commit -F- <<'MSG'
feat(admin): 1층 — 기여 히어로 + 8주 밴드 + 단가 분해 + 가드레일 6종 (플랜B 단계3)

대시보드가 처음으로 "매출 − 광고비 − API원가"를 한 줄에 놓는다.
기존 섹션은 그대로 뒀다 — 2층으로 옮기는 건 다음 태스크다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 7: 1층 매출의 질 + 흐름 줄

**Files:**
- Create: `supabase/migrations/20260921012000_admin_layer1_quality_flow.sql`
- Modify: `lib/admin-metrics.ts` (신규 키 4개)
- Modify: `app/admin/page.tsx`

> 스펙 §3 의 1층은 네 줄이다 — **매출의 질 · 흐름 · 단가 · 가드레일**. Task 6 이 뒤의 두 줄을 만들었고 이 태스크가 앞의 두 줄을 만든다.

- [ ] **Step 1: 레지스트리에 신규 키 4개를 추가한다**

`lib/admin-metrics.ts` 의 `subscription_won` 아래에 넣는다:

```ts
  new_payment_won: {
    key: "new_payment_won",
    label: "신규 결제",
    definition: "창 안 완료 결제 중 **그 사람의 첫 결제**인 건의 금액 합.",
    unit: "won",
    source: "admin_layer1_quality",
    minSample: MIN_SAMPLE.NONE,
  },
  repeat_payment_won: {
    key: "repeat_payment_won",
    label: "재결제",
    definition: "창 안 완료 결제 중 그 사람의 첫 결제가 아닌 건의 금액 합.",
    unit: "won",
    source: "admin_layer1_quality",
    minSample: MIN_SAMPLE.NONE,
    caveat: "2026-09-20 전 기간 실측 비중 30.8% — 결제자 200명 중 재결제자 27명이 매출의 1/3을 만든다.",
  },
  subscriber_net: {
    key: "subscriber_net",
    label: "구독자 순증",
    definition: "창 안 신규 구독 수 − 창 안 만료 수.",
    unit: "count",
    source: "admin_layer1_quality",
    minSample: MIN_SAMPLE.NONE,
    caveat: "5일+ 방문이 0.7%인 위에서 30일 구독이 성립하는지를 리텐션과 같은 줄에서 읽을 것.",
  },
  readings_count: {
    key: "readings_count",
    label: "리딩",
    definition: "창 안에 생성된 readings 행 수(어드민 제외).",
    unit: "count",
    source: "admin_layer1_flow",
    minSample: MIN_SAMPLE.NONE,
  },
```

`lib/admin-metrics.test.ts` 의 키 개수 계약이 있으면 갱신한다(없으면 추가하지 않는다).

- [ ] **Step 2: 마이그레이션을 쓴다**

```sql
-- admin_layer1_quality / admin_layer1_flow — 1층의 "매출의 질" 과 "흐름" 줄.

-- ── 매출의 질 ──────────────────────────────────────────────────────────────
-- 🔴 구독 매출은 원화 결제가 아니라 **별 소모**다(byeolmaru_subscriptions.stars_spent).
--    무료별(웰컴 보너스 등)로 산 구독의 매출 기여는 0 이므로 유료별만 환산한다.
--    환산 단가는 **실효 단가** = 완료 결제의 SUM(amount_won)/SUM(stars_given) — 패키지 믹스를
--    그대로 반영한다. 고정 상수를 쓰면 가격 개편 때마다 과거가 소급 변조된다.
-- ⚠️ free-first 귀속: 한 사람의 별 소모는 무료별을 먼저 태운 것으로 본다
--    (admin_star_spend_breakdown 과 같은 규칙). 여기서는 구독 소모만 보므로 그 사람의
--    누적 무료별 잔량을 구독 시점 기준으로 비교한다.
CREATE OR REPLACE FUNCTION admin_layer1_quality(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (
  new_payment_won BIGINT,
  repeat_payment_won BIGINT,
  subscription_won BIGINT,
  subscription_stars BIGINT,
  subs_started BIGINT,
  subs_expired BIGINT
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH win_pay AS (
    SELECT p.id, p.user_id, p.amount_won,
           -- 그 사람의 첫 완료 결제인가. 창 밖의 과거 결제도 봐야 하므로 전 기간 MIN 과 비교한다.
           (p.created_at = (SELECT MIN(q.created_at) FROM payments q
                            WHERE q.user_id = p.user_id AND q.status = 'completed')) AS is_first
    FROM payments p
    WHERE p.status = 'completed'
      AND p.created_at >= p_since
      AND (p_until IS NULL OR p.created_at < p_until)
      AND (p.user_id IS NULL OR p.user_id <> ALL(p_exclude))
  ), rate AS (
    -- 별 1개의 실효 원화 단가. 분모 0 방어.
    SELECT COALESCE(SUM(amount_won)::NUMERIC / NULLIF(SUM(stars_given), 0), 0) AS won_per_star
    FROM payments WHERE status = 'completed'
  ), sub AS (
    SELECT s.user_id, s.stars_spent, s.started_at, s.expires_at
    FROM byeolmaru_subscriptions s
    WHERE s.user_id <> ALL(p_exclude)
  ), sub_win AS (
    SELECT * FROM sub
    WHERE started_at >= p_since AND (p_until IS NULL OR started_at < p_until)
  ), sub_paid AS (
    -- 유료별 몫: 그 유저가 구독 시점까지 받은 무료별 총량을 넘어선 소모만 유료로 본다.
    SELECT w.stars_spent,
           GREATEST(
             0,
             w.stars_spent - GREATEST(
               0,
               COALESCE((SELECT SUM(t.amount) FROM star_transactions t
                         WHERE t.user_id = w.user_id AND t.type = 'charge'
                           AND t.payment_id IS NULL AND t.created_at <= w.started_at), 0)
               - COALESCE((SELECT SUM(-t.amount) FROM star_transactions t
                           WHERE t.user_id = w.user_id AND t.type = 'spend'
                             AND t.created_at < w.started_at), 0)
             )
           ) AS paid_stars
    FROM sub_win w
  )
  SELECT
    (SELECT COALESCE(SUM(amount_won), 0) FROM win_pay WHERE is_first)::BIGINT,
    (SELECT COALESCE(SUM(amount_won), 0) FROM win_pay WHERE NOT is_first)::BIGINT,
    (SELECT ROUND(COALESCE(SUM(paid_stars), 0) * (SELECT won_per_star FROM rate)) FROM sub_paid)::BIGINT,
    (SELECT COALESCE(SUM(stars_spent), 0) FROM sub_win)::BIGINT,
    (SELECT COUNT(*) FROM sub_win)::BIGINT,
    (SELECT COUNT(*) FROM sub
     WHERE expires_at >= p_since AND (p_until IS NULL OR expires_at < p_until))::BIGINT;
$$;

REVOKE ALL ON FUNCTION admin_layer1_quality(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer1_quality(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;

-- ── 흐름 ───────────────────────────────────────────────────────────────────
-- 🔴 D7 은 **7일 성숙한 코호트만** 분모다. 성숙 전 가입을 넣으면 분모가 부풀어 값이
--    구조적으로 낮게 나온다(roadmap-kpi-snapshot.sql 과 같은 규율).
-- ⚠️ UV 는 **페이지뷰 귀속**(uv_pageview) 이다 — 세션 시작 귀속(uv_session)과 분모가 다르다.
--    두 정의의 공존은 의도된 것이고, 화면이 어느 쪽인지 반드시 라벨로 말해야 한다.
CREATE OR REPLACE FUNCTION admin_layer1_flow(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (
  signups BIGINT,
  readings BIGINT,
  uv BIGINT,
  d7_return BIGINT,
  d7_eligible BIGINT,
  visit2 BIGINT,
  cohort BIGINT
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH coh AS (
    SELECT u.id, u.created_at
    FROM users u
    WHERE u.created_at >= p_since
      AND (p_until IS NULL OR u.created_at < p_until)
      AND u.id <> ALL(p_exclude)
  ), visits AS (
    SELECT pv.user_id,
           COUNT(DISTINCT (pv.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date) AS days,
           MAX(pv.created_at) AS last_at
    FROM page_views pv
    JOIN coh ON coh.id = pv.user_id
    WHERE NOT COALESCE(pv.is_bot, false)
    GROUP BY 1
  )
  SELECT
    (SELECT COUNT(*) FROM coh)::BIGINT,
    (SELECT COUNT(*) FROM readings r
      WHERE r.created_at >= p_since
        AND (p_until IS NULL OR r.created_at < p_until)
        AND r.user_id <> ALL(p_exclude))::BIGINT,
    (SELECT COUNT(DISTINCT pv.anon_id) FROM page_views pv
      WHERE pv.anon_id IS NOT NULL
        AND NOT COALESCE(pv.is_bot, false)
        AND pv.created_at >= p_since
        AND (p_until IS NULL OR pv.created_at < p_until)
        AND (pv.user_id IS NULL OR pv.user_id <> ALL(p_exclude)))::BIGINT,
    (SELECT COUNT(*) FROM coh c
      JOIN visits v ON v.user_id = c.id
      WHERE c.created_at < now() - INTERVAL '7 days'
        AND v.last_at >= c.created_at + INTERVAL '7 days')::BIGINT,
    (SELECT COUNT(*) FROM coh WHERE created_at < now() - INTERVAL '7 days')::BIGINT,
    (SELECT COUNT(*) FROM visits WHERE days >= 2)::BIGINT,
    (SELECT COUNT(*) FROM coh)::BIGINT;
$$;

REVOKE ALL ON FUNCTION admin_layer1_flow(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer1_flow(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;
```

- [ ] **Step 3: 🔴 두 함수 본문을 **전부** prod 에서 실행한다**

`/tmp/verify-quality-flow.sql` 에 두 본문을 리터럴 치환해 넣는다. 창은 09-20 기준 30일.

Run: `SUPABASE_PAT=... node scripts/run-prod-query.mjs /tmp/verify-quality-flow.sql`

**합격 조건:**
| 값 | 기대 | 출처 |
|---|---|---|
| `new_payment_won + repeat_payment_won` (30일) | ≈ **274,900** | §2-4 — Task 4 의 매출과 같아야 한다 |
| `repeat / (new+repeat)` (전 기간 창) | ≈ **30.8%** | §2-5 |
| `flow.signups` (30일) | **914** | §2-4 |
| `flow.visit2 / flow.cohort` | ≈ **12.1%** | §2-5 |
| `subscription_stars` | dev QA 구독행이 prod 엔 없으므로 **0 이 정상** | — |

🔴 **`new + repeat` 가 Task 4 의 30일 매출과 1원이라도 다르면 멈춘다.** 두 RPC 가 같은 `payments` 를 다르게 필터하고 있다는 뜻이고, 1층 안에서 숫자가 어긋나면 화면 전체의 신뢰가 죽는다.

- [ ] **Step 4: 화면에 두 줄을 추가한다**

`loadLayer1()` 의 `Promise.all` 에 두 RPC 를 더한다:

```ts
    supa.rpc("admin_layer1_quality", { p_since: win7, p_until: null, p_exclude }),
    supa.rpc("admin_layer1_flow", { p_since: win7, p_until: null, p_exclude }),
```

반환 객체에 추가한다:

```ts
  const qRow = ((qualityRes.data ?? []) as Record<string, string>[])[0];
  const fRow = ((flowRes.data ?? []) as Record<string, string>[])[0];
  const quality = {
    newPaymentWon: Number(qRow?.new_payment_won ?? 0),
    repeatPaymentWon: Number(qRow?.repeat_payment_won ?? 0),
    subscriptionWon: Number(qRow?.subscription_won ?? 0),
    subscriptionStars: Number(qRow?.subscription_stars ?? 0),
    subsNet: Number(qRow?.subs_started ?? 0) - Number(qRow?.subs_expired ?? 0),
  };
  const flow = {
    signups: Number(fRow?.signups ?? 0),
    readings: Number(fRow?.readings ?? 0),
    uv: Number(fRow?.uv ?? 0),
    d7Return: Number(fRow?.d7_return ?? 0),
    d7Eligible: Number(fRow?.d7_eligible ?? 0),
    visit2: Number(fRow?.visit2 ?? 0),
    cohort: Number(fRow?.cohort ?? 0),
  };
```

`failed` 에 `quality`·`flow` 를 추가하고, 단가 줄 **위**에 두 섹션을 넣는다:

```tsx
      <section>
        <h2 className="text-sm text-white/60 mb-3">
          매출의 질 <span className="text-white/35">(최근 7일)</span>
        </h2>
        {L1.failed.quality ? (
          <LoadFailed block="매출의 질(admin_layer1_quality)" />
        ) : (
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
            <Metric metricKey="new_payment_won" value={L1.quality.newPaymentWon} n={0} />
            <Metric metricKey="repeat_payment_won" value={L1.quality.repeatPaymentWon} n={0} />
            <Metric
              metricKey="subscription_won"
              value={L1.quality.subscriptionWon}
              n={0}
              sub={`전체 ${L1.quality.subscriptionStars.toLocaleString("ko-KR")}별 중 유료별만 환산`}
            />
            <Metric metricKey="subscriber_net" value={L1.quality.subsNet} n={0} />
          </div>
        )}
      </section>

      <section>
        <h2 className="text-sm text-white/60 mb-3">
          흐름 <span className="text-white/35">(최근 7일 · UV 는 페이지뷰 귀속)</span>
        </h2>
        {L1.failed.flow ? (
          <LoadFailed block="흐름(admin_layer1_flow)" />
        ) : (
          <div className="grid grid-cols-2 lg:grid-cols-5 gap-3">
            <Metric metricKey="rev_per_signup" value={L1.flow.signups} n={0} />
            <Metric metricKey="readings_count" value={L1.flow.readings} n={0} />
            <Metric metricKey="uv_pageview" value={L1.flow.uv} n={0} />
            <Metric
              metricKey="d7_return"
              value={L1.flow.d7Eligible > 0 ? (L1.flow.d7Return / L1.flow.d7Eligible) * 100 : null}
              n={L1.flow.d7Eligible}
              sub={`성숙 ${L1.flow.d7Eligible}명 기준`}
            />
            <Metric
              metricKey="visit_2d_plus"
              value={L1.flow.cohort > 0 ? (L1.flow.visit2 / L1.flow.cohort) * 100 : null}
              n={L1.flow.cohort}
            />
          </div>
        )}
      </section>
```

🔴 **흐름 첫 칸의 `metricKey` 를 고친다** — 위 코드의 `rev_per_signup` 은 자리표시가 아니라 **버그**다. 신규 가입 수를 그리는 칸이므로 레지스트리에 `signups_count` 키를 추가하고 그걸 쓴다:

```ts
  signups_count: {
    key: "signups_count",
    label: "신규 가입",
    definition: "창 안에 생성된 users 행 수(어드민 제외).",
    unit: "count",
    source: "admin_layer1_flow",
    minSample: MIN_SAMPLE.NONE,
  },
```

그리고 화면은 `<Metric metricKey="signups_count" value={L1.flow.signups} n={0} />`.

- [ ] **Step 5: 게이트**

Run: `npx tsc --noEmit` · `node --import tsx --test "lib/**/*.test.ts"` · `npm run build`
Expected: 전부 통과

- [ ] **Step 6: 커밋**

```bash
git add supabase/migrations/20260921012000_admin_layer1_quality_flow.sql lib/admin-metrics.ts lib/admin-metrics.test.ts app/admin/page.tsx
git commit -F- <<'MSG'
feat(admin): 1층 매출의 질 + 흐름 줄 (플랜B 단계3)

신규/재결제를 가르고, 구독은 유료별만 실효 단가로 환산한다.
D7 은 7일 성숙한 코호트만 분모로 쓴다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## 단계 4 — 2층

### Task 8: 2층 셸 + 기존 섹션 이동

**Files:**
- Create: `lib/admin/layer2-types.ts`
- Create: `components/admin/DivergingBar.tsx`
- Create: `components/admin/Drilldown.tsx`
- Create: `app/api/admin/layer2/route.ts`
- Modify: `app/admin/page.tsx`

- [ ] **Step 1: 블록 스키마 (`lib/admin/layer2-types.ts`)**

```ts
// lib/admin/layer2-types.ts — 2층 드릴다운의 공통 반환 스키마. 순수(타입만).
//
// 🔴 왜 공통 스키마인가 — 2층은 섹션이 8개고 각각 모양이 다르다. 섹션마다 전용 컴포넌트를
//    만들면 8개의 렌더러와 8개의 타입이 생기고, 그 중 하나가 깨져도 다른 7개 화면에서 안 보인다.
//    블록 3종(bars·table·link)으로 좁히면 렌더러가 하나다.
import type { MetricUnit } from "@/lib/admin-metrics";

/** 발산 가로 막대 — +/− 를 위치로도 전달한다(색만으로 부호를 전하지 않는다, 스펙 §8). */
export interface BarsBlock {
  kind: "bars";
  title: string;
  unit: MetricUnit;
  items: { label: string; value: number }[];
  note?: string;
}

export interface TableBlock {
  kind: "table";
  title: string;
  columns: string[];
  /** null 은 화면에서 "—". 빈 문자열과 구분된다. */
  rows: (string | number | null)[][];
  note?: string;
}

/** 기존 화면으로 내려가는 링크. 2층은 그 화면들을 **삭제하지 않고 목적지로 삼는다**(스펙 §3). */
export interface LinkBlock {
  kind: "link";
  title: string;
  href: string;
  label: string;
}

export type Layer2Block = BarsBlock | TableBlock | LinkBlock;

/** 드릴다운 섹션 키 — 1층의 어느 값을 펼쳤나. */
export const LAYER2_SECTIONS = [
  "contribution",
  "revenue",
  "subscription",
  "signups",
  "readings",
  "uv",
  "d7",
  "withdrawal",
] as const;

export type Layer2Section = (typeof LAYER2_SECTIONS)[number];

export function isLayer2Section(v: string): v is Layer2Section {
  return (LAYER2_SECTIONS as readonly string[]).includes(v);
}

export interface Layer2Response {
  section: Layer2Section;
  blocks: Layer2Block[];
  /** 조회가 일부라도 실패했나 — 빈 결과를 "데이터 없음"으로 위장하지 않는다. */
  failed?: string[];
}
```

- [ ] **Step 2: `components/admin/DivergingBar.tsx`**

```tsx
// components/admin/DivergingBar.tsx — 발산 가로 막대.
//
// 스펙 §8: +/− 는 **위치로도** 전달한다(0선 기준 좌우). 색만으로 부호를 전하면 CVD 에서 사라진다.
// 데이터 끝단만 4px 라운드 · 막대 사이 2px 간격.
import { formatMetric } from "@/lib/admin/format";
import type { BarsBlock } from "@/lib/admin/layer2-types";

export function DivergingBar({ block }: { block: BarsBlock }) {
  const max = Math.max(1, ...block.items.map((i) => Math.abs(i.value)));
  return (
    <div>
      <div className="text-[13px] text-white/70 mb-2">{block.title}</div>
      <div className="space-y-[2px]">
        {block.items.map((it) => {
          const pct = (Math.abs(it.value) / max) * 50; // 0선이 가운데 → 최대 50%
          const negative = it.value < 0;
          return (
            <div key={it.label} className="flex items-center gap-2 text-[12px]">
              <div className="w-28 shrink-0 truncate text-white/60" title={it.label}>
                {it.label}
              </div>
              <div className="relative h-4 flex-1">
                <div className="absolute inset-y-0 left-1/2 w-px bg-white/20" />
                <div
                  className="absolute inset-y-[2px]"
                  style={{
                    [negative ? "right" : "left"]: "50%",
                    width: `${pct}%`,
                    background: negative ? "#d95926" : "#199e70",
                    borderRadius: negative ? "4px 0 0 4px" : "0 4px 4px 0",
                  }}
                />
              </div>
              <div className="w-24 shrink-0 text-right text-white/80 tabular-nums">
                {formatMetric(it.value, block.unit)}
              </div>
            </div>
          );
        })}
      </div>
      {block.note && <div className="text-[11px] text-white/35 mt-2 leading-snug">{block.note}</div>}
    </div>
  );
}
```

- [ ] **Step 3: `components/admin/Drilldown.tsx`**

```tsx
"use client";

// components/admin/Drilldown.tsx — 1층 값을 클릭해 펼치는 2층.
//
// 🔴 왜 lazy fetch 인가 — 2층 섹션이 8개고 각각 무거운 집계다. 서버에서 미리 다 돌리면
//    /admin 첫 페인트가 8배 느려지고, 실제로 펼치는 건 하루 한두 개다.
// 🔴 실패를 빈 화면으로 위장하지 않는다 — 조회가 죽으면 그렇게 말한다.
import { useState } from "react";
import { DivergingBar } from "./DivergingBar";
import type { Layer2Block, Layer2Response, Layer2Section } from "@/lib/admin/layer2-types";

export function Drilldown({
  section,
  label,
  days,
}: {
  section: Layer2Section;
  label: string;
  days: number;
}) {
  const [open, setOpen] = useState(false);
  const [data, setData] = useState<Layer2Response | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  async function toggle() {
    const next = !open;
    setOpen(next);
    if (!next || data || loading) return;
    setLoading(true);
    setError(null);
    try {
      const res = await fetch(`/api/admin/layer2?section=${section}&days=${days}`, { cache: "no-store" });
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      setData((await res.json()) as Layer2Response);
    } catch (e) {
      setError(e instanceof Error ? e.message : "조회 실패");
    } finally {
      setLoading(false);
    }
  }

  return (
    <div className="rounded-xl border border-white/10 bg-white/[0.03]">
      <button
        type="button"
        onClick={toggle}
        aria-expanded={open}
        className="flex w-full items-center justify-between px-4 py-3 text-left text-[13px] text-white/75 hover:bg-white/5"
      >
        <span>{label}</span>
        <span className="text-white/40">{open ? "▾" : "▸"}</span>
      </button>
      {open && (
        <div className="space-y-5 border-t border-white/10 px-4 py-4">
          {loading && <div className="text-[12px] text-white/40">불러오는 중…</div>}
          {error && <div className="text-[12px] text-[#ec835a]">조회 실패 — {error}</div>}
          {data?.failed?.length ? (
            <div className="text-[12px] text-[#ec835a]">
              일부 조회 실패: {data.failed.join(", ")} — 아래 숫자는 불완전하다.
            </div>
          ) : null}
          {data?.blocks.map((b, i) => (
            <BlockView key={i} block={b} />
          ))}
          {data && data.blocks.length === 0 && !error && (
            <div className="text-[12px] text-white/40">표시할 데이터가 없다.</div>
          )}
        </div>
      )}
    </div>
  );
}

function BlockView({ block }: { block: Layer2Block }) {
  if (block.kind === "bars") return <DivergingBar block={block} />;
  if (block.kind === "link")
    return (
      <div className="text-[13px]">
        <span className="text-white/70">{block.title} </span>
        <a className="text-[#E8C26A] underline underline-offset-2" href={block.href}>
          {block.label} →
        </a>
      </div>
    );
  return (
    <div>
      <div className="text-[13px] text-white/70 mb-2">{block.title}</div>
      <div className="overflow-x-auto">
        <table className="w-full text-[12px]">
          <thead>
            <tr className="text-white/45">
              {block.columns.map((c) => (
                <th key={c} className="px-2 py-1 text-left font-normal whitespace-nowrap">
                  {c}
                </th>
              ))}
            </tr>
          </thead>
          <tbody>
            {block.rows.map((r, i) => (
              <tr key={i} className="border-t border-white/5">
                {r.map((cell, j) => (
                  <td key={j} className="px-2 py-1 text-white/80 tabular-nums whitespace-nowrap">
                    {cell === null ? <span className="text-white/30">—</span> : cell}
                  </td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      {block.note && <div className="text-[11px] text-white/35 mt-2 leading-snug">{block.note}</div>}
    </div>
  );
}
```

- [ ] **Step 4: `app/api/admin/layer2/route.ts` (뼈대 + 기존 섹션 이동분)**

```ts
// app/api/admin/layer2/route.ts — 2층 드릴다운 데이터.
//
// 섹션 하나당 한 번 호출된다(Drilldown 이 펼칠 때만). 집계는 전부 RPC 가 하고 이 라우트는
// 블록으로 옮기기만 한다 — 원본 행을 앱으로 끌어오지 않는다(AGENTS.md).
import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { requireAdmin } from "@/lib/admin-actions";
import { adminExclusionArray } from "@/lib/admin";
import { daysAgoKstIso } from "@/lib/admin-time";
import { FORTUNE_CONFIG } from "@/lib/fortune/types";
import { isLayer2Section, type Layer2Block, type Layer2Response } from "@/lib/admin/layer2-types";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export async function GET(req: NextRequest) {
  const gate = await requireAdmin();
  if (gate instanceof NextResponse) return gate;

  const sectionRaw = req.nextUrl.searchParams.get("section") ?? "";
  if (!isLayer2Section(sectionRaw)) {
    return NextResponse.json({ error: "bad_section" }, { status: 400 });
  }
  const days = Math.min(365, Math.max(1, Number(req.nextUrl.searchParams.get("days") ?? 7)));
  const since = daysAgoKstIso(days - 1);

  const supa = getServiceSupabase();
  const p_exclude = adminExclusionArray();
  const blocks: Layer2Block[] = [];
  const failed: string[] = [];

  if (sectionRaw === "revenue") {
    // 별 소모 5종 — 1층에서 내려온 것(스펙 §3). /admin/analytics 와 **같은 RPC** 를 창만 맞춰 쓴다.
    const spend = await supa.rpc("admin_star_spend_breakdown", {
      p_since: since,
      p_until: null,
      p_exclude,
      p_fortune_types: Object.keys(FORTUNE_CONFIG),
    });
    if (spend.error) failed.push("admin_star_spend_breakdown");
    else {
      type SpendRow = { domain: string; product: string; cnt: string; stars: string; free_stars: string };
      const rows = (spend.data ?? []) as SpendRow[];
      const isRelSkill = (r: SpendRow) => r.domain === "relationship" && r.product.startsWith("스킬:");
      const sum = (pred: (r: SpendRow) => boolean) =>
        rows.filter(pred).reduce((s, r) => s + Number(r.stars), 0);
      blocks.push({
        kind: "bars",
        title: "별 소모 5종",
        unit: "count",
        items: [
          { label: "타로 대화", value: sum((r) => r.domain === "tarot") },
          { label: "운세 리포트", value: sum((r) => r.domain === "fortune") },
          { label: "인챗 업셀", value: sum((r) => r.domain === "upsell") },
          { label: "연애 상담", value: sum((r) => r.domain === "relationship" && !isRelSkill(r)) },
          { label: "연애 스킬", value: sum(isRelSkill) },
        ],
        note: "별 소모는 매출이 아니다 — 무료별이 섞여 있다. 원화 기여는 '기여 ▾'에서 본다.",
      });
      blocks.push({
        kind: "link",
        title: "상품별·코호트별 상세는",
        href: "/admin/analytics",
        label: "애널리틱스",
      });
    }
  }

  if (sectionRaw === "withdrawal") {
    const { count, error } = await supa
      .from("account_withdrawals")
      .select("id", { count: "exact", head: true })
      .gte("withdrawn_at", since);
    if (error) failed.push("account_withdrawals");
    else
      blocks.push({
        kind: "table",
        title: `최근 ${days}일 탈퇴`,
        columns: ["탈퇴 수"],
        rows: [[count ?? 0]],
        note: "🔴 어드민·테스트 계정을 분자에서 뺄 수 없다 — account_withdrawals 는 kakao_id_hash 만 남기고 user_id 를 안 남긴다(탈퇴 = 유저 삭제). 실제보다 소폭 높게 나온다.",
      });
    blocks.push({ kind: "link", title: "이탈 사유는", href: "/admin/survey", label: "이탈 설문" });
  }

  // 나머지 섹션은 Task 9~14 에서 채운다. 빈 배열이면 Drilldown 이 "표시할 데이터가 없다"를 그린다.
  const body: Layer2Response = { section: sectionRaw, blocks, ...(failed.length ? { failed } : {}) };
  return NextResponse.json(body);
}
```

- [ ] **Step 5: 1층 화면에서 기존 섹션을 2층으로 옮긴다**

`app/admin/page.tsx` 에서:
1. **별 소모 섹션**(`<h2>별 소모 …`)과 **탈퇴 Stat**(전체 섹션의 탈퇴 카드), **연애 상담 섹션**을 지운다.
2. 가드레일 줄 아래에 드릴다운 목록을 넣는다:

```tsx
      <section>
        <h2 className="text-sm text-white/60 mb-3">
          왜 <span className="text-white/35">(클릭해서 펼침 · 최근 7일)</span>
        </h2>
        <div className="space-y-2">
          <Drilldown section="contribution" label="기여 — 상품별 기여마진" days={7} />
          <Drilldown section="revenue" label="매출 — 별 소모 · 패키지 · 연애 상담" days={7} />
          <Drilldown section="subscription" label="구독 — 신규·해지·체험 전환" days={7} />
          <Drilldown section="signups" label="신규 가입 — 유입 경로 · 무료 상품 공유" days={7} />
          <Drilldown section="readings" label="리딩 — 종목별 · 완료율 · 결과 열람" days={7} />
          <Drilldown section="uv" label="UV — 라우트별 · 경로 판독기" days={7} />
          <Drilldown section="d7" label="D7 — 코호트 리텐션 곡선" days={7} />
          <Drilldown section="withdrawal" label="탈퇴 — 이탈 설문" days={7} />
        </div>
      </section>
```

3. `import { Drilldown } from "@/components/admin/Drilldown";` 를 추가한다.
4. 🔴 **`loadStats()` 에서 별 소모·연애 상담 조회를 지운다** — 화면에서 사라진 데이터를 계속 조회하면 /admin 이 그만큼 느려진다. 지우는 것: `spendTRes` · `spendYRes` · `apRes` · `skTRes` · `skYRes` 와 그것들만 쓰던 `star` · `rel` · `passBuys` · `toGroups` · `sumBy` · `starOf` · `isRelSkill` · `p_fortune_types`, 그리고 `failed.spend`. `StarSpendGroup` · `FORTUNE_CONFIG` import 도 쓰이지 않으면 지운다.
   ⚠️ **탈퇴 카운트(`tw`·`yw`·`aw`)는 남긴다** — "전체" 섹션의 누적 탈퇴율 표시가 아직 그걸 쓴다.

- [ ] **Step 6: 게이트 + 실렌더**

Run: `npx tsc --noEmit` · `npm run build`
Expected: 오류 0 (특히 **지운 변수의 잔존 참조**가 여기서 잡힌다)

dev 서버에서 `/admin` 을 열어: 드릴다운 8줄이 접혀 있고, `매출 ▾` 을 펼치면 별 소모 5종 막대가 뜨고, `기여 ▾` 은 "표시할 데이터가 없다"를 그린다(아직 미구현 — 정상).

- [ ] **Step 7: 커밋**

```bash
git add lib/admin/layer2-types.ts components/admin/DivergingBar.tsx components/admin/Drilldown.tsx app/api/admin/layer2/route.ts app/admin/page.tsx
git commit -F- <<'MSG'
feat(admin): 2층 드릴다운 셸 + 별 소모·탈퇴 이동 (플랜B 단계4)

블록 3종(bars·table·link) 공통 스키마로 섹션 8개를 한 렌더러가 그린다.
펼칠 때만 fetch 한다 — 8섹션을 미리 다 돌리면 첫 페인트가 죽는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 9: 기여 ▾ — 상품별 기여마진

**Files:**
- Create: `lib/admin/product-map.ts`
- Create: `lib/admin/product-map.test.ts`
- Create: `supabase/migrations/20260921020000_admin_layer2_contribution.sql`
- Modify: `app/api/admin/layer2/route.ts`

> 2층의 핵심이자 이 워크스트림의 존재 이유다 — **어느 상품이 돈을 벌고 어느 상품이 태우나.**
> 매출은 별 소모(유료별만) × 실효 단가로 귀속하고, 원가는 `llm_usage.route` 로 귀속한다.

- [ ] **Step 1: 실패 테스트를 쓴다 (`lib/admin/product-map.test.ts`)**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { labelOfRoute, labelOfSpendDomain, PRODUCT_LABELS } from "./product-map.ts";

// 🔴 실제 prod 에 적재되는 route 문자열이다(lib/claude.ts · lib/sensitive.ts · 각 chat 라우트 확인).
test("labelOfRoute — 실제 route 값이 전부 매핑된다", () => {
  assert.equal(labelOfRoute("/api/consultations/tarot/chat"), "타로");
  assert.equal(labelOfRoute("/api/consultations/saju/chat"), "사주");
  assert.equal(labelOfRoute("/api/relationship/chat"), "연애");
  assert.equal(labelOfRoute("/api/relationship/sim/turn"), "연애");
  assert.equal(labelOfRoute("/api/fortune/report"), "운세");
  assert.equal(labelOfRoute("/api/byeolmaru/pair-narrative"), "별마루");
  assert.equal(labelOfRoute("lib/claude.summarizeOlder"), "공통");
  assert.equal(labelOfRoute("lib/sensitive.detectSensitiveAsync"), "공통");
});

test("labelOfRoute — 모르는 route 는 기타로 (크래시하지 않는다)", () => {
  assert.equal(labelOfRoute("/api/something/new"), "기타");
  assert.equal(labelOfRoute(""), "기타");
});

// upsell 은 타로/사주 대화 **안에서** 일어나 원가가 그 chat route 에 이미 잡힌다.
// 매출만 따로 두면 기여마진이 상품마다 어긋나므로 타로로 합친다(한계는 note 로 표시).
test("labelOfSpendDomain — 별 소모 5종이 전부 매핑되고 upsell 은 타로로 합쳐진다", () => {
  assert.equal(labelOfSpendDomain("tarot"), "타로");
  assert.equal(labelOfSpendDomain("saju"), "사주");
  assert.equal(labelOfSpendDomain("fortune"), "운세");
  assert.equal(labelOfSpendDomain("relationship"), "연애");
  assert.equal(labelOfSpendDomain("upsell"), "타로");
  assert.equal(labelOfSpendDomain("made_up"), "기타");
});

test("PRODUCT_LABELS 가 표시 순서를 고정한다", () => {
  assert.deepEqual([...PRODUCT_LABELS], ["타로", "사주", "운세", "연애", "별마루", "공통", "기타"]);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/admin/product-map.test.ts`
Expected: FAIL — `Cannot find module './product-map.ts'`

- [ ] **Step 3: `lib/admin/product-map.ts`**

```ts
// lib/admin/product-map.ts — 원가(route)와 매출(별 소모 domain)을 같은 상품 라벨로 모은다. 순수.
//
// 🔴 왜 코드에 두나 — 이 매핑이 틀리면 "어느 상품이 적자인가"가 통째로 뒤집힌다. SQL 안의
//    CASE 문은 테스트할 수 없지만 여기는 유닛으로 잠긴다. route 가 늘면 테스트가 먼저 깨진다.

export const PRODUCT_LABELS = ["타로", "사주", "운세", "연애", "별마루", "공통", "기타"] as const;
export type ProductLabel = (typeof PRODUCT_LABELS)[number];

/**
 * llm_usage.route → 상품 라벨.
 * route 는 API 경로 문자열이거나(`/api/...`), 스트리밍을 안 타는 우회 2곳의 모듈 경로다.
 */
export function labelOfRoute(route: string): ProductLabel {
  if (route.startsWith("/api/consultations/tarot")) return "타로";
  if (route.startsWith("/api/consultations/saju")) return "사주";
  if (route.startsWith("/api/relationship")) return "연애";
  if (route.startsWith("/api/fortune")) return "운세";
  if (route.startsWith("/api/byeolmaru")) return "별마루";
  // 공통 경로 — 종목을 가리지 않고 모든 대화에 얹힌다(롤링 요약 · 민감 2차 판정).
  if (route.startsWith("lib/claude.") || route.startsWith("lib/sensitive.")) return "공통";
  return "기타";
}

/** admin_star_spend_breakdown 의 domain → 상품 라벨. */
export function labelOfSpendDomain(domain: string): ProductLabel {
  switch (domain) {
    case "tarot":
      return "타로";
    case "saju":
      return "사주";
    case "fortune":
      return "운세";
    case "relationship":
      return "연애";
    // 인챗 업셀(카드 더 뽑기·대화 연장)은 타로/사주 대화 안에서 일어나 원가가 그 chat route 에
    // 이미 포함된다. 매출만 따로 세우면 타로가 실제보다 적자로, 업셀이 순이익으로 보인다.
    case "upsell":
      return "타로";
    default:
      return "기타";
  }
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/admin/product-map.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 마이그레이션**

```sql
-- admin_layer2_cost_by_route / admin_star_won_rate — 기여 분해의 두 입력.

-- 별 1개의 실효 원화 단가. 패키지 믹스를 그대로 반영한다(고정 상수는 가격 개편 때 과거를 변조한다).
CREATE OR REPLACE FUNCTION admin_star_won_rate()
RETURNS NUMERIC
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT COALESCE(SUM(amount_won)::NUMERIC / NULLIF(SUM(stars_given), 0), 0)
  FROM payments WHERE status = 'completed';
$$;

REVOKE ALL ON FUNCTION admin_star_won_rate() FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_star_won_rate() TO service_role;

-- route 별 API 원가. 라벨 매핑은 앱(lib/admin/product-map.ts)이 한다 — SQL CASE 는 테스트가 안 된다.
-- ⚠️ route IS NULL 은 QA·probe 행이라 뺀다.
CREATE OR REPLACE FUNCTION admin_layer2_cost_by_route(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (route TEXT, cost_won NUMERIC, calls BIGINT, free_user_cost_won NUMERIC)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT l.route,
         COALESCE(SUM(l.cost_won), 0),
         COUNT(*)::BIGINT,
         -- 🔴 "그 호출 시점에 결제 이력이 있었나" — is_free_user 컬럼을 두지 않은 이유가 이것이다
         --    (플랜 A). 나중에 결제해도 과거 행의 기준이 바뀌지 않아야 한다.
         COALESCE(SUM(l.cost_won) FILTER (
           WHERE l.user_id IS NULL OR NOT EXISTS (
             SELECT 1 FROM payments p
             WHERE p.user_id = l.user_id AND p.status = 'completed' AND p.created_at < l.created_at
           )
         ), 0)
  FROM llm_usage l
  WHERE l.created_at >= p_since
    AND (p_until IS NULL OR l.created_at < p_until)
    AND l.route IS NOT NULL
    AND (l.user_id IS NULL OR l.user_id <> ALL(p_exclude))
  GROUP BY l.route
  ORDER BY 2 DESC;
$$;

REVOKE ALL ON FUNCTION admin_layer2_cost_by_route(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer2_cost_by_route(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;
```

- [ ] **Step 6: 🔴 두 함수 본문을 **전부** 검증한다**

`llm_usage` 는 **dev 에만** 있다(플랜 A). 따라서:
- `admin_star_won_rate` 본문 → **prod** 에서 실행. 기대: 전 기간 `₩657,200 / SUM(stars_given)` 로 나오는 양수. 0 이면 `stars_given` 이 전부 0 이라는 뜻이니 멈춘다.
- `admin_layer2_cost_by_route` 본문 → **dev** 에서 실행(prod 엔 테이블이 없다). 기대: 플랜 A 카나리아가 넣은 행 2건이 route 별로 잡히거나, 실트래픽이 있으면 chat route 가 최상위.

Run(prod): `SUPABASE_PAT=... node scripts/run-prod-query.mjs /tmp/verify-rate.sql`

- [ ] **Step 7: 라우트에 `contribution` 섹션을 붙인다**

`app/api/admin/layer2/route.ts` 의 `if (sectionRaw === "revenue")` **위**에 추가한다. import 에 `labelOfRoute`·`labelOfSpendDomain`·`PRODUCT_LABELS`·`type ProductLabel` 를 더한다.

```ts
  if (sectionRaw === "contribution") {
    const [spend, cost, rate] = await Promise.all([
      supa.rpc("admin_star_spend_breakdown", {
        p_since: since,
        p_until: null,
        p_exclude,
        p_fortune_types: Object.keys(FORTUNE_CONFIG),
      }),
      supa.rpc("admin_layer2_cost_by_route", { p_since: since, p_until: null, p_exclude }),
      supa.rpc("admin_star_won_rate"),
    ]);
    if (spend.error) failed.push("admin_star_spend_breakdown");
    if (cost.error) failed.push("admin_layer2_cost_by_route");
    if (rate.error) failed.push("admin_star_won_rate");

    // 🔴 NUMERIC 은 PostgREST 를 지나며 문자열로 온다 — Number() 없이 더하면 문자열 연결이 된다.
    const wonPerStar = Number(rate.data ?? 0);
    const margin = new Map<ProductLabel, { rev: number; cost: number }>();
    const bump = (k: ProductLabel, rev: number, c: number) => {
      const cur = margin.get(k) ?? { rev: 0, cost: 0 };
      margin.set(k, { rev: cur.rev + rev, cost: cur.cost + c });
    };

    type SpendRow = { domain: string; stars: string; free_stars: string };
    for (const r of (spend.data ?? []) as SpendRow[]) {
      // 유료별만 매출이다 — 무료별(웰컴 보너스 등)로 산 소비는 현금이 들어온 적이 없다.
      const paidStars = Number(r.stars) - Number(r.free_stars);
      bump(labelOfSpendDomain(r.domain), paidStars * wonPerStar, 0);
    }
    type CostRow = { route: string; cost_won: string };
    for (const r of (cost.data ?? []) as CostRow[]) {
      bump(labelOfRoute(r.route), 0, Number(r.cost_won));
    }

    const items = PRODUCT_LABELS.map((label) => {
      const m = margin.get(label) ?? { rev: 0, cost: 0 };
      return { label, value: m.rev - m.cost };
    }).filter((i) => i.value !== 0);

    blocks.push({
      kind: "bars",
      title: "상품별 기여마진 (매출 귀속 − API 원가 귀속)",
      unit: "won",
      items,
      note: "매출 귀속 = 유료별 소모 × 실효 단가(₩/별). 무료별 소비는 매출 0이다. 인챗 업셀은 타로 대화 안에서 일어나 타로에 합산된다(사주 업셀도 여기 섞인다). '공통'은 롤링 요약·민감 판정처럼 종목을 가리지 않는 원가라 매출이 없다 — 항상 음수인 게 정상이다.",
    });

    const rows = ((cost.data ?? []) as { route: string; cost_won: string; calls: string; free_user_cost_won: string }[])
      .slice(0, 12)
      .map((r) => [r.route, Math.round(Number(r.cost_won)), Number(r.calls), Math.round(Number(r.free_user_cost_won))]);
    blocks.push({
      kind: "table",
      title: "route 별 API 원가 (상위 12)",
      columns: ["route", "원가(원)", "호출", "미결제자 몫(원)"],
      rows,
      note: "미결제자 몫 = 그 호출 시점에 완료 결제 이력이 없던 사람에게 태운 원가. 2026-08-10 실측에서 무료별 87%가 미결제자에게 갔다.",
    });
  }
```

- [ ] **Step 8: 게이트 + 실렌더**

Run: `npx tsc --noEmit` · `node --import tsx --test "lib/**/*.test.ts"` · `npm run build`
dev `/admin` → `기여 ▾` 을 펼쳐 막대 5~7개와 route 표가 뜨는지 확인.

- [ ] **Step 9: 커밋**

```bash
git add lib/admin/product-map.ts lib/admin/product-map.test.ts supabase/migrations/20260921020000_admin_layer2_contribution.sql app/api/admin/layer2/route.ts
git commit -F- <<'MSG'
feat(admin): 2층 기여 ▾ — 상품별 기여마진 (플랜B 단계4)

매출은 유료별 소모 × 실효 단가로, 원가는 llm_usage.route 로 귀속한다.
route↔domain 매핑을 코드로 옮기고 유닛으로 잠근다 — SQL CASE 는 테스트가 안 된다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 10: 매출 ▾ 보강 + 구독 ▾

**Files:**
- Create: `supabase/migrations/20260921021000_admin_layer2_revenue.sql`
- Modify: `app/api/admin/layer2/route.ts`

- [ ] **Step 1: 마이그레이션**

```sql
-- admin_layer2_revenue_mix / admin_layer2_subscription — 매출·구독 분해.

-- 패키지별 × 신규/재결제. 1층 "매출의 질" 줄이 합계만 보여주고 여기서 쪼갠다.
CREATE OR REPLACE FUNCTION admin_layer2_revenue_mix(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (package_type TEXT, is_first BOOLEAN, cnt BIGINT, won BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  SELECT p.package_type::TEXT,
         (p.created_at = (SELECT MIN(q.created_at) FROM payments q
                          WHERE q.user_id = p.user_id AND q.status = 'completed')) AS is_first,
         COUNT(*)::BIGINT,
         SUM(p.amount_won)::BIGINT
  FROM payments p
  WHERE p.status = 'completed'
    AND p.created_at >= p_since
    AND (p_until IS NULL OR p.created_at < p_until)
    AND (p.user_id IS NULL OR p.user_id <> ALL(p_exclude))
  GROUP BY 1, 2
  ORDER BY 4 DESC;
$$;

REVOKE ALL ON FUNCTION admin_layer2_revenue_mix(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer2_revenue_mix(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;

-- 구독 — 신규·만료·활성 + 구독자의 방문일수.
-- 🔴 5일+ 방문이 0.7% 인 위에서 30일 구독이 성립하는지가 이 서비스의 핵심 질문이다(스펙 §2-5).
--    그래서 구독 수와 **구독자 방문일수**를 같은 블록에서 읽는다.
CREATE OR REPLACE FUNCTION admin_layer2_subscription(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (
  started BIGINT,
  expired BIGINT,
  active_now BIGINT,
  stars_spent BIGINT,
  free_stars BIGINT,
  avg_visit_days NUMERIC
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH sub AS (
    SELECT s.* FROM byeolmaru_subscriptions s WHERE s.user_id <> ALL(p_exclude)
  ), win AS (
    SELECT * FROM sub WHERE started_at >= p_since AND (p_until IS NULL OR started_at < p_until)
  ), vis AS (
    SELECT pv.user_id,
           COUNT(DISTINCT (pv.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date) AS days
    FROM page_views pv
    WHERE pv.user_id IN (SELECT user_id FROM win)
      AND NOT COALESCE(pv.is_bot, false)
    GROUP BY 1
  )
  SELECT (SELECT COUNT(*) FROM win)::BIGINT,
         (SELECT COUNT(*) FROM sub
          WHERE expires_at >= p_since AND (p_until IS NULL OR expires_at < p_until))::BIGINT,
         (SELECT COUNT(*) FROM sub WHERE expires_at > now())::BIGINT,
         (SELECT COALESCE(SUM(stars_spent), 0) FROM win)::BIGINT,
         -- 무료별 몫은 star_transactions 의 구독 소모 행에서 free-first 로 계산한다.
         (SELECT COALESCE(SUM(LEAST(w.stars_spent, GREATEST(0,
            COALESCE((SELECT SUM(t.amount) FROM star_transactions t
                      WHERE t.user_id = w.user_id AND t.type = 'charge'
                        AND t.payment_id IS NULL AND t.created_at <= w.started_at), 0)
            - COALESCE((SELECT SUM(-t.amount) FROM star_transactions t
                        WHERE t.user_id = w.user_id AND t.type = 'spend'
                          AND t.created_at < w.started_at), 0)))), 0) FROM win w)::BIGINT,
         (SELECT ROUND(AVG(days), 1) FROM vis);
$$;

REVOKE ALL ON FUNCTION admin_layer2_subscription(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer2_subscription(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;
```

- [ ] **Step 2: 🔴 두 함수 본문을 **전부** prod 에서 실행한다**

Run: `SUPABASE_PAT=... node scripts/run-prod-query.mjs /tmp/verify-revenue.sql`

**합격 조건:**
- `revenue_mix` 의 `SUM(won)` (전 기간) = **657,200** (§2-5, `completed` 262건)
- `is_first=true` 인 결제자 수 = **200** (§2-5 결제자 200명과 일치해야 한다)
- `subscription` 은 prod 에 구독 행이 없어 **전부 0 이 정상** — 0 이 아니면 dev 데이터가 섞인 것이다

- [ ] **Step 3: 라우트에 두 섹션을 붙인다**

`revenue` 섹션에 블록을 **추가**한다(기존 별 소모 블록 아래):

```ts
    const mix = await supa.rpc("admin_layer2_revenue_mix", { p_since: since, p_until: null, p_exclude });
    if (mix.error) failed.push("admin_layer2_revenue_mix");
    else {
      type MixRow = { package_type: string; is_first: boolean; cnt: string; won: string };
      blocks.push({
        kind: "table",
        title: "패키지별 × 신규/재결제",
        columns: ["패키지", "구분", "건수", "금액(원)"],
        rows: ((mix.data ?? []) as MixRow[]).map((r) => [
          r.package_type,
          r.is_first ? "신규" : "재결제",
          Number(r.cnt),
          Number(r.won),
        ]),
      });
    }
    blocks.push({ kind: "link", title: "결제 원장은", href: "/admin/payments", label: "결제/정산" });
    blocks.push({ kind: "link", title: "연애 상담 지표는", href: "/admin/relationship", label: "연애 상담" });
```

새 섹션을 추가한다:

```ts
  if (sectionRaw === "subscription") {
    const sub = await supa.rpc("admin_layer2_subscription", { p_since: since, p_until: null, p_exclude });
    if (sub.error) failed.push("admin_layer2_subscription");
    else {
      const r = ((sub.data ?? []) as Record<string, string>[])[0];
      const started = Number(r?.started ?? 0);
      const expired = Number(r?.expired ?? 0);
      const stars = Number(r?.stars_spent ?? 0);
      const free = Number(r?.free_stars ?? 0);
      blocks.push({
        kind: "table",
        title: `구독 (최근 ${days}일)`,
        columns: ["신규", "만료", "순증", "현재 활성", "소모 별", "무료별 몫", "구독자 평균 방문일"],
        rows: [[
          started,
          expired,
          started - expired,
          Number(r?.active_now ?? 0),
          stars,
          free,
          r?.avg_visit_days === null || r?.avg_visit_days === undefined ? null : Number(r.avg_visit_days),
        ]],
        note: "🔴 구독 매출은 별 소모다. 무료별 몫은 현금이 들어온 적이 없어 매출 0이다. 5일+ 방문이 0.7%인 서비스에서 30일 구독이 성립하는지는 '구독자 평균 방문일'로 읽는다.",
      });
      blocks.push({ kind: "link", title: "별마루 상세는", href: "/admin/free/byeolmaru", label: "별마루" });
    }
  }
```

- [ ] **Step 4: 게이트 + 커밋**

Run: `npx tsc --noEmit` · `npm run build`

```bash
git add supabase/migrations/20260921021000_admin_layer2_revenue.sql app/api/admin/layer2/route.ts
git commit -F- <<'MSG'
feat(admin): 2층 매출 ▾ 보강 + 구독 ▾ (플랜B 단계4)

패키지별 × 신규/재결제를 쪼개고, 구독은 순증과 구독자 방문일수를
같은 블록에서 읽게 한다 — 30일 구독의 성립 조건이 리텐션이라서다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 11: 신규 가입 ▾ + 무료 상품 공유 퍼널 (§5)

**Files:**
- Create: `supabase/migrations/20260921022000_admin_free_share_funnel.sql`
- Modify: `app/api/admin/layer2/route.ts`

- [ ] **Step 1: 마이그레이션**

```sql
-- admin_layer2_signup_mix / admin_free_share_funnel — 가입의 출처와 무료 상품 공유의 획득 기여.

-- 유입 경로별 가입 + 그 코호트의 결제. 오가닉 = user_acquisition 행이 **없는** 가입이다(스펙 §7).
-- ⚠️ LEFT JOIN 이 필수다 — INNER JOIN 하면 오가닉이 통째로 사라진다.
CREATE OR REPLACE FUNCTION admin_layer2_signup_mix(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (source TEXT, signups BIGINT, payers BIGINT, revenue_won BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH coh AS (
    SELECT u.id, COALESCE(a.utm_source, '(오가닉·유입기록 없음)') AS src
    FROM users u
    LEFT JOIN user_acquisition a ON a.user_id = u.id
    WHERE u.created_at >= p_since
      AND (p_until IS NULL OR u.created_at < p_until)
      AND u.id <> ALL(p_exclude)
  ), pay AS (
    SELECT c.src, c.id, SUM(p.amount_won)::BIGINT AS won
    FROM coh c
    JOIN payments p ON p.user_id = c.id AND p.status = 'completed'
    GROUP BY 1, 2
  )
  SELECT c.src,
         COUNT(*)::BIGINT,
         (SELECT COUNT(*) FROM pay WHERE pay.src = c.src)::BIGINT,
         (SELECT COALESCE(SUM(won), 0) FROM pay WHERE pay.src = c.src)::BIGINT
  FROM coh c
  GROUP BY c.src
  ORDER BY 2 DESC;
$$;

REVOKE ALL ON FUNCTION admin_layer2_signup_mix(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer2_signup_mix(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;

-- 무료 상품 공유 퍼널 — 착지 → 가입 → 결제. 무료 상품 3종을 **같은 정의**로 잰다(스펙 §5).
--
-- 🔴 코호트 판정이 utm 쿠키에 기대지 않는다. `byeolkong_acq` 쿠키는 first-touch 라 30일간
--    덮어쓰지 않아, 광고로 먼저 왔던 사람이 공유링크로 재방문해 가입하면 utm_source='meta' 로
--    기록되고 공유 기여가 은폐된다(§5-3). 그래서 **page_views 착지 → 같은 anon 의 로그인**
--    브리지를 쓴다(MBTI 가 이미 쓰는 기법).
-- 🔴 결제는 **착지 이후 것만** 센다. admin_cohort_payments 가 전 기간을 합해 인과로 읽을 수
--    없었던 결함(§5-1)을 여기서 고친다.
CREATE OR REPLACE FUNCTION admin_free_share_funnel(
  p_product TEXT,
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ
)
RETURNS TABLE (landings BIGINT, signups BIGINT, payers BIGINT, revenue_won BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH land AS (
    -- 그 상품 링크로 착지한 anon 과 첫 접촉 시각.
    SELECT pv.anon_id, MIN(pv.created_at) AS first_touch_at
    FROM page_views pv
    WHERE pv.utm_source = p_product
      AND pv.anon_id IS NOT NULL
      AND NOT COALESCE(pv.is_bot, false)
      AND pv.created_at >= p_since
      AND (p_until IS NULL OR pv.created_at < p_until)
    GROUP BY 1
  ), bridged AS (
    -- 같은 anon 이 나중에 로그인한 user_id. 한 anon 이 여러 계정을 만들 수 있어 DISTINCT 로 접는다.
    SELECT DISTINCT l.anon_id, pv.user_id, l.first_touch_at
    FROM land l
    JOIN page_views pv ON pv.anon_id = l.anon_id
    WHERE pv.user_id IS NOT NULL
      AND pv.created_at >= l.first_touch_at
  ), signed AS (
    SELECT DISTINCT b.user_id, MIN(b.first_touch_at) AS first_touch_at
    FROM bridged b
    GROUP BY b.user_id
  )
  SELECT (SELECT COUNT(*) FROM land)::BIGINT,
         (SELECT COUNT(*) FROM signed)::BIGINT,
         (SELECT COUNT(*) FROM signed s WHERE EXISTS (
            SELECT 1 FROM payments p
            WHERE p.user_id = s.user_id AND p.status = 'completed'
              AND p.created_at > s.first_touch_at))::BIGINT,
         (SELECT COALESCE(SUM(p.amount_won), 0) FROM signed s
          JOIN payments p ON p.user_id = s.user_id AND p.status = 'completed'
          WHERE p.created_at > s.first_touch_at)::BIGINT;
$$;

REVOKE ALL ON FUNCTION admin_free_share_funnel(TEXT, TIMESTAMPTZ, TIMESTAMPTZ) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_free_share_funnel(TEXT, TIMESTAMPTZ, TIMESTAMPTZ) TO service_role;
```

- [ ] **Step 2: 🔴 두 함수 본문을 **전부** prod 에서 실행한다**

**합격 조건:**
- `signup_mix` 의 `SUM(signups)` (30일) = **914** — Task 5 의 `unit.signups` 와 **정확히 같아야 한다**
- `(오가닉·유입기록 없음)` 행의 비율 ≈ **7.5%** (30일 기준, §7)
- `free_share_funnel('byeoljari', …)` 의 `landings` = **1**, `free_share_funnel('saju_mbti', …)` = **1**, 둘 다 `signups = 0` (§5 — 배선은 살아 있고 표본이 0. 이 값이 나오면 함수가 맞게 도는 것이다)
- `free_share_funnel('byeolmaru_tarot', …)` = **0** — 별마루 공유 링크에 utm 이 아직 없다(별마루 세션 소유). 0 이 정상이다.

- [ ] **Step 3: 라우트에 `signups` 섹션을 붙인다**

```ts
  if (sectionRaw === "signups") {
    const FREE_PRODUCTS = [
      { utm: "byeoljari", label: "별 인연 별자리" },
      { utm: "saju_mbti", label: "사주 MBTI" },
      { utm: "byeolmaru_tarot", label: "별마루 오늘의 타로" },
      { utm: "byeolmaru_saju", label: "별마루 오늘의 사주" },
    ];
    const [mix, ...funnels] = await Promise.all([
      supa.rpc("admin_layer2_signup_mix", { p_since: since, p_until: null, p_exclude }),
      ...FREE_PRODUCTS.map((p) =>
        supa.rpc("admin_free_share_funnel", { p_product: p.utm, p_since: since, p_until: null })
      ),
    ]);
    if (mix.error) failed.push("admin_layer2_signup_mix");
    else {
      type MixRow = { source: string; signups: string; payers: string; revenue_won: string };
      blocks.push({
        kind: "table",
        title: "유입 경로별 가입 · 결제",
        columns: ["경로", "가입", "결제자", "매출(원)", "가입당(원)"],
        rows: ((mix.data ?? []) as MixRow[]).map((r) => {
          const n = Number(r.signups);
          return [r.source, n, Number(r.payers), Number(r.revenue_won), n > 0 ? Math.round(Number(r.revenue_won) / n) : null];
        }),
        note: "오가닉은 user_acquisition 행이 없는 가입이다 — 쿠키 차단·acq 쿠키 30일 만료도 섞이므로 오가닉의 **상한**으로 읽을 것.",
      });
    }
    const funnelRows = FREE_PRODUCTS.map((p, i) => {
      const res = funnels[i];
      if (res.error) {
        failed.push(`admin_free_share_funnel(${p.utm})`);
        return [p.label, null, null, null, null];
      }
      const r = ((res.data ?? []) as Record<string, string>[])[0];
      return [
        p.label,
        Number(r?.landings ?? 0),
        Number(r?.signups ?? 0),
        Number(r?.payers ?? 0),
        Number(r?.revenue_won ?? 0),
      ];
    });
    blocks.push({
      kind: "table",
      title: "무료 상품 공유 퍼널 (착지 → 가입 → 결제)",
      columns: ["상품", "착지", "가입", "결제자", "매출(원)"],
      rows: funnelRows,
      note: "🔴 무료 상품을 '전환율'로 재지 않는다 — 2026-08-24 실측에서 무료→결제는 깔때기가 아니라 역인과였다. 이 표는 **획득(바이럴)** 을 잰다. 결제는 착지 이후 것만 세고, 코호트는 utm 쿠키가 아니라 착지 anon→로그인 브리지로 잡는다(first-touch 은폐 회피). 별마루 2종은 공유 링크 utm 이 붙기 전까지 구조적으로 0이다.",
    });
    blocks.push({ kind: "link", title: "소재별 지출은", href: "/admin/ads", label: "광고 지출" });
  }
```

- [ ] **Step 4: 게이트 + 커밋**

Run: `npx tsc --noEmit` · `npm run build`

```bash
git add supabase/migrations/20260921022000_admin_free_share_funnel.sql app/api/admin/layer2/route.ts
git commit -F- <<'MSG'
feat(admin): 2층 신규 가입 ▾ + 무료 상품 공유 퍼널 (플랜B 단계4·§5)

무료 상품 3종을 같은 정의로 잰다. 코호트는 utm 쿠키가 아니라
착지 anon→로그인 브리지로 잡아 first-touch 은폐를 피하고,
결제는 착지 이후 것만 세어 인과로 읽을 수 있게 한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 12: 리딩 ▾

**Files:**
- Create: `supabase/migrations/20260921023000_admin_layer2_readings.sql`
- Modify: `app/api/admin/layer2/route.ts`

- [ ] **Step 1: 마이그레이션**

```sql
-- admin_layer2_readings — 종목별 리딩 · 완료율 · 결과 열람 · 고민 분류.
--
-- ⚠️ "완료"는 별콩이 발화에 [END] 마커가 있는 것이다(messages.content). turn_close 컬럼이
--    아니다 — 그건 발화가 어떤 상태에서 생성됐는지를 남기는 **추가** 계측이고, 종료 판정의
--    정본은 여전히 [END] 다(3층 roadmap KPI 와 같은 정의).
-- ⚠️ LIKE '%[END]%' 의 대괄호는 Postgres LIKE 에서 특별한 뜻이 없다(SIMILAR TO·정규식과 다르다).
--    `_` 와 `%` 만 와일드카드이고 여기엔 없다.
CREATE OR REPLACE FUNCTION admin_layer2_readings(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[]
)
RETURNS TABLE (
  consultation_type TEXT,
  cnt BIGINT,
  paid_cnt BIGINT,
  ended_cnt BIGINT,
  viewed_cnt BIGINT,
  avg_user_turns NUMERIC
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH win AS (
    SELECT r.id, r.consultation_type, r.stars_spent, r.result_viewed_at
    FROM readings r
    WHERE r.created_at >= p_since
      AND (p_until IS NULL OR r.created_at < p_until)
      AND r.user_id <> ALL(p_exclude)
  ), msg AS (
    SELECT m.reading_id,
           COUNT(*) FILTER (WHERE m.role = 'user') AS user_turns,
           bool_or(m.role = 'assistant' AND m.content LIKE '%[END]%') AS ended
    FROM messages m
    JOIN win ON win.id = m.reading_id
    GROUP BY 1
  )
  SELECT COALESCE(w.consultation_type, '(없음)')::TEXT,
         COUNT(*)::BIGINT,
         COUNT(*) FILTER (WHERE w.stars_spent > 0)::BIGINT,
         COUNT(*) FILTER (WHERE COALESCE(m.ended, false))::BIGINT,
         COUNT(w.result_viewed_at)::BIGINT,
         ROUND(AVG(m.user_turns), 2)
  FROM win w
  LEFT JOIN msg m ON m.reading_id = w.id
  GROUP BY 1
  ORDER BY 2 DESC;
$$;

REVOKE ALL ON FUNCTION admin_layer2_readings(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer2_readings(TIMESTAMPTZ, TIMESTAMPTZ, UUID[]) TO service_role;
```

- [ ] **Step 2: 🔴 prod 에서 본문 실행**

**합격 조건:** 전 기간 창으로 돌렸을 때 종목별 건수가 §2-3 과 맞는다 — `tarot` **2,171** · `saju` **295** · `relationship` **140** · `relationship_sim` **16**.
어긋나면 `consultation_type` 필터나 어드민 제외가 틀린 것이다.

- [ ] **Step 3: 라우트에 `readings` 섹션**

```ts
  if (sectionRaw === "readings") {
    const rd = await supa.rpc("admin_layer2_readings", { p_since: since, p_until: null, p_exclude });
    if (rd.error) failed.push("admin_layer2_readings");
    else {
      type RdRow = {
        consultation_type: string;
        cnt: string;
        paid_cnt: string;
        ended_cnt: string;
        viewed_cnt: string;
        avg_user_turns: string | null;
      };
      const pct = (a: number, b: number) => (b > 0 ? Math.round((a / b) * 1000) / 10 : null);
      blocks.push({
        kind: "table",
        title: `종목별 리딩 (최근 ${days}일)`,
        columns: ["종목", "리딩", "유료", "완료율(%)", "결과 열람(%)", "평균 유저 턴"],
        rows: ((rd.data ?? []) as RdRow[]).map((r) => {
          const n = Number(r.cnt);
          return [
            r.consultation_type,
            n,
            Number(r.paid_cnt),
            pct(Number(r.ended_cnt), n),
            pct(Number(r.viewed_cnt), n),
            r.avg_user_turns === null ? null : Number(r.avg_user_turns),
          ];
        }),
        note: "완료 = 별콩이 발화에 [END] 마커가 있다. 결과 열람은 여기선 **리딩 기준**이다 — 1층 가드레일의 결과 열람은 **코호트 기준**이라 값이 다르다(둘 다 정본이고 분모가 다르다). 2026-09-12 실측: 종료 원인은 마무리 버튼 59% · 자발 13% · 무언 이탈 27%.",
      });
      blocks.push({ kind: "link", title: "개별 리딩은", href: "/admin/readings", label: "리딩/상담" });
    }
  }
```

- [ ] **Step 4: 게이트 + 커밋**

```bash
git add supabase/migrations/20260921023000_admin_layer2_readings.sql app/api/admin/layer2/route.ts
git commit -F- <<'MSG'
feat(admin): 2층 리딩 ▾ — 종목별 완료율·결과열람·유저턴 (플랜B 단계4)

완료 판정은 [END] 마커다(turn_close 가 아니다 — 3층과 같은 정의).
결과 열람이 1층(코호트 기준)과 분모가 다르다는 사실을 표에 적는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 13: UV ▾ + 경로 판독기 (§6)

**Files:**
- Create: `supabase/migrations/20260921024000_admin_path_reader.sql`
- Modify: `app/api/admin/layer2/route.ts`

> 🔴 **스펙 §6 에서 정정 1건** — 스펙은 "세션 정의는 기존 `SESSION_GAP`(30분)을 재사용한다"고 했으나
> **anon 단위 첫 N스텝**으로 간다. §2-6 의 로그인 누수 실측(도달 1,531 · 성공 67.6%)이 anon 단위로
> 산출됐고, 세션 단위로 바꾸면 **그 숫자를 재현할 수 없다.** 판독기의 첫 임무는 이미 발견된 누수를
> 화면에 고정하는 것이므로 실측과 같은 정의를 쓴다. (세션 단위 분해가 필요해지면 그때 따로 붙인다.)

- [ ] **Step 1: 마이그레이션**

```sql
-- admin_path_sequences / admin_path_exits — 경로 판독기(스펙 §6).
--
-- 🔴 새 로깅이 없다. page_views 37,010행이 이미 모든 라우트를 anon 단위·시간순으로 들고 있고,
--    부족했던 건 **읽는 화면**이었다. 그래서 §2-6 의 로그인 누수가 2개월 넘게 안 보였다.
-- ⚠️ /admin* 은 제외한다 — 운영자 동선이 유저 경로를 오염시킨다.
-- ⚠️ 반환 행수는 경로 카디널리티에 비례한다 → p_limit 으로 자르고, 호출부가 **눈에 보이는**
--    절단 경고를 그린다(플래그만 두고 안 보여주면 의미가 없다, AGENTS.md).

CREATE OR REPLACE FUNCTION admin_path_sequences(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_steps INT,
  p_limit INT
)
RETURNS TABLE (seq TEXT, people BIGINT, logged_in BIGINT, paid BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH pv AS (
    SELECT pv.anon_id, pv.path, pv.user_id,
           ROW_NUMBER() OVER (PARTITION BY pv.anon_id ORDER BY pv.created_at, pv.id) AS rn
    FROM page_views pv
    WHERE pv.anon_id IS NOT NULL
      AND NOT COALESCE(pv.is_bot, false)
      AND pv.path NOT LIKE '/admin%'
      AND pv.created_at >= p_since
      AND (p_until IS NULL OR pv.created_at < p_until)
  ), head AS (
    SELECT anon_id, string_agg(path, ' → ' ORDER BY rn) AS seq
    FROM pv WHERE rn <= p_steps
    GROUP BY anon_id
  ), logged_anons AS (
    -- 로그인 도달 = 그 anon 의 어떤 page_view 에든 user_id 가 붙었다(첫 N스텝 밖이어도 센다).
    SELECT DISTINCT pv.anon_id FROM pv WHERE pv.user_id IS NOT NULL
  ), paid_anons AS (
    -- 🔴 상관 서브쿼리로 쓰면 anon 마다 page_views ⋈ payments 를 다시 돈다(37,010행 × 경로 수).
    --    한 번에 집합으로 만들어 IN 으로 붙인다.
    SELECT DISTINCT q.anon_id
    FROM page_views q
    JOIN payments p ON p.user_id = q.user_id AND p.status = 'completed'
    WHERE q.anon_id IS NOT NULL AND q.user_id IS NOT NULL
  ), flag AS (
    SELECT h.anon_id, h.seq,
           (h.anon_id IN (SELECT anon_id FROM logged_anons)) AS did_login,
           (h.anon_id IN (SELECT anon_id FROM paid_anons)) AS did_pay
    FROM head h
  )
  SELECT flag.seq,
         COUNT(*)::BIGINT,
         COUNT(*) FILTER (WHERE did_login)::BIGINT,
         COUNT(*) FILTER (WHERE did_pay)::BIGINT
  FROM flag
  GROUP BY flag.seq
  ORDER BY 2 DESC
  LIMIT p_limit;
$$;

REVOKE ALL ON FUNCTION admin_path_sequences(TIMESTAMPTZ, TIMESTAMPTZ, INT, INT) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_path_sequences(TIMESTAMPTZ, TIMESTAMPTZ, INT, INT) TO service_role;

-- 이탈 지점 — 그 anon 의 **마지막** path 분포.
CREATE OR REPLACE FUNCTION admin_path_exits(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_limit INT
)
RETURNS TABLE (path TEXT, people BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH last_pv AS (
    SELECT DISTINCT ON (pv.anon_id) pv.anon_id, pv.path
    FROM page_views pv
    WHERE pv.anon_id IS NOT NULL
      AND NOT COALESCE(pv.is_bot, false)
      AND pv.path NOT LIKE '/admin%'
      AND pv.created_at >= p_since
      AND (p_until IS NULL OR pv.created_at < p_until)
    ORDER BY pv.anon_id, pv.created_at DESC, pv.id DESC
  )
  SELECT last_pv.path, COUNT(*)::BIGINT
  FROM last_pv
  GROUP BY last_pv.path
  ORDER BY 2 DESC
  LIMIT p_limit;
$$;

REVOKE ALL ON FUNCTION admin_path_exits(TIMESTAMPTZ, TIMESTAMPTZ, INT) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_path_exits(TIMESTAMPTZ, TIMESTAMPTZ, INT) TO service_role;
```

- [ ] **Step 2: 🔴 두 함수 본문을 **전부** prod 에서 실행한다**

**합격 조건 — §2-6 재현:** 창을 30일(09-20 기준)로 두고 `p_steps=4` 로 돌렸을 때 상위 경로가 이것들과 **같은 순서·같은 규모**로 나와야 한다:

| 경로 | 인원 | 로그인 |
|---|---|---|
| `/ → /login → /concern → /tarot` | 653 | 97% |
| `/ → /login → / → /login` | 252 | 50% |
| `/ → /login` | 88 | 0% |
| `/ → /login → /` | 72 | 1% |

🔴 **재현이 안 되면 멈춘다.** 이 판독기의 첫 임무는 이미 발견된 누수를 화면에 고정하는 것이라, 숫자가 다르면 다른 것을 재고 있는 것이다. 흔한 원인 3가지: ①`is_bot` 필터 누락 ②`/admin%` 제외 누락 ③`rn` 의 ORDER BY 에 타이브레이커(`pv.id`)가 없어 같은 초에 찍힌 두 행의 순서가 실행마다 바뀜.

- [ ] **Step 3: 라우트에 `uv` 섹션**

```ts
  if (sectionRaw === "uv") {
    const PATH_LIMIT = 15;
    const [routes, seqs, exits] = await Promise.all([
      supa.rpc("admin_traffic_routes", { p_since: since, p_exclude }),
      supa.rpc("admin_path_sequences", { p_since: since, p_until: null, p_steps: 4, p_limit: PATH_LIMIT }),
      supa.rpc("admin_path_exits", { p_since: since, p_until: null, p_limit: PATH_LIMIT }),
    ]);
    if (routes.error) failed.push("admin_traffic_routes");
    if (seqs.error) failed.push("admin_path_sequences");
    if (exits.error) failed.push("admin_path_exits");

    if (!seqs.error) {
      type SeqRow = { seq: string; people: string; logged_in: string; paid: string };
      const rows = (seqs.data ?? []) as SeqRow[];
      blocks.push({
        kind: "table",
        title: "첫 4스텝 경로 (상위 15)",
        columns: ["경로", "인원", "로그인(%)", "결제(%)"],
        rows: rows.map((r) => {
          const n = Number(r.people);
          return [
            r.seq,
            n,
            n > 0 ? Math.round((Number(r.logged_in) / n) * 1000) / 10 : null,
            n > 0 ? Math.round((Number(r.paid) / n) * 1000) / 10 : null,
          ];
        }),
        note:
          (rows.length >= PATH_LIMIT ? `⚠️ 상위 ${PATH_LIMIT}개만 표시 — 잘린 경로가 있다. ` : "") +
          "anon 단위 첫 4스텝이다(세션 단위가 아니다). 로그인·결제는 첫 4스텝 밖의 도달도 센다. 2026-09-20 실측: /login 도달 1,531명 중 496명이 로그인 없이 이탈(성공률 67.6%).",
      });
    }
    if (!exits.error) {
      type ExitRow = { path: string; people: string };
      blocks.push({
        kind: "bars",
        title: "이탈 지점 (마지막으로 본 화면)",
        unit: "count",
        items: ((exits.data ?? []) as ExitRow[]).map((r) => ({ label: r.path, value: Number(r.people) })),
      });
    }
    blocks.push({ kind: "link", title: "라우트별·유입별 상세는", href: "/admin/traffic", label: "트래픽 UV/PV" });
  }
```

- [ ] **Step 4: 게이트 + 커밋**

```bash
git add supabase/migrations/20260921024000_admin_path_reader.sql app/api/admin/layer2/route.ts
git commit -F- <<'MSG'
feat(admin): 2층 UV ▾ + 경로 판독기 (플랜B 단계4·§6)

새 로깅 없이 page_views 2개월치를 읽는다. anon 단위 첫 4스텝으로
§2-6 의 로그인 누수 실측(1,531 도달 · 67.6% 성공)을 화면에 고정한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 14: D7 ▾ — 코호트 리텐션 곡선 + 무료 상품 리프트

**Files:**
- Create: `supabase/migrations/20260921025000_admin_layer2_retention.sql`
- Modify: `app/api/admin/layer2/route.ts`

- [ ] **Step 1: 마이그레이션**

```sql
-- admin_layer2_retention / admin_free_product_lift — 주 코호트 리텐션과 무료 상품 리프트.
--
-- 🔴 무료 상품의 KPI 는 전환율이 아니라 ①리텐션 리프트 ②경유 가입이다(스펙 §5).
--    2026-08-24 실측에서 무료→결제는 깔때기가 아니라 역인과로 판명됐다.

-- 주 단위 가입 코호트 × D1/D3/D7/D14/D28 재방문.
CREATE OR REPLACE FUNCTION admin_layer2_retention(
  p_weeks INT,
  p_exclude UUID[]
)
RETURNS TABLE (cohort_week DATE, users BIGINT, d1 BIGINT, d3 BIGINT, d7 BIGINT, d14 BIGINT, d28 BIGINT)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH coh AS (
    SELECT u.id, u.created_at,
           date_trunc('week', (u.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours'))::date AS wk
    FROM users u
    WHERE u.created_at >= now() - (p_weeks || ' weeks')::interval
      AND u.id <> ALL(p_exclude)
  ), vis AS (
    SELECT pv.user_id, MAX(pv.created_at) AS last_at
    FROM page_views pv
    JOIN coh ON coh.id = pv.user_id
    WHERE NOT COALESCE(pv.is_bot, false)
    GROUP BY 1
  )
  SELECT c.wk,
         COUNT(*)::BIGINT,
         -- 🔴 각 Dn 은 **성숙한 코호트만** 분자에 센다. 분모는 전체라 미성숙 주는 값이 낮게
         --    보이는데, 그게 맞다 — 호출부가 성숙 여부를 캡션으로 말한다.
         COUNT(*) FILTER (WHERE v.last_at >= c.created_at + INTERVAL '1 day')::BIGINT,
         COUNT(*) FILTER (WHERE v.last_at >= c.created_at + INTERVAL '3 days')::BIGINT,
         COUNT(*) FILTER (WHERE v.last_at >= c.created_at + INTERVAL '7 days')::BIGINT,
         COUNT(*) FILTER (WHERE v.last_at >= c.created_at + INTERVAL '14 days')::BIGINT,
         COUNT(*) FILTER (WHERE v.last_at >= c.created_at + INTERVAL '28 days')::BIGINT
  FROM coh c
  LEFT JOIN vis v ON v.user_id = c.id
  GROUP BY c.wk
  ORDER BY c.wk DESC;
$$;

REVOKE ALL ON FUNCTION admin_layer2_retention(INT, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_layer2_retention(INT, UUID[]) TO service_role;

-- 무료 상품 접촉자 vs 비접촉자의 방문일수. 접촉 시각은 상품마다 테이블이 다르다.
-- ⚠️ 인과가 아니다 — 무료 상품을 쓰는 사람이 원래 더 오래 남는 사람일 수 있다(선택 편향).
--    "리프트"라는 말이 인과를 암시하므로 호출부가 반드시 이 한계를 적는다.
CREATE OR REPLACE FUNCTION admin_free_product_lift(
  p_product TEXT,
  p_exclude UUID[]
)
RETURNS TABLE (touched BIGINT, touched_avg_days NUMERIC, untouched BIGINT, untouched_avg_days NUMERIC)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH touched_users AS (
    SELECT DISTINCT m.user_id
    FROM star_map_members m
    WHERE p_product = 'byeoljari' AND m.user_id IS NOT NULL
    UNION
    SELECT DISTINCT e.user_id
    FROM ui_events e
    WHERE p_product = 'saju_mbti' AND e.event = 'saju_mbti_completed' AND e.user_id IS NOT NULL
    UNION
    SELECT DISTINCT s.user_id
    FROM byeolmaru_subscriptions s
    WHERE p_product = 'byeolmaru'
  ), vis AS (
    SELECT pv.user_id,
           COUNT(DISTINCT (pv.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date) AS days
    FROM page_views pv
    WHERE pv.user_id IS NOT NULL
      AND NOT COALESCE(pv.is_bot, false)
      AND pv.user_id <> ALL(p_exclude)
    GROUP BY 1
  )
  SELECT
    (SELECT COUNT(*) FROM vis WHERE user_id IN (SELECT user_id FROM touched_users))::BIGINT,
    (SELECT ROUND(AVG(days), 2) FROM vis WHERE user_id IN (SELECT user_id FROM touched_users)),
    (SELECT COUNT(*) FROM vis WHERE user_id NOT IN (SELECT user_id FROM touched_users))::BIGINT,
    (SELECT ROUND(AVG(days), 2) FROM vis WHERE user_id NOT IN (SELECT user_id FROM touched_users));
$$;

REVOKE ALL ON FUNCTION admin_free_product_lift(TEXT, UUID[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_free_product_lift(TEXT, UUID[]) TO service_role;
```

- [ ] **Step 2: 🔴 두 함수 본문을 **전부** prod 에서 실행한다**

**합격 조건:**
- `retention` 의 최근 주 `users` 합이 그 기간 가입 수와 맞는다
- `free_product_lift('byeoljari', …)` 의 `touched` 가 **12 이하**다(§2-3 `star_map_members` 12행, 전원 익명이면 0 도 정상). 🔴 **이 값이 30 미만이면 소표본 게이트에 걸려야 한다** — 화면이 숫자를 그리면 안 된다.
- `NOT IN` 에 NULL 이 섞이면 전체가 빈 결과가 된다(3값 논리) — `touched_users` 의 `user_id IS NOT NULL` 필터가 각 갈래에 있는지 확인한다. `byeolmaru_subscriptions.user_id` 는 NOT NULL 이라 안전하다.

- [ ] **Step 3: 라우트에 `d7` 섹션**

```ts
  if (sectionRaw === "d7") {
    const FREE_LIFT = [
      { key: "byeoljari", label: "별 인연 별자리" },
      { key: "saju_mbti", label: "사주 MBTI" },
      { key: "byeolmaru", label: "별마루 구독" },
    ];
    const [ret, ...lifts] = await Promise.all([
      supa.rpc("admin_layer2_retention", { p_weeks: 8, p_exclude }),
      ...FREE_LIFT.map((p) => supa.rpc("admin_free_product_lift", { p_product: p.key, p_exclude })),
    ]);
    if (ret.error) failed.push("admin_layer2_retention");
    else {
      type RetRow = { cohort_week: string; users: string; d1: string; d3: string; d7: string; d14: string; d28: string };
      const pct = (a: string, b: number) => (b > 0 ? Math.round((Number(a) / b) * 1000) / 10 : null);
      blocks.push({
        kind: "table",
        title: "주 코호트 리텐션 (최근 8주)",
        columns: ["가입 주", "인원", "D1(%)", "D3(%)", "D7(%)", "D14(%)", "D28(%)"],
        rows: ((ret.data ?? []) as RetRow[]).map((r) => {
          const n = Number(r.users);
          return [r.cohort_week, n, pct(r.d1, n), pct(r.d3, n), pct(r.d7, n), pct(r.d14, n), pct(r.d28, n)];
        }),
        note: "🔴 최근 주는 아직 성숙하지 않았다 — D28 칸은 가입 후 28일이 지난 코호트에서만 의미가 있다. 위쪽(최근) 행의 긴 Dn 은 구조적으로 낮게 나온다.",
      });
    }
    const liftRows = FREE_LIFT.map((p, i) => {
      const res = lifts[i];
      if (res.error) {
        failed.push(`admin_free_product_lift(${p.key})`);
        return [p.label, null, null, null, null];
      }
      const r = ((res.data ?? []) as Record<string, string>[])[0];
      const touched = Number(r?.touched ?? 0);
      // 소표본 게이트 — 비율/평균은 n≥30 이 아니면 숫자를 그리지 않는다(스펙 §7).
      const gated = touched < 30;
      return [
        p.label,
        touched,
        gated ? null : Number(r?.touched_avg_days ?? 0),
        Number(r?.untouched ?? 0),
        Number(r?.untouched_avg_days ?? 0),
      ];
    });
    blocks.push({
      kind: "table",
      title: "무료 상품 접촉 × 방문일수",
      columns: ["상품", "접촉자", "접촉자 평균 방문일", "비접촉자", "비접촉자 평균 방문일"],
      rows: liftRows,
      note: "🔴 인과가 아니다 — 무료 상품을 쓰는 사람이 원래 더 오래 남는 사람일 수 있다(선택 편향). 접촉자 n<30 이면 평균을 그리지 않는다. 2026-08-24 실측에서 무료→결제는 역인과였고, 유의미했던 건 리텐션 쪽의 약한 긍정(13.6% vs 2.4%)뿐이다.",
    });
  }
```

- [ ] **Step 4: 게이트 + 실렌더**

Run: `npx tsc --noEmit` · `node --import tsx --test "lib/**/*.test.ts"` · `npm run build`
dev `/admin` 에서 드릴다운 **8개를 전부 펼쳐** 각각 블록이 뜨거나 "표시할 데이터가 없다"가 뜨는지 확인한다(조용한 빈 화면이 없어야 한다).

- [ ] **Step 5: 커밋**

```bash
git add supabase/migrations/20260921025000_admin_layer2_retention.sql app/api/admin/layer2/route.ts
git commit -F- <<'MSG'
feat(admin): 2층 D7 ▾ — 주 코호트 리텐션 + 무료 상품 리프트 (플랜B 단계4)

무료 상품은 전환율이 아니라 리텐션 리프트로 잰다(역인과 실측 반영).
접촉자 n<30 이면 평균을 그리지 않는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## 단계 5 — 3층 (판정)

> 🔴 **스펙 §3 의 "`messages.turn_close` prod 선적용 전제"는 사실이 아니다**(플랜 상단 정정 1).
> 무언 이탈은 `[END]` 마커로, 유저 턴은 `role='user'` 카운트로 계산한다 — 둘 다 `turn_close` 를
> 읽지 않는다. 3층은 지금 prod 데이터로 그대로 만들고 검증한다.

### Task 15: 판정 KPI RPC 이식

**Files:**
- Create: `supabase/migrations/20260921030000_admin_roadmap_kpi.sql`

- [ ] **Step 1: 마이그레이션**

`scripts/roadmap-kpi-snapshot.sql` 을 RPC 로 옮긴다. 창·공휴일·제외를 인자로 빼고, **성숙 규율(D7 7일 대기)·고래 캡 ₩20k 는 함수 안에 유지**한다.

```sql
-- admin_roadmap_kpi — 흑자 전환 로드맵 v2 §3 의 판정 지표. 주 1회 읽는 3층.
--
-- 원본 = scripts/roadmap-kpi-snapshot.sql (사람이 손으로 돌리던 것). 창·공휴일만 인자로 빼고
-- 나머지 산식은 **한 글자도 바꾸지 않는다** — 판정 지표는 배포 전에 고정했고, 화면으로 옮기면서
-- 정의가 바뀌면 베이스라인과 비교가 불가능해진다.
--
-- ⚠️ 공휴일 가입 제외 · 고래 캡 ₩20,000 · D7 7일 성숙은 로드맵 §3 의 "읽는 법"이다.
-- ⚠️ LEAST 는 NULL 을 무시하므로 비결제자를 **먼저 0 으로** 만들어야 한다
--    (안 하면 비결제자가 20000 으로 잡혀 고래 캡 지표가 통째로 뒤집힌다).
CREATE OR REPLACE FUNCTION admin_roadmap_kpi(
  p_since TIMESTAMPTZ,
  p_until TIMESTAMPTZ,
  p_exclude UUID[],
  p_holidays DATE[]
)
RETURNS TABLE (
  users BIGINT,
  first_reading_pct NUMERIC,
  premium_first_share_pct NUMERIC,
  two_first_share_pct NUMERIC,
  two_first_payer_pct NUMERIC,
  two_first_rev_ps NUMERIC,
  payer_pct NUMERIC,
  arppu NUMERIC,
  rev_ps NUMERIC,
  rev_ps_whalecap20k NUMERIC,
  repeat_payers BIGINT,
  silent_exit_pct NUMERIC,
  viewed_pct NUMERIC,
  avg_user_turns NUMERIC,
  shop_visit_pct NUMERIC,
  shop_to_pay_pct NUMERIC,
  inchat_sheet_opens BIGINT,
  organic_pct NUMERIC,
  visit2_pct NUMERIC,
  d7_eligible BIGINT,
  d7_return_pct NUMERIC
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public AS $$
  WITH u AS (
    SELECT usr.id, (usr.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours') AS ts
    FROM users usr
    WHERE usr.created_at >= p_since
      AND usr.created_at < p_until
      AND usr.id <> ALL(p_exclude)
      AND (usr.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date <> ALL(p_holidays)
  ), fr AS (
    SELECT x.* FROM (
      SELECT r.*, ROW_NUMBER() OVER (PARTITION BY r.user_id ORDER BY r.created_at) AS rn
      FROM readings r
    ) x WHERE x.rn = 1
  ), m AS (
    SELECT msg.reading_id,
           COUNT(*) FILTER (WHERE msg.role = 'user') AS ut,
           bool_or(msg.role = 'assistant' AND msg.content LIKE '%[END]%') AS ended
    FROM messages msg GROUP BY 1
  ), pay AS (
    SELECT p.user_id, SUM(p.amount_won) AS rev, COUNT(*) AS n
    FROM payments p WHERE p.status = 'completed' GROUP BY 1
  ), sh AS (
    SELECT pv.user_id, MIN(pv.created_at) AS t
    FROM page_views pv
    WHERE pv.path LIKE '/shop%' AND pv.user_id IS NOT NULL
    GROUP BY 1
  ), ic AS (
    SELECT COUNT(*) AS n FROM ui_events e
    WHERE e.event = 'recharge_sheet_opened'
      AND e.meta->>'source' = 'inchat'
      AND e.created_at >= p_since AND e.created_at < p_until
  ), v AS (
    SELECT pv.user_id,
           COUNT(DISTINCT (pv.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours')::date) AS days,
           MAX(pv.created_at AT TIME ZONE 'UTC' + INTERVAL '9 hours') AS last_ts
    FROM page_views pv
    WHERE pv.user_id IS NOT NULL AND NOT COALESCE(pv.is_bot, false)
    GROUP BY 1
  ), g AS (
    SELECT u.id, u.ts,
      CASE WHEN fr.spread_type IN ('relationship_5','deep_feelings_5','reunion_5','reunion_deep_7',
                                   'potential_7','new_love_5','readiness_6','healing_6','checkin_6') THEN 'premium'
           WHEN fr.spread_type = 'three_card' THEN 'three'
           WHEN fr.spread_type = 'two_card' THEN 'two'
           WHEN fr.spread_type = 'one_card' THEN 'one'
           WHEN fr.id IS NULL THEN 'none' ELSE 'other' END AS grp,
      fr.id AS rid, fr.stars_spent, fr.result_viewed_at, m.ut, m.ended,
      pay.rev, pay.n AS pn, sh.t AS sh_t, v.days, v.last_ts,
      (SELECT COUNT(*) FROM user_acquisition a WHERE a.user_id = u.id) AS acq_rows
    FROM u
    LEFT JOIN fr ON fr.user_id = u.id
    LEFT JOIN m ON m.reading_id = fr.id
    LEFT JOIN pay ON pay.user_id = u.id
    LEFT JOIN sh ON sh.user_id = u.id
    LEFT JOIN v ON v.user_id = u.id
  )
  SELECT
    COUNT(*)::BIGINT,
    ROUND(100.0 * COUNT(rid) / NULLIF(COUNT(*), 0), 1),
    ROUND(100.0 * COUNT(*) FILTER (WHERE grp = 'premium') / NULLIF(COUNT(*), 0), 1),
    ROUND(100.0 * COUNT(*) FILTER (WHERE grp = 'two') / NULLIF(COUNT(*), 0), 1),
    ROUND(100.0 * COUNT(rev) FILTER (WHERE grp = 'two') / NULLIF(COUNT(*) FILTER (WHERE grp = 'two'), 0), 1),
    ROUND(COALESCE(SUM(rev) FILTER (WHERE grp = 'two'), 0) / NULLIF(COUNT(*) FILTER (WHERE grp = 'two'), 0)),
    ROUND(100.0 * COUNT(rev) / NULLIF(COUNT(*), 0), 1),
    ROUND(COALESCE(SUM(rev), 0) / NULLIF(COUNT(rev), 0)),
    ROUND(COALESCE(SUM(rev), 0) / NULLIF(COUNT(*), 0)),
    ROUND(SUM(LEAST(COALESCE(rev, 0), 20000)) / NULLIF(COUNT(*), 0)),
    COUNT(*) FILTER (WHERE pn > 1)::BIGINT,
    ROUND(100.0 * COUNT(*) FILTER (WHERE stars_spent > 0 AND NOT COALESCE(ended, false))
          / NULLIF(COUNT(*) FILTER (WHERE stars_spent > 0), 0), 1),
    ROUND(100.0 * COUNT(result_viewed_at) FILTER (WHERE stars_spent > 0)
          / NULLIF(COUNT(*) FILTER (WHERE stars_spent > 0), 0), 1),
    ROUND(AVG(ut) FILTER (WHERE stars_spent > 0), 2),
    ROUND(100.0 * COUNT(sh_t) / NULLIF(COUNT(*), 0), 1),
    ROUND(100.0 * COUNT(rev) FILTER (WHERE sh_t IS NOT NULL) / NULLIF(COUNT(sh_t), 0), 1),
    (SELECT n FROM ic)::BIGINT,
    -- 오가닉 = user_acquisition 행이 **없는** 가입(스펙 §7 정본).
    ROUND(100.0 * COUNT(*) FILTER (WHERE acq_rows = 0) / NULLIF(COUNT(*), 0), 1),
    ROUND(100.0 * COUNT(*) FILTER (WHERE days >= 2) / NULLIF(COUNT(*), 0), 1),
    COUNT(*) FILTER (WHERE ts < (now() AT TIME ZONE 'UTC' + INTERVAL '9 hours') - INTERVAL '7 days')::BIGINT,
    ROUND(100.0 * COUNT(*) FILTER (WHERE last_ts >= ts + INTERVAL '7 days')
          / NULLIF(COUNT(*) FILTER (WHERE ts < (now() AT TIME ZONE 'UTC' + INTERVAL '9 hours') - INTERVAL '7 days'), 0), 1)
  FROM g;
$$;

REVOKE ALL ON FUNCTION admin_roadmap_kpi(TIMESTAMPTZ, TIMESTAMPTZ, UUID[], DATE[]) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION admin_roadmap_kpi(TIMESTAMPTZ, TIMESTAMPTZ, UUID[], DATE[]) TO service_role;
```

- [ ] **Step 2: 🔴 원본 스크립트와 **행 단위 diff** 로 검증한다**

이게 이 태스크의 핵심 검증이다. 같은 창으로 둘을 돌려 **모든 컬럼이 일치**해야 한다.

1. `scripts/roadmap-kpi-snapshot.sql` 을 그대로 실행:
   `SUPABASE_PAT=... node scripts/run-prod-query.mjs scripts/roadmap-kpi-snapshot.sql`
2. 위 RPC 본문을 인라인한 `/tmp/verify-roadmap.sql` 을 **같은 창**(`s='2026-09-01'`, `e='2026-09-21'`)·**같은 공휴일**·**같은 제외 6명**으로 실행
3. 두 결과를 컬럼별로 대조

**합격 조건:** `organic_pct` 를 제외한 **모든 컬럼이 완전히 같다**(`organic_pct` 는 RPC 에만 있는 신규 컬럼).
베이스라인 참조값(로드맵 §1, W5 09-01~18 485명): `rev_ps_whalecap20k` **252** · `payer_pct` **7.4** · `arppu` **3392** · `premium_first_share_pct` **3.9** · `two_first_share_pct` **64** · `two_first_rev_ps` **84** · `silent_exit_pct` **27.7** · `viewed_pct` **65** · `avg_user_turns` **3.79** · `shop_visit_pct` **35** · `shop_to_pay_pct` **7.3** · `visit2_pct` **9.6**.
창이 다르면 값도 다르므로, **일치 판정은 1↔2 사이에서** 하고 위 표는 자릿수 감각 확인용으로만 쓴다.

🔴 **한 컬럼이라도 어긋나면 멈춘다.** 3층의 유일한 가치는 "배포 전에 고정한 정의 그대로"라는 데 있다.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260921030000_admin_roadmap_kpi.sql
git commit -F- <<'MSG'
feat(admin): 3층 판정 KPI RPC — roadmap 스냅샷 SQL 이식 (플랜B 단계5)

손으로 돌리던 scripts/roadmap-kpi-snapshot.sql 을 그대로 옮긴다.
창·공휴일만 인자로 빼고 산식은 바꾸지 않았다 — 배포 전 고정한 정의라서다.
오가닉 비율만 신규 컬럼으로 추가(스펙 §7 정본).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 16: 3층 화면 — 베이스라인·목표선·판정

**Files:**
- Create: `lib/admin/roadmap.ts`
- Create: `lib/admin/roadmap.test.ts`
- Create: `app/admin/roadmap/page.tsx`

- [ ] **Step 1: 실패 테스트 (`lib/admin/roadmap.test.ts`)**

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { ROADMAP_KPIS, judge, HOLIDAYS_2026, kpiByColumn } from "./roadmap.ts";

test("Primary 는 고래 캡 지표이고 베이스라인·목표선이 로드맵 §3 과 같다", () => {
  const primary = kpiByColumn("rev_ps_whalecap20k")!;
  assert.equal(primary.baseline, 252);
  assert.equal(primary.target, 394);
  assert.equal(primary.floor, 200);
  assert.equal(primary.direction, "up");
});

test("judge — up 방향: 목표 이상이면 good, 바닥 이하면 bad, 사이는 watch", () => {
  const k = kpiByColumn("rev_ps_whalecap20k")!;
  assert.equal(judge(k, 400), "good");
  assert.equal(judge(k, 300), "watch");
  assert.equal(judge(k, 200), "bad"); // 경계 포함
  assert.equal(judge(k, null), "unknown");
});

test("judge — down 방향(무언 이탈): 목표 이하면 good", () => {
  const k = kpiByColumn("silent_exit_pct")!;
  assert.equal(k.direction, "down");
  assert.equal(judge(k, 21), "good");
  assert.equal(judge(k, 27.7), "watch"); // 베이스라인 그대로 = 변화 없음
});

test("judge — 가드레일은 floor 만 있다: 바닥 위면 good", () => {
  const k = kpiByColumn("first_reading_pct")!;
  assert.equal(k.target, undefined);
  assert.equal(judge(k, 85), "good");
  assert.equal(judge(k, 79), "bad");
});

test("judge — 목표선도 바닥도 없는 참고 지표는 ref", () => {
  const k = kpiByColumn("arppu")!;
  assert.equal(judge(k, 3392), "ref");
});

test("공휴일 목록에 추석·개천절·한글날이 있다", () => {
  assert.ok(HOLIDAYS_2026.includes("2026-09-24"));
  assert.ok(HOLIDAYS_2026.includes("2026-10-03"));
  assert.ok(HOLIDAYS_2026.includes("2026-10-09"));
});

test("모든 KPI 가 고유한 컬럼명을 갖는다", () => {
  const cols = ROADMAP_KPIS.map((k) => k.column);
  assert.equal(new Set(cols).size, cols.length);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/admin/roadmap.test.ts`
Expected: FAIL — `Cannot find module './roadmap.ts'`

- [ ] **Step 3: `lib/admin/roadmap.ts`**

```ts
// lib/admin/roadmap.ts — 3층 판정 규칙. 순수(DB·React import 0).
//
// 🔴 베이스라인·목표선은 `plans/2026-09-19-흑자전환-로드맵-v2-별마루배포포함.md` §1·§3 이 정본이다.
//    "판정 지표는 배포 전에 고정하고 배포 후 바꾸지 않는다"가 그 문서의 원칙이라, 이 파일의
//    숫자를 고치는 것은 **판정 규칙을 바꾸는 일**이다. 근거 없이 건드리지 말 것.
import type { MetricUnit } from "@/lib/admin-metrics";

export interface KpiDef {
  /** admin_roadmap_kpi 의 반환 컬럼명. */
  column: string;
  label: string;
  unit: MetricUnit;
  /** 로드맵 §1 베이스라인. */
  baseline: number;
  /** 성공선. 없으면 참고 지표이거나 가드레일. */
  target?: number;
  /** 이 값에 닿으면 되돌리기 검토(가드레일은 경보선). */
  floor?: number;
  /** 높을수록 좋은가 낮을수록 좋은가. */
  direction: "up" | "down";
  /** 사람이 읽는 판정 규칙 — 화면에 그대로 뜬다. */
  rule: string;
  group: "primary" | "welcome15" | "turnclose" | "byeolmaru" | "guardrail" | "ref";
}

// 🔴 선언부 타입 주석이다 — `as const satisfies` 를 쓰면 색인·filter 결과가 원소별 좁은
//    유니온이 되어 `k.target`·`k.floor` 접근이 TS2339 로 죽는다(Task 1 에서 METRICS 로 같은
//    함정을 확인했다). 여기선 리터럴 타입이 필요 없고, 이 자리에 타입을 달아야 각 원소가
//    KpiDef 를 만족하는지도 검사된다(`as` 캐스트는 그 검사를 약화시킨다).
export const ROADMAP_KPIS: readonly KpiDef[] = [
  {
    column: "rev_ps_whalecap20k",
    label: "가입당 매출 (고래캡 ₩20k)",
    unit: "won",
    baseline: 252,
    target: 394,
    floor: 200,
    direction: "up",
    rule: "4주 창에서 ≥₩394 연속 2창 → 증액 스위치 ON. ≤₩200 → 해당 Phase 되돌리기 검토.",
    group: "primary",
  },
  {
    column: "two_first_rev_ps",
    label: "two_card 첫선택 층 가입당",
    unit: "won",
    baseline: 84,
    target: 84,
    floor: 60,
    direction: "up",
    rule: "3주 창 <₩60 → 되돌리기 또는 구독가 상향 재논의. ≥₩84 유지. 그 사이는 4주까지 연장.",
    group: "welcome15",
  },
  {
    column: "two_first_payer_pct",
    label: "two_card 층 결제율",
    unit: "percent",
    baseline: 2.6,
    direction: "up",
    rule: "가입당의 성분. 7월 W3 은 4.7% 였다.",
    group: "welcome15",
  },
  {
    column: "two_first_share_pct",
    label: "two_card 첫선택 비중",
    unit: "percent",
    baseline: 64,
    direction: "up",
    rule: "웰컴 재화가 스프레드 선택을 지배한다 — 웰컴 15 의 직접 효과가 여기 나온다.",
    group: "welcome15",
  },
  {
    column: "silent_exit_pct",
    label: "무언 이탈 (유료 첫 리딩)",
    unit: "percent",
    baseline: 27.7,
    target: 22,
    direction: "down",
    rule: "<22% 이고 유저 턴 ≥4.0 이면 성공 → Phase 3 페르소나 A/B 불필요.",
    group: "turnclose",
  },
  {
    column: "avg_user_turns",
    label: "평균 유저 턴",
    unit: "ratio",
    baseline: 3.79,
    target: 4.0,
    direction: "up",
    rule: "무언 이탈과 **함께** 읽는다. 둘 다 좋아져야 턴마무리 성공이다.",
    group: "turnclose",
  },
  {
    column: "d7_return_pct",
    label: "D7 재방문",
    unit: "percent",
    baseline: 6.2,
    target: 10,
    direction: "up",
    rule: "단월 판정 금지. 12주 누적(12-14)에 구독 ≥ 가입의 0.5% 또는 D7 ≥10% 면 별마루 유지.",
    group: "byeolmaru",
  },
  {
    column: "visit2_pct",
    label: "2일+ 방문",
    unit: "percent",
    baseline: 9.6,
    direction: "up",
    rule: "30일 구독의 선행 조건. 5일+ 는 0.7% 다.",
    group: "byeolmaru",
  },
  {
    column: "first_reading_pct",
    label: "첫 리딩 비율",
    unit: "percent",
    baseline: 87,
    floor: 80,
    direction: "up",
    rule: "<80% 면 온보딩 회귀 즉시 조사.",
    group: "guardrail",
  },
  {
    column: "viewed_pct",
    label: "결과 열람",
    unit: "percent",
    baseline: 65,
    floor: 55,
    direction: "up",
    rule: "<55% 면 결과 화면 회귀.",
    group: "guardrail",
  },
  {
    column: "shop_to_pay_pct",
    label: "shop→결제",
    unit: "percent",
    baseline: 7.3,
    target: 12,
    direction: "up",
    rule: "Phase 2 판정선. W3 은 15.7% 였다.",
    group: "ref",
  },
  {
    column: "premium_first_share_pct",
    label: "프리미엄 첫선택 비중",
    unit: "percent",
    baseline: 3.9,
    target: 5,
    direction: "up",
    rule: "3개월 불변이던 값. Phase 2 판정선 5%+.",
    group: "ref",
  },
  {
    column: "payer_pct",
    label: "결제율",
    unit: "percent",
    baseline: 7.4,
    direction: "up",
    rule: "가입당 매출의 성분. 09-19 분석이 하락의 본체로 지목한 쪽.",
    group: "ref",
  },
  {
    column: "arppu",
    label: "ARPPU",
    unit: "won",
    baseline: 3392,
    direction: "up",
    rule: "가입당 매출의 다른 성분.",
    group: "ref",
  },
  {
    column: "organic_pct",
    label: "오가닉 비율",
    unit: "percent",
    baseline: 7.5,
    direction: "up",
    rule: "유입 기록이 없는 가입 비율 — 오가닉의 **상한**으로 읽는다.",
    group: "ref",
  },
  {
    column: "inchat_sheet_opens",
    label: "인챗 충전 시트 열림",
    unit: "count",
    baseline: 7,
    direction: "up",
    rule: "결제 순간이 대화 안에 있는가. Phase 2 목표 월 100건+.",
    group: "ref",
  },
];

export function kpiByColumn(column: string): KpiDef | undefined {
  return ROADMAP_KPIS.find((k) => k.column === column);
}

export type Verdict = "good" | "watch" | "bad" | "ref" | "unknown";

/**
 * 판정. 값이 없으면 unknown, 목표선도 바닥도 없으면 ref(참고).
 * 경계는 **포함**해서 나쁘게 센다 — 정확히 바닥값이면 bad 다(보수적).
 */
export function judge(def: KpiDef, value: number | null): Verdict {
  if (value === null || Number.isNaN(value)) return "unknown";
  if (def.target === undefined && def.floor === undefined) return "ref";
  if (def.direction === "up") {
    if (def.floor !== undefined && value <= def.floor) return "bad";
    if (def.target !== undefined) return value >= def.target ? "good" : "watch";
    return "good"; // floor 만 있는 가드레일 — 바닥 위면 정상
  }
  if (def.floor !== undefined && value >= def.floor) return "bad";
  if (def.target !== undefined) return value <= def.target ? "good" : "watch";
  return "good";
}

/** 로드맵 §3 "읽는 법" — 공휴일 가입은 판정에서 뺀다. */
export const HOLIDAYS_2026 = [
  "2026-09-24",
  "2026-09-25",
  "2026-09-26",
  "2026-09-27",
  "2026-10-03",
  "2026-10-09",
] as const;
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/admin/roadmap.test.ts`
Expected: PASS (7 tests)

- [ ] **Step 5: `app/admin/roadmap/page.tsx`**

```tsx
// app/admin/roadmap/page.tsx — 3층. 주 1회 읽는 판정 화면.
//
// 1·2층이 "지금 무슨 일이 일어나고 있나"라면 여기는 "그래서 이 변경이 성공인가"에 답한다.
// 베이스라인·목표선·판정 규칙을 **화면에 박아** 기억에 의존하지 않게 한다.
import { getServiceSupabase } from "@/lib/supabase";
import { adminExclusionArray } from "@/lib/admin";
import LoadFailed from "@/components/admin/LoadFailed";
import { formatMetric } from "@/lib/admin/format";
import { ROADMAP_KPIS, judge, HOLIDAYS_2026, type Verdict } from "@/lib/admin/roadmap";
import { daysAgoKstIso, startOfTodayKstIso } from "@/lib/admin-time";

export const dynamic = "force-dynamic";

const VERDICT_STYLE: Record<Verdict, { color: string; label: string }> = {
  good: { color: "#0ca30c", label: "달성" },
  watch: { color: "#fab219", label: "관찰" },
  bad: { color: "#d03b3b", label: "경보" },
  ref: { color: "#7A6BA0", label: "참고" },
  unknown: { color: "#6b6b6b", label: "—" },
};

const GROUP_LABEL: Record<string, string> = {
  primary: "Primary",
  welcome15: "웰컴 15",
  turnclose: "턴마무리",
  byeolmaru: "별마루",
  guardrail: "가드레일",
  ref: "참고",
};

/**
 * 창: 기본은 최근 28일. `?since=YYYY-MM-DD&until=YYYY-MM-DD` 로 덮어쓴다.
 * 🔴 KST 자정 경계는 `lib/admin-time.ts` 가 단일 원천이다 — 여기서 다시 계산하면 그게 드리프트다.
 */
function parseWindow(sp: Record<string, string | string[] | undefined>) {
  const one = (v: string | string[] | undefined) => (Array.isArray(v) ? v[0] : v);
  const since = one(sp.since);
  const until = one(sp.until);
  const iso = (d: string) => `${d}T00:00:00+09:00`;
  return {
    // daysAgoKstIso(28) = 오늘 KST 자정에서 28일 전. until 은 오늘 KST 자정(= 어제까지, 반개구간).
    since: since ? iso(since) : daysAgoKstIso(28),
    until: until ? iso(until) : startOfTodayKstIso(),
  };
}

export default async function RoadmapPage({
  searchParams,
}: {
  // Next 16: searchParams 는 Promise 다.
  searchParams: Promise<Record<string, string | string[] | undefined>>;
}) {
  const sp = await searchParams;
  const win = parseWindow(sp);
  const supa = getServiceSupabase();
  const res = await supa.rpc("admin_roadmap_kpi", {
    p_since: win.since,
    p_until: win.until,
    p_exclude: adminExclusionArray(),
    p_holidays: [...HOLIDAYS_2026],
  });

  const row = ((res.data ?? []) as Record<string, string | null>[])[0];
  const users = Number(row?.users ?? 0);
  const d7Eligible = Number(row?.d7_eligible ?? 0);

  return (
    <div className="space-y-6">
      <h1 className="text-xl font-bold">판정 — 흑자 전환 로드맵 v2</h1>
      <div className="text-[12px] text-white/50 leading-relaxed">
        창 {win.since.slice(0, 10)} ~ {win.until.slice(0, 10)} (가입 코호트 · KST) · 코호트{" "}
        <b className="text-white/75">{users.toLocaleString("ko-KR")}명</b> · 공휴일 가입 제외 · 지인
        제외
        <br />
        결제는 즉시 성숙(p90 49분), <b className="text-white/70">D7 은 7일 대기</b> — 현재 성숙 분모{" "}
        {d7Eligible.toLocaleString("ko-KR")}명.
        <br />
        판정 규칙 정본 = `plans/2026-09-19-흑자전환-로드맵-v2-별마루배포포함.md` §3. 배포 후 지표를
        바꾸지 않는다.
      </div>

      {res.error && <LoadFailed block="판정 KPI(admin_roadmap_kpi)" />}

      {!res.error &&
        (["primary", "welcome15", "turnclose", "byeolmaru", "guardrail", "ref"] as const).map((grp) => {
          const items = ROADMAP_KPIS.filter((k) => k.group === grp);
          if (items.length === 0) return null;
          return (
            <section key={grp}>
              <h2 className="text-sm text-white/60 mb-2">{GROUP_LABEL[grp]}</h2>
              <div className="space-y-2">
                {items.map((k) => {
                  const raw = row?.[k.column];
                  const value = raw === null || raw === undefined ? null : Number(raw);
                  const v = judge(k, value);
                  const st = VERDICT_STYLE[v];
                  // 🔴 D7 은 성숙 분모가 30 미만이면 숫자를 그리지 않는다(소표본 게이트와 같은 규율).
                  const gated = k.column === "d7_return_pct" && d7Eligible < 30;
                  return (
                    <div
                      key={k.column}
                      className="rounded-xl border border-white/10 bg-white/[0.03] px-4 py-3"
                      style={v === "bad" ? { borderColor: st.color } : undefined}
                    >
                      <div className="flex flex-wrap items-baseline gap-x-3 gap-y-1">
                        <span className="text-[13px] text-white/70">{k.label}</span>
                        <span className="text-xl font-bold text-white">
                          {gated ? (
                            <span className="text-sm font-normal text-white/40">
                              성숙 n={d7Eligible} · 판단 보류
                            </span>
                          ) : value === null ? (
                            <span className="text-white/40">—</span>
                          ) : (
                            formatMetric(value, k.unit)
                          )}
                        </span>
                        <span
                          className="rounded px-1.5 py-0.5 text-[11px] font-bold"
                          style={{ background: `${st.color}22`, color: st.color }}
                        >
                          {st.label}
                        </span>
                        <span className="text-[11px] text-white/40">
                          기준 {formatMetric(k.baseline, k.unit)}
                          {k.target !== undefined && ` · 목표 ${formatMetric(k.target, k.unit)}`}
                          {k.floor !== undefined && ` · 바닥 ${formatMetric(k.floor, k.unit)}`}
                        </span>
                      </div>
                      <div className="mt-1 text-[11px] leading-snug text-white/40">{k.rule}</div>
                    </div>
                  );
                })}
              </div>
            </section>
          );
        })}
    </div>
  );
}
```

- [ ] **Step 6: 게이트 + 실렌더**

Run: `npx tsc --noEmit` · `node --import tsx --test "lib/**/*.test.ts"` · `npm run build`

dev `/admin/roadmap` 을 열어: 그룹 6개가 뜨고, 각 항목에 값·판정 뱃지·기준선·규칙 한 줄이 있고, D7 이 성숙 분모 부족이면 "판단 보류"가 뜨는지 확인.

- [ ] **Step 7: 커밋**

```bash
git add lib/admin/roadmap.ts lib/admin/roadmap.test.ts app/admin/roadmap/page.tsx
git commit -F- <<'MSG'
feat(admin): 3층 판정 화면 — 베이스라인·목표선·판정 규칙 (플랜B 단계5)

로드맵 v2 §3 을 화면에 박는다. 기억에 의존한 판정을 없앤다.
D7 은 7일 성숙 분모가 30 미만이면 숫자를 그리지 않는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

### Task 17: nav 배선 + 전체 게이트 + 실렌더 검증

**Files:**
- Modify: `components/admin/AdminNav.tsx`
- Modify: `components/admin/AdminMobileNav.tsx` (같은 목록을 쓰면)

- [ ] **Step 1: nav 에 판정(3층)을 추가한다**

`AdminNav.tsx` 의 `HOME` 아래, `GROUPS` 의 `analytics` 그룹 맨 앞에 넣는다:

```ts
    { href: "/admin/roadmap", label: "판정(주간)", emoji: "🎯" },
```

`AdminMobileNav.tsx` 가 같은 목록을 복제하고 있으면 거기에도 넣는다(복제라면 이 기회에 `GROUPS` 를 공유하는 게 맞지만, **그건 이 플랜의 범위 밖이다** — 발견하면 사용자에게 보고만 한다).

- [ ] **Step 2: 🔴 어드민 가드를 확인한다**

`/admin/roadmap` 은 새 페이지다. AGENTS.md: **페이지 자체 가드는 `relationship-readings` 하나뿐이고 나머지는 `proxy.ts` 가 유일한 문**이다. 따라서 새 페이지도 `proxy.ts` 의 `/admin` 매칭에 자동으로 걸려야 한다.

검증: 토큰 없는 시크릿 창에서 `/admin/roadmap` → 리다이렉트되는지 확인한다. 리다이렉트되지 않으면 **즉시 멈추고** `proxy.ts` 의 matcher 를 확인한다(이건 데이터 노출이다).

`/api/admin/layer2` 도 같은 방식으로 확인한다: 쿠키 없이 호출 → **401**.

- [ ] **Step 3: 전체 게이트**

Run: `npx tsc --noEmit`
Expected: 오류 0

Run: `node --import tsx --test "lib/**/*.test.ts"`
Expected: 전부 pass (플랜 A 736 + 이 플랜 신규 ≈ 37)

Run: `npm run build`
Expected: exit 0 · 라우트 표에 `ƒ Proxy` 가 있다

- [ ] **Step 4: 실렌더 최종 확인 (dev)**

임시 `ADMIN_USER_IDS` + HMAC 쿠키 주입으로 `/admin` 을 열고 체크리스트를 통과시킨다. **검증 후 원복 필수.**

| 확인 | 기대 |
|---|---|
| 1층 히어로 | 기여 값 + 성분 3칸. 원가 미축적이면 `—` + 축적 N/62일 캡션 |
| 밴드 | 롤링 표본이 8개 미만이면 "밴드는 8주치가 모여야" 문구 |
| 매출의 질 4칸 · 흐름 5칸 · 단가 4칸 | 전부 숫자 또는 `n=N · 판단 보류` |
| 가드레일 6칸 | 6개 전부 존재. 임계 밖만 빨강 |
| 드릴다운 8개 | 전부 펼쳐 블록이 뜨거나 "표시할 데이터가 없다". **조용한 빈 화면 없음** |
| `/admin/roadmap` | 그룹 6개 · 판정 뱃지 |
| 기존 화면 | `/admin/analytics` · `/admin/traffic` · `/admin/paywall` · `/admin/relationship` 이 **전부 살아 있다**(삭제 0) |
| 모바일 375px | 가로 스크롤 없음 |

- [ ] **Step 5: 커밋**

```bash
git add components/admin/AdminNav.tsx components/admin/AdminMobileNav.tsx
git commit -F- <<'MSG'
feat(admin): nav 에 판정(3층) 추가 + 플랜B 전체 게이트 통과

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

- [ ] **Step 6: 플랜 문서에 완료 표기 + 메모리 갱신**

이 파일 상단에 진행 요약을 적고, 메모리 `admin-plan-a-progress` 를 잇는 항목을 갱신한다(플랜 C = 정밀 검증이 다음이다).

---

## 자체 검토

**스펙 커버리지** (§10 단계 3~6):

| 스펙 | 태스크 |
|---|---|
| §3 1층 — 기여 히어로 · 밴드 | Task 2·4·6 |
| §3 1층 — 매출의 질 · 흐름 | Task 7 |
| §3 1층 — 단가 분해 | Task 3·5·6 |
| §3 1층 — 가드레일 6종 | Task 3·5·6 |
| §3 1층 — 별 소모·탈퇴를 2층으로 | Task 8 |
| §3 2층 — 기여 ▾ | Task 9 |
| §3 2층 — 매출 ▾ · 구독 ▾ | Task 10 |
| §3 2층 — 신규 가입 ▾ | Task 11 |
| §3 2층 — 리딩 ▾ | Task 12 |
| §3 2층 — UV ▾ | Task 13 |
| §3 2층 — D7 ▾ · 탈퇴 ▾ | Task 14 · 8 |
| §3 3층 — roadmap KPI 이식 + 판정 | Task 15·16 |
| §5 무료 상품 공유 퍼널 | Task 11 |
| §5 리텐션 리프트 | Task 14 |
| §6 경로 판독기 | Task 13 |
| §7 소표본 게이트 | Task 1 (+ 11·14·16 에서 적용) |
| §7 정의 통일 — `result_viewed` drift | Task 5 |
| §7 정의 통일 — 오가닉 정본 | Task 11·15 |
| §8 시각화 사양 | Task 6(골드 마커·상태색) · 9(발산 막대) |

**스펙에 있으나 이 플랜이 하지 않는 것 — 의도된 것:**
- **§6 좁은 로깅 3종**(로그인 실패 사유 · 페이월 노출 시각 · 별마루 utm) — 전부 **다른 소유자의 코드**다. 별마루 utm 2줄은 별마루 세션 소유(§11), 페이월 노출은 별마루 P6-4 가 `meta.surface` 로 이미 넣었고, 로그인 실패 사유는 `lib/kakao.ts`·OAuth 콜백 변경이라 어드민 워크스트림의 범위를 넘는다. **판독기는 새 로깅 없이 과거 2개월을 읽으므로 이것들 없이도 동작한다.**
- **§5-2 별자리 코호트 분모 0** — 멤버 12명이 전원 익명이라 구조적으로 0이다. Task 11 의 착지 브리지가 같은 문제를 우회하는 설계이고, 별자리 화면 자체의 수정은 플랜 C(정밀 검증)에서 다룬다.
- **§9 정밀 검증** — 플랜 C.

**타입 일관성 확인:**
- `MetricKey` 신규 4개(`new_payment_won`·`repeat_payment_won`·`subscriber_net`·`readings_count`) + `signups_count` → Task 7 에서 전부 레지스트리에 추가된다. Task 7 Step 4 의 "🔴 metricKey 를 고친다" 항목이 이 누락을 잡는다.
- `Layer2Section` 8개 ↔ Task 8 의 드릴다운 8줄 ↔ 라우트의 `if` 분기: contribution(9) · revenue(8·10) · subscription(10) · signups(11) · readings(12) · uv(13) · d7(14) · withdrawal(8). **8개 전부 구현된다.**
- `DailyPnl` 필드명(`costRows`)이 Task 2 정의 ↔ Task 6 매핑에서 일치한다.
- `GuardView.key` 는 `MetricKey` 이고 `GUARDRAILS` 6개와 `admin_layer1_guard` 의 metric 문자열 6개가 1:1 대응한다.

**남은 위험 3가지 (실행 중 확인할 것):**
1. **`llm_usage` 가 prod 에 없다** — 1층 원가 칸과 2층 기여 ▾ 는 dev 에서만 완전히 검증된다. prod 검증은 배포 후로 밀린다(Task 4 Step 2 에 명시).
2. **`admin_star_won_rate` 의 실효 단가가 전 기간 평균**이라, 가격 개편이 있으면 과거 구독 매출이 현재 단가로 환산된다. 지금은 개편 이력이 없어 무해하지만 웰컴 15 배포 후에는 재검토 대상이다.
3. **`admin_layer2_subscription` 의 free-first 계산이 근사**다 — 구독 시점까지의 무료별 수급을 `star_transactions` 로 재구성하는데, `admin_star_spend_breakdown` 의 정식 free-first 사다리와 완전히 같지는 않다. 구독 표본이 0인 지금은 검증이 불가능하므로, **표본이 생기면 두 경로를 대조**한다(플랜 C 후보).
