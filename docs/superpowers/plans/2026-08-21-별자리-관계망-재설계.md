# 별자리 관계망 재설계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별자리 지도를 "모든 쌍 오행색 실선" → "특별 인연(천간합·육합·삼합)만 잇는 관계망"으로. 친구끼리 공개, 상단 필터를 관계타입으로 교체, 용어·등급명 정리.

**Architecture:** 지도는 특별 인연 선만 그린다(오행색 실선 제거). API는 게스트끼리 특별 인연 엣지를 `compat_visible` 무관하게 내려준다(호스트 엣지는 순위용으로 전부 유지). 상단 필터는 사회분류 대신 관계타입 [전체·끌림·결속·무리]로, `lib/byeoljari/bond-filter.ts`(순수)가 존재 종류 추출·dim 판정을 담당. 용어 "인연도"→"인연 점수", 등급명 하늘·별 계열.

**Tech Stack:** Next.js 16 / React 19 / TS strict, Tailwind v4, SVG(viewBox 0..100), node:test(`node --import tsx --test`).

**Spec:** `docs/superpowers/specs/2026-08-21-별자리-관계망-재설계-design.md`

## 파일 구조

- `lib/byeoljari/inyeon.ts` — 등급 라벨 4종 문자열 교체(경계·tone 유지).
- `lib/byeoljari/bond-filter.ts` (신규) — 관계타입 필터 순수 로직 + 라벨.
- `app/api/fortune/byeoljari/[shareId]/route.ts` — 게스트끼리 엣지 게이트(특별 인연만), 노드 `compatVisible` 제거.
- `app/fortune/byeoljari/[shareId]/page.tsx` — join 폼 `compat_visible` 옵트인 UI 제거(있으면).
- `components/byeoljari/ConstellationCanvas.tsx` — 특별 인연 엣지만 렌더, bond 기반 dim.
- `components/byeoljari/ConstellationView.tsx` — 관계타입 칩, 선 범례, "인연 점수" 표기.
- `components/byeoljari/InyeonDetail.tsx` — "인연 점수" 표기.

각 태스크 후 `npx tsc --noEmit` green + 앱 빌드 가능하도록 순서 배치.

---

### Task 1: 등급 라벨 하늘·별 계열로 교체 (TDD)

**Files:**
- Modify: `lib/byeoljari/inyeon.ts` (`inyeonGrade`)
- Test: `lib/byeoljari/inyeon.test.ts`

라벨 문자열만 바꾼다. 경계(≥85/≥70/≥55)·tone(high/mid/low/faint)·점수 공식은 그대로.

- [ ] **Step 1: 테스트를 새 라벨로 갱신(실패 확인용)**

`lib/byeoljari/inyeon.test.ts`를 열어 `inyeonGrade` 라벨을 단정하는 곳을 새 라벨로 바꾼다. 매핑:
- `"인연 진함"` → `"하늘이 맺은 인연"`
- `"도타운 사이"` → `"깊은 인연"`
- `"이어진 사이"` → `"이어진 인연"`
- `"잔잔한 사이"` → `"잔잔한 인연"`
(tone 값 `"high"/"mid"/"low"/"faint"`와 경계 점수 단정은 그대로 둔다.)

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `node --import tsx --test lib/byeoljari/inyeon.test.ts`
Expected: FAIL (구현이 아직 옛 라벨 반환).

- [ ] **Step 3: 구현 교체**

`lib/byeoljari/inyeon.ts`의 `inyeonGrade` 라벨 4곳을 교체:

```ts
export function inyeonGrade(score: number): { label: string; tone: "high" | "mid" | "low" | "faint" } {
  if (score >= 85) return { label: "하늘이 맺은 인연", tone: "high" };
  if (score >= 70) return { label: "깊은 인연", tone: "mid" };
  if (score >= 55) return { label: "이어진 인연", tone: "low" };
  return { label: "잔잔한 인연", tone: "faint" };
}
```
(기존 반환 구조와 동일 — 문자열만 변경. `inyeonScore`·`inyeonReasons`·`inyeonComment`은 손대지 않는다. 단, `inyeonComment`이 옛 등급 어휘를 문장에 쓰면 어색하지 않게만 확인 — 어휘 언급 없으면 무변경.)

- [ ] **Step 4: 테스트 실행 → 통과**

Run: `node --import tsx --test lib/byeoljari/inyeon.test.ts`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add lib/byeoljari/inyeon.ts lib/byeoljari/inyeon.test.ts
git commit -m "feat(byeoljari): 인연 점수 등급명 하늘·별 계열로(하늘이 맺은/깊은/이어진/잔잔한 인연)"
```

---

### Task 2: 관계타입 필터 순수 로직 (신규, TDD)

**Files:**
- Create: `lib/byeoljari/bond-filter.ts`
- Test: `lib/byeoljari/bond-filter.test.ts`

지도 필터를 사회분류(친구/연인/지인)에서 관계타입(끌림/결속/무리)으로 바꾸기 위한 순수 헬퍼. 이 태스크는 로직만(배선은 Task 5).

- [ ] **Step 1: 실패 테스트 작성**

`lib/byeoljari/bond-filter.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  BOND_FILTER_LABEL,
  presentBondFilters,
  edgeActiveForBond,
  nodeActiveForBond,
} from "./bond-filter.ts";

const E = (a: string, b: string, h: boolean, s: boolean) => ({ a, b, heavenlyCombo: h, sixCombo: s });

test("BOND_FILTER_LABEL — 끌림/결속/무리", () => {
  assert.equal(BOND_FILTER_LABEL.heavenly, "끌림");
  assert.equal(BOND_FILTER_LABEL.six, "결속");
  assert.equal(BOND_FILTER_LABEL.triad, "무리");
});

test("presentBondFilters — 존재하는 종류만, 순서 heavenly→six→triad", () => {
  const edges = [E("x", "y", true, false), E("y", "z", false, true)];
  const triads = [{ memberIds: ["a", "b", "c"] }];
  assert.deepEqual(presentBondFilters(edges, triads), ["heavenly", "six", "triad"]);
});

test("presentBondFilters — 없는 종류는 빠짐", () => {
  assert.deepEqual(presentBondFilters([E("x", "y", false, false)], []), []);
  assert.deepEqual(presentBondFilters([E("x", "y", false, true)], []), ["six"]);
});

test("edgeActiveForBond — 필터 없으면 전부, triad면 엣지 전부 비활성", () => {
  const e = E("x", "y", true, false);
  assert.equal(edgeActiveForBond(e, null), true);
  assert.equal(edgeActiveForBond(e, "heavenly"), true);
  assert.equal(edgeActiveForBond(e, "six"), false);
  assert.equal(edgeActiveForBond(e, "triad"), false);
});

test("nodeActiveForBond — heavenly/six는 강조 엣지 끝점, triad는 삼합 멤버", () => {
  const edges = [E("me", "y", true, false), E("me", "z", false, true)];
  const triadIds = new Set(["me", "z", "w"]);
  assert.equal(nodeActiveForBond("y", "heavenly", edges, triadIds), true);
  assert.equal(nodeActiveForBond("z", "heavenly", edges, triadIds), false);
  assert.equal(nodeActiveForBond("z", "six", edges, triadIds), true);
  assert.equal(nodeActiveForBond("w", "triad", edges, triadIds), true);
  assert.equal(nodeActiveForBond("y", "triad", edges, triadIds), false);
  assert.equal(nodeActiveForBond("y", null, edges, triadIds), true);
});
```

- [ ] **Step 2: 실행 → 실패 확인**

Run: `node --import tsx --test lib/byeoljari/bond-filter.test.ts`
Expected: FAIL (모듈 없음).

- [ ] **Step 3: 구현**

`lib/byeoljari/bond-filter.ts`:

```ts
// 지도 관계타입 필터(순수). 끌림=천간합 · 결속=육합 · 무리=삼합.
export type BondFilter = "heavenly" | "six" | "triad";

export const BOND_FILTER_LABEL: Record<BondFilter, string> = {
  heavenly: "끌림",
  six: "결속",
  triad: "무리",
};

const ORDER: BondFilter[] = ["heavenly", "six", "triad"];

type EdgeBond = { a: string; b: string; heavenlyCombo: boolean; sixCombo: boolean };

/** 맵에 실제로 존재하는 관계타입만(칩 구성용). 순서 고정. */
export function presentBondFilters(
  edges: Pick<EdgeBond, "heavenlyCombo" | "sixCombo">[],
  triads: { memberIds: string[] }[]
): BondFilter[] {
  return ORDER.filter((f) => {
    if (f === "heavenly") return edges.some((e) => e.heavenlyCombo);
    if (f === "six") return edges.some((e) => e.sixCombo);
    return triads.length > 0;
  });
}

/** 엣지가 현재 필터에서 강조 대상인가. triad면 엣지는 전부 비강조(삼합은 노드로 표시). */
export function edgeActiveForBond(
  edge: Pick<EdgeBond, "heavenlyCombo" | "sixCombo">,
  filter: BondFilter | null
): boolean {
  if (!filter) return true;
  if (filter === "heavenly") return edge.heavenlyCombo;
  if (filter === "six") return edge.sixCombo;
  return false;
}

/** 노드가 현재 필터에서 강조 대상인가. heavenly/six=강조 엣지 끝점, triad=삼합 멤버. */
export function nodeActiveForBond(
  nodeId: string,
  filter: BondFilter | null,
  edges: EdgeBond[],
  triadMemberIds: Set<string>
): boolean {
  if (!filter) return true;
  if (filter === "triad") return triadMemberIds.has(nodeId);
  return edges.some((e) => (e.a === nodeId || e.b === nodeId) && edgeActiveForBond(e, filter));
}
```

- [ ] **Step 4: 실행 → 통과**

Run: `node --import tsx --test lib/byeoljari/bond-filter.test.ts`
Expected: PASS. 회귀: `node --import tsx --test lib/byeoljari/layout.test.ts` green.

- [ ] **Step 5: 커밋**

```bash
git add lib/byeoljari/bond-filter.ts lib/byeoljari/bond-filter.test.ts
git commit -m "feat(byeoljari): 관계타입 필터 순수 로직(끌림/결속/무리 존재추출·dim판정) TDD"
```

---

### Task 3: compat_visible 폐지 — 게스트끼리 특별 인연 공개

**Files:**
- Modify: `app/api/fortune/byeoljari/[shareId]/route.ts`
- Modify: `app/fortune/byeoljari/[shareId]/page.tsx` (join 폼, `compat_visible` 옵트인 UI 있으면)

- [ ] **Step 1: API 엣지 게이트 변경**

`route.ts` 엣지 루프(현재 `for i / for j`)에서 `compat_visible` 게이트를 특별 인연 게이트로 교체. `pairRelation`을 먼저 계산하도록 순서 조정:

```ts
  for (let i = 0; i < members.length; i++) {
    for (let j = i + 1; j < members.length; j++) {
      const r = pairRelation(saju[i], saju[j]);
      const host = members[i].is_host || members[j].is_host;
      const special = r.heavenlyCombo || r.sixCombo;
      // 호스트 낀 엣지는 순위·점수용으로 전부. 게스트끼리는 특별 인연(끌림/결속)만 지도에 노출.
      if (!host && !special) continue;
      const triadShared = triadSets.some(
        (s) => s.has(members[i].id) && s.has(members[j].id)
      );
      edges.push({
        a: members[i].id,
        b: members[j].id,
        element: r.element,
        labelAtoB: r.labelAtoB,
        labelBtoA: r.labelBtoA,
        tenGodAtoB: r.tenGodAtoB,
        tenGodBtoA: r.tenGodBtoA,
        inyeon: inyeonScore({
          element: r.element,
          heavenlyCombo: r.heavenlyCombo,
          sixCombo: r.sixCombo,
          triadShared,
        }),
        triadShared,
        heavenlyCombo: r.heavenlyCombo,
        sixCombo: r.sixCombo,
      });
    }
  }
```
(기존 `const visible = host || (...compat_visible...)` / `if (!visible) continue;` 라인 삭제.)

- [ ] **Step 2: 노드 payload에서 compatVisible 제거**

`route.ts` 노드 매핑에서 `compatVisible` 필드 삭제:

```ts
  const nodes = members.map((m, i) => ({
    id: m.id,
    name: m.name_public ? m.display_name : null,
    isHost: m.is_host,
    relationType: m.relation_type,
    element: saju[i].dayElement,
  }));
```
`select(...)` 문자열의 `compat_visible`도 함께 제거(미사용).

- [ ] **Step 3: types.ts에서 GraphNode.compatVisible 제거(있으면)**

`lib/byeoljari/types.ts`에서 `GraphNode`에 `compatVisible`가 있으면 삭제. 참조하는 곳(grep `compatVisible`)이 없어야 함 — 있으면 함께 정리.

- [ ] **Step 4: join 폼 compat_visible 옵트인 UI 제거**

`app/fortune/byeoljari/[shareId]/page.tsx`에서 `compat_visible`/`compatVisible` 체크박스·상태·요청 필드를 grep. 있으면 UI + join 요청 바디에서 제거(join 라우트는 미전달 시 `false`로 처리하니 서버는 무변경). 없으면 이 스텝 스킵.

- [ ] **Step 5: 검증 + 커밋**

Run: `npx tsc --noEmit` → clean. grep `compatVisible` / `compat_visible` → 남은 참조가 DB 컬럼·join 라우트 insert(무해)뿐인지 확인.

로컬 실호출(선택, dev 서버 떠 있으면): 게스트끼리 특별 인연 엣지가 내려오는지 `7uBv0cLKet` GET 확인.

```bash
git add app/api/fortune/byeoljari/[shareId]/route.ts app/fortune/byeoljari/[shareId]/page.tsx lib/byeoljari/types.ts
git commit -m "feat(byeoljari): 친구끼리 특별 인연 공개 — compat_visible 게이트 폐지(게스트 엣지=천간합/육합만)"
```

---

### Task 4: 캔버스 — 특별 인연 엣지만 렌더

**Files:**
- Modify: `components/byeoljari/ConstellationCanvas.tsx`

이 태스크는 렌더만: 비특별 엣지(호스트-게스트 일반)를 안 그린다. 필터 dim(사회분류)은 아직 그대로 — Task 5에서 bond로 교체. 그래서 이 태스크만으로도 빌드·동작 유지.

- [ ] **Step 1: 비특별 엣지 렌더 스킵**

`edges.map`의 콜백 상단(`if (!pa || !pb) return null;` 다음)에 특별 인연 아닌 엣지 스킵 추가:

```tsx
          if (!pa || !pb) return null;
          if (!e.heavenlyCombo && !e.sixCombo) return null; // 오행색 실선 폐지 — 특별 인연만
```
(나머지 엣지 렌더 로직은 그대로. `gold = e.heavenlyCombo || e.sixCombo`는 항상 true가 되므로 관계선은 전부 금색 실선/점선.)

- [ ] **Step 2: 검증 + 커밋**

Run: `npx tsc --noEmit` → clean.

```bash
git add components/byeoljari/ConstellationCanvas.tsx
git commit -m "feat(byeoljari): 지도에 특별 인연 선만 — 오행색 실선(바퀴살) 렌더 제거"
```

---

### Task 5: 필터 관계타입 교체 + 선 범례 + "인연 점수" 표기

**Files:**
- Modify: `components/byeoljari/ConstellationCanvas.tsx` (dim을 bond 기반으로)
- Modify: `components/byeoljari/ConstellationView.tsx` (칩·범례·용어)
- Modify: `components/byeoljari/InyeonDetail.tsx` (용어)

Canvas의 `activeFilter` 의미를 사회분류 → 관계타입(BondFilter)으로 바꾸고, View가 그에 맞는 칩·상태를 넘긴다. 두 파일을 한 태스크에서 원자적으로.

- [ ] **Step 1: Canvas dim을 bond 기반으로**

`ConstellationCanvas.tsx` import 교체: `nodeMatchesFilter, edgeMatchesFilter`(layout) 제거하고 bond-filter import 추가:
```tsx
import { starPoints, resolveGlyph } from "@/lib/byeoljari/layout";
import { edgeActiveForBond, nodeActiveForBond, type BondFilter } from "@/lib/byeoljari/bond-filter";
```
`Props.activeFilter` 타입을 `string | null` → `BondFilter | null`로 변경.

컴포넌트 상단(triadRing 계산 근처)에 삼합 멤버 집합 추가:
```tsx
  const triadMemberIds = new Set(graph.triads.flatMap((t) => t.memberIds));
```

엣지 dim 계산(Task 4에서 특별 엣지만 남음) 교체:
```tsx
          const dimByFilter = activeFilter != null && !edgeActiveForBond(e, activeFilter);
          const dimByPair =
            highlightPairIds != null &&
            !(highlightPairIds.includes(e.a) && highlightPairIds.includes(e.b));
          const dim = dimByFilter || dimByPair ? DIM : 1;
```

노드 dim 계산 교체:
```tsx
          const dimByFilter =
            activeFilter != null && !nodeActiveForBond(n.id, activeFilter, graph.edges, triadMemberIds);
          const dimByPair = highlightPairIds != null && !highlightPairIds.includes(n.id);
          const nodeOpacity = dimByFilter || dimByPair ? DIM : 1;
```

- [ ] **Step 2: View — 칩을 관계타입으로**

`ConstellationView.tsx`:
- import: `RELATION_TYPE_LABEL`(필터용)·`relationTypeLabel`은 지도 카드 헤딩에서 계속 쓰이면 유지. 필터 전용으로 쓰던 `RELATION_ORDER` 상수 제거. bond-filter import 추가:
```tsx
import { presentBondFilters, BOND_FILTER_LABEL, type BondFilter } from "@/lib/byeoljari/bond-filter";
```
- 상태 타입: `const [activeFilter, setActiveFilter] = useState<BondFilter | null>(null);`
- `filterTypes`/`showFilter`/`effectiveFilter` 재정의:
```tsx
  const filterTypes = useMemo(
    () => presentBondFilters(graph.edges, graph.triads),
    [graph.edges, graph.triads]
  );
  const showFilter = filterTypes.length >= 1; // 전체 + ≥1 = 칩 2개↑
  const effectiveFilter = activeFilter && filterTypes.includes(activeFilter) ? activeFilter : null;
```
- 칩 렌더의 라벨을 BOND_FILTER_LABEL로:
```tsx
              >
                {t === null ? "전체" : BOND_FILTER_LABEL[t]}
              </button>
```
(칩 맵의 key/aria-pressed 로직은 그대로, `relationTypeLabel(t)` → `BOND_FILTER_LABEL[t]`.)
- `<ConstellationCanvas ... activeFilter={effectiveFilter} ... />`는 그대로(타입만 BondFilter로 흐름).

- [ ] **Step 3: View — 선 종류 미니 범례 추가**

오행 범례(LEGEND) 블록 바로 아래에 선 범례 추가:
```tsx
      <div className="mt-2 flex flex-wrap justify-center gap-3 text-xs text-text-light">
        <span className="inline-flex items-center gap-1">
          <svg width="16" height="6" aria-hidden><line x1="0" y1="3" x2="16" y2="3" stroke="#F2D78A" strokeWidth="1.5" strokeLinecap="round" /></svg>
          끌림
        </span>
        <span className="inline-flex items-center gap-1">
          <svg width="16" height="6" aria-hidden><line x1="0" y1="3" x2="16" y2="3" stroke="#F2D78A" strokeWidth="1.5" strokeDasharray="3 2" strokeLinecap="round" /></svg>
          결속
        </span>
        <span className="inline-flex items-center gap-1">
          <svg width="10" height="10" aria-hidden><circle cx="5" cy="5" r="4" fill="#FBC94D" opacity="0.5" /><circle cx="5" cy="1.5" r="1" fill="#FBC94D" /></svg>
          무리
        </span>
      </div>
```

- [ ] **Step 4: View — "인연도" → "인연 점수"**

`ConstellationView.tsx` 순위 영역:
- 헤더 `인연이 진한 순` → `인연 점수 높은 순`
- 각 행 `인연도 {r.inyeon} · {grade.label}` → `인연 점수 {r.inyeon} · {grade.label}`

- [ ] **Step 5: InyeonDetail — "인연도" → "인연 점수"**

`InyeonDetail.tsx`의 `인연도 {inyeon.score}` → `인연 점수 {inyeon.score}`.

- [ ] **Step 6: 검증 + 커밋**

Run: `npx tsc --noEmit` → clean. grep `RELATION_ORDER`·`nodeMatchesFilter`·`edgeMatchesFilter`가 지도 컴포넌트에서 사라졌는지(layout.ts의 함수 정의·테스트는 유지). grep `인연도`가 지도 UI에서 사라졌는지.

```bash
git add components/byeoljari/ConstellationCanvas.tsx components/byeoljari/ConstellationView.tsx components/byeoljari/InyeonDetail.tsx
git commit -m "feat(byeoljari): 관계타입 필터(끌림/결속/무리)+선 범례+인연 점수 표기"
```

---

### Task 6: 브라우저 E2E 검증

**Files:** 없음(검증 전용).

- [ ] **Step 1: dev 서버 + 로엔 맵**

`preview_start {name:"byeolkong-dev"}` → `http://localhost:3000/fortune/byeoljari/7uBv0cLKet`(13인, 친구끼리 특별 인연 있음). mobile 375.

- [ ] **Step 2: 선 검증**

DOM: `svg line` 중 오행색(비금색) 실선 **0개**(전부 `stroke="#F2D78A"`). 친구-친구(둘 다 비호스트) 사이에도 금색 선이 존재하는지(관계망). 특별 인연 없는 노드는 연결선 없음.

- [ ] **Step 3: 필터 검증**

칩이 `전체 · 끌림 · 결속 · 무리` 중 **존재하는 종류만** 뜨는지. "끌림" 탭 → 천간합 선/끝점만 밝고 나머지 dim. "무리" 탭 → 삼합 멤버 노드만 밝고 선 dim. "전체" 리셋.

- [ ] **Step 4: 용어·등급 검증**

순위 헤더 "인연 점수 높은 순", 행 "인연 점수 NN · 하늘이 맺은 인연/깊은 인연/이어진 인연/잔잔한 인연". 1:1 상세도 "인연 점수".

- [ ] **Step 5: 범례·회귀·콘솔**

선 범례(끌림 실선·결속 점선·무리 표시) 노출. 노드 모양(나=원·친구=별)·배경·1:1 카피 무회귀. `read_console_messages` onlyErrors = 0. 2인 맵(`HGRGPjuruS`)도 깨짐 없나 확인.

---

## Self-Review (계획 작성자 체크)

- **스펙 커버리지:** 실선 제거(T4)·특별 인연만(T4)·친구끼리 공개(T3)·필터 관계타입(T2·T5)·범례(T5)·용어(T5)·등급명(T1) 전부 매핑.
- **타입 일관성:** `BondFilter`(T2) ↔ Canvas prop(T5) ↔ View state(T5) 일치. `presentBondFilters`/`edgeActiveForBond`/`nodeActiveForBond` 시그니처 ↔ 사용처 일치. `inyeonGrade` 반환 구조 무변경(T1).
- **빌드 유지:** T1·T2 독립. T3 후 구 UI가 새 데이터로 동작(relationType 유지). T4는 렌더만(구 필터 유지). T5에서 필터 타입 원자 교체(canvas+view 동시). 각 단계 tsc green.
- **선점 보존:** layout.ts의 `nodeMatchesFilter`/`edgeMatchesFilter`/`starPoints` 함수·테스트는 유지(지도에서 import만 제거). `compat_visible` DB 컬럼·join insert는 유지(게이트만 폐지).
