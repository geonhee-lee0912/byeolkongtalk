# 별자리 유저당 1개 — 재진입/멱등 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 유저(호스트)당 별자리 1개 모델을 실제로 강제한다 — `/fortune/byeoljari` 재진입 시 내 맵으로 자동 이동, 만들기 API는 이미 있으면 기존 걸 반환(중복 생성 차단). 기존 중복 1건 정리.

**Architecture:** `star_maps`에서 세션 소유자(로그인=`owner_user_id`, 비로그인=`creator_anon_id`&owner NULL)의 맵을 찾는 `findOwnMap` 헬퍼를 route.ts에 둔다. GET `/api/fortune/byeoljari` = 내 맵 반환(없으면 null). POST = 이미 있으면 기존 반환(멱등). 만들기 페이지는 마운트 시 claim→내 맵 조회→있으면 me-key 저장+redirect, 없으면 폼.

**Tech Stack:** Next.js 16 route handlers, React 19 client page, Supabase service role.

**설계 근거:** 현재 `/fortune/byeoljari`는 무조건 만들기 폼만 렌더(기존 맵 무시)라 재진입=매번 새 맵. "유저당 1개"(사용자 확정)를 재진입 이동 + 생성 멱등으로 강제.

## 파일
- `app/api/fortune/byeoljari/route.ts` — `findOwnMap` 헬퍼 + GET(내 맵) + POST 멱등.
- `app/fortune/byeoljari/page.tsx` — 마운트 시 내 맵 있으면 redirect, 없으면 폼(+로딩 상태).
- (데이터) dev `HIaEZM9Ono` 삭제 — 코드 아님, 컨트롤러가 스크립트로.

---

### Task 1: route.ts — 내 맵 조회(GET) + 생성 멱등(POST)

**Files:** Modify `app/api/fortune/byeoljari/route.ts`

- [ ] **Step 1: `findOwnMap` 헬퍼 추가** (파일 상단 import 아래, POST 위)

```ts
// 세션 소유자의 별자리 1개 조회(유저당 1개 모델). 로그인=owner_user_id / 비로그인=미claim anon.
// 방어적으로 최신 1개만. 호스트 memberId 도 함께(뷰어=나 식별용 me-key).
async function findOwnMap(
  supa: ReturnType<typeof getServiceSupabase>,
  userId: string | null,
  anonymousId: string
): Promise<{ shareId: string; memberId: string | null } | null> {
  const base = supa
    .from("star_maps")
    .select("id, share_id")
    .order("created_at", { ascending: false })
    .limit(1);
  const { data: maps, error } = await (userId
    ? base.eq("owner_user_id", userId)
    : base.eq("creator_anon_id", anonymousId).is("owner_user_id", null));
  if (error || !maps || maps.length === 0) return null;
  const map = maps[0];
  const { data: host } = await supa
    .from("star_map_members")
    .select("id")
    .eq("map_id", map.id)
    .eq("is_host", true)
    .maybeSingle();
  return { shareId: map.share_id, memberId: host?.id ?? null };
}
```

- [ ] **Step 2: GET 핸들러 추가** (POST 옆)

```ts
export async function GET() {
  const { userId, anonymousId } = await getSession();
  if (!anonymousId) return NextResponse.json({ ok: true, map: null });
  const supa = getServiceSupabase();
  const map = await findOwnMap(supa, userId ?? null, anonymousId);
  return NextResponse.json({ ok: true, map });
}
```

- [ ] **Step 3: POST 멱등** — 세션 확보 직후(`if (!anonymousId)` 가드 다음), body 파싱 전에 기존 맵 체크 삽입:

```ts
  const supa0 = getServiceSupabase();
  const existing = await findOwnMap(supa0, userId ?? null, anonymousId);
  if (existing) {
    return NextResponse.json({
      ok: true,
      shareId: existing.shareId,
      memberId: existing.memberId,
      existing: true,
    });
  }
```
그 아래 기존 생성 로직은 그대로(이미 `const supa = getServiceSupabase();`가 뒤에 있으면 그대로 두거나 supa0 재사용 — 중복 선언 피하려면 기존 `const supa =` 를 지우고 위 `supa0` 를 `supa` 로 명명해 한 번만 선언할 것). 최종적으로 `getServiceSupabase()` 호출은 POST 당 1회, 변수명 `supa` 하나로 정리.

- [ ] **Step 4: 검증**

`npx tsc --noEmit` → clean.
로컬 실호출(dev 서버 있으면): `GET /api/fortune/byeoljari` 를 로그인 세션으로 호출 시 `map:{shareId,memberId}` 반환(비로그인·무맵이면 `map:null`). POST 재호출 시 `existing:true`.

- [ ] **Step 5: 커밋**

```bash
git add app/api/fortune/byeoljari/route.ts
git commit -m "feat(byeoljari): 유저당 1개 — GET 내 맵 조회 + POST 생성 멱등(중복 차단)"
```

---

### Task 2: 만들기 페이지 — 내 맵 있으면 재진입 이동

**Files:** Modify `app/fortune/byeoljari/page.tsx`

- [ ] **Step 1: 마운트 시 claim → 내 맵 조회 → redirect / 폼**

기존의 두 `useEffect`(claim, loggedIn) 를 아래로 재구성. `checking` 상태를 추가해 조회 중엔 폼을 감춘다(폼 깜빡임·중복 생성 방지):

```tsx
  const [checking, setChecking] = useState(true);
```
```tsx
  useEffect(() => {
    setLoggedIn(!!localStorage.getItem("byeolkong_user"));
    let cancelled = false;
    (async () => {
      // claim(익명 맵을 로그인 계정으로 이관) 먼저 → 그래야 owner_user_id 로 내 맵이 잡힌다.
      await fetch("/api/fortune/byeoljari/claim", { method: "POST" }).catch(() => {});
      const res = await fetch("/api/fortune/byeoljari").then((r) => r.json()).catch(() => null);
      if (cancelled) return;
      if (res?.map?.shareId) {
        if (res.map.memberId) {
          localStorage.setItem(`byeoljari:me:${res.map.shareId}`, res.map.memberId);
        }
        router.replace(`/fortune/byeoljari/${res.map.shareId}`);
        return; // redirect 중 checking 유지(폼 안 보임)
      }
      setChecking(false);
    })();
    return () => {
      cancelled = true;
    };
  }, [router]);
```
(기존 `useEffect(() => { fetch(".../claim") }, [])` 와 `useEffect(() => setLoggedIn(...), [])` 는 삭제하고 위 하나로 합침.)

- [ ] **Step 2: 로딩 중 렌더 가드**

`return (` 직전에 조회 중 로딩 표시 추가:

```tsx
  if (checking) {
    return (
      <main className="mx-auto max-w-md px-4 py-16 text-center text-text-light">
        <p className="animate-star-twinkle text-2xl">✨</p>
        <p className="mt-3 text-sm">별자리를 불러오는 중…</p>
      </main>
    );
  }
```

- [ ] **Step 3: 검증 + 커밋**

`npx tsc --noEmit` → clean.

```bash
git add app/fortune/byeoljari/page.tsx
git commit -m "feat(byeoljari): 만들기 재진입 — 내 맵 있으면 자동 이동(유저당 1개)"
```

---

### Task 3: 데이터 정리 + E2E (컨트롤러 수행)

**Files:** 없음(dev 데이터 + 브라우저).

- [ ] **Step 1: 중복 맵 삭제** — dev `star_maps` 에서 `HIaEZM9Ono`(owner 0370400f, 1명, 빈 것) 삭제. 삭제 전 owner·member수 재확인(owner=0370400f && members<=1 인지). member 는 `ON DELETE CASCADE` 로 함께 삭제. `7uBv0cLKet` 는 보존.
- [ ] **Step 2: E2E** — dev 서버로 `/fortune/byeoljari`(로그인 세션 주입) 진입 → 만들기 폼이 아니라 **`7uBv0cLKet` 로 redirect** 되는지. me-key 저장 확인. POST 재호출 시 `existing:true` + 새 맵 미생성(맵 개수 불변).

---

## Self-Review
- **스펙 커버리지:** 재진입 이동(T2)·생성 멱등(T1)·조회(T1)·중복 정리(T3) 매핑.
- **타입 일관성:** `findOwnMap` 반환 `{shareId, memberId}` ↔ GET `map` ↔ 페이지 `res.map.shareId/memberId` 일치. POST 반환에 `existing` 추가(기존 `{ok,shareId,memberId}` 계약 유지, 페이지 create()는 shareId/memberId만 사용하니 무영향).
- **빌드 유지:** T1(라우트) → T2(페이지) 순서, 각 tsc green.
- **claim 순서:** 페이지가 claim 후 GET 조회 — 로그인 유저의 익명 맵이 owner 로 이관된 뒤 잡히도록.
- **주의:** POST 에서 `getServiceSupabase()` 중복 선언 금지(변수 `supa` 하나로).
