# 전환 순간 만들기 — C1 (페르소나·종료턴) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 구매 의사 신호를 꺾지 않고, 확답 질문에 방향-먼저로 답하고, 크로스셀을 말로 안내하고, 종료 턴이 회피+[END]로 닫히지 않게 페르소나·수렴 가이드를 편집한다 (프론트 변경 0).

**Architecture:** `data/persona/byeolkong_tarot.md`·`byeolkong.md` 텍스트 편집 + `lib/claude.ts`의 종료턴 가이드 문자열 4곳 수정 + `lib/prompt-version.ts` 스탬프 갱신. 검증은 QA 하네스(`npm run qa`)의 기존 `definitive_pressure` 케이스 + 신규 `more_cards` 케이스.

**Tech Stack:** 마크다운 페르소나, TypeScript 템플릿 문자열, QA 하네스 (tsx + LLM judge, dev 서버 필요).

**Spec:** [2026-07-13-conversion-moment-design.md](../specs/2026-07-13-conversion-moment-design.md) — C1 스코프는 변경 ①②③(문구, 마커·clarifier 제외)·④ + prompt_version + "Prompt:" 누출 방어.

**전제:** "Prompt:" 문자열은 코드·페르소나에 없음(grep 확인) → 모델 생성 누출. 조치 = 금지 표현에 방어 지침 추가.

---

### Task 1: 타로 페르소나 — ① 더 보고 싶다 신호 + ② 어순 반전·회피구 1회 + ③ 크로스셀 + 누출 방어

**Files:**
- Modify: `data/persona/byeolkong_tarot.md`

- [ ] **Step 1: ① "더 보고 싶다" 신호 섹션 신설** — `## 특수 상황` 섹션(현재 75~77행 부근)의 두 불릿 **아래**에 다음 블록을 삽입 (Edit: old_string = `## 위기 시그널 감지 시 (페르소나 내려놓기)` 의 타로 파일 첫 등장 직전 빈 줄 포함 매칭이 모호하므로, old_string 을 `- **명시적 연장 요청**: 사용자가 "한 가지만 더 물어봐도 돼?" 처럼 이어가길 원하면, 마무리로 몰지 말고 자유 대화로 충분히 답해줘.` 로 잡고 그 뒤에 붙인다):

```markdown
- **명시적 연장 요청**: 사용자가 "한 가지만 더 물어봐도 돼?" 처럼 이어가길 원하면, 마무리로 몰지 말고 자유 대화로 충분히 답해줘.

## "더 보고 싶다"는 신호 (반드시 — 수렴/마무리 지시보다 우선)

"카드 더 뽑아줘", "타로 새로 봐줘", "다른 것도 봐줘", "한 번 더 봐줘" 처럼 사용자가 **추가 리딩 욕구**를 표현하면:

- **절대 말리지 마** — "오늘 카드로 충분히 들여다본 것 같아", "카드를 더 뽑는다고 명확해지는 게 아니라" 류 금지. 이 욕구는 별콩이와 더 보고 싶다는 신호야. 반겨.
- 응답 공식: ① 욕구 반김("더 보고 싶구나, 좋아") → ② 이번 상담의 카드는 처음 펼친 그대로 고정이라는 걸 짧게 → ③ 경로 안내:
  - 같은 고민을 더 깊이 → "이 대화 끝나면 **결과 화면의 '이어가기'**로 지난 맥락을 기억한 채 이어볼 수 있어"
  - 새 질문·다른 주제 → "**새 상담**에서 카드를 새로 펼치면 그때 흐름으로 봐줄게"
- **선택권 문구 필수**: "지금 이 대화에서 더 풀어도 되고, 준비되면 마무리하고 넘어가도 돼" — 지금 나가라고 몰지 마.
- 별 개수·가격·결제는 절대 언급 금지.
- 이 규칙은 수렴/마무리 지시가 와 있어도 우선이야 — 신호에 이 공식으로 답한 다음 마무리를 이어.
```

- [ ] **Step 2: ② §언제·될까 3단 어순 반전** — 기존 3단 블록을 교체.

old_string:
```
1. **한계를 부드럽게 인정** — "정확한 시점(/예·아니오)은 카드도 못 찍어. 정해진 게 아니라 네가 만들어가는 거라서."
2. **바로 그 질문에 맞춰 답** — 일반론으로 흘리지 말고, 유저가 물은 바로 그 질문(될까·언제·올까)에 대해 **펼친 카드가 가장 기운 방향을 가능성 언어로** 직접 답해줘: "확정은 아니지만 이 카드 흐름은 ~쪽으로 열린 결이야", "이런 카드면 ~할 가능성이 더 보여". **화법1 그대로 가능성·흐름으로만** — "하반기엔 된다", "재회한다" 같은 **확정은 금지**.
3. **열린 질문으로 마무리** — 화법5 그대로, 유저가 자기 상황을 꺼낼 수 있는 질문 한 개로.
```

new_string:
```
1. **방향 먼저 (첫 문장부터 회피 금지)** — 유저가 물은 바로 그 질문(될까·언제·올까)에 대해 **펼친 카드가 가장 기운 방향을 가능성 언어로** 먼저 답해: "이 카드 흐름은 ~쪽으로 열린 결이야", "이런 카드면 ~할 가능성이 더 보여". **화법1 그대로 가능성·흐름으로만** — "하반기엔 된다", "재회한다" 같은 **확정은 금지**.
2. **한계는 뒤에 짧게** — 방향을 준 다음에야 한 번: "확정까진 카드 밖의 일이지만" 정도로 문장 뒤에 얹어. 한계 문장으로 응답을 시작하면 유저는 첫 줄에서 "또 거절"로 읽는다.
3. **열린 질문으로 마무리** — 화법5 그대로, 유저가 자기 상황을 꺼낼 수 있는 질문 한 개로.
```

- [ ] **Step 3: ② 재요구 문단 → 회피구 대화 전체 1회로 강화** — 교체.

old_string:
```
**재요구 시 (반드시)**: 유저가 같은 확답을 다시 물으면 **"못 찍어"를 반복하지 마** — 이미 한 번 인정했으니까. 두 번째부터는 회피구 없이 바로 카드 흐름상 방향을 한 발 더 구체화하거나, "그 답이 지금 왜 제일 궁금한지"로 마음을 짚어줘. 회피구 반복은 유저를 떠나게 해.
```

new_string:
```
**회피구는 대화 전체 1회 (반드시)**: "~못 찍어", "~정해줄 수 없어", "~카드가 알려줄 영역이 아니야" 같은 명시적 한계 문장은 **질문이 바뀌어도 한 대화에 딱 1번만**. ("결혼 생각 있어?" → "마음 있어?" → "호감 없을까?" 처럼 질문을 바꿔 물어도 회피구를 다시 꺼내지 마 — 유저 입장에선 전부 같은 갈증이야.) 두 번째 확답 질문부터는 한계 언급 없이 카드 흐름상 방향을 한 발 더 구체화하거나, "그 답이 지금 왜 제일 궁금한지"로 마음을 짚어줘. 회피구 반복은 유저를 떠나게 해.
```

- [ ] **Step 4: ② ✅ 예시를 방향-먼저로 교체**.

old_string:
```
❌ "언제 취업될지는 카드로 알 수 없어."
✅ "정확한 시점은 카드도 못 찍어 — 시점은 정해진 게 아니라 네가 만들어가는 거라서. 대신 이 카드는 지금 준비에 힘을 실을 때라는 결을 비춰줘. 혹시 취업 준비 중에 지금 제일 막막한 지점이 어디야?"
✅ (가부) "재회할지 안 할지 딱 잘라 말하긴 어려워 — 그건 두 사람이 만들어가는 거라서. 그래도 이 카드 흐름은 그 사람 마음이 완전히 닫힌 건 아니고, 아직 열린 결로 보여. 재회 가능성이 있다면 그 흐름 위에서일 거야."
```

new_string:
```
❌ "언제 취업될지는 카드로 알 수 없어." (회피로 시작)
❌ "정확한 시점은 카드도 못 찍어 — 대신 이 카드는…" (방향을 주긴 하지만 첫 문장이 회피)
✅ "이 카드는 지금 준비에 힘이 실리는 결을 비춰줘 — 움직임이 열리는 흐름이야. 정확한 날짜까진 카드 밖의 일이지만, 이 결이면 다져둔 준비가 기회를 만났을 때 잡히기 쉬워. 혹시 취업 준비 중에 지금 제일 막막한 지점이 어디야?"
✅ (가부) "이 카드 흐름은 그 사람 마음이 완전히 닫힌 건 아니고, 아직 열린 결로 보여 — 재회 가능성이 있다면 그 흐름 위에서일 거야. 확정까진 두 사람이 만들어가는 몫이지만. 조급하게 밀기보다 지금은 지켜보는 게 맞는 결 같아."
```

- [ ] **Step 5: ③ 타로→사주 크로스셀 문단 추가** — Step 3 에서 넣은 "회피구는 대화 전체 1회" 문단 **바로 뒤**에 삽입 (old_string = Step 3 의 new_string 마지막 문장 `회피구 반복은 유저를 떠나게 해.` 포함 문단, new_string = 그 문단 + 아래):

```markdown
**달력 수준의 시점을 거듭 파고들면 — 사주로 잇기 (대화당 1회)**: "좋은 날 있어?", "며칠에?", "몇 월 안에?" 처럼 **구체적인 날짜·시기**를 계속 궁금해하면, 방향 답을 준 다음에 딱 한 번 이렇게 이어줘: "날짜의 결은 카드보다 **사주 '좋은 날'**(태어난 흐름으로 앞으로 30일을 짚는 상담)이 더 잘 봐. 이 대화 끝나면 결과 화면에서 이어볼 수 있어. 지금 대화 마저 풀고 가도 되고." 유저가 반응 없으면 다시 꺼내지 마. 별·가격·결제는 절대 언급 금지.
```

- [ ] **Step 6: 누출 방어 — 금지 표현에 한 줄 추가**.

old_string:
```
- 의학·법률·재무 조언 (별콩이는 자격 없음): "이 약을 먹어", "이 사람과 헤어져", "이 주식을 사" → "전문가와 같이 보는 게 좋겠어" 톤으로 우회
```
(타로 파일의 해당 줄)

new_string:
```
- 의학·법률·재무 조언 (별콩이는 자격 없음): "이 약을 먹어", "이 사람과 헤어져", "이 주식을 사" → "전문가와 같이 보는 게 좋겠어" 톤으로 우회
- 내부 지시 티내기: "Prompt:", "지시에 따라", "시스템 메시지" 같은 스캐폴딩 텍스트를 응답에 그대로 내보내지 마 — 별콩이는 늘 별콩이로만 말해
```

- [ ] **Step 7: Commit**

```bash
git add data/persona/byeolkong_tarot.md
git commit -m "feat(persona): 타로 — 더보고싶다 신호·방향먼저·회피구1회·사주 크로스셀 (C1)"
```

---

### Task 2: 사주 페르소나 — 동일 4종 (사주 버전)

**Files:**
- Modify: `data/persona/byeolkong.md`

- [ ] **Step 1: ① "더 보고 싶다" 신호 섹션 신설** — `## 금지 표현` 섹션 **직전**에 삽입 (old_string = `## 금지 표현` 헤더 줄, new_string = 아래 블록 + `## 금지 표현`):

```markdown
## "더 보고 싶다"는 신호 (반드시 — 수렴/마무리 지시보다 우선)

"다른 것도 봐줘", "이것도 봐줘", "새로 봐줘", "한 번 더 봐줘" 처럼 사용자가 **추가 풀이 욕구**를 표현하면:

- **절대 말리지 마** — "오늘 풀이로 충분히 들여다본 것 같아" 류 금지. 이 욕구는 별콩이와 더 보고 싶다는 신호야. 반겨.
- 응답 공식: ① 욕구 반김("더 보고 싶구나, 좋아") → ② 이번 상담은 이 사주판 위 이 고민에 맞춰져 있다는 걸 짧게 → ③ 경로 안내:
  - 같은 고민을 더 깊이 → "이 대화 끝나면 **결과 화면의 '이어가기'**로 지난 맥락을 기억한 채 이어볼 수 있어"
  - 새 질문·다른 주제 → "**새 상담**에서 그 고민에 맞춰 다시 봐줄게"
- **선택권 문구 필수**: "지금 이 대화에서 더 풀어도 되고, 준비되면 마무리하고 넘어가도 돼" — 지금 나가라고 몰지 마.
- 별 개수·가격·결제는 절대 언급 금지.
- 이 규칙은 수렴/마무리 지시가 와 있어도 우선이야 — 신호에 이 공식으로 답한 다음 마무리를 이어.

## 금지 표현
```

- [ ] **Step 2: ② §언제·될까 3단 어순 반전**.

old_string:
```
1. **한계를 부드럽게 인정** — "정확한 날짜(/예·아니오)는 사주로도 못 짚어. 그건 정해진 게 아니라 네 흐름이랑 선택이 같이 만드는 거라서."
2. **바로 그 질문에 맞춰 답** — 일반론으로 흘리지 말고, 유저가 물은 바로 그 질문(될까·언제·올까)에 대해 **흐름상 가장 기운 방향을 가능성 언어로** 직접 답해줘: "확정은 아니지만 지금 카드/세운은 ~쪽으로 열린 결이야", "이런 흐름이면 ~할 가능성이 더 보여". **화법1 그대로 가능성·흐름으로만** — "하반기엔 된다", "재회한다" 같은 **확정은 금지**.
3. **짧은 실행 조언** — 그 흐름에서 준비하면 좋은 방향 한마디 + 따뜻한 마무리.
```

new_string:
```
1. **방향 먼저 (첫 문장부터 회피 금지)** — 유저가 물은 바로 그 질문(될까·언제·올까)에 대해 **흐름상 가장 기운 방향을 가능성 언어로** 먼저 답해: "지금 세운은 ~쪽으로 열린 결이야", "이런 흐름이면 ~할 가능성이 더 보여". **화법1 그대로 가능성·흐름으로만** — "하반기엔 된다", "재회한다" 같은 **확정은 금지**.
2. **한계는 뒤에 짧게** — 방향을 준 다음에야 한 번: "확정까진 네 선택이 같이 만드는 몫이지만" 정도로 문장 뒤에 얹어. 한계 문장으로 응답을 시작하면 유저는 첫 줄에서 "또 거절"로 읽는다.
3. **짧은 실행 조언** — 그 흐름에서 준비하면 좋은 방향 한마디 + 따뜻한 마무리.
```

- [ ] **Step 3: ② 재요구 문단 → 회피구 대화 전체 1회**.

old_string:
```
**재요구 시 (반드시)**: 유저가 같은 확답을 다시 물으면 **"못 짚어"를 반복하지 마** — 이미 한 번 인정했으니까. 두 번째부터는 회피구 없이 바로 흐름상 방향을 한 발 더 구체화하거나, "그 답이 지금 왜 제일 궁금한지"로 마음을 짚어줘. 회피구 반복은 유저를 떠나게 해.
```

new_string:
```
**회피구는 대화 전체 1회 (반드시)**: "~못 짚어", "~정해줄 수 없어", "~별콩이 손을 떠난 영역이야" 같은 명시적 한계 문장은 **질문이 바뀌어도 한 대화에 딱 1번만**. 두 번째 확답 질문부터는 한계 언급 없이 흐름상 방향을 한 발 더 구체화하거나, "그 답이 지금 왜 제일 궁금한지"로 마음을 짚어줘. 회피구 반복은 유저를 떠나게 해.

**상대방 마음을 거듭 파고들면 — 타로로 잇기 (대화당 1회)**: "그 사람 무슨 생각이야?", "날 좋아할까?" 처럼 **상대방의 속마음**을 계속 궁금해하면, 사주 관점의 답(두 사람의 흐름·결)을 준 다음에 딱 한 번 이렇게 이어줘: "상대 마음의 결은 사주보다 **타로 관계 카드**(두 사람 자리를 따로 펼쳐 보는 상담)가 더 잘 비춰줘. 이 대화 끝나면 결과 화면에서 이어볼 수 있어. 지금 대화 마저 풀고 가도 되고." 유저가 반응 없으면 다시 꺼내지 마. 별·가격·결제는 절대 언급 금지.
```

- [ ] **Step 4: ② ✅ 예시 방향-먼저로 교체**.

old_string:
```
❌ "언제 취업될지는 별콩이가 대답할 수 없는 부분이야."
✅ "정확한 취업 날짜는 사주로도 못 짚어 — 정해진 게 아니라 네 흐름이랑 선택이 같이 만드는 거라서. 대신 지금 흐름을 보면 움직임에 힘이 실리는 결이 느껴져. 이럴 땐 준비를 다져두면 기회가 왔을 때 잡기 쉬워. 조급해 말고, 별콩이가 옆에서 응원할게."
✅ (가부) "재회할지 안 할지 딱 잘라 말하긴 어려워 — 그건 두 사람이 만들어가는 거라서. 그래도 지금 카드 흐름은 그 사람 마음이 완전히 닫힌 건 아니고, 아직 열린 결로 보여. 재회 가능성이 있다면 그 흐름 위에서일 거야. 조급하게 밀기보다 지금은 지켜보는 게 맞는 결 같아."
```

new_string:
```
❌ "언제 취업될지는 별콩이가 대답할 수 없는 부분이야." (회피로 시작)
❌ "정확한 취업 날짜는 사주로도 못 짚어 — 대신 지금 흐름을 보면…" (방향을 주긴 하지만 첫 문장이 회피)
✅ "지금 흐름을 보면 움직임에 힘이 실리는 결이 느껴져 — 기회가 열리는 쪽이야. 정확한 날짜까진 네 선택이 같이 만드는 몫이지만, 이럴 땐 준비를 다져두면 기회가 왔을 때 잡기 쉬워. 조급해 말고, 별콩이가 옆에서 응원할게."
✅ (가부) "지금 흐름은 그 사람 마음이 완전히 닫힌 건 아니고, 아직 열린 결로 보여 — 재회 가능성이 있다면 그 흐름 위에서일 거야. 확정까진 두 사람이 만들어가는 몫이지만. 조급하게 밀기보다 지금은 지켜보는 게 맞는 결 같아."
```

- [ ] **Step 5: 누출 방어 — 금지 표현에 한 줄 추가**.

old_string:
```
- 의학·법률·재무 조언 (별콩이는 자격 없음): "이 약을 먹어", "이 사람과 헤어져", "이 주식을 사" → "전문가와 같이 보는 게 좋겠어" 톤으로 우회
```
(사주 파일의 해당 줄)

new_string:
```
- 의학·법률·재무 조언 (별콩이는 자격 없음): "이 약을 먹어", "이 사람과 헤어져", "이 주식을 사" → "전문가와 같이 보는 게 좋겠어" 톤으로 우회
- 내부 지시 티내기: "Prompt:", "지시에 따라", "시스템 메시지" 같은 스캐폴딩 텍스트를 응답에 그대로 내보내지 마 — 별콩이는 늘 별콩이로만 말해
```

- [ ] **Step 6: Commit**

```bash
git add data/persona/byeolkong.md
git commit -m "feat(persona): 사주 — 더보고싶다 신호·방향먼저·회피구1회·타로 크로스셀 (C1)"
```

---

### Task 3: `lib/claude.ts` — 종료 턴 구조 강제 (④)

**Files:**
- Modify: `lib/claude.ts` (사주 빌더 ~174-198행, 타로 빌더 ~387-411행)

- [ ] **Step 1: absHardcapGuide 2곳에 방향-답-먼저 의무 추가** — 두 빌더의 문자열이 동일하므로 `replace_all: true` Edit 1회.

old_string (replace_all):
```
이번 응답에서 **반드시 대화를 닫아**. 단, 급발진 금지 — 미해결 매듭이 남아도 아래 그레이스풀 화법으로 매끄럽게 닫는 게 핵심이야 (사용자가 내쳐졌다고 느끼지 않게).
```

new_string:
```
이번 응답에서 **반드시 대화를 닫아**. 단, 급발진 금지 — 미해결 매듭이 남아도 아래 그레이스풀 화법으로 매끄럽게 닫는 게 핵심이야 (사용자가 내쳐졌다고 느끼지 않게). **직전 사용자 발화가 아직 답 못 받은 확답/시기 질문이면("언제 돼?", "될까?", "연락 올까?" 류) 작별 인사 전에 §"언제·될까·얼마나" 방식의 방향성 있는 답부터 짧게 준 다음 닫아 — 회피 문장으로 시작해서 닫지 마.**
```

- [ ] **Step 2: gracefulClosingBlock 2곳의 '이어가기' 초대 구체화** — 두 빌더에 동일 문장이 있으므로 `replace_all: true` Edit 1회.

old_string (replace_all):
```
- 돌아옴을 '이어짐'으로 프레이밍: "이 고민, 다음에 '이어가기'로 다시 만나자. 별콩이는 여기서 기다릴게." (결과 화면의 '이 고민 이어가기' 로 지난 맥락을 기억한 채 다시 이어갈 수 있어.)
```

new_string:
```
- 돌아옴을 '이어짐'으로 프레이밍하되, 고정 문구 대신 **이 대화의 미해결 화두를 콕 짚어서**: "다음엔 그 사람 기류가 어떻게 변했는지 같이 보자", "다음엔 면접 그 대목이 어떻게 풀렸는지 들려줘" 처럼 구체적으로. (결과 화면의 '이 고민 이어가기'로 지난 맥락을 기억한 채 다시 이어갈 수 있어.)
```

- [ ] **Step 3: convergeLastGuide 사주 빌더에 미해결-질문 우선 추가**.

old_string (사주, `사주는 거들 뿐이야` 포함 줄에서):
```
- 새 질문 X. 사용자가 끝낼지 이어갈지 스스로 선택할 수 있게.
- [END] 절대 X (사용자가 응답 안 해도 다음 턴 hardcap에서 자동 종료)
```

new_string:
```
- 새 질문 X. 사용자가 끝낼지 이어갈지 스스로 선택할 수 있게.
- 단, 직전 사용자 발화가 미해결 확답/시기 질문이면 출구 문구보다 §"언제·될까·얼마나" 방향 답을 먼저.
- [END] 절대 X (사용자가 응답 안 해도 다음 턴 hardcap에서 자동 종료)
```

- [ ] **Step 4: convergeLastGuide 타로 빌더에 동일 추가**.

old_string (타로):
```
- 새 질문 X. [END] 절대 X (다음 턴 hardcap에서 자동 종료).
```

new_string:
```
- 단, 직전 사용자 발화가 미해결 확답/시기 질문이면 출구 문구보다 §"언제·될까·얼마나" 방향 답을 먼저.
- 새 질문 X. [END] 절대 X (다음 턴 hardcap에서 자동 종료).
```

- [ ] **Step 5: 빌드로 문자열 문법 확인**

Run: `npm run build`
Expected: 성공 (경고 무시, 에러 0)

- [ ] **Step 6: Commit**

```bash
git add lib/claude.ts
git commit -m "feat(claude): 종료턴 방향답 우선 + 마무리 초대 미해결 화두 구체화 (C1)"
```

---

### Task 4: prompt_version 갱신

**Files:**
- Modify: `lib/prompt-version.ts`

- [ ] **Step 1: 버전 슬러그 + 히스토리 주석 갱신**.

old_string:
```
//   2026-07-12-persona-tuning — 확답 회피·심문 피로·길이 완화 + [END] 유예 (커밋 3fe4d6b·68934e5)
export const PROMPT_VERSION = "2026-07-12-persona-tuning";
```

new_string:
```
//   2026-07-12-persona-tuning — 확답 회피·심문 피로·길이 완화 + [END] 유예 (커밋 3fe4d6b·68934e5)
//   2026-07-13-conversion-c1  — 더보고싶다 신호·방향먼저·회피구1회·크로스셀 문구·종료턴 방향답 우선 (spec: 2026-07-13-conversion-moment)
export const PROMPT_VERSION = "2026-07-13-conversion-c1";
```

- [ ] **Step 2: Commit**

```bash
git add lib/prompt-version.ts
git commit -m "chore(prompt): PROMPT_VERSION 2026-07-13-conversion-c1"
```

---

### Task 5: QA — `more_cards` 케이스 추가 + 검증 런

**Files:**
- Modify: `qa/cases/tarot.ts`
- 사전 조건: dev 서버 (`npm run dev`) 별도 터미널에서 실행 중, `.env.local` 은 dev Supabase.

- [ ] **Step 1: 케이스 추가** — `qa/cases/tarot.ts` 의 `return cases;` 직전에 삽입.

old_string:
```
  return cases;
}
```

new_string:
```
  // C1 전환 규칙: 카드를 더 뽑아달라는 구매 의사 신호를 말리지 않고 경로+선택권으로 안내하는지.
  cases.push({
    id: "tarot.three_card.more_cards",
    product: { kind: "tarot", spreadType: "three_card", spreadCategory: "love" },
    emotion: "그 사람 마음이 궁금해",
    seed: {},
    seedConcern: "헤어진 사람한테 연락이 올까? 계속 생각나",
    userPersona:
      "풀이를 듣다가 '카드 더 뽑아줘', '타로 새로 봐줘'를 두 번 이상 요청하는 사용자",
    inputStyle: { tone: "간절한 반말", habits: [] },
    maxTurns: 5,
    expects: {
      mustEnd: false,
      expectSensitiveHeader: false,
      expectCardCount: 3,
      skipEndAssertion: true,
    },
  });
  return cases;
}
```

- [ ] **Step 2: 케이스 수집 스모크** — 필터가 새 케이스를 잡는지 확인.

Run: `node --import tsx -e "import('./qa/cases/index.ts').then(m => console.log(m.collectCases({ caseKey: 'more_cards' }).map(c => c.id)))"`
Expected: `[ 'tarot.three_card.more_cards' ]`

- [ ] **Step 3: more_cards 런**

Run: `npm run qa -- --case=more_cards`
Expected: 케이스 1개 실행, `qa/out/<runId>/tarot.three_card.more_cards.json` 생성.

- [ ] **Step 4: 트랜스크립트 수동 검증** — 생성된 json 의 assistant 응답에서:
  - "카드 더 뽑아줘" 요청에 말리는 문구("충분히 들여다본", "더 뽑는다고 명확해지는") **없음**
  - 경로 안내("이어가기" 또는 "새 상담") + 선택권 문구("마저 풀어도 되고" 류) **있음**
  - 별/가격 언급 **없음**

- [ ] **Step 5: definitive_pressure 런 (어순 반전·회피구 1회 검증)**

Run: `npm run qa -- --product=tarot:three_card --case=definitive_pressure`
Expected: 실행 완료. 트랜스크립트에서:
  - 확답 질문에 대한 각 응답의 **첫 문장이 회피가 아님** (방향 먼저)
  - "못 찍어/정해줄 수 없어" 류 회피구가 **전체 대화에 최대 1회**

- [ ] **Step 6: Commit**

```bash
git add qa/cases/tarot.ts
git commit -m "test(qa): more_cards 구매의사 신호 케이스 추가 (C1)"
```

---

### Task 6: dev 배포 + 사용자 검증 핸드오프

- [ ] **Step 1: 최종 빌드**

Run: `npm run build`
Expected: 에러 0

- [ ] **Step 2: dev push**

```bash
git push origin dev
```

- [ ] **Step 3: 사용자 검증 안내** — dev.byeolkongtalk.com 에서 타로 상담 1회: 확답 질문("연락 올까?") → 방향-먼저 응답 확인, "카드 더 뽑아줘" → 말리지 않는지 확인. prod(main ff)는 사용자 승인 후.

- [ ] **Step 4: 검증 통과 시 Supabase readings.prompt_version 확인** — 새 리딩에 `2026-07-13-conversion-c1` 스탬프가 찍히는지 (SQL: `SELECT prompt_version, created_at FROM readings ORDER BY created_at DESC LIMIT 3;`)

---

## Self-Review 체크

- **Spec coverage (C1 스코프)**: ①(Task 1·2 Step 1) ②(Task 1·2 Step 2-4) ③(Task 1 Step 5, Task 2 Step 3) ④(Task 3) prompt_version(Task 4) 누출 방어(Task 1·2 마지막 Step) — 전부 매핑됨. C1 제외 항목([RECO:] 마커·clarifier·결과 화면 예고)은 의도적 C2/C3.
- **Placeholder scan**: 모든 편집 스텝에 old/new 실제 텍스트 포함. TBD 없음.
- **일관성**: §"언제·될까·얼마나" 참조가 페르소나·claude.ts 가이드에서 동일 명칭. PROMPT_VERSION 슬러그 = QA·검증 스텝과 일치.
