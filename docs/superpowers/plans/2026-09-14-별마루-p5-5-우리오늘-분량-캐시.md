# 별마루 P5-5 — 우리 오늘 분량·캐시·셀 태그 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** "우리 오늘"이 스스로 어기고 있던 depth 원칙(무료 = 유료의 20~30%)을 바로잡는다 — 무료 한 줄 ~50자 → **정적 뱅크 ~350자**, 유료 ~600자 → **~1,200자**. 거기에 매 호출 재생성되던 `pair-narrative` 에 **(유저, 상대, 날짜) 일별 캐시**를 달고, 우리 달력 셀에 **끌림·결속·삐걱 마크**를 띄우고, `/byeolmaru/woori` 의 옛 CTA·티저를 **P5-4 미끼(`woori_30d`)로 통일**한다.

**Architecture:** 무료는 **룰 100%·변동비 0** — `getSajuTaste`(나 탭)와 **같은 구조**의 슬롯 뱅크(`data/byeolmaru/pair-taste.json` + `getPairTaste`)를 만들어 `getPairStaticLine` 을 대체한다. 유료는 프롬프트 분량 지시 + 토큰 상한만 올리고(모델·경로 무변경), 앞에 캐시를 세워 **일관성(같은 날 같은 서술)과 속도(실측 5.1초 → 캐시 히트 시 DB 1회)**를 얻는다(원가가 아니라 이 둘이 근거 — 스펙 §8). 셀 마크는 **나 탭과 같은 글리프 문법**을 재사용한다 — 판정 primitive 가 실제로 같기 때문이다(끌림=천간합 ✧ · 결속=육합 ◇ · 삐걱=충 △).

**Tech Stack:** Next.js 16 App Router · React 19 · TypeScript strict · Tailwind v4 `@theme` · `node --test`(tsx) · Supabase(마이그레이션 1장) · gpt-5-nano(`generateOnce`)

---

## 🔴 이 플랜을 새 세션에서 시작할 때 (인수인계)

**작성 시점: 2026-09-14.** P5-1 ~ P5-4 가 **origin/dev 에 푸시 완료**(마지막 `4137d25`)이고, Preview 검증은 사용자 대기 중이다. **prod(main) 미배포** — 배포는 사용자의 명시 "go" 전까지 금지.

### 먼저 읽을 것
- 메모리 `byeolmaru-p5-spec` — P5-1~4 결과와 **교훈**(특히 "범위를 바꾸면 그걸 전제한 계산·문구가 조용히 틀린다", "`truncate` 는 오버플로 0 단정을 무력화한다", "정적 카피에 '오늘'을 박지 마라")
- 스펙 `docs/superpowers/specs/2026-09-12-별마루-p5-화면개편-design.md` **§8(우리 오늘)·§9(페이월)·§13(계측)** — Grep 으로 해당 절만(326행짜리다)
- 직전 플랜 `docs/superpowers/plans/2026-09-13-별마루-p5-4-페이월-구독시트.md` — 미끼(`PremiumBlock`/`bait.ts`)가 어떻게 생겼는지

### 현재 화면 (P5-4 결과)
```
/byeolmaru 허브     header · 달력 판(출석 스트립 · 오늘 히어로 · 인연 칩 · 격자 · 미끼) · 무료 목록 3종 · 낙수
/byeolmaru/saju     격자 + 상세(무료 taste ~450자) + 미끼 또는 리포트(~2,100자) + 주차 요약
/byeolmaru/tarot    오늘의 카드(뽑기 · 정적 taste ~350자 · 미끼 또는 LLM ~800자)
/byeolmaru/woori    상대 토글 + 격자 + 상세(무료 한 줄 ~50자 ★ 또는 LLM ~600자 ★) + 옛 CTA ★  ← P5-5 가 고치는 자리
```

### 🔴 이 리포의 함정 (실제로 물린 것들)
- **다른 Claude 세션이 같은 브랜치에 동시에 커밋한다.** `git add` 로 스테이징하면 그 세션 커밋에 섞인 사고가 실제로 났다 → **pathspec 커밋만**(`git commit <경로> -m "..."`), 직후 `git show --stat HEAD` 확인
- **테스트 명령에 `-not -path "./.claude/*"` 를 반드시 붙인다** — 다른 세션의 격리 워크트리가 무관한 실패를 대량으로 만든다
- **`qa/*.test.ts` 3종은 `--env-file=.env.local` 없이는 `CLAUDE_API_KEY` 로 실패한다** — 코드 결함으로 오판하지 말 것(그래서 아래 find 가 `./qa/*` 를 제외한다)
- **`npm run dev` 를 Bash 로 새로 띄우지 마라** — 이미 떠 있고 다른 세션의 활성 탭이 있을 수 있다
- **브라우저 쿠키가 오리진 단위로 탭 간 공유된다** — 세션 주입 검증 중 **사용자 탭 로그인을 덮어쓴 사고**가 났다. 주입 전 기존 값을 캡처하고 끝나면 복원할 것
- **Bash 한 번에 보내는 명령이 너무 길면 `ENAMETOOLONG` 으로 죽는다**(Windows) — 큰 파일은 Write 툴로 만들 것
- dev DB 테스트 데이터: QA봇(`11111111-1111-4111-8111-111111111111`)에 생년월일 프로필 + 상대 "테스트상대"(비구독)가 등록돼 있다

### 결정된 것 (다시 묻지 말 것)
- **셀 태그는 이모지(✨🔗)가 아니라 나 탭과 같은 단색 글리프 ✧◇△ + 관계 어휘 라벨**(사용자 확인 2026-09-14). 8px 셀에서 이모지는 형태가 뭉개지고 고유색이 골드 셀과 부딪힌다. 스펙 §8 의 `✨끌림 🔗결속` 은 어휘를 가리킨 표기지 글리프 지정이 아니다
- **삐걱(충)도 셀에 그린다**(사용자 확인 2026-09-14) — 나 탭이 이미 △충을 셀에 그리므로 두 탭이 대칭이 된다
- **유료 우리 오늘은 1,200자까지만** — 오늘 사주(2,100자)와 같은 급으로 키우지 않는다(하루 4,200자면 둘 다 안 읽히고 "오늘의 본편" 위계가 사라진다, 스펙 §8)
- **캐시의 근거는 원가가 아니다** — 회당 ₩1 수준이라 매번 돌려도 된다. 근거는 ①일관성(칩을 다시 눌렀는데 서술이 바뀌면 신뢰가 깨진다) ②속도(실측 5.1초)

---

## 스코프

**건드리는 것:**
- `lib/byeolmaru/pair-day.ts`(`pairMarks` 추가 · `getPairStaticLine`/`STATUS_FRAME` 제거)
- `data/byeolmaru/pair-taste.json`(신규) · `lib/byeolmaru/static-lines.ts`(`getPairTaste` 추가)
- `components/byeolmaru/PairDayDetailCard.tsx` · `WooriTodayView.tsx` · `ByeolmaruHub.tsx`(마크 배선 1줄)
- `lib/byeolmaru/narrative-prompt.ts`(우리 프롬프트 분량 + 전용 토큰 상한)
- `lib/byeolmaru/pair-narrative.ts`(신규 캐시 래퍼) · `app/api/byeolmaru/pair-narrative/route.ts`
- `supabase/migrations/20260914000000_byeolmaru_pair_narrative.sql`(신규)
- 테스트: `lib/byeolmaru/pair-day.test.ts` · `static-lines.test.ts` · `narrative-prompt.test.ts`

**🚫 건드리지 않는 것:**
- **판정 엔진**(`pairDayScoreAndTags`·가중치·임계치) — 분량과 표시만 바꾼다. score/tone 이 바뀌면 P5-2 무료선·달력 색이 전부 흔들린다
- 무료선(`splitByFreeLine`) · 달력 범위 · 출석 · 히어로 · 무료 목록 — P5-2·P5-3 결과
- `PremiumBlock` 내부 · `bait.ts` 카피 — P5-4 결과(woori 는 **쓰기만** 한다)
- 나 탭 `getSajuTaste`·`DayDetailCard`·`daily-report` — 무회귀
- `/api/byeolmaru/narrative`(나 1인 서술)와 `buildTeaserLine` — **클라 소비자가 없는 선재 부채**다. P5-5 범위 밖(지우는 것도 별건)
- `app/fortune/*` · 홈 · 마이 · 하단탭

## 사전 지식

- 테스트 `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")` · 타입체크 `npx tsc --noEmit` · 빌드 `npm run build`
- 디자인 토큰은 `app/globals.css` `@theme` 이 정본. **새 토큰을 만들지 않는다**
- 별콩이 화법: 반말(해체) · 따뜻함 · **단정적 예언 금지**(흐름·가능성·선택) · 불안 자극·운명론적 협박 금지 · 별표/제목/마커 금지
- 마이그레이션은 `dev` push 시 Supabase dev 에 **자동 적용**된다(수동 적용 금지). 새 테이블은 `ENABLE RLS` + `REVOKE ALL ... FROM PUBLIC, anon, authenticated` + `GRANT ALL ... TO service_role` 3종 세트
- 주석은 "왜"를 남기는 밀도 높은 한글. 커밋 메시지도 한글

## 현재 코드 (바꾸기 전)

`lib/byeolmaru/pair-day.ts` 끝 — **이 두 개가 사라진다**:
```ts
const STATUS_FRAME: Record<RelationshipStatus, string> = { onesided: "짝사랑이라 마음이 더 쓰이지만, ", ... };
export function getPairStaticLine(cell: PairDayCell, status?: RelationshipStatus | null): string { ... }  // 한 줄 ~50자
```

`components/byeolmaru/PairDayDetailCard.tsx` 비자격 분기 — **이게 바뀐다**:
```tsx
{staticLine ? <p ...>{staticLine}</p> : null}
<p className="... [mask-image:linear-gradient(#000,transparent)] opacity-60">별콩이가 둘 사이 오늘을 풀어주고, 이번 달 전체 흐름까지 펼쳐주면…</p>
{!trialUsed ? <button ...>3일 무료 체험 시작</button> : <button ...>구독하고 우리 오늘 이번 달 전체 보기</button>}
```

---

## Task 1: 우리 달력 셀에 끌림·결속·삐걱 마크

**Files:**
- Modify: `lib/byeolmaru/pair-day.ts` (파일 끝에 추가)
- Modify: `components/byeolmaru/ByeolmaruHub.tsx` (`gridCells` 의 pair 분기 — `marks: []` → `pairMarks(c.tags)`)
- Modify: `components/byeolmaru/WooriTodayView.tsx` (`pairGridCells` — 동일)
- Test: `lib/byeolmaru/pair-day.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/byeolmaru/pair-day.test.ts` 의 import 줄에 `pairMarks`, `type PairDayTags` 를 추가하고 파일 끝에 붙인다:

```ts
test("pairMarks: 끌림 ✧ · 결속 ◇ · 삐걱 △ — 순서 고정, lead 는 마크가 아니다", () => {
  const tg = (p: Partial<PairDayTags>): PairDayTags => ({ spark: false, bond: false, friction: false, lead: null, ...p });
  assert.deepEqual(pairMarks(tg({})), []);
  assert.deepEqual(pairMarks(tg({ spark: true })), [{ glyph: "✧", label: "끌림" }]);
  assert.deepEqual(pairMarks(tg({ bond: true })), [{ glyph: "◇", label: "결속" }]);
  assert.deepEqual(pairMarks(tg({ friction: true })), [{ glyph: "△", label: "삐걱" }]);
  // 셀은 첫 마크만 그린다(겹침 실측) — 배열 순서가 곧 우선순위다. 나 탭 dayMarks 와 같은 순서.
  assert.deepEqual(pairMarks(tg({ spark: true, bond: true, friction: true })).map((m) => m.glyph), ["✧", "◇", "△"]);
  // lead 는 두 사람 점수 비교지 "그날의 원인"이 아니다 — 마크로 만들면 끌림을 셀에서 밀어낸다.
  assert.deepEqual(pairMarks(tg({ lead: "me" })), []);
  assert.deepEqual(pairMarks(tg({ lead: "partner" })), []);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/byeolmaru/pair-day.test.ts`
Expected: FAIL — `pairMarks` 가 export 되지 않음

- [ ] **Step 3: `pairMarks` 구현**

`lib/byeolmaru/pair-day.ts` 상단 import 에 추가:
```ts
import type { DayMark } from "./day-label.ts";
```
파일 끝에 추가:
```ts
/** 우리 셀 마크 — 나 탭(`dayMarks`)과 **같은 글리프 문법**을 쓴다. 판정 primitive 가 실제로 같기
 *  때문이다(끌림=천간합 ✧ · 결속=육합 ◇ · 삐걱=충 △ — 위 pairDayScoreAndTags 를 보라).
 *  🔴 이모지(✨🔗)를 쓰지 않는다: 셀 글리프는 8px 라 이모지 형태가 뭉개지고, 고유색이 좋은 날의
 *     골드 배경과 부딪힌다. 스펙 §8 의 `✨끌림 🔗결속` 은 **어휘** 지정이지 글리프 지정이 아니다.
 *  🔴 lead 는 마크가 아니다 — 두 사람 점수 차이라 "그날의 원인"이 아니고, 셀은 마크를 1개만
 *     그리므로(겹침 실측) 리드가 끌림을 밀어낸다. 리드는 상세 카드 칩으로만 남는다. */
export function pairMarks(tags: PairDayTags): DayMark[] {
  const out: DayMark[] = [];
  if (tags.spark) out.push({ glyph: "✧", label: "끌림" });
  if (tags.bond) out.push({ glyph: "◇", label: "결속" });
  if (tags.friction) out.push({ glyph: "△", label: "삐걱" });
  return out;
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/byeolmaru/pair-day.test.ts`
Expected: PASS (기존 테스트 포함 전부)

- [ ] **Step 5: 두 호출부 배선**

`components/byeolmaru/ByeolmaruHub.tsx` — import 를 `import { PAIR_TONE_LABEL, pairMarks } from "@/lib/byeolmaru/pair-day";` 로 바꾸고, `gridCells` 블록과 그 위 주석을 교체한다(옛 주석이 "우리 셀엔 마크가 없다"고 말하고 있어 그대로 두면 거짓이 된다):
```tsx
  // 격자에 넘길 정규화 셀. 나·우리 두 판정 엔진이 같은 GridCell 계약으로 수렴한다.
  // 🔴 우리 셀 마크는 나 탭과 같은 글리프 문법이다(P5-5 pairMarks) — 판정 primitive 가 같아서
  //    ✧천간합→끌림 · ◇육합→결속 · △충→삐걱 으로 라벨만 관계 어휘로 바뀐다.
  const gridCells: GridCell[] = isPair
    ? pairCells.map((c) => ({ date: c.date, ganji: c.ganji, tone: c.tone, label: PAIR_TONE_LABEL[c.tone], isToday: c.isToday, marks: pairMarks(c.tags) }))
    : data.cells.map((c) => ({ date: c.date, ganji: c.ganji, tone: c.grade.tone, label: c.grade.label, isToday: c.isToday, marks: c.marks }));
```

`components/byeolmaru/WooriTodayView.tsx` — import 를 `import { PAIR_TONE_LABEL, pairMarks } from "@/lib/byeolmaru/pair-day";` 로 바꾼다(이 줄의 `getPairStaticLine` 은 Task 3 에서 정리한다. 지금 지우면 아래 호출부가 깨지니 **이 단계에선 남겨둔다**):
```tsx
  const pairGridCells: GridCell[] = pairData
    ? pairData.cells.map((c) => ({ date: c.date, ganji: c.ganji, tone: c.tone, label: PAIR_TONE_LABEL[c.tone], isToday: c.isToday, marks: pairMarks(c.tags) }))
    : [];
```

- [ ] **Step 6: 타입체크 + 전체 테스트**

Run: `npx tsc --noEmit`
Expected: 에러 0

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")`
Expected: 전부 pass (기준선 655 + 1)

- [ ] **Step 7: 커밋**

```bash
git commit lib/byeolmaru/pair-day.ts lib/byeolmaru/pair-day.test.ts components/byeolmaru/ByeolmaruHub.tsx components/byeolmaru/WooriTodayView.tsx -m "feat(byeolmaru): 우리 달력 셀에 끌림·결속·삐걱 마크 (P5-5 T1)" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: 무료 우리 오늘 ~350자 정적 뱅크

**Files:**
- Create: `data/byeolmaru/pair-taste.json`
- Modify: `lib/byeolmaru/static-lines.ts`
- Modify: `lib/byeolmaru/pair-day.ts` (`STATUS_FRAME`·`getPairStaticLine` 삭제)
- Test: `lib/byeolmaru/static-lines.test.ts` (추가) · `lib/byeolmaru/pair-day.test.ts` (옛 테스트 2개 삭제)

**설계:** 슬롯 4개를 날짜 시드로 조합한다 — `signal`(오늘 둘 사이 결) + `relation`(관계 유형 프레이밍) + `lead`(누가 앞서나) + `advice`(마무리 한 줄). `getSajuTaste` 와 **같은 구조·같은 mix32 로테이션**이다. 상대 이름은 **넣지 않는다** — 이름을 템플릿에 끼우면 조사(와/과, 이/가)가 받침에서 틀린다(P5-4 에서 실제로 물렸다).

- [ ] **Step 1: 뱅크 JSON 생성**

`data/byeolmaru/pair-taste.json` 를 **아래 내용 그대로** 만든다. 자수가 Step 2 테스트 밴드에 맞춰 실측된 원고다 — 임의로 고치지 말 것. (파일이 커서 Bash 히어독이 `ENAMETOOLONG` 으로 죽을 수 있다 → **Write 툴로 만들 것**.)

```json
{
  "_note": "우리 오늘 무료 taste 뱅크(P5-5). 슬롯 4개(signal·relation·lead·advice)를 날짜 시드로 조합해 ~350자. 룰 100%·변동비 0. signal 키 우선순위 = friction > spark_bond > spark > bond > tone(good/normal/caution). 상대 이름은 넣지 않는다(조사 오류 방지) — '그 사람'으로 받는다.",
  "signal": {
    "friction": [
      "오늘은 둘 사이 결이 살짝 엇갈릴 수 있는 날이야. 같은 말도 오늘따라 다르게 닿을 수 있어서, 서둘러 결론을 내기보다 한 박자 늦게 말하는 게 훨씬 나아. 부딪히는 건 사이가 나빠서가 아니라 오늘 기운이 서로를 밀어보는 쪽으로 도는 거야.",
      "오늘 흐름은 둘을 조금 팽팽하게 당겨놔. 별것 아닌 말에도 날이 설 수 있으니, 지금 꼭 정해야 하는 얘기가 아니면 내일로 미뤄도 괜찮아. 한 사람이 먼저 숨을 고르면 그 자리에서 금방 풀리는 결이야."
    ],
    "spark_bond": [
      "오늘은 끌림도 결속도 같이 도는 날이야. 마음이 먼저 움직이고 그 뒤를 편안함이 따라와서, 애쓰지 않아도 대화가 자연스럽게 이어져. 가까워지고 싶은 마음이 있다면 오늘은 그 마음을 조금 드러내도 괜찮은 결이야.",
      "서로에게 눈이 가는 기운과 오래 붙어 있고 싶은 기운이 함께 도는 날이야. 새로 시작하는 이야기든 하던 이야기든 오늘은 한 겹 더 깊이 들어가기 쉬워. 욕심내지 않아도 알아서 가까워지는 날이라 마음 편히 둬도 돼."
    ],
    "spark": [
      "오늘은 둘 사이에 끌림이 도는 날이야. 평소 같았으면 지나쳤을 말투나 표정 하나가 오늘따라 마음에 남을 수 있어. 큰 걸 하지 않아도 돼 — 먼저 연락하거나 가볍게 웃어 보이는 정도로도 오늘 기운은 충분히 흘러.",
      "마음이 먼저 반응하는 날이야. 생각보다 감정이 반 박자 빨리 움직여서 오늘 오간 한마디가 꽤 오래 남을 수 있어. 재고 따지기보다 솔직한 쪽이 오늘의 너희한테는 더 잘 어울리는 옷이야. 대신 한 번에 다 쏟지는 말고."
    ],
    "bond": [
      "오늘은 서로 편안하게 이어지는 결이야. 두근거림보다는 곁에 있는 게 익숙하고 순한 쪽이라, 특별한 이벤트 없이 같이 보내는 시간이 제일 잘 맞아. 오래 가는 사이는 이런 날에 조용히 쌓여.",
      "붙어 있는 게 자연스러운 날이야. 말수가 적어도 어색하지 않고, 하던 얘기를 이어가기에도 좋아. 오늘은 새로 벌이기보다 이미 있는 걸 한 번 더 다정하게 확인하는 쪽이 결에 맞아. 그 확인이 생각보다 오래 남아."
    ],
    "good": [
      "특별한 신호가 크게 뜨진 않아도 오늘 둘 사이 흐름은 순한 편이야. 서로 무리하지 않아도 대화가 부드럽게 굴러가서, 미뤄뒀던 얘기를 꺼내기에도 나쁘지 않아. 조용히 좋은 날이라는 게 이런 날이야.",
      "오늘은 큰 파도 없이 잔잔하게 좋은 결이야. 뭔가를 증명하거나 확인하려 애쓰지 않아도 서로 편한 자리에 그대로 있을 수 있어. 이런 날의 여유가 나중에 생각보다 크게 남더라 — 오늘은 그냥 누려도 돼."
    ],
    "normal": [
      "오늘 둘 사이는 무난하게 흐르는 날이야. 확 끌리지도 크게 부딪히지도 않는 결이라, 평소 하던 대로 지내면 그게 제일 맞아. 굳이 오늘 뭔가를 정하려 하지 않아도 괜찮고, 그냥 지나가는 하루도 사이엔 필요해.",
      "특별한 기울기 없이 평평하게 지나가는 날이야. 잔잔한 건 사이가 식었다는 뜻이 아니라 서로 숨을 고르는 시간이라는 뜻이기도 해. 오늘은 가볍게 안부 정도만 오가도 충분하고, 그걸로도 결은 이어져."
    ],
    "caution": [
      "오늘은 서로 조금 챙겨주면 좋은 결이야. 크게 어긋날 일은 없어도 각자 밖에서 쓰고 온 기운이 남아 있어서, 사소한 데서 서운함이 생기기 쉬워. 잘잘못을 가리기보다 컨디션을 먼저 물어봐줘.",
      "둘 다 조금 지쳐 있는 날일 수 있어. 상대의 반응이 평소보다 밋밋해도 마음이 변한 거라고 읽지 않는 게 오늘은 맞아. 크게 다가가기보다 서로 쉬게 두는 쪽이 오늘의 사이를 지켜줘."
    ]
  },
  "relation": {
    "onesided": [
      "짝사랑이라 오늘은 상대의 반응 하나에 마음이 크게 출렁일 수 있어. 그 사람 속을 읽어내려 애쓰기보다, 네 마음이 어디까지 가고 싶은지를 먼저 봐주면 좋겠어.",
      "혼자 품는 마음은 작은 신호도 크게 키워서 보게 만들어. 오늘 오간 게 있다면 의미를 확정하지 말고 기분 좋은 일 하나로 남겨둬 — 아직 답을 낼 단계는 아니야."
    ],
    "crush": [
      "썸이라 설레는 만큼 오늘 오가는 신호가 다 커 보일 거야. 확인하고 싶어져도 오늘 당장 관계에 이름을 붙이려 하지 않는 게 좋아 — 흐름은 아직 만들어지는 중이야.",
      "아직 선이 안 그어진 사이라 오늘은 한 걸음이 크게 느껴져. 먼저 다가가는 것도, 하루 기다려보는 것도 다 괜찮은 선택이야. 서두르지만 않으면 결은 알아서 또렷해져."
    ],
    "dating": [
      "연애 중이라 오늘의 결은 큰 사건보다 말투와 타이밍으로 나타날 거야. 잘 지내다가도 한마디에 온도가 바뀌는 사이니까, 오늘은 표현을 조금 더 친절한 쪽으로 골라봐.",
      "이미 이어져 있는 사이라 오늘 새로 증명할 건 없어. 익숙해서 그냥 지나치던 걸 한 번 말로 꺼내주는 정도가 오늘 제일 잘 닿는 결이야. 크게 안 해도 돼."
    ],
    "breakup": [
      "지난 사이라 오늘 흐름은 마음보다 기억을 먼저 건드릴 수 있어. 떠오르는 게 있어도 그게 곧 다시 시작하라는 신호는 아니야 — 오늘은 판단하지 말고 지나가게 둬도 돼.",
      "끝난 관계에도 결은 남아서 가끔 이렇게 울려. 연락하고 싶어진다면 그 마음이 그 사람 때문인지 지금의 네 허전함 때문인지만 한 번 구분해보면 좋겠어."
    ],
    "unknown": [
      "둘이 어떤 사이인지는 네가 제일 잘 알 거야. 오늘 결을 읽을 때도 정답을 찾기보다, 지금 너희 거리에 맞게 가져다 쓰면 그게 제일 잘 맞아.",
      "관계마다 같은 기운도 다르게 떨어져. 오늘 흐름은 참고로만 두고, 너희가 지금 어디쯤 있는지를 먼저 떠올려보면 훨씬 또렷하게 읽힐 거야."
    ]
  },
  "lead": {
    "me": [
      "오늘은 네 쪽 기운이 조금 더 앞서. 먼저 말을 꺼내거나 분위기를 여는 쪽이 자연스럽게 너일 수 있어 — 무겁게 끌지 말고 가볍게 두드리는 정도면 충분해.",
      "흐름의 핸들이 오늘은 네게 있어. 네가 편한 쪽으로 방향을 잡아도 되는 날이라, 하고 싶었던 얘기가 있었다면 오늘 꺼내기 나쁘지 않아."
    ],
    "partner": [
      "오늘은 그 사람 쪽 기운이 조금 더 앞서. 먼저 연락이 오거나 분위기를 여는 쪽이 상대일 수 있으니, 네가 다 끌고 가려 하지 않아도 돼.",
      "오늘 움직임은 상대 쪽에서 먼저 나올 가능성이 있어. 조급하게 재촉하기보다 반응할 자리를 비워두면 결이 더 자연스럽게 이어져. 기다리는 것도 오늘은 하나의 선택이야."
    ],
    "even": [
      "오늘은 어느 한쪽이 끌고 가는 날은 아니야. 둘 다 비슷한 온도라 누가 먼저냐를 따지기보다 오는 대로 주고받으면 딱 맞는 결이야.",
      "기운이 한쪽으로 기울지 않은 날이야. 먼저 연락하든 기다리든 크게 달라지지 않으니, 오늘은 네가 편한 쪽을 골라도 괜찮아. 어느 쪽이든 결은 비슷해."
    ]
  },
  "advice": {
    "good": [
      "좋은 결일수록 당연하게 넘기기 쉬워 — 오늘 고마운 게 있었다면 그냥 말로 해줘.",
      "오늘처럼 순한 날의 기억은 나중에 흔들릴 때 꺼내 쓸 수 있어. 사소해도 하나 남겨둬."
    ],
    "normal": [
      "잔잔한 날엔 크게 뭘 안 해도 돼. 사이는 이런 날들에 조용히 쌓이는 거니까.",
      "오늘 아무 일 없이 지나갔다면 그것도 나쁜 신호가 아니야 — 그냥 평범한 하루야."
    ],
    "caution": [
      "오늘 어긋나도 사이가 상한 건 아니야. 결이 잠깐 미끄러졌을 뿐이니 너무 크게 읽지 마.",
      "챙길 날이라고 겁낼 것 없어. 조금 천천히 말하는 것만으로도 충분히 지나갈 결이야."
    ]
  }
}
```

- [ ] **Step 2: 실패하는 테스트 작성**

`lib/byeolmaru/static-lines.test.ts` 상단 import 블록을 늘린다:
```ts
import { getCardLine, getCardTaste, getSkeletonLine, getSajuTaste, getPairTaste } from "./static-lines.ts";
import pairTasteBank from "@/data/byeolmaru/pair-taste.json";
import type { PairDayTags, PairTone } from "./pair-day.ts";
import type { RelationshipStatus } from "@/lib/relationship/types";
```
파일 끝에 추가(`assertClean` 은 이 파일 상단에 이미 있는 헬퍼다):
```ts
// ── P5-5 우리 오늘 무료 taste ──────────────────────────────────────────────
const PAIR_TONES: PairTone[] = ["good", "normal", "caution"];
const STATUSES: (RelationshipStatus | null)[] = ["crush", "dating", "breakup", "onesided", null];
const tg = (p: Partial<PairDayTags>): PairDayTags => ({ spark: false, bond: false, friction: false, lead: null, ...p });
const TAG_CASES: PairDayTags[] = [
  tg({}),
  tg({ spark: true, lead: "me" }),
  tg({ bond: true, lead: "partner" }),
  tg({ spark: true, bond: true }),
  tg({ friction: true, lead: "me" }),
  tg({ spark: true, friction: true, lead: "partner" }),
];

test("pair-taste: 모든 (tone × tags × status) 조합이 4슬롯을 채우고 금지문자가 없다", () => {
  for (const tone of PAIR_TONES) {
    for (const tags of TAG_CASES) {
      for (const status of STATUSES) {
        const t = getPairTaste(tone, tags, status, "2026-09-14");
        for (const [slot, v] of Object.entries(t)) {
          assert.ok(v.trim().length > 0, `${tone}/${status}/${slot} 비어있음`);
          assertClean(v, `${tone}/${status}/${slot}`);
        }
      }
    }
  }
});

test("pair-taste: 슬롯 자수 밴드와 합계 — 무료는 ~350자다(유료 1,200자의 29%)", () => {
  // 실측 밴드(저작 원고): signal 101~134 · relation 76~93 · lead 72~92 · advice 43~49.
  // 🔴 밴드를 넓히지 말고 문장을 고쳐라 — 이 규율이 무료/유료 depth 비율(스펙 §8)을 지킨다.
  for (const tone of PAIR_TONES) {
    for (const tags of TAG_CASES) {
      for (const status of STATUSES) {
        const t = getPairTaste(tone, tags, status, "2026-09-14");
        assert.ok(t.signal.length >= 95 && t.signal.length <= 140, `signal 자수 ${t.signal.length}`);
        assert.ok(t.relation.length >= 70 && t.relation.length <= 100, `relation 자수 ${t.relation.length}`);
        assert.ok(t.lead.length >= 68 && t.lead.length <= 95, `lead 자수 ${t.lead.length}`);
        assert.ok(t.advice.length >= 40 && t.advice.length <= 55, `advice 자수 ${t.advice.length}`);
        const total = t.signal.length + t.relation.length + t.lead.length + t.advice.length;
        assert.ok(total >= 290 && total <= 370, `합계 ${total}자`);
      }
    }
  }
});

test("pair-taste: signal 키 우선순위 friction > 끌림+결속 > 끌림 > 결속 > tone", () => {
  const bank = pairTasteBank as unknown as { signal: Record<string, string[]> };
  const pick = (tone: PairTone, tags: PairDayTags) => getPairTaste(tone, tags, null, "2026-09-14").signal;
  // 삐걱이 있으면 다른 신호가 같이 떠도 friction 뱅크에서 나온다(옛 getPairStaticLine 규칙 계승).
  assert.ok(bank.signal.friction.includes(pick("good", tg({ friction: true, spark: true, bond: true }))));
  assert.ok(bank.signal.spark_bond.includes(pick("caution", tg({ spark: true, bond: true }))));
  assert.ok(bank.signal.spark.includes(pick("caution", tg({ spark: true }))));
  assert.ok(bank.signal.bond.includes(pick("caution", tg({ bond: true }))));
  // 신호가 하나도 없을 때만 톤 뱅크로 떨어진다.
  for (const tone of PAIR_TONES) assert.ok(bank.signal[tone].includes(pick(tone, tg({}))));
});

test("pair-taste: 결정론 — 같은 (tone,tags,status,date) 는 늘 같은 조합", () => {
  const a = getPairTaste("good", tg({ spark: true, lead: "me" }), "dating", "2026-09-14");
  const b = getPairTaste("good", tg({ spark: true, lead: "me" }), "dating", "2026-09-14");
  assert.deepEqual(a, b);
});

test("pair-taste: 30일 로테이션이 슬롯마다 variant 를 다 쓴다(같은 문장 반복 완화)", () => {
  const seen = { signal: new Set<string>(), relation: new Set<string>(), lead: new Set<string>(), advice: new Set<string>() };
  for (let d = 1; d <= 30; d++) {
    const t = getPairTaste("normal", tg({ spark: true, lead: "me" }), "crush", `2026-09-${String(d).padStart(2, "0")}`);
    seen.signal.add(t.signal); seen.relation.add(t.relation); seen.lead.add(t.lead); seen.advice.add(t.advice);
  }
  for (const [slot, s] of Object.entries(seen)) assert.ok(s.size >= 2, `${slot} 30일간 ${s.size}종`);
});

test("pair-taste: 상대 이름을 문장에 끼우지 않는다(받침 조사 사고 방지)", () => {
  // 🔴 JSON 전체를 stringify 해서 "{" 를 찾으면 **객체 중괄호 때문에 늘 참**이다(그 테스트는
  //    버그를 실행하면서 통과한다 — P5-4 의 `.includes("지민")` 전례). 문장만 꺼내서 본다.
  const bank = pairTasteBank as unknown as Record<string, unknown>;
  const lines: string[] = [];
  for (const [slot, group] of Object.entries(bank)) {
    if (slot === "_note" || typeof group !== "object" || group === null) continue;
    for (const arr of Object.values(group as Record<string, string[]>)) lines.push(...arr);
  }
  assert.equal(lines.length, 36, "슬롯 4개(14+10+6+6)가 다 읽혔는지 — 키가 빠지면 아래 루프가 무의미해진다");
  for (const s of lines) {
    assert.ok(!s.includes("{"), `자리표시자 금지: ${s.slice(0, 20)}…`);
    assert.ok(!s.includes("$"), `템플릿 리터럴 금지: ${s.slice(0, 20)}…`);
  }
});
```

- [ ] **Step 3: 실패 확인**

Run: `node --import tsx --test lib/byeolmaru/static-lines.test.ts`
Expected: FAIL — `getPairTaste` 가 없음

- [ ] **Step 4: `getPairTaste` 구현**

`lib/byeolmaru/static-lines.ts` 상단 import 에 추가:
```ts
import pairTaste from "@/data/byeolmaru/pair-taste.json";
import type { PairDayTags, PairTone } from "./pair-day.ts";
import type { RelationshipStatus } from "@/lib/relationship/types";
```
`TASTE` 상수 아래에 뱅크 타입을 붙인다:
```ts
// P5-5 우리 오늘 무료 taste — signal(오늘 결) + relation(관계 유형) + lead(누가 앞서나) + advice.
// saju-taste 와 같은 규율로 유니온 Record 를 강제한다 — 새 status/tone 이 생기면 JSON 키를 채우기
// 전까지 tsc 가 깨진다(Record<string> 이면 키 오타가 조용히 "" 로 샌다).
type PairSignalKey = "friction" | "spark_bond" | "spark" | "bond" | PairTone;
const PAIR_TASTE = pairTaste as {
  signal: Record<PairSignalKey, string[]>;
  relation: Record<RelationshipStatus | "unknown", string[]>;
  lead: Record<"me" | "partner" | "even", string[]>;
  advice: Record<PairTone, string[]>;
};
```
파일 끝에 추가:
```ts
export interface PairTaste {
  signal: string;
  relation: string;
  lead: string;
  advice: string;
}

/** signal 뱅크 키 — 옛 getPairStaticLine 의 분기 우선순위를 그대로 계승한다(삐걱이 최우선).
 *  삐걱은 "오늘 조심할 것"이라 같이 뜬 끌림보다 먼저 말해야 한다. */
function pairSignalKey(tone: PairTone, tags: PairDayTags): PairSignalKey {
  if (tags.friction) return "friction";
  if (tags.spark && tags.bond) return "spark_bond";
  if (tags.spark) return "spark";
  if (tags.bond) return "bond";
  return tone;
}

/** 무료 우리 오늘 taste(~350자) — 룰 100%·₩0. 유료 서술(~1,200자)의 29% taste(스펙 §8).
 *  🔴 상대 이름을 받지 않는다: 이름을 문장에 끼우면 받침에 따라 조사가 틀린다(P5-4 실사고).
 *     이름이 필요한 자리는 카드 헤더·미끼가 따로 맡는다.
 *  🔴 slot 시드를 10~13 으로 띄운 이유: getSajuTaste 가 0~4 를 쓴다. 같은 날 나 탭과 우리 탭이
 *     같은 인덱스로 굴러 "둘 다 1번 문장"이 되는 상관을 피한다. */
export function getPairTaste(
  tone: PairTone,
  tags: PairDayTags,
  status: RelationshipStatus | null,
  date: string
): PairTaste {
  const base = hashDate(date);
  const pick = (arr: string[] | undefined, slot: number): string =>
    arr && arr.length ? arr[mix32(base * 31 + slot) % arr.length] : "";
  return {
    signal: pick(PAIR_TASTE.signal[pairSignalKey(tone, tags)], 10),
    relation: pick(PAIR_TASTE.relation[status ?? "unknown"], 11),
    lead: pick(PAIR_TASTE.lead[tags.lead ?? "even"], 12),
    advice: pick(PAIR_TASTE.advice[tone], 13),
  };
}
```

- [ ] **Step 5: 통과 확인**

Run: `node --import tsx --test lib/byeolmaru/static-lines.test.ts`
Expected: PASS (새 6개 포함)

- [ ] **Step 6: 옛 한 줄(`getPairStaticLine`) 제거**

`lib/byeolmaru/pair-day.ts` 에서 `STATUS_FRAME` 상수와 `getPairStaticLine` 함수를 **통째로 삭제**한다. 그러면 이 파일의 `import type { RelationshipStatus } from "@/lib/relationship/types";` 도 쓰이지 않으므로 **같이 삭제**한다(내 변경이 만든 고아는 내가 치운다).

`lib/byeolmaru/pair-day.test.ts` 에서 `getPairStaticLine` 를 쓰는 테스트 **2개**(`"getPairStaticLine: friction 우선 …"`, `"getPairStaticLine: status 있으면 …"`)를 삭제하고 import 목록에서도 `getPairStaticLine` 을 뺀다. 우선순위 계약은 Step 2 의 `pair-taste: signal 키 우선순위` 테스트가 이어받는다.

- [ ] **Step 7: 타입체크 — 남은 호출부가 드러난다**

Run: `npx tsc --noEmit`
Expected: `components/byeolmaru/WooriTodayView.tsx` 에서 `getPairStaticLine` 미존재 에러 1건. **Task 3 에서 고친다** — 여기서 임시 수정하지 말고 바로 Task 3 로 넘어간다(커밋도 Task 3 끝에서 함께한다. 중간 커밋은 tsc 가 깨진 상태를 남긴다).

---

## Task 3: 상세 카드에 무료 taste 렌더 + 칩 통일 + 조사 버그 2건

**Files:**
- Modify: `components/byeolmaru/PairDayDetailCard.tsx`
- Modify: `components/byeolmaru/WooriTodayView.tsx`

**조사 버그(선재 부채, 이 카드 안에 있다):** `{partnerName}와 나` 는 받침 있는 이름에서 "지민와 나"가 되고, `${partnerName}가 리드` 는 "지민가 리드"가 된다. **조사 헬퍼를 새로 만들지 않고 어순·표기로 없앤다** — 제목은 `나와 {partnerName}`(조사가 '나'에 붙어 항상 '와'), 리드 칩은 `{partnerName} 리드`(무조사, `narrative-prompt.ts` 의 `formatPairGoodDay` 가 이미 쓰는 표기와 일치).

- [ ] **Step 1: `PairDayDetailCard` props 교체**

상단 import 와 props 를 바꾼다:
```tsx
import Image from "next/image";
import type { PairDayCell, PairBackdrop } from "@/lib/byeolmaru/pair-day";
import { PAIR_TONE_LABEL, pairMarks } from "@/lib/byeolmaru/pair-day";
import type { PairTaste } from "@/lib/byeolmaru/static-lines";
import { branchAnimal } from "@/lib/byeolmaru/branch-animal";

export default function PairDayDetailCard({
  cell,
  backdrop,
  partnerName,
  entitled,
  taste,
  narrative,
  narrativeLoading,
}: {
  cell: PairDayCell;
  backdrop: PairBackdrop;
  partnerName: string;
  entitled?: boolean;
  /** 비자격자용 무료 taste(~350자, 룰 100%). 자격자는 null — 아래 narrative 가 그 자리를 받는다. */
  taste?: PairTaste | null;
  narrative?: string | null;
  narrativeLoading?: boolean;
}) {
```
> `trialUsed`/`onStartTrial`/`onSubscribe`/`staticLine` prop 은 **사라진다** — 페이월은 Task 4 에서 카드 밖 `PremiumBlock` 이 받는다.

- [ ] **Step 2: 태그 칩을 `pairMarks` 문법으로 통일 + 조사 수정**

`md`/`tags` 계산 블록을 다음으로 교체:
```tsx
  const md = `${Number(cell.date.slice(5, 7))}월 ${Number(cell.date.slice(8, 10))}일`;
  // 칩은 셀 마크와 같은 어휘·같은 글리프를 쓴다(P5-5) — 달력에서 본 ✧ 가 여기서 "끌림"으로 풀린다.
  const marks = pairMarks(cell.tags);
  // 리드는 마크가 아니라 별도 칩(두 사람 점수 비교라 "그날의 원인"이 아니다).
  // 🔴 `${partnerName}가` 는 받침 있는 이름에서 틀린다("지민가") — 무조사 표기로 고정한다
  //    (narrative-prompt.ts 의 formatPairGoodDay 와 같은 표기).
  const leadChip = cell.tags.lead === "me" ? "네가 리드" : cell.tags.lead === "partner" ? `${partnerName} 리드` : null;
```
헤더 제목(조사 수정):
```tsx
          <h2 className="font-display text-lg text-eye-purple">
            {/* 🔴 `{partnerName}와 나` 는 받침에서 틀린다("지민와") — 어순을 뒤집어 조사를 '나'에 붙인다. */}
            나와 {partnerName} · {cell.isToday ? "오늘" : md}
          </h2>
```
칩 목록(기존 `{tags.length > 0 && (<ul …>)}` 블록 교체):
```tsx
      {(marks.length > 0 || leadChip) && (
        <ul className="mb-4 flex flex-wrap gap-2">
          {marks.map((m) => (
            <li key={m.glyph} className="rounded-full border border-lilac-mid px-3 py-1 text-xs text-eye-purple">
              <span aria-hidden>{m.glyph}</span> {m.label}
            </li>
          ))}
          {leadChip && (
            <li className="rounded-full border border-lilac-mid px-3 py-1 text-xs text-eye-purple">{leadChip}</li>
          )}
        </ul>
      )}
```

- [ ] **Step 3: 비자격 분기를 taste 4슬롯으로 교체**

`{entitled === false ? ( … ) : narrativeLoading ? …` 블록 전체를 다음으로 교체:
```tsx
      {entitled === false ? (
        // 무료 ~350자(스펙 §8) — 나 탭 DayDetailCard 의 taste 블록과 같은 골격이라 두 탭이 같은
        // 리듬으로 읽힌다. CTA·미끼는 이 카드 밖 PremiumBlock(slot="woori_30d")이 받는다.
        taste ? (
          <div className="mt-3 space-y-2 text-sm leading-relaxed text-eye-purple">
            <p>{taste.signal}</p>
            <p>{taste.relation}</p>
            <p>{taste.lead}</p>
            <p className="text-text-light">{taste.advice}</p>
          </div>
        ) : null
      ) : narrativeLoading ? (
        <p className="mt-3 text-sm text-text-light">별콩이가 둘 사이 오늘을 읽고 있어…</p>
      ) : narrative ? (
        <div className="mt-3 space-y-2">
          {/* pair-narrative 라우트는 date 파라미터가 없어 이 서술은 항상 '오늘' 기준이다 — 다른 날을
              보고 있을 땐 이 글이 그 날이 아니라 오늘 얘기라는 걸 조용히 밝힌다. */}
          {!cell.isToday && <p className="text-xs text-text-light">오늘 기준으로 들려주는 이야기야</p>}
          <p className="whitespace-pre-line text-sm leading-relaxed text-eye-purple">{narrative}</p>
        </div>
      ) : null}
```

- [ ] **Step 4: `WooriTodayView` 배선**

import 교체:
```tsx
import { PAIR_TONE_LABEL, pairMarks } from "@/lib/byeolmaru/pair-day";
import { getPairTaste } from "@/lib/byeolmaru/static-lines";
```
`PairDayDetailCard` 호출부를 교체(페이월 props 제거 + taste 전달):
```tsx
          <PairDayDetailCard
            cell={pairCell}
            backdrop={pairData.backdrop}
            partnerName={pairData.partnerName}
            entitled={pairData.entitled}
            // 🔴 선택한 셀 기준이다(오늘 고정이 아니다) — 무료도 이번 달 지나간 날을 고를 수 있다(P5-2).
            taste={pairData.entitled ? null : getPairTaste(pairCell.tone, pairCell.tags, pairData.status, pairCell.date)}
            narrative={pairNarrative}
            narrativeLoading={pairNarrativeLoading}
          />
```

- [ ] **Step 5: 타입체크 + 전체 테스트**

Run: `npx tsc --noEmit`
Expected: 에러 0 (Task 2 Step 7 의 에러가 여기서 해소된다)

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")`
Expected: 전부 pass (기준선 655 + 1 − 2 + 6 = 660)

- [ ] **Step 6: 커밋**

```bash
git commit data/byeolmaru/pair-taste.json lib/byeolmaru/static-lines.ts lib/byeolmaru/static-lines.test.ts lib/byeolmaru/pair-day.ts lib/byeolmaru/pair-day.test.ts components/byeolmaru/PairDayDetailCard.tsx components/byeolmaru/WooriTodayView.tsx -m "feat(byeolmaru): 무료 우리 오늘을 한 줄에서 ~350자 정적 뱅크로 (P5-5 T2·T3)" -m "칩·제목 조사 버그(지민와/지민가)를 어순·표기로 제거" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: `/byeolmaru/woori` 페이월을 P5-4 미끼로 통일

**Files:**
- Modify: `components/byeolmaru/WooriTodayView.tsx`

옛 CTA(`3일 무료 체험 시작` · `구독하고 우리 오늘 이번 달 전체 보기`)와 옛 티저는 Task 3 에서 이미 사라졌다. 이제 그 자리에 허브 인연 탭과 **같은 물건**(`woori_30d` 미끼 = 자물쇠 없음 · `구독` 배지 · 증거 칩 · 이어받는 첫 줄 + 페이드 · `3일 무료로 열어보기` · 가격 2단 · 당일 접힘)을 놓는다.

- [ ] **Step 1: `PremiumBlock` 을 상세 카드 아래 형제로 배치**

import 추가:
```tsx
import PremiumBlock from "./PremiumBlock";
```
`<PairDayDetailCard … />` 바로 아래에 추가:
```tsx
          {/* 🔴 자격자에겐 아예 렌더하지 않는다 — 우리 탭의 서술은 PairDayDetailCard 안에 있고,
              PremiumBlock 의 자격자 분기("별콩이의 오늘")까지 띄우면 빈 블록이 하나 더 생긴다.
              slot 은 허브 인연 탭과 같은 woori_30d — 같은 물건을 파는 자리라 카피도 같아야 한다(스펙 §9).
              ⚠️ 판독 주의: 이제 woori_30d 의 노출 면이 허브·이 화면 둘이다. gate_shown 비율을 자리끼리
              비교할 땐 (user_id, slot, KST일자) distinct 로 정규화할 것(P5-4 판독 규칙과 동일). */}
          {!pairData.entitled && (
            <PremiumBlock
              entitled={false}
              trialUsed={trialUsed}
              narrative={null}
              teaser={null}
              loading={false}
              slot="woori_30d"
              baitCtx={{ partnerName: pairData.partnerName }}
              onStartTrial={(slot) => {
                trackUiEvent("byeolmaru_subscribe_from_woori", { meta: { action: "trial", slot } });
                void startTrial(slot);
              }}
              onSubscribe={(slot) => {
                trackUiEvent("byeolmaru_subscribe_from_woori", { meta: { action: "subscribe", slot } });
                openSubscribe(slot);
              }}
            />
          )}
```
> `byeolmaru_subscribe_from_woori` 를 계속 찍는 이유: 이 화면과 허브 인연 탭이 같은 slot 을 쓰므로 **이 이벤트가 "woori 페이지에서 눌렀다"의 유일한 구분자**다. 어드민 RPC `admin_byeolmaru_watch_summary` 가 이 이름을 이미 읽는다 — 이름을 바꾸면 그 집계가 조용히 0 이 된다.

- [ ] **Step 2: 타입체크 + 빌드**

Run: `npx tsc --noEmit`
Expected: 에러 0

Run: `npm run build`
Expected: exit 0, 라우트 표에 `ƒ Proxy` 존재

- [ ] **Step 3: 커밋**

```bash
git commit components/byeolmaru/WooriTodayView.tsx -m "feat(byeolmaru): 우리 오늘 페이월을 woori_30d 미끼로 통일 (P5-5 T4)" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: `pair-narrative` 일별 캐시 (마이그레이션)

**Files:**
- Create: `supabase/migrations/20260914000000_byeolmaru_pair_narrative.sql`
- Create: `lib/byeolmaru/pair-narrative.ts`
- Modify: `app/api/byeolmaru/pair-narrative/route.ts`

- [ ] **Step 1: 마이그레이션 작성**

`supabase/migrations/20260914000000_byeolmaru_pair_narrative.sql`:
```sql
-- 20260914000000_byeolmaru_pair_narrative.sql — 별마루 유료 "우리 오늘" 서술 캐시(유저·상대·날짜당 1행).
-- byeolmaru_daily_report(20260907000000)의 캐시 패턴을 그대로 미러한다. 근거는 원가가 아니라(회당 ₩1
-- 수준) ①일관성 — 같은 날 같은 상대인데 칩을 다시 누를 때마다 서술이 바뀌면 운세 앱의 신뢰가 깨진다
-- ②속도 — 생성 실측 5.1초(P5-5 로 분량이 2배가 되면 더 길다). 스펙 §8.
CREATE TABLE IF NOT EXISTS byeolmaru_pair_narrative (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  partner_profile_id UUID NOT NULL REFERENCES user_profiles(id) ON DELETE CASCADE,
  narrative_date DATE NOT NULL,
  narrative TEXT NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  PRIMARY KEY (user_id, partner_profile_id, narrative_date)
);
CREATE INDEX IF NOT EXISTS idx_byeolmaru_pair_narrative_user
  ON byeolmaru_pair_narrative(user_id, narrative_date DESC);
ALTER TABLE byeolmaru_pair_narrative ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE byeolmaru_pair_narrative FROM PUBLIC, anon, authenticated;
GRANT ALL ON TABLE byeolmaru_pair_narrative TO service_role;
```

- [ ] **Step 2: 캐시 래퍼 작성**

`lib/byeolmaru/pair-narrative.ts`:
```ts
// lib/byeolmaru/pair-narrative.ts — 유료 "우리 오늘" 서술 캐시((유저,상대,날짜)별 1회 생성).
// daily-report.ts 와 같은 모양·같은 규율(23505 무시 = 동시 생성 시 먼저 쓴 쪽을 남긴다).
import { getServiceSupabase } from "@/lib/supabase";

/** 캐시된 서술 조회. 없으면 null. */
export async function getCachedPairNarrative(
  userId: string,
  partnerProfileId: string,
  dateStr: string
): Promise<string | null> {
  const { data, error } = await getServiceSupabase()
    .from("byeolmaru_pair_narrative")
    .select("narrative")
    .eq("user_id", userId)
    .eq("partner_profile_id", partnerProfileId)
    .eq("narrative_date", dateStr)
    .maybeSingle();
  if (error || !data) return null;
  return data.narrative as string;
}

/** 서술 저장. 이미 있으면(동시 생성) 덮지 않는다(23505 무시). */
export async function savePairNarrative(
  userId: string,
  partnerProfileId: string,
  dateStr: string,
  narrative: string
): Promise<void> {
  const { error } = await getServiceSupabase()
    .from("byeolmaru_pair_narrative")
    .insert({ user_id: userId, partner_profile_id: partnerProfileId, narrative_date: dateStr, narrative });
  if (error && (error as { code?: string }).code !== "23505") {
    throw error;
  }
}
```

- [ ] **Step 3: 라우트 배선 — 자격 게이트 직후 캐시 조회**

`app/api/byeolmaru/pair-narrative/route.ts` import 에 추가:
```ts
import { getCachedPairNarrative, savePairNarrative } from "@/lib/byeolmaru/pair-narrative";
```
자격 판정(`if (!ent.entitled) return …`) 바로 아래, `const supa = getServiceSupabase();` **위**에 삽입:
```ts
    // 🔴 캐시는 자격 게이트 **뒤**, 프로필/사주 계산 **앞**이다. 비자격자는 DB 도 안 건드리고(원가 0),
    //    자격자는 히트 시 calcSaju·일진·watch 조회를 통째로 건너뛴다(생성 실측 5.1초 → DB 1회).
    //    남의 상대 id 를 넣어도 키가 (내 user_id, 그 id) 라 행이 없어 자연히 미스 → 아래 소유 검증으로 간다.
    const todayKst = kstDate(new Date().toISOString());
    const cached = await getCachedPairNarrative(userId, subject, todayKst);
    if (cached) return NextResponse.json({ entitled: true, narrative: cached });
```
그리고 아래쪽에 있던 기존 `const todayKst = kstDate(new Date().toISOString());` **한 줄을 삭제**한다(위로 올라왔으므로 중복 선언이 된다).

생성 성공 분기의 `return NextResponse.json({ entitled: true, narrative });` **바로 위**에:
```ts
      // 캐시 저장은 best-effort — 실패해도 이미 만든 서술은 그대로 응답한다(daily-report 와 동일 경계).
      try {
        await savePairNarrative(userId, subject, todayKst, narrative);
      } catch (e) {
        await logError(e, { ...logCtx, extra: { stage: "cache_save" } });
      }
```

- [ ] **Step 4: 타입체크 + 전체 테스트 + 빌드**

Run: `npx tsc --noEmit`
Expected: 에러 0

Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")`
Expected: 전부 pass (새 유닛 없음 — DB 래퍼는 `daily-report.ts` 와 같은 무-테스트 관례를 따르고, 실동작은 Task 7 라이브 E2E 항목 10 이 본다)

Run: `npm run build`
Expected: exit 0

- [ ] **Step 5: 커밋**

```bash
git commit supabase/migrations/20260914000000_byeolmaru_pair_narrative.sql lib/byeolmaru/pair-narrative.ts app/api/byeolmaru/pair-narrative/route.ts -m "feat(byeolmaru): 우리 오늘 서술 (유저·상대·날짜) 일별 캐시 (P5-5 T5)" -m "근거는 원가가 아니라 일관성(칩 재선택마다 다른 서술)과 속도(실측 5.1초)" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: 유료 우리 오늘 ~1,200자

**Files:**
- Modify: `lib/byeolmaru/narrative-prompt.ts`
- Modify: `app/api/byeolmaru/pair-narrative/route.ts` (토큰 상한 교체)
- Test: `lib/byeolmaru/narrative-prompt.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/byeolmaru/narrative-prompt.test.ts` 는 이미 `calcSaju`·`calcTemporalLuck`·`buildPairCalendar`·`pairBackdrop`·`buildPairNarrativeSystem` 을 import 하고 형제 테스트(`buildPairNarrativeSystem: 두 사람·너희 결…`)가 아래 픽스처를 쓴다 — **같은 픽스처를 그대로 쓴다**. import 목록에 `PAIR_NARRATIVE_MAX_TOKENS` 만 추가하고 파일 끝에 붙인다:
```ts
test("pair 프롬프트: 분량 지시가 1,100~1,300자이고 토큰 상한이 그 분량을 감당한다", () => {
  const a = calcSaju({ year: 1996, month: 4, day: 11, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
  const b = calcSaju({ year: 1994, month: 11, day: 3, hour: 21, gender: "male", isLunar: false, isLeapMonth: false });
  const t = calcTemporalLuck(baseDateForKst("2026-09-05"), 1996, { includeMonth: true });
  const cell = buildPairCalendar(a, b, t.dailyLuck!, "2026-09-05")[0];
  const sys = buildPairNarrativeSystem(a, b, pairBackdrop(a, b), cell, "임오", "지우");
  assert.ok(sys.includes("1,100~1,300자"), "분량 지시가 프롬프트에 있어야 한다");
  assert.ok(!sys.includes("3~4문단"), "옛 분량 지시(3~4문단 ≈ 600자)가 남아 있으면 안 된다");
  // nano 는 추론 토큰이 max 안에 함께 카운트된다 — 본문 1,200자(≈670토큰)에 추론 헤드룸을 더한 값.
  assert.ok(PAIR_NARRATIVE_MAX_TOKENS >= 3600, `상한 ${PAIR_NARRATIVE_MAX_TOKENS} 은 1,200자에 부족하다`);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --import tsx --test lib/byeolmaru/narrative-prompt.test.ts`
Expected: FAIL — `PAIR_NARRATIVE_MAX_TOKENS` 미존재 / 분량 지시 없음

- [ ] **Step 3: 프롬프트·상한 구현**

`lib/byeolmaru/narrative-prompt.ts` 의 `buildPairNarrativeSystem` **마지막 규칙 문자열**(`"규칙: 3~4문단, 반말, …"` 로 시작하는 항목)을 아래로 교체한다. `goodList` 분기는 그대로 유지한다:
```ts
    "분량: 1,100~1,300자. 하나로 흐르는 줄글 4~5문단.",
    "이 순서로 자연스럽게 이어서 써(각 부분을 라벨로 표시하지 마): 먼저 오늘 일진이 둘 사이를 어떻게 건드리는지로 열고, 이어서 오늘의 신호(끌림·결속·삐걱·리드)가 실제 하루에서 어떤 장면으로 나타날지 구체적으로 그리고, 그다음 너희 고정된 결(십신 관계·연월조화)이 오늘과 겹쳐 어디를 편하게 하고 어디를 걸리게 하는지 짚고, 마지막은 따뜻한 한 줄로 맺어.",
    "규칙: 반말, 단정적 예언 금지(흐름·가능성·선택). 한쪽을 탓하지 말고 둘의 흐름으로 말할 것. 같은 말을 바꿔 쓰며 늘리지 말고(분량은 장면과 예시로 채운다), 위에 준 신호 낱말을 라벨처럼 앞세우지 말 것." +
      (goodList
        ? " 마무리 즈음에 위 '좋은 날' 목록에서만 골라 1~2개를 관계 타이밍으로 자연스럽게 짚어줘(연락·만남 하기 좋은 결). '이 날 뭘 해라' 식 지시 대신 '이 무렵 결이 잘 맞아' 식 흐름으로. 목록 밖 날짜를 지어내지 마."
        : "") +
      " 별표/제목/마커 없이 줄글만.",
```
`PAIR_NARRATIVE_KICKOFF` 아래에 상한을 신설한다:
```ts
// 우리 오늘은 P5-5 에서 ~600자 → ~1,200자로 커졌다(스펙 §8). 나 1인 서술(NARRATIVE_MAX_TOKENS=900,
// ~600자)을 더 못 쓴다. nano 는 추론 토큰이 max 안에 함께 카운트되고 런마다 출렁여서, 본문
// 1,200자(한글 ≈1.8자/토큰 → ≈670토큰)에 추론 헤드룸을 넉넉히 얹는다. 카드 서술(~800자 → 3000)과
// daily 리포트(~2,100자 → 4500) 사이 값이다. 미생성 토큰은 과금되지 않는다 — 상한만 넉넉히,
// 실분량은 위 "1,100~1,300자" 지침이 잡는다.
export const PAIR_NARRATIVE_MAX_TOKENS = 4000;
```
`app/api/byeolmaru/pair-narrative/route.ts` 에서 import 와 `generateOnce` 인자를 `NARRATIVE_MAX_TOKENS` → `PAIR_NARRATIVE_MAX_TOKENS` 로 바꾼다. **`NARRATIVE_MAX_TOKENS` 상수 자체는 지우지 말 것** — `/api/byeolmaru/narrative` 가 계속 쓴다.

- [ ] **Step 4: 통과 확인**

Run: `node --import tsx --test lib/byeolmaru/narrative-prompt.test.ts`
Expected: PASS

- [ ] **Step 5: 🔴 런타임 실측 — 진짜로 1,200자가 나오는지 nano 로 3회 재본다**

유닛은 "프롬프트에 지시가 있다"만 본다. 실제 분량·잘림은 모델을 돌려야 안다(1C-타로에서 `max_tokens` 부족으로 어절 중간 잘림이 실측된 전례가 있다).

스크래치패드에 일회성 스크립트를 만든다(**리포에 남기지 않는다**):
```ts
// <scratchpad>/pair-len-check.ts
import { calcSaju, calcTemporalLuck, baseDateForKst } from "@/lib/saju/calc";
import {
  buildPairNarrativeSystem, PAIR_NARRATIVE_KICKOFF,
  BYEOLMARU_NARRATIVE_MODEL, PAIR_NARRATIVE_MAX_TOKENS,
} from "@/lib/byeolmaru/narrative-prompt";
import { pairBackdrop, buildPairCalendar } from "@/lib/byeolmaru/pair-day";
import { generateOnce } from "@/lib/claude";

const a = calcSaju({ year: 1996, month: 4, day: 11, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
const b = calcSaju({ year: 1994, month: 11, day: 3, hour: 21, gender: "male", isLunar: false, isLeapMonth: false });
const t = calcTemporalLuck(baseDateForKst("2026-09-14"), 1996, { includeMonth: true });
const cal = buildPairCalendar(a, b, t.dailyLuck!, "2026-09-14");
const cell = cal[0];
// 관계 유형(dating)까지 실어 실사용 프롬프트와 같은 모양으로 잰다.
const sys = buildPairNarrativeSystem(a, b, pairBackdrop(a, b), cell, cell.ganji, "지우", cal.filter((c) => c.tone === "good").slice(0, 3), "dating");
for (let i = 0; i < 3; i++) {
  const out = await generateOnce(
    sys, [{ role: "user", content: PAIR_NARRATIVE_KICKOFF }],
    PAIR_NARRATIVE_MAX_TOKENS, { route: "len-check" }, BYEOLMARU_NARRATIVE_MODEL, undefined
  );
  console.log(`--- run ${i + 1}: ${out.length}자 · 끝: ${JSON.stringify(out.slice(-30))}`);
  console.log(out);
}
```
Run: `node --import tsx --env-file=.env.local <scratchpad>/pair-len-check.ts`

합격 기준:
- 3회 모두 **900~1,500자**. 900자 미만이 나오면 분량 지시를 강화하고, **끝이 어절 중간에서 끊기면**(`끝:` 출력이 문장부호로 안 끝남) `PAIR_NARRATIVE_MAX_TOKENS` 를 4800 으로 올린 뒤 재측정
- 반말·2인칭 유지, 별표/제목/마커 없음, 단정적 예언("반드시 ~한다" 류) 없음
- 같은 말을 바꿔 쓰며 늘린 흔적이 없을 것(있으면 "장면과 예시로 채운다" 지시를 강화)

측정한 3회 자수를 커밋 메시지에 남긴다.

- [ ] **Step 6: 커밋**

```bash
git commit lib/byeolmaru/narrative-prompt.ts lib/byeolmaru/narrative-prompt.test.ts app/api/byeolmaru/pair-narrative/route.ts -m "feat(byeolmaru): 유료 우리 오늘 서술을 ~600자에서 ~1,200자로 (P5-5 T6)" -m "nano 실측 3회: <자수1>/<자수2>/<자수3>자, 잘림 없음" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: 최종 게이트 + 라이브 E2E

**Files:** 없음(검증만). 문제가 나오면 해당 파일을 고치고 별도 커밋.

- [ ] **Step 1: 전체 게이트 3종**

Run: `npx tsc --noEmit` → 에러 0
Run: `node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*" -not -path "./.claude/*")` → 전부 pass
Run: `npm run build` → exit 0 + 라우트 표에 `ƒ Proxy`

- [ ] **Step 2: 라이브 E2E (로컬 dev 서버 · 세션 주입)**

🔴 **주입 전 기존 쿠키 값을 캡처하고 끝나면 복원한다**(사용자 탭 로그인을 덮어쓴 사고가 실제로 있었다).

QA봇(`11111111-1111-4111-8111-111111111111`, 비구독 · 상대 "테스트상대" 등록됨)으로 `/byeolmaru/woori` 를 열고 확인한다:

1. **셀 마크** — 우리 격자 칸 우상단에 ✧/◇/△ 중 하나가 뜨고, 격자 하단 범례가 그 달에 실제로 뜬 것만 "✧ 끌림 / ◇ 결속 / △ 삐걱" 으로 보여준다
2. **무료 taste** — 상세 카드에 4문단(~350자)이 뜬다. 옛 한 줄(~50자)이 아니다
3. **셀 전환** — 달력에서 **다른 날**(이번 달 지난 날)을 고르면 taste 가 그 날 기준으로 바뀐다(오늘 고정이 아니다)
4. **칩·제목** — 제목이 "나와 테스트상대 · 오늘", 리드 칩이 "테스트상대 리드"(← "테스트상대가 리드" 아님)
5. **미끼** — 상세 카드 **아래**에 `둘 사이를 매일 짚어줄게` + `구독` 배지 + 칩 2개 + `3일 무료로 열어보기` + `체험 끝나면 20별 / 30일`. 자물쇠(🔒) 없음
6. **375px 오버플로** — 미끼 제목이 잘리지 않고 줄바꿈된다(`truncate` 금지 규율), 가로 스크롤 없음
7. **당일 접힘** — "오늘은 그만" 누르면 미끼가 사라지고, 새로고침해도 안 뜬다
8. **허브 무회귀** — `/byeolmaru` 인연 칩을 눌러 우리 격자로 바꾸면 마크가 뜨고, 미끼는 `woori_30d` 한 벌만 뜬다
9. **나 탭 무회귀** — `/byeolmaru/saju` 의 taste(~450자)·마크·미끼(`saju_report`)가 그대로다
10. **캐시** — 체험 시작(또는 구독)으로 자격을 켜고 `/byeolmaru/woori` 진입 → 서술이 뜬 뒤 상대 칩을 `나`로 바꿨다가 다시 상대로 돌아온다. **두 번째는 즉시 뜨고 문장이 첫 번째와 글자 그대로 같아야 한다**
    ⚠️ 이전 실행으로 만들어진 캐시 행이 있으면 옛 ~600자가 나온다. 그럴 땐 dev DB 에서 `delete from byeolmaru_pair_narrative where user_id='11111111-1111-4111-8111-111111111111'` 를 **사용자에게 부탁**하거나(쓰기는 항상 사용자 손) 다른 상대로 검증한다
11. **계측** — 위 과정에서 `ui_events` 에 `byeolmaru_gate_shown{slot:"woori_30d"}` · `byeolmaru_gate_dismissed{slot:"woori_30d"}` · (CTA 를 눌렀다면) `byeolmaru_subscribe_from_woori{action,slot}` 이 들어간다

- [ ] **Step 3: 결과 보고**

통과/실패를 항목 번호로 보고한다. 실패가 있으면 고치고 `git commit <파일> -m "fix(byeolmaru): … (P5-5 라이브 검증)"`.

---

## 완료 기준

- [ ] `npx tsc --noEmit` 0 · 전체 유닛 pass · `npm run build` exit 0 + `ƒ Proxy`
- [ ] 무료 우리 오늘 = **4슬롯 ~350자**(실측 290~370), 유료 = **~1,200자**(nano 3회 실측 기록)
- [ ] `pair-narrative` 가 (유저·상대·날짜)로 캐시된다 — 같은 날 재진입 시 **문장이 동일**
- [ ] 우리 셀에 ✧끌림 ◇결속 △삐걱 마크 + 범례
- [ ] `/byeolmaru/woori` 의 CTA·미끼가 허브 인연 탭과 **같은 물건**(`woori_30d`)
- [ ] 나 탭(사주·타로)·허브 무회귀
- [ ] 🚫 prod(main) 배포는 **하지 않는다** — 사용자의 명시 "go" 전까지 금지. dev 푸시 여부도 사용자에게 묻는다

## 리스크 · 미검증

- **`static-lines.ts` 가 클라 번들에 JSON 4개를 싣는다**(`card-taste.json` 135KB 포함). `pair-taste.json`(~10KB)이 다섯 번째다 — **선재 부채이고 이번에 늘어나는 양은 작다.** 분할은 별건
- **`woori_30d` 노출 면이 2곳이 됐다**(허브 인연 탭 + `/byeolmaru/woori`). raw `gate_shown` 으로 자리끼리 비교하면 과대계상된다 — `(user_id, slot, KST일자)` distinct 로 정규화할 것
- **유료 분량 2배 = 첫 호출 체감 대기도 늘어난다.** 캐시는 두 번째부터 듣는다. 지금 UI 는 "별콩이가 둘 사이 오늘을 읽고 있어…" 한 줄뿐이라, 실측 대기가 10초를 넘으면 스켈레톤/진행 표시를 별건으로 검토
- **뱅크 원고는 n=1 저작이다** — 톤 과잉/부족은 실사용 전엔 모른다. 페르소나 조정은 1회로 끊고 실데이터로 판단(`persona-tuning-pendulum`)
