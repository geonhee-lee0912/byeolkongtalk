# 별자리(byeoljari) P3-3 카피 엔진 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 1:1 관계 뷰 문구를 관계분류(친구·연인·지인·윗사람)별로 다르게 — 관계분류 × 십신 40칸 카피 테이블 + 방어적 조립.

**Architecture:** 서버(API)는 십신 **코드**까지만 실어 보내고, 카피 조립은 클라(프레젠테이션)에서 `COPY[관계분류][정렬된 십신] ?? 중립 라벨`로. pivot(나)이 클라에서 정해지고 관계분류는 target(그)의 것이라 서버가 조립 못 함. 룰 100%·LLM 0.

**Tech Stack:** Next.js 16 · React 19 · TS strict · 순수함수 TDD(`node:test`).

---

## 배경 / 전제 (구현 전 필독)

- **dev 브랜치 직접 작업**(P1~P3-2 동일). 현재 clean, `dev`=`origin/dev`.
- **정본 스펙**: `docs/superpowers/specs/2026-08-17-p3-3-카피-엔진-design.md`.
- **테스트 규칙**: `node:test` + `node:assert/strict`. import에 `.ts` 확장자. 실행 `node --import tsx --test <files>`. package.json에 test 스크립트 없음.
- **방어 규칙**([[recurring-crash-class-config-by-dynamic-key]]): 동적 키(relationType·tenGod) 조회는 반드시 폴백. 미지 값 → null → 호출부가 중립 라벨로.
- **기존 계약**:
  - `lib/saju/pairing.ts` — `export type TenGod = "비견"|"겁재"|"식신"|"상관"|"편재"|"정재"|"편관"|"정관"|"편인"|"정인"`. `PairRelation`은 이미 `tenGodAtoB`/`tenGodBtoA: TenGod`와 `labelAtoB`/`labelBtoA: string`(중립 `TEN_GOD_LABEL`)을 반환.
  - `lib/byeoljari/types.ts` — `GraphEdge { a, b, element, labelAtoB, labelBtoA, heavenlyCombo, sixCombo }`.
  - `lib/byeoljari/layout.ts` — `orientEdge(edge, pivotId)`가 pivot 기준 `{iSeeThem, theySeeMe, element}` 반환(pivot=b면 label 스왑 + `invertElementRelation`). `orientEdge.test.ts`는 `layout.test.ts` 안에 있음.
  - `components/byeoljari/OneToOnePanel.tsx` — `oriented: {iSeeThem, theySeeMe, element} | null`을 받아 표시. relationType은 `target.relationType`으로 이미 접근 가능.
  - `app/api/fortune/byeoljari/[shareId]/route.ts` — `pairRelation(saju[i], saju[j])` → edge 조립. relationType 값: `friend|lover|acquaintance|senior`.
- 한글 주석([[always-korean-replies]]).

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lib/byeoljari/copy.ts` | 40칸 카피 테이블 + `relationTenGodCopy` 방어 룩업 | **신규** |
| `lib/byeoljari/copy.test.ts` | 40칸 완전성 + 룩업 + 폴백 | **신규** |
| `lib/byeoljari/types.ts` | `GraphEdge`에 `tenGodAtoB`/`tenGodBtoA` | 수정 |
| `lib/byeoljari/layout.ts` | `orientEdge`에 십신 방향 정렬 2필드 | 수정 |
| `lib/byeoljari/layout.test.ts` | orientEdge 십신 방향 테스트 | 수정 |
| `app/api/fortune/byeoljari/[shareId]/route.ts` | edge에 십신 코드 | 수정 |
| `components/byeoljari/OneToOnePanel.tsx` | COPY 룩업 + 톤 | 수정 |

**순서:** copy.ts(독립) → 십신 배선(types+orientEdge+API) → OneToOnePanel 조립 → 통합 검증.

---

### Task 1: 40칸 카피 테이블 `lib/byeoljari/copy.ts` (TDD)

**Files:** Create `lib/byeoljari/copy.ts`, `lib/byeoljari/copy.test.ts`

- [ ] **Step 1: 실패 테스트 작성** — Create `lib/byeoljari/copy.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { RELATION_TEN_GOD_COPY, relationTenGodCopy } from "./copy.ts";

const RELATIONS = ["friend", "lover", "acquaintance", "senior"];
const TEN_GODS = ["비견", "겁재", "식신", "상관", "편재", "정재", "편관", "정관", "편인", "정인"];

test("40칸 전부 존재 + 빈 문자열 없음", () => {
  for (const r of RELATIONS) {
    for (const t of TEN_GODS) {
      const v = RELATION_TEN_GOD_COPY[r]?.[t as keyof (typeof RELATION_TEN_GOD_COPY)[string]];
      assert.ok(v && v.length > 0, `${r}/${t} 비어있음`);
    }
  }
});

test("relationTenGodCopy 정상 룩업", () => {
  assert.equal(relationTenGodCopy("friend", "정관"), "믿고 기댈 친구");
  assert.equal(relationTenGodCopy("lover", "편관"), "자꾸 밀당하게 되는 상대");
  assert.equal(relationTenGodCopy("senior", "편인"), "영감을 주는 멘토");
});

test("relationTenGodCopy 미지 relationType → null", () => {
  assert.equal(relationTenGodCopy("unknown", "정관"), null);
});

test("relationTenGodCopy 미지 tenGod → null(크래시 금지)", () => {
  assert.equal(relationTenGodCopy("friend", "칠살"), null);
});
```

- [ ] **Step 2: 실패 확인** — Run: `node --import tsx --test lib/byeoljari/copy.test.ts` → FAIL(module 없음).

- [ ] **Step 3: 구현** — Create `lib/byeoljari/copy.ts`:

```ts
// 관계분류 × 십신 40칸 카피 — 1:1 관계 뷰 문구(별콩이 반말·낙인 제거·완곡). 룰 100%·LLM 0.
// ⚠️ 방어적: enum 밖 relationType/tenGod 는 null 폴백 → 호출부가 중립 라벨(TEN_GOD_LABEL)로.
import type { TenGod } from "@/lib/saju/pairing";

// relationType 키: friend|lover|acquaintance|senior (GraphNode.relationType).
// Record<TenGod,string> 라 tsc 가 각 관계분류의 10칸 완전성을 강제한다(빠지면 컴파일 에러).
export const RELATION_TEN_GOD_COPY: Record<string, Record<TenGod, string>> = {
  friend: {
    비견: "말 안 해도 통하는 친구",
    겁재: "티격태격해도 붙어 다니는 친구",
    식신: "챙겨주고 싶은 친구",
    상관: "내 텐션 올려주는 친구",
    편재: "내가 앞장서게 되는 친구",
    정재: "오래 곁에 두고 싶은 친구",
    편관: "정신 번쩍 들게 하는 친구",
    정관: "믿고 기댈 친구",
    편인: "생각을 넓혀주는 친구",
    정인: "마음이 놓이는 친구",
  },
  lover: {
    비견: "편하게 닮아가는 사람",
    겁재: "밀당이 은근 재밌는 상대",
    식신: "다 해주고 싶은 사람",
    상관: "나를 반짝이게 하는 사람",
    편재: "내가 리드하는 사람",
    정재: "곱게 아끼는 사람",
    편관: "자꾸 밀당하게 되는 상대",
    정관: "듬직하게 안아주는 사람",
    편인: "새로운 세계를 보여주는 사람",
    정인: "포근하게 감싸주는 사람",
  },
  acquaintance: {
    비견: "왠지 나랑 비슷한 사람",
    겁재: "묘하게 경쟁되는 사람",
    식신: "나도 모르게 돌봐주는 사람",
    상관: "내 얘기 술술 꺼내게 하는 사람",
    편재: "내가 먼저 나서는 사람",
    정재: "잘 지내고 싶은 사람",
    편관: "괜히 신경 쓰이는 사람",
    정관: "은근히 의지되는 사람",
    편인: "묘하게 끌리는 사람",
    정인: "편히 기대게 되는 사람",
  },
  senior: {
    비견: "결이 닮은 선배",
    겁재: "자꾸 비교하게 되는 선배",
    식신: "살뜰히 챙기게 되는 분",
    상관: "내 재능 알아봐 주는 분",
    편재: "내가 먼저 다가가는 분",
    정재: "신뢰를 쌓고 싶은 분",
    편관: "어렵지만 날 키우는 어른",
    정관: "배울 게 많은 어른",
    편인: "영감을 주는 멘토",
    정인: "따뜻하게 이끌어 주는 분",
  },
};

/** 관계분류 × 십신 문구. 미지 조합이면 null(호출부가 중립 라벨로 폴백). */
export function relationTenGodCopy(relationType: string, tenGod: string): string | null {
  const byRel = RELATION_TEN_GOD_COPY[relationType];
  if (!byRel) return null;
  return byRel[tenGod as TenGod] ?? null;
}
```

- [ ] **Step 4: 통과 확인** — Run: `node --import tsx --test lib/byeoljari/copy.test.ts` → 4 tests pass. 그리고 `npx tsc --noEmit` → exit 0(Record<TenGod> 완전성 컴파일 통과 = 40칸 다 채워짐).

- [ ] **Step 5: 커밋**

```bash
git add lib/byeoljari/copy.ts lib/byeoljari/copy.test.ts
git commit -m "feat(byeoljari): P3-3 관계분류×십신 40칸 카피 테이블+방어 룩업 TDD"
```

---

### Task 2: 십신 코드 배선 (types + orientEdge + API)

**Files:** Modify `lib/byeoljari/types.ts`, `lib/byeoljari/layout.ts`, `lib/byeoljari/layout.test.ts`, `app/api/fortune/byeoljari/[shareId]/route.ts`

- [ ] **Step 1: orientEdge 십신 방향 테스트 추가** — `lib/byeoljari/layout.test.ts`에 추가(기존 import·구조 유지; GraphEdge 목 객체는 신규 필드 포함):

```ts
test("orientEdge — 십신도 pivot 기준 방향 정렬", () => {
  const edge = {
    a: "A", b: "B", element: "아극",
    labelAtoB: "라벨AB", labelBtoA: "라벨BA",
    tenGodAtoB: "편재", tenGodBtoA: "정관",
    heavenlyCombo: false, sixCombo: false,
  };
  // pivot = a: 내가 보는 그 = A→B 십신(편재), 그가 보는 나 = B→A 십신(정관)
  const oa = orientEdge(edge, "A");
  assert.equal(oa?.iSeeThemTenGod, "편재");
  assert.equal(oa?.theySeeMeTenGod, "정관");
  // pivot = b: 스왑
  const ob = orientEdge(edge, "B");
  assert.equal(ob?.iSeeThemTenGod, "정관");
  assert.equal(ob?.theySeeMeTenGod, "편재");
  // pivot 무관계 → null
  assert.equal(orientEdge(edge, "Z"), null);
});
```

> ⚠️ `orientEdge`가 이미 `layout.test.ts`에서 테스트되면 그 import를 재사용. 없으면 `import { orientEdge } from "./layout.ts";` 추가.

- [ ] **Step 2: 실패 확인** — Run: `node --import tsx --test lib/byeoljari/layout.test.ts` → FAIL(iSeeThemTenGod undefined / 타입).

- [ ] **Step 3: `types.ts` GraphEdge에 십신 코드 추가** — `lib/byeoljari/types.ts`의 `GraphEdge` 인터페이스에 2필드 삽입(기존 필드 유지):

```ts
export interface GraphEdge {
  a: string;
  b: string;
  element: string; // a 기준 오행 관계: 비화|생아|아생|극아|아극 (오행 아님)
  labelAtoB: string; // 별콩 라벨 a→b (중립 폴백)
  labelBtoA: string; // 별콩 라벨 b→a (중립 폴백)
  tenGodAtoB: string; // a→b 십신 코드 (관계분류 조립용)
  tenGodBtoA: string; // b→a 십신 코드
  heavenlyCombo: boolean;
  sixCombo: boolean;
}
```

- [ ] **Step 4: `orientEdge` 십신 방향 정렬** — `lib/byeoljari/layout.ts`의 `orientEdge`를 아래로 교체(반환 타입 + 2필드):

```ts
/** pivot 입장에서 본 방향 라벨. iSeeThem=내가 보는 상대, theySeeMe=상대가 보는 나(골드 강조 대상).
 *  십신 코드도 같은 방향으로 정렬(카피 조립은 호출부가 관계분류와 함께). */
export function orientEdge(
  edge: GraphEdge,
  pivotId: string
): {
  iSeeThem: string;
  theySeeMe: string;
  element: string;
  iSeeThemTenGod: string;
  theySeeMeTenGod: string;
} | null {
  if (edge.a === pivotId) {
    return {
      iSeeThem: edge.labelAtoB,
      theySeeMe: edge.labelBtoA,
      element: edge.element,
      iSeeThemTenGod: edge.tenGodAtoB,
      theySeeMeTenGod: edge.tenGodBtoA,
    };
  }
  if (edge.b === pivotId) {
    return {
      iSeeThem: edge.labelBtoA,
      theySeeMe: edge.labelAtoB,
      element: invertElementRelation(edge.element),
      iSeeThemTenGod: edge.tenGodBtoA,
      theySeeMeTenGod: edge.tenGodAtoB,
    };
  }
  return null;
}
```

- [ ] **Step 5: API route에 십신 코드 실음** — `app/api/fortune/byeoljari/[shareId]/route.ts`:

(a) `edges` 배열 타입 선언에 2필드 추가:
```ts
  const edges: Array<{
    a: string;
    b: string;
    element: string;
    labelAtoB: string;
    labelBtoA: string;
    tenGodAtoB: string;
    tenGodBtoA: string;
    heavenlyCombo: boolean;
    sixCombo: boolean;
  }> = [];
```

(b) `edges.push({...})`에 2필드 추가(`r`은 `pairRelation` 결과):
```ts
      edges.push({
        a: members[i].id,
        b: members[j].id,
        element: r.element,
        labelAtoB: r.labelAtoB,
        labelBtoA: r.labelBtoA,
        tenGodAtoB: r.tenGodAtoB,
        tenGodBtoA: r.tenGodBtoA,
        heavenlyCombo: r.heavenlyCombo,
        sixCombo: r.sixCombo,
      });
```

- [ ] **Step 6: 통과 + 빌드 확인** — Run: `node --import tsx --test lib/byeoljari/layout.test.ts` → pass. `npx tsc --noEmit` → exit 0. `npm run build` → exit 0.

- [ ] **Step 7: 커밋**

```bash
git add lib/byeoljari/types.ts lib/byeoljari/layout.ts lib/byeoljari/layout.test.ts "app/fortune/byeoljari" app/api/fortune/byeoljari/"[shareId]"/route.ts
git commit -m "feat(byeoljari): P3-3 edge에 십신 코드+orientEdge 방향정렬(카피 조립 배선)"
```

> 커밋에 실제로 스테이지된 파일만 들어가면 됨: `lib/byeoljari/types.ts`, `lib/byeoljari/layout.ts`, `lib/byeoljari/layout.test.ts`, `app/api/fortune/byeoljari/[shareId]/route.ts`.

---

### Task 3: OneToOnePanel 카피 조립 + 톤

**Files:** Modify `components/byeoljari/OneToOnePanel.tsx`

- [ ] **Step 1: import + oriented 타입 갱신** — 상단 import에 추가:

```ts
import { relationTenGodCopy } from "@/lib/byeoljari/copy";
```

`Props`의 `oriented` 타입을 orientEdge 신규 반환에 맞춤:

```ts
  oriented: {
    iSeeThem: string;
    theySeeMe: string;
    element: string;
    iSeeThemTenGod: string;
    theySeeMeTenGod: string;
  } | null;
```

- [ ] **Step 2: COPY 룩업으로 문구 조립** — `oriented` 렌더 블록에서, 표시 문구를 관계분류 반영으로. `oriented ? (` 직후 블록 상단에 조립 상수 추가하고 표시부에서 사용:

```tsx
      {oriented ? (
        (() => {
          const iSee = relationTenGodCopy(target.relationType, oriented.iSeeThemTenGod) ?? oriented.iSeeThem;
          const theySee = relationTenGodCopy(target.relationType, oriented.theySeeMeTenGod) ?? oriented.theySeeMe;
          return (
            <div className="mt-3 space-y-3">
              <p className="text-sm text-eye-purple">{elementRelationLabel(oriented.element)}</p>
              <div className="rounded-xl bg-white/60 p-3 text-sm">
                <div className="text-text-light">내가 보는 {them}</div>
                <div className="text-eye-purple">{iSee}</div>
              </div>
              {/* 남이 보는 나 = 스펙 §2 차별점 = 골드 강조 */}
              <div className="rounded-xl bg-gold-soft/40 p-3 text-sm ring-1 ring-gold">
                <div className="text-text-light">{them}{subjectParticle(them)} 보는 나</div>
                <div className="font-semibold text-eye-purple">{theySee}</div>
              </div>
              {(heavenlyCombo || sixCombo) && (
                <div className="flex gap-2">
                  {heavenlyCombo && (
                    <span className="rounded-full bg-gold/20 px-3 py-1 text-xs text-eye-purple">✨ 케미 스파크</span>
                  )}
                  {sixCombo && (
                    <span className="rounded-full bg-lilac/40 px-3 py-1 text-xs text-eye-purple">🔗 결속</span>
                  )}
                </div>
              )}
            </div>
          );
        })()
      ) : (
        <p className="mt-3 text-sm text-text-light">이 별과의 궁합은 아직 볼 수 없어.</p>
      )}
```

> 위 블록은 기존 `oriented ? (...) : (...)` 삼항을 통째로 대체한다. 변경점: ①`iSee`/`theySee`를 COPY 룩업(폴백 중립 라벨)으로 ②비공개 문구 존댓말→반말("아직 볼 수 없어").

- [ ] **Step 3: tsc + 빌드** — Run: `npx tsc --noEmit` → exit 0. `npm run build` → exit 0.

- [ ] **Step 4: 커밋**

```bash
git add components/byeoljari/OneToOnePanel.tsx
git commit -m "feat(byeoljari): P3-3 1:1 뷰 관계분류별 카피 조립(COPY 룩업·폴백)+톤 반말"
```

---

### Task 4: 통합 검증 + 브라우저 E2E

**Files:** 없음(검증). 이슈 시 해당 파일 수정 후 재검증.

- [ ] **Step 1: 전체 유닛 회귀** — Run: `node --import tsx --test lib/byeoljari/*.test.ts` → 기존 + copy(4) + orientEdge 신규 전부 pass.

- [ ] **Step 2: tsc + build** — Run: `npx tsc --noEmit && npm run build` → 둘 다 exit 0.

- [ ] **Step 3: dev 서버** — preview_start `{name: "byeolkong-dev"}` (이미 떠 있으면 재사용).

- [ ] **Step 4: 관계분류별 문구 실물 확인** — dev 데이터에서 1:1 뷰 진입해 문구 확인:
  - navigate `http://localhost:3000/fortune/byeoljari/hKQDZjRoOd` (15인 혼합맵, 친구/연인/지인/윗사람 섞임).
  - 관계선/노드 클릭(computer 또는 javascript로 노드 click 디스패치) → 1:1 패널.
  - **확인**: "내가 보는 그"/"그가 보는 나" 문구가 **관계분류별 40칸 카피**(예 친구="믿고 기댈 친구")로 뜨는지, 중립 라벨("든든한 지원군")이 아닌지. `getComputedStyle`/`innerText`로 단정.
  - ⚠️ 문구가 뜨려면 edge가 있어야(호스트 낀 엣지는 항상, 게스트끼리는 compat_visible 양쪽). pivot=me(없으면 호스트). 문구가 중립 라벨로만 나오면 relationType 값 또는 조립 경로 점검.
  - read_console_messages → 에러 0.

- [ ] **Step 5: 폴백 확인(선택)** — javascript로 `relationTenGodCopy` 경로가 미지 relationType에 null→중립 폴백하는지는 유닛이 커버. 실물은 정상 4종만 있으면 생략 가능.

- [ ] **Step 6: 최종 보고** — 스크린샷(타임아웃 시 DOM 단정) + 관계분류별 문구 차이 증거. 이슈 없으면 완료.

---

## Self-Review (작성자 체크)

**1. 스펙 커버리지:** §1 축(십신10·관계4·오행5) → T1 테이블 ✅ / §2 40칸 → T1 전량 ✅ / §3 오행 공통 유지 → 변경 없음(현행) ✅ / §4 데이터흐름(edge 십신·orientEdge·copy·panel) → T2·T3 ✅ / §5 UI+톤 → T3 ✅ / §7 테스트 → T1·T2·T4 ✅

**2. 플레이스홀더:** 40칸 전량 실제 문구, 코드 완비. 없음 ✅

**3. 타입 정합:** `TenGod`(pairing.ts) → copy.ts `Record<TenGod>` → 완전성 강제. `orientEdge` 반환 5필드 → OneToOnePanel `oriented` 타입 일치(T2 정의 = T3 소비). `relationTenGodCopy(string,string):string|null` → T3 `?? 폴백` ✅. `GraphEdge` +2필드(T2) → API push(T2)·orientEdge(T2) 사용 일치 ✅

**관련:** 스펙 `2026-08-17-p3-3-카피-엔진-design.md` · [[star-inyeon-map-progress]] · [[recurring-crash-class-config-by-dynamic-key]].
