# 운세 리포트 luna 전환 + 구조화 출력 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 유료 운세 리포트 5종 + 사주 chat resume 를 `claude-sonnet-5` → `gpt-5.6-luna` 로 이관하고, 활성 JSON 리포트 4종(saju_full·monthly·compat·compat_social)에 OpenAI 구조화 출력(`json_schema` strict)을 적용해 "report parse failed" 를 구조적으로 제거한다.

**Architecture:** 리포트 생성은 `generateOnce → streamChat → adapter.stream` 경로. 어댑터 계약(`AdapterStreamArgs`)에 옵셔널 `responseFormat` 를 추가하고, OpenAI 어댑터만 이를 `response_format:{type:"json_schema",strict:true}` 로 변환한다(anthropic/gemini 는 무시=우아한 폴백). 스키마는 각 리포트 모듈에 co-locate 하고, 기존 `parseXReportJson` 은 개수·정규화 2차 검증으로 유지한다. 구조화 출력은 sonnet 에선 무해(anthropic 무시)하므로 **어댑터·스키마·배선을 먼저 랜딩한 뒤 마지막에 모델을 luna 로 플립**해 "luna인데 스키마 없음" 상태를 만들지 않는다.

**Tech Stack:** Next 16 · TypeScript strict · OpenAI SDK(`openai@7.4`) · node:test 유닛(`node --import tsx --test`) · dev only 배포(prod=로드맵 ④).

**Phase 0 — 카나리아 (✅ 2026-08-29 완료·기록):** `gpt-5.6-luna` 가 스트리밍에서 `response_format: json_schema`(strict) 를 수용하고 스키마대로 유효 JSON 을 반환함을 실 API 로 확인(임시 `scripts/luna-canary.ts` 로 검증 후 삭제). → 아래 primary(strict) 경로로 진행. (폴백 `json_object` 는 불필요해짐.)

---

## 파일 구조

- `lib/claude/adapters/types.ts` — `AdapterStreamArgs` 에 `responseFormat?` 추가 (계약).
- `lib/claude/adapters/openai.ts` — 순수 헬퍼 `openaiResponseFormat()` 추가 + `stream()` 에서 사용.
- `lib/claude/adapters/openai-params.test.ts` — **신규** 헬퍼 유닛.
- `lib/claude.ts` — `streamChat`/`generateOnce` 시그니처에 `responseFormat?` 6번째 인자 추가·전달.
- `lib/fortune/saju-full-report.ts` · `monthly-report.ts` · `compat-report.ts` — 각 `*_REPORT_SCHEMA` co-locate export.
- `lib/fortune/report-schemas.test.ts` — **신규** 스키마 strict 불변식 유닛.
- `lib/fortune/response-format.ts` — **신규** `fortuneResponseFormat(type)` 헬퍼.
- `lib/fortune/response-format.test.ts` — **신규** 헬퍼 유닛.
- `app/api/fortune/create/route.ts` — generateOnce 호출에 responseFormat 전달 + monthly/saju_full/compat 재시도 raw 로깅.
- `lib/fortune/model.ts` · `model.test.ts` — `FORTUNE_REPORT_MODEL` + fortuneModel 플립.
- `app/api/consultations/saju/chat/route.ts` — streamChat 에 `CHAT_MODEL` 배선.
- `lib/prompt-version.ts` — 코호트 bump.
- `lib/fortune/types.ts` — `MAX_TOKENS_BY_FORTUNE` luna 재정합(Task 9 실측 후).
- `scripts/fortune-length-probe-luna.ts` — **신규** luna 실측 프로브.

---

## Task 1: 어댑터 계약에 responseFormat 추가 + OpenAI 헬퍼

**Files:**
- Modify: `lib/claude/adapters/types.ts` (AdapterStreamArgs)
- Modify: `lib/claude/adapters/openai.ts`
- Test: `lib/claude/adapters/openai-params.test.ts` (create)

- [ ] **Step 1: 계약에 옵셔널 필드 추가**

`lib/claude/adapters/types.ts` 의 `AdapterStreamArgs` 에 필드 추가:

```typescript
export interface AdapterStreamArgs {
  /** 페르소나 등 정적 블록(프로바이더별 캐시 마킹 대상). */
  systemStatic: string;
  /** turn-specific 동적 블록(캐시 미마킹). 없으면 빈 문자열. */
  systemDynamic: string;
  messages: { role: "user" | "assistant"; content: string }[];
  maxTokens: number;
  /** registry 가 고른 구체 model id (예: "gpt-5-mini"). */
  model: string;
  /** 구조화 출력 스키마(운세 리포트용). OpenAI 어댑터만 사용, 나머지는 무시. */
  responseFormat?: { name: string; schema: object };
}
```

- [ ] **Step 2: 실패하는 헬퍼 테스트 작성**

`lib/claude/adapters/openai-params.test.ts` 생성:

```typescript
// lib/claude/adapters/openai-params.test.ts
// OpenAI response_format 조립 헬퍼 유닛 — responseFormat 유무에 따른 파라미터 분기.
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { openaiResponseFormat } from "./openai.ts";

describe("openaiResponseFormat", () => {
  it("responseFormat 없으면 빈 객체(=create 파라미터에 response_format 미주입)", () => {
    assert.deepEqual(openaiResponseFormat(undefined), {});
  });
  it("responseFormat 있으면 json_schema strict 로 감싼다", () => {
    const schema = { type: "object", additionalProperties: false, properties: {}, required: [] };
    const out = openaiResponseFormat({ name: "x_report", schema });
    assert.deepEqual(out, {
      response_format: {
        type: "json_schema",
        json_schema: { name: "x_report", strict: true, schema },
      },
    });
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `node --import tsx --test lib/claude/adapters/openai-params.test.ts`
Expected: FAIL — `openaiResponseFormat` export 없음.

- [ ] **Step 4: 헬퍼 구현 + stream() 배선**

`lib/claude/adapters/openai.ts` 에 순수 헬퍼를 추가하고 `stream()` 이 사용하게 한다. `stream()` 의 `create({...})` 인자에 `...openaiResponseFormat(responseFormat)` 를 스프레드로 병합(없으면 `{}` 라 기존 동작 그대로).

```typescript
/** responseFormat → OpenAI create 파라미터 조각. 없으면 {} (response_format 미주입 = 기존 동작). */
export function openaiResponseFormat(
  rf: { name: string; schema: object } | undefined
): { response_format?: { type: "json_schema"; json_schema: { name: string; strict: true; schema: object } } } {
  if (!rf) return {};
  return {
    response_format: {
      type: "json_schema",
      json_schema: { name: rf.name, strict: true, schema: rf.schema },
    },
  };
}

export const openaiAdapter: ProviderAdapter = {
  async *stream({ systemStatic, systemDynamic, messages, maxTokens, model, responseFormat }: AdapterStreamArgs) {
    const system = systemDynamic ? `${systemStatic}\n\n---\n\n${systemDynamic}` : systemStatic;
    const stream = await client().chat.completions.create({
      model,
      max_completion_tokens: maxTokens,
      reasoning_effort: "low",
      stream: true,
      messages: [{ role: "system", content: system }, ...messages],
      ...openaiResponseFormat(responseFormat),
    });
    let stop: StopReason = null;
    for await (const chunk of stream) {
      const delta = chunk.choices[0]?.delta?.content;
      if (delta) yield delta;
      const fr = chunk.choices[0]?.finish_reason;
      if (fr) stop = mapOpenAIFinish(fr);
    }
    return stop;
  },
  isRetryableError(err: unknown) {
    const status = (err as { status?: number })?.status;
    return status === 429 || status === 500 || status === 503 || status === 529;
  },
};
```

`stream()` 시그니처의 구조분해에 `responseFormat` 를 추가했는지 확인. anthropic/gemini 어댑터는 `responseFormat` 를 구조분해하지 않으므로 자동으로 무시된다(변경 없음).

- [ ] **Step 5: 테스트 통과 확인**

Run: `node --import tsx --test lib/claude/adapters/openai-params.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 6: tsc + 커밋**

Run: `npx tsc --noEmit`
Expected: exit 0

```bash
git add lib/claude/adapters/types.ts lib/claude/adapters/openai.ts lib/claude/adapters/openai-params.test.ts
git commit -m "feat(claude): 어댑터 계약에 responseFormat + OpenAI json_schema 헬퍼"
```

---

## Task 2: streamChat / generateOnce 에 responseFormat 전달

**Files:**
- Modify: `lib/claude.ts` (`streamChat` ~L346, `generateOnce` ~L440)

- [ ] **Step 1: streamChat 시그니처 + 어댑터 전달**

`streamChat` 에 6번째 옵셔널 인자를 추가하고, `adapter.stream({...})` 호출(현재 L374)에 넘긴다.

```typescript
export async function* streamChat(
  systemMessage: { staticPart: string; dynamicPart: string } | string,
  messages: { role: "user" | "assistant"; content: string }[],
  maxTokens: number = 2660,
  logCtx?: LogContext,
  model?: string,
  responseFormat?: { name: string; schema: object }
) {
  const resolved = resolveChatModel(model);
  const adapter = ADAPTERS[providerOf(resolved)];

  const systemStatic =
    typeof systemMessage === "string" ? systemMessage : systemMessage.staticPart;
  const systemDynamic =
    typeof systemMessage === "string" ? "" : systemMessage.dynamicPart;

  const MAX_ATTEMPTS = 2;
  let lastStopReason: StopReason = null;

  for (let attempt = 1; attempt <= MAX_ATTEMPTS; attempt++) {
    const it = adapter.stream({ systemStatic, systemDynamic, messages, maxTokens, model: resolved, responseFormat });
    // ... 이하 기존 재시도/빈응답 로직 그대로 ...
```

⚠️ `adapter.stream({...})` 호출은 for 루프 안 1곳(현재 L374)뿐 — 거기에 `responseFormat` 만 추가한다. 나머지 재시도·빈응답·로깅 로직은 손대지 않는다.

- [ ] **Step 2: generateOnce 시그니처 + 전달**

```typescript
export async function generateOnce(
  systemMessage: { staticPart: string; dynamicPart: string } | string,
  messages: { role: "user" | "assistant"; content: string }[],
  maxTokens: number = 2660,
  logCtx?: LogContext,
  model?: string,
  responseFormat?: { name: string; schema: object }
): Promise<string> {
  let out = "";
  const it = streamChat(systemMessage, messages, maxTokens, logCtx, model, responseFormat);
  let res = await it.next();
  while (!res.done) {
    out += res.value;
    res = await it.next();
  }
  if (res.value === "max_tokens") {
    void logWarn("generateOnce hit max_tokens — response truncated", {
      ...logCtx,
      extra: { ...logCtx?.extra, maxTokens, outputLen: out.length },
    });
  }
  return out.trim();
}
```

- [ ] **Step 3: tsc 확인**

Run: `npx tsc --noEmit`
Expected: exit 0 (기존 호출부는 5개 인자라 6번째 옵셔널 = 무영향)

- [ ] **Step 4: 기존 유닛 회귀 확인**

Run: `node --import tsx --test lib/claude/adapters/parse.test.ts lib/claude/adapters/openai-params.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add lib/claude.ts
git commit -m "feat(claude): streamChat/generateOnce 에 responseFormat 전달 배선"
```

---

## Task 3: 리포트별 JSON 스키마 co-locate + 불변식 테스트

**Files:**
- Modify: `lib/fortune/saju-full-report.ts` · `monthly-report.ts` · `compat-report.ts`
- Test: `lib/fortune/report-schemas.test.ts` (create)

OpenAI strict 규칙: 모든 object 에 `additionalProperties: false`, 모든 property 는 `required` 에 포함. 개수 규칙(월 12·주 4·섹션 5·advice 3·supplements 2~4)은 strict 가 강제 못 하므로 스키마엔 안 넣고 기존 파서가 계속 담당한다.

- [ ] **Step 1: 실패하는 스키마 불변식 테스트 작성**

`lib/fortune/report-schemas.test.ts` 생성:

```typescript
// lib/fortune/report-schemas.test.ts
// 리포트 JSON 스키마의 OpenAI strict 불변식 검사: 모든 object 는 additionalProperties:false +
// 모든 property 가 required 에 포함. (개수 규칙은 스키마가 아니라 parseXReportJson 담당.)
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { SAJU_FULL_REPORT_SCHEMA } from "./saju-full-report.ts";
import { MONTHLY_REPORT_SCHEMA } from "./monthly-report.ts";
import { COMPAT_REPORT_SCHEMA } from "./compat-report.ts";

type JsonSchema = {
  type?: string;
  additionalProperties?: boolean;
  properties?: Record<string, JsonSchema>;
  required?: string[];
  items?: JsonSchema;
  enum?: unknown[];
};

/** 모든 object 노드가 strict 규칙을 만족하는지 재귀 검사. 위반 경로 목록 반환. */
function strictViolations(node: JsonSchema, path = "$"): string[] {
  const out: string[] = [];
  if (node.type === "object") {
    if (node.additionalProperties !== false) out.push(`${path}: additionalProperties!=false`);
    const props = Object.keys(node.properties ?? {});
    const req = new Set(node.required ?? []);
    for (const p of props) if (!req.has(p)) out.push(`${path}.${p}: not in required`);
    for (const p of props) out.push(...strictViolations(node.properties![p], `${path}.${p}`));
  }
  if (node.type === "array" && node.items) out.push(...strictViolations(node.items, `${path}[]`));
  return out;
}

describe("report schemas — OpenAI strict 불변식", () => {
  for (const [name, schema] of [
    ["saju_full", SAJU_FULL_REPORT_SCHEMA],
    ["monthly", MONTHLY_REPORT_SCHEMA],
    ["compat", COMPAT_REPORT_SCHEMA],
  ] as const) {
    it(`${name}: 전 object additionalProperties:false + 전 property required`, () => {
      assert.deepEqual(strictViolations(schema as JsonSchema), []);
    });
  }

  it("saju_full 최상위 필드 집합이 인터페이스와 일치", () => {
    const req = (SAJU_FULL_REPORT_SCHEMA as JsonSchema).required!.slice().sort();
    assert.deepEqual(req, [
      "actions", "lucky", "mission", "monthly", "note", "relations2026",
      "self", "summary", "theme", "timing", "year",
    ]);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --import tsx --test lib/fortune/report-schemas.test.ts`
Expected: FAIL — `SAJU_FULL_REPORT_SCHEMA` 등 export 없음.

- [ ] **Step 3: saju-full-report.ts 에 스키마 추가**

`SajuFullReportAI` 인터페이스 아래에 추가:

```typescript
/** OpenAI 구조화 출력 스키마 — SajuFullReportAI 미러. strict: 전 필드 required + additionalProperties:false.
 *  개수 규칙(monthly 12·actions 3·supplements 2~4)은 parseSajuFullReportJson 이 담당(strict 로 강제 불가). */
export const SAJU_FULL_REPORT_SCHEMA = {
  type: "object",
  additionalProperties: false,
  properties: {
    theme: { type: "string" },
    summary: { type: "string" },
    lucky: {
      type: "object",
      additionalProperties: false,
      properties: {
        color: { type: "string" },
        direction: { type: "string" },
        months: { type: "string" },
        keyword: { type: "string" },
      },
      required: ["color", "direction", "months", "keyword"],
    },
    self: {
      type: "object",
      additionalProperties: false,
      properties: {
        nature: { type: "string" },
        strength: { type: "string" },
        caution: { type: "string" },
        balance: {
          type: "object",
          additionalProperties: false,
          properties: {
            lack: { type: "string" },
            supplements: { type: "array", items: { type: "string" } },
          },
          required: ["lack", "supplements"],
        },
        aptitude: { type: "string" },
      },
      required: ["nature", "strength", "caution", "balance", "aptitude"],
    },
    year: {
      type: "object",
      additionalProperties: false,
      properties: {
        flow: { type: "string" },
        mind: { type: "string" },
        love: { type: "string" },
        relationship: { type: "string" },
        career: { type: "string" },
        wealth: { type: "string" },
        health: { type: "string" },
      },
      required: ["flow", "mind", "love", "relationship", "career", "wealth", "health"],
    },
    relations2026: { type: "string" },
    mission: { type: "string" },
    monthly: {
      type: "array",
      items: {
        type: "object",
        additionalProperties: false,
        properties: { month: { type: "number" }, body: { type: "string" } },
        required: ["month", "body"],
      },
    },
    timing: {
      type: "object",
      additionalProperties: false,
      properties: { good: { type: "string" }, caution: { type: "string" } },
      required: ["good", "caution"],
    },
    actions: { type: "array", items: { type: "string" } },
    note: { type: "string" },
  },
  required: [
    "theme", "summary", "lucky", "self", "year", "relations2026",
    "mission", "monthly", "timing", "actions", "note",
  ],
} as const;
```

- [ ] **Step 4: monthly-report.ts 에 스키마 추가**

`MonthlyReportAI` 아래에 추가:

```typescript
/** OpenAI 구조화 출력 스키마 — MonthlyReportAI 미러. 개수 규칙(weekly 4·sections 5)은 parseMonthlyReportJson 담당. */
export const MONTHLY_REPORT_SCHEMA = {
  type: "object",
  additionalProperties: false,
  properties: {
    stars: { type: "number" },
    theme: { type: "string" },
    summary: { type: "string" },
    lucky: {
      type: "object",
      additionalProperties: false,
      properties: { keyword: { type: "string" }, color: { type: "string" } },
      required: ["keyword", "color"],
    },
    intro: { type: "string" },
    weekly: {
      type: "array",
      items: {
        type: "object",
        additionalProperties: false,
        properties: { week: { type: "number" }, body: { type: "string" } },
        required: ["week", "body"],
      },
    },
    sections: {
      type: "array",
      items: {
        type: "object",
        additionalProperties: false,
        properties: {
          key: { type: "string", enum: ["money", "work", "love", "health", "study"] },
          body: { type: "string" },
        },
        required: ["key", "body"],
      },
    },
    timing: {
      type: "object",
      additionalProperties: false,
      properties: { good: { type: "string" }, caution: { type: "string" } },
      required: ["good", "caution"],
    },
    action: { type: "string" },
    balance: {
      type: "object",
      additionalProperties: false,
      properties: { good: { type: "string" }, warn: { type: "string" } },
      required: ["good", "warn"],
    },
    note: { type: "string" },
  },
  required: [
    "stars", "theme", "summary", "lucky", "intro", "weekly",
    "sections", "timing", "action", "balance", "note",
  ],
} as const;
```

- [ ] **Step 5: compat-report.ts 에 스키마 추가**

`CompatReportAI` 아래에 추가(등급 enum 은 `COMPAT_GRADES` 합집합 재사용 — 종류별 서브셋은 프롬프트가 강제, 파서도 합집합 허용이라 정합):

```typescript
/** OpenAI 구조화 출력 스키마 — CompatReportAI 미러(compat/compat_social 공유). advice 3개 규칙은 parseCompatReportJson 담당.
 *  grade enum 은 합집합 — 종류별 서브셋 강제는 프롬프트가, 파서(isGrade)도 합집합 허용이라 현행과 정합. */
export const COMPAT_REPORT_SCHEMA = {
  type: "object",
  additionalProperties: false,
  properties: {
    grade: { type: "string", enum: [...COMPAT_GRADES] },
    theme: { type: "string" },
    summary: { type: "string" },
    chemistry: { type: "string" },
    attraction: { type: "string" },
    conflict: { type: "string" },
    communication: { type: "string" },
    longterm: { type: "string" },
    growth: { type: "string" },
    advice: { type: "array", items: { type: "string" } },
    note: { type: "string" },
  },
  required: [
    "grade", "theme", "summary", "chemistry", "attraction", "conflict",
    "communication", "longterm", "growth", "advice", "note",
  ],
} as const;
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `node --import tsx --test lib/fortune/report-schemas.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 7: tsc + 커밋**

Run: `npx tsc --noEmit`
Expected: exit 0

```bash
git add lib/fortune/saju-full-report.ts lib/fortune/monthly-report.ts lib/fortune/compat-report.ts lib/fortune/report-schemas.test.ts
git commit -m "feat(fortune): 리포트 3종 JSON 스키마 co-locate (saju_full·monthly·compat)"
```

---

## Task 4: fortuneResponseFormat 헬퍼

**Files:**
- Create: `lib/fortune/response-format.ts`
- Test: `lib/fortune/response-format.test.ts` (create)

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
// lib/fortune/response-format.test.ts
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { fortuneResponseFormat } from "./response-format.ts";

describe("fortuneResponseFormat", () => {
  it("활성 JSON 리포트 4종은 responseFormat 반환", () => {
    for (const t of ["saju_full", "monthly", "compat", "compat_social"] as const) {
      const rf = fortuneResponseFormat(t);
      assert.ok(rf, `${t} 는 responseFormat 있어야`);
      assert.equal(typeof rf!.name, "string");
      assert.equal((rf!.schema as { type?: string }).type, "object");
    }
  });
  it("compat/compat_social 은 같은 스키마 공유", () => {
    assert.equal(fortuneResponseFormat("compat")!.schema, fortuneResponseFormat("compat_social")!.schema);
  });
  it("good_days(마크다운)·daily(nano)·tarot(비활성)은 undefined", () => {
    for (const t of ["good_days", "daily", "tarot_daily", "tarot_love", "tarot_money", "tarot_career", "tarot_relation"] as const) {
      assert.equal(fortuneResponseFormat(t), undefined);
    }
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --import tsx --test lib/fortune/response-format.test.ts`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 헬퍼 구현**

```typescript
// lib/fortune/response-format.ts
// 운세 리포트 종류 → OpenAI 구조화 출력 스키마 매핑. 활성 JSON 리포트 4종만 반환하고,
// good_days(마크다운)·무료 daily(nano)·비활성 tarot 은 undefined(구조화 출력 미적용).
// 🔴 tarot 리포트 재활성(FORTUNE_CONFIG active:true) 시 여기에 tarot 스키마 추가 필수.
import type { FortuneType } from "./types.ts";
import { SAJU_FULL_REPORT_SCHEMA } from "./saju-full-report.ts";
import { MONTHLY_REPORT_SCHEMA } from "./monthly-report.ts";
import { COMPAT_REPORT_SCHEMA } from "./compat-report.ts";

export function fortuneResponseFormat(
  type: FortuneType
): { name: string; schema: object } | undefined {
  switch (type) {
    case "saju_full":
      return { name: "saju_full_report", schema: SAJU_FULL_REPORT_SCHEMA };
    case "monthly":
      return { name: "monthly_report", schema: MONTHLY_REPORT_SCHEMA };
    case "compat":
    case "compat_social":
      return { name: "compat_report", schema: COMPAT_REPORT_SCHEMA };
    default:
      return undefined;
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --import tsx --test lib/fortune/response-format.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: tsc + 커밋**

Run: `npx tsc --noEmit`
Expected: exit 0

```bash
git add lib/fortune/response-format.ts lib/fortune/response-format.test.ts
git commit -m "feat(fortune): fortuneResponseFormat 종류→스키마 매핑 헬퍼"
```

---

## Task 5: create 라우트 배선 (responseFormat 전달 + 재시도 raw 로깅)

**Files:**
- Modify: `app/api/fortune/create/route.ts`

구조화 출력은 sonnet(현재 모델)에선 anthropic 어댑터가 무시하므로 이 태스크는 **행동 무변경**(luna 플립하는 Task 6 에서 활성). 함께 `fortune-report-parse-failure` 가 지목한 재시도 raw 로깅 갭을 monthly/saju_full/compat 3분기에 메운다.

- [ ] **Step 1: import 추가**

`app/api/fortune/create/route.ts` 상단 import 에 추가:

```typescript
import { fortuneResponseFormat } from "@/lib/fortune/response-format";
```

- [ ] **Step 2: 최초 generateOnce 에 responseFormat 전달**

현재 L415:

```typescript
report = await generateOnce(system, [{ role: "user", content: FORTUNE_KICKOFF }], MAX_TOKENS_BY_FORTUNE[cfg.type], fortuneLogCtx, fortuneModel(cfg.type), fortuneResponseFormat(cfg.type));
```

- [ ] **Step 3: monthly 분기 — 재시도 responseFormat + retry raw 로깅**

현재 monthly 분기(L449~L470)를 다음으로 교체. `retryRaw` 를 hoist 해 실패 로그에 재시도 원문 꼬리를 남긴다:

```typescript
    } else if (cfg.type === "monthly") {
      let ai = parseMonthlyReportJson(report);
      let retryRaw: string | undefined;
      if (!ai) {
        try {
          const system = buildFortuneSystem(cfg.type, { saju });
          retryRaw = await generateOnce(system, [{ role: "user", content: FORTUNE_KICKOFF }], MAX_TOKENS_BY_FORTUNE[cfg.type], fortuneLogCtx, fortuneModel(cfg.type), fortuneResponseFormat(cfg.type));
          ai = parseMonthlyReportJson(retryRaw);
        } catch (err) {
          await logError(err, { route: "/api/fortune/create", userId, extra: { type, stage: "monthly_retry" } });
        }
      }
      if (!ai || !saju?.temporal) {
        await failGeneration(new Error("monthly report parse failed"), "monthly_parse", {
          rawLen: report.length,
          rawHead: report.slice(0, 2000),
          rawTail: report.slice(-400),
          retryRawHead: retryRaw?.slice(0, 2000),
          retryRawTail: retryRaw?.slice(-400),
          hasTemporal: !!saju?.temporal,
        });
        return;
      }
      storedContent = serializeMonthlyReport(buildMonthlyReport(ai, saju.temporal));
```

- [ ] **Step 4: saju_full 분기 — 동일 패턴**

현재 saju_full 분기(L471~L490)를 교체:

```typescript
    } else if (cfg.type === "saju_full") {
      let ai = parseSajuFullReportJson(report);
      let retryRaw: string | undefined;
      if (!ai) {
        try {
          const system = buildFortuneSystem(cfg.type, { saju });
          retryRaw = await generateOnce(system, [{ role: "user", content: FORTUNE_KICKOFF }], MAX_TOKENS_BY_FORTUNE[cfg.type], fortuneLogCtx, fortuneModel(cfg.type), fortuneResponseFormat(cfg.type));
          ai = parseSajuFullReportJson(retryRaw);
        } catch (err) {
          await logError(err, { route: "/api/fortune/create", userId, extra: { type, stage: "saju_full_retry" } });
        }
      }
      if (!ai) {
        await failGeneration(new Error("saju_full report parse failed"), "saju_full_parse", {
          rawLen: report.length,
          rawHead: report.slice(0, 2000),
          rawTail: report.slice(-400),
          retryRawHead: retryRaw?.slice(0, 2000),
          retryRawTail: retryRaw?.slice(-400),
        });
        return;
      }
      storedContent = serializeSajuFullReport(buildSajuFullReport(ai));
```

- [ ] **Step 5: compat 분기 — 동일 패턴**

현재 compat 분기(L491~L510)를 교체:

```typescript
    } else if (cfg.type === "compat" || cfg.type === "compat_social") {
      let ai = parseCompatReportJson(report);
      let retryRaw: string | undefined;
      if (!ai) {
        try {
          const system = buildFortuneSystem(cfg.type, systemInput);
          retryRaw = await generateOnce(system, [{ role: "user", content: FORTUNE_KICKOFF }], MAX_TOKENS_BY_FORTUNE[cfg.type], fortuneLogCtx, fortuneModel(cfg.type), fortuneResponseFormat(cfg.type));
          ai = parseCompatReportJson(retryRaw);
        } catch (err) {
          await logError(err, { route: "/api/fortune/create", userId, extra: { type, stage: "compat_retry" } });
        }
      }
      if (!ai) {
        await failGeneration(new Error("compat report parse failed"), "compat_parse", {
          rawLen: report.length,
          rawHead: report.slice(0, 2000),
          rawTail: report.slice(-400),
          retryRawHead: retryRaw?.slice(0, 2000),
          retryRawTail: retryRaw?.slice(-400),
        });
        return;
      }
      storedContent = serializeCompatReport(buildCompatReport(ai));
```

⚠️ daily 분기(L428~448)와 tarot 분기(L511~532)는 이번 스코프 밖 — `fortuneResponseFormat` 이 이들엔 undefined 를 주므로 최초 generateOnce(Step 2)만으로 안전하게 no-op. daily 재시도(L433)·tarot 재시도(L516)에도 `fortuneResponseFormat(cfg.type)` 를 6번째 인자로 넣어 일관성 유지(값은 undefined = 무해). retry raw 로깅은 파싱 실패가 관측된 3종만.

- [ ] **Step 6: daily/tarot 재시도 호출에도 인자 일관성 부여**

L433(daily 재시도)·L516(tarot 재시도)의 `generateOnce(...)` 끝에 `, fortuneResponseFormat(cfg.type)` 추가(undefined 전달 = 무해, 시그니처 일관).

- [ ] **Step 7: tsc + 빌드 확인**

Run: `npx tsc --noEmit`
Expected: exit 0

Run: `npm run build`
Expected: exit 0 (라우트 컴파일 통과)

- [ ] **Step 8: 커밋**

```bash
git add app/api/fortune/create/route.ts
git commit -m "feat(fortune): create 라우트에 구조화 출력 + 재시도 raw 로깅 배선"
```

---

## Task 6: 모델 플립 — 유료 리포트 → luna

**Files:**
- Modify: `lib/fortune/model.ts`
- Test: `lib/fortune/model.test.ts`

이 태스크가 실제로 리포트를 luna 로 넘기고 Task 1~5 의 구조화 출력을 활성화한다.

- [ ] **Step 1: 테스트를 luna 기대값으로 갱신(먼저 실패)**

`lib/fortune/model.test.ts` 교체:

```typescript
// lib/fortune/model.test.ts
// fortune one-shot 모델 정책 매핑 유닛. 무료 데일리=저가(nano), 나머지=luna 리포트 모델.
import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { fortuneModel, FORTUNE_CHEAP_MODEL, FORTUNE_REPORT_MODEL } from "./model.ts";

describe("fortuneModel", () => {
  it("무료 데일리는 저가 모델(nano)", () => {
    assert.equal(fortuneModel("daily"), FORTUNE_CHEAP_MODEL);
    assert.equal(fortuneModel("tarot_daily"), FORTUNE_CHEAP_MODEL);
  });
  it("유료 리포트 5종은 luna 리포트 모델", () => {
    for (const t of ["monthly", "saju_full", "compat", "compat_social", "good_days"] as const) {
      assert.equal(fortuneModel(t), FORTUNE_REPORT_MODEL);
    }
  });
  it("비활성 타로 리포트도 규칙상 luna 로 라우팅", () => {
    for (const t of ["tarot_love", "tarot_money", "tarot_career", "tarot_relation"] as const) {
      assert.equal(fortuneModel(t), FORTUNE_REPORT_MODEL);
    }
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --import tsx --test lib/fortune/model.test.ts`
Expected: FAIL — `FORTUNE_REPORT_MODEL` export 없음 + 값 불일치.

- [ ] **Step 3: model.ts 구현**

```typescript
// lib/fortune/model.ts
// fortune one-shot 모델 정책: 무료 데일리는 최저가(nano), 유료 리포트는 luna(구조화 출력으로 형식 안정).
// spec docs/superpowers/specs/2026-08-29-운세리포트-luna-구조화출력-design.md
import type { FortuneType } from "./types.ts";

// 무료 데일리 저가 모델.
export const FORTUNE_CHEAP_MODEL = "gpt-5-nano";
// 유료 리포트 모델 — 2026-08-29 sonnet→luna 이관. chat 의 CHAT_MODEL 과 우연히 같은 값이나 의미가 달라 별도 상수.
export const FORTUNE_REPORT_MODEL = "gpt-5.6-luna";

export function fortuneModel(type: FortuneType): string {
  return type === "daily" || type === "tarot_daily" ? FORTUNE_CHEAP_MODEL : FORTUNE_REPORT_MODEL;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --import tsx --test lib/fortune/model.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: tsc + 커밋**

Run: `npx tsc --noEmit`
Expected: exit 0

```bash
git add lib/fortune/model.ts lib/fortune/model.test.ts
git commit -m "feat(fortune): 유료 리포트 모델 sonnet→gpt-5.6-luna 플립"
```

---

## Task 7: 사주 chat resume → luna 통일

**Files:**
- Modify: `app/api/consultations/saju/chat/route.ts`

- [ ] **Step 1: streamChat 호출이 1곳인지 재확인**

Run: `grep -n "streamChat(" app/api/consultations/saju/chat/route.ts`
Expected: 1곳(현재 L255). 2곳 이상이면 각각에 동일 배선.

- [ ] **Step 2: CHAT_MODEL import + 배선**

import 에 추가:

```typescript
import { CHAT_MODEL } from "@/lib/claude/model-registry";
```

L255 의 `streamChat(...)` 호출에 5번째 인자 `CHAT_MODEL` 추가(3번째 maxTokens 는 `undefined` 유지):

```typescript
        for await (const chunk of streamChat(systemMessage, body.messages, undefined, {
          route: "/api/consultations/saju/chat",
          userId,
          extra: { readingId: reading.id },
        }, CHAT_MODEL)) {
```

- [ ] **Step 3: tsc + 빌드**

Run: `npx tsc --noEmit`
Expected: exit 0

Run: `npm run build`
Expected: exit 0

- [ ] **Step 4: 커밋**

```bash
git add app/api/consultations/saju/chat/route.ts
git commit -m "feat(saju): 사주 chat resume 를 CHAT_MODEL(luna)로 통일"
```

---

## Task 8: PROMPT_VERSION 코호트 bump

**Files:**
- Modify: `lib/prompt-version.ts`

- [ ] **Step 1: 버전 문자열 + 히스토리 주석 추가**

히스토리 목록 맨 아래에 한 줄 추가하고 상수 값 교체:

```typescript
//   2026-08-29-fortune-luna   — 유료 리포트 5종 + 사주 chat resume sonnet→gpt-5.6-luna 이관 +
//                               활성 JSON 리포트 4종 구조화 출력(json_schema strict) (spec: 2026-08-29-운세리포트-luna-구조화출력)
export const PROMPT_VERSION = "2026-08-29-fortune-luna";
```

- [ ] **Step 2: tsc + 커밋**

Run: `npx tsc --noEmit`
Expected: exit 0

```bash
git add lib/prompt-version.ts
git commit -m "chore(prompt): PROMPT_VERSION bump — 2026-08-29-fortune-luna 코호트"
```

---

## Task 9: luna 실측 프로브 + max_tokens 밴드 재정합

**Files:**
- Create: `scripts/fortune-length-probe-luna.ts`
- Modify: `lib/fortune/types.ts` (`MAX_TOKENS_BY_FORTUNE`) — 실측 결과에 따라

🔴 구조화 출력은 `max_tokens` 절단을 못 막는다 — luna 가 길게 써서 캡을 넘기면 절단된 불완전 JSON → 실패. 이 태스크로 luna 실측 길이를 재고 캡을 꼬리 위로 올린다. 프로브는 프로덕션 경로(`generateOnce` + `fortuneModel` + `fortuneResponseFormat`)를 그대로 태워 파싱까지 재현한다.

- [ ] **Step 1: luna 프로브 스크립트 작성**

```typescript
// scripts/fortune-length-probe-luna.ts — TEMP. Run: node --import tsx --env-file=.env.local scripts/fortune-length-probe-luna.ts
// 유료 리포트 5종을 프로덕션 경로(generateOnce→luna→구조화 출력)로 생성해 (a)파싱 성공 여부 (b)산문 길이를 측정.
// 파싱 실패(=절단/형식) 나면 해당 type 의 MAX_TOKENS_BY_FORTUNE 를 올려야 한다. ⚠️ 변동폭 크니 여러 번 실행.
import { buildFortuneSystem, FORTUNE_KICKOFF } from "@/lib/fortune/prompt";
import { MAX_TOKENS_BY_FORTUNE, FORTUNE_CONFIG, type FortuneType } from "@/lib/fortune/types";
import { fortuneModel } from "@/lib/fortune/model";
import { fortuneResponseFormat } from "@/lib/fortune/response-format";
import { generateOnce } from "@/lib/claude";
import { parseMonthlyReportJson } from "@/lib/fortune/monthly-report";
import { parseSajuFullReportJson } from "@/lib/fortune/saju-full-report";
import { parseCompatReportJson } from "@/lib/fortune/compat-report";
import { calcSaju, calcTemporalLuck, type SajuInput, type SajuResult } from "@/lib/saju/calc";

const BIRTH_A: SajuInput = { year: 1994, month: 5, day: 12, hour: 9, gender: "female" };
const BIRTH_B: SajuInput = { year: 1992, month: 11, day: 3, hour: 14, gender: "male" };

function sajuForType(type: FortuneType): SajuResult {
  const saju = calcSaju(BIRTH_A);
  if (type === "monthly") saju.temporal = calcTemporalLuck(new Date(), BIRTH_A.year);
  else if (type === "good_days") saju.temporal = calcTemporalLuck(new Date(), BIRTH_A.year, { includeMonth: true });
  return saju;
}

function parseFor(type: FortuneType, raw: string): boolean {
  if (type === "monthly") return !!parseMonthlyReportJson(raw);
  if (type === "saju_full") return !!parseSajuFullReportJson(raw);
  if (type === "compat" || type === "compat_social") return !!parseCompatReportJson(raw);
  return raw.trim().length > 0; // good_days = 마크다운
}

const TYPES: FortuneType[] = ["monthly", "compat", "compat_social", "good_days", "saju_full"];

async function main() {
  for (const type of TYPES) {
    const input =
      type === "compat" || type === "compat_social"
        ? { saju: sajuForType(type), sajuB: calcSaju(BIRTH_B), names: { a: "가", b: "나" } }
        : { saju: sajuForType(type) };
    const system = buildFortuneSystem(type, input);
    try {
      const raw = await generateOnce(system, [{ role: "user", content: FORTUNE_KICKOFF }], MAX_TOKENS_BY_FORTUNE[type], undefined, fortuneModel(type), fortuneResponseFormat(type));
      const ok = parseFor(type, raw);
      console.log(`${type.padEnd(14)} 정가 ${String(FORTUNE_CONFIG[type].cost).padStart(2)}별  raw ${String(raw.length).padStart(5)}자  parse=${ok ? "OK" : "❌FAIL(캡↑ 필요)"}  cap=${MAX_TOKENS_BY_FORTUNE[type]}`);
    } catch (err) {
      console.error(`${type.padEnd(14)} ERROR`, err instanceof Error ? err.message : err);
    }
  }
}
main().catch((e) => { console.error("probe 실패:", e); process.exit(1); });
```

- [ ] **Step 2: 프로브 실행(2~3회)**

Run: `node --import tsx --env-file=.env.local scripts/fortune-length-probe-luna.ts`
Expected: 5종 전부 `parse=OK`. ⚠️ `generateOnce` 는 절단 시 `logWarn` 을 콘솔에 남긴다 — "hit max_tokens" 경고가 뜨거나 `parse=❌FAIL` 이면 해당 type 캡 부족.

- [ ] **Step 3: 캡 조정 (실측 기반)**

`parse=FAIL` 또는 max_tokens 경고가 난 type 의 `MAX_TOKENS_BY_FORTUNE`(lib/fortune/types.ts) 를 실측 raw 길이 여유 위로 상향. 커밋 안 된 compat/compat_social `14000`(sonnet 기준 핫픽스)도 luna 실측으로 재확인 — 충분하면 유지, 부족하면 상향. 조정 후 Step 2 재실행해 5종 전부 `parse=OK` 확인.

⚠️ 판단 기준: n=1 노이즈가 크니 여유 마진(실측 꼬리 +15~20%)을 둘 것. saju_full 은 원래 분산이 큰 리포트(`scripts/fortune-length-probe.ts` 주석 참고).

- [ ] **Step 4: 프로브 삭제 + 커밋**

luna 프로브는 일회성 도구 — 캡 조정만 커밋하고 프로브는 삭제(기존 `scripts/fortune-length-probe.ts` 는 sonnet 용이라 그대로 둠).

```bash
rm scripts/fortune-length-probe-luna.ts
git add lib/fortune/types.ts
git commit -m "fix(fortune): MAX_TOKENS_BY_FORTUNE luna 실측 재정합"
```

⚠️ 이 커밋에 커밋 안 됐던 compat 14000 변경이 함께 스테이징된다(같은 파일). 의도된 것 — luna 재확인을 거친 값으로 확정.

---

## Task 10: dev E2E 검증 (리포트 4종 + good_days + 사주 chat)

**Files:** 없음(검증 태스크). dev 서버·QA 하네스 사용.

- [ ] **Step 1: 리포트 생성 E2E — 파싱·구조·톤**

Task 9 프로브가 이미 5종 `parse=OK` 를 확인했으므로 JSON 유효성은 커버됨. 추가로 **톤 육안 검증**: 프로브를 raw 출력하도록 잠깐 수정하거나(콘솔 raw 400자), 또는 dev 서버(`npm run dev`)에서 각 리포트를 실제 생성해 결과 화면 확인. 확인 포인트:
- 별콩이 fortune 페르소나 정합(`data/persona/byeolkong_fortune.md`) — 단정 예언 톤 없음, 흐름·가능성·선택.
- good_days: "향후 30일 목록 밖 날짜 지어내기 금지" 규칙을 luna 가 지키는지(구조화 아님 — 프롬프트 준수만).
- 마크다운 별표(**) 노출 없음.

- [ ] **Step 2: 사주 chat resume luna 스모크**

사주 진입은 폐쇄 — 기존 사주 reading 을 resume 해 한 턴 주고받는다. dev 서버에서 기존 사주 리딩 URL(`/saju/...`) 진입 → 메시지 전송 → luna 응답이 별콩이 톤으로 스트리밍되고 저장되는지 확인. (QA 하네스 `npm run qa` 는 chat 지면 대상이나 사주 chat 경로 포함 여부 불확실 — 포함 안 되면 이 수동 스모크로 충분, 저트래픽 레거시 지면.)

- [ ] **Step 3: error_logs 확인**

dev 에서 리포트 몇 건 생성 후 `error_logs` 에 `*_parse` 실패나 `generateOnce hit max_tokens` 가 없는지 확인:

Run: `node --import tsx scripts/run-prod-query.mjs --sql "select level, message, count(*) from error_logs where created_at > now() - interval '1 hour' group by 1,2 order by 3 desc"` (⚠️ dev 리소스 대상인지 `.env.local` 확인 — read_only 고정)
Expected: `*_parse` 실패 0.

- [ ] **Step 4: 검증 노트 (커밋 없음)**

결과를 사용자에게 보고(파싱 성공률·톤 육안·max_tokens 경고 유무). 이상 발견 시 해당 Task 로 복귀.

---

## Task 11: 최종 게이트 + 정리

**Files:** 없음(게이트).

- [ ] **Step 1: 전체 유닛 스위트**

Run: `node --import tsx --test`
Expected: 전 테스트 PASS (신규 openai-params·report-schemas·response-format 포함, model.test 갱신 반영).

- [ ] **Step 2: tsc + build**

Run: `npx tsc --noEmit && npm run build`
Expected: 둘 다 exit 0.

- [ ] **Step 3: 임시 파일 잔존 확인**

Run: `git status --short && ls scripts/luna-canary.ts scripts/fortune-length-probe-luna.ts 2>/dev/null`
Expected: 카나리아·luna 프로브 없음(삭제됨). working tree 에 의도치 않은 변경 없음.

- [ ] **Step 4: origin/dev push**

```bash
git push origin dev
```

Expected: Vercel Preview 배포 + Supabase dev(마이그레이션 0 — 이 작업은 DB 변경 없음). prod(main)은 로드맵 ④ 배치 — **여기서 하지 않음.**

- [ ] **Step 5: 메모리 갱신**

`fortune-report-parse-failure` · `model-router-qa-progress` · `product-backlog`#6 · `saju-mbti-progress`(다음 세션 큐 순서) 를 이 작업 완료 상태로 갱신.

---

## Self-Review (플랜 작성자 기록)

**스펙 커버리지:** ①모델 라우팅 = Task 6·7 / ②어댑터 계약 = Task 1·2 / ③리포트 스키마 = Task 3·4·5 / ④밴드 재정합 = Task 9 / ⑤계측+코호트 = Task 5(retry raw)·8(PROMPT_VERSION) / ⑥검증(0단계 카나리아=Phase 0 완료, 리포트별·chat=Task 10) / ⑦배포 dev only = Task 11 Step 4. 전 섹션 커버.

**Placeholder 스캔:** 없음. Task 9·10 의 "실측 후 조정"은 측정 기반 태스크의 본질(구체 명령·판정 기준 명시)이지 placeholder 아님.

**타입 일관성:** `responseFormat: { name: string; schema: object }` 시그니처가 AdapterStreamArgs·streamChat·generateOnce·fortuneResponseFormat 전부 동일. `FORTUNE_REPORT_MODEL`·`openaiResponseFormat`·`fortuneResponseFormat`·`*_REPORT_SCHEMA` 명칭이 정의처와 사용처 일치.

**⚠️ 실행 중 확인:** (a) `openai@7.4` 의 `reasoning_effort` + `response_format` + `stream:true` 동시 사용 시 tsc 타입 — 카나리아에서 런타임은 확인됨, 타입은 Task 1 tsc 게이트가 잡음. (b) 사주 chat streamChat 호출 개수(Task 7 Step 1). (c) compat grade 합집합 enum 이 compat_social 에서도 프롬프트로 서브셋 강제되는지(현행 동작 유지 확인).
