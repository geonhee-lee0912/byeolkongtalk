# 리포트 가독성 L1 — generic 엔진 Implementation Plan (Phase 2a)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 신규 15종의 공용 엔진(`GenericReportView`) 리포트를 통짜 줄글에서 **마크다운(볼드·문단·불릿) + 섹션 헤더 아이콘 + 상단 요약 칩(결정론)** 으로 바꿔 가독성을 끌어올린다. 한 번의 엔진 변경으로 13개 generic 종목 일괄 적용.

**Architecture:** LLM 은 `body`/`intro`/`note` 에 **마크다운만** 쓴다(스키마 불변 — 문자열 그대로, parse 리스크 0 추가). 렌더러가 마크다운을 React 요소로 변환하고, 섹션 heading 앞의 이모지를 아이콘 타일로 분리하며, 상단에 saju 계산값(일간·강한 기운·음양) 요약 칩을 얹는다. 시각 데이터는 전부 결정론(서버 계산 `SajuResult`) — LLM 무관. `section.visual` 을 스키마에 넣지 않고(리스크 회피) 시각요소는 렌더러/전용 래퍼가 배치(오행 차트는 기존 `ElementBalanceView` 유지).

**Tech Stack:** React 19, TypeScript, Tailwind v4, node:test + tsx(유닛). fortune 리포트는 OpenAI 구조화출력(json_schema strict)이라 문자열 내 `\n` 이 유효 JSON 으로 보장됨 → 마크다운 개행 안전.

**Scope (Phase 2a):** generic 엔진(`GenericReportView`) + 그것을 쓰는 13종 + `prompt.ts` genericGuide + 결과 페이지 배선. **범위 밖(Phase 2b 이후):** 대운 표(데이터 적재 필요)·bespoke 리포트(saju_full·monthly·compat·daily)·L2/L3 일러스트·상품별 신규 viz. `element_balance` 는 기존 `ElementBalanceView`(오행 차트 + 내부 GenericReportView)를 그대로 두되, 내부 GenericReportView 도 이 변경의 마크다운/아이콘 혜택을 자동으로 받는다(칩은 중복 방지 위해 미전달).

---

### Task 1: markdown-lite 렌더러 (순수 파서 + 컴포넌트)

**Files:**
- Create: `lib/markdown-lite.tsx`
- Test: `lib/markdown-lite.test.ts`

TDD: 파서(순수)만 테스트한다.

- [ ] **Step 1: 실패 테스트 작성**

`lib/markdown-lite.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { parseInline, parseBlocks } from "./markdown-lite.tsx";

test("parseInline: **볼드** 런 분리", () => {
  assert.deepEqual(parseInline("a **b** c"), [
    { t: "text", s: "a " },
    { t: "b", s: "b" },
    { t: "text", s: " c" },
  ]);
  assert.deepEqual(parseInline("plain"), [{ t: "text", s: "plain" }]);
});

test("parseBlocks: 빈 줄로 문단 분리", () => {
  const b = parseBlocks("첫 문단\n\n둘째 문단");
  assert.equal(b.length, 2);
  assert.equal(b[0].t, "p");
  assert.equal(b[1].t, "p");
});

test("parseBlocks: '- ' 연속 줄은 불릿 리스트", () => {
  const b = parseBlocks("- 하나\n- 둘");
  assert.equal(b.length, 1);
  assert.equal(b[0].t, "ul");
  assert.equal(b[0].t === "ul" ? b[0].items.length : 0, 2);
});

test("parseBlocks: 일반 여러 줄은 한 문단으로 합침", () => {
  const b = parseBlocks("줄1\n줄2");
  assert.equal(b.length, 1);
  assert.equal(b[0].t, "p");
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx tsx --test lib/markdown-lite.test.ts`
Expected: FAIL (module/export 없음).

- [ ] **Step 3: 구현**

`lib/markdown-lite.tsx`:

```tsx
import React from "react";

// 경량 마크다운 — 신뢰된 LLM 출력 전용(중첩·이스케이프 미지원). dangerouslySetInnerHTML 안 씀.
export type InlinePart = { t: "text"; s: string } | { t: "b"; s: string };
export type Block = { t: "p"; parts: InlinePart[] } | { t: "ul"; items: InlinePart[][] };

/** **볼드** 런을 분리. */
export function parseInline(text: string): InlinePart[] {
  const parts: InlinePart[] = [];
  const re = /\*\*(.+?)\*\*/g;
  let last = 0;
  let m: RegExpExecArray | null;
  while ((m = re.exec(text)) !== null) {
    if (m.index > last) parts.push({ t: "text", s: text.slice(last, m.index) });
    parts.push({ t: "b", s: m[1] });
    last = m.index + m[0].length;
  }
  if (last < text.length) parts.push({ t: "text", s: text.slice(last) });
  return parts.length ? parts : [{ t: "text", s: text }];
}

/** 빈 줄(\n\n)로 문단 분리, '- ' 로 시작하는 연속 줄은 불릿 리스트로. */
export function parseBlocks(text: string): Block[] {
  const blocks: Block[] = [];
  for (const para of text.split(/\n\s*\n/)) {
    const lines = para.split("\n").map((l) => l.trim()).filter(Boolean);
    if (lines.length === 0) continue;
    if (lines.every((l) => l.startsWith("- "))) {
      blocks.push({ t: "ul", items: lines.map((l) => parseInline(l.slice(2).trim())) });
    } else {
      blocks.push({ t: "p", parts: parseInline(lines.join(" ")) });
    }
  }
  return blocks.length ? blocks : [{ t: "p", parts: [{ t: "text", s: text }] }];
}

function renderInline(parts: InlinePart[], key: string) {
  return parts.map((p, i) =>
    p.t === "b" ? (
      <strong key={`${key}-${i}`} className="font-bold text-eye-purple">
        {p.s}
      </strong>
    ) : (
      <React.Fragment key={`${key}-${i}`}>{p.s}</React.Fragment>
    )
  );
}

export function MarkdownLite({ text, className }: { text: string; className?: string }) {
  const blocks = parseBlocks(text);
  return (
    <div className={className}>
      {blocks.map((b, i) =>
        b.t === "ul" ? (
          <ul key={i} className="flex flex-col gap-1.5 my-2.5">
            {b.items.map((it, j) => (
              <li key={j} className="flex gap-2">
                <span className="shrink-0 text-lilac-deep font-bold">·</span>
                <span>{renderInline(it, `${i}-${j}`)}</span>
              </li>
            ))}
          </ul>
        ) : (
          <p key={i} className={i > 0 ? "mt-3" : ""}>
            {renderInline(b.parts, `${i}`)}
          </p>
        )
      )}
    </div>
  );
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx tsx --test lib/markdown-lite.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: 커밋**

```bash
git add lib/markdown-lite.tsx lib/markdown-lite.test.ts
git commit -m "feat(fortune): 경량 마크다운 렌더러(볼드·문단·불릿) + 유닛" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 요약 칩 컴포넌트 (결정론)

**Files:**
- Create: `components/fortune/SajuSummaryChips.tsx`

- [ ] **Step 1: 구현**

`components/fortune/SajuSummaryChips.tsx`:

```tsx
import type { SajuResult } from "@/lib/saju/calc";
import type { FiveElement } from "@/lib/saju/elements";
import { ELEMENT_COLOR } from "@/lib/fortune/element";

const ORDER: FiveElement[] = ["목", "화", "토", "금", "수"];

// 사주 요약 칩 — 전부 결정론(SajuResult 계산값). 일간·강한 기운·음양.
export default function SajuSummaryChips({ saju }: { saju: SajuResult | null }) {
  if (!saju) return null;
  const counts = saju.elementCount;
  const maxEl = ORDER.reduce((a, b) => ((counts[b] ?? 0) > (counts[a] ?? 0) ? b : a), ORDER[0]);
  const yy = saju.yinYangCount;
  const dayHanja = saju.pillars?.day?.hanja?.slice(0, 1) ?? "";
  const chips: { k: string; v: string; color?: string }[] = [
    { k: "일간", v: `${dayHanja} ${saju.dayStem}${saju.dayElement}`, color: ELEMENT_COLOR[saju.dayElement] },
    { k: "강한 기운", v: `${maxEl} ${counts[maxEl] ?? 0}개`, color: ELEMENT_COLOR[maxEl] },
    { k: "음양", v: yy.yang > yy.yin ? "양 우세" : yy.yin > yy.yang ? "음 우세" : "음양 균형" },
  ];
  return (
    <div className="w-full max-w-md mx-auto px-5 flex flex-wrap gap-2">
      {chips.map((c) => (
        <div
          key={c.k}
          className="flex items-center gap-1.5 bg-cream-warm border border-lilac-mid/25 rounded-2xl px-3 py-2"
        >
          <span className="text-[10px] font-bold text-text-light/70">{c.k}</span>
          <span className="text-[12.5px] font-bold" style={{ color: c.color ?? "#5A3E8C" }}>
            {c.v}
          </span>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: 타입체크**

Run: `npx tsc --noEmit`
Expected: exit 0. (`ELEMENT_COLOR` 는 `lib/fortune/element` 에 존재 — `ElementBalanceView` 가 이미 사용. 없으면 STOP 하고 보고.)

- [ ] **Step 3: 커밋**

```bash
git add components/fortune/SajuSummaryChips.tsx
git commit -m "feat(fortune): 사주 요약 칩(일간·강한기운·음양, 결정론)" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: GenericReportView 재작성 + 결과 페이지 saju 전달

**Files:**
- Modify: `components/fortune/GenericReportView.tsx`
- Modify: `app/fortune/result/page.tsx` (generic 분기에 `saju` 전달)

- [ ] **Step 1: GenericReportView 재작성**

`components/fortune/GenericReportView.tsx` 전체를 아래로 교체:

```tsx
import type { GenericReport } from "@/lib/fortune/generic-report";
import type { SajuResult } from "@/lib/saju/calc";
import { MarkdownLite } from "@/lib/markdown-lite";
import SajuSummaryChips from "./SajuSummaryChips";

// heading 앞 이모지를 아이콘 타일로 분리. 이모지 없으면 기본 별.
function splitHeadingEmoji(heading: string): { emoji: string; title: string } {
  const first = [...heading][0] ?? "";
  const isEmoji = /\p{Extended_Pictographic}/u.test(first);
  if (isEmoji) return { emoji: first, title: heading.slice(first.length).trim() };
  return { emoji: "✦", title: heading };
}

// 공용 섹션 리포트 렌더 — 요약 칩 + intro + 섹션 카드(아이콘 헤더·마크다운) + 별콩이 한마디.
export default function GenericReportView({
  report,
  accentEmoji,
  saju,
}: {
  report: GenericReport;
  accentEmoji?: string;
  /** 있으면 상단 요약 칩 노출(결정론). 오행 차트를 따로 보여주는 래퍼(element_balance)는 중복 방지로 미전달. */
  saju?: SajuResult | null;
}) {
  return (
    <div className="w-full max-w-md mx-auto px-5 flex flex-col gap-4">
      {saju && (
        <div className="-mb-1">
          <SajuSummaryChips saju={saju} />
        </div>
      )}

      {/* 도입 */}
      <div className="bg-white rounded-3xl border border-lilac-mid/20 shadow-[0_8px_30px_rgba(40,30,70,0.08)] px-[22px] py-6">
        <MarkdownLite
          text={report.intro}
          className="text-[14px] leading-[1.9] text-[#4F4A5E]"
        />
      </div>

      {/* 섹션들 */}
      {report.sections.map((s, i) => {
        const { emoji, title } = splitHeadingEmoji(s.heading);
        return (
          <div
            key={i}
            className="bg-white rounded-3xl border border-lilac-mid/20 shadow-[0_8px_30px_rgba(40,30,70,0.08)] px-[22px] py-6"
          >
            <div className="flex items-center gap-2.5 mb-3">
              <span
                className="w-9 h-9 rounded-xl flex items-center justify-center text-[19px] shrink-0"
                style={{ background: "linear-gradient(135deg, #F3E9DF, #EADFF2)" }}
                aria-hidden
              >
                {emoji}
              </span>
              <h3 className="text-[14.5px] font-extrabold text-eye-purple">{title}</h3>
            </div>
            <MarkdownLite
              text={s.body}
              className="text-[13.5px] leading-[1.9] text-[#4F4A5E]"
            />
          </div>
        );
      })}

      {/* 별콩이 한마디 */}
      <div
        className="rounded-3xl px-6 py-6 text-white"
        style={{ background: "linear-gradient(140deg, #2A1F4D, #1F1735)" }}
      >
        <h3 className="text-[14px] font-bold text-gold mb-2">
          {accentEmoji ?? "🌙"} 별콩이의 한마디
        </h3>
        <MarkdownLite
          text={report.note}
          className="text-[13.5px] leading-[1.95] text-white/90"
        />
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 결과 페이지 generic 분기에 saju 전달**

`app/fortune/result/page.tsx` 의 generic 분기(현재 `<GenericReportView report={genericReport} .../>`, 대략 496행)에 `saju={sajuData}` 를 추가한다. `accentEmoji` 등 기존 prop 이 있으면 유지. 예:

```tsx
        <GenericReportView report={genericReport} saju={sajuData} />
```

(현재 렌더 코드가 여러 줄이면 `report={genericReport}` 뒤에 `saju={sajuData}` 만 추가. `sajuData` 는 이미 같은 파일에서 선언·세팅됨 — line 198/274.)

- [ ] **Step 3: 타입체크**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 4: 커밋**

```bash
git add components/fortune/GenericReportView.tsx "app/fortune/result/page.tsx"
git commit -m "feat(fortune): GenericReportView 마크다운·아이콘 헤더·요약칩 + 결과페이지 saju 전달" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: 프롬프트 마크다운 지시 (genericGuide)

**Files:**
- Modify: `lib/fortune/prompt.ts` (`genericGuide` 규칙 줄)

- [ ] **Step 1: 규칙 줄 교체**

`genericGuide` 함수의 마지막 `[규칙] ...` 문자열(현재 `... JSON 문자열 안에서 큰따옴표는 escape(\\")하고 줄바꿈은 넣지 마.` 로 끝남)을 아래로 교체한다. 큰따옴표 escape 규칙은 유지하되 **줄바꿈 금지 → 마크다운 허용**으로 바꾼다:

```ts
    `[규칙] 모든 문장은 반말 친구 말투(호칭은 '너'). 단정적 예언 금지 — 흐름·가능성("~한 흐름이 보여","~해보면 좋아")으로. 사주 근거(일간·오행·기둥)를 구체적으로 녹이고, 물타기·반복·공허한 미사여구 금지(밀도로 채워라). sections 는 위 heading 순서·개수 그대로 전부 채워라. 따뜻한 마무리.\n[서식] body 는 읽기 쉽게 마크다운으로 써라: 핵심 단어·구절은 **굵게**(별표 두 개), 문단은 빈 줄(\\n\\n)로 나누고, 나열·목록은 각 줄을 "- " 로 시작하는 불릿으로. heading 은 맨 앞 이모지 1개 + 제목 형태를 유지해라. 남발하지 말고 핵심에만 굵게/불릿을 써라. JSON 문자열 안에서 큰따옴표는 escape(\\")하고, 줄바꿈은 반드시 \\n 으로 이스케이프해라(생 줄바꿈 금지).`
```

주의: 이 파일은 TS 문자열이므로 위 내용의 `\\n`·`\\"` 는 소스에 그대로(백슬래시 2개) 있어야 런타임 문자열에 `\n`·`\"` 로 들어간다. genericGuide 만 바꾸고 daily/monthly/saju_full/compat 등 전용 가이드는 건드리지 마라.

- [ ] **Step 2: 타입체크 + 빌드**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 3: 커밋**

```bash
git add lib/fortune/prompt.ts
git commit -m "feat(fortune): genericGuide 마크다운 서식 지시(볼드·문단·불릿)" -m "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 검증 (빌드 + 유닛 + 브라우저)

**Files:** (없음 — 검증)

- [ ] **Step 1: 유닛 + 타입체크**

Run: `npx tsx --test lib/markdown-lite.test.ts && npx tsc --noEmit`
Expected: 유닛 PASS(4) · tsc exit 0.

- [ ] **Step 2: 브라우저 — 기존 저장본(마크다운 없음) 회귀**

dev 서버 + 소유자 쿠키로 기존 generic 리딩(예: `fortune:love_self` 또는 `fortune:nature_self`)의 결과 페이지를 연다. 기존 저장본은 마크다운이 없어 **한 문단으로 정상 렌더**되어야 하고(크래시·빈 화면 없음), 요약 칩(일간·강한기운·음양)과 섹션 이모지 아이콘 타일이 보여야 한다. `read_page`/스샷으로 확인. (기존 리딩 id 는 dev DB 조회로 확보 — `emotion_tag`='fortune:<generic type>', `profile_id` not null.)

- [ ] **Step 3: (선택) 신규 생성 스모크**

여유가 되면 저비용 generic 1종을 `force` 로 새로 생성해 마크다운(볼드·불릿)이 실제로 렌더되는지 확인. 별 소모가 생기니 생략 가능 — 생략 시 프롬프트 변경은 QA 하네스로 별도 확인 권장(⚠️ 프롬프트 수정 후 dev 서버 재시작 필요, 모듈 캐시).

- [ ] **Step 4: dev 배포 대기**

태스크별 커밋 완료. 배포는 사용자 지시 시 `git push origin dev`.

---

## Self-Review 결과

- **스펙 커버리지(Part B / L1 generic)**: 마크다운 렌더(Task 1·3) · 섹션 헤더 아이콘(Task 3, heading 이모지 분리) · 요약 칩(Task 2·3) · 프롬프트 마크다운(Task 4) · 오행 차트는 기존 `ElementBalanceView` 유지(변경 불필요). ✅ 대운 표·bespoke·L2/L3 는 명시적으로 Phase 2b 이후.
- **리스크 완화**: LLM 스키마 불변(문자열만) + 구조화출력이 `\n` 이스케이프 보장 → parse 리스크 0 추가. 마크다운 렌더러는 자체 파서(볼드/문단/불릿만)라 XSS 무해. 기존 저장본은 파서가 단일 문단으로 안전 폴백(Task 5-2 로 검증).
- **타입 일관성**: `parseInline`/`parseBlocks`/`MarkdownLite`·`SajuSummaryChips({saju})`·`GenericReportView({report,saju?})` 시그니처가 태스크 전반 일치. `SajuResult` 필드(`dayStem`·`dayElement`·`elementCount`·`yinYangCount`·`pillars.day.hanja`)는 `lib/saju/calc.ts` 정의와 일치.
- **플레이스홀더**: 없음.

## 범위 밖 / 후속 (Phase 2b+)
- 대운 표(life_full) — 생성 시 daeun 을 `saju_data` 에 적재 후 `DaeunTable` 렌더.
- bespoke 리포트(saju_full·monthly·compat·daily) 동일 어휘 적용.
- L2 플래그십 히어로 일러스트 · L3 상품별 신규 viz · 기존 별콩이 일러스트 재사용.
