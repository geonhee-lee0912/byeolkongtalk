# 별자리 인연도 순위 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 별자리에 종합 인연도 점수(숫자) → 전체 순위 리스트 → 사람별 근거(1:1 뷰)를 더한다.

**Architecture:** 순수 로직(`inyeon.ts`)이 pairRelation 값 + 삼합 공유로 0~100 점수·등급·근거를 계산. API가 edge에 `inyeon`(점수)+`triadShared`를 실어 보내고, ConstellationView가 pivot 기준 순위 리스트를 그리며, OneToOnePanel이 근거 섹션을 붙인다. 룰 100%·LLM 0.

**Tech Stack:** Next.js 16 · React 19 · TS strict · node:test TDD.

---

## 배경 / 전제

- dev 브랜치 직접 작업. 정본 스펙 `docs/superpowers/specs/2026-08-17-인연도-순위-design.md`.
- 테스트: `node:test` + `node:assert/strict`, import `.ts` 확장자. 실행 `node --import tsx --test <files>`.
- 방어 규칙([[recurring-crash-class-config-by-dynamic-key]]): 동적 값 조회는 폴백.
- 기존 계약:
  - `lib/saju/pairing.ts` `pairRelation` → `{ element, tenGodAtoB, tenGodBtoA, labelAtoB, labelBtoA, heavenlyCombo, sixCombo }`. `findTriads(branches)` → `{branches, element}[]`.
  - `lib/byeoljari/display.ts` `elementRelationLabel(rel)` → 오행관계 한 줄(비화/생아/아생/극아/아극).
  - `lib/byeoljari/types.ts` `GraphEdge { a,b,element,labelAtoB,labelBtoA,tenGodAtoB,tenGodBtoA,heavenlyCombo,sixCombo }`.
  - `app/api/fortune/byeoljari/[shareId]/route.ts` — edges 루프(모든 visible 쌍 pairRelation) + triads 계산(현재 edges 뒤). member.id·compat_visible 규칙 존재.
  - `components/byeoljari/ConstellationView.tsx` — pivotId(meId ?? host), `orientEdge(edge,pivotId)`로 oriented, selectedId→OneToOnePanel. 범례 div + 진화 힌트 있음.
  - `components/byeoljari/OneToOnePanel.tsx` — `oriented:{iSeeThem,theySeeMe,element,iSeeThemTenGod,theySeeMeTenGod}|null` + 오행 한 줄 + 카피 + 케미/결속 뱃지.
- 한글 주석.

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `lib/byeoljari/inyeon.ts` | 점수·등급·근거·코멘트 순수 로직 | **신규** |
| `lib/byeoljari/inyeon.test.ts` | TDD | **신규** |
| `lib/byeoljari/types.ts` | GraphEdge에 `inyeon`·`triadShared` | 수정 |
| `app/api/fortune/byeoljari/[shareId]/route.ts` | triads 선계산 + edge에 inyeon·triadShared | 수정 |
| `components/byeoljari/ConstellationView.tsx` | 인연도 순위 리스트 + 선택 시 근거 전달 | 수정 |
| `components/byeoljari/OneToOnePanel.tsx` | 인연도 근거 섹션 | 수정 |

**순서:** I1(로직) → I2(배선) → I3(순위 리스트) → I4(1:1 근거) → I5(검증).

---

### Task I1: 인연도 순수 로직 `lib/byeoljari/inyeon.ts` (TDD)

**Files:** Create `lib/byeoljari/inyeon.ts`, `lib/byeoljari/inyeon.test.ts`

- [ ] **Step 1: 실패 테스트** — Create `lib/byeoljari/inyeon.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { inyeonScore, inyeonGrade, inyeonReasons, inyeonComment } from "./inyeon.ts";

const mk = (o: Partial<Parameters<typeof inyeonScore>[0]> = {}) => ({
  element: "생아", heavenlyCombo: false, sixCombo: false, triadShared: false, ...o,
});

test("inyeonScore — 오행 base(상생·상극 62, 비화 50)", () => {
  assert.equal(inyeonScore(mk({ element: "비화" })), 50);
  assert.equal(inyeonScore(mk({ element: "생아" })), 62);
  assert.equal(inyeonScore(mk({ element: "아생" })), 62);
  assert.equal(inyeonScore(mk({ element: "극아" })), 62);
  assert.equal(inyeonScore(mk({ element: "아극" })), 62);
});

test("inyeonScore — 특별관계 가점 + cap 100", () => {
  assert.equal(inyeonScore(mk({ element: "생아", heavenlyCombo: true })), 87);
  assert.equal(inyeonScore(mk({ element: "비화", sixCombo: true })), 65);
  assert.equal(inyeonScore(mk({ element: "생아", heavenlyCombo: true, sixCombo: true, triadShared: true })), 100);
});

test("inyeonScore — 미지 오행은 비화 취급(방어)", () => {
  assert.equal(inyeonScore(mk({ element: "??" })), 50);
});

test("inyeonGrade — 경계", () => {
  assert.equal(inyeonGrade(85).tone, "high");
  assert.equal(inyeonGrade(84).tone, "mid");
  assert.equal(inyeonGrade(70).tone, "mid");
  assert.equal(inyeonGrade(69).tone, "low");
  assert.equal(inyeonGrade(55).tone, "low");
  assert.equal(inyeonGrade(54).tone, "faint");
});

test("inyeonReasons — 특별관계 + 오행 결", () => {
  const r = inyeonReasons(mk({ element: "생아", heavenlyCombo: true }));
  assert.ok(r.some((s) => s.includes("케미 스파크")));
  assert.ok(r.some((s) => s.includes("북돋아")));
});

test("inyeonReasons — 특별관계 없으면 오행 한 줄만", () => {
  assert.equal(inyeonReasons(mk({ element: "비화" })).length, 1);
});

test("inyeonComment — 모든 톤 비어있지 않음", () => {
  for (const t of ["high", "mid", "low", "faint"] as const) {
    assert.ok(inyeonComment(t).length > 0);
  }
});
```

- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/byeoljari/inyeon.test.ts` → FAIL(module 없음).

- [ ] **Step 3: 구현** — Create `lib/byeoljari/inyeon.ts`:

```ts
// 인연도 점수·등급·근거 — 순수. pairRelation 값 + 삼합 공유로 나↔상대 인연도(0~100).
// 유불리 아님: 상생·상극 동급(강렬한 인연), 비화만 소폭 낮음(잔잔). 특별관계는 가점.
import { elementRelationLabel } from "@/lib/byeoljari/display";

export interface InyeonInput {
  element: string; // 오행관계(나 기준): 비화|생아|아생|극아|아극
  heavenlyCombo: boolean;
  sixCombo: boolean;
  triadShared: boolean; // 나와 상대가 같은 삼합 국 멤버
}

/** 0~100 인연도. 비화 50 · 그 외 오행 62 + 천간합25 + 육합15 + 삼합15, cap 100. */
export function inyeonScore(x: InyeonInput): number {
  let score = x.element === "비화" ? 50 : 62; // 미지 값도 62가 아니라? — 아래 방어
  if (!["생아", "아생", "극아", "아극"].includes(x.element)) score = 50; // 비화·미지 = 50
  if (x.heavenlyCombo) score += 25;
  if (x.sixCombo) score += 15;
  if (x.triadShared) score += 15;
  return Math.min(score, 100);
}

export interface InyeonGrade {
  label: string;
  tone: "high" | "mid" | "low" | "faint";
}

/** 점수 → 등급(무낙인 톤). */
export function inyeonGrade(score: number): InyeonGrade {
  if (score >= 85) return { label: "인연 진함", tone: "high" };
  if (score >= 70) return { label: "도타운 사이", tone: "mid" };
  if (score >= 55) return { label: "이어진 사이", tone: "low" };
  return { label: "잔잔한 사이", tone: "faint" };
}

/** 점수에 기여한 요소를 별콩이 톤 근거 문구로. 오행 한 줄은 항상 마지막. */
export function inyeonReasons(x: InyeonInput): string[] {
  const r: string[] = [];
  if (x.heavenlyCombo) r.push("✨ 케미 스파크 — 유난히 끌리는 기운");
  if (x.sixCombo) r.push("🔗 결속 — 단단히 묶인 사이");
  if (x.triadShared) r.push("🌟 같은 국 — 함께면 시너지가 나");
  r.push(elementRelationLabel(x.element));
  return r;
}

/** 등급 톤별 별콩이 한마디(종합). */
export function inyeonComment(tone: InyeonGrade["tone"]): string {
  switch (tone) {
    case "high":
      return "이 별자리에서 유독 진하게 엮인 인연이야";
    case "mid":
      return "든든하게 이어진 사이야";
    case "low":
      return "잔잔히 이어진 인연이야";
    default:
      return "잔잔하지만 분명히 이어진 사이야";
  }
}
```

> 주의: `inyeonScore`의 base 계산은 화이트리스트(`생아/아생/극아/아극`=62, 그 외=50)로 단일화 — 위 코드의 두 줄은 결국 "화이트리스트에 있으면 62, 아니면 50". 구현 시 아래처럼 한 줄로 깔끔히 해도 동일:
> ```ts
> let score = ["생아","아생","극아","아극"].includes(x.element) ? 62 : 50;
> ```
> (테스트가 계약이니 결과만 같으면 됨.)

- [ ] **Step 4: 통과 + tsc** — `node --import tsx --test lib/byeoljari/inyeon.test.ts` → 7 pass. `npx tsc --noEmit` → 0.

- [ ] **Step 5: 커밋**
```bash
git add lib/byeoljari/inyeon.ts lib/byeoljari/inyeon.test.ts
git commit -m "feat(byeoljari): 인연도 점수·등급·근거 순수 로직 TDD"
```

---

### Task I2: edge에 인연도·삼합공유 배선 (types + API)

**Files:** Modify `lib/byeoljari/types.ts`, `app/api/fortune/byeoljari/[shareId]/route.ts`

- [ ] **Step 1: `types.ts` GraphEdge 확장** — `heavenlyCombo` 앞이나 끝에 2필드 추가(기존 유지):
```ts
  inyeon: number; // 0~100 종합 인연도(대칭)
  triadShared: boolean; // 두 끝점이 같은 삼합 국(근거 재구성용)
```

- [ ] **Step 2: API route — triads 선계산 + edge에 반영** — `app/api/fortune/byeoljari/[shareId]/route.ts`:

(a) import 추가(상단, pairRelation 옆):
```ts
import { pairRelation, findTriads } from "@/lib/saju/pairing";
import { inyeonScore } from "@/lib/byeoljari/inyeon";
```
(findTriads는 이미 import 되어 있으면 중복 추가 말 것.)

(b) **triads를 edges 루프 앞으로 이동.** 현재 edges 루프 뒤에 있는 triads 계산 블록을 edges 루프 **위로** 옮기고, memberId Set 배열을 만든다. edges 루프 직전에:
```ts
  // 삼합 먼저 계산(각 edge 의 triadShared 판정에 필요)
  const triads = findTriads(saju.map((s) => s.pillars.day.branch)).map((t) => ({
    element: t.element,
    memberIds: members
      .filter((_, i) => t.branches.includes(saju[i].pillars.day.branch))
      .map((m) => m.id),
  }));
  const triadSets = triads.map((t) => new Set(t.memberIds));
```
(원래 위치의 triads 계산은 삭제 — 중복 금지. return 문의 `triads` 는 이 상수를 그대로 씀.)

(c) edges 배열 타입에 2필드 추가:
```ts
    inyeon: number;
    triadShared: boolean;
```

(d) edges.push 에서 inyeon·triadShared 계산:
```ts
      const r = pairRelation(saju[i], saju[j]);
      const triadShared = triadSets.some(
        (s) => s.has(members[i].id) && s.has(members[j].id)
      );
      edges.push({
        a: members[i].id,
        b: members[j].id,
        element: r.element,
        labelAtoB: r.labelAtoB,
        labelBtoA: r.labelBtoA,
        tenGodAtoB: r.tenGodAtoB,
        tenGodBtoA: r.tenGodBtoA,
        inyeon: inyeonScore({
          element: r.element,
          heavenlyCombo: r.heavenlyCombo,
          sixCombo: r.sixCombo,
          triadShared,
        }),
        triadShared,
        heavenlyCombo: r.heavenlyCombo,
        sixCombo: r.sixCombo,
      });
```

- [ ] **Step 3: 검증** — `npx tsc --noEmit` → 0. `npm run build` → 0. `node --import tsx --test lib/byeoljari/*.test.ts` → 회귀 pass.

- [ ] **Step 4: 커밋**
```bash
git add lib/byeoljari/types.ts app/api/fortune/byeoljari/"[shareId]"/route.ts
git commit -m "feat(byeoljari): edge 에 인연도 점수+삼합공유 배선(triads 선계산)"
```

---

### Task I3: 인연도 순위 리스트 (ConstellationView)

**Files:** Modify `components/byeoljari/ConstellationView.tsx`

- [ ] **Step 1: import + 순위 계산** — 상단 import에 추가:
```ts
import { inyeonGrade } from "@/lib/byeoljari/inyeon";
```
`oriented` 계산부 근처(pivotId 정의 후)에 useMemo 추가:
```ts
  // pivot(나) 기준 인연도 내림차순 순위. edge 는 이미 compat_visible 로 필터돼 보이는 관계만.
  const ranking = useMemo(() => {
    if (!pivotId) return [];
    return graph.edges
      .filter((e) => e.a === pivotId || e.b === pivotId)
      .map((e) => {
        const otherId = e.a === pivotId ? e.b : e.a;
        const other = graph.nodes.find((n) => n.id === otherId);
        return { id: otherId, name: other?.name ?? null, inyeon: e.inyeon };
      })
      .sort((x, y) => y.inyeon - x.inyeon);
  }, [graph.edges, graph.nodes, pivotId]);
```

- [ ] **Step 2: 순위 리스트 렌더** — 범례 `<div className="mt-3 ...">...</div>` **다음**, 진화 힌트 `<p>` 들 **앞**에 삽입:
```tsx
      {ranking.length > 0 && (
        <div className="mt-4">
          <div className="mb-2 text-xs font-semibold text-eye-purple">인연이 진한 순</div>
          <div className="space-y-1.5">
            {ranking.map((r, i) => {
              const grade = inyeonGrade(r.inyeon);
              return (
                <button
                  key={r.id}
                  type="button"
                  onClick={() => handleSelect(r.id)}
                  className="flex w-full items-center justify-between rounded-xl bg-cream-warm px-3 py-2 text-left transition hover:bg-lilac-soft/50 active:scale-[0.99]"
                >
                  <span className="text-sm text-eye-purple">
                    {i + 1}위 · {r.name ?? "이 별"}
                  </span>
                  <span className="text-xs text-text-light">
                    인연도 {r.inyeon} · {grade.label}
                  </span>
                </button>
              );
            })}
          </div>
        </div>
      )}
```
(`handleSelect` 는 기존 함수 — id===pivotId 면 무시하지만 순위엔 pivot 이 없으니 항상 선택됨.)

- [ ] **Step 3: 검증** — `npx tsc --noEmit` → 0. `npm run build` → 0.

- [ ] **Step 4: 커밋**
```bash
git add components/byeoljari/ConstellationView.tsx
git commit -m "feat(byeoljari): 별자리 아래 인연도 순위 리스트(전체·숫자·등급·탭)"
```

---

### Task I4: 1:1 뷰 인연도 근거 섹션 (OneToOnePanel)

**Files:** Modify `components/byeoljari/OneToOnePanel.tsx`, `components/byeoljari/ConstellationView.tsx`

- [ ] **Step 1: ConstellationView — 선택된 edge 의 인연도 정보 계산·전달** — import 확장:
```ts
import { inyeonGrade, inyeonReasons, inyeonComment } from "@/lib/byeoljari/inyeon";
```
`oriented` 정의 다음에:
```ts
  // 선택된 1:1 의 인연도 근거(오행은 pivot 기준 oriented.element, 나머지는 edge).
  const inyeonInfo = useMemo(() => {
    if (!edge || !oriented) return null;
    const grade = inyeonGrade(edge.inyeon);
    return {
      score: edge.inyeon,
      grade,
      reasons: inyeonReasons({
        element: oriented.element,
        heavenlyCombo: edge.heavenlyCombo,
        sixCombo: edge.sixCombo,
        triadShared: edge.triadShared,
      }),
      comment: inyeonComment(grade.tone),
    };
  }, [edge, oriented]);
```
`<OneToOnePanel ... />` 호출에 prop 추가: `inyeon={inyeonInfo}`.

- [ ] **Step 2: OneToOnePanel — props 타입 + 근거 섹션** — `Props` 에 추가:
```ts
  inyeon: {
    score: number;
    grade: { label: string; tone: string };
    reasons: string[];
    comment: string;
  } | null;
```
`oriented ? (...)` 블록 안, 오행 한 줄 `<p>` **앞**(맨 위)에 인연도 근거 섹션 삽입:
```tsx
          {inyeon && (
            <div className="rounded-xl bg-lilac-soft/40 p-3">
              <div className="flex items-baseline gap-2">
                <span className="font-display text-lg text-eye-purple">인연도 {inyeon.score}</span>
                <span className="text-xs text-eye-purple/70">{inyeon.grade.label}</span>
              </div>
              <ul className="mt-2 space-y-1 text-sm text-eye-purple">
                {inyeon.reasons.map((r, i) => (
                  <li key={i}>{r}</li>
                ))}
              </ul>
              <p className="mt-2 text-xs text-text-light">별콩이 — {inyeon.comment}</p>
            </div>
          )}
```
(IIFE 블록이면 `iSee`/`theySee` 계산 아래, 반환 JSX 맨 위에 둔다. `inyeon` 은 컴포넌트 스코프 prop 이라 IIFE 안에서 접근 가능.)

- [ ] **Step 3: 검증** — `npx tsc --noEmit` → 0. `npm run build` → 0.

- [ ] **Step 4: 커밋**
```bash
git add components/byeoljari/OneToOnePanel.tsx components/byeoljari/ConstellationView.tsx
git commit -m "feat(byeoljari): 1:1 뷰 인연도 근거 섹션(점수·등급·근거·별콩이 한마디)"
```

---

### Task I5: 통합 검증 + 브라우저 E2E

**Files:** 없음(검증)

- [ ] **Step 1: 유닛 회귀** — `node --import tsx --test lib/byeoljari/*.test.ts` → inyeon(7) 포함 전부 pass.
- [ ] **Step 2: tsc + build** — `npx tsc --noEmit && npm run build` → 둘 다 exit 0.
- [ ] **Step 3: dev 서버** — preview_start `{name:"byeolkong-dev"}`.
- [ ] **Step 4: 브라우저 E2E** — dev 실데이터(`hKQDZjRoOd` 15인)에서:
  - navigate → 별자리 아래 "인연이 진한 순" 리스트가 뜨고, 각 행에 `N위 · 이름 · 인연도 숫자 · 등급` 표시. 내림차순 확인(첫 행 최고점).
  - 순위 행 탭(또는 XHR로 API 확인) → 1:1 패널에 "인연도 N · 등급 + 근거 리스트 + 별콩이 한마디" 섹션 확인.
  - API 응답 edge 에 `inyeon`·`triadShared` 존재 확인(XHR).
  - 콘솔 에러 0.
- [ ] **Step 5: 최종 보고** — 스크린샷(타임아웃 시 DOM 단정) + 순위·근거 증거.

---

## Self-Review (작성자 체크)

**1. 스펙 커버리지:** §1 점수 공식 → I1 ✅ / §1 등급 → I1 ✅ / §2 순위 UI(전체·숫자·등급·탭) → I3 ✅ / §3 1:1 근거(점수·근거·코멘트) → I4 ✅ / §4 카피(특별관계·등급·코멘트) → I1 ✅ / §5 데이터흐름(inyeon.ts·edge·View·Panel) → I1~I4 ✅ / §7 테스트 → I1·I5 ✅

**2. 플레이스홀더:** 전 스텝 실제 코드. base 계산 단일화 주석으로 명확화. 없음 ✅

**3. 타입 정합:** `InyeonInput{element,heavenlyCombo,sixCombo,triadShared}` (I1) = API 호출(I2)·View 근거 호출(I4) 일치. `GraphEdge.inyeon:number·triadShared:boolean`(I2) = API push(I2)·View 순위(I3)·근거(I4) 사용 일치. `inyeonGrade` 반환 `{label,tone}` = OneToOnePanel props(I4) 일치. `oriented.element`(pivot 기준) 로 근거 오행 방향 정합.

**관련:** 스펙 `2026-08-17-인연도-순위-design.md` · [[recurring-crash-class-config-by-dynamic-key]] · pairing.ts.
