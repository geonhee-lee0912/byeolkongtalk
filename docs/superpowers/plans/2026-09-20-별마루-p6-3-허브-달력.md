# 별마루 P6-3 허브·달력 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 별마루 허브를 "배너 → 인연 칩 → 롤링 7일 스트립 → 접이식 월간 격자 → 무료 목록 4종"으로 재편하고, 달력 셀 시각을 §3 확정안으로 바꾼다.

**Architecture:** 판정 엔진(순수 함수)은 그대로 두고 **와이어 계약 두 개**만 넓힌다 — ①`splitByFreeLine` 이 잠긴 날의 **간지**를 함께 실어 보내고(만세력은 비밀이 아니다 · 판정은 여전히 안 나간다) ②캘린더 라우트가 **오늘 중심 7일 창**을 추가로 계산해 내려준다. 화면은 그 위에서 조립만 한다. 페어 점수는 §3-1-a 확정안(V4)으로 교체한다.

**Tech Stack:** Next.js 16 App Router · React 19 · Tailwind v4(`@theme` 토큰) · `node:test` + `tsx` 유닛 · Supabase(이 플랜은 **마이그레이션 없음**)

---

## 이 플랜이 서 있는 확정 사항

| 출처 | 확정 |
|---|---|
| 스펙 §2 | 달력 상단 유지 · **오늘 골드 히어로 타일 제거** · **크로스셀 삭제** · `/byeolmaru/saju` 달력 중복 제거 |
| 스펙 §2-1 | 롤링 7일(지난 3 + 오늘 + 앞으로 3) · 오늘 칸 2배 폭 · 미래 3칸 블러 고정 |
| 스펙 §2-2 | 월간 격자 기본 접힘 · 첫 방문만 펼침(localStorage) · **격자 셀은 날짜 + 톤 색면 + 마크 띠만**(동물·간지는 스트립/상세 담당) |
| 스펙 §2-3 | 무료 목록 **4종**(오늘 사주 추가) · 좌측 4px 바 제거 · 칩 문법 통일 |
| 스펙 §3 | 간지 제거 · 챙길 날 연보라 · 마크 = 2글자 컬러 라벨 · 미래 칸은 날짜(·스트립은 동물)까지 선명 |
| 스펙 §3-1-a | 페어 점수 **V4** = 끌림·결속 OR→가중(둘 다 full · 한 명 절반) + **baseline 을 점수에서 제거** · 삐걱은 OR 유지 · 임계 70/45 유지 |
| 사용자 2026-09-20 | 마크는 **진하게(둘 다)/연하게(한 명) 2단** · 출석 스트립은 **연속 방문만** 남기고 칸 문구는 격자 접힘 버튼으로 |
| 스펙 §8-7 | 배너 캐릭터 슬롯 **폭 120px**(실측 하한 110) · 에셋 `public/byeolmaru-byeolkong.webp` |

**스코프 밖(건드리지 말 것)** — §4 날짜 상세·§5 한 장 리포트·C안 블러는 **P6-4** 소유다. `DayDetailCard`·`DailyCardBlock`·`CardReportView`·`daily-report`·`card-narrative` 는 이 플랜에서 **열지 않는다**. `PremiumBlock` 은 스펙의 제거 목록에 없으므로 **유지**하고 자리만 옮긴다.

---

## 파일 구조

**신규**

| 파일 | 책임 |
|---|---|
| `lib/byeolmaru/strip.ts` | 롤링 7일 창의 날짜 계산(순수). 미래 칸 수는 `FUTURE_REPORT_DAYS` 를 import — 재정의 금지 |
| `lib/byeolmaru/strip.test.ts` | 위 계약(7칸·오늘 위치·월/연 경계) |
| `components/byeolmaru/HubBanner.tsx` | 배너 1장 — lilac 그라데이션 + 좌측 텍스트 + 우측 120px 캐릭터 |
| `components/byeolmaru/DayStrip.tsx` | 롤링 7일 스트립 + 미래 칸 블러 + 비자격 CTA |
| `components/byeolmaru/MonthGridSection.tsx` | 격자 접이식 래퍼(첫 방문만 펼침 · 토글 계측) |

**수정**

| 파일 | 변경 |
|---|---|
| `lib/byeolmaru/calendar.ts` | `splitByFreeLine` → `lockedCells:{date,ganji}[]` · `buildCalendarPayload` 반환 키 변경 |
| `lib/byeolmaru/day-label.ts` | `DayMark.strength` 추가 · 마크 라벨 2글자 통일 · `MARK_COLOR` 추가 |
| `lib/byeolmaru/pair-day.ts` | V4 적용(가중 + baseline 제거) · `PairDayTags` 에 `*Both` 3필드 · `pairMarks` 가 strength 부여 |
| `lib/analytics/ui-events.ts` | 이벤트 2종 추가 · 죽은 1종 제거 |
| `app/api/byeolmaru/calendar/route.ts` | `strip` 응답 추가(self·pair) · `lockedCells` 로 키 변경 |
| `components/byeolmaru/CalendarGrid.tsx` | §3 셀 시각 · `lockedCells` prop |
| `components/byeolmaru/FreeList.tsx` | 4종 · 4px 바 제거 · 칩 문법 |
| `components/byeolmaru/AttendanceStrip.tsx` | 연속 방문만 |
| `components/byeolmaru/ByeolmaruHub.tsx` | 재조립 |
| `components/byeolmaru/SajuTodayView.tsx` | 달력 중복 제거 · `lockedCells` |
| `components/byeolmaru/WooriTodayView.tsx` | `lockedCells` |
| `lib/byeolmaru/calendar.test.ts`·`free-line.test.ts`·`pair-day.test.ts`·`day-label.test.ts` | 계약 갱신 |

**삭제**: `components/byeolmaru/TodayHeroTile.tsx` · `components/byeolmaru/CrossSellCard.tsx` · `lib/byeolmaru/crosssell.ts` · `lib/byeolmaru/crosssell.test.ts`

---

## 🔴 실행 규율 (P6-1·P6-2 에서 물린 것)

1. **매 태스크 끝에 tsc 가 초록이어야 한다.** 심볼을 넓히는 태스크(2·4·5)는 **소비처를 같은 커밋에서** 고친다. 게이트를 의도적으로 깨는 순서를 만들지 말 것.
2. **형제 지점을 훑어라.** `lockedDates` 는 6파일에 있다(`grep -rn "lockedDates" lib components app`). 클래스명이 아니라 "무엇을 렌더하나"로 훑을 것.
3. **테스트 제목이 보장 못 하는 걸 보장한다고 쓰지 말 것.** 폴백이 통과시키는 단정(`?? "all"` 류)은 대조 단정으로 닫는다.
4. 유닛 실행: `node --import tsx --test "lib/**/*.test.ts"`

---

### Task 1: 롤링 7일 창 (`lib/byeolmaru/strip.ts`)

**Files:**
- Create: `lib/byeolmaru/strip.ts`
- Test: `lib/byeolmaru/strip.test.ts`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/byeolmaru/strip.test.ts`:

```ts
// lib/byeolmaru/strip.test.ts — 롤링 7일 창 계약. 월·연·윤년 경계에서 칸 수가 출렁이지 않는지가 핵심.
import { test } from "node:test";
import assert from "node:assert/strict";
import { STRIP_LENGTH, STRIP_PAST_DAYS, stripDates, stripRange } from "./strip.ts";
import { FUTURE_REPORT_DAYS } from "./report-date.ts";

test("길이는 지난 3 + 오늘 + 미래 3 = 7", () => {
  assert.equal(STRIP_LENGTH, 7);
  // 🔴 상수 대조 단정 — 미래 칸 수를 스트립이 따로 정의하면 리포트 생성 범위와 어긋난다(스펙 §11 교차 계약).
  assert.equal(STRIP_LENGTH, STRIP_PAST_DAYS + 1 + FUTURE_REPORT_DAYS);
});

test("오늘이 정확히 4번째 칸(index 3)", () => {
  const d = stripDates("2026-09-20");
  assert.equal(d.length, 7);
  assert.equal(d[3], "2026-09-20");
  assert.deepEqual(d, [
    "2026-09-17", "2026-09-18", "2026-09-19", "2026-09-20",
    "2026-09-21", "2026-09-22", "2026-09-23",
  ]);
});

test("월 경계를 넘어도 7칸 — 달 1일", () => {
  assert.deepEqual(stripDates("2026-10-01"), [
    "2026-09-28", "2026-09-29", "2026-09-30", "2026-10-01",
    "2026-10-02", "2026-10-03", "2026-10-04",
  ]);
});

test("월 경계를 넘어도 7칸 — 달 말일", () => {
  const d = stripDates("2026-09-30");
  assert.equal(d.length, 7);
  assert.equal(d[6], "2026-10-03");
});

test("연 경계", () => {
  const d = stripDates("2027-01-01");
  assert.equal(d[0], "2026-12-29");
  assert.equal(d[6], "2027-01-04");
});

test("윤년 2월 29일", () => {
  const d = stripDates("2028-02-28");
  assert.equal(d[4], "2028-02-29");
  assert.equal(d[5], "2028-03-01");
});

test("stripRange 는 stripDates 의 양끝과 같다", () => {
  const d = stripDates("2026-09-20");
  assert.deepEqual(stripRange("2026-09-20"), { start: d[0], end: d[6] });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/strip.test.ts`
Expected: FAIL — `Cannot find module './strip.ts'`

- [ ] **Step 3: 최소 구현**

`lib/byeolmaru/strip.ts`:

```ts
// lib/byeolmaru/strip.ts — 허브 롤링 7일 스트립의 날짜 창(순수 · 네트워크 0).
// 스펙 §2-1: 오늘 중심 롤링이라 "이번 주"라는 개념이 없다 → 주 바뀜·월 경계로 칸 수가 출렁이지 않는다
// (요일 기준이면 월요일 6칸·토요일 1칸으로 흐린 칸이 출렁여 기각됐다).
// 🔴 미래 칸 수를 여기서 다시 정의하지 않는다 — report-date.ts 의 FUTURE_REPORT_DAYS 가 정본이다.
//    어긋나면 "흐리게 보이니 눌러보는 칸"과 "누르면 리포트가 생성되는 칸"이 달라져, 유저가 누른 칸이
//    out_of_range 로 거절되는 조합이 생긴다(스펙 §11 교차 계약).
import { FUTURE_REPORT_DAYS } from "./report-date.ts";

export const STRIP_PAST_DAYS = 3;
export const STRIP_LENGTH = STRIP_PAST_DAYS + 1 + FUTURE_REPORT_DAYS;

/** KST 날짜 문자열을 days 만큼 민다. UTC 자정 기준 산술이라 서버 TZ·DST 에 안 흔들린다. */
function shift(dateKst: string, days: number): string {
  return new Date(Date.parse(`${dateKst}T00:00:00Z`) + days * 86400000).toISOString().slice(0, 10);
}

/** 스트립이 덮는 날짜 범위(양끝 포함) — calcDailyLuckRange 인자로 그대로 쓴다. */
export function stripRange(todayKst: string): { start: string; end: string } {
  return { start: shift(todayKst, -STRIP_PAST_DAYS), end: shift(todayKst, FUTURE_REPORT_DAYS) };
}

/** 스트립 7칸의 날짜 — 과거→미래 순. 오늘은 항상 index STRIP_PAST_DAYS. */
export function stripDates(todayKst: string): string[] {
  return Array.from({ length: STRIP_LENGTH }, (_, i) => shift(todayKst, i - STRIP_PAST_DAYS));
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/strip.test.ts`
Expected: PASS (7 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/strip.ts lib/byeolmaru/strip.test.ts
git commit -m "feat(byeolmaru): 롤링 7일 스트립 날짜 창 — 미래 칸 수는 FUTURE_REPORT_DAYS 재사용 (P6-3 §2-1)"
```

---

### Task 2: 무료선이 간지를 함께 내보낸다 (`lockedDates` → `lockedCells`)

미래 칸에 일지 캐릭터를 그리려면 간지가 필요하다(스펙 §3 "날짜·동물은 선명 — 만세력이라 비밀이 아니다"). **판정(score·grade·tenGod·marks·axes)은 계속 안 내보낸다.**

**Files:**
- Modify: `lib/byeolmaru/calendar.ts:80-115`(splitByFreeLine·buildCalendarPayload)
- Modify: `app/api/byeolmaru/calendar/route.ts`(self·pair 두 응답)
- Modify: `components/byeolmaru/CalendarGrid.tsx`(prop)
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`·`SajuTodayView.tsx`·`WooriTodayView.tsx`
- Test: `lib/byeolmaru/free-line.test.ts`·`lib/byeolmaru/calendar.test.ts`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/byeolmaru/free-line.test.ts` 의 기존 단정들에서 `lockedDates` 를 `lockedCells` 로 바꾸고, 아래 두 테스트를 **추가**한다:

```ts
test("잠긴 칸은 간지를 싣고 판정은 안 싣는다", () => {
  const saju = calcSaju({ year: 1994, month: 5, day: 12, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
  const luck = calcDailyLuckRange("2026-09-01", "2026-09-30");
  const { cells, lockedCells } = buildCalendarPayload(saju, luck, "2026-09-10", false);

  assert.ok(lockedCells.length > 0, "비자격이면 안 온 날이 있어야 한다");
  for (const lc of lockedCells) {
    assert.match(lc.ganji, /^[가-힣]{2}$/, "간지 2자가 실려야 일지 캐릭터를 그린다");
    // 🔴 판정이 새지 않는지 — 키 존재를 직접 확인한다(타입만 믿으면 런타임에 뭐가 실렸는지 모른다).
    assert.deepEqual(Object.keys(lc).sort(), ["date", "ganji"]);
  }
  assert.ok(cells.every((c) => c.date <= "2026-09-10"));
});

test("자격자는 잠긴 칸이 0", () => {
  const saju = calcSaju({ year: 1994, month: 5, day: 12, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
  const luck = calcDailyLuckRange("2026-09-01", "2026-09-30");
  const { lockedCells } = buildCalendarPayload(saju, luck, "2026-09-10", true);
  assert.equal(lockedCells.length, 0);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/free-line.test.ts`
Expected: FAIL — `lockedCells` 가 undefined

- [ ] **Step 3: `lib/byeolmaru/calendar.ts` 를 고친다**

`splitByFreeLine` 과 `buildCalendarPayload` 를 아래로 교체:

```ts
/** 아직 안 온 날에 실어 보내는 것 — 날짜와 **간지뿐**이다.
 *  🔴 간지를 싣는 건 무료선 완화가 아니다: 간지는 만세력이라 누구나 계산할 수 있고(비밀이 아니다),
 *     화면은 그걸로 일지 캐릭터만 그린다(스펙 §3). 돈 받는 건 **판정**(점수·등급·하루 이름·마크·축)
 *     이고 그건 여전히 한 글자도 안 나간다. */
export interface LockedCell {
  date: string;
  ganji: string;
}

/**
 * 무료선(P5-2 스펙 §6): **무료 = 지나간 날 + 오늘 · 구독 = 앞당겨 보기.**
 * 🔴 안 온 날은 "가려서 보여주는" 게 아니라 **판정 결과를 아예 안 실어 보낸다**.
 *    클라에서 가리면 devtools 로 다 보이고, 그건 무료선이 아니라 눈속임이다.
 * 나(DayCell)·우리(PairDayCell) 양쪽에 쓰므로 date·ganji 만 요구하는 제네릭이다.
 */
export function splitByFreeLine<T extends { date: string; ganji: string }>(
  cells: T[],
  todayKst: string,
  entitled: boolean
): { open: T[]; lockedCells: LockedCell[] } {
  if (entitled) return { open: cells, lockedCells: [] };
  const open: T[] = [];
  const lockedCells: LockedCell[] = [];
  for (const c of cells) {
    if (c.date <= todayKst) open.push(c);
    else lockedCells.push({ date: c.date, ganji: c.ganji });
  }
  return { open, lockedCells };
}
```

`buildCalendarPayload` 의 반환부만:

```ts
export function buildCalendarPayload(
  saju: SajuResult,
  dailyLuck: DailyLuck[],
  todayKst: string,
  entitled: boolean
): { cells: DayCell[]; lockedCells: LockedCell[]; weeks: WeekBucket[] } {
  const all = buildCalendar(saju, dailyLuck, todayKst);
  const { open, lockedCells } = splitByFreeLine(all, todayKst, entitled);
  return { cells: open, lockedCells, weeks: weekBuckets(open) };
}
```

- [ ] **Step 4: 소비처 5곳을 같은 커밋에서 고친다**

`app/api/byeolmaru/calendar/route.ts`
- pair 분기: `const { open, lockedDates } = splitByFreeLine(...)` → `const { open, lockedCells } = splitByFreeLine(...)`, 응답 `lockedDates` → `lockedCells`.
- self 분기: `const { cells, lockedDates, weeks } = buildCalendarPayload(...)` → `lockedCells`, 응답 키도 `lockedCells`.

`components/byeolmaru/CalendarGrid.tsx`
- prop 선언:

```ts
  /** 아직 안 온 날(P5-2 무료선). 판정 없이 날짜·간지만 온다 — 서버가 내용을 안 실어 보낸다. */
  lockedCells: LockedCell[];
```
- import 에 `import type { LockedCell } from "@/lib/byeolmaru/calendar";` 추가, 시그니처의 `lockedDates` → `lockedCells`.
- 슬롯 조립:

```ts
  const slots: { date: string; cell?: GridCell }[] = [
    ...cells.map((c) => ({ date: c.date, cell: c })),
    ...lockedCells.map((l) => ({ date: l.date })),
  ].sort((a, b) => a.date.localeCompare(b.date));
```
- 하단 안내 조건 두 곳의 `lockedDates.length` → `lockedCells.length`.

`components/byeolmaru/ByeolmaruHub.tsx`
- `CalendarResponse.lockedDates: string[]` → `lockedCells: LockedCell[]`
- `const [pairLocked, setPairLocked] = useState<string[]>([])` → `useState<LockedCell[]>([])`
- pair effect: `setPairLocked(Array.isArray(j.lockedCells) ? j.lockedCells : [])`
- `const gridLocked = isPair ? pairLocked : data.lockedCells;` · `<CalendarGrid lockedCells={gridLocked} …>`
- `EmptyMonthShell`: `lockedCells={dates.map((d) => ({ date: d, ganji: "" }))}`
  (여기엔 사주가 없어 간지를 만들 수 없다 — 빈 문자열이면 `branchAnimal` 이 null 을 주고 동물이 안 그려진다. 그게 맞는 화면이다.)

`components/byeolmaru/SajuTodayView.tsx`·`components/byeolmaru/WooriTodayView.tsx`
- 응답 타입과 `<CalendarGrid …>` 호출의 `lockedDates` → `lockedCells` (SajuTodayView 의 격자는 Task 12 에서 삭제되지만, 지금은 컴파일이 되게만 바꾼다).

`lib/byeolmaru/calendar.test.ts` 의 `lockedDates` 단정도 `lockedCells` 로 맞춘다.

- [ ] **Step 5: 게이트**

Run: `node --import tsx --test "lib/**/*.test.ts"` → PASS
Run: `npx tsc --noEmit` → exit 0
Run: `grep -rn "lockedDates" lib components app` → **출력 0줄** (남아 있으면 그 파일이 낡은 계약을 쓰고 있다)

- [ ] **Step 6: 커밋**

```bash
git add lib/byeolmaru/calendar.ts lib/byeolmaru/calendar.test.ts lib/byeolmaru/free-line.test.ts app/api/byeolmaru/calendar/route.ts components/byeolmaru/CalendarGrid.tsx components/byeolmaru/ByeolmaruHub.tsx components/byeolmaru/SajuTodayView.tsx components/byeolmaru/WooriTodayView.tsx
git commit -m "refactor(byeolmaru): 무료선이 잠긴 날의 간지를 함께 내보낸다 — 판정은 그대로 차단 (P6-3 §3)"
```

---

### Task 3: 캘린더 라우트가 롤링 7일 창을 함께 내려준다

**Files:**
- Modify: `app/api/byeolmaru/calendar/route.ts`

- [ ] **Step 1: import 를 추가한다**

```ts
import { buildCalendar, buildCalendarPayload, monthRange, splitByFreeLine } from "@/lib/byeolmaru/calendar";
import { stripRange } from "@/lib/byeolmaru/strip";
```

- [ ] **Step 2: 공통 계산을 월 범위 바로 아래에 넣는다**

`const monthLuck = calcDailyLuckRange(monthStart, monthEnd);` 바로 뒤:

```ts
    // 허브 롤링 스트립(스펙 §2-1) — 오늘 중심 7일. 월 범위와 겹치지만 **월 경계를 넘는 날**이 있어
    // monthLuck 에서 잘라 쓸 수 없다(1일이면 지난 3일이 전달, 말일이면 미래 3일이 다음 달).
    // 룰 100% 라 7일 추가 계산의 원가는 0이다.
    const { start: stripStart, end: stripEnd } = stripRange(todayKst);
    const stripLuck = calcDailyLuckRange(stripStart, stripEnd);
```

- [ ] **Step 3: self 응답에 strip 을 싣는다**

`const { cells, lockedCells, weeks } = buildCalendarPayload(...)` 바로 뒤:

```ts
    // 스트립도 **같은 무료선**을 탄다(splitByFreeLine 단일 원천) — 비자격은 미래 3칸이 간지만,
    // 구독자는 7칸 전부 열린다.
    const stripSplit = splitByFreeLine(buildCalendar(saju, stripLuck, todayKst), todayKst, ent.entitled);
```

응답 객체에 추가:

```ts
      strip: { cells: stripSplit.open, lockedCells: stripSplit.lockedCells },
```

- [ ] **Step 4: pair 응답에도 싣는다**

pair 분기의 `const { open, lockedCells } = splitByFreeLine(pairCells, todayKst, ent.entitled);` 뒤:

```ts
      const pairStrip = splitByFreeLine(
        buildPairCalendar(saju, partnerSaju, stripLuck, todayKst),
        todayKst,
        ent.entitled
      );
```

pair 응답 객체에 추가:

```ts
        strip: { cells: pairStrip.open, lockedCells: pairStrip.lockedCells },
```

- [ ] **Step 5: 실제 호출로 확인한다**

dev 서버를 띄우고(`.claude/launch.json` 의 dev), QA 봇 세션으로 `/api/byeolmaru/calendar` 를 호출해 `strip.cells` 가 존재하고 `strip.cells.length + strip.lockedCells.length === 7` 인지 본다.

Run: `npx tsc --noEmit` → exit 0

- [ ] **Step 6: 커밋**

```bash
git add app/api/byeolmaru/calendar/route.ts
git commit -m "feat(byeolmaru): 캘린더 응답에 오늘 중심 7일 스트립 추가 — 월 경계를 넘으므로 월 범위에서 자를 수 없다 (P6-3 §2-1)"
```

---

### Task 4: 마크를 2글자 컬러 라벨로 (`day-label.ts`)

**Files:**
- Modify: `lib/byeolmaru/day-label.ts:70-90`
- Test: `lib/byeolmaru/day-label.test.ts`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/byeolmaru/day-label.test.ts` 에 추가:

```ts
import { MARK_COLOR, dayMarks, type DayGlyph } from "./day-label.ts";

test("마크 라벨은 전부 2글자 — 셀 하단 띠가 한 줄에 들어가야 한다", () => {
  const all = [
    dayMarks({ relation: "비화", heavenlyCombo: true, sixCombo: true, clash: false, scarcity: "absent" }),
    dayMarks({ relation: "비화", heavenlyCombo: false, sixCombo: false, clash: true, scarcity: "balanced" }),
  ].flat();
  assert.ok(all.length >= 4, "네 종류가 다 나와야 한다");
  for (const m of all) assert.equal(m.label.length, 2, `${m.glyph} 라벨이 2글자가 아니다: ${m.label}`);
});

test("나 탭 마크는 전부 full — 강도 2단은 우리 탭에서만 쓴다", () => {
  const ms = dayMarks({ relation: "비화", heavenlyCombo: true, sixCombo: false, clash: false, scarcity: "balanced" });
  assert.equal(ms[0].strength, "full");
});

test("MARK_COLOR 가 글리프 4종을 빠짐없이 덮는다", () => {
  // 🔴 Record 타입만 믿지 않는다 — 실제 키 집합을 대조한다(폴백 없는 조회라 빠지면 런타임 undefined).
  const glyphs: DayGlyph[] = ["✧", "◇", "△", "＋"];
  assert.deepEqual(Object.keys(MARK_COLOR).sort(), [...glyphs].sort());
  for (const g of glyphs) assert.match(MARK_COLOR[g], /^#[0-9A-F]{6}$/i);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/day-label.test.ts`
Expected: FAIL — `MARK_COLOR` 가 export 되지 않음

- [ ] **Step 3: 구현**

`lib/byeolmaru/day-label.ts` 의 `DayMark` 선언부터 `dayMarks` 끝까지를 아래로 교체:

```ts
export type DayGlyph = "✧" | "◇" | "△" | "＋";

/** 마크 강도 — 우리 탭에서 **두 사람 다** 걸리면 full, 한 명만이면 half(스펙 §3-1-a).
 *  점수도 같은 규칙으로 가중되므로 시각과 점수가 어긋나지 않는다. 나 탭은 사람이 하나라 항상 full. */
export type MarkStrength = "full" | "half";

export interface DayMark {
  glyph: DayGlyph;
  /** 🔴 2글자 고정 — 셀 하단 띠(42px 칸)에 한 줄로 들어가야 한다(스펙 §3). 늘리려면 셀 폭부터 재라. */
  label: string;
  /** 🔴 옵셔널이 아니다 — 옵셔널이면 생산자가 빠뜨려도 타입이 통과하고 화면에선 조용히 연한 쪽으로
   *     떨어진다(우리 탭 전 칸이 half 로 보이는 사고). 생산자 둘 다 명시한다. */
  strength: MarkStrength;
}

/** 2글자 라벨의 색(스펙 §3). 8px 회색 글리프가 사실상 안 보여서 색으로 구분을 옮겼다.
 *  🔴 @theme 토큰으로 올리지 않는다 — 달력 판 안에서만 쓰는 국소 팔레트다(CalendarGrid 의
 *     TONE_STYLE 과 같은 이유). ＋(채움)만 §3 표에 없어 lilac-deep 을 쓴다. */
export const MARK_COLOR: Record<DayGlyph, string> = {
  "✧": "#C99A28",
  "◇": "#3E8E8A",
  "△": "#C4738E",
  "＋": "#9F8AD0",
};

/**
 * 그날을 그렇게 만든 원인 마크. 달력 셀 하단 띠 + 상세 칩에 쓴다.
 * 🔴 어휘는 우리 탭(pairMarks)과 **같다** — 판정 primitive 가 실제로 같기 때문이다(천간합·육합·충).
 *    예전 라벨(천간합·육합·충·빈 곳 채움)은 용어였고 길이도 1~5자로 들쭉날쭉해 2글자 띠에 안 들어갔다.
 *    두 탭이 한 문법을 쓰게 한다는 건 이 파일이 처음부터 적어둔 의도다.
 * 순서는 고정(천간합 → 육합 → 충 → 빈 곳)이라 같은 날이 늘 같은 순서로 보인다.
 */
export function dayMarks(f: DayFactors): DayMark[] {
  const out: DayMark[] = [];
  if (f.heavenlyCombo) out.push({ glyph: "✧", label: "끌림", strength: "full" });
  if (f.sixCombo) out.push({ glyph: "◇", label: "결속", strength: "full" });
  if (f.clash) out.push({ glyph: "△", label: "삐걱", strength: "full" });
  // scarce(1개)는 마크를 붙이지 않는다 — "아예 없던 게 채워지는" 날만 눈에 띄게 한다.
  if (f.scarcity === "absent") out.push({ glyph: "＋", label: "채움", strength: "full" });
  return out;
}
```

- [ ] **Step 4: 기존 단정 갱신 + 게이트**

`lib/byeolmaru/day-label.test.ts`·`lib/byeolmaru/calendar.test.ts` 에 `"천간합"`·`"육합"`·`"충"`·`"빈 곳 채움"` 을 기대하는 단정이 있으면 새 라벨로 바꾼다.

Run: `node --import tsx --test "lib/**/*.test.ts"` → PASS
Run: `npx tsc --noEmit` → exit 0 (여기서 `pairMarks` 가 `strength` 를 안 줘서 깨진다 — **Task 5 로 미루지 말고 지금 같은 커밋에서** 아래 한 줄씩 채운다)

`lib/byeolmaru/pair-day.ts` 의 `pairMarks` 임시 보정(정식 강도 계산은 Task 5):

```ts
  if (tags.spark) out.push({ glyph: "✧", label: "끌림", strength: "full" });
  if (tags.bond) out.push({ glyph: "◇", label: "결속", strength: "full" });
  if (tags.friction) out.push({ glyph: "△", label: "삐걱", strength: "full" });
```

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/day-label.ts lib/byeolmaru/day-label.test.ts lib/byeolmaru/calendar.test.ts lib/byeolmaru/pair-day.ts
git commit -m "feat(byeolmaru): 마크를 2글자 컬러 라벨로 — 나·우리 어휘 통일 + 강도 필드 (P6-3 §3)"
```

---

### Task 5: 페어 점수 V4 (`pair-day.ts`)

측정 결과(`specs/2026-09-20-별마루-페어-톤-분포-findings.md`)로 확정된 처방. **끌림·결속을 OR 에서 가중으로, baseline 을 점수에서 제거.** 삐걱과 임계값은 그대로.

**Files:**
- Modify: `lib/byeolmaru/pair-day.ts`
- Test: `lib/byeolmaru/pair-day.test.ts`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`lib/byeolmaru/pair-day.test.ts` 에 추가:

```ts
test("V4 — 한 명만 끌리면 절반, 둘 다면 full", () => {
  // 같은 날·같은 사람들로 tags 를 직접 만들어 pairMarks 의 강도만 본다(점수 경로는 아래 테스트가 본다).
  const half = pairMarks({ spark: true, sparkBoth: false, bond: false, bondBoth: false, friction: false, frictionBoth: false, lead: null });
  const full = pairMarks({ spark: true, sparkBoth: true, bond: false, bondBoth: false, friction: false, frictionBoth: false, lead: null });
  assert.equal(half[0].strength, "half");
  assert.equal(full[0].strength, "full");
});

test("V4 — baseline 이 점수에서 빠졌다: 궁합이 좋아도 달 전체가 들리지 않는다", () => {
  // 🔴 대조 단정: 같은 두 사람의 30일 점수 평균이 "두 사람 각자 점수 평균 + 그날 사건"만으로 설명돼야
  //    한다. baseline 이 남아 있으면 배경(천간합/육합/harmony)이 있는 쌍에서 평균이 그만큼 높다.
  const a = calcSaju({ year: 1994, month: 5, day: 12, hour: 9, gender: "female", isLunar: false, isLeapMonth: false });
  const b = calcSaju({ year: 1992, month: 11, day: 3, hour: null, gender: "male", isLunar: false, isLeapMonth: false });
  const luck = calcDailyLuckRange("2026-09-01", "2026-09-30");
  const backdrop = pairBackdrop(a, b);
  const cells = buildPairCalendar(a, b, luck, "2026-09-20");

  const noSignal = cells.filter((c) => !c.tags.spark && !c.tags.bond && !c.tags.friction);
  assert.ok(noSignal.length > 0, "사건 없는 날이 있어야 이 단정이 의미가 있다");
  for (const c of noSignal) {
    const solo = (dayScore(dayFactors(toDaySelf(a), luckOf(luck, c.date))) +
                  dayScore(dayFactors(toDaySelf(b), luckOf(luck, c.date)))) / 2;
    assert.equal(c.score, Math.round(solo), `${c.date}: 사건 없는 날은 두 사람 평균 그대로여야 한다`);
  }
  // backdrop 자체는 계속 나간다 — 배경 설명("너희 결")이 그걸 쓴다.
  assert.equal(typeof backdrop.harmony, "number");
});

function luckOf(luck: { date: string; stem: string; branch: string; element: FiveElement }[], date: string) {
  const d = luck.find((x) => x.date === date);
  assert.ok(d, `${date} 일진이 표본에 있어야 한다`);
  return { stem: d.stem, branch: d.branch, element: d.element };
}
```

필요한 import: `import { dayFactors, dayScore } from "./day-score.ts";` · `import { toDaySelf } from "./calendar.ts";` · `import { calcDailyLuckRange } from "@/lib/saju/calc";` · `import type { FiveElement } from "@/lib/saju/elements";`

- [ ] **Step 2: 실패를 확인한다**

Run: `node --import tsx --test lib/byeolmaru/pair-day.test.ts`
Expected: FAIL — `sparkBoth` 가 타입에 없고, 사건 없는 날 점수가 baseline 만큼 높다

- [ ] **Step 3: 구현**

`PairDayTags` 교체:

```ts
export interface PairDayTags {
  /** 둘 중 **한 명이라도** 걸렸나 — 표시·서술이 쓰는 값(narrative-prompt·static-lines 무회귀). */
  spark: boolean;
  /** 둘 **다** 걸렸나 — 점수 가중과 마크 강도가 쓰는 값(스펙 §3-1-a). */
  sparkBoth: boolean;
  bond: boolean;
  bondBoth: boolean;
  friction: boolean;
  frictionBoth: boolean;
  lead: "me" | "partner" | null;
}
```

상수에서 baseline 3종을 지우고(`BASELINE_SPARK_W`·`BASELINE_BOND_W`·`BASELINE_HARMONY_W`) 주석을 남긴다:

```ts
const LEAD_THRESHOLD = 8;
const SPARK_W = 12;
const BOND_W = 9;
const FRICTION_W = -14;
// 🔴 baseline(쌍 고정 가산)은 2026-09-20 측정으로 **점수에서 제거**됐다(스펙 §3-1-a).
//    쌍의 76%가 +0 이라 레벨 요인이 아니었고, 남기면 궁합 좋은 쌍만 달 전체가 들려 금색 꼬리를 만든다.
//    궁합은 pairBackdrop 으로 계속 나가고 **배경 설명**이 그걸 말한다 — 점수엔 안 들어간다.
```

`pairDayScoreAndTags` 교체(인자에서 `backdrop` 제거):

```ts
function pairDayScoreAndTags(a: SajuResult, b: SajuResult, d: DailyLuck) {
  const dl = { stem: d.stem, branch: d.branch, element: d.element };
  const scoreA = dayScore(dayFactors(toDaySelf(a), dl));
  const scoreB = dayScore(dayFactors(toDaySelf(b), dl));
  const base = (scoreA + scoreB) / 2;

  // 🔴 OR 이 아니라 **머릿수**를 센다. OR 이면 한 명만 걸린 날(발화 칸의 95%)이 둘 다인 날과 똑같이
  //    만점을 받아, 1인용 임계(70)에 2배 확률의 가산점을 얹는 꼴이었다(실측 1.91×·1.92×).
  const sparkN = (heavenlyCombo(d.stem, a.dayStem) ? 1 : 0) + (heavenlyCombo(d.stem, b.dayStem) ? 1 : 0);
  const bondN = (earthlySixCombo(d.branch, a.pillars.day.branch) ? 1 : 0) + (earthlySixCombo(d.branch, b.pillars.day.branch) ? 1 : 0);
  // 삐걱은 OR 그대로 — 한 명만 충이어도 그날 조심할 이유는 실재하고, 가중하면 "챙길 날"만
  // 18.9%→15.4% 로 희석된다(금색 과다와 무관한 부작용).
  const frictionN = (earthlySixClash(d.branch, a.pillars.day.branch) ? 1 : 0) + (earthlySixClash(d.branch, b.pillars.day.branch) ? 1 : 0);

  const score = clamp(base + SPARK_W * (sparkN / 2) + BOND_W * (bondN / 2) + (frictionN > 0 ? FRICTION_W : 0));
  const lead: PairDayTags["lead"] =
    Math.abs(scoreA - scoreB) >= LEAD_THRESHOLD ? (scoreA > scoreB ? "me" : "partner") : null;

  return {
    score,
    tags: {
      spark: sparkN > 0, sparkBoth: sparkN === 2,
      bond: bondN > 0, bondBoth: bondN === 2,
      friction: frictionN > 0, frictionBoth: frictionN === 2,
      lead,
    } satisfies PairDayTags,
  };
}
```

`buildPairCalendar` 에서 backdrop 인자 전달을 뺀다:

```ts
export function buildPairCalendar(a: SajuResult, b: SajuResult, dailyLuck: DailyLuck[], todayKst: string): PairDayCell[] {
  return dailyLuck.map((d) => {
    const { score, tags } = pairDayScoreAndTags(a, b, d);
    return { date: d.date, ganji: d.stem + d.branch, score, tone: pairDayTone(score), tags, isToday: d.date === todayKst };
  });
}
```

`pairMarks` 에 강도를 넣는다:

```ts
export function pairMarks(tags: PairDayTags): DayMark[] {
  const out: DayMark[] = [];
  // 강도 = 점수 가중과 같은 규칙(둘 다 full · 한 명 half). 실측상 "둘 다"는 발화 칸의 5% 뿐이라,
  // 둘 다일 때만 마크를 띄우면 30일에 0.3번이라 사실상 마크 폐지가 된다 — 그래서 끄지 않고 연하게 쓴다.
  if (tags.spark) out.push({ glyph: "✧", label: "끌림", strength: tags.sparkBoth ? "full" : "half" });
  if (tags.bond) out.push({ glyph: "◇", label: "결속", strength: tags.bondBoth ? "full" : "half" });
  if (tags.friction) out.push({ glyph: "△", label: "삐걱", strength: tags.frictionBoth ? "full" : "half" });
  return out;
}
```

- [ ] **Step 4: 실측 재현으로 확인한다**

측정 스크립트의 복제 상수를 새 공식에 맞춰 갱신하고 돌려, 표가 findings 의 **V4 행**과 일치하는지 본다.

`scripts/byeolmaru-pair-tone-freq.ts` 상단 주석에 아래를 덧붙인다:

```ts
// ⚠️ 2026-09-20 이후 pair-day.ts 는 V4(가중 + baseline 제거)다. 이 스크립트의 V0 은 **옛 공식**이라
//    대조 단정이 깨진다 — V0 을 현행 공식(=V4)으로 바꾸고 돌리거나, 과거 비교용이면 단정을 끌 것.
```

Run: `node --import tsx --test "lib/**/*.test.ts"` → PASS
Run: `npx tsc --noEmit` → exit 0

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/pair-day.ts lib/byeolmaru/pair-day.test.ts scripts/byeolmaru-pair-tone-freq.ts
git commit -m "feat(byeolmaru): 우리 오늘 점수 V4 — 끌림·결속 가중 + baseline 제거 (P6-3 §3-1-a)"
```

---

### Task 6: 달력 셀 시각 (`CalendarGrid.tsx`)

§2-2 가 격자 셀을 **날짜 + 톤 색면 + 마크 띠**로 줄였다(동물·간지는 스트립과 상세 담당).

**Files:**
- Modify: `components/byeolmaru/CalendarGrid.tsx`

- [ ] **Step 1: 톤 팔레트를 §3 확정값으로 바꾼다**

```ts
const TONE_STYLE: Record<DayTone, { background: string; border?: string; boxShadow?: string }> = {
  // 챙길 날이 rgba(255,255,255,.45) 라 무난한 날(순백)과 거의 같은 색이었다 — 실물에서 구분 불가.
  good: { background: "linear-gradient(160deg,#F7DFA4,#E8C26A)", boxShadow: "0 2px 6px rgba(232,194,106,0.45)" },
  normal: { background: "#ffffff" },
  caution: { background: "linear-gradient(160deg,#EFE7F8,#DCCFF0)", border: "1px solid rgba(184,168,216,.30)" },
};
```

- [ ] **Step 2: 셀 내용을 교체한다**

`<span>날짜</span>` 부터 간지 `<span>` 까지(현행 148~180행)를 아래로 교체:

```tsx
              {/* 날짜 + 마크 띠. 간지 텍스트와 일지 캐릭터는 셀에서 뺐다(스펙 §2-2) — 42px 칸에 4겹이
                  들어가 제일 큰 요소(동물)가 의미를 안 담고 진짜 내용(톤)이 제일 약했다.
                  동물은 스트립이, 간지는 상세 카드가 담당한다. */}
              <span className="text-[13px] font-semibold leading-none text-eye-purple">
                {Number(c.date.slice(8, 10))}
              </span>
              {c.marks.length ? (
                <span
                  // 🔴 셀에는 최우선 마크 1개만. 2개부터 42px 칸에서 날짜와 겹친다(실측).
                  //    전체 목록은 aria-label 과 상세 카드의 마크 칩이 받는다.
                  className="mt-1 rounded-full px-1 text-[9px] font-bold leading-[13px]"
                  style={{
                    color: MARK_COLOR[c.marks[0].glyph],
                    // half = 한 명만 걸린 날(우리 탭). 점수도 절반이라 시각과 점수가 어긋나지 않는다.
                    background: `${MARK_COLOR[c.marks[0].glyph]}${c.marks[0].strength === "full" ? "2E" : "14"}`,
                    opacity: c.marks[0].strength === "full" ? 1 : 0.75,
                  }}
                >
                  {c.marks[0].label}
                </span>
              ) : null}
```

import 에 `MARK_COLOR` 를 추가하고, 더 이상 쓰지 않는 `Image`·`branchAnimal` import 를 **지운다**(내 변경이 만든 orphan).

- [ ] **Step 3: 잠긴 칸에 "가려진 자리"를 만든다**

잠긴 칸 렌더를 교체:

```tsx
            return (
              <div
                key={date}
                role="img"
                aria-label={`${date} 아직 안 온 날`}
                className="flex aspect-square flex-col items-center justify-center rounded-xl border border-dashed"
                style={{ background: "rgba(255,255,255,.28)", borderColor: "rgba(184,168,216,.40)" }}
              >
                <span className="text-[13px] font-semibold leading-none text-text-light/60">
                  {Number(date.slice(8, 10))}
                </span>
                {/* 마크 띠 자리를 **빈 채로** 남긴다 — 가짜 내용을 흐려 보여주지 않는다(없는 걸 있는 척
                    하지 않는다). 자리만 비어 있어 "여기 뭔가 들어온다"가 레이아웃으로 읽힌다. */}
                <span aria-hidden className="mt-1 h-[13px] w-6 rounded-full bg-lilac-mid/25" />
              </div>
            );
```

- [ ] **Step 4: 범례를 강도까지 반영한다**

```tsx
          {legend.length > 0 && (
            <p>
              {legend.map((m) => (
                <span key={m.glyph} className="mr-2" style={{ color: MARK_COLOR[m.glyph] }}>
                  <span className="font-bold">{m.label}</span>
                </span>
              ))}
              {/* 연한 라벨의 뜻을 한 번만 설명한다 — 우리 탭에서만 나온다. */}
              {subjectKind === "pair" && <span className="text-text-light">연한 건 한 명만 걸린 날이야</span>}
            </p>
          )}
```

- [ ] **Step 5: 게이트**

Run: `npx tsc --noEmit` → exit 0
Run: `npm run build` → exit 0

- [ ] **Step 6: 커밋**

```bash
git add components/byeolmaru/CalendarGrid.tsx
git commit -m "feat(byeolmaru): 달력 셀 시각 개편 — 간지·동물 제거, 챙길 날 연보라, 마크 2글자 컬러 라벨 (P6-3 §3)"
```

---

### Task 7: 롤링 7일 스트립 (`DayStrip.tsx`)

**Files:**
- Create: `components/byeolmaru/DayStrip.tsx`

- [ ] **Step 1: 컴포넌트를 만든다**

```tsx
"use client";

// components/byeolmaru/DayStrip.tsx — 허브 롤링 7일 스트립(스펙 §2-1).
// 지난 3일 + 오늘(2배 폭) + 앞으로 3일. 가려지는 게 **미래**라 "안 준다"가 아니라 "아직 안 온 날"로
// 읽힌다 — 자물쇠를 쓰지 않는다는 P5 §2 원칙과 맞는 유일한 블러 형태다.
// 🔴 흐린 칸 수를 세지 않는다 — lockedCells 가 곧 그 집합이다(서버 무료선의 산물).
import Image from "next/image";
import { branchAnimal } from "@/lib/byeolmaru/branch-animal";
import { MARK_COLOR, type DayMark } from "@/lib/byeolmaru/day-label";
import type { LockedCell } from "@/lib/byeolmaru/calendar";
import type { DayTone } from "@/lib/byeolmaru/day-score";
import { trackUiEvent } from "@/lib/analytics/ui-events";

/** 스트립 한 칸이 그리는 것만 받는다 — 나·우리 두 판정이 이 모양으로 수렴한다(GridCell 과 같은 패턴). */
export interface StripCell {
  date: string;
  ganji: string;
  tone: DayTone;
  /** 하루 이름(나 탭) 또는 톤 라벨(우리 탭). 오늘 칸에만 보인다. */
  title: string;
  marks: DayMark[];
  isToday: boolean;
}

const TONE_BG: Record<DayTone, string> = {
  good: "linear-gradient(160deg,#F7DFA4,#E8C26A)",
  normal: "#ffffff",
  caution: "linear-gradient(160deg,#EFE7F8,#DCCFF0)",
};

const WEEKDAY = ["일", "월", "화", "수", "목", "금", "토"];

function weekdayOf(date: string): string {
  return WEEKDAY[new Date(`${date}T00:00:00Z`).getUTCDay()];
}

interface Props {
  cells: StripCell[];
  lockedCells: LockedCell[];
  todayDate: string;
  /** 열린 칸 탭 — 그날 상세로 보낸다. */
  onSelect: (date: string) => void;
  /** 흐린(미래) 칸 탭 — 비자격자에게 구독 안내. */
  onLockedSelect: () => void;
  subjectKind: "me" | "pair";
}

export default function DayStrip({ cells, lockedCells, todayDate, onSelect, onLockedSelect, subjectKind }: Props) {
  const slots = [
    ...cells.map((c) => ({ date: c.date, ganji: c.ganji, cell: c as StripCell | undefined })),
    ...lockedCells.map((l) => ({ date: l.date, ganji: l.ganji, cell: undefined })),
  ].sort((a, b) => a.date.localeCompare(b.date));
  if (slots.length === 0) return null;

  return (
    <div
      // 오늘 칸이 2배 폭이라 트랙은 7이 아니라 8칸이다. 오늘이 span 2 를 먹는다.
      className="grid gap-1"
      style={{ gridTemplateColumns: "repeat(8, minmax(0,1fr))" }}
    >
      {slots.map(({ date, ganji, cell }) => {
        const today = cell?.isToday ?? false;
        const animal = branchAnimal(ganji);
        const common = "flex flex-col items-center justify-center rounded-xl py-1.5";

        if (!cell) {
          return (
            <button
              key={date}
              type="button"
              onClick={() => {
                const offset = Math.round((Date.parse(`${date}T00:00:00Z`) - Date.parse(`${todayDate}T00:00:00Z`)) / 86400000);
                trackUiEvent("byeolmaru_strip_future_tapped", { meta: { offset, subjectKind } });
                onLockedSelect();
              }}
              aria-label={`${date} 아직 안 온 날`}
              className={`${common} border border-dashed`}
              style={{ background: "rgba(255,255,255,.28)", borderColor: "rgba(184,168,216,.40)" }}
            >
              <span className="text-[10px] leading-none text-text-light/70">{weekdayOf(date)}</span>
              <span className="mt-0.5 text-[12px] font-semibold leading-none text-text-light/70">{Number(date.slice(8, 10))}</span>
              {/* 날짜·동물은 선명하다 — 만세력이라 비밀이 아니다(스펙 §3). 가리는 건 판정뿐. */}
              {animal ? <Image src={animal.assetSrc} alt="" width={18} height={18} className="mt-0.5 h-[18px] w-[18px] object-contain opacity-70" /> : null}
              <span aria-hidden className="mt-1 h-[10px] w-5 rounded-full bg-lilac-mid/30" />
            </button>
          );
        }

        return (
          <button
            key={date}
            type="button"
            onClick={() => {
              const offset = Math.round((Date.parse(`${date}T00:00:00Z`) - Date.parse(`${todayDate}T00:00:00Z`)) / 86400000);
              trackUiEvent("byeolmaru_day_selected", { meta: { offset, tone: cell.tone, subjectKind } });
              onSelect(date);
            }}
            aria-label={`${date} ${cell.title}${cell.marks.length ? ` · ${cell.marks.map((m) => m.label).join(", ")}` : ""}`}
            className={common}
            style={{
              background: TONE_BG[cell.tone],
              gridColumn: today ? "span 2" : undefined,
              ...(today ? { boxShadow: "0 0 0 2px #5A3E8C" } : {}),
            }}
          >
            <span className="text-[10px] leading-none text-text-light">{today ? "오늘" : weekdayOf(date)}</span>
            <span className="mt-0.5 text-[12px] font-semibold leading-none text-eye-purple">{Number(date.slice(8, 10))}</span>
            {animal ? <Image src={animal.assetSrc} alt="" width={today ? 26 : 18} height={today ? 26 : 18} className="mt-0.5 object-contain" style={{ width: today ? 26 : 18, height: today ? 26 : 18 }} /> : null}
            {/* 정보는 오늘 칸에 몰린다 — 나머지 6칸이 조용해야 "A는 번잡 / B는 허전" 딜레마가 풀린다. */}
            {today && (
              <span className="mt-0.5 line-clamp-2 px-1 text-center text-[10px] leading-tight text-eye-purple">{cell.title}</span>
            )}
            {cell.marks.length ? (
              <span
                className="mt-0.5 rounded-full px-1 text-[9px] font-bold leading-[13px]"
                style={{
                  color: MARK_COLOR[cell.marks[0].glyph],
                  background: `${MARK_COLOR[cell.marks[0].glyph]}${cell.marks[0].strength === "full" ? "2E" : "14"}`,
                  opacity: cell.marks[0].strength === "full" ? 1 : 0.75,
                }}
              >
                {cell.marks[0].label}
              </span>
            ) : null}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: 계측 이름을 allowlist 에 넣는다**

`lib/analytics/ui-events.ts` 의 `UI_EVENTS` 에 추가:

```ts
  /** 별마루 — 스트립의 안 온 날(흐린 칸) 탭. meta:{offset, subjectKind} — "앞으로 3일"이 체험 전환을 만드는지(§10) */
  "byeolmaru_strip_future_tapped",
```

- [ ] **Step 3: 게이트**

Run: `npx tsc --noEmit` → exit 0

- [ ] **Step 4: 커밋**

```bash
git add components/byeolmaru/DayStrip.tsx lib/analytics/ui-events.ts
git commit -m "feat(byeolmaru): 롤링 7일 스트립 — 오늘 2배 폭·미래 3칸은 날짜/동물만 선명 (P6-3 §2-1)"
```

---

### Task 8: 접이식 월간 격자 (`MonthGridSection.tsx`)

**Files:**
- Create: `components/byeolmaru/MonthGridSection.tsx`
- Modify: `lib/analytics/ui-events.ts`

- [ ] **Step 1: 컴포넌트를 만든다**

```tsx
"use client";

// components/byeolmaru/MonthGridSection.tsx — 월간 격자 접이식 래퍼(스펙 §2-2).
// 별마루는 실사용 데이터가 0이라 "사람들이 한 달을 훑는가"를 아무도 모른다. 항상 펼침은 그 가설에
// 370px 를 선지불하는 것이고 폐지는 확인할 기회를 없앤다 → **접힘 클릭률로 답을 얻는다**.
// 🔴 채움 서사("N칸 열림")를 이 버튼이 지고 간다 — 그래서 AttendanceStrip 에서 같은 문구를 뺐다.
import { useEffect, useState } from "react";
import { trackUiEvent } from "@/lib/analytics/ui-events";

const KEY = "byeolkong_month_grid_seen";

interface Props {
  /** 이번 달 열린 칸 수 — 버튼 문구의 N. */
  filledDays: number;
  children: React.ReactNode;
}

export default function MonthGridSection({ filledDays, children }: Props) {
  // 🔴 null 로 시작해 effect 에서 정한다 — localStorage 는 서버에 없어 초기 렌더에 읽으면
  //    하이드레이션 불일치가 난다(useBaitDismiss 와 같은 이유). 허브는 어차피 캘린더를 fetch 한
  //    뒤에 이 자리를 그리므로 실제로는 깜빡임이 보이지 않는다.
  const [open, setOpen] = useState<boolean | null>(null);

  useEffect(() => {
    let seen = false;
    try {
      seen = localStorage.getItem(KEY) === "1";
    } catch {
      /* 프라이빗 창 등 — 못 읽으면 "첫 방문"으로 떨어뜨린다(한 번 펼쳐 보여주는 쪽이 안전) */
    }
    setOpen(!seen); // 첫 방문만 펼친 상태로 시작
  }, []);

  function toggle() {
    const next = !(open ?? false);
    setOpen(next);
    trackUiEvent("byeolmaru_month_grid_toggled", { meta: { open: next } });
    try {
      localStorage.setItem(KEY, "1"); // 한 번이라도 손대면 그 뒤로는 접힌 채 시작한다
    } catch {
      /* 저장 실패해도 이번 세션 동안은 유지된다 */
    }
  }

  return (
    <section className="space-y-2">
      <button
        type="button"
        onClick={toggle}
        aria-expanded={open ?? false}
        className="flex w-full items-center justify-between rounded-2xl bg-cream-warm px-4 py-3 text-left"
      >
        <span className="text-sm font-medium text-eye-purple">
          이번 달 전체 보기 · {filledDays}칸 열림
        </span>
        <span aria-hidden className="text-xs text-text-light">{open ? "▴" : "▾"}</span>
      </button>
      {open ? children : null}
    </section>
  );
}
```

- [ ] **Step 2: 계측 이름을 allowlist 에 넣는다**

```ts
  /** 별마루 — 월간 격자 펼침/접힘. meta:{open} — §2-2 "한 달 조망이 필요한가" 가설의 관문(§10) */
  "byeolmaru_month_grid_toggled",
```

- [ ] **Step 3: 게이트**

Run: `npx tsc --noEmit` → exit 0

- [ ] **Step 4: 커밋**

```bash
git add components/byeolmaru/MonthGridSection.tsx lib/analytics/ui-events.ts
git commit -m "feat(byeolmaru): 월간 격자 접이식 — 첫 방문만 펼침 + 토글 계측 (P6-3 §2-2)"
```

---

### Task 9: 배너 (`HubBanner.tsx`)

**Files:**
- Create: `components/byeolmaru/HubBanner.tsx`

- [ ] **Step 1: 컴포넌트를 만든다**

```tsx
// components/byeolmaru/HubBanner.tsx — 별마루 전용 배너(스펙 §8).
// 2탭 FortuneHeader 의 rich variant 와 **동형**이되 치수는 다르다: 배경은 CSS 그라데이션이 그리고
// 캐릭터만 투명 webp 를 얹는다(배경까지 구워 넣으면 CSS 와 싸우고 폭이 바뀔 때 깨진다).
// 🔴 슬롯 폭 120px 은 실측값이다(스펙 §8-7). 이 에셋의 잘라낸 비율이 세로/가로 ≈ 1.0(정사각)이라
//    2탭의 77px 슬롯에 넣으면 77×77 만 써서 얼굴이 뭉갠다 — 110 미만으로 줄이지 말 것.
import Image from "next/image";

export default function HubBanner() {
  return (
    <section
      className="relative overflow-hidden rounded-2xl px-5 py-6"
      style={{ background: "linear-gradient(160deg,#E8DEF5 0%,#D4C7EE 100%)" }}
    >
      <div className="relative max-w-[62%]">
        <h1 className="font-display text-2xl text-eye-purple">별마루</h1>
        <p className="mt-1 text-[12.5px] leading-relaxed text-text-light">
          오늘 너의 하늘, 한 자리에 모아뒀어
        </p>
      </div>
      <div className="pointer-events-none absolute right-4 top-1/2 h-[120px] w-[120px] -translate-y-1/2">
        <Image src="/byeolmaru-byeolkong.webp" alt="" fill sizes="120px" className="object-contain" priority />
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 실물 확인**

dev 서버에서 `/byeolmaru` 를 열고 배너가 잘리지 않는지, 375px 폭에서 텍스트와 캐릭터가 겹치지 않는지 본다(Task 11 이후에야 화면에 붙으므로, 이 태스크에선 tsc·build 만).

Run: `npx tsc --noEmit` → exit 0

- [ ] **Step 3: 커밋**

```bash
git add components/byeolmaru/HubBanner.tsx
git commit -m "feat(byeolmaru): 허브 배너 — 전용 별콩이 + 120px 슬롯 (P6-3 §8)"
```

---

### Task 10: 무료 목록 4종 동형화 (`FreeList.tsx`)

**Files:**
- Modify: `components/byeolmaru/FreeList.tsx`

- [ ] **Step 1: 타입에서 좌측 바를 뺀다**

`FreeListItem` 에서 `barColor` 필드를 지우고 JSDoc 을 고친다:

```ts
  /** 타일 배경(아이콘 뒤 48px 사각). 좌측 4px 바는 §2-3 에서 제거됐다 — 2탭과 동형으로 맞추는 게
   *  목적이고, 위 격자와 섞여 보이는 문제는 격자가 색면을 갖게 되면서(§3) 사라졌다. */
  tileBg: string;
```

- [ ] **Step 2: 렌더에서 바 `<span>` 을 지운다**

```tsx
            className="relative w-full overflow-hidden rounded-2xl border border-lilac-mid/20 bg-white p-4 shadow-[0_2px_10px_rgba(159,138,208,0.08)] transition hover:border-lilac-deep/60 active:scale-[0.99]"
          >
            <div className="flex items-center gap-3.5">
```
(`pl-5` 를 `p-4` 로 되돌리고 바 `<span aria-hidden … />` 한 줄을 삭제)

- [ ] **Step 3: 오늘 사주를 목록 맨 앞에 넣는다**

`buildFreeItems` 의 배열 맨 앞에:

```ts
    {
      key: "saju_today",
      href: "/byeolmaru/saju",
      label: "오늘 사주",
      tagline: "오늘 일진으로 보는 하루 흐름 — 이름·등급·축 3종까지",
      hashtags: ["무료", "하루흐름"],
      tileBg: "linear-gradient(135deg, #FFF3D6 0%, #EFE6FA 100%)",
      // 🔴 오늘 사주가 목록으로 내려온다(P5 §3 의 "목록에 없다"를 뒤집는다) — 지금은 오늘 타로만
      //    목록이고 오늘 사주는 달력 안 타일이라 진입점 문법이 달랐다. 히어로 타일은 §2 에서 없앴다.
      chip: "무료 · 456자",
      chipTone: "lilac",
      image: "/icons/fortune/daily.webp",
    },
```

나머지 3종의 칩 문구를 §2-3 문법으로 맞춘다:
- 오늘 타로: `chip: drawn ? "오늘 뽑음" : "하루 1회 무료 · 389자"`
- 사주 MBTI: `chip: "무료"`
- 별 인연 지도: `chip: "무료"`

- [ ] **Step 4: 섹션 제목을 §2 문구로**

```tsx
        <h2 className="font-display text-base text-eye-purple">무료로 다 보는 것</h2>
```

- [ ] **Step 5: 게이트**

Run: `npx tsc --noEmit` → exit 0 (`barColor` 를 지웠으므로 `EmptyMonthShell` 쪽도 같이 컴파일되는지 확인)

- [ ] **Step 6: 커밋**

```bash
git add components/byeolmaru/FreeList.tsx
git commit -m "feat(byeolmaru): 무료 목록 4종 — 오늘 사주 편입·좌측 바 제거·칩 문법 통일 (P6-3 §2-3)"
```

---

### Task 11: 허브 재조립 (`ByeolmaruHub.tsx`) + 죽은 컴포넌트 삭제

**Files:**
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`
- Modify: `components/byeolmaru/AttendanceStrip.tsx`
- Modify: `lib/analytics/ui-events.ts`
- Delete: `components/byeolmaru/TodayHeroTile.tsx`·`CrossSellCard.tsx`·`lib/byeolmaru/crosssell.ts`·`lib/byeolmaru/crosssell.test.ts`

- [ ] **Step 1: 출석 스트립을 연속 방문만 남긴다**

`components/byeolmaru/AttendanceStrip.tsx` 전체를 교체:

```tsx
// components/byeolmaru/AttendanceStrip.tsx — 연속 방문만. 칸 채움 서사("이번 달 N칸 열림")는
// 월간 격자 접힘 버튼(MonthGridSection)이 가져갔다(스펙 §2-2) — 두 자리에 같은 문구를 두지 않는다.
// P5-2 부터 별 환급은 없다(보상은 그날의 운세다).
"use client";
import type { AttendanceState } from "@/lib/byeolmaru/attendance";

export default function AttendanceStrip({ attendance }: { attendance: AttendanceState | null }) {
  // 연속이 1일이면 "연속"이라 부를 게 없다 — 아예 안 그린다(빈 띠가 자리만 먹는 걸 막는다).
  if (!attendance || attendance.streak <= 1) return null;
  return (
    <p className="px-1 text-[12px] text-text-light">
      <span className="font-medium text-eye-purple">{attendance.streak}일 연속</span> 들르는 중이야
    </p>
  );
}
```

- [ ] **Step 2: 허브 본문을 재조립한다**

`ByeolmaruHub.tsx` 의 `return (…)` 블록(현행 225~312행)을 아래로 교체:

```tsx
  return (
    <main className="mx-auto w-full max-w-md space-y-4 p-4">
      <HubBanner />

      <PartnerChips
        partners={partners}
        selected={subject}
        onSelect={(id) => { if (id !== "me") trackUiEvent("byeolmaru_partner_selected"); setSubject(id); }}
        onAdd={() => setAddOpen(true)}
      />

      <AttendanceStrip attendance={attendance} />

      {viewingPair && !isPair ? (
        <p className="rounded-2xl bg-cream-warm p-4 text-center text-sm text-text-light">우리 달력을 펼치는 중…</p>
      ) : (
        <>
          <DayStrip
            cells={stripCells}
            lockedCells={stripLocked}
            todayDate={data.today}
            subjectKind={isPair ? "pair" : "me"}
            onSelect={(date) => openDay(date)}
            onLockedSelect={() => { if (data.trialUsed) openSubscribe(); else startTrial(); }}
          />
          {!data.entitled && (
            <p className="px-1 text-center text-[12px] text-text-light">
              앞으로 3일도 미리 볼래? · <button type="button" onClick={() => (data.trialUsed ? openSubscribe() : startTrial())} className="font-bold text-lilac-deep underline">3일 무료</button>
            </p>
          )}

          <MonthGridSection filledDays={filled}>
            <CalendarGrid
              cells={gridCells}
              lockedCells={gridLocked}
              todayDate={data.today}
              selectedDate={data.today}
              subjectKind={isPair ? "pair" : "me"}
              onSelect={(date) => openDay(date)}
            />
          </MonthGridSection>
        </>
      )}

      {/* 🔴 미끼는 자리마다 다른 물건이다(스펙 §9). 나 탭은 오늘 사주 리포트를, 인연 탭은 우리 오늘을
          판다. 스펙 §2 의 제거 목록에 없으므로 유지하고 자리만 격자 뒤로 옮겼다. */}
      {!data.entitled && (
        <PremiumBlock
          entitled={false}
          trialUsed={data.trialUsed}
          narrative={null}
          teaser={null}
          loading={false}
          onStartTrial={startTrial}
          onSubscribe={openSubscribe}
          slot={viewingPair ? "woori_30d" : "saju_report"}
          baitCtx={
            viewingPair
              ? { partnerName: partners.find((p) => p.id === subject)?.name }
              : { gradeLabel: todayCell.grade.label }
          }
        />
      )}

      <FreeList items={buildFreeItems(dailyCard)} />

      {subscribeModal}
      {addOpen && (
        <WatchAddModal
          onClose={() => setAddOpen(false)}
          onAdded={async (id) => { setAddOpen(false); await loadPartners(); setSubject(id); }}
        />
      )}
    </main>
  );
```

- [ ] **Step 3: 파생값과 핸들러를 추가한다**

`const gridLocked = …` 아래에 붙인다:

```tsx
  // 스트립 셀 — 나/우리 두 판정이 StripCell 로 수렴한다(격자의 GridCell 과 같은 패턴).
  // 제목은 나 탭이 "하루 이름"(DAY_NAME), 우리 탭이 톤 라벨이다 — 우리 탭엔 십신 개념이 없다.
  const stripSource = isPair ? pairStrip : data.strip;
  const stripCells: StripCell[] = isPair
    ? (stripSource?.cells as PairDayCell[] ?? []).map((c) => ({
        date: c.date, ganji: c.ganji, tone: c.tone, title: PAIR_TONE_LABEL[c.tone], marks: pairMarks(c.tags), isToday: c.isToday,
      }))
    : (stripSource?.cells as DayCell[] ?? []).map((c) => ({
        date: c.date, ganji: c.ganji, tone: c.grade.tone, title: DAY_NAME[c.tenGod], marks: c.marks, isToday: c.isToday,
      }));
  const stripLocked = stripSource?.lockedCells ?? [];

  // 날짜 탭의 목적지 — 격자와 스트립이 같은 규칙을 쓴다(둘이 갈리면 같은 날이 두 곳으로 간다).
  function openDay(date: string) {
    if (isPair) { router.push(`/byeolmaru/woori?subject=${encodeURIComponent(subject)}`); return; }
    router.push(date === data.today ? "/byeolmaru/saju" : `/byeolmaru/saju?date=${date}`);
  }
```

pair 응답의 strip 을 담을 상태를 기존 `pairLocked` 옆에 추가하고, pair effect 에서 채운다:

```tsx
  const [pairStrip, setPairStrip] = useState<{ cells: PairDayCell[]; lockedCells: LockedCell[] } | null>(null);
```
- effect 시작 리셋에 `setPairStrip(null);` 추가
- 실패 분기 3곳에 `setPairStrip(null);` 추가
- 성공 시 `setPairStrip(j.strip ?? null);`

`CalendarResponse` 타입에 추가:

```ts
  strip: { cells: DayCell[]; lockedCells: LockedCell[] };
```

- [ ] **Step 4: import 정리**

추가: `HubBanner`·`DayStrip`(+ `type StripCell`)·`MonthGridSection`·`DAY_NAME`(`@/lib/byeolmaru/day-label`)·`type LockedCell`
삭제: `TodayHeroTile`·`CrossSellCard`·`pickCrossSell`
`const crossSell = pickCrossSell(todayCell);` 줄도 지운다.

- [ ] **Step 5: 죽은 파일을 지운다**

```bash
git rm components/byeolmaru/TodayHeroTile.tsx components/byeolmaru/CrossSellCard.tsx lib/byeolmaru/crosssell.ts lib/byeolmaru/crosssell.test.ts
```

`lib/analytics/ui-events.ts` 에서 `"byeolmaru_crosssell_clicked"` 줄과 그 JSDoc 을 지운다(내 변경이 만든 orphan — 과거 행은 DB 에 남고 새 전송만 막힌다).

- [ ] **Step 6: 게이트**

Run: `grep -rn "TodayHeroTile\|CrossSellCard\|pickCrossSell\|crosssell" lib components app` → **출력 0줄**
Run: `node --import tsx --test "lib/**/*.test.ts"` → PASS
Run: `npx tsc --noEmit` → exit 0
Run: `npm run build` → exit 0 · 라우트 표에 `ƒ Proxy` 가 있는지 확인

- [ ] **Step 7: 커밋**

```bash
git add -A components/byeolmaru lib/analytics/ui-events.ts
git commit -m "feat(byeolmaru): 허브 재조립 — 배너/칩/스트립/접이식 격자/무료 4종, 히어로·크로스셀 제거 (P6-3 §2)"
```

---

### Task 12: `/byeolmaru/saju` 달력 중복 제거

**Files:**
- Modify: `components/byeolmaru/SajuTodayView.tsx`

- [ ] **Step 1: 격자와 그 배선을 걷어낸다**

- `<CalendarGrid … />` 블록(현행 142행 근방)을 삭제한다.
- `selfGridCells` 파생값을 삭제한다.
- `CalendarGrid`·`type GridCell` import 를 삭제한다.
- `selected` 상태는 **남긴다** — `?date=` 로 들어온 날을 고르는 데 계속 쓰인다(격자가 없어져도 허브·스트립이 그 파라미터를 실어 보낸다).
- 격자가 사라져 날짜를 바꿀 수단이 화면에서 없어지므로, 카드 위에 "← 허브에서 날짜를 고르면 그날 기록이 열려" 같은 안내를 **넣지 않는다**(P6-4 가 이 화면을 "그날의 기록"으로 다시 그린다 — 임시 문구를 넣으면 두 번 만든다).

- [ ] **Step 2: 게이트**

Run: `npx tsc --noEmit` → exit 0
Run: `npm run build` → exit 0

- [ ] **Step 3: 커밋**

```bash
git add components/byeolmaru/SajuTodayView.tsx
git commit -m "refactor(byeolmaru): 오늘 사주 상세에서 달력 중복 제거 — 허브가 소유한다 (P6-3 §2)"
```

---

### Task 12-b: 오늘 사주 공유 링크에 `utm` 을 붙인다 (스펙 §10-1①)

> 🔴 **2026-09-20 추가** (어드민 재설계 세션 발견 · 스펙 §10-1① 반영분). 기존 태스크 번호를 건드리지
> 않으려고 12-b 로 넣었다. Task 12 직후·Task 13(전체 게이트) 앞에서 실행한다.
>
> **왜** — 별마루는 공유 버튼과 전용 OG 까지 만들어놓고 `byeolmaru_share_clicked` 로 **보낸 것만** 센다.
> 도착·가입·결제를 셀 방법이 없다. 형제 상품은 둘 다 갖췄다(`lib/byeoljari/invite-link.ts` ·
> `lib/saju-mbti/share-url.ts`). 파라미터가 붙는 즉시 `page_views` 가 착지를 기록하고, 어드민의
> 공통 공유 퍼널(착지 → 가입 → 결제)이 네 상품을 같은 정의로 읽는다.
>
> ⚠️ **`DailyCardBlock.tsx`(오늘의 타로)는 이 플랜 스코프 밖이다** — 이 플랜 머리의 "열지 않는다" 목록에
> 들어 있다. 타로 쪽 같은 한 줄은 **P6-4** 가 처리한다(스펙 §10-1① 에 그렇게 적혀 있다).

**Files:**
- Modify: `components/byeolmaru/SajuTodayView.tsx`

- [ ] **Step 1: 공유 `link` 에 파라미터를 붙인다**

`handleShare` 의 `link` 가 지금 파라미터 0개다. `utm_source`·`utm_medium` 을 붙인다:

```ts
link: `${window.location.origin}/byeolmaru/saju?utm_source=byeolmaru_saju&utm_medium=share`,
```

- `utm_source` 값은 **`byeolmaru_saju`** 로 고정한다 — 어드민 공통 리졸버가 상품 키로 쓴다.
- `imageUrl`(OG 라우트)은 **건드리지 않는다**. 착지 URL 만 바뀐다.
- 착지 라우트가 파라미터를 읽을 필요는 없다 — `AuthBootstrap` 의 first-touch 캡처와 `/api/pv` 가 알아서 집는다.

- [ ] **Step 2: 게이트**

Run: `npx tsc --noEmit` → exit 0
Run: `npm run build` → exit 0

- [ ] **Step 3: 커밋**

```bash
git add components/byeolmaru/SajuTodayView.tsx
git commit -m "feat(byeolmaru): 오늘 사주 공유 링크에 utm — 바이럴 루프를 측정 가능하게 (P6-3 §10-1①)"
```

---

### Task 13: 전체 게이트 + 라이브 E2E

- [ ] **Step 1: 정적 게이트**

```bash
npx tsc --noEmit
node --import tsx --test "lib/**/*.test.ts"
npm run build
```
Expected: tsc exit 0 · 유닛 전건 PASS · build exit 0 + 라우트 표에 `ƒ Proxy`

- [ ] **Step 2: 잔재 확인**

```bash
grep -rn "lockedDates\|TodayHeroTile\|CrossSellCard\|pickCrossSell" lib components app scripts
```
Expected: 출력 없음

- [ ] **Step 3: 라이브 E2E (dev 서버 + 브라우저)**

dev QA 봇 계정(`11111111-…`, 구독 만료 2026-10-14)으로 확인한다. **비자격 상태도 반드시 함께** 본다.

| # | 확인 | 기대 |
|---|---|---|
| 1 | `/byeolmaru` 진입 | 배너 → 인연 칩 → (연속 2일↑면) 출석 한 줄 → 스트립 → 접힘 버튼 → 무료 4종 순서 |
| 2 | 스트립 | 칸이 정확히 7개 · 오늘이 4번째 · 오늘 칸만 2배 폭 |
| 3 | 비자격 | 미래 3칸이 점선 + 날짜·동물만 · 그 아래 "앞으로 3일도 미리 볼래?" 줄 |
| 4 | 구독자 | 미래 3칸도 열려 있고 CTA 줄이 없다 |
| 5 | 미래 칸 탭(비자격) | 체험/구독 모달 · `byeolmaru_strip_future_tapped` 전송 |
| 6 | 접힘 버튼 | 첫 방문은 펼쳐짐 → 접고 새로고침하면 접힌 채 시작 · `byeolmaru_month_grid_toggled` 전송 |
| 7 | 격자 셀 | 간지·동물 없음 · 챙길 날이 연보라로 무난한 날과 구분됨 · 마크가 2글자 컬러 라벨 |
| 8 | 인연 칩 → 상대 | 스트립·격자 둘 다 우리 버전으로 바뀜 · 한 명만 걸린 마크가 연하게 |
| 9 | 스트립/격자 날짜 탭 | 나 탭은 `/byeolmaru/saju?date=…`, 우리 탭은 `/byeolmaru/woori?subject=…` |
| 10 | `/byeolmaru/saju` | 달력이 없다 · `?date=` 로 들어온 날이 그대로 선택돼 있다 |
| 11 | 375px 폭 | 배너 텍스트와 캐릭터가 안 겹침 · 스트립 8트랙이 가로 스크롤 없이 들어감 |
| 12 | `/admin/errors` | 이 세션 동안 새 error 0건 |

- [ ] **Step 4: 문서·메모리 갱신**

- 스펙 §11 단계 분해 표의 **P6-3 행을 ✅ 완료**로 바꾸고, 구현하며 드러난 것이 있으면 `§11-2 P6-3 실행 중 발견 — P6-4 가 챙길 것` 절을 새로 만들어 적는다.
- 메모리 `byeolmaru-p6-progress` 의 실행 순서 표에서 4번 행을 완료로 바꾸고 다음을 P6-4 로 가리킨다.

- [ ] **Step 5: 푸시**

```bash
git push origin dev
```

🔴 **prod(main) 배포는 하지 않는다** — P6 전체 완성 후 사용자의 명시적 "배포하자"가 있을 때 한 판으로 나간다.

---

## 자가 리뷰

**1. 스펙 커버리지**

| 스펙 항목 | 태스크 |
|---|---|
| §2 배너 자리 | 9·11 |
| §2 히어로 타일 제거 · 크로스셀 삭제 | 11 |
| §2 `/byeolmaru/saju` 달력 중복 제거 | 12 |
| §2-1 롤링 7일 스트립 | 1·3·7·11 |
| §2-2 접이식 격자 · 첫 방문 펼침 · 셀 축소 | 6·8 |
| §2-3 무료 목록 4종 · 4px 바 제거 · 칩 문법 | 10 |
| §3 간지 제거 · 챙길 날 연보라 · 2글자 컬러 라벨 | 4·6 |
| §3 미래 칸 날짜·동물 선명 | 2·7 |
| §3-1-a V4 | 5 |
| §8-7 슬롯 120px | 9 |
| §10 계측(격자 펼침률 · 미래 칸 탭) | 7·8 |
| §10-1① 공유 링크 utm — **사주만** (타로는 P6-4) | 12-b |
| 사용자 결정(마크 2단 · 출석 연속만) | 5·11 |

**2. 플레이스홀더 스캔** — 없음. 코드가 필요한 스텝은 전부 실제 코드를 담았다.

**3. 타입 정합성**

- `LockedCell` = `{date, ganji}` — Task 2 에서 정의하고 3·6·7·11 이 같은 이름으로 쓴다.
- `DayMark.strength` — Task 4 에서 **필수**로 추가하고 같은 커밋에서 `pairMarks` 를 임시로 `"full"` 로 채워 tsc 를 초록으로 유지한 뒤, Task 5 가 실제 강도로 바꾼다.
- `StripCell` — Task 7 에서 정의하고 Task 11 이 import 한다.
- `PairDayTags` 에 `*Both` 3필드 추가는 **가산**이라 `narrative-prompt.ts`·`static-lines.ts` 의 기존 `tags.spark`·`tags.bond`·`tags.friction` 사용은 그대로 컴파일된다(무회귀).
- `AttendanceStrip` 이 `filledDays`·`entitled` prop 을 잃으므로 호출부(허브)도 Task 11 에서 같이 바뀐다 — 다른 호출부는 없다(`grep -rn "AttendanceStrip"` 로 확인할 것).

**4. 열린 것 (P6-4 로 넘긴다)**

- 스트립·격자에서 **미래 날짜를 탭한 구독자**는 지금 `/byeolmaru/saju?date=…` 로 가고, 그 화면의 미래 리포트 생성은 §4(P6-4) 소유다. 이 플랜은 링크만 맞춰두고 도착지는 손대지 않는다.
- §10 의 "리포트 생성 대기 이탈" 계측은 대기 UX(§4, P6-4)와 같이 들어가야 의미가 있어 여기서 넣지 않는다.
