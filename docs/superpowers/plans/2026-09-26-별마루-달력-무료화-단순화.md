# 별마루 달력 전면 무료화 + 판 단순화 (1단계) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 허브 달력에서 무료선과 7일 스트립을 걷어내고, 한 달 격자 하나가 판을 지게 만든다.

**Architecture:** 무료선(`splitByFreeLine`)을 제거해 달력 칸을 전원 공개하고, 리포트 생성 범위를 오늘까지로 좁혀 `FUTURE_REPORT_DAYS`에 묶여 있던 스트립 계약을 끊는다. 그 다음 스트립·접힘 래퍼를 삭제하고 격자의 시각을 다듬는다. 순수 lib → 라우트 → 컴포넌트 순으로 내려가며, 각 태스크는 그 자체로 빌드가 서고 화면이 돈다.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript strict · Tailwind v4 · `node:test` + `tsx`

**스펙:** [`docs/superpowers/specs/2026-09-26-별마루-달력-무료화-단순화-design.md`](../specs/2026-09-26-별마루-달력-무료화-단순화-design.md)

**테스트 실행 (이 저장소엔 `npm test` 스크립트가 없다):**

```bash
node --import tsx --test lib/byeolmaru/calendar.test.ts
```

전량:

```bash
node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")
```

---

## File Structure

| 파일 | 이 플랜에서의 책임 |
|---|---|
| `lib/byeolmaru/calendar.ts` | **수정** — `splitByFreeLine` 삭제 · `buildCalendarPayload` 가 `{cells, fillCells, weeks}` 반환 · `gridRange` 신설 |
| `lib/byeolmaru/calendar.test.ts` | **수정** — 무료선 테스트 3건 제거 · `gridRange` 계약 추가 |
| `lib/byeolmaru/free-line.test.ts` | **삭제** → `lib/byeolmaru/calendar-open.test.ts` 로 대체(전면 무료 계약) |
| `lib/byeolmaru/strip.ts` · `strip.test.ts` | **삭제** |
| `lib/byeolmaru/report-date.ts` · `report-date.test.ts` | **수정** — `FUTURE_REPORT_DAYS` 3→0 |
| `lib/byeolmaru/calendar-visual.ts` · `calendar-visual.test.ts` | **수정** — 보라 알파 상한 0.30→0.45 |
| `lib/fortune/prompt.ts` | **수정** — `dailyDateContextLine` + 호출부 삭제 |
| `app/api/byeolmaru/calendar/route.ts` | **수정** — 무료선·스트립 제거 · 격자 범위를 `gridRange` 로 |
| `components/byeolmaru/CalendarGrid.tsx` | **수정** — 월 표시 · `fillCells` · "점" 제거 · 날짜 가독성 · `lockedHint` 삭제 |
| `components/byeolmaru/DayStrip.tsx` · `MonthGridSection.tsx` | **삭제** |
| `components/byeolmaru/ByeolmaruHub.tsx` | **수정** — 판 재구성 |
| `components/byeolmaru/SajuTodayView.tsx` | **수정** — `entitled` 분기 2곳 · `outOfRange` 문구 |

---

## Task 1: 무료선 폐지 — 순수 lib

**Files:**
- Modify: `lib/byeolmaru/calendar.ts`
- Modify: `lib/byeolmaru/calendar.test.ts`
- Delete: `lib/byeolmaru/free-line.test.ts`
- Create: `lib/byeolmaru/calendar-open.test.ts`

- [ ] **Step 1: 새 계약 테스트를 먼저 쓴다**

`lib/byeolmaru/calendar-open.test.ts` 를 새로 만든다. (옛 `free-line.test.ts` 를 대체한다 — 이름이 곧 계약이라, 없어진 개념을 파일명에 남기지 않는다.)

```ts
// 전면 무료 계약 — 라우트(app/api/byeolmaru/calendar/route.ts)가 실제로 호출하는 함수를 그대로
// 불러 "자격과 무관하게 이번 달 전체가 나온다"를 코드 경로에 묶는다.
// 🔴 옛 free-line.test.ts(무료선 = 비자격자에게 미래를 안 싣는다)를 대체한다. 그 개념은
//    2026-09-26 에 폐지됐다 — 달력 칸은 룰 계산이라 변동비 0인데 잠겨 있었다.
import { test } from "node:test";
import assert from "node:assert/strict";
import { calcSaju, calcDailyLuckRange } from "@/lib/saju/calc";
import { buildCalendarPayload, monthRange } from "./calendar.ts";
import { buildPairCalendar } from "./pair-day.ts";

const TODAY = "2026-09-13";
const saju = calcSaju({ year: 1994, month: 5, day: 17, hour: 14, gender: "other", isLunar: false });
const partnerSaju = calcSaju({ year: 1992, month: 11, day: 3, hour: 21, gender: "other", isLunar: false });

function build() {
  const { start, end } = monthRange(TODAY);
  return buildCalendarPayload(saju, calcDailyLuckRange(start, end), TODAY);
}

test("이번 달 달력은 1일부터 말일까지 전부 계산된다", () => {
  const { cells } = build();
  assert.equal(cells.length, 30, "2026-09 는 30일");
  assert.equal(cells[0].date, "2026-09-01");
  assert.equal(cells[29].date, "2026-09-30");
  assert.equal(cells.filter((c) => c.isToday).length, 1);
});

test("미래 날짜도 판정이 실린다(전면 무료 — 잠긴 칸 개념이 없다)", () => {
  const { cells } = build();
  const future = cells.filter((c) => c.date > TODAY);
  assert.equal(future.length, 17, "9/14~9/30");
  assert.ok(future.every((c) => typeof c.score === "number" && c.grade != null));
});

test("주차 요약도 이번 달 전체를 집계한다", () => {
  const { weeks } = build();
  assert.equal(weeks[0].startDate, "2026-09-01");
  assert.equal(weeks[weeks.length - 1].endDate, "2026-09-30");
});

test("[우리] pair 달력도 이번 달 전부가 판정과 함께 나온다", () => {
  const { start, end } = monthRange(TODAY);
  const all = buildPairCalendar(saju, partnerSaju, calcDailyLuckRange(start, end), TODAY);
  assert.equal(all.length, 30);
  assert.ok(all.every((c) => c.tone != null));
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
node --import tsx --test lib/byeolmaru/calendar-open.test.ts
```

Expected: FAIL — `buildCalendarPayload` 가 아직 4번째 인자 `entitled` 를 요구하고 `cells` 가 13개만 나온다.

- [ ] **Step 3: `splitByFreeLine` 을 삭제하고 `buildCalendarPayload` 를 고친다**

`lib/byeolmaru/calendar.ts` 에서 `splitByFreeLine` 함수와 그 위 주석 블록을 통째로 지운다. `LockedCell` 인터페이스는 **남긴다** — `EmptyMonthShell`(비로그인·생일 미입력)이 "전부 잠긴 격자"를 그리는 데 계속 쓴다.

`LockedCell` 주석을 새 용도에 맞게 바꾼다:

```ts
/** 판정 없이 날짜만 있는 칸. 🔴 남은 용도는 **게스트 셸 하나**다 —
 *  비로그인·생일 미입력이 보는 "안 칠해진 이번 달"(ByeolmaruHub 의 EmptyMonthShell).
 *  거기서의 "잠김"은 "안 온 날"이 아니라 "생일이 없어서 못 보는 날"이다.
 *  🔴 무료선(비자격자에게 미래를 안 싣던 splitByFreeLine)은 2026-09-26 에 폐지됐다 —
 *     달력 칸은 룰 계산이라 변동비가 0인데 잠겨 있었고, 그 잠금은 누를 수도 없었고
 *     구독을 말하지도 않았다. 되살리기 전에 스펙 2026-09-26 §2 의 기각안을 읽을 것. */
export interface LockedCell {
  date: string;
  ganji: string;
}
```

파일 맨 위 헤더 주석에서 무료선 문장을 지운다:

```ts
// 별마루 이번 달(1일~말일) 캘린더 조립 — 순수. dailyLuck(tyme4ts 결정론) × 내 사주 → 날짜별 셀 + 주차 버킷.
// 오늘 판정은 인자로 받은 KST 날짜로만 한다(서버 TZ 에 좌우되지 않게 — 라우트가 계산해 넘긴다).
// 🔴 달력은 **전면 무료**다(2026-09-26). 자격에 따라 갈리는 건 리포트 글뿐이고, 칸의 판정
//    (점수·등급·마크·축)은 룰 계산이라 누구에게나 나간다.
```

`buildCalendarPayload` 를 교체한다:

```ts
/**
 * 나(self) 캘린더 라우트가 응답에 그대로 실어 보내는 조각 — build → 이번 달/채움 분리 →
 * 주차 집계를 하나로 묶는다.
 *
 * 🔴 `cells`(이번 달)와 `fillCells`(앞뒤 채움)를 **필드로 가른다.** 섞어 보내면 weekBuckets 가
 *    조용히 다른 달 날짜를 세서 "이번 달 잘 맞는 날 N일"이 거짓이 된다. 소비처가 매번 월
 *    접두사를 비교해 거르는 규약은 한 곳만 빠뜨려도 틀리므로 경계를 필드로 굳힌다.
 *
 * @param gridLuck gridRange(todayKst) 범위로 계산한 일진 — 이번 달 + 앞뒤 채움을 모두 덮는다.
 */
export function buildCalendarPayload(
  saju: SajuResult,
  gridLuck: DailyLuck[],
  todayKst: string
): { cells: DayCell[]; fillCells: DayCell[]; weeks: WeekBucket[] } {
  const { start, end } = monthRange(todayKst);
  const all = buildCalendar(saju, gridLuck, todayKst);
  const cells = all.filter((c) => c.date >= start && c.date <= end);
  const fillCells = all.filter((c) => c.date < start || c.date > end);
  return { cells, fillCells, weeks: weekBuckets(cells) };
}
```

- [ ] **Step 4: `calendar.test.ts` 에서 무료선 테스트를 걷어낸다**

`lib/byeolmaru/calendar.test.ts` 3행의 import 에서 `splitByFreeLine` 을 뺀다:

```ts
import { buildCalendar, weekBuckets, toDaySelf, monthRange } from "./calendar.ts";
```

그리고 아래 세 개 `test(...)` 블록을 통째로 지운다:

- `"splitByFreeLine — 비자격은 오늘까지만 열리고 나머지는 날짜·간지만 남는다"`
- `"splitByFreeLine — 자격자는 전부 열리고 잠긴 날이 없다"`
- `"splitByFreeLine — 오늘은 언제나 열린 쪽(경계 off-by-one 고정)"`

- [ ] **Step 5: 옛 테스트 파일을 지운다**

```bash
git rm lib/byeolmaru/free-line.test.ts
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
node --import tsx --test lib/byeolmaru/calendar.test.ts lib/byeolmaru/calendar-open.test.ts
```

Expected: PASS (`calendar-open.test.ts` 4건 + `calendar.test.ts` 남은 것 전부).

> ⚠️ 이 시점에 라우트는 아직 안 고쳤으므로 `tsc` 는 `buildCalendarPayload` 인자 개수로 실패한다. Task 2에서 붙인다. **여기서 커밋하지 않는다.**

---

## Task 2: 무료선 폐지 — 라우트 + 화면 배선

**Files:**
- Modify: `app/api/byeolmaru/calendar/route.ts`
- Modify: `components/byeolmaru/CalendarGrid.tsx`
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`
- Modify: `components/byeolmaru/SajuTodayView.tsx`
- Modify: `components/byeolmaru/WooriTodayView.tsx`

- [ ] **Step 1: 라우트 self 분기**

`app/api/byeolmaru/calendar/route.ts` — import 에서 `splitByFreeLine` 을 뺀다. self 분기의 payload 호출을 교체한다:

```ts
    // 🔴 무료선은 2026-09-26 에 폐지됐다 — 달력 칸은 룰 계산(변동비 0)이라 자격과 무관하게 전부
    //    실어 보낸다. 자격이 가르는 건 리포트 글(daily-report 라우트)뿐이다.
    const { cells, fillCells, weeks } = buildCalendarPayload(saju, monthLuck, todayKst);
```

> 🔴 이 태스크에서는 일진 계산 변수가 아직 `monthLuck`(= `monthRange` 범위)이다. Task 8에서 `gridLuck`(= `gridRange` 범위)으로 넓히면서 이름을 바꾼다. **지금 `gridLuck` 을 쓰면 정의되지 않은 변수다.**
> 그래서 이 시점의 `fillCells` 는 **항상 빈 배열**이다 — 필드만 먼저 나가고 Task 8에서 채워진다.

응답에서 `lockedCells` 를 빼고 `fillCells` 를 넣는다:

```ts
      cells,
      fillCells,
      weeks,
```

- [ ] **Step 2: 라우트 pair 분기**

pair 분기에서 `splitByFreeLine` 두 번 호출을 지우고 직접 쓴다:

```ts
      const pairCells = buildPairCalendar(saju, partnerSaju, monthLuck, todayKst);
```

응답에서 `cells: open` → `cells: pairCells`, `lockedCells` 줄 삭제.

> `ent` 는 여전히 필요하다 — 응답의 `entitled` 필드가 `PaywallCut` 분기를 가른다. **지우지 말 것.**

- [ ] **Step 3: `CalendarGrid` 에서 잠김 안내와 `lockedHint` 제거**

`components/byeolmaru/CalendarGrid.tsx`:

`lockedHint` prop 을 인터페이스와 구조분해에서 지우고, 파일 맨 아래 안내 문구 블록을 지운다:

```tsx
      {/* 🔴 "점선 칸은 아직 안 온 날이야 — 그날이 오면 열려" 안내는 2026-09-26 에 제거됐다.
          달력이 전면 무료가 되면서 로그인 유저에겐 잠긴 칸이 아예 없고, 남은 유일한 호출부
          (EmptyMonthShell)의 잠김은 "안 온 날"이 아니라 "생일이 없어서 못 보는 날"이라
          그 문구가 틀린 말이었다(바로 아래 "네 생일만 있으면…" 이 정확한 설명이다). */}
```

`lockedCells` prop 에 기본값을 준다 — 이제 대부분의 호출부가 안 넘긴다:

```tsx
  /** 판정 없이 날짜만 있는 칸. 🔴 남은 호출부는 **게스트 셸 하나**다(ByeolmaruHub 의
   *  EmptyMonthShell). 로그인 유저의 달력엔 잠긴 칸이 없다 — 달력이 전면 무료다. */
  lockedCells?: LockedCell[];
```

```tsx
  lockedCells = [],
```

- [ ] **Step 4: 허브 배선**

`components/byeolmaru/ByeolmaruHub.tsx` — `CalendarResponse` 에서 `lockedCells` 를 빼고 `fillCells` 를 넣는다:

```ts
  cells: DayCell[];
  fillCells: DayCell[];
  weeks: WeekBucket[];
```

격자 호출에서 `lockedCells={data.lockedCells}` 줄을 지운다. `EmptyMonthShell` 의 `<CalendarGrid ... lockedHint={false} />` 에서 `lockedHint={false}` 만 지운다(`lockedCells` 는 그대로 둔다).

- [ ] **Step 5: 자격 분기 문구 2곳을 고정 문구로**

`components/byeolmaru/SajuTodayView.tsx` — 달력이 전면 무료라 두 분기가 항상 같은 값이 된다:

```tsx
        <p className="text-center text-[13px] text-text-light">
          이번 달, <span className="font-bold text-eye-purple">잘 맞는 날 {good}일</span> ✨
        </p>
```

```tsx
        <h2 className="mb-2 font-display text-base text-eye-purple">이번 달 흐름</h2>
```

그리고 그 아래 `weeks.map` 안의 주석 *"무료선(비자격)은 cells 가 … 길이가 7k+1 인 날"* 을 고친다 — 이제 `cells` 는 항상 이번 달 전체라 마지막 버킷이 하루짜리가 되는 건 **말일이 7의 배수+1일 때**다:

```tsx
            // cells 가 이번 달 전체(28~31일)라 마지막 버킷은 1~7일짜리다 — 29일인 달이면
            // 마지막이 하루뿐이라 시작일=종료일이 된다. 그때만 날짜를 한 번 찍는다.
```

`components/byeolmaru/WooriTodayView.tsx` — 응답 타입에서 `lockedCells` 참조가 있으면 같이 지운다.

- [ ] **Step 6: 타입·빌드 확인**

```bash
npx tsc --noEmit
```

Expected: 에러 0.

```bash
node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")
```

Expected: 전량 PASS.

- [ ] **Step 7: 커밋**

```bash
git add lib/byeolmaru/calendar.ts lib/byeolmaru/calendar.test.ts lib/byeolmaru/calendar-open.test.ts lib/byeolmaru/free-line.test.ts app/api/byeolmaru/calendar/route.ts components/byeolmaru/CalendarGrid.tsx components/byeolmaru/ByeolmaruHub.tsx components/byeolmaru/SajuTodayView.tsx components/byeolmaru/WooriTodayView.tsx
git commit -m "$(cat <<'EOF'
feat(byeolmaru): 달력 전면 무료 — 무료선(splitByFreeLine) 폐지

달력 칸은 일간10×일진60=600 으로 유한한 룰 계산이라 변동비가 0인데
잠겨 있었다. 게다가 그 잠금은 누를 수도 없고(div) 하단 문구가
"그날이 오면 열려"라 구독을 말하지도 않았다. 원설계("무료 = 30일
등급 조회 전부")로 복귀한다.

- splitByFreeLine 삭제, buildCalendarPayload 가 {cells, fillCells, weeks}
- LockedCell 과 격자의 잠긴 칸 렌더는 유지 — EmptyMonthShell(게스트)이 쓴다
- free-line.test.ts → calendar-open.test.ts (전면 무료 계약)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

> `fillCells` 는 아직 격자가 안 그린다(Task 8). 라우트가 내려보내기만 하고 소비처가 없는 상태이고, 빌드·타입은 선다.

---

## Task 3: 리포트는 오늘까지

**Files:**
- Modify: `lib/byeolmaru/report-date.ts`
- Modify: `lib/byeolmaru/report-date.test.ts`
- Modify: `lib/fortune/prompt.ts`
- Modify: `components/byeolmaru/SajuTodayView.tsx:218-224`

- [ ] **Step 1: 테스트를 먼저 고친다**

`lib/byeolmaru/report-date.test.ts` — 두 테스트를 교체한다:

```ts
test("reportDatePolicy: 과거는 캐시만, 오늘만 생성, 내일부터는 거부", () => {
  assert.equal(reportDatePolicy("2026-09-18", T), "cache_only");
  assert.equal(reportDatePolicy("2026-08-01", T), "cache_only");
  assert.equal(reportDatePolicy(T, T), "generate");
  assert.equal(reportDatePolicy("2026-09-20", T), "out_of_range"); // 내일
  assert.equal(reportDatePolicy("2026-09-22", T), "out_of_range");
  assert.equal(reportDatePolicy("nope", T), "out_of_range");
});

test("reportDatePolicy: 월 경계를 건너도 일수로 센다", () => {
  assert.equal(reportDatePolicy("2026-09-30", "2026-09-30"), "generate");
  assert.equal(reportDatePolicy("2026-10-01", "2026-09-30"), "out_of_range"); // +1
  assert.equal(reportDatePolicy("2026-09-29", "2026-09-30"), "cache_only"); // -1
});
```

마지막 테스트를 교체한다:

```ts
// 🔴 3 으로 되돌리려면 그 3일을 **설명하는 표면**을 같이 만들어야 한다 — 예전엔 스트립의
//    점선 칸 3개가 그 역할이었고, 그 스트립이 2026-09-26 에 삭제됐다. 규칙만 남고 설명이
//    사라지는 게 제일 나쁜 조합이다(스펙 2026-09-26 §1-④).
test("FUTURE_REPORT_DAYS 는 0 — 리포트는 오늘 것만 생성한다", () => {
  assert.equal(FUTURE_REPORT_DAYS, 0);
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --import tsx --test lib/byeolmaru/report-date.test.ts
```

Expected: FAIL — `AssertionError: 3 !== 0` 외 2건.

- [ ] **Step 3: 상수와 헤더 주석을 고친다**

`lib/byeolmaru/report-date.ts` 상단 주석과 상수:

```ts
// lib/byeolmaru/report-date.ts — 리포트 대상 날짜 정책(순수 · 네트워크 0).
// 과거는 "있던 것만"(소급 생성 금지 — 그때 받은 글이 아니면 기록이 아니다), 생성은 오늘만.
// 🔴 미래 3일 생성은 2026-09-26 에 폐지됐다. 그건 P6 스펙이 "덤이지 파는 물건이 아니다"라고
//    적은 것이고, 그 3일을 설명하는 유일한 UI(허브 스트립의 점선 칸 3개)가 같은 날 삭제됐다.

/** 미래 리포트를 만들어주는 일수. 0 = 오늘만. */
export const FUTURE_REPORT_DAYS = 0;
```

`reportDatePolicy` 본문은 **그대로 둔다** — `d <= 0` 이 되어 자연히 오늘만 통과한다.

- [ ] **Step 4: 통과 확인**

```bash
node --import tsx --test lib/byeolmaru/report-date.test.ts
```

Expected: PASS.

- [ ] **Step 5: 도달 불가가 된 시제 분기를 지운다**

`lib/fortune/prompt.ts` 에서 `dailyDateContextLine` 함수와 그 위 주석 블록을 통째로 지운다.

그 다음 `buildFortuneSystem` 안에서 이 함수를 부르는 "대상 날짜 모드" 분기를 찾아 지운다:

```bash
grep -n "dailyDateContextLine" lib/fortune/prompt.ts
```

호출부는 `reportDate`·`todayKst` 가 **둘 다** 있을 때만 켜지므로, 지워도 `buildFortuneSystem` 의 다른 사용처(`/api/fortune/create` 등)는 영향이 없다. 지운 자리에 근거를 남긴다:

```ts
  // 🔴 dailyDateContextLine(대상 날짜 시제 지시)은 2026-09-26 에 삭제됐다 — 과거 분기는
  //    reportDatePolicy 의 cache_only 때문에 이미 도달 불가였고, FUTURE_REPORT_DAYS 가 0 이
  //    되면서 미래 분기도 사라져 함수 전체가 죽었다. 되살릴 일이 생긴다면(소급 생성 또는 미래
  //    생성 재개) SECTION_GUIDE.daily 의 미래형 동사(벌어질·할 선택·건넬 말)도 같이 볼 것 —
  //    그 동사 선택의 근거가 "생성되는 건 앞으로 1~3일뿐"이었다.
```

> `{{DAY_WORD}}` 토큰과 `dayWordFor` 는 **남긴다** — 과거 리포트를 *읽을* 때 `dayWord="그날"` 로 렌더되고, 화면 문구("그날은 카드를 안 뽑았어")도 계속 쓴다.

- [ ] **Step 6: `outOfRange` 문구 교체**

`components/byeolmaru/SajuTodayView.tsx` — 보간이 `0`이 되면 문장이 깨지므로 고정 문구로 바꾸고 `FUTURE_REPORT_DAYS` import 를 지운다:

```tsx
          ) : outOfRange ? (
            <p className="mt-4 border-t border-lilac-mid/20 pt-4 text-center text-sm text-text-light">
              그날 이야기는 그날 아침에 들려줄게.
            </p>
```

import 줄:

```tsx
import { dayWordFor, reportDatePolicy } from "@/lib/byeolmaru/report-date";
```

- [ ] **Step 7: 타입·테스트·빌드**

```bash
npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")
```

Expected: 에러 0, 전량 PASS.

> ⚠️ `strip.test.ts` 는 `STRIP_LENGTH === STRIP_PAST_DAYS + 1 + FUTURE_REPORT_DAYS` 를 검증한다. `FUTURE_REPORT_DAYS = 0` 이면 `STRIP_LENGTH` 가 4가 되므로 `"길이는 지난 3 + 오늘 + 미래 3 = 7"` 테스트가 **깨진다.** 이건 정상이다 — Task 4에서 파일째 삭제한다. 이 태스크에서 커밋하려면 Task 4를 먼저 하거나 두 태스크를 한 커밋으로 묶는다. **권장: Task 4까지 마치고 커밋한다.**

---

## Task 4: 스트립 삭제

**Files:**
- Delete: `lib/byeolmaru/strip.ts`, `lib/byeolmaru/strip.test.ts`
- Delete: `components/byeolmaru/DayStrip.tsx`, `components/byeolmaru/MonthGridSection.tsx`
- Modify: `app/api/byeolmaru/calendar/route.ts`
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`
- Modify: `lib/analytics/ui-events.ts`

- [ ] **Step 1: lib·컴포넌트 삭제**

```bash
git rm lib/byeolmaru/strip.ts lib/byeolmaru/strip.test.ts components/byeolmaru/DayStrip.tsx components/byeolmaru/MonthGridSection.tsx
```

- [ ] **Step 2: 라우트에서 스트립 계산·payload 제거**

`app/api/byeolmaru/calendar/route.ts`:

- `import { stripRange } from "@/lib/byeolmaru/strip";` 삭제
- `const { start: stripStart, end: stripEnd } = stripRange(todayKst);` 와 `const stripLuck = calcDailyLuckRange(stripStart, stripEnd);` 삭제
- pair 분기의 `pairStrip` 계산과 응답의 `strip: {...}` 삭제
- self 분기의 `stripSplit` 계산과 응답의 `strip: {...}` 삭제

`monthLuck` 계산 자리에 근거를 남긴다:

```ts
    // 🔴 스트립(오늘 중심 7일)은 2026-09-26 에 삭제됐다 — 09-24 에 마크 칩·동물을 빼면서
    //    격자와 같은 그림이 됐고(실측: 같은 날이 같은 배경색으로 두 번), 남은 역할이었던
    //    "페이월 경계 고정"도 달력이 전면 무료가 되면서 사라졌다. 월 경계는 격자의 앞뒤 달
    //    채움(gridRange)이 대신한다.
```

- [ ] **Step 3: 허브에서 스트립·접힘 제거**

`components/byeolmaru/ByeolmaruHub.tsx`:

import 2줄 삭제:

```tsx
import DayStrip, { type StripCell } from "./DayStrip";
import MonthGridSection from "./MonthGridSection";
```

`CalendarResponse` 에서 `strip` 필드 삭제. `stripCells`·`stripLocked`·`todayCell` 조립부를 교체한다 — `TodayLead` 가 오늘의 이름·점수를 써야 하므로 `todayCell` 은 **격자 셀에서** 찾는다:

```tsx
  // 🔴 오늘 칸은 격자에서 찾는다(예전엔 스트립 셀 집합에서 찾았다). 두 집합이 갈릴 일이
  //    없어졌으므로 원천은 하나다.
  const todayCell = data.cells.find((c) => c.isToday) ?? null;
```

`TodayLead` 호출의 `todayName` 을 격자 셀 기준으로 고친다:

```tsx
            <TodayLead
              todayName={todayCell ? DAY_NAME[todayCell.tenGod] : null}
              todayScore={todayCell ? scoreDisplay(todayCell.score) : null}
              attendance={attendance}
            />
```

판 내부를 교체한다 — `DayStrip`, 구분선, `MonthGridSection` 을 걷어내고 격자를 직접 넣는다:

```tsx
        {/* 🔴 p-3 은 칸 폭 계산의 일부다 — main p-4(32) → 343 / 판 p-3(24) → 319 /
            gap 2px × 6 = 12 → (319 − 12) / 7 = 43.9px. p-4 로 되돌리면 격자 칸이 41px 로 돌아간다. */}
        <section className="rounded-2xl p-3" style={{ background: PANEL_BG, border: PANEL_BORDER, boxShadow: PANEL_SHADOW }}>
          {/* 🔴 판 안 구분선이 없다 — 그 선은 스트립과 격자를 가르던 것이고 둘 중 하나가
              사라졌다. 판 안에 남은 층은 리드 줄 + 격자 둘뿐이다. */}
          <div className="space-y-2">
            <TodayLead
              todayName={todayCell ? DAY_NAME[todayCell.tenGod] : null}
              todayScore={todayCell ? scoreDisplay(todayCell.score) : null}
              attendance={attendance}
            />
            {/* 🔴 접이식(MonthGridSection)이 사라졌다 — 그 래퍼의 존재 이유는 "접힘 클릭률로
                폐지 여부를 답 얻는다"였는데 별마루가 prod 에 안 나가 그 실험은 시작된 적이
                없고, seen 플래그가 **토글했을 때만** 심어져서 한 번도 안 건드린 사람에겐
                애초에 계속 펼쳐져 있었다. 즉 항상 펼침은 다수에게 현상 유지다. */}
            <CalendarGrid
              cells={gridCells}
              todayDate={data.today}
              selectedDate={data.today}
              onSelect={openDay}
              panel={false}
            />
          </div>
        </section>
```

- [ ] **Step 4: 죽은 계측에 근거를 남긴다**

`lib/analytics/ui-events.ts` 에서 `byeolmaru_strip_future_tapped` 항목을 찾아 주석으로 사망 기록을 남긴다(상수는 저장소 관례대로 남긴다 — 09-24 에도 같은 방식이었다):

```ts
  /** 🪦 영구 0 (2026-09-26) — 스트립 삭제. 이 이벤트가 재려던 질문("앞으로 3일이 체험 전환을
   *  만드는가")은 별마루가 prod 에 안 나가 표본 0 인 채로 끝났다. `byeolmaru_month_grid_toggled`
   *  도 같은 날 같은 이유로 죽었다(접이식 래퍼 삭제). */
```

`byeolmaru_day_selected` 의 `surface` 축 주석에서 `"strip"` 을 뺀다 — 이제 `"grid"` 하나다.

- [ ] **Step 5: 타입·테스트·빌드**

```bash
npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*")
```

Expected: 에러 0, 전량 PASS(`strip.test.ts` 는 삭제됐다).

```bash
npm run build
```

Expected: exit 0, 라우트 표에 `ƒ Proxy` 가 그대로 있다.

- [ ] **Step 6: 커밋**

```bash
git add -u && git add lib/byeolmaru/report-date.ts
git commit -m "$(cat <<'EOF'
feat(byeolmaru): 리포트는 오늘까지 + 7일 스트립 삭제

스트립은 09-24 에 마크 칩·동물을 빼면서 격자와 같은 그림이 됐다
(실측: 9/23~9/29 가 같은 배경색으로 두 번 그려짐). 남은 역할이었던
"페이월 경계 고정"(흐린 칸 항상 3개)도 달력 전면 무료로 사라졌다.

- FUTURE_REPORT_DAYS 3→0. P6 스펙이 "덤이지 파는 물건이 아니다"라고
  적은 것이고, 그 3일을 설명하던 유일한 UI 가 이 커밋에서 사라진다.
- 도달 불가가 된 dailyDateContextLine(시제 분기) + 호출부 삭제
- strip.ts · DayStrip · MonthGridSection 삭제 (격자는 항상 펼침)
- 죽은 계측 2종은 상수를 남기고 주석에 근거 기록

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 시각 ② — 면 색 계조 넓히기

**Files:**
- Modify: `lib/byeolmaru/calendar-visual.ts`
- Modify: `lib/byeolmaru/calendar-visual.test.ts`

- [ ] **Step 1: 새 계약 테스트를 쓴다**

`lib/byeolmaru/calendar-visual.test.ts` 의 `"cellTint — 알파 상한이 낮아졌다(숫자 대비 보호)"` 테스트를 교체한다:

```ts
// 🔴 보라 상한을 0.30 → 0.45 로 올렸다(2026-09-26). 실측에서 보라 칸 전체가 0.14~0.29 안에
//    들어 3점과 64점이 구분되지 않았다 — 스펙은 "양방향 채도"라고 적었으나 실현은 단방향
//    (금색만 튐)이었고 "살짝 챙길 날"이 화면에서 사라졌다.
//    대비: #5A3E8C on 보라 0.45 = 5.38 (AA 4.5 통과). 09-24 에 상한을 낮춘 이유였던
//    "진한 면 위에서 숫자 대비가 깎인다"는 이 계산으로 해소된다 — 되돌리려면 다시 계산할 것.
test("cellTint — 보라 상한 0.45 · 금색 상한 0.65", () => {
  for (let s = 0; s <= 100; s++) {
    const css = cellTint(s);
    const a = alphaOf(css);
    const cap = css.startsWith("rgba(232") ? 0.65 : 0.45;
    assert.ok(a <= cap, `score ${s} alpha ${a} cap ${cap}`);
  }
});

test("cellTint — 제일 나쁜 날은 보라 상한에 닿는다(계조를 다 쓴다)", () => {
  assert.equal(alphaOf(cellTint(0)), 0.45);
});
```

`"같은 한 주의 알파도 벌어진다"` 테스트의 임계를 올린다 — 계조가 넓어졌으니 스프레드도 커져야 한다:

```ts
test("cellTint — 같은 한 주의 알파가 뚜렷하게 벌어진다", () => {
  const week = [25, 51, 56, 60, 61, 69].map((s) => alphaOf(cellTint(s)));
  assert.ok(Math.max(...week) - Math.min(...week) >= 0.28, week.join(","));
});
```

- [ ] **Step 2: 실패 확인**

```bash
node --import tsx --test lib/byeolmaru/calendar-visual.test.ts
```

Expected: FAIL — `cellTint(0)` 이 `0.3` 이고 주간 스프레드가 `0.178`.

- [ ] **Step 3: `cellTint` 보라 구간을 고친다**

`lib/byeolmaru/calendar-visual.ts`:

```ts
  // 🔴 바닥 0.11 은 good 임계 직전(p = GOOD_PERCENTILE)에서 정확히 나온다 — 0.45 − 0.34.
  //    바닥은 판(PANEL_BG #ffffff) 위에서 칸이 사라지지 않는 하한이라 **옮기지 않는다**.
  const t = Math.max(0, Math.min(1, p / GOOD_PERCENTILE));
  return rgba(LILAC_DEEP, 0.45 - t * 0.34);
```

바로 위 JSDoc 의 상한 문장을 고친다:

```
 *  🔴 보라 상한 0.45(2026-09-26, 0.30 에서 올림) / 금색 0.45~0.65(불변). 0.30 에서는 실측
 *     보라 칸 전체가 0.14~0.29 에 몰려 3점과 64점이 구분되지 않았다. 대비 실측:
 *     #5A3E8C on 보라 0.45 = 5.38, #412402 on 금 0.65 = 10.09.
```

- [ ] **Step 4: 통과 확인**

```bash
node --import tsx --test lib/byeolmaru/calendar-visual.test.ts
```

Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add lib/byeolmaru/calendar-visual.ts lib/byeolmaru/calendar-visual.test.ts
git commit -m "$(cat <<'EOF'
fix(byeolmaru): 격자 면 색 계조를 넓힌다 (보라 0.30 → 0.45)

실측에서 보라 칸 전체가 알파 0.14~0.29 안에 들어 9/30(3점)과
9/1(64점)이 눈으로 구분되지 않았다. 스펙은 "양방향 채도"라고 적었으나
실현은 단방향(금색만 튐)이었고 "살짝 챙길 날" 3개가 사라졌다.

바닥 0.11(흰 판 위에서 칸이 사라지지 않는 하한)과 금색 구간은 불변.
#5A3E8C on 보라 0.45 = 5.38 로 AA 통과.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 시각 ③④ — 격자 칸에서 "점" 제거 + 날짜 가독성

**Files:**
- Modify: `components/byeolmaru/CalendarGrid.tsx`

- [ ] **Step 1: 날짜 span 을 키운다**

열린 칸의 날짜 span:

```tsx
              {/* 🔴 10px/.6 에서 올렸다(2026-09-26) — 점수(16px bold)가 압도해서 달력인데
                  "몇 일"을 찾기 어려웠다(30칸이 다 찬 뒤 드러난 결함). 점수가 주 신호라는
                  위계는 유지한다: bold 로 만들거나 점수와 같은 크기로 올리지 않는다. */}
              <span className="text-[11px] leading-[13px]" style={{ color: c.isToday ? "rgba(255,255,255,.72)" : "rgba(122,107,160,.78)" }}>
                {Number(c.date.slice(8, 10))}
              </span>
```

잠긴 칸(게스트 셸)의 날짜 span 도 같은 크기로 맞춘다 — 두 칸이 같은 격자에 서는 일은 없지만 게스트 화면만 작은 날짜로 남는 건 드리프트다:

```tsx
                <span className="text-[11px] leading-[13px] text-text-light/60">{Number(date.slice(8, 10))}</span>
```

- [ ] **Step 2: "점" 을 뺀다**

점수 블록을 교체한다:

```tsx
              {/* 🔴 "점" 단위를 뺐다(2026-09-26) — 30칸에 30번 반복되면 잡음이고, 두 자리
                  숫자+점이 43.6px 칸을 꽉 채웠다. 단위가 들어갔던 근거("맨숫자는 점수로 안
                  읽힌다")는 스트립 7칸 기준이었고 그 스트립은 삭제됐다. 맥락은 같은 판 위의
                  TodayLead("오늘 40점")가 진다 — 오늘 칸 숫자와 그 줄이 같은 값이라 연결된다. */}
              <span
                className="text-[17px] font-semibold leading-[20px]"
                style={{ color: c.isToday ? "#ffffff" : isGoodScore(c.score) ? "#412402" : "#5A3E8C" }}
              >
                {scoreDisplay(c.score)}
              </span>
```

잠긴 칸의 높이 스페이서를 새 줄높이에 맞춘다:

```tsx
                <span aria-hidden className="h-[20px]" />
```

> `aria-label` 의 `${scoreDisplay(c.score)}점` 은 **그대로 둔다** — 스크린리더는 단위가 없으면 숫자의 뜻을 잃는다.

- [ ] **Step 3: 타입·빌드**

```bash
npx tsc --noEmit && npm run build
```

Expected: 에러 0, exit 0.

- [ ] **Step 4: 커밋**

```bash
git add components/byeolmaru/CalendarGrid.tsx
git commit -m "$(cat <<'EOF'
fix(byeolmaru): 격자 칸 — "점" 단위 제거 + 날짜 가독성

스트립이 사라지고 30칸이 다 차면서 드러난 것 둘.
- "점"이 30번 반복돼 잡음이었다. 단위를 넣은 근거는 스트립 7칸
  기준이었고 그 스트립은 삭제됐다. 맥락은 TodayLead("오늘 40점")가
  진다. aria-label 의 "점"은 유지 — 스크린리더는 단위가 필요하다.
- 날짜 10px/.6 → 11px/.78. 달력인데 "몇 일"을 찾기 어려웠다.
  점수가 주 신호라는 위계는 유지(bold 아님, 크기 아래).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 시각 ① — 월 표시

**Files:**
- Modify: `components/byeolmaru/CalendarGrid.tsx`

- [ ] **Step 1: 요일 헤더 위에 월을 넣는다**

`CalendarGrid` 의 요일 헤더 `<div className="mb-2 grid grid-cols-7 …">` **바로 앞**에 넣는다:

```tsx
      {/* 🔴 월 표시는 필수다 — 예전엔 접이식 버튼 텍스트("이번 달 달력 보기")가 유일한 월
          맥락이었는데 그 래퍼가 2026-09-26 에 삭제됐다. 없으면 이게 몇 월인지 화면 어디에도
          없다. 연도는 안 쓴다 — 달력은 항상 이번 달이고 연도는 잡음이다.
          🔴 월 이동(◀▶)은 이번 스코프가 아니다 — 넣으면 "지난 달 리포트 소급 생성"·"몇 달
             전까지 보나" 같은 정책이 줄줄이 붙는다. */}
      <p className="mb-1.5 px-0.5 text-[13px] font-semibold text-eye-purple">
        {Number(todayDate.slice(5, 7))}월
      </p>
```

> `todayDate` 는 이미 prop 으로 있고 게스트 셸도 넘긴다 — 두 화면 다 월이 뜬다.

- [ ] **Step 2: 빌드**

```bash
npx tsc --noEmit && npm run build
```

Expected: 에러 0, exit 0.

- [ ] **Step 3: 커밋**

```bash
git add components/byeolmaru/CalendarGrid.tsx
git commit -m "$(cat <<'EOF'
feat(byeolmaru): 격자에 월 표시

접이식 래퍼가 삭제되면서 "이번 달 달력 보기" 버튼 텍스트가 지던
유일한 월 맥락이 사라졌다. 그게 없으면 이게 몇 월인지 화면 어디에도
없다. 연도는 넣지 않는다(달력은 항상 이번 달).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 시각 ⑤ — 앞뒤 달 채움

**Files:**
- Modify: `lib/byeolmaru/calendar.ts`
- Modify: `lib/byeolmaru/calendar.test.ts`
- Modify: `app/api/byeolmaru/calendar/route.ts`
- Modify: `components/byeolmaru/CalendarGrid.tsx`
- Modify: `components/byeolmaru/ByeolmaruHub.tsx`

- [ ] **Step 1: `gridRange` 테스트를 먼저 쓴다**

`lib/byeolmaru/calendar.test.ts` 에 추가한다(import 에 `gridRange` 를 더한다):

```ts
test("gridRange — 1일의 요일만큼 앞으로, 말일에서 토요일까지 뒤로", () => {
  // 2026-09-01 은 화요일(요일 index 2) → 앞 2칸(8/30 일, 8/31 월)
  // 2026-09-30 은 수요일(index 3) → 뒤 3칸(10/1 목, 10/2 금, 10/3 토)
  assert.deepEqual(gridRange("2026-09-26"), { start: "2026-08-30", end: "2026-10-03" });
});

test("gridRange — 1일이 일요일이면 앞 채움이 없다", () => {
  // 2026-11-01 은 일요일 → 앞 0칸
  assert.equal(gridRange("2026-11-15").start, "2026-11-01");
});

test("gridRange — 말일이 토요일이면 뒤 채움이 없다", () => {
  // 2026-10-31 은 토요일 → 뒤 0칸
  assert.equal(gridRange("2026-10-15").end, "2026-10-31");
});

test("gridRange — 연 경계를 넘는다", () => {
  // 2026-12-01 은 화요일 → 앞 2칸(11/29, 11/30) / 2026-12-31 은 목요일 → 뒤 2칸(1/1, 1/2)
  assert.deepEqual(gridRange("2026-12-10"), { start: "2026-11-29", end: "2027-01-02" });
});

test("buildCalendarPayload — 채움 칸은 cells 에 안 섞이고 주차 집계에도 안 들어간다", () => {
  // 8/30(앞 채움) + 9/1~9/2(이번 달) 세 칸만으로 경계를 본다.
  const rows: DailyLuck[] = [
    { date: "2026-08-30", stem: "갑", branch: "자", element: "목", hanja: "甲子" },
    { date: "2026-09-01", stem: "기", branch: "축", element: "토", hanja: "己丑" },
    { date: "2026-09-02", stem: "경", branch: "오", element: "금", hanja: "庚午" },
  ];
  const { cells, fillCells, weeks } = buildCalendarPayload(SAJU, rows, "2026-09-01");
  assert.deepEqual(cells.map((c) => c.date), ["2026-09-01", "2026-09-02"]);
  assert.deepEqual(fillCells.map((c) => c.date), ["2026-08-30"]);
  assert.equal(weeks.length, 1);
  assert.equal(weeks[0].startDate, "2026-09-01", "채움 칸이 주차 집계에 새면 안 된다");
  assert.equal(weeks[0].endDate, "2026-09-02");
});
```

> `SAJU` 와 `DailyLuck` import 는 `calendar.test.ts` 상단에 **이미 있다**(`const SAJU = {...} as unknown as SajuResult` · `import type { DailyLuck, SajuResult } from "@/lib/saju/calc"`). import 줄에는 `gridRange` 와 `buildCalendarPayload` 만 더하면 된다:
>
> ```ts
> import { buildCalendar, weekBuckets, toDaySelf, monthRange, gridRange, buildCalendarPayload } from "./calendar.ts";
> ```

- [ ] **Step 2: 실패 확인**

```bash
node --import tsx --test lib/byeolmaru/calendar.test.ts
```

Expected: FAIL — `gridRange` 가 없다.

- [ ] **Step 3: `gridRange` 를 만든다**

`lib/byeolmaru/calendar.ts` 의 `monthRange` 바로 아래에 넣는다:

```ts
/** KST 날짜 문자열을 days 만큼 민다. UTC 자정 기준 산술이라 서버 TZ·DST 에 안 흔들린다. */
function shiftDate(dateKst: string, days: number): string {
  return new Date(Date.parse(`${dateKst}T00:00:00Z`) + days * 86400000).toISOString().slice(0, 10);
}

/**
 * 격자가 실제로 그리는 범위 — 이번 달 + 앞뒤 채움. 일반 달력 관행대로 첫 줄 앞과 마지막 줄
 * 뒤를 옆 달 날짜로 메운다. **고정 6주가 아니다** — 달마다 0~6칸씩 달라진다.
 *
 * 🔴 이게 스트립이 지던 "월 경계"를 대신한다 — 9/29 에 10/1~10/2 를 볼 수 있다.
 * 🔴 채움 칸도 룰 계산이라 원가는 0이다(tyme4ts 결정론).
 */
export function gridRange(todayKst: string): { start: string; end: string } {
  const { start, end } = monthRange(todayKst);
  const lead = new Date(`${start}T00:00:00Z`).getUTCDay(); // 0(일)~6(토)
  const trail = 6 - new Date(`${end}T00:00:00Z`).getUTCDay();
  return { start: shiftDate(start, -lead), end: shiftDate(end, trail) };
}
```

- [ ] **Step 4: 통과 확인**

```bash
node --import tsx --test lib/byeolmaru/calendar.test.ts
```

Expected: PASS.

- [ ] **Step 5: 라우트가 넓은 범위를 계산하게 한다**

`app/api/byeolmaru/calendar/route.ts` — `monthRange` 호출은 응답의 `monthStart`/`monthEnd` 에 그대로 쓰고, 일진 계산만 `gridRange` 로 넓힌다:

```ts
    const { start: monthStart, end: monthEnd } = monthRange(todayKst);
    // 🔴 일진은 격자가 그리는 범위(앞뒤 달 채움 포함)로 계산한다 — monthRange 로만 계산하면
    //    마지막 줄이 휑하고 월 경계에서 앞으로 며칠이 잘린다. 룰 계산이라 추가 원가는 0이다.
    const { start: gridStart, end: gridEnd } = gridRange(todayKst);
    const gridLuck = calcDailyLuckRange(gridStart, gridEnd);
```

그리고 **`monthLuck` 계산 줄을 지운다** — `gridLuck` 이 그 범위를 포함한다. `monthLuck` 을 쓰던 두 자리를 `gridLuck` 으로 바꾼다:

- self: `buildCalendarPayload(saju, gridLuck, todayKst)`
- pair: `buildPairCalendar(saju, partnerSaju, gridLuck, todayKst)` 로 넓히고, 이번 달/채움 분리를 같은 규칙으로 한다:

```ts
      const pairAll = buildPairCalendar(saju, partnerSaju, gridLuck, todayKst);
      const pairCells = pairAll.filter((c) => c.date >= monthStart && c.date <= monthEnd);
      const pairFillCells = pairAll.filter((c) => c.date < monthStart || c.date > monthEnd);
```

pair 응답에 `fillCells: pairFillCells` 를 더한다.

빈 결과 가드를 `gridLuck` 기준으로 바꾼다:

```ts
    if (!gridLuck.length) {
      return NextResponse.json({ error: "calc_failed" }, { status: 500 });
    }
```

- [ ] **Step 6: 격자가 채움 칸을 그린다**

`components/byeolmaru/CalendarGrid.tsx` — prop 을 더한다:

```tsx
  /** 앞뒤 달 채움 칸(일반 달력 관행). 이번 달 칸(`cells`)과 **타입은 같고 소속이 다르다** —
   *  섞으면 "이번 달 잘 맞는 날 N일" 집계가 다른 달을 센다. 흐리게 그린다. */
  fillCells?: GridCell[];
```

```tsx
  fillCells = [],
```

슬롯 조립에 채움을 더하고 플래그를 단다:

```tsx
  // 열린 칸 + 채움 칸 + (게스트 셸의) 잠긴 칸을 날짜순으로 합친다.
  const slots: { date: string; cell?: GridCell; fill?: boolean }[] = [
    ...cells.map((c) => ({ date: c.date, cell: c })),
    ...fillCells.map((c) => ({ date: c.date, cell: c, fill: true })),
    ...lockedCells.map((l) => ({ date: l.date })),
  ].sort((a, b) => a.date.localeCompare(b.date));
  if (slots.length === 0) return null;
```

> `blanks` 계산은 **그대로 둔다** — 채움이 있으면 첫 슬롯이 일요일이라 `firstWeekday`가 0이 되어 빈칸이 저절로 0개가 된다. 채움이 없는 게스트 셸에서는 지금처럼 앞을 비운다.

슬롯 구조분해와 버튼 렌더를 고친다:

```tsx
        {slots.map(({ date, cell: c, fill }) => {
```

```tsx
              className="relative flex aspect-square flex-col items-center justify-center rounded-xl"
              style={{
                background: c.isToday ? "#5A3E8C" : cellTint(c.score),
                // 🔴 채움 칸은 흐리게 — "이번 달"이라는 판의 말을 흐리지 않기 위해서다.
                //    누르면 그 날짜 상세로 간다(룰 계산이라 내용이 있다).
                ...(fill ? { opacity: 0.45 } : {}),
                ...(selected && !c.isToday ? { boxShadow: "0 0 0 2px rgba(159,138,208,.75)" } : {}),
              }}
```

`aria-label` 에 다른 달임을 밝힌다:

```tsx
              aria-label={`${fill ? "다른 달 " : ""}${c.date} ${c.label} ${scoreDisplay(c.score)}점${c.marks.length ? ` · ${c.marks.map((m) => m.label).join(", ")}` : ""}`}
```

- [ ] **Step 7: 허브가 채움을 넘긴다**

`components/byeolmaru/ByeolmaruHub.tsx` — `gridCells` 매핑 바로 아래에 같은 모양으로 더한다:

```tsx
  const fillGridCells: GridCell[] = data.fillCells.map((c) => ({
    date: c.date, score: c.score, tone: c.grade.tone, label: c.grade.label, isToday: c.isToday, marks: c.marks,
  }));
```

격자 호출에 넘긴다:

```tsx
            <CalendarGrid
              cells={gridCells}
              fillCells={fillGridCells}
              todayDate={data.today}
              selectedDate={data.today}
              onSelect={openDay}
              panel={false}
            />
```

- [ ] **Step 8: 타입·테스트·빌드**

```bash
npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*") && npm run build
```

Expected: 에러 0, 전량 PASS, build exit 0.

- [ ] **Step 9: 커밋**

```bash
git add -u
git commit -m "$(cat <<'EOF'
feat(byeolmaru): 격자 앞뒤 달 채움 — 마지막 줄 공백과 월 경계 손실을 메운다

일반 달력 관행대로 첫 줄 앞과 마지막 줄 뒤를 옆 달 날짜로 흐리게
채운다. 스트립이 지던 "월 경계"(9/29 에 10/1~10/2 보기)를 이게
대신한다. 룰 계산이라 추가 원가 0.

🔴 채움 칸은 cells 가 아니라 fillCells 로 내려간다 — 섞으면
weekBuckets 가 다른 달을 세서 "이번 달 잘 맞는 날 N일"이 거짓이
된다. 규약 대신 필드로 경계를 굳혔다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 실물 검수

**Files:** 없음 (검증 전용)

> 🔴 **목업을 만들지 않는다.** 이 저장소는 목업 대체물에 두 번 속았다 — 동물을 단색 회색 원으로 목업해 14px 로 정했는데 실제 파스텔 chibi 에셋은 그 크기에서 형체가 사라졌다.

- [ ] **Step 1: dev 서버를 띄운다**

`.claude/launch.json` 의 `byeolkong-dev` 를 쓴다(`npm run dev`, port 3000). Bash 로 서버를 직접 띄우지 않는다.

- [ ] **Step 2: 구독 계정으로 허브를 본다 (375×812)**

세션 주입: `document.cookie='byeolkong_user_id=11111111-1111-4111-8111-111111111111; path=/'`
(navigate 뒤 날아가면 다시 심는다.)

| # | 확인 |
|---|---|
| 1 | 판이 `타이틀 → 오늘 줄 → 월 표시 → 격자` 4층이고 스트립·접힘 버튼이 없다 |
| 2 | 30칸이 다 차고 **낮은 날(3·8·14점)이 실제로 눈에 띈다** |
| 3 | "점" 없는 숫자가 **점수로 읽힌다** — 위의 "오늘 N점"과 연결되는가 |
| 4 | 날짜를 **찾을 수 있다** |
| 5 | 첫 줄·마지막 줄의 **채움 칸이 흐리고**, 이번 달 칸과 구분된다 |
| 6 | 보라 상한 0.45 와 금색 하한 0.45 가 **서로 다른 무게로 보인다** (§6-2 실물 검수) |

- [ ] **Step 3: 비구독 계정으로 같은 화면을 본다**

`0370400f-f29a-4aa9-8113-64b18b5cc7a8`

| # | 확인 |
|---|---|
| 7 | **30칸이 똑같이 다 찬다** — 점선 칸이 하나도 없다 (A의 핵심) |
| 8 | 배너가 체험/구독 CTA 를 그대로 띄운다 |

- [ ] **Step 4: 게스트 화면**

쿠키를 지우고 `/byeolmaru` 를 연다.

| # | 확인 |
|---|---|
| 9 | `EmptyMonthShell` 이 **그대로 전부 점선**이고 채움 칸이 없다 |
| 10 | 월 표시가 뜬다 |
| 11 | *"점선 칸은 아직 안 온 날이야"* 문구가 **없고** *"네 생일만 있으면 이 칸이 다 칠해져"* 만 있다 |

- [ ] **Step 5: 날짜 상세 — 리포트 범위 (구독 계정)**

격자의 **내일 칸**을 누른다.

| # | 확인 |
|---|---|
| 12 | 일진 히어로·하루 이름·taste 4조각·축 게이지가 **나온다** |
| 13 | 리포트 자리에 *"그날 이야기는 그날 아침에 들려줄게."* 가 뜬다 |
| 14 | 오늘 칸은 리포트가 정상 생성된다 |

- [ ] **Step 5-b: 거짓 페이월 (비구독 계정)** 🔴 *스펙 §4-2·§4-4 — 작업 중 추가됨*

`0370400f-f29a-4aa9-8113-64b18b5cc7a8` 로 본다. **이 둘은 코드로 판정이 안 된다** — 라우트가 DB·세션을 물어 유닛에서 못 돌고, 이 프로젝트엔 컴포넌트 테스트가 0개다.

| # | 확인 |
|---|---|
| 15 | **내일 칸** — `PaywallCut`("여기부터 1,800자가 더 있어")이 **안 뜨고** *"그날 이야기는 그날 아침에 들려줄게."* 가 뜬다 |
| 16 | **과거 칸** — 페이월이 **안 뜨고**, 그 날 받은 글이 있으면 글이 / 없으면 *"그날은 리포트를 안 받았어…"* 가 뜬다 |
| 17 | **오늘 칸** — 페이월이 **그대로 뜬다**(여기만 파는 자리다). 이게 안 뜨면 판매가 통째로 사라진 것이다 |
| 18 | 오늘 타로도 15~17 과 같은 규칙인가 (`card-narrative` 도 같이 고쳤다) |

- [ ] **Step 5-c: 실물로만 판정되는 것 3건** 🔴 *전부 "사용자 지적으로 들어간 값을 뒤집은 것"이라 나쁘면 되돌린다*

| # | 확인 | 되돌릴 대상 |
|---|---|---|
| 19 | 보라 상한 `0.45` 와 금색 하한 `0.45` 가 **서로 다른 무게**로 보이나 — 같은 알파라 "제일 나쁜 날"과 "약한 좋은 날"이 비슷해 보일 위험 | §6-2 |
| 20 | 점수 숫자가 **17px** 로 적절한가 — 플랜이 "점" 제거와 함께 16→17px 를 **묶어서** 지시했다. 스펙 §6-3 은 제거만 말했으므로 이 1px 은 명시 안 된 변경이다 | §6-3 |
| 21 | 미래 날짜 상세에서 *"카드는 그날 뽑는 거야."* 와 *"그날 이야기는 그날 아침에 들려줄게."* 가 **연달아** 붙는다. 가리키는 대상은 다른데(카드/리포트) 어조·구문이 거의 같아 한 화면에서 중복으로 읽히나 | 문구 |

- [ ] **Step 6: 콘솔·네트워크 확인**

`read_console_messages` 로 에러 0, `read_network_requests` 로 `/api/byeolmaru/calendar` 응답에 `strip`·`lockedCells` 가 **없고** `fillCells` 가 있는지 본다.

- [ ] **Step 7: 결과를 사용자에게 스크린샷으로 보고**

실패한 항목이 있으면 **고치지 말고 먼저 보고한다** — ②③④는 09-24 에 사용자 지적으로 들어간 값을 뒤집는 것이라, 실물에서 나쁘면 되돌릴지가 사용자 결정이다.

---

## 마무리

- [ ] **전량 검증**

```bash
npx tsc --noEmit && node --import tsx --test $(find . -name "*.test.ts" -not -path "./node_modules/*" -not -path "./qa/*") && npm run build
```

Expected: 에러 0 / 전량 PASS / exit 0 + 라우트 표에 `ƒ Proxy`.

- [ ] **`origin/dev` 푸시** — 사용자 확인 후.
- [ ] 🔴 **prod(main) 배포는 하지 않는다** — 사용자가 명시할 때까지.
- [ ] **2단계**(날짜 상세 탭 통합)는 별도 스펙·플랜. 스펙 §8 에 조사 결과가 다 있다.
