# 별자리 포커스 보완 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 방금 배포한 포커스를 보완 — 걸음→1단계+"전체 지도 보기" 버튼, 카드에 X 관계 전체·해석 추가, 지도↔카드 연동, 순위 목록 리스타일, 별콩이 한마디 삭제, 헤더 "{신수}자리", 최대 20명.

**Spec:** `docs/superpowers/specs/2026-08-21-별자리-포커스-보완-design.md`

**Tech Stack:** Next.js 16 / React 19 / TS strict, SVG, node:test.

## 파일
- `lib/byeoljari/focus.ts` — `focusSummary` 추가.
- `lib/constants.ts` — `MAX_STAR_MAP_MEMBERS`.
- `app/api/fortune/byeoljari/[shareId]/join/route.ts` — 20명 상한 게이트.
- `app/fortune/byeoljari/[shareId]/page.tsx` — "full" 안내(있는 join UI에).
- `components/byeoljari/InyeonDetail.tsx` — 별콩이 한마디 삭제.
- `components/byeoljari/ConstellationView.tsx` — focusId 단일·버튼·카드 ②·연동·순위 리스타일·헤더.

---

### Task 1: focusSummary 순수 함수 (TDD)

**Files:** Modify `lib/byeoljari/focus.ts`, Create/append `lib/byeoljari/focus.test.ts`

- [ ] **Step 1: 실패 테스트 추가**(focus.test.ts에 append; 기존 buildFocusGraph 테스트의 `edge`/`G` 헬퍼 재사용 가능하면 재사용, 아니면 새로 작성)
```ts
import { focusSummary } from "./focus.ts";

test("focusSummary — 나 제외 이웃 수 + 끌림/결속/무리 카운트", () => {
  // me-a(host,비특별), a-b(천간합), a-c(육합), 삼합[a,d]
  const G2 = {
    ok:true, shareId:"s", claimed:false,
    nodes:[
      {id:"me",name:"나",isHost:true,relationType:"friend",element:"화"},
      {id:"a",name:"A",isHost:false,relationType:"friend",element:"목"},
      {id:"b",name:"B",isHost:false,relationType:"friend",element:"수"},
      {id:"c",name:"C",isHost:false,relationType:"friend",element:"토"},
      {id:"d",name:"D",isHost:false,relationType:"friend",element:"금"},
    ],
    edges:[
      {a:"me",b:"a",element:"생아",labelAtoB:"",labelBtoA:"",tenGodAtoB:"",tenGodBtoA:"",inyeon:62,triadShared:false,heavenlyCombo:false,sixCombo:false},
      {a:"a",b:"b",element:"생아",labelAtoB:"",labelBtoA:"",tenGodAtoB:"",tenGodBtoA:"",inyeon:87,triadShared:false,heavenlyCombo:true,sixCombo:false},
      {a:"a",b:"c",element:"생아",labelAtoB:"",labelBtoA:"",tenGodAtoB:"",tenGodBtoA:"",inyeon:77,triadShared:false,heavenlyCombo:false,sixCombo:true},
    ],
    triads:[{element:"화",memberIds:["a","d"]}],
  };
  const s = focusSummary("a", "me", G2);
  assert.equal(s.total, 3);   // b, c, d (me 제외)
  assert.equal(s.chemi, 1);   // a-b 천간합
  assert.equal(s.bond, 1);    // a-c 육합
  assert.equal(s.triad, 1);   // d 삼합
  assert.equal(typeof s.comment, "string");
});

test("focusSummary — 이웃 0이면 잔잔 코멘트", () => {
  const G0 = { ok:true, shareId:"s", claimed:false,
    nodes:[{id:"me",name:"나",isHost:true,relationType:"friend",element:"화"},{id:"a",name:"A",isHost:false,relationType:"friend",element:"목"}],
    edges:[{a:"me",b:"a",element:"비화",labelAtoB:"",labelBtoA:"",tenGodAtoB:"",tenGodBtoA:"",inyeon:50,triadShared:false,heavenlyCombo:false,sixCombo:false}],
    triads:[] };
  const s = focusSummary("a", "me", G0);
  assert.equal(s.total, 0);
});
```
- [ ] **Step 2: 실패 확인** — `node --import tsx --test lib/byeoljari/focus.test.ts`
- [ ] **Step 3: 구현**(focus.ts에 추가)
```ts
import type { StarGraph } from "./types.ts"; // (이미 import돼 있으면 재사용)

/** focus(centerId)에서 X의 이웃(나=pivot 제외) 특별 인연 요약(카드 ② 해석용). 순수. */
export function focusSummary(
  centerId: string,
  pivotId: string | null,
  graph: StarGraph
): { total: number; chemi: number; bond: number; triad: number; comment: string } {
  const partners = new Set<string>();
  graph.edges.forEach((e) => {
    if (e.a === centerId && e.b !== pivotId) partners.add(e.b);
    else if (e.b === centerId && e.a !== pivotId) partners.add(e.a);
  });
  const triadMates = new Set<string>();
  graph.triads.forEach((t) => {
    if (t.memberIds.includes(centerId))
      t.memberIds.forEach((id) => {
        if (id !== centerId && id !== pivotId) triadMates.add(id);
      });
  });
  triadMates.forEach((id) => partners.add(id));

  let chemi = 0, bond = 0, triad = 0;
  partners.forEach((id) => {
    const e = graph.edges.find(
      (x) => (x.a === centerId && x.b === id) || (x.a === id && x.b === centerId)
    );
    if (e?.heavenlyCombo) chemi++;
    if (e?.sixCombo) bond++;
    if (triadMates.has(id)) triad++;
  });
  const total = partners.size;
  const comment =
    total === 0 ? "아직 특별한 인연은 잔잔한 편이야"
    : total <= 2 ? "몇몇과 깊게 이어진 별이야"
    : "여기저기 잘 통하는 별이네";
  return { total, chemi, bond, triad, comment };
}
```
- [ ] **Step 4: 통과 확인** + 회귀(`buildFocusGraph` 테스트 green).
- [ ] **Step 5: 커밋** — `feat(byeoljari): focusSummary — 포커스 X 서클 요약(끌림/결속/무리 카운트) TDD`

---

### Task 2: 최대 20명 상한 (const + join 게이트 + 안내)

**Files:** `lib/constants.ts`, `app/api/fortune/byeoljari/[shareId]/join/route.ts`, `app/fortune/byeoljari/[shareId]/page.tsx`

- [ ] **Step 1: 상수** — `lib/constants.ts`에 추가(파일 스타일 맞춰):
```ts
export const MAX_STAR_MAP_MEMBERS = 20; // 별자리 1개 최대 인원(호스트 포함). 라벨 상한과 일치.
```
- [ ] **Step 2: join 게이트** — `join/route.ts`에서 map 조회 성공 후, member insert 전에 인원 카운트:
```ts
  const { count, error: cntErr } = await supa
    .from("star_map_members")
    .select("id", { count: "exact", head: true })
    .eq("map_id", map.id);
  if (cntErr) { /* logError + 500 lookup 패턴 */ }
  if ((count ?? 0) >= MAX_STAR_MAP_MEMBERS) {
    return NextResponse.json({ ok: false, reason: "full" }, { status: 409 });
  }
```
(`import { MAX_STAR_MAP_MEMBERS } from "@/lib/constants";` 추가. logError 패턴은 파일의 기존 방식 따를 것.)
- [ ] **Step 3: 클라 안내** — join 요청 응답 처리부(`[shareId]/page.tsx` 또는 join UI)에서 `reason==="full"`이면 "인원이 가득 찼어 (최대 20명)" 메시지 표시. 기존 에러 표시 패턴 따를 것(없으면 alert/inline 최소).
- [ ] **Step 4: 검증** — `npx tsc --noEmit`. 로컬 실호출(선택): 20명 맵에 21번째 join → `{ok:false,reason:"full"}` 409.
- [ ] **Step 5: 커밋** — `feat(byeoljari): 별자리 최대 20명 — join 상한 게이트 + 안내`

---

### Task 3: InyeonDetail — 별콩이 한마디 삭제

**Files:** `components/byeoljari/InyeonDetail.tsx`

- [ ] **Step 1** — `별콩이 — {inyeon.comment}` `<p>` 줄 삭제. `inyeon` prop 의 `comment` 필드는 남겨도 무방하나 미사용이면 타입에서 빼도 됨(호출부 View가 안 넘기게 T5에서 정리). 인연 점수 블록·근거 리스트·카피·뱃지는 유지.
- [ ] **Step 2** — `npx tsc --noEmit` clean.
- [ ] **Step 3: 커밋** — `feat(byeoljari): 상세에서 별콩이 한마디 제거`

---

### Task 4: View — 1단계 포커스 + "전체 지도 보기" 버튼 + 헤더 + 순위 리스타일

**Files:** `components/byeoljari/ConstellationView.tsx`

이 태스크는 **구조·리스타일**(카드 ② 섹션은 Task 5). 걸음/breadcrumb 제거하되 카드는 일단 `나↔X`만.

- [ ] **Step 1: 상태 단일화** — `focusPath: string[]` → `focusId: string | null`. `inspected` 제거. `listOpenId` 유지. `expandedNeighborId` 는 Task 5에서 추가(지금은 미도입).
```tsx
  const [focusId, setFocusId] = useState<string | null>(null);
  const [listOpenId, setListOpenId] = useState<string | null>(null);
  const [activeFilter, setActiveFilter] = useState<BondFilter | null>(null);
```
- [ ] **Step 2: 파생** — `viewGraph`/`layout`/`sizes` 를 focusId 로(현행 focusPath→focusId 치환). breadcrumb 관련 파생 제거.
- [ ] **Step 3: handlers** — `handleNode(id)`: id===pivot → overview(setFocusId(null)); focusId 없을 때(overview) 게스트 탭 → setFocusId(id)+setListOpenId(null); focusId 있을 때(포커스) 이웃 탭 → **재포커스 안 함**(Task 5에서 expandedNeighborId 세팅; 지금은 no-op 자리만). `resetToOverview()` = setFocusId(null). Canvas `onEdgeSelect` 는 Task 5까지 no-op 또는 미전달.
- [ ] **Step 4: breadcrumb 제거 + "전체 지도 보기" 버튼** — breadcrumb 블록 삭제. 캔버스 wrapper(`relative` div) 안에 focusId 있을 때 좌상단 오버레이 버튼:
```tsx
        {focusId && (
          <button
            type="button"
            onClick={resetToOverview}
            className="absolute left-2 top-2 z-10 rounded-full bg-cream-warm/90 px-3 py-1 text-xs text-eye-purple shadow"
          >
            ← 전체 지도 보기
          </button>
        )}
```
- [ ] **Step 5: 헤더 "{신수}자리"** — h1 내용을 `{shape ? `${shape.name}자리` : "우리 별자리"}` 로. 엠블럼 아이콘 그대로.
- [ ] **Step 6: 순위 리스타일** — ranking `.map` 행:
  - wrapper: `rounded-2xl` + 두꺼운 패딩. 1위(`i===0`)는 **다크톤**: `bg-night text-cream` (또는 `bg-[#1F1735] text-[#FAF6F0]`) — 나머지는 `bg-cream-warm`.
  - 헤더 버튼 내부: 이름 `text-sm`, **인연 점수 크게·볼드**: `<span className="font-display text-lg ...">{r.inyeon}</span>` + 작은 "점" 등급. 1위는 글씨색 크림/금 대비 유지.
  - 상세(아코디언)는 유지(별콩이만 T3로 빠짐).
- [ ] **Step 7: Canvas 전달** — focusId 반영(`graph={viewGraph}`, `focusMode={!!focusId}`, `activeFilter={effectiveFilter}`, `onSelect={handleNode}`, `onBackgroundClick={resetToOverview}`). `onEdgeSelect` 는 Task 5에서.
- [ ] **Step 8: 검증 + 커밋** — tsc clean, breadcrumb/focusPath/inspected 잔재 없음. `feat(byeoljari): 포커스 1단계+전체지도 버튼·헤더 "{신수}자리"·순위 리스타일(1위 다크·점수강조)`

---

### Task 5: View — 카드 ② (X 관계 전체 & 해석) + 지도↔카드 연동

**Files:** `components/byeoljari/ConstellationView.tsx`

- [ ] **Step 1: 상태** — `const [expandedNeighborId, setExpandedNeighborId] = useState<string | null>(null);` focusId 바뀌면 초기화(useEffect 또는 handleNode에서).
- [ ] **Step 2: X 이웃 리스트 파생** — focusId 기준, `viewGraph`(buildFocusGraph)의 노드 중 나(pivot)·X 제외한 이웃들 + 각 X↔이웃 edge(detailFor(focusId, id) 재사용)로 {id, name, inyeon, grade, tags}.
- [ ] **Step 3: 지도↔카드 연동** — Canvas `onSelect`(포커스 중 이웃 노드 탭)와 `onEdgeSelect`(spoke 탭) → `setExpandedNeighborId(id)`. handleNode 수정: focusId 있을 때 이웃 탭 → `setExpandedNeighborId(toggle)`(재포커스 아님). onEdgeSelect 는 spoke 의 나 아닌 끝점 id 로.
- [ ] **Step 4: 카드 렌더** — 기존 `나↔X` 카드 아래 §2 섹션 추가:
```tsx
          {/* ② X의 인연 전체 + 해석 */}
          <div className="mt-4 border-t border-lilac/30 pt-3">
            <p className="mb-2 text-sm text-eye-purple">{summaryText}</p>
            <div className="space-y-1.5">
              {neighbors.map((nb) => {
                const open = expandedNeighborId === nb.id;
                return (
                  <div key={nb.id} className={`overflow-hidden rounded-xl ${open ? "ring-1 ring-lilac" : ""} bg-white/60`}>
                    <button type="button" onClick={() => setExpandedNeighborId(open ? null : nb.id)}
                      className="flex w-full items-center justify-between px-3 py-2 text-left">
                      <span className="text-sm text-eye-purple">{nb.name ?? "이 별"} <span className="text-xs text-text-light">{nb.tag}</span></span>
                      <span className="text-sm font-semibold text-eye-purple">인연 점수 {nb.inyeon}</span>
                    </button>
                    {open && (
                      <div className="px-3 pb-3 pt-1">
                        <InyeonDetail target={nb.node} oriented={nb.oriented} heavenlyCombo={nb.edge?.heavenlyCombo ?? false} sixCombo={nb.edge?.sixCombo ?? false} inyeon={nb.inyeonInfo} pivotIsMe={false} />
                      </div>
                    )}
                  </div>
                );
              })}
            </div>
          </div>
```
  - `summaryText` = focusSummary 결과 조립: `${X이름}이(가) 강하게 엮인 인연은 ${total}명 — 끌림 ${chemi}·결속 ${bond}·같은 무리 ${triad}. ${comment}` (total 0이면 "아직 …" 만).
  - `nb.tag` = 끌림/결속/무리 중 있는 것(edge.heavenlyCombo→끌림, sixCombo→결속, triadShared→무리).
  - detailFor(focusId, nb.id) 로 oriented/edge/inyeonInfo/target(=nb.node) 구성.
- [ ] **Step 5: 스크롤 연동** — expandedNeighborId 바뀌면 해당 행/카드로 scrollIntoView(block:"nearest") — 기존 detailRef 패턴 참고(옵션).
- [ ] **Step 6: 검증 + 커밋** — tsc clean. `feat(byeoljari): 포커스 카드에 X 관계 전체·해석 + 지도↔카드 연동`

---

### Task 6: 브라우저 E2E

- [ ] dev 로엔 `7uBv0cLKet`. 별 탭 → 1단계 포커스 + 좌상단 "전체 지도 보기"(걸음/breadcrumb 없음).
- [ ] 카드: ① 나↔X + ② "X의 인연"(해석 문장 + X↔이웃 리스트). 이웃 별/선 탭 → 카드 그 행 펼침(강조).
- [ ] 순위: 1위 다크톤·두꺼운 카드·점수 강조. 상세에 "별콩이 —" 없음.
- [ ] 헤더 "불새자리". 콘솔0.
- [ ] (선택) 20명 상한 — 20명 맵 join 시 "가득 찼어".

---

## Self-Review
- **커버리지:** ①1단계+버튼(T4) ②카드 나↔X+X전체·해석(T5·T1) ③지도↔카드 연동(T5) ④순위 리스타일(T4) ⑤별콩이 삭제(T3) ⑥헤더(T4) ⑦최대20(T2). 매핑됨.
- **타입 일관성:** focusSummary(T1) ↔ View 사용(T5). MAX_STAR_MAP_MEMBERS(T2) ↔ join. InyeonDetail pivotIsMe(기존) ↔ 이웃 리스트(T5). detailFor(기존) 재사용.
- **빌드 유지:** T1·T2·T3 독립. T4(구조/리스타일, 카드는 나↔X만) 빌드 OK. T5(카드 ② 추가) T4 위에. 각 tsc green.
- **주의:** T4에서 inspected/breadcrumb/focusPath 완전 제거. T5에서 expandedNeighborId 도입. Canvas onEdgeSelect 는 T5에서 배선(T4까진 미전달/no-op).
