# 정성 이탈조사 설문 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 앱 내 잔존·재방문 유저에게 자유서술 6문항 이탈조사 설문을 받고 10별을 보상으로 지급, 응답을 어드민에서 원문으로 읽는다.

**Architecture:** 새 `survey_responses` 테이블(1인1회 partial UNIQUE) + `/api/survey`(GET 자격 / POST 제출·검증·저장·보상). 문항·검증은 `lib/survey/questions.ts` 단일 원천(순수함수, 서버·클라 공용). 진입은 홈 캐러셀 href + 리딩 결과화면 하단 자기완결형 카드. 별 지급·세션·어드민 가드·noindex는 전부 기존 것 재사용, 새 RPC 없음.

**Tech Stack:** Next.js 16 App Router, TypeScript, Supabase(service_role), `node:test`(유닛), 기존 `chargeStars` RPC 래퍼.

**설계 스펙:** `docs/superpowers/specs/2026-08-09-survey-이탈조사-design.md`

---

## File Structure

| 파일 | 책임 | 신설/수정 |
|---|---|---|
| `supabase/migrations/20260809000000_survey_responses.sql` | 테이블 + partial UNIQUE + 권한 4줄 | 신설 |
| `lib/constants.ts` | `SURVEY_REWARD_STARS=10` 추가 | 수정 |
| `lib/survey/questions.ts` | 문항 상수 + `validateSurveyAnswers` 순수함수 | 신설 |
| `lib/survey/questions.test.ts` | 검증 함수 유닛테스트 | 신설 |
| `app/api/survey/route.ts` | GET(자격) + POST(제출·저장·보상) | 신설 |
| `app/survey/page.tsx` | 설문 폼(로그인 가드·닉네임·6 textarea·글자수 게이트) | 신설 |
| `app/survey/layout.tsx` | noindex 메타데이터 | 신설 |
| `components/survey/SurveyResultCard.tsx` | 결과화면 하단 진입 카드(자기완결) | 신설 |
| `app/tarot/result/page.tsx` · `app/(consultations)/saju/result/page.tsx` · `app/fortune/result/page.tsx` | 카드 1줄 삽입 | 수정 |
| `components/common/HeroCarousel.tsx` | survey 카드 href `#`→`/survey` | 수정 |
| `app/admin/survey/page.tsx` | 응답 원문 리스트 | 신설 |
| `components/admin/AdminNav.tsx` | nav 항목 1줄(모바일은 AdminNav 재사용이라 자동) | 수정 |

⚠️ **Task 1 커밋 후 dev push 필요** — 이후 태스크(3·4·5)의 로컬 검증이 dev DB의 `survey_responses` 테이블에 의존한다(`.env.local`이 dev 리소스를 가리킴, `main` Workflow logs SUCCESS 확인 관례).

---

## Task 1: DB 마이그레이션 (survey_responses)

**Files:**
- Create: `supabase/migrations/20260809000000_survey_responses.sql`

- [ ] **Step 1: 마이그레이션 작성**

`ui_events`(`20260731070000`) 권한 패턴을 그대로 복제한다.

```sql
-- 정성 이탈조사 설문 응답 (2026-08-09)
-- 설계: docs/superpowers/specs/2026-08-09-survey-이탈조사-design.md
-- 잔존·재방문 유저 대상 자유서술 6문항. answers 는 [{q,a}] 배열(질문 텍스트 스냅샷)이라
-- 나중에 문항을 고쳐도 과거 응답 해석이 안 깨진다. ui_events 권한 패턴 복제.

CREATE TABLE IF NOT EXISTS survey_responses (
  id BIGSERIAL PRIMARY KEY,

  -- 🔴 users(id) 참조 FK 는 CASCADE/SET NULL 을 반드시 명시(AGENTS.md).
  --    없으면 회원 탈퇴 users DELETE 가 23503 으로 막힌다. 탈퇴해도 정성 응답은 익명 보존.
  user_id UUID REFERENCES users(id) ON DELETE SET NULL,
  anon_id TEXT,
  answers JSONB NOT NULL,   -- [{ "q": "질문 텍스트", "a": "답변" }, ...]

  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 1인 1회. user_id NULL(탈퇴 익명행)은 중복 허용해야 하므로 partial unique.
CREATE UNIQUE INDEX IF NOT EXISTS survey_responses_user_id_key
  ON survey_responses(user_id) WHERE user_id IS NOT NULL;

-- 어드민 최신순 조회
CREATE INDEX IF NOT EXISTS idx_survey_responses_created
  ON survey_responses(created_at DESC);

-- RLS: service_role 만. 클라는 /api/survey 경유 (ui_events·page_views 관행).
-- 2026-07-29 기본권한 정리로 새 테이블은 RLS 를 깜빡해도 anon 이 못 만지지만 이중 방어.
ALTER TABLE survey_responses ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON TABLE survey_responses FROM PUBLIC, anon, authenticated;
GRANT ALL ON TABLE survey_responses TO service_role;
GRANT USAGE, SELECT ON SEQUENCE survey_responses_id_seq TO service_role;
```

- [ ] **Step 2: 문법 자기검토**

새 SECURITY DEFINER RPC 가 없으므로 함수 REVOKE 3종 규칙은 해당 없음(테이블 권한 4줄만). partial unique(`WHERE user_id IS NOT NULL`)가 `ON DELETE SET NULL` 과 짝이 맞는지 확인 — 탈퇴 시 user_id→NULL 되며 여러 NULL 행이 생길 수 있어 partial 이 필수다.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260809000000_survey_responses.sql
git commit -m "feat(survey): survey_responses 테이블 (1인1회 partial unique·ui_events 권한 패턴)"
```

- [ ] **Step 4: dev 적용** — 이 커밋을 포함해 dev push 하면 Supabase dev 에 자동 적용된다(이후 태스크 로컬 검증의 전제). push 시점은 실행자·사용자 판단(subagent-driven이면 체크포인트에서).

---

## Task 2: 문항·검증 순수함수 + 상수 (TDD)

**Files:**
- Modify: `lib/constants.ts`
- Create: `lib/survey/questions.ts`
- Test: `lib/survey/questions.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/survey/questions.test.ts`:

```ts
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  validateSurveyAnswers,
  SURVEY_QUESTIONS,
  SURVEY_MIN_CHARS,
} from "./questions.ts";

const long = "가".repeat(SURVEY_MIN_CHARS); // 정확히 최소 길이
const six = () => SURVEY_QUESTIONS.map(() => long);

test("6개 전부 최소 글자수 충족 → ok + 문항 텍스트 결합", () => {
  const r = validateSurveyAnswers(six());
  assert.equal(r.ok, true);
  if (r.ok) {
    assert.equal(r.normalized.length, SURVEY_QUESTIONS.length);
    assert.equal(r.normalized[0].q, SURVEY_QUESTIONS[0].text);
    assert.equal(r.normalized[0].a, long);
  }
});

test("개수 부족 → fail(answer_count)", () => {
  assert.deepEqual(validateSurveyAnswers(six().slice(0, 5)), {
    ok: false,
    reason: "answer_count",
  });
});

test("하나라도 최소 미만 → fail(too_short)", () => {
  const a = six();
  a[2] = "가".repeat(SURVEY_MIN_CHARS - 1);
  assert.deepEqual(validateSurveyAnswers(a), { ok: false, reason: "too_short" });
});

test("공백 채움은 trim 후 판정 → fail(too_short)", () => {
  const a = six();
  a[0] = "   " + "가".repeat(10) + "   ";
  assert.deepEqual(validateSurveyAnswers(a), { ok: false, reason: "too_short" });
});

test("배열 아님 → fail(answer_count)", () => {
  assert.deepEqual(validateSurveyAnswers("nope"), { ok: false, reason: "answer_count" });
  assert.deepEqual(validateSurveyAnswers(null), { ok: false, reason: "answer_count" });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

Run: `node --import tsx --test lib/survey/questions.test.ts`
Expected: FAIL — `Cannot find module './questions.ts'` (아직 미작성)

- [ ] **Step 3: 문항 상수 + 검증 함수 구현**

`lib/survey/questions.ts`:

```ts
// 정성 이탈조사 설문 — 문항·검증 단일 원천(서버·클라 공용).
// 문항은 코드 상수(어드민 편집 없음, YAGNI). answers 는 순서대로 string[] 로 받아
// 서버가 문항 텍스트를 결합해 [{q,a}] 로 저장 → 문항을 고쳐도 과거 응답이 온전.
// 설계: docs/superpowers/specs/2026-08-09-survey-이탈조사-design.md

export const SURVEY_MIN_CHARS = 50;

export const SURVEY_QUESTIONS = [
  { id: "motive", text: "요즘 어떤 마음으로 별콩이를 찾아왔어? 무슨 이야기가 제일 궁금했어?" },
  { id: "quality", text: "별콩이랑 얘기해보니 어땠어? 좋았던 것도, 아쉬웠던 것도 솔직하게." },
  { id: "length", text: "별콩이 상담이랑 사주·타로 리포트, 분량은 어땠어? 너무 짧거나 길진 않았어?" },
  { id: "revisit", text: "별콩톡에 뭐가 더 있으면 자주 놀러 올 것 같아?" },
  { id: "payment", text: "별(유료 재화) 충전은 어땠어? 안 했다면 뭐가 망설여졌는지 편하게." },
  { id: "freeform", text: "마지막으로 별콩이한테 하고 싶은 말, 뭐든 적어줘." },
] as const;

export type SurveyAnswer = { q: string; a: string };

type ValidateResult =
  | { ok: true; normalized: SurveyAnswer[] }
  | { ok: false; reason: "answer_count" | "too_short" };

/**
 * 클라가 보낸 답변(문항 순서대로 string[])을 검증하고 [{q,a}] 로 정규화.
 * 6개 전부 + 각 trim 후 SURVEY_MIN_CHARS 이상이어야 통과.
 */
export function validateSurveyAnswers(answers: unknown): ValidateResult {
  if (!Array.isArray(answers) || answers.length !== SURVEY_QUESTIONS.length) {
    return { ok: false, reason: "answer_count" };
  }
  const normalized: SurveyAnswer[] = [];
  for (let i = 0; i < SURVEY_QUESTIONS.length; i++) {
    const a = answers[i];
    if (typeof a !== "string" || a.trim().length < SURVEY_MIN_CHARS) {
      return { ok: false, reason: "too_short" };
    }
    normalized.push({ q: SURVEY_QUESTIONS[i].text, a: a.trim() });
  }
  return { ok: true, normalized };
}
```

- [ ] **Step 4: 상수 추가**

`lib/constants.ts` 끝(`FIRST_CHARGE_BONUS_RATE` 아래)에 추가:

```ts
/** 정성 이탈조사 설문 완료 보상 별 — 1인 1회(survey_responses partial unique).
 * 무료별이 변동원가의 93%라 값은 보수적. 응답률 보고 조정 가능하게 상수로 분리.
 * 설계: docs/superpowers/specs/2026-08-09-survey-이탈조사-design.md */
export const SURVEY_REWARD_STARS = 10;
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

Run: `node --import tsx --test lib/survey/questions.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 6: 커밋**

```bash
git add lib/survey/questions.ts lib/survey/questions.test.ts lib/constants.ts
git commit -m "feat(survey): 문항 상수 + validateSurveyAnswers 검증 + SURVEY_REWARD_STARS"
```

---

## Task 3: 저장 API (`/api/survey`)

**Files:**
- Create: `app/api/survey/route.ts`

- [ ] **Step 1: 라우트 작성**

`/api/event` 관행(service_role INSERT, 세션에서만 id 파생)을 따르되, 무음 204 가 아니라 **JSON 응답**(보상 성공/실패를 클라가 알아야 함)이다.

```ts
// 정성 이탈조사 설문 — GET(참여 자격) + POST(제출·검증·저장·보상).
// 보안: user_id/anon_id 는 서버 세션에서만(클라 위조 무시, /api/event 관행).
// 보상은 저장 성공 후 chargeStars(멱등키 survey:userId — 재호출 안전).

import { NextRequest, NextResponse } from "next/server";
import { getServiceSupabase } from "@/lib/supabase";
import { getSession } from "@/lib/session";
import { validateSurveyAnswers } from "@/lib/survey/questions";
import { chargeStars } from "@/lib/stars";
import { SURVEY_REWARD_STARS } from "@/lib/constants";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

// 참여 자격 = 로그인 && 미참여. 결과화면 카드·설문 페이지가 이걸로 노출/차단 판정.
export async function GET() {
  const { userId } = await getSession();
  if (!userId) return NextResponse.json({ eligible: false, participated: false });
  const supa = getServiceSupabase();
  const { data, error } = await supa
    .from("survey_responses")
    .select("id")
    .eq("user_id", userId)
    .maybeSingle();
  if (error) {
    // 조회 실패 시 자격 없음(카드 숨김) — 계측이 제품 경로를 막지 않는다
    console.error("[/api/survey GET] lookup failed:", error);
    return NextResponse.json({ eligible: false, participated: false });
  }
  const participated = !!data;
  return NextResponse.json({ eligible: !participated, participated });
}

export async function POST(req: NextRequest) {
  const { userId, anonymousId } = await getSession();
  if (!userId) {
    return NextResponse.json({ ok: false, reason: "auth" }, { status: 401 });
  }

  let body: unknown;
  try {
    body = await req.json();
  } catch {
    return NextResponse.json({ ok: false, reason: "bad_json" }, { status: 400 });
  }
  const answers = (body as { answers?: unknown })?.answers;
  const v = validateSurveyAnswers(answers);
  if (!v.ok) {
    return NextResponse.json({ ok: false, reason: v.reason }, { status: 400 });
  }

  const supa = getServiceSupabase();
  const { error: insertError } = await supa.from("survey_responses").insert({
    user_id: userId,
    anon_id: anonymousId ?? null,
    answers: v.normalized,
  });
  if (insertError) {
    // 23505 = partial unique 위반 = 이미 참여(1인 1회)
    if (insertError.code === "23505") {
      return NextResponse.json({ ok: false, reason: "already" }, { status: 409 });
    }
    console.error("[/api/survey POST] insert failed:", insertError);
    return NextResponse.json({ ok: false, reason: "save" }, { status: 500 });
  }

  // 저장 성공 → 보상. 저장은 됐는데 보상이 실패해도(희박) 응답은 성공으로 —
  // 재제출은 409 로 막히므로 여기서 throw 하면 유저가 별 없이 잠긴다. 실패는 로그로.
  const charge = await chargeStars(
    userId,
    SURVEY_REWARD_STARS,
    `survey:${userId}`,
    "survey_reward"
  );
  if (!charge.success) {
    console.error("[/api/survey POST] reward charge failed for", userId);
  }
  return NextResponse.json({
    ok: true,
    reward: SURVEY_REWARD_STARS,
    balance: charge.balance,
  });
}
```

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 0 (기존 코드 기준 클린)

- [ ] **Step 3: 실호출 검증** (dev DB에 Task 1 테이블 적용된 상태)

로컬 dev 서버(`npm run dev`)에서, 로그인 세션 쿠키(`byeolkong_user_id`) 있는 상태로:
- `GET /api/survey` → `{eligible:true, participated:false}` (미참여 시)
- `POST /api/survey` body `{"answers":["<50자>", …6개]}` → `{ok:true, reward:10, balance:<증가>}`
- 같은 유저 재 POST → 409 `{ok:false, reason:"already"}`
- 49자 섞어 POST → 400 `{ok:false, reason:"too_short"}`
- 비로그인 POST → 401

Supabase 에서 `select answers from survey_responses` 로 `[{q,a}]` 저장 확인. `star_transactions` 에 `source='survey_reward'` 행 확인.

- [ ] **Step 4: 커밋**

```bash
git add app/api/survey/route.ts
git commit -m "feat(survey): /api/survey GET(자격)+POST(제출·저장·10별 보상)"
```

---

## Task 4: 설문 페이지 (`/survey`)

**Files:**
- Create: `app/survey/layout.tsx`
- Create: `app/survey/page.tsx`

- [ ] **Step 1: noindex layout**

`app/survey/layout.tsx` — `app/tarot/reading/layout.tsx` 패턴 복제:

```tsx
// 메타데이터 전용 레이아웃 — 렌더 결과는 children 그대로.
import { noindexMetadata } from "@/lib/seo/metadata";

export const metadata = noindexMetadata({
  title: "별콩이의 질문",
  description: "너의 이야기를 들려주면 별콩이가 별을 줄게.",
});

export default function SurveyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
```

- [ ] **Step 2: 설문 폼 페이지**

`app/survey/page.tsx`. 로그인 가드는 `auth/me`(shop 관행), 닉네임도 여기서. 6 textarea + 실시간 글자수 게이트.

```tsx
"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { SURVEY_QUESTIONS, SURVEY_MIN_CHARS } from "@/lib/survey/questions";
import { SURVEY_REWARD_STARS } from "@/lib/constants";

type Phase = "loading" | "form" | "already" | "done";

export default function SurveyPage() {
  const router = useRouter();
  const [phase, setPhase] = useState<Phase>("loading");
  const [nickname, setNickname] = useState("");
  const [answers, setAnswers] = useState<string[]>(() => SURVEY_QUESTIONS.map(() => ""));
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    let cancelled = false;
    void (async () => {
      const me = await fetch("/api/auth/me", { cache: "no-store" })
        .then((r) => r.json())
        .catch(() => null);
      if (cancelled) return;
      if (!me?.isAuthenticated || !me.user?.id) {
        router.replace(`/login?next=${encodeURIComponent("/survey")}`);
        return;
      }
      setNickname(me.user.nickname ?? "");
      const s = await fetch("/api/survey", { cache: "no-store" })
        .then((r) => r.json())
        .catch(() => null);
      if (cancelled) return;
      setPhase(s?.participated ? "already" : "form");
    })();
    return () => {
      cancelled = true;
    };
  }, [router]);

  const allValid = answers.every((a) => a.trim().length >= SURVEY_MIN_CHARS);

  const submit = async () => {
    if (!allValid || submitting) return;
    setSubmitting(true);
    setError(null);
    const r = await fetch("/api/survey", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ answers }),
    })
      .then((res) => res.json())
      .catch(() => null);
    setSubmitting(false);
    if (r?.ok) return setPhase("done");
    if (r?.reason === "already") return setPhase("already");
    setError("제출이 안 됐어. 잠시 후 다시 해줄래?");
  };

  if (phase === "loading") {
    return (
      <main className="flex flex-1 items-center justify-center px-5">
        <p className="text-text-light text-sm">불러오는 중…</p>
      </main>
    );
  }
  if (phase === "already") {
    return (
      <main className="flex flex-1 flex-col items-center justify-center px-5 text-center gap-4">
        <p className="text-[15px] text-eye-purple">이미 이야기 들려줬어. 고마워 🌟</p>
        <Link href="/" className="px-6 py-3 rounded-xl bg-lilac-deep text-white font-bold text-[14px]">
          홈으로
        </Link>
      </main>
    );
  }
  if (phase === "done") {
    return (
      <main className="flex flex-1 flex-col items-center justify-center px-5 text-center gap-4">
        <p className="text-[15px] text-eye-purple leading-relaxed">
          들려줘서 고마워!
          <br />
          별콩별 {SURVEY_REWARD_STARS}개를 넣어줬어 🌟
        </p>
        <Link href="/" className="px-6 py-3 rounded-xl bg-lilac-deep text-white font-bold text-[14px]">
          홈으로
        </Link>
      </main>
    );
  }

  return (
    <main className="flex flex-1 flex-col items-center py-8 w-full animate-fade-in">
      <div className="w-full max-w-md mx-auto px-5">
        <h1 className="text-lg font-bold text-eye-purple mb-2">별콩이의 질문</h1>
        <p className="text-[13px] text-text-light leading-relaxed mb-6">
          {nickname ? `${nickname}아, ` : ""}별콩이는 너 같은 친구들 이야기를 들으면서 자라. 한두 문장이면 충분하니까 편하게 적어줘. 다 적으면 별콩별 {SURVEY_REWARD_STARS}개 🌟
        </p>
        <div className="space-y-5">
          {SURVEY_QUESTIONS.map((q, i) => {
            const len = answers[i].trim().length;
            const ok = len >= SURVEY_MIN_CHARS;
            return (
              <div key={q.id}>
                <label className="block text-[13.5px] font-bold text-eye-purple mb-2">
                  {i + 1}. {q.text}
                </label>
                <textarea
                  value={answers[i]}
                  onChange={(e) =>
                    setAnswers((prev) => prev.map((a, j) => (j === i ? e.target.value : a)))
                  }
                  rows={3}
                  className="w-full rounded-xl border border-lilac-mid/40 bg-cream-warm p-3 text-[13px] text-eye-purple resize-none focus:outline-none focus:border-lilac-deep"
                  placeholder="솔직하게 들려줘"
                />
                <div className={`text-[11px] mt-1 text-right ${ok ? "text-lilac-deep" : "text-text-light/50"}`}>
                  {len} / {SURVEY_MIN_CHARS}자
                </div>
              </div>
            );
          })}
        </div>
        {error && <p className="text-[12px] text-rose-500 mt-4">{error}</p>}
        <button
          onClick={submit}
          disabled={!allValid || submitting}
          className="w-full mt-6 py-3.5 rounded-xl bg-lilac-deep text-white font-bold text-[14px] disabled:opacity-40 disabled:cursor-not-allowed"
        >
          {submitting ? "보내는 중…" : `별콩별 ${SURVEY_REWARD_STARS}개 받기`}
        </button>
      </div>
    </main>
  );
}
```

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: 에러 0

- [ ] **Step 4: 브라우저 검증** (dev preview + 세션 쿠키 주입)

- 비로그인 접근 → `/login?next=/survey` 리다이렉트
- 로그인 상태 → 인트로에 닉네임 노출, 6문항 렌더
- 각 textarea 50자 미만이면 버튼 비활성, 전부 충족 시 활성화·글자수 색 전환
- 제출 → "별콩별 10개를 넣어줬어" 화면
- 이미 참여한 유저 재진입 → "이미 이야기 들려줬어" 화면

- [ ] **Step 5: 커밋**

```bash
git add app/survey/page.tsx app/survey/layout.tsx
git commit -m "feat(survey): /survey 폼 페이지(로그인 가드·닉네임·6문항·50자 게이트)"
```

---

## Task 5: 결과화면 진입 카드 + 캐러셀 href

**Files:**
- Create: `components/survey/SurveyResultCard.tsx`
- Modify: `app/tarot/result/page.tsx`, `app/(consultations)/saju/result/page.tsx`, `app/fortune/result/page.tsx`
- Modify: `components/common/HeroCarousel.tsx`

- [ ] **Step 1: 자기완결 카드 컴포넌트**

`components/survey/SurveyResultCard.tsx` — mount 시 `GET /api/survey` 로 자격 확인, 로그인 && 미참여일 때만 렌더(리딩 완료가 전제라 별도 신규 게이트 불필요).

```tsx
"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { SURVEY_REWARD_STARS } from "@/lib/constants";

// 리딩·상담 완료 결과화면 하단 진입 카드. 서버가 "로그인 && 미참여"면만 노출.
// 비로그인(공유뷰)·참여자는 eligible:false 라 자동 숨김.
export default function SurveyResultCard() {
  const [eligible, setEligible] = useState(false);
  useEffect(() => {
    void fetch("/api/survey", { cache: "no-store" })
      .then((r) => r.json())
      .then((d) => setEligible(!!d?.eligible))
      .catch(() => {});
  }, []);
  if (!eligible) return null;
  return (
    <div className="w-full max-w-md mx-auto px-5 mt-4">
      <Link
        href="/survey"
        className="block rounded-2xl border border-gold/40 bg-gradient-to-br from-gold-soft/25 to-cream-warm p-4 hover:shadow-md transition"
      >
        <div className="text-[13.5px] font-bold text-eye-purple">별콩이가 너한테 궁금한 게 있어 🌟</div>
        <div className="text-[12px] text-text-light mt-1">
          이야기 들려주면 별 {SURVEY_REWARD_STARS}개를 줄게 · 참여하기 →
        </div>
      </Link>
    </div>
  );
}
```

- [ ] **Step 2: 타로 결과화면에 삽입**

`app/tarot/result/page.tsx` — import 추가 후, `ResultUpsell`(L343) 다음 줄에 삽입:

import 블록에 추가:
```tsx
import SurveyResultCard from "@/components/survey/SurveyResultCard";
```

`<ResultUpsell variant="counsel" showBonus={false} />` 바로 아래:
```tsx
      <ResultUpsell variant="counsel" showBonus={false} />

      <SurveyResultCard />
```

- [ ] **Step 3: 사주 결과화면에 삽입**

`app/(consultations)/saju/result/page.tsx` — import 추가 후 `ResultUpsell`(L246) 다음에 `<SurveyResultCard />` 삽입(타로와 동일).

import:
```tsx
import SurveyResultCard from "@/components/survey/SurveyResultCard";
```
삽입:
```tsx
      <ResultUpsell variant="counsel" showBonus={false} />

      <SurveyResultCard />
```

- [ ] **Step 4: 운세 결과화면에 삽입**

`app/fortune/result/page.tsx` — import 추가 후, L549 `{!isPublic && ftType && <ResultUpsell variant={ftType} />}` 다음 줄에 공유뷰 제외 조건으로 삽입:

import:
```tsx
import SurveyResultCard from "@/components/survey/SurveyResultCard";
```
삽입:
```tsx
      {!isPublic && ftType && <ResultUpsell variant={ftType} />}

      {!isPublic && <SurveyResultCard />}
```

- [ ] **Step 5: 캐러셀 href 배선**

`components/common/HeroCarousel.tsx` L23 — survey 카드 href 수정:

```tsx
  { id: "survey", img: "/carousel/survey.webp", title: "별콩톡 설문조사", desc: "내 이야기를 들려주면 별콩별을 줄게", cta: "참여하기", href: "/survey" },
```

(`HeroCarousel` 이 카드 클릭 시 `ui_events` 에 `banner_clicked{slot:"survey"}` 를 기록해 클릭 계측이 잡힘 — 추가 배선 불필요. 2026-08-14 정정: 과거 `?b=survey`(page_views) 서술은 미도달이었다.)

- [ ] **Step 6: 타입 체크 + 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 에러 0, 빌드 성공

- [ ] **Step 7: 브라우저 검증**

- 미참여 유저로 타로/사주/운세 결과화면 진입 → 하단에 설문 카드 노출, 클릭 → `/survey`
- 설문 참여 후 결과화면 재진입 → 카드 사라짐(eligible:false)
- 홈 캐러셀 설문 슬라이드 "참여하기" → `/survey`(URL 에 `?b=survey`)

- [ ] **Step 8: 커밋**

```bash
git add components/survey/SurveyResultCard.tsx app/tarot/result/page.tsx "app/(consultations)/saju/result/page.tsx" app/fortune/result/page.tsx components/common/HeroCarousel.tsx
git commit -m "feat(survey): 결과화면 하단 진입 카드 3곳 + 캐러셀 href 배선"
```

---

## Task 6: 어드민 응답 읽기 화면

**Files:**
- Create: `app/admin/survey/page.tsx`
- Modify: `components/admin/AdminNav.tsx`

- [ ] **Step 1: 어드민 페이지 (inquiries 복제)**

`app/admin/survey/page.tsx`:

```tsx
// app/admin/survey/page.tsx — 정성 이탈조사 응답 원문 읽기
import { getServiceSupabase } from "@/lib/supabase";
import LoadFailed from "@/components/admin/LoadFailed";

export const dynamic = "force-dynamic";

interface Row {
  id: number;
  user_id: string | null;
  answers: { q: string; a: string }[];
  created_at: string;
}

export default async function AdminSurvey() {
  const supabase = getServiceSupabase();
  // 자유서술이라 정량 집계 없음 — 최신순 원문 리스트. 200건 상한(표시용).
  const { data, error } = await supabase
    .from("survey_responses")
    .select("id, user_id, answers, created_at")
    .order("created_at", { ascending: false })
    .limit(200);
  const listFailed = !!error;
  const rows = (data ?? []) as Row[];

  const userIds = [...new Set(rows.map((r) => r.user_id).filter((v): v is string => !!v))];
  const nameById = new Map<string, string | null>();
  let namesFailed = false;
  if (userIds.length > 0) {
    const { data: users, error: usersError } = await supabase
      .from("users")
      .select("id, nickname")
      .in("id", userIds);
    namesFailed = !!usersError;
    for (const u of users ?? []) nameById.set(u.id, u.nickname);
  }

  return (
    <div className="space-y-4">
      <h1 className="text-xl font-bold">이탈 설문</h1>
      <p className="text-white/50 text-xs">최신 200건 · 자유서술 응답 원문</p>
      {namesFailed && <LoadFailed block="작성자 닉네임(users)" />}
      {listFailed ? (
        <LoadFailed block="설문 응답(survey_responses)" />
      ) : rows.length === 0 ? (
        <p className="text-white/40 py-6 text-center">아직 응답이 없습니다.</p>
      ) : (
        <div className="space-y-4">
          {rows.map((r) => (
            <div key={r.id} className="rounded-lg border border-white/10 bg-white/[0.03] p-4">
              <div className="flex items-center justify-between text-[12px] text-white/50 mb-3">
                <span>{r.user_id ? nameById.get(r.user_id) ?? "—" : "(탈퇴)"}</span>
                <span>{new Date(r.created_at).toLocaleString("ko-KR", { timeZone: "Asia/Seoul" })}</span>
              </div>
              <div className="space-y-2.5">
                {(r.answers ?? []).map((qa, i) => (
                  <div key={i}>
                    <div className="text-[12px] font-bold text-white/70">{qa.q}</div>
                    <div className="text-[13px] text-white/90 whitespace-pre-wrap mt-0.5">{qa.a}</div>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: nav 항목 추가**

`components/admin/AdminNav.tsx` — `GROUPS` 의 `ops` 그룹 `items` 마지막(`fortune-refunds` 다음)에 한 줄 추가. `AdminMobileNav` 는 `AdminNav` 를 재사용하므로 자동 반영.

```tsx
    { href: "/admin/fortune-refunds", label: "운세 환불", emoji: "🎁" },
    { href: "/admin/survey", label: "이탈 설문", emoji: "📝" },
  ] },
```

- [ ] **Step 3: 타입 체크 + 빌드**

Run: `npx tsc --noEmit && npm run build`
Expected: 에러 0, 빌드 성공. 라우트 표에 `/admin/survey` 등장

- [ ] **Step 4: 어드민 검증** (로컬: 임시 `ADMIN_USER_IDS` + HMAC 쿠키 주입, 검증 후 원복 — memory `admin-local-verify-trick`)

- `/admin/survey` 진입 → 응답 카드 리스트(닉네임·시각·6 Q/A 원문)
- 토큰 없이 진입 → `/` 리다이렉트(proxy 가드)
- nav 사이드바 "운영·고객" 그룹에 "📝 이탈 설문" 노출

- [ ] **Step 5: 커밋**

```bash
git add app/admin/survey/page.tsx components/admin/AdminNav.tsx
git commit -m "feat(survey): 어드민 응답 원문 읽기 화면 + nav 항목"
```

---

## 최종 검증

- [ ] **전체 유닛테스트**: `node --import tsx --test lib/survey/questions.test.ts` (+ 기존 테스트 무회귀: `node --import tsx --test "lib/**/*.test.ts"`)
- [ ] **타입·빌드**: `npx tsc --noEmit && npm run build` → 에러 0
- [ ] **엔드투엔드 스모크**: 캐러셀/결과카드 → `/survey` → 6문항 제출 → 10별 지급 → 재진입 시 "이미 참여" → 어드민에서 응답 원문 확인
- [ ] **dev push** (마이그레이션 dev 적용 + dev 배포). 🔴 prod(main) 아님 — dev 누적분과 함께 로드맵 ④에서 한 판.

## 회귀·주의

- `star_transactions.source='survey_reward'` 는 자유문자열이라 CHECK 변경 없음. 무상 지급 집계에서 제외할지는 선택(어드민 별지급 집계 `20260731050000` 의 무상 목록).
- 보상은 저장 성공 후 지급 — 저장 실패 시 미지급(정상). 저장 성공·보상 실패(희박)는 로그로만 남기고 응답은 성공(재제출이 409 로 막혀 유저가 별 없이 잠기는 것 방지).
- 마이그레이션은 in-place 수정 금지(신규 파일). dev→main 머지 시 브랜치 통째 push(cherry-pick 금지).
